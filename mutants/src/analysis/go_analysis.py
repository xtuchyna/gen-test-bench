
from inspect import signature as _mutmut_signature

def _mutmut_trampoline(orig, mutants, *args, **kwargs):
    import os
    mutant_under_test = os.environ['MUTANT_UNDER_TEST']
    if mutant_under_test == 'fail':
        from mutmut.__main__ import MutmutProgrammaticFailException
        raise MutmutProgrammaticFailException('Failed programmatically')      
    elif mutant_under_test == 'stats':
        from mutmut.__main__ import record_trampoline_hit
        record_trampoline_hit(orig.__module__ + '.' + orig.__name__)
        result = orig(*args, **kwargs)
        return result  # for the yield case
    prefix = orig.__module__ + '.' + orig.__name__ + '__mutmut_'
    if not mutant_under_test.startswith(prefix):
        result = orig(*args, **kwargs)
        return result  # for the yield case
    mutant_name = mutant_under_test.rpartition('.')[-1]
    result = mutants[mutant_name](*args, **kwargs)
    return result


from inspect import signature as _mutmut_signature

def _mutmut_yield_from_trampoline(orig, mutants, *args, **kwargs):
    import os
    mutant_under_test = os.environ['MUTANT_UNDER_TEST']
    if mutant_under_test == 'fail':
        from mutmut.__main__ import MutmutProgrammaticFailException
        raise MutmutProgrammaticFailException('Failed programmatically')      
    elif mutant_under_test == 'stats':
        from mutmut.__main__ import record_trampoline_hit
        record_trampoline_hit(orig.__module__ + '.' + orig.__name__)
        result = yield from orig(*args, **kwargs)
        return result  # for the yield case
    prefix = orig.__module__ + '.' + orig.__name__ + '__mutmut_'
    if not mutant_under_test.startswith(prefix):
        result = yield from orig(*args, **kwargs)
        return result  # for the yield case
    mutant_name = mutant_under_test.rpartition('.')[-1]
    result = yield from mutants[mutant_name](*args, **kwargs)
    return result


import json
import shutil
import os
import tempfile
import subprocess
import re
from collections import Counter

from src.analysis.go_assertion_ratios import assertions_density_go, assertions_mccabe_ratio_go
from src.analysis.python_validation import CompileStatus
from src.stats.go_code_quality import run_golangci


def x_run_command__mutmut_orig(command, cwd=None):
    try:
        result = subprocess.run(command, shell=True, check=True, stdout=subprocess.PIPE, stderr=subprocess.PIPE, cwd=cwd)
        return result.stdout.decode('utf-8'), result.stderr.decode('utf-8'), result.returncode
    except subprocess.CalledProcessError as e:
        return e.stdout.decode('utf-8'), e.stderr.decode('utf-8'), e.returncode


def x_run_command__mutmut_1(command, cwd=None):
    try:
        result = subprocess.run(None, shell=True, check=True, stdout=subprocess.PIPE, stderr=subprocess.PIPE, cwd=cwd)
        return result.stdout.decode('utf-8'), result.stderr.decode('utf-8'), result.returncode
    except subprocess.CalledProcessError as e:
        return e.stdout.decode('utf-8'), e.stderr.decode('utf-8'), e.returncode


def x_run_command__mutmut_2(command, cwd=None):
    try:
        result = subprocess.run(command, shell=False, check=True, stdout=subprocess.PIPE, stderr=subprocess.PIPE, cwd=cwd)
        return result.stdout.decode('utf-8'), result.stderr.decode('utf-8'), result.returncode
    except subprocess.CalledProcessError as e:
        return e.stdout.decode('utf-8'), e.stderr.decode('utf-8'), e.returncode


def x_run_command__mutmut_3(command, cwd=None):
    try:
        result = subprocess.run(command, shell=True, check=False, stdout=subprocess.PIPE, stderr=subprocess.PIPE, cwd=cwd)
        return result.stdout.decode('utf-8'), result.stderr.decode('utf-8'), result.returncode
    except subprocess.CalledProcessError as e:
        return e.stdout.decode('utf-8'), e.stderr.decode('utf-8'), e.returncode


def x_run_command__mutmut_4(command, cwd=None):
    try:
        result = subprocess.run(command, shell=True, check=True, stdout=subprocess.PIPE, stderr=subprocess.PIPE, cwd=None)
        return result.stdout.decode('utf-8'), result.stderr.decode('utf-8'), result.returncode
    except subprocess.CalledProcessError as e:
        return e.stdout.decode('utf-8'), e.stderr.decode('utf-8'), e.returncode


def x_run_command__mutmut_5(command, cwd=None):
    try:
        result = subprocess.run( shell=True, check=True, stdout=subprocess.PIPE, stderr=subprocess.PIPE, cwd=cwd)
        return result.stdout.decode('utf-8'), result.stderr.decode('utf-8'), result.returncode
    except subprocess.CalledProcessError as e:
        return e.stdout.decode('utf-8'), e.stderr.decode('utf-8'), e.returncode


def x_run_command__mutmut_6(command, cwd=None):
    try:
        result = subprocess.run(command, check=True, stdout=subprocess.PIPE, stderr=subprocess.PIPE, cwd=cwd)
        return result.stdout.decode('utf-8'), result.stderr.decode('utf-8'), result.returncode
    except subprocess.CalledProcessError as e:
        return e.stdout.decode('utf-8'), e.stderr.decode('utf-8'), e.returncode


def x_run_command__mutmut_7(command, cwd=None):
    try:
        result = subprocess.run(command, shell=True, stdout=subprocess.PIPE, stderr=subprocess.PIPE, cwd=cwd)
        return result.stdout.decode('utf-8'), result.stderr.decode('utf-8'), result.returncode
    except subprocess.CalledProcessError as e:
        return e.stdout.decode('utf-8'), e.stderr.decode('utf-8'), e.returncode


def x_run_command__mutmut_8(command, cwd=None):
    try:
        result = subprocess.run(command, shell=True, check=True, stderr=subprocess.PIPE, cwd=cwd)
        return result.stdout.decode('utf-8'), result.stderr.decode('utf-8'), result.returncode
    except subprocess.CalledProcessError as e:
        return e.stdout.decode('utf-8'), e.stderr.decode('utf-8'), e.returncode


def x_run_command__mutmut_9(command, cwd=None):
    try:
        result = subprocess.run(command, shell=True, check=True, stdout=subprocess.PIPE, cwd=cwd)
        return result.stdout.decode('utf-8'), result.stderr.decode('utf-8'), result.returncode
    except subprocess.CalledProcessError as e:
        return e.stdout.decode('utf-8'), e.stderr.decode('utf-8'), e.returncode


def x_run_command__mutmut_10(command, cwd=None):
    try:
        result = subprocess.run(command, shell=True, check=True, stdout=subprocess.PIPE, stderr=subprocess.PIPE,)
        return result.stdout.decode('utf-8'), result.stderr.decode('utf-8'), result.returncode
    except subprocess.CalledProcessError as e:
        return e.stdout.decode('utf-8'), e.stderr.decode('utf-8'), e.returncode


def x_run_command__mutmut_11(command, cwd=None):
    try:
        result = None
        return result.stdout.decode('utf-8'), result.stderr.decode('utf-8'), result.returncode
    except subprocess.CalledProcessError as e:
        return e.stdout.decode('utf-8'), e.stderr.decode('utf-8'), e.returncode


def x_run_command__mutmut_12(command, cwd=None):
    try:
        result = subprocess.run(command, shell=True, check=True, stdout=subprocess.PIPE, stderr=subprocess.PIPE, cwd=cwd)
        return result.stdout.decode('XXutf-8XX'), result.stderr.decode('utf-8'), result.returncode
    except subprocess.CalledProcessError as e:
        return e.stdout.decode('utf-8'), e.stderr.decode('utf-8'), e.returncode


def x_run_command__mutmut_13(command, cwd=None):
    try:
        result = subprocess.run(command, shell=True, check=True, stdout=subprocess.PIPE, stderr=subprocess.PIPE, cwd=cwd)
        return result.stdout.decode('utf-8'), result.stderr.decode('XXutf-8XX'), result.returncode
    except subprocess.CalledProcessError as e:
        return e.stdout.decode('utf-8'), e.stderr.decode('utf-8'), e.returncode


def x_run_command__mutmut_14(command, cwd=None):
    try:
        result = subprocess.run(command, shell=True, check=True, stdout=subprocess.PIPE, stderr=subprocess.PIPE, cwd=cwd)
        return result.stdout.decode('utf-8'), result.stderr.decode('utf-8'), result.returncode
    except subprocess.CalledProcessError as e:
        return e.stdout.decode('XXutf-8XX'), e.stderr.decode('utf-8'), e.returncode


def x_run_command__mutmut_15(command, cwd=None):
    try:
        result = subprocess.run(command, shell=True, check=True, stdout=subprocess.PIPE, stderr=subprocess.PIPE, cwd=cwd)
        return result.stdout.decode('utf-8'), result.stderr.decode('utf-8'), result.returncode
    except subprocess.CalledProcessError as e:
        return e.stdout.decode('utf-8'), e.stderr.decode('XXutf-8XX'), e.returncode

x_run_command__mutmut_mutants = {
'x_run_command__mutmut_1': x_run_command__mutmut_1, 
    'x_run_command__mutmut_2': x_run_command__mutmut_2, 
    'x_run_command__mutmut_3': x_run_command__mutmut_3, 
    'x_run_command__mutmut_4': x_run_command__mutmut_4, 
    'x_run_command__mutmut_5': x_run_command__mutmut_5, 
    'x_run_command__mutmut_6': x_run_command__mutmut_6, 
    'x_run_command__mutmut_7': x_run_command__mutmut_7, 
    'x_run_command__mutmut_8': x_run_command__mutmut_8, 
    'x_run_command__mutmut_9': x_run_command__mutmut_9, 
    'x_run_command__mutmut_10': x_run_command__mutmut_10, 
    'x_run_command__mutmut_11': x_run_command__mutmut_11, 
    'x_run_command__mutmut_12': x_run_command__mutmut_12, 
    'x_run_command__mutmut_13': x_run_command__mutmut_13, 
    'x_run_command__mutmut_14': x_run_command__mutmut_14, 
    'x_run_command__mutmut_15': x_run_command__mutmut_15
}

def run_command(*args, **kwargs):
    result = _mutmut_trampoline(x_run_command__mutmut_orig, x_run_command__mutmut_mutants, *args, **kwargs)
    return result 

run_command.__signature__ = _mutmut_signature(x_run_command__mutmut_orig)
x_run_command__mutmut_orig.__name__ = 'x_run_command'




def x_check_syntax__mutmut_orig(file):
    try:
        # Run 'go build' to validate syntax (this does not produce a binary)
        print(os.getcwd())
        print("running go build for file: ", file)
        result = subprocess.run(['go', 'build', file],
                                stdout=subprocess.PIPE, stderr=subprocess.PIPE)

        if result.returncode == 0:
            print("Go code is valid.")
            return CompileStatus.OK
        else:
            print("Go code has syntax errors:")
            print(result.stderr.decode())
            return CompileStatus.SYNTAX_ERROR
    except Exception as e:
        print("Exception occurred during validation: ", e)
        return CompileStatus.EXCEPTION_OCCURRED


def x_check_syntax__mutmut_1(file):
    try:
        # Run 'go build' to validate syntax (this does not produce a binary)
        print(os.getcwd())
        print("XXrunning go build for file: XX", file)
        result = subprocess.run(['go', 'build', file],
                                stdout=subprocess.PIPE, stderr=subprocess.PIPE)

        if result.returncode == 0:
            print("Go code is valid.")
            return CompileStatus.OK
        else:
            print("Go code has syntax errors:")
            print(result.stderr.decode())
            return CompileStatus.SYNTAX_ERROR
    except Exception as e:
        print("Exception occurred during validation: ", e)
        return CompileStatus.EXCEPTION_OCCURRED


def x_check_syntax__mutmut_2(file):
    try:
        # Run 'go build' to validate syntax (this does not produce a binary)
        print(os.getcwd())
        print("running go build for file: ", None)
        result = subprocess.run(['go', 'build', file],
                                stdout=subprocess.PIPE, stderr=subprocess.PIPE)

        if result.returncode == 0:
            print("Go code is valid.")
            return CompileStatus.OK
        else:
            print("Go code has syntax errors:")
            print(result.stderr.decode())
            return CompileStatus.SYNTAX_ERROR
    except Exception as e:
        print("Exception occurred during validation: ", e)
        return CompileStatus.EXCEPTION_OCCURRED


def x_check_syntax__mutmut_3(file):
    try:
        # Run 'go build' to validate syntax (this does not produce a binary)
        print(os.getcwd())
        print("running go build for file: ",)
        result = subprocess.run(['go', 'build', file],
                                stdout=subprocess.PIPE, stderr=subprocess.PIPE)

        if result.returncode == 0:
            print("Go code is valid.")
            return CompileStatus.OK
        else:
            print("Go code has syntax errors:")
            print(result.stderr.decode())
            return CompileStatus.SYNTAX_ERROR
    except Exception as e:
        print("Exception occurred during validation: ", e)
        return CompileStatus.EXCEPTION_OCCURRED


def x_check_syntax__mutmut_4(file):
    try:
        # Run 'go build' to validate syntax (this does not produce a binary)
        print(os.getcwd())
        print("running go build for file: ", file)
        result = subprocess.run(['XXgoXX', 'build', file],
                                stdout=subprocess.PIPE, stderr=subprocess.PIPE)

        if result.returncode == 0:
            print("Go code is valid.")
            return CompileStatus.OK
        else:
            print("Go code has syntax errors:")
            print(result.stderr.decode())
            return CompileStatus.SYNTAX_ERROR
    except Exception as e:
        print("Exception occurred during validation: ", e)
        return CompileStatus.EXCEPTION_OCCURRED


def x_check_syntax__mutmut_5(file):
    try:
        # Run 'go build' to validate syntax (this does not produce a binary)
        print(os.getcwd())
        print("running go build for file: ", file)
        result = subprocess.run(['go', 'XXbuildXX', file],
                                stdout=subprocess.PIPE, stderr=subprocess.PIPE)

        if result.returncode == 0:
            print("Go code is valid.")
            return CompileStatus.OK
        else:
            print("Go code has syntax errors:")
            print(result.stderr.decode())
            return CompileStatus.SYNTAX_ERROR
    except Exception as e:
        print("Exception occurred during validation: ", e)
        return CompileStatus.EXCEPTION_OCCURRED


def x_check_syntax__mutmut_6(file):
    try:
        # Run 'go build' to validate syntax (this does not produce a binary)
        print(os.getcwd())
        print("running go build for file: ", file)
        result = subprocess.run(['go', 'build', file], stderr=subprocess.PIPE)

        if result.returncode == 0:
            print("Go code is valid.")
            return CompileStatus.OK
        else:
            print("Go code has syntax errors:")
            print(result.stderr.decode())
            return CompileStatus.SYNTAX_ERROR
    except Exception as e:
        print("Exception occurred during validation: ", e)
        return CompileStatus.EXCEPTION_OCCURRED


def x_check_syntax__mutmut_7(file):
    try:
        # Run 'go build' to validate syntax (this does not produce a binary)
        print(os.getcwd())
        print("running go build for file: ", file)
        result = subprocess.run(['go', 'build', file],
                                stdout=subprocess.PIPE,)

        if result.returncode == 0:
            print("Go code is valid.")
            return CompileStatus.OK
        else:
            print("Go code has syntax errors:")
            print(result.stderr.decode())
            return CompileStatus.SYNTAX_ERROR
    except Exception as e:
        print("Exception occurred during validation: ", e)
        return CompileStatus.EXCEPTION_OCCURRED


def x_check_syntax__mutmut_8(file):
    try:
        # Run 'go build' to validate syntax (this does not produce a binary)
        print(os.getcwd())
        print("running go build for file: ", file)
        result = None

        if result.returncode == 0:
            print("Go code is valid.")
            return CompileStatus.OK
        else:
            print("Go code has syntax errors:")
            print(result.stderr.decode())
            return CompileStatus.SYNTAX_ERROR
    except Exception as e:
        print("Exception occurred during validation: ", e)
        return CompileStatus.EXCEPTION_OCCURRED


def x_check_syntax__mutmut_9(file):
    try:
        # Run 'go build' to validate syntax (this does not produce a binary)
        print(os.getcwd())
        print("running go build for file: ", file)
        result = subprocess.run(['go', 'build', file],
                                stdout=subprocess.PIPE, stderr=subprocess.PIPE)

        if result.returncode != 0:
            print("Go code is valid.")
            return CompileStatus.OK
        else:
            print("Go code has syntax errors:")
            print(result.stderr.decode())
            return CompileStatus.SYNTAX_ERROR
    except Exception as e:
        print("Exception occurred during validation: ", e)
        return CompileStatus.EXCEPTION_OCCURRED


def x_check_syntax__mutmut_10(file):
    try:
        # Run 'go build' to validate syntax (this does not produce a binary)
        print(os.getcwd())
        print("running go build for file: ", file)
        result = subprocess.run(['go', 'build', file],
                                stdout=subprocess.PIPE, stderr=subprocess.PIPE)

        if result.returncode == 1:
            print("Go code is valid.")
            return CompileStatus.OK
        else:
            print("Go code has syntax errors:")
            print(result.stderr.decode())
            return CompileStatus.SYNTAX_ERROR
    except Exception as e:
        print("Exception occurred during validation: ", e)
        return CompileStatus.EXCEPTION_OCCURRED


def x_check_syntax__mutmut_11(file):
    try:
        # Run 'go build' to validate syntax (this does not produce a binary)
        print(os.getcwd())
        print("running go build for file: ", file)
        result = subprocess.run(['go', 'build', file],
                                stdout=subprocess.PIPE, stderr=subprocess.PIPE)

        if result.returncode == 0:
            print("XXGo code is valid.XX")
            return CompileStatus.OK
        else:
            print("Go code has syntax errors:")
            print(result.stderr.decode())
            return CompileStatus.SYNTAX_ERROR
    except Exception as e:
        print("Exception occurred during validation: ", e)
        return CompileStatus.EXCEPTION_OCCURRED


def x_check_syntax__mutmut_12(file):
    try:
        # Run 'go build' to validate syntax (this does not produce a binary)
        print(os.getcwd())
        print("running go build for file: ", file)
        result = subprocess.run(['go', 'build', file],
                                stdout=subprocess.PIPE, stderr=subprocess.PIPE)

        if result.returncode == 0:
            print("Go code is valid.")
            return CompileStatus.OK
        else:
            print("XXGo code has syntax errors:XX")
            print(result.stderr.decode())
            return CompileStatus.SYNTAX_ERROR
    except Exception as e:
        print("Exception occurred during validation: ", e)
        return CompileStatus.EXCEPTION_OCCURRED


def x_check_syntax__mutmut_13(file):
    try:
        # Run 'go build' to validate syntax (this does not produce a binary)
        print(os.getcwd())
        print("running go build for file: ", file)
        result = subprocess.run(['go', 'build', file],
                                stdout=subprocess.PIPE, stderr=subprocess.PIPE)

        if result.returncode == 0:
            print("Go code is valid.")
            return CompileStatus.OK
        else:
            print("Go code has syntax errors:")
            print(result.stderr.decode())
            return CompileStatus.SYNTAX_ERROR
    except Exception as e:
        print("XXException occurred during validation: XX", e)
        return CompileStatus.EXCEPTION_OCCURRED


def x_check_syntax__mutmut_14(file):
    try:
        # Run 'go build' to validate syntax (this does not produce a binary)
        print(os.getcwd())
        print("running go build for file: ", file)
        result = subprocess.run(['go', 'build', file],
                                stdout=subprocess.PIPE, stderr=subprocess.PIPE)

        if result.returncode == 0:
            print("Go code is valid.")
            return CompileStatus.OK
        else:
            print("Go code has syntax errors:")
            print(result.stderr.decode())
            return CompileStatus.SYNTAX_ERROR
    except Exception as e:
        print("Exception occurred during validation: ", None)
        return CompileStatus.EXCEPTION_OCCURRED


def x_check_syntax__mutmut_15(file):
    try:
        # Run 'go build' to validate syntax (this does not produce a binary)
        print(os.getcwd())
        print("running go build for file: ", file)
        result = subprocess.run(['go', 'build', file],
                                stdout=subprocess.PIPE, stderr=subprocess.PIPE)

        if result.returncode == 0:
            print("Go code is valid.")
            return CompileStatus.OK
        else:
            print("Go code has syntax errors:")
            print(result.stderr.decode())
            return CompileStatus.SYNTAX_ERROR
    except Exception as e:
        print("Exception occurred during validation: ",)
        return CompileStatus.EXCEPTION_OCCURRED

x_check_syntax__mutmut_mutants = {
'x_check_syntax__mutmut_1': x_check_syntax__mutmut_1, 
    'x_check_syntax__mutmut_2': x_check_syntax__mutmut_2, 
    'x_check_syntax__mutmut_3': x_check_syntax__mutmut_3, 
    'x_check_syntax__mutmut_4': x_check_syntax__mutmut_4, 
    'x_check_syntax__mutmut_5': x_check_syntax__mutmut_5, 
    'x_check_syntax__mutmut_6': x_check_syntax__mutmut_6, 
    'x_check_syntax__mutmut_7': x_check_syntax__mutmut_7, 
    'x_check_syntax__mutmut_8': x_check_syntax__mutmut_8, 
    'x_check_syntax__mutmut_9': x_check_syntax__mutmut_9, 
    'x_check_syntax__mutmut_10': x_check_syntax__mutmut_10, 
    'x_check_syntax__mutmut_11': x_check_syntax__mutmut_11, 
    'x_check_syntax__mutmut_12': x_check_syntax__mutmut_12, 
    'x_check_syntax__mutmut_13': x_check_syntax__mutmut_13, 
    'x_check_syntax__mutmut_14': x_check_syntax__mutmut_14, 
    'x_check_syntax__mutmut_15': x_check_syntax__mutmut_15
}

def check_syntax(*args, **kwargs):
    result = _mutmut_trampoline(x_check_syntax__mutmut_orig, x_check_syntax__mutmut_mutants, *args, **kwargs)
    return result 

check_syntax.__signature__ = _mutmut_signature(x_check_syntax__mutmut_orig)
x_check_syntax__mutmut_orig.__name__ = 'x_check_syntax'




def x_check_for_warnings__mutmut_orig(project_dir, tests=True):
    try:
        vet_cmd = "go vet ./..."

        print("Running go vet...")
        # Run the go vet command and capture output
        process = subprocess.Popen(
            vet_cmd,
            shell=True,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            text=True  # Ensures the output is in string format
        )
        stdout, stderr = process.communicate()

        output = stderr.strip()

        # Check the return code to determine if go vet succeeded
        success = process.returncode == 0

        # Parse the warnings from the output
        warnings = []
        pattern = r'^(?:vet: )?(.*):(\d+):(\d+): (.*)$'

        print(output)
        for line in output.splitlines():
            match = re.match(pattern, line)
            if match:
                file_path, line_num, col_num, message = match.groups()
                if tests and "test.go" in file_path:
                    warning = {
                        'file': file_path.strip(),
                        'line': int(line_num),
                        'column': int(col_num),
                        'message': message.strip()
                    }
                    warnings.append(warning)

        # Analyze the types of warnings
        warning_messages = [warning['message'] for warning in warnings]
        warning_counts = Counter(warning_messages)

        # Print the analysis of warning types
        print("\nWarning Types and Counts:")
        for message, count in warning_counts.items():
            print(f"{message}: {count} occurrence(s)")

        # Optionally, return the success status and the list of warnings
        return CompileStatus.OK if success else CompileStatus.SYNTAX_ERROR, warnings
    except:
        return CompileStatus.EXCEPTION_OCCURRED, None


def x_check_for_warnings__mutmut_1(project_dir, tests=False):
    try:
        vet_cmd = "go vet ./..."

        print("Running go vet...")
        # Run the go vet command and capture output
        process = subprocess.Popen(
            vet_cmd,
            shell=True,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            text=True  # Ensures the output is in string format
        )
        stdout, stderr = process.communicate()

        output = stderr.strip()

        # Check the return code to determine if go vet succeeded
        success = process.returncode == 0

        # Parse the warnings from the output
        warnings = []
        pattern = r'^(?:vet: )?(.*):(\d+):(\d+): (.*)$'

        print(output)
        for line in output.splitlines():
            match = re.match(pattern, line)
            if match:
                file_path, line_num, col_num, message = match.groups()
                if tests and "test.go" in file_path:
                    warning = {
                        'file': file_path.strip(),
                        'line': int(line_num),
                        'column': int(col_num),
                        'message': message.strip()
                    }
                    warnings.append(warning)

        # Analyze the types of warnings
        warning_messages = [warning['message'] for warning in warnings]
        warning_counts = Counter(warning_messages)

        # Print the analysis of warning types
        print("\nWarning Types and Counts:")
        for message, count in warning_counts.items():
            print(f"{message}: {count} occurrence(s)")

        # Optionally, return the success status and the list of warnings
        return CompileStatus.OK if success else CompileStatus.SYNTAX_ERROR, warnings
    except:
        return CompileStatus.EXCEPTION_OCCURRED, None


def x_check_for_warnings__mutmut_2(project_dir, tests=True):
    try:
        vet_cmd = "XXgo vet ./...XX"

        print("Running go vet...")
        # Run the go vet command and capture output
        process = subprocess.Popen(
            vet_cmd,
            shell=True,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            text=True  # Ensures the output is in string format
        )
        stdout, stderr = process.communicate()

        output = stderr.strip()

        # Check the return code to determine if go vet succeeded
        success = process.returncode == 0

        # Parse the warnings from the output
        warnings = []
        pattern = r'^(?:vet: )?(.*):(\d+):(\d+): (.*)$'

        print(output)
        for line in output.splitlines():
            match = re.match(pattern, line)
            if match:
                file_path, line_num, col_num, message = match.groups()
                if tests and "test.go" in file_path:
                    warning = {
                        'file': file_path.strip(),
                        'line': int(line_num),
                        'column': int(col_num),
                        'message': message.strip()
                    }
                    warnings.append(warning)

        # Analyze the types of warnings
        warning_messages = [warning['message'] for warning in warnings]
        warning_counts = Counter(warning_messages)

        # Print the analysis of warning types
        print("\nWarning Types and Counts:")
        for message, count in warning_counts.items():
            print(f"{message}: {count} occurrence(s)")

        # Optionally, return the success status and the list of warnings
        return CompileStatus.OK if success else CompileStatus.SYNTAX_ERROR, warnings
    except:
        return CompileStatus.EXCEPTION_OCCURRED, None


def x_check_for_warnings__mutmut_3(project_dir, tests=True):
    try:
        vet_cmd = None

        print("Running go vet...")
        # Run the go vet command and capture output
        process = subprocess.Popen(
            vet_cmd,
            shell=True,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            text=True  # Ensures the output is in string format
        )
        stdout, stderr = process.communicate()

        output = stderr.strip()

        # Check the return code to determine if go vet succeeded
        success = process.returncode == 0

        # Parse the warnings from the output
        warnings = []
        pattern = r'^(?:vet: )?(.*):(\d+):(\d+): (.*)$'

        print(output)
        for line in output.splitlines():
            match = re.match(pattern, line)
            if match:
                file_path, line_num, col_num, message = match.groups()
                if tests and "test.go" in file_path:
                    warning = {
                        'file': file_path.strip(),
                        'line': int(line_num),
                        'column': int(col_num),
                        'message': message.strip()
                    }
                    warnings.append(warning)

        # Analyze the types of warnings
        warning_messages = [warning['message'] for warning in warnings]
        warning_counts = Counter(warning_messages)

        # Print the analysis of warning types
        print("\nWarning Types and Counts:")
        for message, count in warning_counts.items():
            print(f"{message}: {count} occurrence(s)")

        # Optionally, return the success status and the list of warnings
        return CompileStatus.OK if success else CompileStatus.SYNTAX_ERROR, warnings
    except:
        return CompileStatus.EXCEPTION_OCCURRED, None


def x_check_for_warnings__mutmut_4(project_dir, tests=True):
    try:
        vet_cmd = "go vet ./..."

        print("XXRunning go vet...XX")
        # Run the go vet command and capture output
        process = subprocess.Popen(
            vet_cmd,
            shell=True,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            text=True  # Ensures the output is in string format
        )
        stdout, stderr = process.communicate()

        output = stderr.strip()

        # Check the return code to determine if go vet succeeded
        success = process.returncode == 0

        # Parse the warnings from the output
        warnings = []
        pattern = r'^(?:vet: )?(.*):(\d+):(\d+): (.*)$'

        print(output)
        for line in output.splitlines():
            match = re.match(pattern, line)
            if match:
                file_path, line_num, col_num, message = match.groups()
                if tests and "test.go" in file_path:
                    warning = {
                        'file': file_path.strip(),
                        'line': int(line_num),
                        'column': int(col_num),
                        'message': message.strip()
                    }
                    warnings.append(warning)

        # Analyze the types of warnings
        warning_messages = [warning['message'] for warning in warnings]
        warning_counts = Counter(warning_messages)

        # Print the analysis of warning types
        print("\nWarning Types and Counts:")
        for message, count in warning_counts.items():
            print(f"{message}: {count} occurrence(s)")

        # Optionally, return the success status and the list of warnings
        return CompileStatus.OK if success else CompileStatus.SYNTAX_ERROR, warnings
    except:
        return CompileStatus.EXCEPTION_OCCURRED, None


def x_check_for_warnings__mutmut_5(project_dir, tests=True):
    try:
        vet_cmd = "go vet ./..."

        print("Running go vet...")
        # Run the go vet command and capture output
        process = subprocess.Popen(
            None,
            shell=True,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            text=True  # Ensures the output is in string format
        )
        stdout, stderr = process.communicate()

        output = stderr.strip()

        # Check the return code to determine if go vet succeeded
        success = process.returncode == 0

        # Parse the warnings from the output
        warnings = []
        pattern = r'^(?:vet: )?(.*):(\d+):(\d+): (.*)$'

        print(output)
        for line in output.splitlines():
            match = re.match(pattern, line)
            if match:
                file_path, line_num, col_num, message = match.groups()
                if tests and "test.go" in file_path:
                    warning = {
                        'file': file_path.strip(),
                        'line': int(line_num),
                        'column': int(col_num),
                        'message': message.strip()
                    }
                    warnings.append(warning)

        # Analyze the types of warnings
        warning_messages = [warning['message'] for warning in warnings]
        warning_counts = Counter(warning_messages)

        # Print the analysis of warning types
        print("\nWarning Types and Counts:")
        for message, count in warning_counts.items():
            print(f"{message}: {count} occurrence(s)")

        # Optionally, return the success status and the list of warnings
        return CompileStatus.OK if success else CompileStatus.SYNTAX_ERROR, warnings
    except:
        return CompileStatus.EXCEPTION_OCCURRED, None


def x_check_for_warnings__mutmut_6(project_dir, tests=True):
    try:
        vet_cmd = "go vet ./..."

        print("Running go vet...")
        # Run the go vet command and capture output
        process = subprocess.Popen(
            vet_cmd,
            shell=False,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            text=True  # Ensures the output is in string format
        )
        stdout, stderr = process.communicate()

        output = stderr.strip()

        # Check the return code to determine if go vet succeeded
        success = process.returncode == 0

        # Parse the warnings from the output
        warnings = []
        pattern = r'^(?:vet: )?(.*):(\d+):(\d+): (.*)$'

        print(output)
        for line in output.splitlines():
            match = re.match(pattern, line)
            if match:
                file_path, line_num, col_num, message = match.groups()
                if tests and "test.go" in file_path:
                    warning = {
                        'file': file_path.strip(),
                        'line': int(line_num),
                        'column': int(col_num),
                        'message': message.strip()
                    }
                    warnings.append(warning)

        # Analyze the types of warnings
        warning_messages = [warning['message'] for warning in warnings]
        warning_counts = Counter(warning_messages)

        # Print the analysis of warning types
        print("\nWarning Types and Counts:")
        for message, count in warning_counts.items():
            print(f"{message}: {count} occurrence(s)")

        # Optionally, return the success status and the list of warnings
        return CompileStatus.OK if success else CompileStatus.SYNTAX_ERROR, warnings
    except:
        return CompileStatus.EXCEPTION_OCCURRED, None


def x_check_for_warnings__mutmut_7(project_dir, tests=True):
    try:
        vet_cmd = "go vet ./..."

        print("Running go vet...")
        # Run the go vet command and capture output
        process = subprocess.Popen(
            vet_cmd,
            shell=True,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            text=False  # Ensures the output is in string format
        )
        stdout, stderr = process.communicate()

        output = stderr.strip()

        # Check the return code to determine if go vet succeeded
        success = process.returncode == 0

        # Parse the warnings from the output
        warnings = []
        pattern = r'^(?:vet: )?(.*):(\d+):(\d+): (.*)$'

        print(output)
        for line in output.splitlines():
            match = re.match(pattern, line)
            if match:
                file_path, line_num, col_num, message = match.groups()
                if tests and "test.go" in file_path:
                    warning = {
                        'file': file_path.strip(),
                        'line': int(line_num),
                        'column': int(col_num),
                        'message': message.strip()
                    }
                    warnings.append(warning)

        # Analyze the types of warnings
        warning_messages = [warning['message'] for warning in warnings]
        warning_counts = Counter(warning_messages)

        # Print the analysis of warning types
        print("\nWarning Types and Counts:")
        for message, count in warning_counts.items():
            print(f"{message}: {count} occurrence(s)")

        # Optionally, return the success status and the list of warnings
        return CompileStatus.OK if success else CompileStatus.SYNTAX_ERROR, warnings
    except:
        return CompileStatus.EXCEPTION_OCCURRED, None


def x_check_for_warnings__mutmut_8(project_dir, tests=True):
    try:
        vet_cmd = "go vet ./..."

        print("Running go vet...")
        # Run the go vet command and capture output
        process = subprocess.Popen(
            shell=True,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            text=True  # Ensures the output is in string format
        )
        stdout, stderr = process.communicate()

        output = stderr.strip()

        # Check the return code to determine if go vet succeeded
        success = process.returncode == 0

        # Parse the warnings from the output
        warnings = []
        pattern = r'^(?:vet: )?(.*):(\d+):(\d+): (.*)$'

        print(output)
        for line in output.splitlines():
            match = re.match(pattern, line)
            if match:
                file_path, line_num, col_num, message = match.groups()
                if tests and "test.go" in file_path:
                    warning = {
                        'file': file_path.strip(),
                        'line': int(line_num),
                        'column': int(col_num),
                        'message': message.strip()
                    }
                    warnings.append(warning)

        # Analyze the types of warnings
        warning_messages = [warning['message'] for warning in warnings]
        warning_counts = Counter(warning_messages)

        # Print the analysis of warning types
        print("\nWarning Types and Counts:")
        for message, count in warning_counts.items():
            print(f"{message}: {count} occurrence(s)")

        # Optionally, return the success status and the list of warnings
        return CompileStatus.OK if success else CompileStatus.SYNTAX_ERROR, warnings
    except:
        return CompileStatus.EXCEPTION_OCCURRED, None


def x_check_for_warnings__mutmut_9(project_dir, tests=True):
    try:
        vet_cmd = "go vet ./..."

        print("Running go vet...")
        # Run the go vet command and capture output
        process = subprocess.Popen(
            vet_cmd,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            text=True  # Ensures the output is in string format
        )
        stdout, stderr = process.communicate()

        output = stderr.strip()

        # Check the return code to determine if go vet succeeded
        success = process.returncode == 0

        # Parse the warnings from the output
        warnings = []
        pattern = r'^(?:vet: )?(.*):(\d+):(\d+): (.*)$'

        print(output)
        for line in output.splitlines():
            match = re.match(pattern, line)
            if match:
                file_path, line_num, col_num, message = match.groups()
                if tests and "test.go" in file_path:
                    warning = {
                        'file': file_path.strip(),
                        'line': int(line_num),
                        'column': int(col_num),
                        'message': message.strip()
                    }
                    warnings.append(warning)

        # Analyze the types of warnings
        warning_messages = [warning['message'] for warning in warnings]
        warning_counts = Counter(warning_messages)

        # Print the analysis of warning types
        print("\nWarning Types and Counts:")
        for message, count in warning_counts.items():
            print(f"{message}: {count} occurrence(s)")

        # Optionally, return the success status and the list of warnings
        return CompileStatus.OK if success else CompileStatus.SYNTAX_ERROR, warnings
    except:
        return CompileStatus.EXCEPTION_OCCURRED, None


def x_check_for_warnings__mutmut_10(project_dir, tests=True):
    try:
        vet_cmd = "go vet ./..."

        print("Running go vet...")
        # Run the go vet command and capture output
        process = subprocess.Popen(
            vet_cmd,
            shell=True,
            stderr=subprocess.PIPE,
            text=True  # Ensures the output is in string format
        )
        stdout, stderr = process.communicate()

        output = stderr.strip()

        # Check the return code to determine if go vet succeeded
        success = process.returncode == 0

        # Parse the warnings from the output
        warnings = []
        pattern = r'^(?:vet: )?(.*):(\d+):(\d+): (.*)$'

        print(output)
        for line in output.splitlines():
            match = re.match(pattern, line)
            if match:
                file_path, line_num, col_num, message = match.groups()
                if tests and "test.go" in file_path:
                    warning = {
                        'file': file_path.strip(),
                        'line': int(line_num),
                        'column': int(col_num),
                        'message': message.strip()
                    }
                    warnings.append(warning)

        # Analyze the types of warnings
        warning_messages = [warning['message'] for warning in warnings]
        warning_counts = Counter(warning_messages)

        # Print the analysis of warning types
        print("\nWarning Types and Counts:")
        for message, count in warning_counts.items():
            print(f"{message}: {count} occurrence(s)")

        # Optionally, return the success status and the list of warnings
        return CompileStatus.OK if success else CompileStatus.SYNTAX_ERROR, warnings
    except:
        return CompileStatus.EXCEPTION_OCCURRED, None


def x_check_for_warnings__mutmut_11(project_dir, tests=True):
    try:
        vet_cmd = "go vet ./..."

        print("Running go vet...")
        # Run the go vet command and capture output
        process = subprocess.Popen(
            vet_cmd,
            shell=True,
            stdout=subprocess.PIPE,
            text=True  # Ensures the output is in string format
        )
        stdout, stderr = process.communicate()

        output = stderr.strip()

        # Check the return code to determine if go vet succeeded
        success = process.returncode == 0

        # Parse the warnings from the output
        warnings = []
        pattern = r'^(?:vet: )?(.*):(\d+):(\d+): (.*)$'

        print(output)
        for line in output.splitlines():
            match = re.match(pattern, line)
            if match:
                file_path, line_num, col_num, message = match.groups()
                if tests and "test.go" in file_path:
                    warning = {
                        'file': file_path.strip(),
                        'line': int(line_num),
                        'column': int(col_num),
                        'message': message.strip()
                    }
                    warnings.append(warning)

        # Analyze the types of warnings
        warning_messages = [warning['message'] for warning in warnings]
        warning_counts = Counter(warning_messages)

        # Print the analysis of warning types
        print("\nWarning Types and Counts:")
        for message, count in warning_counts.items():
            print(f"{message}: {count} occurrence(s)")

        # Optionally, return the success status and the list of warnings
        return CompileStatus.OK if success else CompileStatus.SYNTAX_ERROR, warnings
    except:
        return CompileStatus.EXCEPTION_OCCURRED, None


def x_check_for_warnings__mutmut_12(project_dir, tests=True):
    try:
        vet_cmd = "go vet ./..."

        print("Running go vet...")
        # Run the go vet command and capture output
        process = subprocess.Popen(
            vet_cmd,
            shell=True,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,  # Ensures the output is in string format
        )
        stdout, stderr = process.communicate()

        output = stderr.strip()

        # Check the return code to determine if go vet succeeded
        success = process.returncode == 0

        # Parse the warnings from the output
        warnings = []
        pattern = r'^(?:vet: )?(.*):(\d+):(\d+): (.*)$'

        print(output)
        for line in output.splitlines():
            match = re.match(pattern, line)
            if match:
                file_path, line_num, col_num, message = match.groups()
                if tests and "test.go" in file_path:
                    warning = {
                        'file': file_path.strip(),
                        'line': int(line_num),
                        'column': int(col_num),
                        'message': message.strip()
                    }
                    warnings.append(warning)

        # Analyze the types of warnings
        warning_messages = [warning['message'] for warning in warnings]
        warning_counts = Counter(warning_messages)

        # Print the analysis of warning types
        print("\nWarning Types and Counts:")
        for message, count in warning_counts.items():
            print(f"{message}: {count} occurrence(s)")

        # Optionally, return the success status and the list of warnings
        return CompileStatus.OK if success else CompileStatus.SYNTAX_ERROR, warnings
    except:
        return CompileStatus.EXCEPTION_OCCURRED, None


def x_check_for_warnings__mutmut_13(project_dir, tests=True):
    try:
        vet_cmd = "go vet ./..."

        print("Running go vet...")
        # Run the go vet command and capture output
        process = None
        stdout, stderr = process.communicate()

        output = stderr.strip()

        # Check the return code to determine if go vet succeeded
        success = process.returncode == 0

        # Parse the warnings from the output
        warnings = []
        pattern = r'^(?:vet: )?(.*):(\d+):(\d+): (.*)$'

        print(output)
        for line in output.splitlines():
            match = re.match(pattern, line)
            if match:
                file_path, line_num, col_num, message = match.groups()
                if tests and "test.go" in file_path:
                    warning = {
                        'file': file_path.strip(),
                        'line': int(line_num),
                        'column': int(col_num),
                        'message': message.strip()
                    }
                    warnings.append(warning)

        # Analyze the types of warnings
        warning_messages = [warning['message'] for warning in warnings]
        warning_counts = Counter(warning_messages)

        # Print the analysis of warning types
        print("\nWarning Types and Counts:")
        for message, count in warning_counts.items():
            print(f"{message}: {count} occurrence(s)")

        # Optionally, return the success status and the list of warnings
        return CompileStatus.OK if success else CompileStatus.SYNTAX_ERROR, warnings
    except:
        return CompileStatus.EXCEPTION_OCCURRED, None


def x_check_for_warnings__mutmut_14(project_dir, tests=True):
    try:
        vet_cmd = "go vet ./..."

        print("Running go vet...")
        # Run the go vet command and capture output
        process = subprocess.Popen(
            vet_cmd,
            shell=True,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            text=True  # Ensures the output is in string format
        )
        stdout, stderr = None

        output = stderr.strip()

        # Check the return code to determine if go vet succeeded
        success = process.returncode == 0

        # Parse the warnings from the output
        warnings = []
        pattern = r'^(?:vet: )?(.*):(\d+):(\d+): (.*)$'

        print(output)
        for line in output.splitlines():
            match = re.match(pattern, line)
            if match:
                file_path, line_num, col_num, message = match.groups()
                if tests and "test.go" in file_path:
                    warning = {
                        'file': file_path.strip(),
                        'line': int(line_num),
                        'column': int(col_num),
                        'message': message.strip()
                    }
                    warnings.append(warning)

        # Analyze the types of warnings
        warning_messages = [warning['message'] for warning in warnings]
        warning_counts = Counter(warning_messages)

        # Print the analysis of warning types
        print("\nWarning Types and Counts:")
        for message, count in warning_counts.items():
            print(f"{message}: {count} occurrence(s)")

        # Optionally, return the success status and the list of warnings
        return CompileStatus.OK if success else CompileStatus.SYNTAX_ERROR, warnings
    except:
        return CompileStatus.EXCEPTION_OCCURRED, None


def x_check_for_warnings__mutmut_15(project_dir, tests=True):
    try:
        vet_cmd = "go vet ./..."

        print("Running go vet...")
        # Run the go vet command and capture output
        process = subprocess.Popen(
            vet_cmd,
            shell=True,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            text=True  # Ensures the output is in string format
        )
        stdout, stderr = process.communicate()

        output = None

        # Check the return code to determine if go vet succeeded
        success = process.returncode == 0

        # Parse the warnings from the output
        warnings = []
        pattern = r'^(?:vet: )?(.*):(\d+):(\d+): (.*)$'

        print(output)
        for line in output.splitlines():
            match = re.match(pattern, line)
            if match:
                file_path, line_num, col_num, message = match.groups()
                if tests and "test.go" in file_path:
                    warning = {
                        'file': file_path.strip(),
                        'line': int(line_num),
                        'column': int(col_num),
                        'message': message.strip()
                    }
                    warnings.append(warning)

        # Analyze the types of warnings
        warning_messages = [warning['message'] for warning in warnings]
        warning_counts = Counter(warning_messages)

        # Print the analysis of warning types
        print("\nWarning Types and Counts:")
        for message, count in warning_counts.items():
            print(f"{message}: {count} occurrence(s)")

        # Optionally, return the success status and the list of warnings
        return CompileStatus.OK if success else CompileStatus.SYNTAX_ERROR, warnings
    except:
        return CompileStatus.EXCEPTION_OCCURRED, None


def x_check_for_warnings__mutmut_16(project_dir, tests=True):
    try:
        vet_cmd = "go vet ./..."

        print("Running go vet...")
        # Run the go vet command and capture output
        process = subprocess.Popen(
            vet_cmd,
            shell=True,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            text=True  # Ensures the output is in string format
        )
        stdout, stderr = process.communicate()

        output = stderr.strip()

        # Check the return code to determine if go vet succeeded
        success = process.returncode != 0

        # Parse the warnings from the output
        warnings = []
        pattern = r'^(?:vet: )?(.*):(\d+):(\d+): (.*)$'

        print(output)
        for line in output.splitlines():
            match = re.match(pattern, line)
            if match:
                file_path, line_num, col_num, message = match.groups()
                if tests and "test.go" in file_path:
                    warning = {
                        'file': file_path.strip(),
                        'line': int(line_num),
                        'column': int(col_num),
                        'message': message.strip()
                    }
                    warnings.append(warning)

        # Analyze the types of warnings
        warning_messages = [warning['message'] for warning in warnings]
        warning_counts = Counter(warning_messages)

        # Print the analysis of warning types
        print("\nWarning Types and Counts:")
        for message, count in warning_counts.items():
            print(f"{message}: {count} occurrence(s)")

        # Optionally, return the success status and the list of warnings
        return CompileStatus.OK if success else CompileStatus.SYNTAX_ERROR, warnings
    except:
        return CompileStatus.EXCEPTION_OCCURRED, None


def x_check_for_warnings__mutmut_17(project_dir, tests=True):
    try:
        vet_cmd = "go vet ./..."

        print("Running go vet...")
        # Run the go vet command and capture output
        process = subprocess.Popen(
            vet_cmd,
            shell=True,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            text=True  # Ensures the output is in string format
        )
        stdout, stderr = process.communicate()

        output = stderr.strip()

        # Check the return code to determine if go vet succeeded
        success = process.returncode == 1

        # Parse the warnings from the output
        warnings = []
        pattern = r'^(?:vet: )?(.*):(\d+):(\d+): (.*)$'

        print(output)
        for line in output.splitlines():
            match = re.match(pattern, line)
            if match:
                file_path, line_num, col_num, message = match.groups()
                if tests and "test.go" in file_path:
                    warning = {
                        'file': file_path.strip(),
                        'line': int(line_num),
                        'column': int(col_num),
                        'message': message.strip()
                    }
                    warnings.append(warning)

        # Analyze the types of warnings
        warning_messages = [warning['message'] for warning in warnings]
        warning_counts = Counter(warning_messages)

        # Print the analysis of warning types
        print("\nWarning Types and Counts:")
        for message, count in warning_counts.items():
            print(f"{message}: {count} occurrence(s)")

        # Optionally, return the success status and the list of warnings
        return CompileStatus.OK if success else CompileStatus.SYNTAX_ERROR, warnings
    except:
        return CompileStatus.EXCEPTION_OCCURRED, None


def x_check_for_warnings__mutmut_18(project_dir, tests=True):
    try:
        vet_cmd = "go vet ./..."

        print("Running go vet...")
        # Run the go vet command and capture output
        process = subprocess.Popen(
            vet_cmd,
            shell=True,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            text=True  # Ensures the output is in string format
        )
        stdout, stderr = process.communicate()

        output = stderr.strip()

        # Check the return code to determine if go vet succeeded
        success = None

        # Parse the warnings from the output
        warnings = []
        pattern = r'^(?:vet: )?(.*):(\d+):(\d+): (.*)$'

        print(output)
        for line in output.splitlines():
            match = re.match(pattern, line)
            if match:
                file_path, line_num, col_num, message = match.groups()
                if tests and "test.go" in file_path:
                    warning = {
                        'file': file_path.strip(),
                        'line': int(line_num),
                        'column': int(col_num),
                        'message': message.strip()
                    }
                    warnings.append(warning)

        # Analyze the types of warnings
        warning_messages = [warning['message'] for warning in warnings]
        warning_counts = Counter(warning_messages)

        # Print the analysis of warning types
        print("\nWarning Types and Counts:")
        for message, count in warning_counts.items():
            print(f"{message}: {count} occurrence(s)")

        # Optionally, return the success status and the list of warnings
        return CompileStatus.OK if success else CompileStatus.SYNTAX_ERROR, warnings
    except:
        return CompileStatus.EXCEPTION_OCCURRED, None


def x_check_for_warnings__mutmut_19(project_dir, tests=True):
    try:
        vet_cmd = "go vet ./..."

        print("Running go vet...")
        # Run the go vet command and capture output
        process = subprocess.Popen(
            vet_cmd,
            shell=True,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            text=True  # Ensures the output is in string format
        )
        stdout, stderr = process.communicate()

        output = stderr.strip()

        # Check the return code to determine if go vet succeeded
        success = process.returncode == 0

        # Parse the warnings from the output
        warnings = None
        pattern = r'^(?:vet: )?(.*):(\d+):(\d+): (.*)$'

        print(output)
        for line in output.splitlines():
            match = re.match(pattern, line)
            if match:
                file_path, line_num, col_num, message = match.groups()
                if tests and "test.go" in file_path:
                    warning = {
                        'file': file_path.strip(),
                        'line': int(line_num),
                        'column': int(col_num),
                        'message': message.strip()
                    }
                    warnings.append(warning)

        # Analyze the types of warnings
        warning_messages = [warning['message'] for warning in warnings]
        warning_counts = Counter(warning_messages)

        # Print the analysis of warning types
        print("\nWarning Types and Counts:")
        for message, count in warning_counts.items():
            print(f"{message}: {count} occurrence(s)")

        # Optionally, return the success status and the list of warnings
        return CompileStatus.OK if success else CompileStatus.SYNTAX_ERROR, warnings
    except:
        return CompileStatus.EXCEPTION_OCCURRED, None


def x_check_for_warnings__mutmut_20(project_dir, tests=True):
    try:
        vet_cmd = "go vet ./..."

        print("Running go vet...")
        # Run the go vet command and capture output
        process = subprocess.Popen(
            vet_cmd,
            shell=True,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            text=True  # Ensures the output is in string format
        )
        stdout, stderr = process.communicate()

        output = stderr.strip()

        # Check the return code to determine if go vet succeeded
        success = process.returncode == 0

        # Parse the warnings from the output
        warnings = []
        pattern = r'XX^(?:vet: )?(.*):(\d+):(\d+): (.*)$XX'

        print(output)
        for line in output.splitlines():
            match = re.match(pattern, line)
            if match:
                file_path, line_num, col_num, message = match.groups()
                if tests and "test.go" in file_path:
                    warning = {
                        'file': file_path.strip(),
                        'line': int(line_num),
                        'column': int(col_num),
                        'message': message.strip()
                    }
                    warnings.append(warning)

        # Analyze the types of warnings
        warning_messages = [warning['message'] for warning in warnings]
        warning_counts = Counter(warning_messages)

        # Print the analysis of warning types
        print("\nWarning Types and Counts:")
        for message, count in warning_counts.items():
            print(f"{message}: {count} occurrence(s)")

        # Optionally, return the success status and the list of warnings
        return CompileStatus.OK if success else CompileStatus.SYNTAX_ERROR, warnings
    except:
        return CompileStatus.EXCEPTION_OCCURRED, None


def x_check_for_warnings__mutmut_21(project_dir, tests=True):
    try:
        vet_cmd = "go vet ./..."

        print("Running go vet...")
        # Run the go vet command and capture output
        process = subprocess.Popen(
            vet_cmd,
            shell=True,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            text=True  # Ensures the output is in string format
        )
        stdout, stderr = process.communicate()

        output = stderr.strip()

        # Check the return code to determine if go vet succeeded
        success = process.returncode == 0

        # Parse the warnings from the output
        warnings = []
        pattern = None

        print(output)
        for line in output.splitlines():
            match = re.match(pattern, line)
            if match:
                file_path, line_num, col_num, message = match.groups()
                if tests and "test.go" in file_path:
                    warning = {
                        'file': file_path.strip(),
                        'line': int(line_num),
                        'column': int(col_num),
                        'message': message.strip()
                    }
                    warnings.append(warning)

        # Analyze the types of warnings
        warning_messages = [warning['message'] for warning in warnings]
        warning_counts = Counter(warning_messages)

        # Print the analysis of warning types
        print("\nWarning Types and Counts:")
        for message, count in warning_counts.items():
            print(f"{message}: {count} occurrence(s)")

        # Optionally, return the success status and the list of warnings
        return CompileStatus.OK if success else CompileStatus.SYNTAX_ERROR, warnings
    except:
        return CompileStatus.EXCEPTION_OCCURRED, None


def x_check_for_warnings__mutmut_22(project_dir, tests=True):
    try:
        vet_cmd = "go vet ./..."

        print("Running go vet...")
        # Run the go vet command and capture output
        process = subprocess.Popen(
            vet_cmd,
            shell=True,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            text=True  # Ensures the output is in string format
        )
        stdout, stderr = process.communicate()

        output = stderr.strip()

        # Check the return code to determine if go vet succeeded
        success = process.returncode == 0

        # Parse the warnings from the output
        warnings = []
        pattern = r'^(?:vet: )?(.*):(\d+):(\d+): (.*)$'

        print(None)
        for line in output.splitlines():
            match = re.match(pattern, line)
            if match:
                file_path, line_num, col_num, message = match.groups()
                if tests and "test.go" in file_path:
                    warning = {
                        'file': file_path.strip(),
                        'line': int(line_num),
                        'column': int(col_num),
                        'message': message.strip()
                    }
                    warnings.append(warning)

        # Analyze the types of warnings
        warning_messages = [warning['message'] for warning in warnings]
        warning_counts = Counter(warning_messages)

        # Print the analysis of warning types
        print("\nWarning Types and Counts:")
        for message, count in warning_counts.items():
            print(f"{message}: {count} occurrence(s)")

        # Optionally, return the success status and the list of warnings
        return CompileStatus.OK if success else CompileStatus.SYNTAX_ERROR, warnings
    except:
        return CompileStatus.EXCEPTION_OCCURRED, None


def x_check_for_warnings__mutmut_23(project_dir, tests=True):
    try:
        vet_cmd = "go vet ./..."

        print("Running go vet...")
        # Run the go vet command and capture output
        process = subprocess.Popen(
            vet_cmd,
            shell=True,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            text=True  # Ensures the output is in string format
        )
        stdout, stderr = process.communicate()

        output = stderr.strip()

        # Check the return code to determine if go vet succeeded
        success = process.returncode == 0

        # Parse the warnings from the output
        warnings = []
        pattern = r'^(?:vet: )?(.*):(\d+):(\d+): (.*)$'

        print(output)
        for line in output.splitlines():
            match = re.match(None, line)
            if match:
                file_path, line_num, col_num, message = match.groups()
                if tests and "test.go" in file_path:
                    warning = {
                        'file': file_path.strip(),
                        'line': int(line_num),
                        'column': int(col_num),
                        'message': message.strip()
                    }
                    warnings.append(warning)

        # Analyze the types of warnings
        warning_messages = [warning['message'] for warning in warnings]
        warning_counts = Counter(warning_messages)

        # Print the analysis of warning types
        print("\nWarning Types and Counts:")
        for message, count in warning_counts.items():
            print(f"{message}: {count} occurrence(s)")

        # Optionally, return the success status and the list of warnings
        return CompileStatus.OK if success else CompileStatus.SYNTAX_ERROR, warnings
    except:
        return CompileStatus.EXCEPTION_OCCURRED, None


def x_check_for_warnings__mutmut_24(project_dir, tests=True):
    try:
        vet_cmd = "go vet ./..."

        print("Running go vet...")
        # Run the go vet command and capture output
        process = subprocess.Popen(
            vet_cmd,
            shell=True,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            text=True  # Ensures the output is in string format
        )
        stdout, stderr = process.communicate()

        output = stderr.strip()

        # Check the return code to determine if go vet succeeded
        success = process.returncode == 0

        # Parse the warnings from the output
        warnings = []
        pattern = r'^(?:vet: )?(.*):(\d+):(\d+): (.*)$'

        print(output)
        for line in output.splitlines():
            match = re.match(pattern, None)
            if match:
                file_path, line_num, col_num, message = match.groups()
                if tests and "test.go" in file_path:
                    warning = {
                        'file': file_path.strip(),
                        'line': int(line_num),
                        'column': int(col_num),
                        'message': message.strip()
                    }
                    warnings.append(warning)

        # Analyze the types of warnings
        warning_messages = [warning['message'] for warning in warnings]
        warning_counts = Counter(warning_messages)

        # Print the analysis of warning types
        print("\nWarning Types and Counts:")
        for message, count in warning_counts.items():
            print(f"{message}: {count} occurrence(s)")

        # Optionally, return the success status and the list of warnings
        return CompileStatus.OK if success else CompileStatus.SYNTAX_ERROR, warnings
    except:
        return CompileStatus.EXCEPTION_OCCURRED, None


def x_check_for_warnings__mutmut_25(project_dir, tests=True):
    try:
        vet_cmd = "go vet ./..."

        print("Running go vet...")
        # Run the go vet command and capture output
        process = subprocess.Popen(
            vet_cmd,
            shell=True,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            text=True  # Ensures the output is in string format
        )
        stdout, stderr = process.communicate()

        output = stderr.strip()

        # Check the return code to determine if go vet succeeded
        success = process.returncode == 0

        # Parse the warnings from the output
        warnings = []
        pattern = r'^(?:vet: )?(.*):(\d+):(\d+): (.*)$'

        print(output)
        for line in output.splitlines():
            match = re.match( line)
            if match:
                file_path, line_num, col_num, message = match.groups()
                if tests and "test.go" in file_path:
                    warning = {
                        'file': file_path.strip(),
                        'line': int(line_num),
                        'column': int(col_num),
                        'message': message.strip()
                    }
                    warnings.append(warning)

        # Analyze the types of warnings
        warning_messages = [warning['message'] for warning in warnings]
        warning_counts = Counter(warning_messages)

        # Print the analysis of warning types
        print("\nWarning Types and Counts:")
        for message, count in warning_counts.items():
            print(f"{message}: {count} occurrence(s)")

        # Optionally, return the success status and the list of warnings
        return CompileStatus.OK if success else CompileStatus.SYNTAX_ERROR, warnings
    except:
        return CompileStatus.EXCEPTION_OCCURRED, None


def x_check_for_warnings__mutmut_26(project_dir, tests=True):
    try:
        vet_cmd = "go vet ./..."

        print("Running go vet...")
        # Run the go vet command and capture output
        process = subprocess.Popen(
            vet_cmd,
            shell=True,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            text=True  # Ensures the output is in string format
        )
        stdout, stderr = process.communicate()

        output = stderr.strip()

        # Check the return code to determine if go vet succeeded
        success = process.returncode == 0

        # Parse the warnings from the output
        warnings = []
        pattern = r'^(?:vet: )?(.*):(\d+):(\d+): (.*)$'

        print(output)
        for line in output.splitlines():
            match = re.match(pattern,)
            if match:
                file_path, line_num, col_num, message = match.groups()
                if tests and "test.go" in file_path:
                    warning = {
                        'file': file_path.strip(),
                        'line': int(line_num),
                        'column': int(col_num),
                        'message': message.strip()
                    }
                    warnings.append(warning)

        # Analyze the types of warnings
        warning_messages = [warning['message'] for warning in warnings]
        warning_counts = Counter(warning_messages)

        # Print the analysis of warning types
        print("\nWarning Types and Counts:")
        for message, count in warning_counts.items():
            print(f"{message}: {count} occurrence(s)")

        # Optionally, return the success status and the list of warnings
        return CompileStatus.OK if success else CompileStatus.SYNTAX_ERROR, warnings
    except:
        return CompileStatus.EXCEPTION_OCCURRED, None


def x_check_for_warnings__mutmut_27(project_dir, tests=True):
    try:
        vet_cmd = "go vet ./..."

        print("Running go vet...")
        # Run the go vet command and capture output
        process = subprocess.Popen(
            vet_cmd,
            shell=True,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            text=True  # Ensures the output is in string format
        )
        stdout, stderr = process.communicate()

        output = stderr.strip()

        # Check the return code to determine if go vet succeeded
        success = process.returncode == 0

        # Parse the warnings from the output
        warnings = []
        pattern = r'^(?:vet: )?(.*):(\d+):(\d+): (.*)$'

        print(output)
        for line in output.splitlines():
            match = None
            if match:
                file_path, line_num, col_num, message = match.groups()
                if tests and "test.go" in file_path:
                    warning = {
                        'file': file_path.strip(),
                        'line': int(line_num),
                        'column': int(col_num),
                        'message': message.strip()
                    }
                    warnings.append(warning)

        # Analyze the types of warnings
        warning_messages = [warning['message'] for warning in warnings]
        warning_counts = Counter(warning_messages)

        # Print the analysis of warning types
        print("\nWarning Types and Counts:")
        for message, count in warning_counts.items():
            print(f"{message}: {count} occurrence(s)")

        # Optionally, return the success status and the list of warnings
        return CompileStatus.OK if success else CompileStatus.SYNTAX_ERROR, warnings
    except:
        return CompileStatus.EXCEPTION_OCCURRED, None


def x_check_for_warnings__mutmut_28(project_dir, tests=True):
    try:
        vet_cmd = "go vet ./..."

        print("Running go vet...")
        # Run the go vet command and capture output
        process = subprocess.Popen(
            vet_cmd,
            shell=True,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            text=True  # Ensures the output is in string format
        )
        stdout, stderr = process.communicate()

        output = stderr.strip()

        # Check the return code to determine if go vet succeeded
        success = process.returncode == 0

        # Parse the warnings from the output
        warnings = []
        pattern = r'^(?:vet: )?(.*):(\d+):(\d+): (.*)$'

        print(output)
        for line in output.splitlines():
            match = re.match(pattern, line)
            if match:
                file_path, line_num, col_num, message = None
                if tests and "test.go" in file_path:
                    warning = {
                        'file': file_path.strip(),
                        'line': int(line_num),
                        'column': int(col_num),
                        'message': message.strip()
                    }
                    warnings.append(warning)

        # Analyze the types of warnings
        warning_messages = [warning['message'] for warning in warnings]
        warning_counts = Counter(warning_messages)

        # Print the analysis of warning types
        print("\nWarning Types and Counts:")
        for message, count in warning_counts.items():
            print(f"{message}: {count} occurrence(s)")

        # Optionally, return the success status and the list of warnings
        return CompileStatus.OK if success else CompileStatus.SYNTAX_ERROR, warnings
    except:
        return CompileStatus.EXCEPTION_OCCURRED, None


def x_check_for_warnings__mutmut_29(project_dir, tests=True):
    try:
        vet_cmd = "go vet ./..."

        print("Running go vet...")
        # Run the go vet command and capture output
        process = subprocess.Popen(
            vet_cmd,
            shell=True,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            text=True  # Ensures the output is in string format
        )
        stdout, stderr = process.communicate()

        output = stderr.strip()

        # Check the return code to determine if go vet succeeded
        success = process.returncode == 0

        # Parse the warnings from the output
        warnings = []
        pattern = r'^(?:vet: )?(.*):(\d+):(\d+): (.*)$'

        print(output)
        for line in output.splitlines():
            match = re.match(pattern, line)
            if match:
                file_path, line_num, col_num, message = match.groups()
                if tests and "XXtest.goXX" in file_path:
                    warning = {
                        'file': file_path.strip(),
                        'line': int(line_num),
                        'column': int(col_num),
                        'message': message.strip()
                    }
                    warnings.append(warning)

        # Analyze the types of warnings
        warning_messages = [warning['message'] for warning in warnings]
        warning_counts = Counter(warning_messages)

        # Print the analysis of warning types
        print("\nWarning Types and Counts:")
        for message, count in warning_counts.items():
            print(f"{message}: {count} occurrence(s)")

        # Optionally, return the success status and the list of warnings
        return CompileStatus.OK if success else CompileStatus.SYNTAX_ERROR, warnings
    except:
        return CompileStatus.EXCEPTION_OCCURRED, None


def x_check_for_warnings__mutmut_30(project_dir, tests=True):
    try:
        vet_cmd = "go vet ./..."

        print("Running go vet...")
        # Run the go vet command and capture output
        process = subprocess.Popen(
            vet_cmd,
            shell=True,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            text=True  # Ensures the output is in string format
        )
        stdout, stderr = process.communicate()

        output = stderr.strip()

        # Check the return code to determine if go vet succeeded
        success = process.returncode == 0

        # Parse the warnings from the output
        warnings = []
        pattern = r'^(?:vet: )?(.*):(\d+):(\d+): (.*)$'

        print(output)
        for line in output.splitlines():
            match = re.match(pattern, line)
            if match:
                file_path, line_num, col_num, message = match.groups()
                if tests and "test.go" not in file_path:
                    warning = {
                        'file': file_path.strip(),
                        'line': int(line_num),
                        'column': int(col_num),
                        'message': message.strip()
                    }
                    warnings.append(warning)

        # Analyze the types of warnings
        warning_messages = [warning['message'] for warning in warnings]
        warning_counts = Counter(warning_messages)

        # Print the analysis of warning types
        print("\nWarning Types and Counts:")
        for message, count in warning_counts.items():
            print(f"{message}: {count} occurrence(s)")

        # Optionally, return the success status and the list of warnings
        return CompileStatus.OK if success else CompileStatus.SYNTAX_ERROR, warnings
    except:
        return CompileStatus.EXCEPTION_OCCURRED, None


def x_check_for_warnings__mutmut_31(project_dir, tests=True):
    try:
        vet_cmd = "go vet ./..."

        print("Running go vet...")
        # Run the go vet command and capture output
        process = subprocess.Popen(
            vet_cmd,
            shell=True,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            text=True  # Ensures the output is in string format
        )
        stdout, stderr = process.communicate()

        output = stderr.strip()

        # Check the return code to determine if go vet succeeded
        success = process.returncode == 0

        # Parse the warnings from the output
        warnings = []
        pattern = r'^(?:vet: )?(.*):(\d+):(\d+): (.*)$'

        print(output)
        for line in output.splitlines():
            match = re.match(pattern, line)
            if match:
                file_path, line_num, col_num, message = match.groups()
                if tests or "test.go" in file_path:
                    warning = {
                        'file': file_path.strip(),
                        'line': int(line_num),
                        'column': int(col_num),
                        'message': message.strip()
                    }
                    warnings.append(warning)

        # Analyze the types of warnings
        warning_messages = [warning['message'] for warning in warnings]
        warning_counts = Counter(warning_messages)

        # Print the analysis of warning types
        print("\nWarning Types and Counts:")
        for message, count in warning_counts.items():
            print(f"{message}: {count} occurrence(s)")

        # Optionally, return the success status and the list of warnings
        return CompileStatus.OK if success else CompileStatus.SYNTAX_ERROR, warnings
    except:
        return CompileStatus.EXCEPTION_OCCURRED, None


def x_check_for_warnings__mutmut_32(project_dir, tests=True):
    try:
        vet_cmd = "go vet ./..."

        print("Running go vet...")
        # Run the go vet command and capture output
        process = subprocess.Popen(
            vet_cmd,
            shell=True,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            text=True  # Ensures the output is in string format
        )
        stdout, stderr = process.communicate()

        output = stderr.strip()

        # Check the return code to determine if go vet succeeded
        success = process.returncode == 0

        # Parse the warnings from the output
        warnings = []
        pattern = r'^(?:vet: )?(.*):(\d+):(\d+): (.*)$'

        print(output)
        for line in output.splitlines():
            match = re.match(pattern, line)
            if match:
                file_path, line_num, col_num, message = match.groups()
                if tests and "test.go" in file_path:
                    warning = {
                        'XXfileXX': file_path.strip(),
                        'line': int(line_num),
                        'column': int(col_num),
                        'message': message.strip()
                    }
                    warnings.append(warning)

        # Analyze the types of warnings
        warning_messages = [warning['message'] for warning in warnings]
        warning_counts = Counter(warning_messages)

        # Print the analysis of warning types
        print("\nWarning Types and Counts:")
        for message, count in warning_counts.items():
            print(f"{message}: {count} occurrence(s)")

        # Optionally, return the success status and the list of warnings
        return CompileStatus.OK if success else CompileStatus.SYNTAX_ERROR, warnings
    except:
        return CompileStatus.EXCEPTION_OCCURRED, None


def x_check_for_warnings__mutmut_33(project_dir, tests=True):
    try:
        vet_cmd = "go vet ./..."

        print("Running go vet...")
        # Run the go vet command and capture output
        process = subprocess.Popen(
            vet_cmd,
            shell=True,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            text=True  # Ensures the output is in string format
        )
        stdout, stderr = process.communicate()

        output = stderr.strip()

        # Check the return code to determine if go vet succeeded
        success = process.returncode == 0

        # Parse the warnings from the output
        warnings = []
        pattern = r'^(?:vet: )?(.*):(\d+):(\d+): (.*)$'

        print(output)
        for line in output.splitlines():
            match = re.match(pattern, line)
            if match:
                file_path, line_num, col_num, message = match.groups()
                if tests and "test.go" in file_path:
                    warning = {
                        'file': file_path.strip(),
                        'XXlineXX': int(line_num),
                        'column': int(col_num),
                        'message': message.strip()
                    }
                    warnings.append(warning)

        # Analyze the types of warnings
        warning_messages = [warning['message'] for warning in warnings]
        warning_counts = Counter(warning_messages)

        # Print the analysis of warning types
        print("\nWarning Types and Counts:")
        for message, count in warning_counts.items():
            print(f"{message}: {count} occurrence(s)")

        # Optionally, return the success status and the list of warnings
        return CompileStatus.OK if success else CompileStatus.SYNTAX_ERROR, warnings
    except:
        return CompileStatus.EXCEPTION_OCCURRED, None


def x_check_for_warnings__mutmut_34(project_dir, tests=True):
    try:
        vet_cmd = "go vet ./..."

        print("Running go vet...")
        # Run the go vet command and capture output
        process = subprocess.Popen(
            vet_cmd,
            shell=True,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            text=True  # Ensures the output is in string format
        )
        stdout, stderr = process.communicate()

        output = stderr.strip()

        # Check the return code to determine if go vet succeeded
        success = process.returncode == 0

        # Parse the warnings from the output
        warnings = []
        pattern = r'^(?:vet: )?(.*):(\d+):(\d+): (.*)$'

        print(output)
        for line in output.splitlines():
            match = re.match(pattern, line)
            if match:
                file_path, line_num, col_num, message = match.groups()
                if tests and "test.go" in file_path:
                    warning = {
                        'file': file_path.strip(),
                        'line': int(None),
                        'column': int(col_num),
                        'message': message.strip()
                    }
                    warnings.append(warning)

        # Analyze the types of warnings
        warning_messages = [warning['message'] for warning in warnings]
        warning_counts = Counter(warning_messages)

        # Print the analysis of warning types
        print("\nWarning Types and Counts:")
        for message, count in warning_counts.items():
            print(f"{message}: {count} occurrence(s)")

        # Optionally, return the success status and the list of warnings
        return CompileStatus.OK if success else CompileStatus.SYNTAX_ERROR, warnings
    except:
        return CompileStatus.EXCEPTION_OCCURRED, None


def x_check_for_warnings__mutmut_35(project_dir, tests=True):
    try:
        vet_cmd = "go vet ./..."

        print("Running go vet...")
        # Run the go vet command and capture output
        process = subprocess.Popen(
            vet_cmd,
            shell=True,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            text=True  # Ensures the output is in string format
        )
        stdout, stderr = process.communicate()

        output = stderr.strip()

        # Check the return code to determine if go vet succeeded
        success = process.returncode == 0

        # Parse the warnings from the output
        warnings = []
        pattern = r'^(?:vet: )?(.*):(\d+):(\d+): (.*)$'

        print(output)
        for line in output.splitlines():
            match = re.match(pattern, line)
            if match:
                file_path, line_num, col_num, message = match.groups()
                if tests and "test.go" in file_path:
                    warning = {
                        'file': file_path.strip(),
                        'line': int(line_num),
                        'XXcolumnXX': int(col_num),
                        'message': message.strip()
                    }
                    warnings.append(warning)

        # Analyze the types of warnings
        warning_messages = [warning['message'] for warning in warnings]
        warning_counts = Counter(warning_messages)

        # Print the analysis of warning types
        print("\nWarning Types and Counts:")
        for message, count in warning_counts.items():
            print(f"{message}: {count} occurrence(s)")

        # Optionally, return the success status and the list of warnings
        return CompileStatus.OK if success else CompileStatus.SYNTAX_ERROR, warnings
    except:
        return CompileStatus.EXCEPTION_OCCURRED, None


def x_check_for_warnings__mutmut_36(project_dir, tests=True):
    try:
        vet_cmd = "go vet ./..."

        print("Running go vet...")
        # Run the go vet command and capture output
        process = subprocess.Popen(
            vet_cmd,
            shell=True,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            text=True  # Ensures the output is in string format
        )
        stdout, stderr = process.communicate()

        output = stderr.strip()

        # Check the return code to determine if go vet succeeded
        success = process.returncode == 0

        # Parse the warnings from the output
        warnings = []
        pattern = r'^(?:vet: )?(.*):(\d+):(\d+): (.*)$'

        print(output)
        for line in output.splitlines():
            match = re.match(pattern, line)
            if match:
                file_path, line_num, col_num, message = match.groups()
                if tests and "test.go" in file_path:
                    warning = {
                        'file': file_path.strip(),
                        'line': int(line_num),
                        'column': int(None),
                        'message': message.strip()
                    }
                    warnings.append(warning)

        # Analyze the types of warnings
        warning_messages = [warning['message'] for warning in warnings]
        warning_counts = Counter(warning_messages)

        # Print the analysis of warning types
        print("\nWarning Types and Counts:")
        for message, count in warning_counts.items():
            print(f"{message}: {count} occurrence(s)")

        # Optionally, return the success status and the list of warnings
        return CompileStatus.OK if success else CompileStatus.SYNTAX_ERROR, warnings
    except:
        return CompileStatus.EXCEPTION_OCCURRED, None


def x_check_for_warnings__mutmut_37(project_dir, tests=True):
    try:
        vet_cmd = "go vet ./..."

        print("Running go vet...")
        # Run the go vet command and capture output
        process = subprocess.Popen(
            vet_cmd,
            shell=True,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            text=True  # Ensures the output is in string format
        )
        stdout, stderr = process.communicate()

        output = stderr.strip()

        # Check the return code to determine if go vet succeeded
        success = process.returncode == 0

        # Parse the warnings from the output
        warnings = []
        pattern = r'^(?:vet: )?(.*):(\d+):(\d+): (.*)$'

        print(output)
        for line in output.splitlines():
            match = re.match(pattern, line)
            if match:
                file_path, line_num, col_num, message = match.groups()
                if tests and "test.go" in file_path:
                    warning = {
                        'file': file_path.strip(),
                        'line': int(line_num),
                        'column': int(col_num),
                        'XXmessageXX': message.strip()
                    }
                    warnings.append(warning)

        # Analyze the types of warnings
        warning_messages = [warning['message'] for warning in warnings]
        warning_counts = Counter(warning_messages)

        # Print the analysis of warning types
        print("\nWarning Types and Counts:")
        for message, count in warning_counts.items():
            print(f"{message}: {count} occurrence(s)")

        # Optionally, return the success status and the list of warnings
        return CompileStatus.OK if success else CompileStatus.SYNTAX_ERROR, warnings
    except:
        return CompileStatus.EXCEPTION_OCCURRED, None


def x_check_for_warnings__mutmut_38(project_dir, tests=True):
    try:
        vet_cmd = "go vet ./..."

        print("Running go vet...")
        # Run the go vet command and capture output
        process = subprocess.Popen(
            vet_cmd,
            shell=True,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            text=True  # Ensures the output is in string format
        )
        stdout, stderr = process.communicate()

        output = stderr.strip()

        # Check the return code to determine if go vet succeeded
        success = process.returncode == 0

        # Parse the warnings from the output
        warnings = []
        pattern = r'^(?:vet: )?(.*):(\d+):(\d+): (.*)$'

        print(output)
        for line in output.splitlines():
            match = re.match(pattern, line)
            if match:
                file_path, line_num, col_num, message = match.groups()
                if tests and "test.go" in file_path:
                    warning = None
                    warnings.append(warning)

        # Analyze the types of warnings
        warning_messages = [warning['message'] for warning in warnings]
        warning_counts = Counter(warning_messages)

        # Print the analysis of warning types
        print("\nWarning Types and Counts:")
        for message, count in warning_counts.items():
            print(f"{message}: {count} occurrence(s)")

        # Optionally, return the success status and the list of warnings
        return CompileStatus.OK if success else CompileStatus.SYNTAX_ERROR, warnings
    except:
        return CompileStatus.EXCEPTION_OCCURRED, None


def x_check_for_warnings__mutmut_39(project_dir, tests=True):
    try:
        vet_cmd = "go vet ./..."

        print("Running go vet...")
        # Run the go vet command and capture output
        process = subprocess.Popen(
            vet_cmd,
            shell=True,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            text=True  # Ensures the output is in string format
        )
        stdout, stderr = process.communicate()

        output = stderr.strip()

        # Check the return code to determine if go vet succeeded
        success = process.returncode == 0

        # Parse the warnings from the output
        warnings = []
        pattern = r'^(?:vet: )?(.*):(\d+):(\d+): (.*)$'

        print(output)
        for line in output.splitlines():
            match = re.match(pattern, line)
            if match:
                file_path, line_num, col_num, message = match.groups()
                if tests and "test.go" in file_path:
                    warning = {
                        'file': file_path.strip(),
                        'line': int(line_num),
                        'column': int(col_num),
                        'message': message.strip()
                    }
                    warnings.append(None)

        # Analyze the types of warnings
        warning_messages = [warning['message'] for warning in warnings]
        warning_counts = Counter(warning_messages)

        # Print the analysis of warning types
        print("\nWarning Types and Counts:")
        for message, count in warning_counts.items():
            print(f"{message}: {count} occurrence(s)")

        # Optionally, return the success status and the list of warnings
        return CompileStatus.OK if success else CompileStatus.SYNTAX_ERROR, warnings
    except:
        return CompileStatus.EXCEPTION_OCCURRED, None


def x_check_for_warnings__mutmut_40(project_dir, tests=True):
    try:
        vet_cmd = "go vet ./..."

        print("Running go vet...")
        # Run the go vet command and capture output
        process = subprocess.Popen(
            vet_cmd,
            shell=True,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            text=True  # Ensures the output is in string format
        )
        stdout, stderr = process.communicate()

        output = stderr.strip()

        # Check the return code to determine if go vet succeeded
        success = process.returncode == 0

        # Parse the warnings from the output
        warnings = []
        pattern = r'^(?:vet: )?(.*):(\d+):(\d+): (.*)$'

        print(output)
        for line in output.splitlines():
            match = re.match(pattern, line)
            if match:
                file_path, line_num, col_num, message = match.groups()
                if tests and "test.go" in file_path:
                    warning = {
                        'file': file_path.strip(),
                        'line': int(line_num),
                        'column': int(col_num),
                        'message': message.strip()
                    }
                    warnings.append(warning)

        # Analyze the types of warnings
        warning_messages = [warning['XXmessageXX'] for warning in warnings]
        warning_counts = Counter(warning_messages)

        # Print the analysis of warning types
        print("\nWarning Types and Counts:")
        for message, count in warning_counts.items():
            print(f"{message}: {count} occurrence(s)")

        # Optionally, return the success status and the list of warnings
        return CompileStatus.OK if success else CompileStatus.SYNTAX_ERROR, warnings
    except:
        return CompileStatus.EXCEPTION_OCCURRED, None


def x_check_for_warnings__mutmut_41(project_dir, tests=True):
    try:
        vet_cmd = "go vet ./..."

        print("Running go vet...")
        # Run the go vet command and capture output
        process = subprocess.Popen(
            vet_cmd,
            shell=True,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            text=True  # Ensures the output is in string format
        )
        stdout, stderr = process.communicate()

        output = stderr.strip()

        # Check the return code to determine if go vet succeeded
        success = process.returncode == 0

        # Parse the warnings from the output
        warnings = []
        pattern = r'^(?:vet: )?(.*):(\d+):(\d+): (.*)$'

        print(output)
        for line in output.splitlines():
            match = re.match(pattern, line)
            if match:
                file_path, line_num, col_num, message = match.groups()
                if tests and "test.go" in file_path:
                    warning = {
                        'file': file_path.strip(),
                        'line': int(line_num),
                        'column': int(col_num),
                        'message': message.strip()
                    }
                    warnings.append(warning)

        # Analyze the types of warnings
        warning_messages = [warning[None] for warning in warnings]
        warning_counts = Counter(warning_messages)

        # Print the analysis of warning types
        print("\nWarning Types and Counts:")
        for message, count in warning_counts.items():
            print(f"{message}: {count} occurrence(s)")

        # Optionally, return the success status and the list of warnings
        return CompileStatus.OK if success else CompileStatus.SYNTAX_ERROR, warnings
    except:
        return CompileStatus.EXCEPTION_OCCURRED, None


def x_check_for_warnings__mutmut_42(project_dir, tests=True):
    try:
        vet_cmd = "go vet ./..."

        print("Running go vet...")
        # Run the go vet command and capture output
        process = subprocess.Popen(
            vet_cmd,
            shell=True,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            text=True  # Ensures the output is in string format
        )
        stdout, stderr = process.communicate()

        output = stderr.strip()

        # Check the return code to determine if go vet succeeded
        success = process.returncode == 0

        # Parse the warnings from the output
        warnings = []
        pattern = r'^(?:vet: )?(.*):(\d+):(\d+): (.*)$'

        print(output)
        for line in output.splitlines():
            match = re.match(pattern, line)
            if match:
                file_path, line_num, col_num, message = match.groups()
                if tests and "test.go" in file_path:
                    warning = {
                        'file': file_path.strip(),
                        'line': int(line_num),
                        'column': int(col_num),
                        'message': message.strip()
                    }
                    warnings.append(warning)

        # Analyze the types of warnings
        warning_messages = None
        warning_counts = Counter(warning_messages)

        # Print the analysis of warning types
        print("\nWarning Types and Counts:")
        for message, count in warning_counts.items():
            print(f"{message}: {count} occurrence(s)")

        # Optionally, return the success status and the list of warnings
        return CompileStatus.OK if success else CompileStatus.SYNTAX_ERROR, warnings
    except:
        return CompileStatus.EXCEPTION_OCCURRED, None


def x_check_for_warnings__mutmut_43(project_dir, tests=True):
    try:
        vet_cmd = "go vet ./..."

        print("Running go vet...")
        # Run the go vet command and capture output
        process = subprocess.Popen(
            vet_cmd,
            shell=True,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            text=True  # Ensures the output is in string format
        )
        stdout, stderr = process.communicate()

        output = stderr.strip()

        # Check the return code to determine if go vet succeeded
        success = process.returncode == 0

        # Parse the warnings from the output
        warnings = []
        pattern = r'^(?:vet: )?(.*):(\d+):(\d+): (.*)$'

        print(output)
        for line in output.splitlines():
            match = re.match(pattern, line)
            if match:
                file_path, line_num, col_num, message = match.groups()
                if tests and "test.go" in file_path:
                    warning = {
                        'file': file_path.strip(),
                        'line': int(line_num),
                        'column': int(col_num),
                        'message': message.strip()
                    }
                    warnings.append(warning)

        # Analyze the types of warnings
        warning_messages = [warning['message'] for warning in warnings]
        warning_counts = Counter(None)

        # Print the analysis of warning types
        print("\nWarning Types and Counts:")
        for message, count in warning_counts.items():
            print(f"{message}: {count} occurrence(s)")

        # Optionally, return the success status and the list of warnings
        return CompileStatus.OK if success else CompileStatus.SYNTAX_ERROR, warnings
    except:
        return CompileStatus.EXCEPTION_OCCURRED, None


def x_check_for_warnings__mutmut_44(project_dir, tests=True):
    try:
        vet_cmd = "go vet ./..."

        print("Running go vet...")
        # Run the go vet command and capture output
        process = subprocess.Popen(
            vet_cmd,
            shell=True,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            text=True  # Ensures the output is in string format
        )
        stdout, stderr = process.communicate()

        output = stderr.strip()

        # Check the return code to determine if go vet succeeded
        success = process.returncode == 0

        # Parse the warnings from the output
        warnings = []
        pattern = r'^(?:vet: )?(.*):(\d+):(\d+): (.*)$'

        print(output)
        for line in output.splitlines():
            match = re.match(pattern, line)
            if match:
                file_path, line_num, col_num, message = match.groups()
                if tests and "test.go" in file_path:
                    warning = {
                        'file': file_path.strip(),
                        'line': int(line_num),
                        'column': int(col_num),
                        'message': message.strip()
                    }
                    warnings.append(warning)

        # Analyze the types of warnings
        warning_messages = [warning['message'] for warning in warnings]
        warning_counts = None

        # Print the analysis of warning types
        print("\nWarning Types and Counts:")
        for message, count in warning_counts.items():
            print(f"{message}: {count} occurrence(s)")

        # Optionally, return the success status and the list of warnings
        return CompileStatus.OK if success else CompileStatus.SYNTAX_ERROR, warnings
    except:
        return CompileStatus.EXCEPTION_OCCURRED, None


def x_check_for_warnings__mutmut_45(project_dir, tests=True):
    try:
        vet_cmd = "go vet ./..."

        print("Running go vet...")
        # Run the go vet command and capture output
        process = subprocess.Popen(
            vet_cmd,
            shell=True,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            text=True  # Ensures the output is in string format
        )
        stdout, stderr = process.communicate()

        output = stderr.strip()

        # Check the return code to determine if go vet succeeded
        success = process.returncode == 0

        # Parse the warnings from the output
        warnings = []
        pattern = r'^(?:vet: )?(.*):(\d+):(\d+): (.*)$'

        print(output)
        for line in output.splitlines():
            match = re.match(pattern, line)
            if match:
                file_path, line_num, col_num, message = match.groups()
                if tests and "test.go" in file_path:
                    warning = {
                        'file': file_path.strip(),
                        'line': int(line_num),
                        'column': int(col_num),
                        'message': message.strip()
                    }
                    warnings.append(warning)

        # Analyze the types of warnings
        warning_messages = [warning['message'] for warning in warnings]
        warning_counts = Counter(warning_messages)

        # Print the analysis of warning types
        print("XX\nWarning Types and Counts:XX")
        for message, count in warning_counts.items():
            print(f"{message}: {count} occurrence(s)")

        # Optionally, return the success status and the list of warnings
        return CompileStatus.OK if success else CompileStatus.SYNTAX_ERROR, warnings
    except:
        return CompileStatus.EXCEPTION_OCCURRED, None

x_check_for_warnings__mutmut_mutants = {
'x_check_for_warnings__mutmut_1': x_check_for_warnings__mutmut_1, 
    'x_check_for_warnings__mutmut_2': x_check_for_warnings__mutmut_2, 
    'x_check_for_warnings__mutmut_3': x_check_for_warnings__mutmut_3, 
    'x_check_for_warnings__mutmut_4': x_check_for_warnings__mutmut_4, 
    'x_check_for_warnings__mutmut_5': x_check_for_warnings__mutmut_5, 
    'x_check_for_warnings__mutmut_6': x_check_for_warnings__mutmut_6, 
    'x_check_for_warnings__mutmut_7': x_check_for_warnings__mutmut_7, 
    'x_check_for_warnings__mutmut_8': x_check_for_warnings__mutmut_8, 
    'x_check_for_warnings__mutmut_9': x_check_for_warnings__mutmut_9, 
    'x_check_for_warnings__mutmut_10': x_check_for_warnings__mutmut_10, 
    'x_check_for_warnings__mutmut_11': x_check_for_warnings__mutmut_11, 
    'x_check_for_warnings__mutmut_12': x_check_for_warnings__mutmut_12, 
    'x_check_for_warnings__mutmut_13': x_check_for_warnings__mutmut_13, 
    'x_check_for_warnings__mutmut_14': x_check_for_warnings__mutmut_14, 
    'x_check_for_warnings__mutmut_15': x_check_for_warnings__mutmut_15, 
    'x_check_for_warnings__mutmut_16': x_check_for_warnings__mutmut_16, 
    'x_check_for_warnings__mutmut_17': x_check_for_warnings__mutmut_17, 
    'x_check_for_warnings__mutmut_18': x_check_for_warnings__mutmut_18, 
    'x_check_for_warnings__mutmut_19': x_check_for_warnings__mutmut_19, 
    'x_check_for_warnings__mutmut_20': x_check_for_warnings__mutmut_20, 
    'x_check_for_warnings__mutmut_21': x_check_for_warnings__mutmut_21, 
    'x_check_for_warnings__mutmut_22': x_check_for_warnings__mutmut_22, 
    'x_check_for_warnings__mutmut_23': x_check_for_warnings__mutmut_23, 
    'x_check_for_warnings__mutmut_24': x_check_for_warnings__mutmut_24, 
    'x_check_for_warnings__mutmut_25': x_check_for_warnings__mutmut_25, 
    'x_check_for_warnings__mutmut_26': x_check_for_warnings__mutmut_26, 
    'x_check_for_warnings__mutmut_27': x_check_for_warnings__mutmut_27, 
    'x_check_for_warnings__mutmut_28': x_check_for_warnings__mutmut_28, 
    'x_check_for_warnings__mutmut_29': x_check_for_warnings__mutmut_29, 
    'x_check_for_warnings__mutmut_30': x_check_for_warnings__mutmut_30, 
    'x_check_for_warnings__mutmut_31': x_check_for_warnings__mutmut_31, 
    'x_check_for_warnings__mutmut_32': x_check_for_warnings__mutmut_32, 
    'x_check_for_warnings__mutmut_33': x_check_for_warnings__mutmut_33, 
    'x_check_for_warnings__mutmut_34': x_check_for_warnings__mutmut_34, 
    'x_check_for_warnings__mutmut_35': x_check_for_warnings__mutmut_35, 
    'x_check_for_warnings__mutmut_36': x_check_for_warnings__mutmut_36, 
    'x_check_for_warnings__mutmut_37': x_check_for_warnings__mutmut_37, 
    'x_check_for_warnings__mutmut_38': x_check_for_warnings__mutmut_38, 
    'x_check_for_warnings__mutmut_39': x_check_for_warnings__mutmut_39, 
    'x_check_for_warnings__mutmut_40': x_check_for_warnings__mutmut_40, 
    'x_check_for_warnings__mutmut_41': x_check_for_warnings__mutmut_41, 
    'x_check_for_warnings__mutmut_42': x_check_for_warnings__mutmut_42, 
    'x_check_for_warnings__mutmut_43': x_check_for_warnings__mutmut_43, 
    'x_check_for_warnings__mutmut_44': x_check_for_warnings__mutmut_44, 
    'x_check_for_warnings__mutmut_45': x_check_for_warnings__mutmut_45
}

def check_for_warnings(*args, **kwargs):
    result = _mutmut_trampoline(x_check_for_warnings__mutmut_orig, x_check_for_warnings__mutmut_mutants, *args, **kwargs)
    return result 

check_for_warnings.__signature__ = _mutmut_signature(x_check_for_warnings__mutmut_orig)
x_check_for_warnings__mutmut_orig.__name__ = 'x_check_for_warnings'




def x_build_project__mutmut_orig(project_dir):
    build_cmd = "go build ./..."

    print("Building the project...")
    build_out, build_err, return_code = run_command(build_cmd, cwd=project_dir)
    print(build_out)
    print(build_err)
    return return_code


def x_build_project__mutmut_1(project_dir):
    build_cmd = "XXgo build ./...XX"

    print("Building the project...")
    build_out, build_err, return_code = run_command(build_cmd, cwd=project_dir)
    print(build_out)
    print(build_err)
    return return_code


def x_build_project__mutmut_2(project_dir):
    build_cmd = None

    print("Building the project...")
    build_out, build_err, return_code = run_command(build_cmd, cwd=project_dir)
    print(build_out)
    print(build_err)
    return return_code


def x_build_project__mutmut_3(project_dir):
    build_cmd = "go build ./..."

    print("XXBuilding the project...XX")
    build_out, build_err, return_code = run_command(build_cmd, cwd=project_dir)
    print(build_out)
    print(build_err)
    return return_code


def x_build_project__mutmut_4(project_dir):
    build_cmd = "go build ./..."

    print("Building the project...")
    build_out, build_err, return_code = run_command(None, cwd=project_dir)
    print(build_out)
    print(build_err)
    return return_code


def x_build_project__mutmut_5(project_dir):
    build_cmd = "go build ./..."

    print("Building the project...")
    build_out, build_err, return_code = run_command(build_cmd, cwd=None)
    print(build_out)
    print(build_err)
    return return_code


def x_build_project__mutmut_6(project_dir):
    build_cmd = "go build ./..."

    print("Building the project...")
    build_out, build_err, return_code = run_command( cwd=project_dir)
    print(build_out)
    print(build_err)
    return return_code


def x_build_project__mutmut_7(project_dir):
    build_cmd = "go build ./..."

    print("Building the project...")
    build_out, build_err, return_code = run_command(build_cmd,)
    print(build_out)
    print(build_err)
    return return_code


def x_build_project__mutmut_8(project_dir):
    build_cmd = "go build ./..."

    print("Building the project...")
    build_out, build_err, return_code = None
    print(build_out)
    print(build_err)
    return return_code


def x_build_project__mutmut_9(project_dir):
    build_cmd = "go build ./..."

    print("Building the project...")
    build_out, build_err, return_code = run_command(build_cmd, cwd=project_dir)
    print(None)
    print(build_err)
    return return_code


def x_build_project__mutmut_10(project_dir):
    build_cmd = "go build ./..."

    print("Building the project...")
    build_out, build_err, return_code = run_command(build_cmd, cwd=project_dir)
    print(build_out)
    print(None)
    return return_code

x_build_project__mutmut_mutants = {
'x_build_project__mutmut_1': x_build_project__mutmut_1, 
    'x_build_project__mutmut_2': x_build_project__mutmut_2, 
    'x_build_project__mutmut_3': x_build_project__mutmut_3, 
    'x_build_project__mutmut_4': x_build_project__mutmut_4, 
    'x_build_project__mutmut_5': x_build_project__mutmut_5, 
    'x_build_project__mutmut_6': x_build_project__mutmut_6, 
    'x_build_project__mutmut_7': x_build_project__mutmut_7, 
    'x_build_project__mutmut_8': x_build_project__mutmut_8, 
    'x_build_project__mutmut_9': x_build_project__mutmut_9, 
    'x_build_project__mutmut_10': x_build_project__mutmut_10
}

def build_project(*args, **kwargs):
    result = _mutmut_trampoline(x_build_project__mutmut_orig, x_build_project__mutmut_mutants, *args, **kwargs)
    return result 

build_project.__signature__ = _mutmut_signature(x_build_project__mutmut_orig)
x_build_project__mutmut_orig.__name__ = 'x_build_project'




def x_run_tests__mutmut_orig(project_dir):
    test_cmd = ["go", "test", "-timeout", "30s", "-json", "-cover", "./..."]
    runtime_errors = 0

    print("Running tests...")
    try:
        result = subprocess.run(
            test_cmd,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            cwd=project_dir,
            text=True
        )
        output = result.stdout
        print(output)
        stderr = result.stderr
        print(stderr)

        # Check if the subprocess exited with a non-zero return code
        if result.returncode != 0:
            # Parse output for timeout messages
            if "test timed out after" in output or "panic: test timed out" in output:
                print("Tests failed due to timeout")
                # Handle timeout scenario
                return {
                    "test_pass_rate": 0,
                    "execution_time_sec": None,
                    "line_coverage_percent": None,
                    "branch_coverage_percent": None,
                    "timeout": True,
                    "internal_error_occurred": False,
                    "runtime_errors_count": None
                }
            else:
                runtime_error_pattern = re.compile(
                    r"(panic:|fatal error:|runtime error:)", re.IGNORECASE
                )
                assertion_error_pattern = re.compile(r"assert")

                # Search for runtime errors in stdout and stderr
                for o in (output, stderr):
                    for line in o.splitlines():
                        # Match lines with runtime errors but exclude assertions
                        if runtime_error_pattern.search(line) and not assertion_error_pattern.search(line):
                            runtime_errors = runtime_errors + 1

        # Parse the JSON output
        passed_tests = 0
        failed_tests = 0
        total_time = 0.0
        coverage = None
        summary_time = None

        for line in output.splitlines():
            if not line.strip():
                continue
            try:
                data = json.loads(line)
            except json.JSONDecodeError:
                continue  # Skip lines that aren't valid JSON

            # Check for test results
            if data.get('Action') == 'pass' and 'Test' in data:
                passed_tests += 1
                total_time += data.get('Elapsed', 0)
            elif data.get('Action') == 'fail' and 'Test' in data:
                failed_tests += 1
                total_time += data.get('Elapsed', 0)

            if (data.get('Action') == 'pass' or data.get('Action') == 'fail') and 'Test' not in data and 'Elapsed' in data:
                summary_time = data["Elapsed"]

            # Check for coverage information
            if data.get('Action') == 'output' and 'Output' in data:
                output_line = data['Output'].strip()
                if output_line.startswith("coverage:"):
                    # Extract the coverage percentage
                    match = re.search(r'coverage: ([\d\.]+)%', output_line)
                    if match:
                        coverage = float(match.group(1))

            # Alternative: Some versions include 'Coverage' field
            elif data.get('Action') == 'pass' and 'Coverage' in data:
                coverage = data['Coverage']

        total_tests = passed_tests + failed_tests
        pass_percentage = round((passed_tests / total_tests) * 100, 2) if total_tests > 0 else 0

        v = {
            "test_pass_rate": pass_percentage,
            "execution_time_sec": summary_time if summary_time is not None else total_time,
            "line_coverage_percent": coverage,
            "branch_coverage_percent": None,
            "timeout": False,
            "internal_error_occurred": False,
            "runtime_errors_count": runtime_errors
        }
        return v
    except Exception as e:
        print(f"Error running Go tests: {e}")
        return {
            "test_pass_rate": 0,
            "execution_time_sec": None,
            "line_coverage_percent": None,
            "branch_coverage_percent": None,
            "timeout": True,
            "internal_error_occurred": True,
            "runtime_errors_count": runtime_errors
        }


def x_run_tests__mutmut_1(project_dir):
    test_cmd = ["XXgoXX", "test", "-timeout", "30s", "-json", "-cover", "./..."]
    runtime_errors = 0

    print("Running tests...")
    try:
        result = subprocess.run(
            test_cmd,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            cwd=project_dir,
            text=True
        )
        output = result.stdout
        print(output)
        stderr = result.stderr
        print(stderr)

        # Check if the subprocess exited with a non-zero return code
        if result.returncode != 0:
            # Parse output for timeout messages
            if "test timed out after" in output or "panic: test timed out" in output:
                print("Tests failed due to timeout")
                # Handle timeout scenario
                return {
                    "test_pass_rate": 0,
                    "execution_time_sec": None,
                    "line_coverage_percent": None,
                    "branch_coverage_percent": None,
                    "timeout": True,
                    "internal_error_occurred": False,
                    "runtime_errors_count": None
                }
            else:
                runtime_error_pattern = re.compile(
                    r"(panic:|fatal error:|runtime error:)", re.IGNORECASE
                )
                assertion_error_pattern = re.compile(r"assert")

                # Search for runtime errors in stdout and stderr
                for o in (output, stderr):
                    for line in o.splitlines():
                        # Match lines with runtime errors but exclude assertions
                        if runtime_error_pattern.search(line) and not assertion_error_pattern.search(line):
                            runtime_errors = runtime_errors + 1

        # Parse the JSON output
        passed_tests = 0
        failed_tests = 0
        total_time = 0.0
        coverage = None
        summary_time = None

        for line in output.splitlines():
            if not line.strip():
                continue
            try:
                data = json.loads(line)
            except json.JSONDecodeError:
                continue  # Skip lines that aren't valid JSON

            # Check for test results
            if data.get('Action') == 'pass' and 'Test' in data:
                passed_tests += 1
                total_time += data.get('Elapsed', 0)
            elif data.get('Action') == 'fail' and 'Test' in data:
                failed_tests += 1
                total_time += data.get('Elapsed', 0)

            if (data.get('Action') == 'pass' or data.get('Action') == 'fail') and 'Test' not in data and 'Elapsed' in data:
                summary_time = data["Elapsed"]

            # Check for coverage information
            if data.get('Action') == 'output' and 'Output' in data:
                output_line = data['Output'].strip()
                if output_line.startswith("coverage:"):
                    # Extract the coverage percentage
                    match = re.search(r'coverage: ([\d\.]+)%', output_line)
                    if match:
                        coverage = float(match.group(1))

            # Alternative: Some versions include 'Coverage' field
            elif data.get('Action') == 'pass' and 'Coverage' in data:
                coverage = data['Coverage']

        total_tests = passed_tests + failed_tests
        pass_percentage = round((passed_tests / total_tests) * 100, 2) if total_tests > 0 else 0

        v = {
            "test_pass_rate": pass_percentage,
            "execution_time_sec": summary_time if summary_time is not None else total_time,
            "line_coverage_percent": coverage,
            "branch_coverage_percent": None,
            "timeout": False,
            "internal_error_occurred": False,
            "runtime_errors_count": runtime_errors
        }
        return v
    except Exception as e:
        print(f"Error running Go tests: {e}")
        return {
            "test_pass_rate": 0,
            "execution_time_sec": None,
            "line_coverage_percent": None,
            "branch_coverage_percent": None,
            "timeout": True,
            "internal_error_occurred": True,
            "runtime_errors_count": runtime_errors
        }


def x_run_tests__mutmut_2(project_dir):
    test_cmd = ["go", "XXtestXX", "-timeout", "30s", "-json", "-cover", "./..."]
    runtime_errors = 0

    print("Running tests...")
    try:
        result = subprocess.run(
            test_cmd,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            cwd=project_dir,
            text=True
        )
        output = result.stdout
        print(output)
        stderr = result.stderr
        print(stderr)

        # Check if the subprocess exited with a non-zero return code
        if result.returncode != 0:
            # Parse output for timeout messages
            if "test timed out after" in output or "panic: test timed out" in output:
                print("Tests failed due to timeout")
                # Handle timeout scenario
                return {
                    "test_pass_rate": 0,
                    "execution_time_sec": None,
                    "line_coverage_percent": None,
                    "branch_coverage_percent": None,
                    "timeout": True,
                    "internal_error_occurred": False,
                    "runtime_errors_count": None
                }
            else:
                runtime_error_pattern = re.compile(
                    r"(panic:|fatal error:|runtime error:)", re.IGNORECASE
                )
                assertion_error_pattern = re.compile(r"assert")

                # Search for runtime errors in stdout and stderr
                for o in (output, stderr):
                    for line in o.splitlines():
                        # Match lines with runtime errors but exclude assertions
                        if runtime_error_pattern.search(line) and not assertion_error_pattern.search(line):
                            runtime_errors = runtime_errors + 1

        # Parse the JSON output
        passed_tests = 0
        failed_tests = 0
        total_time = 0.0
        coverage = None
        summary_time = None

        for line in output.splitlines():
            if not line.strip():
                continue
            try:
                data = json.loads(line)
            except json.JSONDecodeError:
                continue  # Skip lines that aren't valid JSON

            # Check for test results
            if data.get('Action') == 'pass' and 'Test' in data:
                passed_tests += 1
                total_time += data.get('Elapsed', 0)
            elif data.get('Action') == 'fail' and 'Test' in data:
                failed_tests += 1
                total_time += data.get('Elapsed', 0)

            if (data.get('Action') == 'pass' or data.get('Action') == 'fail') and 'Test' not in data and 'Elapsed' in data:
                summary_time = data["Elapsed"]

            # Check for coverage information
            if data.get('Action') == 'output' and 'Output' in data:
                output_line = data['Output'].strip()
                if output_line.startswith("coverage:"):
                    # Extract the coverage percentage
                    match = re.search(r'coverage: ([\d\.]+)%', output_line)
                    if match:
                        coverage = float(match.group(1))

            # Alternative: Some versions include 'Coverage' field
            elif data.get('Action') == 'pass' and 'Coverage' in data:
                coverage = data['Coverage']

        total_tests = passed_tests + failed_tests
        pass_percentage = round((passed_tests / total_tests) * 100, 2) if total_tests > 0 else 0

        v = {
            "test_pass_rate": pass_percentage,
            "execution_time_sec": summary_time if summary_time is not None else total_time,
            "line_coverage_percent": coverage,
            "branch_coverage_percent": None,
            "timeout": False,
            "internal_error_occurred": False,
            "runtime_errors_count": runtime_errors
        }
        return v
    except Exception as e:
        print(f"Error running Go tests: {e}")
        return {
            "test_pass_rate": 0,
            "execution_time_sec": None,
            "line_coverage_percent": None,
            "branch_coverage_percent": None,
            "timeout": True,
            "internal_error_occurred": True,
            "runtime_errors_count": runtime_errors
        }


def x_run_tests__mutmut_3(project_dir):
    test_cmd = ["go", "test", "XX-timeoutXX", "30s", "-json", "-cover", "./..."]
    runtime_errors = 0

    print("Running tests...")
    try:
        result = subprocess.run(
            test_cmd,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            cwd=project_dir,
            text=True
        )
        output = result.stdout
        print(output)
        stderr = result.stderr
        print(stderr)

        # Check if the subprocess exited with a non-zero return code
        if result.returncode != 0:
            # Parse output for timeout messages
            if "test timed out after" in output or "panic: test timed out" in output:
                print("Tests failed due to timeout")
                # Handle timeout scenario
                return {
                    "test_pass_rate": 0,
                    "execution_time_sec": None,
                    "line_coverage_percent": None,
                    "branch_coverage_percent": None,
                    "timeout": True,
                    "internal_error_occurred": False,
                    "runtime_errors_count": None
                }
            else:
                runtime_error_pattern = re.compile(
                    r"(panic:|fatal error:|runtime error:)", re.IGNORECASE
                )
                assertion_error_pattern = re.compile(r"assert")

                # Search for runtime errors in stdout and stderr
                for o in (output, stderr):
                    for line in o.splitlines():
                        # Match lines with runtime errors but exclude assertions
                        if runtime_error_pattern.search(line) and not assertion_error_pattern.search(line):
                            runtime_errors = runtime_errors + 1

        # Parse the JSON output
        passed_tests = 0
        failed_tests = 0
        total_time = 0.0
        coverage = None
        summary_time = None

        for line in output.splitlines():
            if not line.strip():
                continue
            try:
                data = json.loads(line)
            except json.JSONDecodeError:
                continue  # Skip lines that aren't valid JSON

            # Check for test results
            if data.get('Action') == 'pass' and 'Test' in data:
                passed_tests += 1
                total_time += data.get('Elapsed', 0)
            elif data.get('Action') == 'fail' and 'Test' in data:
                failed_tests += 1
                total_time += data.get('Elapsed', 0)

            if (data.get('Action') == 'pass' or data.get('Action') == 'fail') and 'Test' not in data and 'Elapsed' in data:
                summary_time = data["Elapsed"]

            # Check for coverage information
            if data.get('Action') == 'output' and 'Output' in data:
                output_line = data['Output'].strip()
                if output_line.startswith("coverage:"):
                    # Extract the coverage percentage
                    match = re.search(r'coverage: ([\d\.]+)%', output_line)
                    if match:
                        coverage = float(match.group(1))

            # Alternative: Some versions include 'Coverage' field
            elif data.get('Action') == 'pass' and 'Coverage' in data:
                coverage = data['Coverage']

        total_tests = passed_tests + failed_tests
        pass_percentage = round((passed_tests / total_tests) * 100, 2) if total_tests > 0 else 0

        v = {
            "test_pass_rate": pass_percentage,
            "execution_time_sec": summary_time if summary_time is not None else total_time,
            "line_coverage_percent": coverage,
            "branch_coverage_percent": None,
            "timeout": False,
            "internal_error_occurred": False,
            "runtime_errors_count": runtime_errors
        }
        return v
    except Exception as e:
        print(f"Error running Go tests: {e}")
        return {
            "test_pass_rate": 0,
            "execution_time_sec": None,
            "line_coverage_percent": None,
            "branch_coverage_percent": None,
            "timeout": True,
            "internal_error_occurred": True,
            "runtime_errors_count": runtime_errors
        }


def x_run_tests__mutmut_4(project_dir):
    test_cmd = ["go", "test", "-timeout", "XX30sXX", "-json", "-cover", "./..."]
    runtime_errors = 0

    print("Running tests...")
    try:
        result = subprocess.run(
            test_cmd,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            cwd=project_dir,
            text=True
        )
        output = result.stdout
        print(output)
        stderr = result.stderr
        print(stderr)

        # Check if the subprocess exited with a non-zero return code
        if result.returncode != 0:
            # Parse output for timeout messages
            if "test timed out after" in output or "panic: test timed out" in output:
                print("Tests failed due to timeout")
                # Handle timeout scenario
                return {
                    "test_pass_rate": 0,
                    "execution_time_sec": None,
                    "line_coverage_percent": None,
                    "branch_coverage_percent": None,
                    "timeout": True,
                    "internal_error_occurred": False,
                    "runtime_errors_count": None
                }
            else:
                runtime_error_pattern = re.compile(
                    r"(panic:|fatal error:|runtime error:)", re.IGNORECASE
                )
                assertion_error_pattern = re.compile(r"assert")

                # Search for runtime errors in stdout and stderr
                for o in (output, stderr):
                    for line in o.splitlines():
                        # Match lines with runtime errors but exclude assertions
                        if runtime_error_pattern.search(line) and not assertion_error_pattern.search(line):
                            runtime_errors = runtime_errors + 1

        # Parse the JSON output
        passed_tests = 0
        failed_tests = 0
        total_time = 0.0
        coverage = None
        summary_time = None

        for line in output.splitlines():
            if not line.strip():
                continue
            try:
                data = json.loads(line)
            except json.JSONDecodeError:
                continue  # Skip lines that aren't valid JSON

            # Check for test results
            if data.get('Action') == 'pass' and 'Test' in data:
                passed_tests += 1
                total_time += data.get('Elapsed', 0)
            elif data.get('Action') == 'fail' and 'Test' in data:
                failed_tests += 1
                total_time += data.get('Elapsed', 0)

            if (data.get('Action') == 'pass' or data.get('Action') == 'fail') and 'Test' not in data and 'Elapsed' in data:
                summary_time = data["Elapsed"]

            # Check for coverage information
            if data.get('Action') == 'output' and 'Output' in data:
                output_line = data['Output'].strip()
                if output_line.startswith("coverage:"):
                    # Extract the coverage percentage
                    match = re.search(r'coverage: ([\d\.]+)%', output_line)
                    if match:
                        coverage = float(match.group(1))

            # Alternative: Some versions include 'Coverage' field
            elif data.get('Action') == 'pass' and 'Coverage' in data:
                coverage = data['Coverage']

        total_tests = passed_tests + failed_tests
        pass_percentage = round((passed_tests / total_tests) * 100, 2) if total_tests > 0 else 0

        v = {
            "test_pass_rate": pass_percentage,
            "execution_time_sec": summary_time if summary_time is not None else total_time,
            "line_coverage_percent": coverage,
            "branch_coverage_percent": None,
            "timeout": False,
            "internal_error_occurred": False,
            "runtime_errors_count": runtime_errors
        }
        return v
    except Exception as e:
        print(f"Error running Go tests: {e}")
        return {
            "test_pass_rate": 0,
            "execution_time_sec": None,
            "line_coverage_percent": None,
            "branch_coverage_percent": None,
            "timeout": True,
            "internal_error_occurred": True,
            "runtime_errors_count": runtime_errors
        }


def x_run_tests__mutmut_5(project_dir):
    test_cmd = ["go", "test", "-timeout", "30s", "XX-jsonXX", "-cover", "./..."]
    runtime_errors = 0

    print("Running tests...")
    try:
        result = subprocess.run(
            test_cmd,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            cwd=project_dir,
            text=True
        )
        output = result.stdout
        print(output)
        stderr = result.stderr
        print(stderr)

        # Check if the subprocess exited with a non-zero return code
        if result.returncode != 0:
            # Parse output for timeout messages
            if "test timed out after" in output or "panic: test timed out" in output:
                print("Tests failed due to timeout")
                # Handle timeout scenario
                return {
                    "test_pass_rate": 0,
                    "execution_time_sec": None,
                    "line_coverage_percent": None,
                    "branch_coverage_percent": None,
                    "timeout": True,
                    "internal_error_occurred": False,
                    "runtime_errors_count": None
                }
            else:
                runtime_error_pattern = re.compile(
                    r"(panic:|fatal error:|runtime error:)", re.IGNORECASE
                )
                assertion_error_pattern = re.compile(r"assert")

                # Search for runtime errors in stdout and stderr
                for o in (output, stderr):
                    for line in o.splitlines():
                        # Match lines with runtime errors but exclude assertions
                        if runtime_error_pattern.search(line) and not assertion_error_pattern.search(line):
                            runtime_errors = runtime_errors + 1

        # Parse the JSON output
        passed_tests = 0
        failed_tests = 0
        total_time = 0.0
        coverage = None
        summary_time = None

        for line in output.splitlines():
            if not line.strip():
                continue
            try:
                data = json.loads(line)
            except json.JSONDecodeError:
                continue  # Skip lines that aren't valid JSON

            # Check for test results
            if data.get('Action') == 'pass' and 'Test' in data:
                passed_tests += 1
                total_time += data.get('Elapsed', 0)
            elif data.get('Action') == 'fail' and 'Test' in data:
                failed_tests += 1
                total_time += data.get('Elapsed', 0)

            if (data.get('Action') == 'pass' or data.get('Action') == 'fail') and 'Test' not in data and 'Elapsed' in data:
                summary_time = data["Elapsed"]

            # Check for coverage information
            if data.get('Action') == 'output' and 'Output' in data:
                output_line = data['Output'].strip()
                if output_line.startswith("coverage:"):
                    # Extract the coverage percentage
                    match = re.search(r'coverage: ([\d\.]+)%', output_line)
                    if match:
                        coverage = float(match.group(1))

            # Alternative: Some versions include 'Coverage' field
            elif data.get('Action') == 'pass' and 'Coverage' in data:
                coverage = data['Coverage']

        total_tests = passed_tests + failed_tests
        pass_percentage = round((passed_tests / total_tests) * 100, 2) if total_tests > 0 else 0

        v = {
            "test_pass_rate": pass_percentage,
            "execution_time_sec": summary_time if summary_time is not None else total_time,
            "line_coverage_percent": coverage,
            "branch_coverage_percent": None,
            "timeout": False,
            "internal_error_occurred": False,
            "runtime_errors_count": runtime_errors
        }
        return v
    except Exception as e:
        print(f"Error running Go tests: {e}")
        return {
            "test_pass_rate": 0,
            "execution_time_sec": None,
            "line_coverage_percent": None,
            "branch_coverage_percent": None,
            "timeout": True,
            "internal_error_occurred": True,
            "runtime_errors_count": runtime_errors
        }


def x_run_tests__mutmut_6(project_dir):
    test_cmd = ["go", "test", "-timeout", "30s", "-json", "XX-coverXX", "./..."]
    runtime_errors = 0

    print("Running tests...")
    try:
        result = subprocess.run(
            test_cmd,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            cwd=project_dir,
            text=True
        )
        output = result.stdout
        print(output)
        stderr = result.stderr
        print(stderr)

        # Check if the subprocess exited with a non-zero return code
        if result.returncode != 0:
            # Parse output for timeout messages
            if "test timed out after" in output or "panic: test timed out" in output:
                print("Tests failed due to timeout")
                # Handle timeout scenario
                return {
                    "test_pass_rate": 0,
                    "execution_time_sec": None,
                    "line_coverage_percent": None,
                    "branch_coverage_percent": None,
                    "timeout": True,
                    "internal_error_occurred": False,
                    "runtime_errors_count": None
                }
            else:
                runtime_error_pattern = re.compile(
                    r"(panic:|fatal error:|runtime error:)", re.IGNORECASE
                )
                assertion_error_pattern = re.compile(r"assert")

                # Search for runtime errors in stdout and stderr
                for o in (output, stderr):
                    for line in o.splitlines():
                        # Match lines with runtime errors but exclude assertions
                        if runtime_error_pattern.search(line) and not assertion_error_pattern.search(line):
                            runtime_errors = runtime_errors + 1

        # Parse the JSON output
        passed_tests = 0
        failed_tests = 0
        total_time = 0.0
        coverage = None
        summary_time = None

        for line in output.splitlines():
            if not line.strip():
                continue
            try:
                data = json.loads(line)
            except json.JSONDecodeError:
                continue  # Skip lines that aren't valid JSON

            # Check for test results
            if data.get('Action') == 'pass' and 'Test' in data:
                passed_tests += 1
                total_time += data.get('Elapsed', 0)
            elif data.get('Action') == 'fail' and 'Test' in data:
                failed_tests += 1
                total_time += data.get('Elapsed', 0)

            if (data.get('Action') == 'pass' or data.get('Action') == 'fail') and 'Test' not in data and 'Elapsed' in data:
                summary_time = data["Elapsed"]

            # Check for coverage information
            if data.get('Action') == 'output' and 'Output' in data:
                output_line = data['Output'].strip()
                if output_line.startswith("coverage:"):
                    # Extract the coverage percentage
                    match = re.search(r'coverage: ([\d\.]+)%', output_line)
                    if match:
                        coverage = float(match.group(1))

            # Alternative: Some versions include 'Coverage' field
            elif data.get('Action') == 'pass' and 'Coverage' in data:
                coverage = data['Coverage']

        total_tests = passed_tests + failed_tests
        pass_percentage = round((passed_tests / total_tests) * 100, 2) if total_tests > 0 else 0

        v = {
            "test_pass_rate": pass_percentage,
            "execution_time_sec": summary_time if summary_time is not None else total_time,
            "line_coverage_percent": coverage,
            "branch_coverage_percent": None,
            "timeout": False,
            "internal_error_occurred": False,
            "runtime_errors_count": runtime_errors
        }
        return v
    except Exception as e:
        print(f"Error running Go tests: {e}")
        return {
            "test_pass_rate": 0,
            "execution_time_sec": None,
            "line_coverage_percent": None,
            "branch_coverage_percent": None,
            "timeout": True,
            "internal_error_occurred": True,
            "runtime_errors_count": runtime_errors
        }


def x_run_tests__mutmut_7(project_dir):
    test_cmd = ["go", "test", "-timeout", "30s", "-json", "-cover", "XX./...XX"]
    runtime_errors = 0

    print("Running tests...")
    try:
        result = subprocess.run(
            test_cmd,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            cwd=project_dir,
            text=True
        )
        output = result.stdout
        print(output)
        stderr = result.stderr
        print(stderr)

        # Check if the subprocess exited with a non-zero return code
        if result.returncode != 0:
            # Parse output for timeout messages
            if "test timed out after" in output or "panic: test timed out" in output:
                print("Tests failed due to timeout")
                # Handle timeout scenario
                return {
                    "test_pass_rate": 0,
                    "execution_time_sec": None,
                    "line_coverage_percent": None,
                    "branch_coverage_percent": None,
                    "timeout": True,
                    "internal_error_occurred": False,
                    "runtime_errors_count": None
                }
            else:
                runtime_error_pattern = re.compile(
                    r"(panic:|fatal error:|runtime error:)", re.IGNORECASE
                )
                assertion_error_pattern = re.compile(r"assert")

                # Search for runtime errors in stdout and stderr
                for o in (output, stderr):
                    for line in o.splitlines():
                        # Match lines with runtime errors but exclude assertions
                        if runtime_error_pattern.search(line) and not assertion_error_pattern.search(line):
                            runtime_errors = runtime_errors + 1

        # Parse the JSON output
        passed_tests = 0
        failed_tests = 0
        total_time = 0.0
        coverage = None
        summary_time = None

        for line in output.splitlines():
            if not line.strip():
                continue
            try:
                data = json.loads(line)
            except json.JSONDecodeError:
                continue  # Skip lines that aren't valid JSON

            # Check for test results
            if data.get('Action') == 'pass' and 'Test' in data:
                passed_tests += 1
                total_time += data.get('Elapsed', 0)
            elif data.get('Action') == 'fail' and 'Test' in data:
                failed_tests += 1
                total_time += data.get('Elapsed', 0)

            if (data.get('Action') == 'pass' or data.get('Action') == 'fail') and 'Test' not in data and 'Elapsed' in data:
                summary_time = data["Elapsed"]

            # Check for coverage information
            if data.get('Action') == 'output' and 'Output' in data:
                output_line = data['Output'].strip()
                if output_line.startswith("coverage:"):
                    # Extract the coverage percentage
                    match = re.search(r'coverage: ([\d\.]+)%', output_line)
                    if match:
                        coverage = float(match.group(1))

            # Alternative: Some versions include 'Coverage' field
            elif data.get('Action') == 'pass' and 'Coverage' in data:
                coverage = data['Coverage']

        total_tests = passed_tests + failed_tests
        pass_percentage = round((passed_tests / total_tests) * 100, 2) if total_tests > 0 else 0

        v = {
            "test_pass_rate": pass_percentage,
            "execution_time_sec": summary_time if summary_time is not None else total_time,
            "line_coverage_percent": coverage,
            "branch_coverage_percent": None,
            "timeout": False,
            "internal_error_occurred": False,
            "runtime_errors_count": runtime_errors
        }
        return v
    except Exception as e:
        print(f"Error running Go tests: {e}")
        return {
            "test_pass_rate": 0,
            "execution_time_sec": None,
            "line_coverage_percent": None,
            "branch_coverage_percent": None,
            "timeout": True,
            "internal_error_occurred": True,
            "runtime_errors_count": runtime_errors
        }


def x_run_tests__mutmut_8(project_dir):
    test_cmd = None
    runtime_errors = 0

    print("Running tests...")
    try:
        result = subprocess.run(
            test_cmd,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            cwd=project_dir,
            text=True
        )
        output = result.stdout
        print(output)
        stderr = result.stderr
        print(stderr)

        # Check if the subprocess exited with a non-zero return code
        if result.returncode != 0:
            # Parse output for timeout messages
            if "test timed out after" in output or "panic: test timed out" in output:
                print("Tests failed due to timeout")
                # Handle timeout scenario
                return {
                    "test_pass_rate": 0,
                    "execution_time_sec": None,
                    "line_coverage_percent": None,
                    "branch_coverage_percent": None,
                    "timeout": True,
                    "internal_error_occurred": False,
                    "runtime_errors_count": None
                }
            else:
                runtime_error_pattern = re.compile(
                    r"(panic:|fatal error:|runtime error:)", re.IGNORECASE
                )
                assertion_error_pattern = re.compile(r"assert")

                # Search for runtime errors in stdout and stderr
                for o in (output, stderr):
                    for line in o.splitlines():
                        # Match lines with runtime errors but exclude assertions
                        if runtime_error_pattern.search(line) and not assertion_error_pattern.search(line):
                            runtime_errors = runtime_errors + 1

        # Parse the JSON output
        passed_tests = 0
        failed_tests = 0
        total_time = 0.0
        coverage = None
        summary_time = None

        for line in output.splitlines():
            if not line.strip():
                continue
            try:
                data = json.loads(line)
            except json.JSONDecodeError:
                continue  # Skip lines that aren't valid JSON

            # Check for test results
            if data.get('Action') == 'pass' and 'Test' in data:
                passed_tests += 1
                total_time += data.get('Elapsed', 0)
            elif data.get('Action') == 'fail' and 'Test' in data:
                failed_tests += 1
                total_time += data.get('Elapsed', 0)

            if (data.get('Action') == 'pass' or data.get('Action') == 'fail') and 'Test' not in data and 'Elapsed' in data:
                summary_time = data["Elapsed"]

            # Check for coverage information
            if data.get('Action') == 'output' and 'Output' in data:
                output_line = data['Output'].strip()
                if output_line.startswith("coverage:"):
                    # Extract the coverage percentage
                    match = re.search(r'coverage: ([\d\.]+)%', output_line)
                    if match:
                        coverage = float(match.group(1))

            # Alternative: Some versions include 'Coverage' field
            elif data.get('Action') == 'pass' and 'Coverage' in data:
                coverage = data['Coverage']

        total_tests = passed_tests + failed_tests
        pass_percentage = round((passed_tests / total_tests) * 100, 2) if total_tests > 0 else 0

        v = {
            "test_pass_rate": pass_percentage,
            "execution_time_sec": summary_time if summary_time is not None else total_time,
            "line_coverage_percent": coverage,
            "branch_coverage_percent": None,
            "timeout": False,
            "internal_error_occurred": False,
            "runtime_errors_count": runtime_errors
        }
        return v
    except Exception as e:
        print(f"Error running Go tests: {e}")
        return {
            "test_pass_rate": 0,
            "execution_time_sec": None,
            "line_coverage_percent": None,
            "branch_coverage_percent": None,
            "timeout": True,
            "internal_error_occurred": True,
            "runtime_errors_count": runtime_errors
        }


def x_run_tests__mutmut_9(project_dir):
    test_cmd = ["go", "test", "-timeout", "30s", "-json", "-cover", "./..."]
    runtime_errors = 1

    print("Running tests...")
    try:
        result = subprocess.run(
            test_cmd,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            cwd=project_dir,
            text=True
        )
        output = result.stdout
        print(output)
        stderr = result.stderr
        print(stderr)

        # Check if the subprocess exited with a non-zero return code
        if result.returncode != 0:
            # Parse output for timeout messages
            if "test timed out after" in output or "panic: test timed out" in output:
                print("Tests failed due to timeout")
                # Handle timeout scenario
                return {
                    "test_pass_rate": 0,
                    "execution_time_sec": None,
                    "line_coverage_percent": None,
                    "branch_coverage_percent": None,
                    "timeout": True,
                    "internal_error_occurred": False,
                    "runtime_errors_count": None
                }
            else:
                runtime_error_pattern = re.compile(
                    r"(panic:|fatal error:|runtime error:)", re.IGNORECASE
                )
                assertion_error_pattern = re.compile(r"assert")

                # Search for runtime errors in stdout and stderr
                for o in (output, stderr):
                    for line in o.splitlines():
                        # Match lines with runtime errors but exclude assertions
                        if runtime_error_pattern.search(line) and not assertion_error_pattern.search(line):
                            runtime_errors = runtime_errors + 1

        # Parse the JSON output
        passed_tests = 0
        failed_tests = 0
        total_time = 0.0
        coverage = None
        summary_time = None

        for line in output.splitlines():
            if not line.strip():
                continue
            try:
                data = json.loads(line)
            except json.JSONDecodeError:
                continue  # Skip lines that aren't valid JSON

            # Check for test results
            if data.get('Action') == 'pass' and 'Test' in data:
                passed_tests += 1
                total_time += data.get('Elapsed', 0)
            elif data.get('Action') == 'fail' and 'Test' in data:
                failed_tests += 1
                total_time += data.get('Elapsed', 0)

            if (data.get('Action') == 'pass' or data.get('Action') == 'fail') and 'Test' not in data and 'Elapsed' in data:
                summary_time = data["Elapsed"]

            # Check for coverage information
            if data.get('Action') == 'output' and 'Output' in data:
                output_line = data['Output'].strip()
                if output_line.startswith("coverage:"):
                    # Extract the coverage percentage
                    match = re.search(r'coverage: ([\d\.]+)%', output_line)
                    if match:
                        coverage = float(match.group(1))

            # Alternative: Some versions include 'Coverage' field
            elif data.get('Action') == 'pass' and 'Coverage' in data:
                coverage = data['Coverage']

        total_tests = passed_tests + failed_tests
        pass_percentage = round((passed_tests / total_tests) * 100, 2) if total_tests > 0 else 0

        v = {
            "test_pass_rate": pass_percentage,
            "execution_time_sec": summary_time if summary_time is not None else total_time,
            "line_coverage_percent": coverage,
            "branch_coverage_percent": None,
            "timeout": False,
            "internal_error_occurred": False,
            "runtime_errors_count": runtime_errors
        }
        return v
    except Exception as e:
        print(f"Error running Go tests: {e}")
        return {
            "test_pass_rate": 0,
            "execution_time_sec": None,
            "line_coverage_percent": None,
            "branch_coverage_percent": None,
            "timeout": True,
            "internal_error_occurred": True,
            "runtime_errors_count": runtime_errors
        }


def x_run_tests__mutmut_10(project_dir):
    test_cmd = ["go", "test", "-timeout", "30s", "-json", "-cover", "./..."]
    runtime_errors = None

    print("Running tests...")
    try:
        result = subprocess.run(
            test_cmd,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            cwd=project_dir,
            text=True
        )
        output = result.stdout
        print(output)
        stderr = result.stderr
        print(stderr)

        # Check if the subprocess exited with a non-zero return code
        if result.returncode != 0:
            # Parse output for timeout messages
            if "test timed out after" in output or "panic: test timed out" in output:
                print("Tests failed due to timeout")
                # Handle timeout scenario
                return {
                    "test_pass_rate": 0,
                    "execution_time_sec": None,
                    "line_coverage_percent": None,
                    "branch_coverage_percent": None,
                    "timeout": True,
                    "internal_error_occurred": False,
                    "runtime_errors_count": None
                }
            else:
                runtime_error_pattern = re.compile(
                    r"(panic:|fatal error:|runtime error:)", re.IGNORECASE
                )
                assertion_error_pattern = re.compile(r"assert")

                # Search for runtime errors in stdout and stderr
                for o in (output, stderr):
                    for line in o.splitlines():
                        # Match lines with runtime errors but exclude assertions
                        if runtime_error_pattern.search(line) and not assertion_error_pattern.search(line):
                            runtime_errors = runtime_errors + 1

        # Parse the JSON output
        passed_tests = 0
        failed_tests = 0
        total_time = 0.0
        coverage = None
        summary_time = None

        for line in output.splitlines():
            if not line.strip():
                continue
            try:
                data = json.loads(line)
            except json.JSONDecodeError:
                continue  # Skip lines that aren't valid JSON

            # Check for test results
            if data.get('Action') == 'pass' and 'Test' in data:
                passed_tests += 1
                total_time += data.get('Elapsed', 0)
            elif data.get('Action') == 'fail' and 'Test' in data:
                failed_tests += 1
                total_time += data.get('Elapsed', 0)

            if (data.get('Action') == 'pass' or data.get('Action') == 'fail') and 'Test' not in data and 'Elapsed' in data:
                summary_time = data["Elapsed"]

            # Check for coverage information
            if data.get('Action') == 'output' and 'Output' in data:
                output_line = data['Output'].strip()
                if output_line.startswith("coverage:"):
                    # Extract the coverage percentage
                    match = re.search(r'coverage: ([\d\.]+)%', output_line)
                    if match:
                        coverage = float(match.group(1))

            # Alternative: Some versions include 'Coverage' field
            elif data.get('Action') == 'pass' and 'Coverage' in data:
                coverage = data['Coverage']

        total_tests = passed_tests + failed_tests
        pass_percentage = round((passed_tests / total_tests) * 100, 2) if total_tests > 0 else 0

        v = {
            "test_pass_rate": pass_percentage,
            "execution_time_sec": summary_time if summary_time is not None else total_time,
            "line_coverage_percent": coverage,
            "branch_coverage_percent": None,
            "timeout": False,
            "internal_error_occurred": False,
            "runtime_errors_count": runtime_errors
        }
        return v
    except Exception as e:
        print(f"Error running Go tests: {e}")
        return {
            "test_pass_rate": 0,
            "execution_time_sec": None,
            "line_coverage_percent": None,
            "branch_coverage_percent": None,
            "timeout": True,
            "internal_error_occurred": True,
            "runtime_errors_count": runtime_errors
        }


def x_run_tests__mutmut_11(project_dir):
    test_cmd = ["go", "test", "-timeout", "30s", "-json", "-cover", "./..."]
    runtime_errors = 0

    print("XXRunning tests...XX")
    try:
        result = subprocess.run(
            test_cmd,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            cwd=project_dir,
            text=True
        )
        output = result.stdout
        print(output)
        stderr = result.stderr
        print(stderr)

        # Check if the subprocess exited with a non-zero return code
        if result.returncode != 0:
            # Parse output for timeout messages
            if "test timed out after" in output or "panic: test timed out" in output:
                print("Tests failed due to timeout")
                # Handle timeout scenario
                return {
                    "test_pass_rate": 0,
                    "execution_time_sec": None,
                    "line_coverage_percent": None,
                    "branch_coverage_percent": None,
                    "timeout": True,
                    "internal_error_occurred": False,
                    "runtime_errors_count": None
                }
            else:
                runtime_error_pattern = re.compile(
                    r"(panic:|fatal error:|runtime error:)", re.IGNORECASE
                )
                assertion_error_pattern = re.compile(r"assert")

                # Search for runtime errors in stdout and stderr
                for o in (output, stderr):
                    for line in o.splitlines():
                        # Match lines with runtime errors but exclude assertions
                        if runtime_error_pattern.search(line) and not assertion_error_pattern.search(line):
                            runtime_errors = runtime_errors + 1

        # Parse the JSON output
        passed_tests = 0
        failed_tests = 0
        total_time = 0.0
        coverage = None
        summary_time = None

        for line in output.splitlines():
            if not line.strip():
                continue
            try:
                data = json.loads(line)
            except json.JSONDecodeError:
                continue  # Skip lines that aren't valid JSON

            # Check for test results
            if data.get('Action') == 'pass' and 'Test' in data:
                passed_tests += 1
                total_time += data.get('Elapsed', 0)
            elif data.get('Action') == 'fail' and 'Test' in data:
                failed_tests += 1
                total_time += data.get('Elapsed', 0)

            if (data.get('Action') == 'pass' or data.get('Action') == 'fail') and 'Test' not in data and 'Elapsed' in data:
                summary_time = data["Elapsed"]

            # Check for coverage information
            if data.get('Action') == 'output' and 'Output' in data:
                output_line = data['Output'].strip()
                if output_line.startswith("coverage:"):
                    # Extract the coverage percentage
                    match = re.search(r'coverage: ([\d\.]+)%', output_line)
                    if match:
                        coverage = float(match.group(1))

            # Alternative: Some versions include 'Coverage' field
            elif data.get('Action') == 'pass' and 'Coverage' in data:
                coverage = data['Coverage']

        total_tests = passed_tests + failed_tests
        pass_percentage = round((passed_tests / total_tests) * 100, 2) if total_tests > 0 else 0

        v = {
            "test_pass_rate": pass_percentage,
            "execution_time_sec": summary_time if summary_time is not None else total_time,
            "line_coverage_percent": coverage,
            "branch_coverage_percent": None,
            "timeout": False,
            "internal_error_occurred": False,
            "runtime_errors_count": runtime_errors
        }
        return v
    except Exception as e:
        print(f"Error running Go tests: {e}")
        return {
            "test_pass_rate": 0,
            "execution_time_sec": None,
            "line_coverage_percent": None,
            "branch_coverage_percent": None,
            "timeout": True,
            "internal_error_occurred": True,
            "runtime_errors_count": runtime_errors
        }


def x_run_tests__mutmut_12(project_dir):
    test_cmd = ["go", "test", "-timeout", "30s", "-json", "-cover", "./..."]
    runtime_errors = 0

    print("Running tests...")
    try:
        result = subprocess.run(
            None,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            cwd=project_dir,
            text=True
        )
        output = result.stdout
        print(output)
        stderr = result.stderr
        print(stderr)

        # Check if the subprocess exited with a non-zero return code
        if result.returncode != 0:
            # Parse output for timeout messages
            if "test timed out after" in output or "panic: test timed out" in output:
                print("Tests failed due to timeout")
                # Handle timeout scenario
                return {
                    "test_pass_rate": 0,
                    "execution_time_sec": None,
                    "line_coverage_percent": None,
                    "branch_coverage_percent": None,
                    "timeout": True,
                    "internal_error_occurred": False,
                    "runtime_errors_count": None
                }
            else:
                runtime_error_pattern = re.compile(
                    r"(panic:|fatal error:|runtime error:)", re.IGNORECASE
                )
                assertion_error_pattern = re.compile(r"assert")

                # Search for runtime errors in stdout and stderr
                for o in (output, stderr):
                    for line in o.splitlines():
                        # Match lines with runtime errors but exclude assertions
                        if runtime_error_pattern.search(line) and not assertion_error_pattern.search(line):
                            runtime_errors = runtime_errors + 1

        # Parse the JSON output
        passed_tests = 0
        failed_tests = 0
        total_time = 0.0
        coverage = None
        summary_time = None

        for line in output.splitlines():
            if not line.strip():
                continue
            try:
                data = json.loads(line)
            except json.JSONDecodeError:
                continue  # Skip lines that aren't valid JSON

            # Check for test results
            if data.get('Action') == 'pass' and 'Test' in data:
                passed_tests += 1
                total_time += data.get('Elapsed', 0)
            elif data.get('Action') == 'fail' and 'Test' in data:
                failed_tests += 1
                total_time += data.get('Elapsed', 0)

            if (data.get('Action') == 'pass' or data.get('Action') == 'fail') and 'Test' not in data and 'Elapsed' in data:
                summary_time = data["Elapsed"]

            # Check for coverage information
            if data.get('Action') == 'output' and 'Output' in data:
                output_line = data['Output'].strip()
                if output_line.startswith("coverage:"):
                    # Extract the coverage percentage
                    match = re.search(r'coverage: ([\d\.]+)%', output_line)
                    if match:
                        coverage = float(match.group(1))

            # Alternative: Some versions include 'Coverage' field
            elif data.get('Action') == 'pass' and 'Coverage' in data:
                coverage = data['Coverage']

        total_tests = passed_tests + failed_tests
        pass_percentage = round((passed_tests / total_tests) * 100, 2) if total_tests > 0 else 0

        v = {
            "test_pass_rate": pass_percentage,
            "execution_time_sec": summary_time if summary_time is not None else total_time,
            "line_coverage_percent": coverage,
            "branch_coverage_percent": None,
            "timeout": False,
            "internal_error_occurred": False,
            "runtime_errors_count": runtime_errors
        }
        return v
    except Exception as e:
        print(f"Error running Go tests: {e}")
        return {
            "test_pass_rate": 0,
            "execution_time_sec": None,
            "line_coverage_percent": None,
            "branch_coverage_percent": None,
            "timeout": True,
            "internal_error_occurred": True,
            "runtime_errors_count": runtime_errors
        }


def x_run_tests__mutmut_13(project_dir):
    test_cmd = ["go", "test", "-timeout", "30s", "-json", "-cover", "./..."]
    runtime_errors = 0

    print("Running tests...")
    try:
        result = subprocess.run(
            test_cmd,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            cwd=None,
            text=True
        )
        output = result.stdout
        print(output)
        stderr = result.stderr
        print(stderr)

        # Check if the subprocess exited with a non-zero return code
        if result.returncode != 0:
            # Parse output for timeout messages
            if "test timed out after" in output or "panic: test timed out" in output:
                print("Tests failed due to timeout")
                # Handle timeout scenario
                return {
                    "test_pass_rate": 0,
                    "execution_time_sec": None,
                    "line_coverage_percent": None,
                    "branch_coverage_percent": None,
                    "timeout": True,
                    "internal_error_occurred": False,
                    "runtime_errors_count": None
                }
            else:
                runtime_error_pattern = re.compile(
                    r"(panic:|fatal error:|runtime error:)", re.IGNORECASE
                )
                assertion_error_pattern = re.compile(r"assert")

                # Search for runtime errors in stdout and stderr
                for o in (output, stderr):
                    for line in o.splitlines():
                        # Match lines with runtime errors but exclude assertions
                        if runtime_error_pattern.search(line) and not assertion_error_pattern.search(line):
                            runtime_errors = runtime_errors + 1

        # Parse the JSON output
        passed_tests = 0
        failed_tests = 0
        total_time = 0.0
        coverage = None
        summary_time = None

        for line in output.splitlines():
            if not line.strip():
                continue
            try:
                data = json.loads(line)
            except json.JSONDecodeError:
                continue  # Skip lines that aren't valid JSON

            # Check for test results
            if data.get('Action') == 'pass' and 'Test' in data:
                passed_tests += 1
                total_time += data.get('Elapsed', 0)
            elif data.get('Action') == 'fail' and 'Test' in data:
                failed_tests += 1
                total_time += data.get('Elapsed', 0)

            if (data.get('Action') == 'pass' or data.get('Action') == 'fail') and 'Test' not in data and 'Elapsed' in data:
                summary_time = data["Elapsed"]

            # Check for coverage information
            if data.get('Action') == 'output' and 'Output' in data:
                output_line = data['Output'].strip()
                if output_line.startswith("coverage:"):
                    # Extract the coverage percentage
                    match = re.search(r'coverage: ([\d\.]+)%', output_line)
                    if match:
                        coverage = float(match.group(1))

            # Alternative: Some versions include 'Coverage' field
            elif data.get('Action') == 'pass' and 'Coverage' in data:
                coverage = data['Coverage']

        total_tests = passed_tests + failed_tests
        pass_percentage = round((passed_tests / total_tests) * 100, 2) if total_tests > 0 else 0

        v = {
            "test_pass_rate": pass_percentage,
            "execution_time_sec": summary_time if summary_time is not None else total_time,
            "line_coverage_percent": coverage,
            "branch_coverage_percent": None,
            "timeout": False,
            "internal_error_occurred": False,
            "runtime_errors_count": runtime_errors
        }
        return v
    except Exception as e:
        print(f"Error running Go tests: {e}")
        return {
            "test_pass_rate": 0,
            "execution_time_sec": None,
            "line_coverage_percent": None,
            "branch_coverage_percent": None,
            "timeout": True,
            "internal_error_occurred": True,
            "runtime_errors_count": runtime_errors
        }


def x_run_tests__mutmut_14(project_dir):
    test_cmd = ["go", "test", "-timeout", "30s", "-json", "-cover", "./..."]
    runtime_errors = 0

    print("Running tests...")
    try:
        result = subprocess.run(
            test_cmd,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            cwd=project_dir,
            text=False
        )
        output = result.stdout
        print(output)
        stderr = result.stderr
        print(stderr)

        # Check if the subprocess exited with a non-zero return code
        if result.returncode != 0:
            # Parse output for timeout messages
            if "test timed out after" in output or "panic: test timed out" in output:
                print("Tests failed due to timeout")
                # Handle timeout scenario
                return {
                    "test_pass_rate": 0,
                    "execution_time_sec": None,
                    "line_coverage_percent": None,
                    "branch_coverage_percent": None,
                    "timeout": True,
                    "internal_error_occurred": False,
                    "runtime_errors_count": None
                }
            else:
                runtime_error_pattern = re.compile(
                    r"(panic:|fatal error:|runtime error:)", re.IGNORECASE
                )
                assertion_error_pattern = re.compile(r"assert")

                # Search for runtime errors in stdout and stderr
                for o in (output, stderr):
                    for line in o.splitlines():
                        # Match lines with runtime errors but exclude assertions
                        if runtime_error_pattern.search(line) and not assertion_error_pattern.search(line):
                            runtime_errors = runtime_errors + 1

        # Parse the JSON output
        passed_tests = 0
        failed_tests = 0
        total_time = 0.0
        coverage = None
        summary_time = None

        for line in output.splitlines():
            if not line.strip():
                continue
            try:
                data = json.loads(line)
            except json.JSONDecodeError:
                continue  # Skip lines that aren't valid JSON

            # Check for test results
            if data.get('Action') == 'pass' and 'Test' in data:
                passed_tests += 1
                total_time += data.get('Elapsed', 0)
            elif data.get('Action') == 'fail' and 'Test' in data:
                failed_tests += 1
                total_time += data.get('Elapsed', 0)

            if (data.get('Action') == 'pass' or data.get('Action') == 'fail') and 'Test' not in data and 'Elapsed' in data:
                summary_time = data["Elapsed"]

            # Check for coverage information
            if data.get('Action') == 'output' and 'Output' in data:
                output_line = data['Output'].strip()
                if output_line.startswith("coverage:"):
                    # Extract the coverage percentage
                    match = re.search(r'coverage: ([\d\.]+)%', output_line)
                    if match:
                        coverage = float(match.group(1))

            # Alternative: Some versions include 'Coverage' field
            elif data.get('Action') == 'pass' and 'Coverage' in data:
                coverage = data['Coverage']

        total_tests = passed_tests + failed_tests
        pass_percentage = round((passed_tests / total_tests) * 100, 2) if total_tests > 0 else 0

        v = {
            "test_pass_rate": pass_percentage,
            "execution_time_sec": summary_time if summary_time is not None else total_time,
            "line_coverage_percent": coverage,
            "branch_coverage_percent": None,
            "timeout": False,
            "internal_error_occurred": False,
            "runtime_errors_count": runtime_errors
        }
        return v
    except Exception as e:
        print(f"Error running Go tests: {e}")
        return {
            "test_pass_rate": 0,
            "execution_time_sec": None,
            "line_coverage_percent": None,
            "branch_coverage_percent": None,
            "timeout": True,
            "internal_error_occurred": True,
            "runtime_errors_count": runtime_errors
        }


def x_run_tests__mutmut_15(project_dir):
    test_cmd = ["go", "test", "-timeout", "30s", "-json", "-cover", "./..."]
    runtime_errors = 0

    print("Running tests...")
    try:
        result = subprocess.run(
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            cwd=project_dir,
            text=True
        )
        output = result.stdout
        print(output)
        stderr = result.stderr
        print(stderr)

        # Check if the subprocess exited with a non-zero return code
        if result.returncode != 0:
            # Parse output for timeout messages
            if "test timed out after" in output or "panic: test timed out" in output:
                print("Tests failed due to timeout")
                # Handle timeout scenario
                return {
                    "test_pass_rate": 0,
                    "execution_time_sec": None,
                    "line_coverage_percent": None,
                    "branch_coverage_percent": None,
                    "timeout": True,
                    "internal_error_occurred": False,
                    "runtime_errors_count": None
                }
            else:
                runtime_error_pattern = re.compile(
                    r"(panic:|fatal error:|runtime error:)", re.IGNORECASE
                )
                assertion_error_pattern = re.compile(r"assert")

                # Search for runtime errors in stdout and stderr
                for o in (output, stderr):
                    for line in o.splitlines():
                        # Match lines with runtime errors but exclude assertions
                        if runtime_error_pattern.search(line) and not assertion_error_pattern.search(line):
                            runtime_errors = runtime_errors + 1

        # Parse the JSON output
        passed_tests = 0
        failed_tests = 0
        total_time = 0.0
        coverage = None
        summary_time = None

        for line in output.splitlines():
            if not line.strip():
                continue
            try:
                data = json.loads(line)
            except json.JSONDecodeError:
                continue  # Skip lines that aren't valid JSON

            # Check for test results
            if data.get('Action') == 'pass' and 'Test' in data:
                passed_tests += 1
                total_time += data.get('Elapsed', 0)
            elif data.get('Action') == 'fail' and 'Test' in data:
                failed_tests += 1
                total_time += data.get('Elapsed', 0)

            if (data.get('Action') == 'pass' or data.get('Action') == 'fail') and 'Test' not in data and 'Elapsed' in data:
                summary_time = data["Elapsed"]

            # Check for coverage information
            if data.get('Action') == 'output' and 'Output' in data:
                output_line = data['Output'].strip()
                if output_line.startswith("coverage:"):
                    # Extract the coverage percentage
                    match = re.search(r'coverage: ([\d\.]+)%', output_line)
                    if match:
                        coverage = float(match.group(1))

            # Alternative: Some versions include 'Coverage' field
            elif data.get('Action') == 'pass' and 'Coverage' in data:
                coverage = data['Coverage']

        total_tests = passed_tests + failed_tests
        pass_percentage = round((passed_tests / total_tests) * 100, 2) if total_tests > 0 else 0

        v = {
            "test_pass_rate": pass_percentage,
            "execution_time_sec": summary_time if summary_time is not None else total_time,
            "line_coverage_percent": coverage,
            "branch_coverage_percent": None,
            "timeout": False,
            "internal_error_occurred": False,
            "runtime_errors_count": runtime_errors
        }
        return v
    except Exception as e:
        print(f"Error running Go tests: {e}")
        return {
            "test_pass_rate": 0,
            "execution_time_sec": None,
            "line_coverage_percent": None,
            "branch_coverage_percent": None,
            "timeout": True,
            "internal_error_occurred": True,
            "runtime_errors_count": runtime_errors
        }


def x_run_tests__mutmut_16(project_dir):
    test_cmd = ["go", "test", "-timeout", "30s", "-json", "-cover", "./..."]
    runtime_errors = 0

    print("Running tests...")
    try:
        result = subprocess.run(
            test_cmd,
            stderr=subprocess.PIPE,
            cwd=project_dir,
            text=True
        )
        output = result.stdout
        print(output)
        stderr = result.stderr
        print(stderr)

        # Check if the subprocess exited with a non-zero return code
        if result.returncode != 0:
            # Parse output for timeout messages
            if "test timed out after" in output or "panic: test timed out" in output:
                print("Tests failed due to timeout")
                # Handle timeout scenario
                return {
                    "test_pass_rate": 0,
                    "execution_time_sec": None,
                    "line_coverage_percent": None,
                    "branch_coverage_percent": None,
                    "timeout": True,
                    "internal_error_occurred": False,
                    "runtime_errors_count": None
                }
            else:
                runtime_error_pattern = re.compile(
                    r"(panic:|fatal error:|runtime error:)", re.IGNORECASE
                )
                assertion_error_pattern = re.compile(r"assert")

                # Search for runtime errors in stdout and stderr
                for o in (output, stderr):
                    for line in o.splitlines():
                        # Match lines with runtime errors but exclude assertions
                        if runtime_error_pattern.search(line) and not assertion_error_pattern.search(line):
                            runtime_errors = runtime_errors + 1

        # Parse the JSON output
        passed_tests = 0
        failed_tests = 0
        total_time = 0.0
        coverage = None
        summary_time = None

        for line in output.splitlines():
            if not line.strip():
                continue
            try:
                data = json.loads(line)
            except json.JSONDecodeError:
                continue  # Skip lines that aren't valid JSON

            # Check for test results
            if data.get('Action') == 'pass' and 'Test' in data:
                passed_tests += 1
                total_time += data.get('Elapsed', 0)
            elif data.get('Action') == 'fail' and 'Test' in data:
                failed_tests += 1
                total_time += data.get('Elapsed', 0)

            if (data.get('Action') == 'pass' or data.get('Action') == 'fail') and 'Test' not in data and 'Elapsed' in data:
                summary_time = data["Elapsed"]

            # Check for coverage information
            if data.get('Action') == 'output' and 'Output' in data:
                output_line = data['Output'].strip()
                if output_line.startswith("coverage:"):
                    # Extract the coverage percentage
                    match = re.search(r'coverage: ([\d\.]+)%', output_line)
                    if match:
                        coverage = float(match.group(1))

            # Alternative: Some versions include 'Coverage' field
            elif data.get('Action') == 'pass' and 'Coverage' in data:
                coverage = data['Coverage']

        total_tests = passed_tests + failed_tests
        pass_percentage = round((passed_tests / total_tests) * 100, 2) if total_tests > 0 else 0

        v = {
            "test_pass_rate": pass_percentage,
            "execution_time_sec": summary_time if summary_time is not None else total_time,
            "line_coverage_percent": coverage,
            "branch_coverage_percent": None,
            "timeout": False,
            "internal_error_occurred": False,
            "runtime_errors_count": runtime_errors
        }
        return v
    except Exception as e:
        print(f"Error running Go tests: {e}")
        return {
            "test_pass_rate": 0,
            "execution_time_sec": None,
            "line_coverage_percent": None,
            "branch_coverage_percent": None,
            "timeout": True,
            "internal_error_occurred": True,
            "runtime_errors_count": runtime_errors
        }


def x_run_tests__mutmut_17(project_dir):
    test_cmd = ["go", "test", "-timeout", "30s", "-json", "-cover", "./..."]
    runtime_errors = 0

    print("Running tests...")
    try:
        result = subprocess.run(
            test_cmd,
            stdout=subprocess.PIPE,
            cwd=project_dir,
            text=True
        )
        output = result.stdout
        print(output)
        stderr = result.stderr
        print(stderr)

        # Check if the subprocess exited with a non-zero return code
        if result.returncode != 0:
            # Parse output for timeout messages
            if "test timed out after" in output or "panic: test timed out" in output:
                print("Tests failed due to timeout")
                # Handle timeout scenario
                return {
                    "test_pass_rate": 0,
                    "execution_time_sec": None,
                    "line_coverage_percent": None,
                    "branch_coverage_percent": None,
                    "timeout": True,
                    "internal_error_occurred": False,
                    "runtime_errors_count": None
                }
            else:
                runtime_error_pattern = re.compile(
                    r"(panic:|fatal error:|runtime error:)", re.IGNORECASE
                )
                assertion_error_pattern = re.compile(r"assert")

                # Search for runtime errors in stdout and stderr
                for o in (output, stderr):
                    for line in o.splitlines():
                        # Match lines with runtime errors but exclude assertions
                        if runtime_error_pattern.search(line) and not assertion_error_pattern.search(line):
                            runtime_errors = runtime_errors + 1

        # Parse the JSON output
        passed_tests = 0
        failed_tests = 0
        total_time = 0.0
        coverage = None
        summary_time = None

        for line in output.splitlines():
            if not line.strip():
                continue
            try:
                data = json.loads(line)
            except json.JSONDecodeError:
                continue  # Skip lines that aren't valid JSON

            # Check for test results
            if data.get('Action') == 'pass' and 'Test' in data:
                passed_tests += 1
                total_time += data.get('Elapsed', 0)
            elif data.get('Action') == 'fail' and 'Test' in data:
                failed_tests += 1
                total_time += data.get('Elapsed', 0)

            if (data.get('Action') == 'pass' or data.get('Action') == 'fail') and 'Test' not in data and 'Elapsed' in data:
                summary_time = data["Elapsed"]

            # Check for coverage information
            if data.get('Action') == 'output' and 'Output' in data:
                output_line = data['Output'].strip()
                if output_line.startswith("coverage:"):
                    # Extract the coverage percentage
                    match = re.search(r'coverage: ([\d\.]+)%', output_line)
                    if match:
                        coverage = float(match.group(1))

            # Alternative: Some versions include 'Coverage' field
            elif data.get('Action') == 'pass' and 'Coverage' in data:
                coverage = data['Coverage']

        total_tests = passed_tests + failed_tests
        pass_percentage = round((passed_tests / total_tests) * 100, 2) if total_tests > 0 else 0

        v = {
            "test_pass_rate": pass_percentage,
            "execution_time_sec": summary_time if summary_time is not None else total_time,
            "line_coverage_percent": coverage,
            "branch_coverage_percent": None,
            "timeout": False,
            "internal_error_occurred": False,
            "runtime_errors_count": runtime_errors
        }
        return v
    except Exception as e:
        print(f"Error running Go tests: {e}")
        return {
            "test_pass_rate": 0,
            "execution_time_sec": None,
            "line_coverage_percent": None,
            "branch_coverage_percent": None,
            "timeout": True,
            "internal_error_occurred": True,
            "runtime_errors_count": runtime_errors
        }


def x_run_tests__mutmut_18(project_dir):
    test_cmd = ["go", "test", "-timeout", "30s", "-json", "-cover", "./..."]
    runtime_errors = 0

    print("Running tests...")
    try:
        result = subprocess.run(
            test_cmd,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            text=True
        )
        output = result.stdout
        print(output)
        stderr = result.stderr
        print(stderr)

        # Check if the subprocess exited with a non-zero return code
        if result.returncode != 0:
            # Parse output for timeout messages
            if "test timed out after" in output or "panic: test timed out" in output:
                print("Tests failed due to timeout")
                # Handle timeout scenario
                return {
                    "test_pass_rate": 0,
                    "execution_time_sec": None,
                    "line_coverage_percent": None,
                    "branch_coverage_percent": None,
                    "timeout": True,
                    "internal_error_occurred": False,
                    "runtime_errors_count": None
                }
            else:
                runtime_error_pattern = re.compile(
                    r"(panic:|fatal error:|runtime error:)", re.IGNORECASE
                )
                assertion_error_pattern = re.compile(r"assert")

                # Search for runtime errors in stdout and stderr
                for o in (output, stderr):
                    for line in o.splitlines():
                        # Match lines with runtime errors but exclude assertions
                        if runtime_error_pattern.search(line) and not assertion_error_pattern.search(line):
                            runtime_errors = runtime_errors + 1

        # Parse the JSON output
        passed_tests = 0
        failed_tests = 0
        total_time = 0.0
        coverage = None
        summary_time = None

        for line in output.splitlines():
            if not line.strip():
                continue
            try:
                data = json.loads(line)
            except json.JSONDecodeError:
                continue  # Skip lines that aren't valid JSON

            # Check for test results
            if data.get('Action') == 'pass' and 'Test' in data:
                passed_tests += 1
                total_time += data.get('Elapsed', 0)
            elif data.get('Action') == 'fail' and 'Test' in data:
                failed_tests += 1
                total_time += data.get('Elapsed', 0)

            if (data.get('Action') == 'pass' or data.get('Action') == 'fail') and 'Test' not in data and 'Elapsed' in data:
                summary_time = data["Elapsed"]

            # Check for coverage information
            if data.get('Action') == 'output' and 'Output' in data:
                output_line = data['Output'].strip()
                if output_line.startswith("coverage:"):
                    # Extract the coverage percentage
                    match = re.search(r'coverage: ([\d\.]+)%', output_line)
                    if match:
                        coverage = float(match.group(1))

            # Alternative: Some versions include 'Coverage' field
            elif data.get('Action') == 'pass' and 'Coverage' in data:
                coverage = data['Coverage']

        total_tests = passed_tests + failed_tests
        pass_percentage = round((passed_tests / total_tests) * 100, 2) if total_tests > 0 else 0

        v = {
            "test_pass_rate": pass_percentage,
            "execution_time_sec": summary_time if summary_time is not None else total_time,
            "line_coverage_percent": coverage,
            "branch_coverage_percent": None,
            "timeout": False,
            "internal_error_occurred": False,
            "runtime_errors_count": runtime_errors
        }
        return v
    except Exception as e:
        print(f"Error running Go tests: {e}")
        return {
            "test_pass_rate": 0,
            "execution_time_sec": None,
            "line_coverage_percent": None,
            "branch_coverage_percent": None,
            "timeout": True,
            "internal_error_occurred": True,
            "runtime_errors_count": runtime_errors
        }


def x_run_tests__mutmut_19(project_dir):
    test_cmd = ["go", "test", "-timeout", "30s", "-json", "-cover", "./..."]
    runtime_errors = 0

    print("Running tests...")
    try:
        result = subprocess.run(
            test_cmd,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            cwd=project_dir,
        )
        output = result.stdout
        print(output)
        stderr = result.stderr
        print(stderr)

        # Check if the subprocess exited with a non-zero return code
        if result.returncode != 0:
            # Parse output for timeout messages
            if "test timed out after" in output or "panic: test timed out" in output:
                print("Tests failed due to timeout")
                # Handle timeout scenario
                return {
                    "test_pass_rate": 0,
                    "execution_time_sec": None,
                    "line_coverage_percent": None,
                    "branch_coverage_percent": None,
                    "timeout": True,
                    "internal_error_occurred": False,
                    "runtime_errors_count": None
                }
            else:
                runtime_error_pattern = re.compile(
                    r"(panic:|fatal error:|runtime error:)", re.IGNORECASE
                )
                assertion_error_pattern = re.compile(r"assert")

                # Search for runtime errors in stdout and stderr
                for o in (output, stderr):
                    for line in o.splitlines():
                        # Match lines with runtime errors but exclude assertions
                        if runtime_error_pattern.search(line) and not assertion_error_pattern.search(line):
                            runtime_errors = runtime_errors + 1

        # Parse the JSON output
        passed_tests = 0
        failed_tests = 0
        total_time = 0.0
        coverage = None
        summary_time = None

        for line in output.splitlines():
            if not line.strip():
                continue
            try:
                data = json.loads(line)
            except json.JSONDecodeError:
                continue  # Skip lines that aren't valid JSON

            # Check for test results
            if data.get('Action') == 'pass' and 'Test' in data:
                passed_tests += 1
                total_time += data.get('Elapsed', 0)
            elif data.get('Action') == 'fail' and 'Test' in data:
                failed_tests += 1
                total_time += data.get('Elapsed', 0)

            if (data.get('Action') == 'pass' or data.get('Action') == 'fail') and 'Test' not in data and 'Elapsed' in data:
                summary_time = data["Elapsed"]

            # Check for coverage information
            if data.get('Action') == 'output' and 'Output' in data:
                output_line = data['Output'].strip()
                if output_line.startswith("coverage:"):
                    # Extract the coverage percentage
                    match = re.search(r'coverage: ([\d\.]+)%', output_line)
                    if match:
                        coverage = float(match.group(1))

            # Alternative: Some versions include 'Coverage' field
            elif data.get('Action') == 'pass' and 'Coverage' in data:
                coverage = data['Coverage']

        total_tests = passed_tests + failed_tests
        pass_percentage = round((passed_tests / total_tests) * 100, 2) if total_tests > 0 else 0

        v = {
            "test_pass_rate": pass_percentage,
            "execution_time_sec": summary_time if summary_time is not None else total_time,
            "line_coverage_percent": coverage,
            "branch_coverage_percent": None,
            "timeout": False,
            "internal_error_occurred": False,
            "runtime_errors_count": runtime_errors
        }
        return v
    except Exception as e:
        print(f"Error running Go tests: {e}")
        return {
            "test_pass_rate": 0,
            "execution_time_sec": None,
            "line_coverage_percent": None,
            "branch_coverage_percent": None,
            "timeout": True,
            "internal_error_occurred": True,
            "runtime_errors_count": runtime_errors
        }


def x_run_tests__mutmut_20(project_dir):
    test_cmd = ["go", "test", "-timeout", "30s", "-json", "-cover", "./..."]
    runtime_errors = 0

    print("Running tests...")
    try:
        result = None
        output = result.stdout
        print(output)
        stderr = result.stderr
        print(stderr)

        # Check if the subprocess exited with a non-zero return code
        if result.returncode != 0:
            # Parse output for timeout messages
            if "test timed out after" in output or "panic: test timed out" in output:
                print("Tests failed due to timeout")
                # Handle timeout scenario
                return {
                    "test_pass_rate": 0,
                    "execution_time_sec": None,
                    "line_coverage_percent": None,
                    "branch_coverage_percent": None,
                    "timeout": True,
                    "internal_error_occurred": False,
                    "runtime_errors_count": None
                }
            else:
                runtime_error_pattern = re.compile(
                    r"(panic:|fatal error:|runtime error:)", re.IGNORECASE
                )
                assertion_error_pattern = re.compile(r"assert")

                # Search for runtime errors in stdout and stderr
                for o in (output, stderr):
                    for line in o.splitlines():
                        # Match lines with runtime errors but exclude assertions
                        if runtime_error_pattern.search(line) and not assertion_error_pattern.search(line):
                            runtime_errors = runtime_errors + 1

        # Parse the JSON output
        passed_tests = 0
        failed_tests = 0
        total_time = 0.0
        coverage = None
        summary_time = None

        for line in output.splitlines():
            if not line.strip():
                continue
            try:
                data = json.loads(line)
            except json.JSONDecodeError:
                continue  # Skip lines that aren't valid JSON

            # Check for test results
            if data.get('Action') == 'pass' and 'Test' in data:
                passed_tests += 1
                total_time += data.get('Elapsed', 0)
            elif data.get('Action') == 'fail' and 'Test' in data:
                failed_tests += 1
                total_time += data.get('Elapsed', 0)

            if (data.get('Action') == 'pass' or data.get('Action') == 'fail') and 'Test' not in data and 'Elapsed' in data:
                summary_time = data["Elapsed"]

            # Check for coverage information
            if data.get('Action') == 'output' and 'Output' in data:
                output_line = data['Output'].strip()
                if output_line.startswith("coverage:"):
                    # Extract the coverage percentage
                    match = re.search(r'coverage: ([\d\.]+)%', output_line)
                    if match:
                        coverage = float(match.group(1))

            # Alternative: Some versions include 'Coverage' field
            elif data.get('Action') == 'pass' and 'Coverage' in data:
                coverage = data['Coverage']

        total_tests = passed_tests + failed_tests
        pass_percentage = round((passed_tests / total_tests) * 100, 2) if total_tests > 0 else 0

        v = {
            "test_pass_rate": pass_percentage,
            "execution_time_sec": summary_time if summary_time is not None else total_time,
            "line_coverage_percent": coverage,
            "branch_coverage_percent": None,
            "timeout": False,
            "internal_error_occurred": False,
            "runtime_errors_count": runtime_errors
        }
        return v
    except Exception as e:
        print(f"Error running Go tests: {e}")
        return {
            "test_pass_rate": 0,
            "execution_time_sec": None,
            "line_coverage_percent": None,
            "branch_coverage_percent": None,
            "timeout": True,
            "internal_error_occurred": True,
            "runtime_errors_count": runtime_errors
        }


def x_run_tests__mutmut_21(project_dir):
    test_cmd = ["go", "test", "-timeout", "30s", "-json", "-cover", "./..."]
    runtime_errors = 0

    print("Running tests...")
    try:
        result = subprocess.run(
            test_cmd,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            cwd=project_dir,
            text=True
        )
        output = None
        print(output)
        stderr = result.stderr
        print(stderr)

        # Check if the subprocess exited with a non-zero return code
        if result.returncode != 0:
            # Parse output for timeout messages
            if "test timed out after" in output or "panic: test timed out" in output:
                print("Tests failed due to timeout")
                # Handle timeout scenario
                return {
                    "test_pass_rate": 0,
                    "execution_time_sec": None,
                    "line_coverage_percent": None,
                    "branch_coverage_percent": None,
                    "timeout": True,
                    "internal_error_occurred": False,
                    "runtime_errors_count": None
                }
            else:
                runtime_error_pattern = re.compile(
                    r"(panic:|fatal error:|runtime error:)", re.IGNORECASE
                )
                assertion_error_pattern = re.compile(r"assert")

                # Search for runtime errors in stdout and stderr
                for o in (output, stderr):
                    for line in o.splitlines():
                        # Match lines with runtime errors but exclude assertions
                        if runtime_error_pattern.search(line) and not assertion_error_pattern.search(line):
                            runtime_errors = runtime_errors + 1

        # Parse the JSON output
        passed_tests = 0
        failed_tests = 0
        total_time = 0.0
        coverage = None
        summary_time = None

        for line in output.splitlines():
            if not line.strip():
                continue
            try:
                data = json.loads(line)
            except json.JSONDecodeError:
                continue  # Skip lines that aren't valid JSON

            # Check for test results
            if data.get('Action') == 'pass' and 'Test' in data:
                passed_tests += 1
                total_time += data.get('Elapsed', 0)
            elif data.get('Action') == 'fail' and 'Test' in data:
                failed_tests += 1
                total_time += data.get('Elapsed', 0)

            if (data.get('Action') == 'pass' or data.get('Action') == 'fail') and 'Test' not in data and 'Elapsed' in data:
                summary_time = data["Elapsed"]

            # Check for coverage information
            if data.get('Action') == 'output' and 'Output' in data:
                output_line = data['Output'].strip()
                if output_line.startswith("coverage:"):
                    # Extract the coverage percentage
                    match = re.search(r'coverage: ([\d\.]+)%', output_line)
                    if match:
                        coverage = float(match.group(1))

            # Alternative: Some versions include 'Coverage' field
            elif data.get('Action') == 'pass' and 'Coverage' in data:
                coverage = data['Coverage']

        total_tests = passed_tests + failed_tests
        pass_percentage = round((passed_tests / total_tests) * 100, 2) if total_tests > 0 else 0

        v = {
            "test_pass_rate": pass_percentage,
            "execution_time_sec": summary_time if summary_time is not None else total_time,
            "line_coverage_percent": coverage,
            "branch_coverage_percent": None,
            "timeout": False,
            "internal_error_occurred": False,
            "runtime_errors_count": runtime_errors
        }
        return v
    except Exception as e:
        print(f"Error running Go tests: {e}")
        return {
            "test_pass_rate": 0,
            "execution_time_sec": None,
            "line_coverage_percent": None,
            "branch_coverage_percent": None,
            "timeout": True,
            "internal_error_occurred": True,
            "runtime_errors_count": runtime_errors
        }


def x_run_tests__mutmut_22(project_dir):
    test_cmd = ["go", "test", "-timeout", "30s", "-json", "-cover", "./..."]
    runtime_errors = 0

    print("Running tests...")
    try:
        result = subprocess.run(
            test_cmd,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            cwd=project_dir,
            text=True
        )
        output = result.stdout
        print(None)
        stderr = result.stderr
        print(stderr)

        # Check if the subprocess exited with a non-zero return code
        if result.returncode != 0:
            # Parse output for timeout messages
            if "test timed out after" in output or "panic: test timed out" in output:
                print("Tests failed due to timeout")
                # Handle timeout scenario
                return {
                    "test_pass_rate": 0,
                    "execution_time_sec": None,
                    "line_coverage_percent": None,
                    "branch_coverage_percent": None,
                    "timeout": True,
                    "internal_error_occurred": False,
                    "runtime_errors_count": None
                }
            else:
                runtime_error_pattern = re.compile(
                    r"(panic:|fatal error:|runtime error:)", re.IGNORECASE
                )
                assertion_error_pattern = re.compile(r"assert")

                # Search for runtime errors in stdout and stderr
                for o in (output, stderr):
                    for line in o.splitlines():
                        # Match lines with runtime errors but exclude assertions
                        if runtime_error_pattern.search(line) and not assertion_error_pattern.search(line):
                            runtime_errors = runtime_errors + 1

        # Parse the JSON output
        passed_tests = 0
        failed_tests = 0
        total_time = 0.0
        coverage = None
        summary_time = None

        for line in output.splitlines():
            if not line.strip():
                continue
            try:
                data = json.loads(line)
            except json.JSONDecodeError:
                continue  # Skip lines that aren't valid JSON

            # Check for test results
            if data.get('Action') == 'pass' and 'Test' in data:
                passed_tests += 1
                total_time += data.get('Elapsed', 0)
            elif data.get('Action') == 'fail' and 'Test' in data:
                failed_tests += 1
                total_time += data.get('Elapsed', 0)

            if (data.get('Action') == 'pass' or data.get('Action') == 'fail') and 'Test' not in data and 'Elapsed' in data:
                summary_time = data["Elapsed"]

            # Check for coverage information
            if data.get('Action') == 'output' and 'Output' in data:
                output_line = data['Output'].strip()
                if output_line.startswith("coverage:"):
                    # Extract the coverage percentage
                    match = re.search(r'coverage: ([\d\.]+)%', output_line)
                    if match:
                        coverage = float(match.group(1))

            # Alternative: Some versions include 'Coverage' field
            elif data.get('Action') == 'pass' and 'Coverage' in data:
                coverage = data['Coverage']

        total_tests = passed_tests + failed_tests
        pass_percentage = round((passed_tests / total_tests) * 100, 2) if total_tests > 0 else 0

        v = {
            "test_pass_rate": pass_percentage,
            "execution_time_sec": summary_time if summary_time is not None else total_time,
            "line_coverage_percent": coverage,
            "branch_coverage_percent": None,
            "timeout": False,
            "internal_error_occurred": False,
            "runtime_errors_count": runtime_errors
        }
        return v
    except Exception as e:
        print(f"Error running Go tests: {e}")
        return {
            "test_pass_rate": 0,
            "execution_time_sec": None,
            "line_coverage_percent": None,
            "branch_coverage_percent": None,
            "timeout": True,
            "internal_error_occurred": True,
            "runtime_errors_count": runtime_errors
        }


def x_run_tests__mutmut_23(project_dir):
    test_cmd = ["go", "test", "-timeout", "30s", "-json", "-cover", "./..."]
    runtime_errors = 0

    print("Running tests...")
    try:
        result = subprocess.run(
            test_cmd,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            cwd=project_dir,
            text=True
        )
        output = result.stdout
        print(output)
        stderr = None
        print(stderr)

        # Check if the subprocess exited with a non-zero return code
        if result.returncode != 0:
            # Parse output for timeout messages
            if "test timed out after" in output or "panic: test timed out" in output:
                print("Tests failed due to timeout")
                # Handle timeout scenario
                return {
                    "test_pass_rate": 0,
                    "execution_time_sec": None,
                    "line_coverage_percent": None,
                    "branch_coverage_percent": None,
                    "timeout": True,
                    "internal_error_occurred": False,
                    "runtime_errors_count": None
                }
            else:
                runtime_error_pattern = re.compile(
                    r"(panic:|fatal error:|runtime error:)", re.IGNORECASE
                )
                assertion_error_pattern = re.compile(r"assert")

                # Search for runtime errors in stdout and stderr
                for o in (output, stderr):
                    for line in o.splitlines():
                        # Match lines with runtime errors but exclude assertions
                        if runtime_error_pattern.search(line) and not assertion_error_pattern.search(line):
                            runtime_errors = runtime_errors + 1

        # Parse the JSON output
        passed_tests = 0
        failed_tests = 0
        total_time = 0.0
        coverage = None
        summary_time = None

        for line in output.splitlines():
            if not line.strip():
                continue
            try:
                data = json.loads(line)
            except json.JSONDecodeError:
                continue  # Skip lines that aren't valid JSON

            # Check for test results
            if data.get('Action') == 'pass' and 'Test' in data:
                passed_tests += 1
                total_time += data.get('Elapsed', 0)
            elif data.get('Action') == 'fail' and 'Test' in data:
                failed_tests += 1
                total_time += data.get('Elapsed', 0)

            if (data.get('Action') == 'pass' or data.get('Action') == 'fail') and 'Test' not in data and 'Elapsed' in data:
                summary_time = data["Elapsed"]

            # Check for coverage information
            if data.get('Action') == 'output' and 'Output' in data:
                output_line = data['Output'].strip()
                if output_line.startswith("coverage:"):
                    # Extract the coverage percentage
                    match = re.search(r'coverage: ([\d\.]+)%', output_line)
                    if match:
                        coverage = float(match.group(1))

            # Alternative: Some versions include 'Coverage' field
            elif data.get('Action') == 'pass' and 'Coverage' in data:
                coverage = data['Coverage']

        total_tests = passed_tests + failed_tests
        pass_percentage = round((passed_tests / total_tests) * 100, 2) if total_tests > 0 else 0

        v = {
            "test_pass_rate": pass_percentage,
            "execution_time_sec": summary_time if summary_time is not None else total_time,
            "line_coverage_percent": coverage,
            "branch_coverage_percent": None,
            "timeout": False,
            "internal_error_occurred": False,
            "runtime_errors_count": runtime_errors
        }
        return v
    except Exception as e:
        print(f"Error running Go tests: {e}")
        return {
            "test_pass_rate": 0,
            "execution_time_sec": None,
            "line_coverage_percent": None,
            "branch_coverage_percent": None,
            "timeout": True,
            "internal_error_occurred": True,
            "runtime_errors_count": runtime_errors
        }


def x_run_tests__mutmut_24(project_dir):
    test_cmd = ["go", "test", "-timeout", "30s", "-json", "-cover", "./..."]
    runtime_errors = 0

    print("Running tests...")
    try:
        result = subprocess.run(
            test_cmd,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            cwd=project_dir,
            text=True
        )
        output = result.stdout
        print(output)
        stderr = result.stderr
        print(None)

        # Check if the subprocess exited with a non-zero return code
        if result.returncode != 0:
            # Parse output for timeout messages
            if "test timed out after" in output or "panic: test timed out" in output:
                print("Tests failed due to timeout")
                # Handle timeout scenario
                return {
                    "test_pass_rate": 0,
                    "execution_time_sec": None,
                    "line_coverage_percent": None,
                    "branch_coverage_percent": None,
                    "timeout": True,
                    "internal_error_occurred": False,
                    "runtime_errors_count": None
                }
            else:
                runtime_error_pattern = re.compile(
                    r"(panic:|fatal error:|runtime error:)", re.IGNORECASE
                )
                assertion_error_pattern = re.compile(r"assert")

                # Search for runtime errors in stdout and stderr
                for o in (output, stderr):
                    for line in o.splitlines():
                        # Match lines with runtime errors but exclude assertions
                        if runtime_error_pattern.search(line) and not assertion_error_pattern.search(line):
                            runtime_errors = runtime_errors + 1

        # Parse the JSON output
        passed_tests = 0
        failed_tests = 0
        total_time = 0.0
        coverage = None
        summary_time = None

        for line in output.splitlines():
            if not line.strip():
                continue
            try:
                data = json.loads(line)
            except json.JSONDecodeError:
                continue  # Skip lines that aren't valid JSON

            # Check for test results
            if data.get('Action') == 'pass' and 'Test' in data:
                passed_tests += 1
                total_time += data.get('Elapsed', 0)
            elif data.get('Action') == 'fail' and 'Test' in data:
                failed_tests += 1
                total_time += data.get('Elapsed', 0)

            if (data.get('Action') == 'pass' or data.get('Action') == 'fail') and 'Test' not in data and 'Elapsed' in data:
                summary_time = data["Elapsed"]

            # Check for coverage information
            if data.get('Action') == 'output' and 'Output' in data:
                output_line = data['Output'].strip()
                if output_line.startswith("coverage:"):
                    # Extract the coverage percentage
                    match = re.search(r'coverage: ([\d\.]+)%', output_line)
                    if match:
                        coverage = float(match.group(1))

            # Alternative: Some versions include 'Coverage' field
            elif data.get('Action') == 'pass' and 'Coverage' in data:
                coverage = data['Coverage']

        total_tests = passed_tests + failed_tests
        pass_percentage = round((passed_tests / total_tests) * 100, 2) if total_tests > 0 else 0

        v = {
            "test_pass_rate": pass_percentage,
            "execution_time_sec": summary_time if summary_time is not None else total_time,
            "line_coverage_percent": coverage,
            "branch_coverage_percent": None,
            "timeout": False,
            "internal_error_occurred": False,
            "runtime_errors_count": runtime_errors
        }
        return v
    except Exception as e:
        print(f"Error running Go tests: {e}")
        return {
            "test_pass_rate": 0,
            "execution_time_sec": None,
            "line_coverage_percent": None,
            "branch_coverage_percent": None,
            "timeout": True,
            "internal_error_occurred": True,
            "runtime_errors_count": runtime_errors
        }


def x_run_tests__mutmut_25(project_dir):
    test_cmd = ["go", "test", "-timeout", "30s", "-json", "-cover", "./..."]
    runtime_errors = 0

    print("Running tests...")
    try:
        result = subprocess.run(
            test_cmd,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            cwd=project_dir,
            text=True
        )
        output = result.stdout
        print(output)
        stderr = result.stderr
        print(stderr)

        # Check if the subprocess exited with a non-zero return code
        if result.returncode == 0:
            # Parse output for timeout messages
            if "test timed out after" in output or "panic: test timed out" in output:
                print("Tests failed due to timeout")
                # Handle timeout scenario
                return {
                    "test_pass_rate": 0,
                    "execution_time_sec": None,
                    "line_coverage_percent": None,
                    "branch_coverage_percent": None,
                    "timeout": True,
                    "internal_error_occurred": False,
                    "runtime_errors_count": None
                }
            else:
                runtime_error_pattern = re.compile(
                    r"(panic:|fatal error:|runtime error:)", re.IGNORECASE
                )
                assertion_error_pattern = re.compile(r"assert")

                # Search for runtime errors in stdout and stderr
                for o in (output, stderr):
                    for line in o.splitlines():
                        # Match lines with runtime errors but exclude assertions
                        if runtime_error_pattern.search(line) and not assertion_error_pattern.search(line):
                            runtime_errors = runtime_errors + 1

        # Parse the JSON output
        passed_tests = 0
        failed_tests = 0
        total_time = 0.0
        coverage = None
        summary_time = None

        for line in output.splitlines():
            if not line.strip():
                continue
            try:
                data = json.loads(line)
            except json.JSONDecodeError:
                continue  # Skip lines that aren't valid JSON

            # Check for test results
            if data.get('Action') == 'pass' and 'Test' in data:
                passed_tests += 1
                total_time += data.get('Elapsed', 0)
            elif data.get('Action') == 'fail' and 'Test' in data:
                failed_tests += 1
                total_time += data.get('Elapsed', 0)

            if (data.get('Action') == 'pass' or data.get('Action') == 'fail') and 'Test' not in data and 'Elapsed' in data:
                summary_time = data["Elapsed"]

            # Check for coverage information
            if data.get('Action') == 'output' and 'Output' in data:
                output_line = data['Output'].strip()
                if output_line.startswith("coverage:"):
                    # Extract the coverage percentage
                    match = re.search(r'coverage: ([\d\.]+)%', output_line)
                    if match:
                        coverage = float(match.group(1))

            # Alternative: Some versions include 'Coverage' field
            elif data.get('Action') == 'pass' and 'Coverage' in data:
                coverage = data['Coverage']

        total_tests = passed_tests + failed_tests
        pass_percentage = round((passed_tests / total_tests) * 100, 2) if total_tests > 0 else 0

        v = {
            "test_pass_rate": pass_percentage,
            "execution_time_sec": summary_time if summary_time is not None else total_time,
            "line_coverage_percent": coverage,
            "branch_coverage_percent": None,
            "timeout": False,
            "internal_error_occurred": False,
            "runtime_errors_count": runtime_errors
        }
        return v
    except Exception as e:
        print(f"Error running Go tests: {e}")
        return {
            "test_pass_rate": 0,
            "execution_time_sec": None,
            "line_coverage_percent": None,
            "branch_coverage_percent": None,
            "timeout": True,
            "internal_error_occurred": True,
            "runtime_errors_count": runtime_errors
        }


def x_run_tests__mutmut_26(project_dir):
    test_cmd = ["go", "test", "-timeout", "30s", "-json", "-cover", "./..."]
    runtime_errors = 0

    print("Running tests...")
    try:
        result = subprocess.run(
            test_cmd,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            cwd=project_dir,
            text=True
        )
        output = result.stdout
        print(output)
        stderr = result.stderr
        print(stderr)

        # Check if the subprocess exited with a non-zero return code
        if result.returncode != 1:
            # Parse output for timeout messages
            if "test timed out after" in output or "panic: test timed out" in output:
                print("Tests failed due to timeout")
                # Handle timeout scenario
                return {
                    "test_pass_rate": 0,
                    "execution_time_sec": None,
                    "line_coverage_percent": None,
                    "branch_coverage_percent": None,
                    "timeout": True,
                    "internal_error_occurred": False,
                    "runtime_errors_count": None
                }
            else:
                runtime_error_pattern = re.compile(
                    r"(panic:|fatal error:|runtime error:)", re.IGNORECASE
                )
                assertion_error_pattern = re.compile(r"assert")

                # Search for runtime errors in stdout and stderr
                for o in (output, stderr):
                    for line in o.splitlines():
                        # Match lines with runtime errors but exclude assertions
                        if runtime_error_pattern.search(line) and not assertion_error_pattern.search(line):
                            runtime_errors = runtime_errors + 1

        # Parse the JSON output
        passed_tests = 0
        failed_tests = 0
        total_time = 0.0
        coverage = None
        summary_time = None

        for line in output.splitlines():
            if not line.strip():
                continue
            try:
                data = json.loads(line)
            except json.JSONDecodeError:
                continue  # Skip lines that aren't valid JSON

            # Check for test results
            if data.get('Action') == 'pass' and 'Test' in data:
                passed_tests += 1
                total_time += data.get('Elapsed', 0)
            elif data.get('Action') == 'fail' and 'Test' in data:
                failed_tests += 1
                total_time += data.get('Elapsed', 0)

            if (data.get('Action') == 'pass' or data.get('Action') == 'fail') and 'Test' not in data and 'Elapsed' in data:
                summary_time = data["Elapsed"]

            # Check for coverage information
            if data.get('Action') == 'output' and 'Output' in data:
                output_line = data['Output'].strip()
                if output_line.startswith("coverage:"):
                    # Extract the coverage percentage
                    match = re.search(r'coverage: ([\d\.]+)%', output_line)
                    if match:
                        coverage = float(match.group(1))

            # Alternative: Some versions include 'Coverage' field
            elif data.get('Action') == 'pass' and 'Coverage' in data:
                coverage = data['Coverage']

        total_tests = passed_tests + failed_tests
        pass_percentage = round((passed_tests / total_tests) * 100, 2) if total_tests > 0 else 0

        v = {
            "test_pass_rate": pass_percentage,
            "execution_time_sec": summary_time if summary_time is not None else total_time,
            "line_coverage_percent": coverage,
            "branch_coverage_percent": None,
            "timeout": False,
            "internal_error_occurred": False,
            "runtime_errors_count": runtime_errors
        }
        return v
    except Exception as e:
        print(f"Error running Go tests: {e}")
        return {
            "test_pass_rate": 0,
            "execution_time_sec": None,
            "line_coverage_percent": None,
            "branch_coverage_percent": None,
            "timeout": True,
            "internal_error_occurred": True,
            "runtime_errors_count": runtime_errors
        }


def x_run_tests__mutmut_27(project_dir):
    test_cmd = ["go", "test", "-timeout", "30s", "-json", "-cover", "./..."]
    runtime_errors = 0

    print("Running tests...")
    try:
        result = subprocess.run(
            test_cmd,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            cwd=project_dir,
            text=True
        )
        output = result.stdout
        print(output)
        stderr = result.stderr
        print(stderr)

        # Check if the subprocess exited with a non-zero return code
        if result.returncode != 0:
            # Parse output for timeout messages
            if "XXtest timed out afterXX" in output or "panic: test timed out" in output:
                print("Tests failed due to timeout")
                # Handle timeout scenario
                return {
                    "test_pass_rate": 0,
                    "execution_time_sec": None,
                    "line_coverage_percent": None,
                    "branch_coverage_percent": None,
                    "timeout": True,
                    "internal_error_occurred": False,
                    "runtime_errors_count": None
                }
            else:
                runtime_error_pattern = re.compile(
                    r"(panic:|fatal error:|runtime error:)", re.IGNORECASE
                )
                assertion_error_pattern = re.compile(r"assert")

                # Search for runtime errors in stdout and stderr
                for o in (output, stderr):
                    for line in o.splitlines():
                        # Match lines with runtime errors but exclude assertions
                        if runtime_error_pattern.search(line) and not assertion_error_pattern.search(line):
                            runtime_errors = runtime_errors + 1

        # Parse the JSON output
        passed_tests = 0
        failed_tests = 0
        total_time = 0.0
        coverage = None
        summary_time = None

        for line in output.splitlines():
            if not line.strip():
                continue
            try:
                data = json.loads(line)
            except json.JSONDecodeError:
                continue  # Skip lines that aren't valid JSON

            # Check for test results
            if data.get('Action') == 'pass' and 'Test' in data:
                passed_tests += 1
                total_time += data.get('Elapsed', 0)
            elif data.get('Action') == 'fail' and 'Test' in data:
                failed_tests += 1
                total_time += data.get('Elapsed', 0)

            if (data.get('Action') == 'pass' or data.get('Action') == 'fail') and 'Test' not in data and 'Elapsed' in data:
                summary_time = data["Elapsed"]

            # Check for coverage information
            if data.get('Action') == 'output' and 'Output' in data:
                output_line = data['Output'].strip()
                if output_line.startswith("coverage:"):
                    # Extract the coverage percentage
                    match = re.search(r'coverage: ([\d\.]+)%', output_line)
                    if match:
                        coverage = float(match.group(1))

            # Alternative: Some versions include 'Coverage' field
            elif data.get('Action') == 'pass' and 'Coverage' in data:
                coverage = data['Coverage']

        total_tests = passed_tests + failed_tests
        pass_percentage = round((passed_tests / total_tests) * 100, 2) if total_tests > 0 else 0

        v = {
            "test_pass_rate": pass_percentage,
            "execution_time_sec": summary_time if summary_time is not None else total_time,
            "line_coverage_percent": coverage,
            "branch_coverage_percent": None,
            "timeout": False,
            "internal_error_occurred": False,
            "runtime_errors_count": runtime_errors
        }
        return v
    except Exception as e:
        print(f"Error running Go tests: {e}")
        return {
            "test_pass_rate": 0,
            "execution_time_sec": None,
            "line_coverage_percent": None,
            "branch_coverage_percent": None,
            "timeout": True,
            "internal_error_occurred": True,
            "runtime_errors_count": runtime_errors
        }


def x_run_tests__mutmut_28(project_dir):
    test_cmd = ["go", "test", "-timeout", "30s", "-json", "-cover", "./..."]
    runtime_errors = 0

    print("Running tests...")
    try:
        result = subprocess.run(
            test_cmd,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            cwd=project_dir,
            text=True
        )
        output = result.stdout
        print(output)
        stderr = result.stderr
        print(stderr)

        # Check if the subprocess exited with a non-zero return code
        if result.returncode != 0:
            # Parse output for timeout messages
            if "test timed out after" not in output or "panic: test timed out" in output:
                print("Tests failed due to timeout")
                # Handle timeout scenario
                return {
                    "test_pass_rate": 0,
                    "execution_time_sec": None,
                    "line_coverage_percent": None,
                    "branch_coverage_percent": None,
                    "timeout": True,
                    "internal_error_occurred": False,
                    "runtime_errors_count": None
                }
            else:
                runtime_error_pattern = re.compile(
                    r"(panic:|fatal error:|runtime error:)", re.IGNORECASE
                )
                assertion_error_pattern = re.compile(r"assert")

                # Search for runtime errors in stdout and stderr
                for o in (output, stderr):
                    for line in o.splitlines():
                        # Match lines with runtime errors but exclude assertions
                        if runtime_error_pattern.search(line) and not assertion_error_pattern.search(line):
                            runtime_errors = runtime_errors + 1

        # Parse the JSON output
        passed_tests = 0
        failed_tests = 0
        total_time = 0.0
        coverage = None
        summary_time = None

        for line in output.splitlines():
            if not line.strip():
                continue
            try:
                data = json.loads(line)
            except json.JSONDecodeError:
                continue  # Skip lines that aren't valid JSON

            # Check for test results
            if data.get('Action') == 'pass' and 'Test' in data:
                passed_tests += 1
                total_time += data.get('Elapsed', 0)
            elif data.get('Action') == 'fail' and 'Test' in data:
                failed_tests += 1
                total_time += data.get('Elapsed', 0)

            if (data.get('Action') == 'pass' or data.get('Action') == 'fail') and 'Test' not in data and 'Elapsed' in data:
                summary_time = data["Elapsed"]

            # Check for coverage information
            if data.get('Action') == 'output' and 'Output' in data:
                output_line = data['Output'].strip()
                if output_line.startswith("coverage:"):
                    # Extract the coverage percentage
                    match = re.search(r'coverage: ([\d\.]+)%', output_line)
                    if match:
                        coverage = float(match.group(1))

            # Alternative: Some versions include 'Coverage' field
            elif data.get('Action') == 'pass' and 'Coverage' in data:
                coverage = data['Coverage']

        total_tests = passed_tests + failed_tests
        pass_percentage = round((passed_tests / total_tests) * 100, 2) if total_tests > 0 else 0

        v = {
            "test_pass_rate": pass_percentage,
            "execution_time_sec": summary_time if summary_time is not None else total_time,
            "line_coverage_percent": coverage,
            "branch_coverage_percent": None,
            "timeout": False,
            "internal_error_occurred": False,
            "runtime_errors_count": runtime_errors
        }
        return v
    except Exception as e:
        print(f"Error running Go tests: {e}")
        return {
            "test_pass_rate": 0,
            "execution_time_sec": None,
            "line_coverage_percent": None,
            "branch_coverage_percent": None,
            "timeout": True,
            "internal_error_occurred": True,
            "runtime_errors_count": runtime_errors
        }


def x_run_tests__mutmut_29(project_dir):
    test_cmd = ["go", "test", "-timeout", "30s", "-json", "-cover", "./..."]
    runtime_errors = 0

    print("Running tests...")
    try:
        result = subprocess.run(
            test_cmd,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            cwd=project_dir,
            text=True
        )
        output = result.stdout
        print(output)
        stderr = result.stderr
        print(stderr)

        # Check if the subprocess exited with a non-zero return code
        if result.returncode != 0:
            # Parse output for timeout messages
            if "test timed out after" in output or "XXpanic: test timed outXX" in output:
                print("Tests failed due to timeout")
                # Handle timeout scenario
                return {
                    "test_pass_rate": 0,
                    "execution_time_sec": None,
                    "line_coverage_percent": None,
                    "branch_coverage_percent": None,
                    "timeout": True,
                    "internal_error_occurred": False,
                    "runtime_errors_count": None
                }
            else:
                runtime_error_pattern = re.compile(
                    r"(panic:|fatal error:|runtime error:)", re.IGNORECASE
                )
                assertion_error_pattern = re.compile(r"assert")

                # Search for runtime errors in stdout and stderr
                for o in (output, stderr):
                    for line in o.splitlines():
                        # Match lines with runtime errors but exclude assertions
                        if runtime_error_pattern.search(line) and not assertion_error_pattern.search(line):
                            runtime_errors = runtime_errors + 1

        # Parse the JSON output
        passed_tests = 0
        failed_tests = 0
        total_time = 0.0
        coverage = None
        summary_time = None

        for line in output.splitlines():
            if not line.strip():
                continue
            try:
                data = json.loads(line)
            except json.JSONDecodeError:
                continue  # Skip lines that aren't valid JSON

            # Check for test results
            if data.get('Action') == 'pass' and 'Test' in data:
                passed_tests += 1
                total_time += data.get('Elapsed', 0)
            elif data.get('Action') == 'fail' and 'Test' in data:
                failed_tests += 1
                total_time += data.get('Elapsed', 0)

            if (data.get('Action') == 'pass' or data.get('Action') == 'fail') and 'Test' not in data and 'Elapsed' in data:
                summary_time = data["Elapsed"]

            # Check for coverage information
            if data.get('Action') == 'output' and 'Output' in data:
                output_line = data['Output'].strip()
                if output_line.startswith("coverage:"):
                    # Extract the coverage percentage
                    match = re.search(r'coverage: ([\d\.]+)%', output_line)
                    if match:
                        coverage = float(match.group(1))

            # Alternative: Some versions include 'Coverage' field
            elif data.get('Action') == 'pass' and 'Coverage' in data:
                coverage = data['Coverage']

        total_tests = passed_tests + failed_tests
        pass_percentage = round((passed_tests / total_tests) * 100, 2) if total_tests > 0 else 0

        v = {
            "test_pass_rate": pass_percentage,
            "execution_time_sec": summary_time if summary_time is not None else total_time,
            "line_coverage_percent": coverage,
            "branch_coverage_percent": None,
            "timeout": False,
            "internal_error_occurred": False,
            "runtime_errors_count": runtime_errors
        }
        return v
    except Exception as e:
        print(f"Error running Go tests: {e}")
        return {
            "test_pass_rate": 0,
            "execution_time_sec": None,
            "line_coverage_percent": None,
            "branch_coverage_percent": None,
            "timeout": True,
            "internal_error_occurred": True,
            "runtime_errors_count": runtime_errors
        }


def x_run_tests__mutmut_30(project_dir):
    test_cmd = ["go", "test", "-timeout", "30s", "-json", "-cover", "./..."]
    runtime_errors = 0

    print("Running tests...")
    try:
        result = subprocess.run(
            test_cmd,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            cwd=project_dir,
            text=True
        )
        output = result.stdout
        print(output)
        stderr = result.stderr
        print(stderr)

        # Check if the subprocess exited with a non-zero return code
        if result.returncode != 0:
            # Parse output for timeout messages
            if "test timed out after" in output or "panic: test timed out" not in output:
                print("Tests failed due to timeout")
                # Handle timeout scenario
                return {
                    "test_pass_rate": 0,
                    "execution_time_sec": None,
                    "line_coverage_percent": None,
                    "branch_coverage_percent": None,
                    "timeout": True,
                    "internal_error_occurred": False,
                    "runtime_errors_count": None
                }
            else:
                runtime_error_pattern = re.compile(
                    r"(panic:|fatal error:|runtime error:)", re.IGNORECASE
                )
                assertion_error_pattern = re.compile(r"assert")

                # Search for runtime errors in stdout and stderr
                for o in (output, stderr):
                    for line in o.splitlines():
                        # Match lines with runtime errors but exclude assertions
                        if runtime_error_pattern.search(line) and not assertion_error_pattern.search(line):
                            runtime_errors = runtime_errors + 1

        # Parse the JSON output
        passed_tests = 0
        failed_tests = 0
        total_time = 0.0
        coverage = None
        summary_time = None

        for line in output.splitlines():
            if not line.strip():
                continue
            try:
                data = json.loads(line)
            except json.JSONDecodeError:
                continue  # Skip lines that aren't valid JSON

            # Check for test results
            if data.get('Action') == 'pass' and 'Test' in data:
                passed_tests += 1
                total_time += data.get('Elapsed', 0)
            elif data.get('Action') == 'fail' and 'Test' in data:
                failed_tests += 1
                total_time += data.get('Elapsed', 0)

            if (data.get('Action') == 'pass' or data.get('Action') == 'fail') and 'Test' not in data and 'Elapsed' in data:
                summary_time = data["Elapsed"]

            # Check for coverage information
            if data.get('Action') == 'output' and 'Output' in data:
                output_line = data['Output'].strip()
                if output_line.startswith("coverage:"):
                    # Extract the coverage percentage
                    match = re.search(r'coverage: ([\d\.]+)%', output_line)
                    if match:
                        coverage = float(match.group(1))

            # Alternative: Some versions include 'Coverage' field
            elif data.get('Action') == 'pass' and 'Coverage' in data:
                coverage = data['Coverage']

        total_tests = passed_tests + failed_tests
        pass_percentage = round((passed_tests / total_tests) * 100, 2) if total_tests > 0 else 0

        v = {
            "test_pass_rate": pass_percentage,
            "execution_time_sec": summary_time if summary_time is not None else total_time,
            "line_coverage_percent": coverage,
            "branch_coverage_percent": None,
            "timeout": False,
            "internal_error_occurred": False,
            "runtime_errors_count": runtime_errors
        }
        return v
    except Exception as e:
        print(f"Error running Go tests: {e}")
        return {
            "test_pass_rate": 0,
            "execution_time_sec": None,
            "line_coverage_percent": None,
            "branch_coverage_percent": None,
            "timeout": True,
            "internal_error_occurred": True,
            "runtime_errors_count": runtime_errors
        }


def x_run_tests__mutmut_31(project_dir):
    test_cmd = ["go", "test", "-timeout", "30s", "-json", "-cover", "./..."]
    runtime_errors = 0

    print("Running tests...")
    try:
        result = subprocess.run(
            test_cmd,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            cwd=project_dir,
            text=True
        )
        output = result.stdout
        print(output)
        stderr = result.stderr
        print(stderr)

        # Check if the subprocess exited with a non-zero return code
        if result.returncode != 0:
            # Parse output for timeout messages
            if "test timed out after" in output and "panic: test timed out" in output:
                print("Tests failed due to timeout")
                # Handle timeout scenario
                return {
                    "test_pass_rate": 0,
                    "execution_time_sec": None,
                    "line_coverage_percent": None,
                    "branch_coverage_percent": None,
                    "timeout": True,
                    "internal_error_occurred": False,
                    "runtime_errors_count": None
                }
            else:
                runtime_error_pattern = re.compile(
                    r"(panic:|fatal error:|runtime error:)", re.IGNORECASE
                )
                assertion_error_pattern = re.compile(r"assert")

                # Search for runtime errors in stdout and stderr
                for o in (output, stderr):
                    for line in o.splitlines():
                        # Match lines with runtime errors but exclude assertions
                        if runtime_error_pattern.search(line) and not assertion_error_pattern.search(line):
                            runtime_errors = runtime_errors + 1

        # Parse the JSON output
        passed_tests = 0
        failed_tests = 0
        total_time = 0.0
        coverage = None
        summary_time = None

        for line in output.splitlines():
            if not line.strip():
                continue
            try:
                data = json.loads(line)
            except json.JSONDecodeError:
                continue  # Skip lines that aren't valid JSON

            # Check for test results
            if data.get('Action') == 'pass' and 'Test' in data:
                passed_tests += 1
                total_time += data.get('Elapsed', 0)
            elif data.get('Action') == 'fail' and 'Test' in data:
                failed_tests += 1
                total_time += data.get('Elapsed', 0)

            if (data.get('Action') == 'pass' or data.get('Action') == 'fail') and 'Test' not in data and 'Elapsed' in data:
                summary_time = data["Elapsed"]

            # Check for coverage information
            if data.get('Action') == 'output' and 'Output' in data:
                output_line = data['Output'].strip()
                if output_line.startswith("coverage:"):
                    # Extract the coverage percentage
                    match = re.search(r'coverage: ([\d\.]+)%', output_line)
                    if match:
                        coverage = float(match.group(1))

            # Alternative: Some versions include 'Coverage' field
            elif data.get('Action') == 'pass' and 'Coverage' in data:
                coverage = data['Coverage']

        total_tests = passed_tests + failed_tests
        pass_percentage = round((passed_tests / total_tests) * 100, 2) if total_tests > 0 else 0

        v = {
            "test_pass_rate": pass_percentage,
            "execution_time_sec": summary_time if summary_time is not None else total_time,
            "line_coverage_percent": coverage,
            "branch_coverage_percent": None,
            "timeout": False,
            "internal_error_occurred": False,
            "runtime_errors_count": runtime_errors
        }
        return v
    except Exception as e:
        print(f"Error running Go tests: {e}")
        return {
            "test_pass_rate": 0,
            "execution_time_sec": None,
            "line_coverage_percent": None,
            "branch_coverage_percent": None,
            "timeout": True,
            "internal_error_occurred": True,
            "runtime_errors_count": runtime_errors
        }


def x_run_tests__mutmut_32(project_dir):
    test_cmd = ["go", "test", "-timeout", "30s", "-json", "-cover", "./..."]
    runtime_errors = 0

    print("Running tests...")
    try:
        result = subprocess.run(
            test_cmd,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            cwd=project_dir,
            text=True
        )
        output = result.stdout
        print(output)
        stderr = result.stderr
        print(stderr)

        # Check if the subprocess exited with a non-zero return code
        if result.returncode != 0:
            # Parse output for timeout messages
            if "test timed out after" in output or "panic: test timed out" in output:
                print("XXTests failed due to timeoutXX")
                # Handle timeout scenario
                return {
                    "test_pass_rate": 0,
                    "execution_time_sec": None,
                    "line_coverage_percent": None,
                    "branch_coverage_percent": None,
                    "timeout": True,
                    "internal_error_occurred": False,
                    "runtime_errors_count": None
                }
            else:
                runtime_error_pattern = re.compile(
                    r"(panic:|fatal error:|runtime error:)", re.IGNORECASE
                )
                assertion_error_pattern = re.compile(r"assert")

                # Search for runtime errors in stdout and stderr
                for o in (output, stderr):
                    for line in o.splitlines():
                        # Match lines with runtime errors but exclude assertions
                        if runtime_error_pattern.search(line) and not assertion_error_pattern.search(line):
                            runtime_errors = runtime_errors + 1

        # Parse the JSON output
        passed_tests = 0
        failed_tests = 0
        total_time = 0.0
        coverage = None
        summary_time = None

        for line in output.splitlines():
            if not line.strip():
                continue
            try:
                data = json.loads(line)
            except json.JSONDecodeError:
                continue  # Skip lines that aren't valid JSON

            # Check for test results
            if data.get('Action') == 'pass' and 'Test' in data:
                passed_tests += 1
                total_time += data.get('Elapsed', 0)
            elif data.get('Action') == 'fail' and 'Test' in data:
                failed_tests += 1
                total_time += data.get('Elapsed', 0)

            if (data.get('Action') == 'pass' or data.get('Action') == 'fail') and 'Test' not in data and 'Elapsed' in data:
                summary_time = data["Elapsed"]

            # Check for coverage information
            if data.get('Action') == 'output' and 'Output' in data:
                output_line = data['Output'].strip()
                if output_line.startswith("coverage:"):
                    # Extract the coverage percentage
                    match = re.search(r'coverage: ([\d\.]+)%', output_line)
                    if match:
                        coverage = float(match.group(1))

            # Alternative: Some versions include 'Coverage' field
            elif data.get('Action') == 'pass' and 'Coverage' in data:
                coverage = data['Coverage']

        total_tests = passed_tests + failed_tests
        pass_percentage = round((passed_tests / total_tests) * 100, 2) if total_tests > 0 else 0

        v = {
            "test_pass_rate": pass_percentage,
            "execution_time_sec": summary_time if summary_time is not None else total_time,
            "line_coverage_percent": coverage,
            "branch_coverage_percent": None,
            "timeout": False,
            "internal_error_occurred": False,
            "runtime_errors_count": runtime_errors
        }
        return v
    except Exception as e:
        print(f"Error running Go tests: {e}")
        return {
            "test_pass_rate": 0,
            "execution_time_sec": None,
            "line_coverage_percent": None,
            "branch_coverage_percent": None,
            "timeout": True,
            "internal_error_occurred": True,
            "runtime_errors_count": runtime_errors
        }


def x_run_tests__mutmut_33(project_dir):
    test_cmd = ["go", "test", "-timeout", "30s", "-json", "-cover", "./..."]
    runtime_errors = 0

    print("Running tests...")
    try:
        result = subprocess.run(
            test_cmd,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            cwd=project_dir,
            text=True
        )
        output = result.stdout
        print(output)
        stderr = result.stderr
        print(stderr)

        # Check if the subprocess exited with a non-zero return code
        if result.returncode != 0:
            # Parse output for timeout messages
            if "test timed out after" in output or "panic: test timed out" in output:
                print("Tests failed due to timeout")
                # Handle timeout scenario
                return {
                    "XXtest_pass_rateXX": 0,
                    "execution_time_sec": None,
                    "line_coverage_percent": None,
                    "branch_coverage_percent": None,
                    "timeout": True,
                    "internal_error_occurred": False,
                    "runtime_errors_count": None
                }
            else:
                runtime_error_pattern = re.compile(
                    r"(panic:|fatal error:|runtime error:)", re.IGNORECASE
                )
                assertion_error_pattern = re.compile(r"assert")

                # Search for runtime errors in stdout and stderr
                for o in (output, stderr):
                    for line in o.splitlines():
                        # Match lines with runtime errors but exclude assertions
                        if runtime_error_pattern.search(line) and not assertion_error_pattern.search(line):
                            runtime_errors = runtime_errors + 1

        # Parse the JSON output
        passed_tests = 0
        failed_tests = 0
        total_time = 0.0
        coverage = None
        summary_time = None

        for line in output.splitlines():
            if not line.strip():
                continue
            try:
                data = json.loads(line)
            except json.JSONDecodeError:
                continue  # Skip lines that aren't valid JSON

            # Check for test results
            if data.get('Action') == 'pass' and 'Test' in data:
                passed_tests += 1
                total_time += data.get('Elapsed', 0)
            elif data.get('Action') == 'fail' and 'Test' in data:
                failed_tests += 1
                total_time += data.get('Elapsed', 0)

            if (data.get('Action') == 'pass' or data.get('Action') == 'fail') and 'Test' not in data and 'Elapsed' in data:
                summary_time = data["Elapsed"]

            # Check for coverage information
            if data.get('Action') == 'output' and 'Output' in data:
                output_line = data['Output'].strip()
                if output_line.startswith("coverage:"):
                    # Extract the coverage percentage
                    match = re.search(r'coverage: ([\d\.]+)%', output_line)
                    if match:
                        coverage = float(match.group(1))

            # Alternative: Some versions include 'Coverage' field
            elif data.get('Action') == 'pass' and 'Coverage' in data:
                coverage = data['Coverage']

        total_tests = passed_tests + failed_tests
        pass_percentage = round((passed_tests / total_tests) * 100, 2) if total_tests > 0 else 0

        v = {
            "test_pass_rate": pass_percentage,
            "execution_time_sec": summary_time if summary_time is not None else total_time,
            "line_coverage_percent": coverage,
            "branch_coverage_percent": None,
            "timeout": False,
            "internal_error_occurred": False,
            "runtime_errors_count": runtime_errors
        }
        return v
    except Exception as e:
        print(f"Error running Go tests: {e}")
        return {
            "test_pass_rate": 0,
            "execution_time_sec": None,
            "line_coverage_percent": None,
            "branch_coverage_percent": None,
            "timeout": True,
            "internal_error_occurred": True,
            "runtime_errors_count": runtime_errors
        }


def x_run_tests__mutmut_34(project_dir):
    test_cmd = ["go", "test", "-timeout", "30s", "-json", "-cover", "./..."]
    runtime_errors = 0

    print("Running tests...")
    try:
        result = subprocess.run(
            test_cmd,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            cwd=project_dir,
            text=True
        )
        output = result.stdout
        print(output)
        stderr = result.stderr
        print(stderr)

        # Check if the subprocess exited with a non-zero return code
        if result.returncode != 0:
            # Parse output for timeout messages
            if "test timed out after" in output or "panic: test timed out" in output:
                print("Tests failed due to timeout")
                # Handle timeout scenario
                return {
                    "test_pass_rate": 1,
                    "execution_time_sec": None,
                    "line_coverage_percent": None,
                    "branch_coverage_percent": None,
                    "timeout": True,
                    "internal_error_occurred": False,
                    "runtime_errors_count": None
                }
            else:
                runtime_error_pattern = re.compile(
                    r"(panic:|fatal error:|runtime error:)", re.IGNORECASE
                )
                assertion_error_pattern = re.compile(r"assert")

                # Search for runtime errors in stdout and stderr
                for o in (output, stderr):
                    for line in o.splitlines():
                        # Match lines with runtime errors but exclude assertions
                        if runtime_error_pattern.search(line) and not assertion_error_pattern.search(line):
                            runtime_errors = runtime_errors + 1

        # Parse the JSON output
        passed_tests = 0
        failed_tests = 0
        total_time = 0.0
        coverage = None
        summary_time = None

        for line in output.splitlines():
            if not line.strip():
                continue
            try:
                data = json.loads(line)
            except json.JSONDecodeError:
                continue  # Skip lines that aren't valid JSON

            # Check for test results
            if data.get('Action') == 'pass' and 'Test' in data:
                passed_tests += 1
                total_time += data.get('Elapsed', 0)
            elif data.get('Action') == 'fail' and 'Test' in data:
                failed_tests += 1
                total_time += data.get('Elapsed', 0)

            if (data.get('Action') == 'pass' or data.get('Action') == 'fail') and 'Test' not in data and 'Elapsed' in data:
                summary_time = data["Elapsed"]

            # Check for coverage information
            if data.get('Action') == 'output' and 'Output' in data:
                output_line = data['Output'].strip()
                if output_line.startswith("coverage:"):
                    # Extract the coverage percentage
                    match = re.search(r'coverage: ([\d\.]+)%', output_line)
                    if match:
                        coverage = float(match.group(1))

            # Alternative: Some versions include 'Coverage' field
            elif data.get('Action') == 'pass' and 'Coverage' in data:
                coverage = data['Coverage']

        total_tests = passed_tests + failed_tests
        pass_percentage = round((passed_tests / total_tests) * 100, 2) if total_tests > 0 else 0

        v = {
            "test_pass_rate": pass_percentage,
            "execution_time_sec": summary_time if summary_time is not None else total_time,
            "line_coverage_percent": coverage,
            "branch_coverage_percent": None,
            "timeout": False,
            "internal_error_occurred": False,
            "runtime_errors_count": runtime_errors
        }
        return v
    except Exception as e:
        print(f"Error running Go tests: {e}")
        return {
            "test_pass_rate": 0,
            "execution_time_sec": None,
            "line_coverage_percent": None,
            "branch_coverage_percent": None,
            "timeout": True,
            "internal_error_occurred": True,
            "runtime_errors_count": runtime_errors
        }


def x_run_tests__mutmut_35(project_dir):
    test_cmd = ["go", "test", "-timeout", "30s", "-json", "-cover", "./..."]
    runtime_errors = 0

    print("Running tests...")
    try:
        result = subprocess.run(
            test_cmd,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            cwd=project_dir,
            text=True
        )
        output = result.stdout
        print(output)
        stderr = result.stderr
        print(stderr)

        # Check if the subprocess exited with a non-zero return code
        if result.returncode != 0:
            # Parse output for timeout messages
            if "test timed out after" in output or "panic: test timed out" in output:
                print("Tests failed due to timeout")
                # Handle timeout scenario
                return {
                    "test_pass_rate": 0,
                    "XXexecution_time_secXX": None,
                    "line_coverage_percent": None,
                    "branch_coverage_percent": None,
                    "timeout": True,
                    "internal_error_occurred": False,
                    "runtime_errors_count": None
                }
            else:
                runtime_error_pattern = re.compile(
                    r"(panic:|fatal error:|runtime error:)", re.IGNORECASE
                )
                assertion_error_pattern = re.compile(r"assert")

                # Search for runtime errors in stdout and stderr
                for o in (output, stderr):
                    for line in o.splitlines():
                        # Match lines with runtime errors but exclude assertions
                        if runtime_error_pattern.search(line) and not assertion_error_pattern.search(line):
                            runtime_errors = runtime_errors + 1

        # Parse the JSON output
        passed_tests = 0
        failed_tests = 0
        total_time = 0.0
        coverage = None
        summary_time = None

        for line in output.splitlines():
            if not line.strip():
                continue
            try:
                data = json.loads(line)
            except json.JSONDecodeError:
                continue  # Skip lines that aren't valid JSON

            # Check for test results
            if data.get('Action') == 'pass' and 'Test' in data:
                passed_tests += 1
                total_time += data.get('Elapsed', 0)
            elif data.get('Action') == 'fail' and 'Test' in data:
                failed_tests += 1
                total_time += data.get('Elapsed', 0)

            if (data.get('Action') == 'pass' or data.get('Action') == 'fail') and 'Test' not in data and 'Elapsed' in data:
                summary_time = data["Elapsed"]

            # Check for coverage information
            if data.get('Action') == 'output' and 'Output' in data:
                output_line = data['Output'].strip()
                if output_line.startswith("coverage:"):
                    # Extract the coverage percentage
                    match = re.search(r'coverage: ([\d\.]+)%', output_line)
                    if match:
                        coverage = float(match.group(1))

            # Alternative: Some versions include 'Coverage' field
            elif data.get('Action') == 'pass' and 'Coverage' in data:
                coverage = data['Coverage']

        total_tests = passed_tests + failed_tests
        pass_percentage = round((passed_tests / total_tests) * 100, 2) if total_tests > 0 else 0

        v = {
            "test_pass_rate": pass_percentage,
            "execution_time_sec": summary_time if summary_time is not None else total_time,
            "line_coverage_percent": coverage,
            "branch_coverage_percent": None,
            "timeout": False,
            "internal_error_occurred": False,
            "runtime_errors_count": runtime_errors
        }
        return v
    except Exception as e:
        print(f"Error running Go tests: {e}")
        return {
            "test_pass_rate": 0,
            "execution_time_sec": None,
            "line_coverage_percent": None,
            "branch_coverage_percent": None,
            "timeout": True,
            "internal_error_occurred": True,
            "runtime_errors_count": runtime_errors
        }


def x_run_tests__mutmut_36(project_dir):
    test_cmd = ["go", "test", "-timeout", "30s", "-json", "-cover", "./..."]
    runtime_errors = 0

    print("Running tests...")
    try:
        result = subprocess.run(
            test_cmd,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            cwd=project_dir,
            text=True
        )
        output = result.stdout
        print(output)
        stderr = result.stderr
        print(stderr)

        # Check if the subprocess exited with a non-zero return code
        if result.returncode != 0:
            # Parse output for timeout messages
            if "test timed out after" in output or "panic: test timed out" in output:
                print("Tests failed due to timeout")
                # Handle timeout scenario
                return {
                    "test_pass_rate": 0,
                    "execution_time_sec": None,
                    "XXline_coverage_percentXX": None,
                    "branch_coverage_percent": None,
                    "timeout": True,
                    "internal_error_occurred": False,
                    "runtime_errors_count": None
                }
            else:
                runtime_error_pattern = re.compile(
                    r"(panic:|fatal error:|runtime error:)", re.IGNORECASE
                )
                assertion_error_pattern = re.compile(r"assert")

                # Search for runtime errors in stdout and stderr
                for o in (output, stderr):
                    for line in o.splitlines():
                        # Match lines with runtime errors but exclude assertions
                        if runtime_error_pattern.search(line) and not assertion_error_pattern.search(line):
                            runtime_errors = runtime_errors + 1

        # Parse the JSON output
        passed_tests = 0
        failed_tests = 0
        total_time = 0.0
        coverage = None
        summary_time = None

        for line in output.splitlines():
            if not line.strip():
                continue
            try:
                data = json.loads(line)
            except json.JSONDecodeError:
                continue  # Skip lines that aren't valid JSON

            # Check for test results
            if data.get('Action') == 'pass' and 'Test' in data:
                passed_tests += 1
                total_time += data.get('Elapsed', 0)
            elif data.get('Action') == 'fail' and 'Test' in data:
                failed_tests += 1
                total_time += data.get('Elapsed', 0)

            if (data.get('Action') == 'pass' or data.get('Action') == 'fail') and 'Test' not in data and 'Elapsed' in data:
                summary_time = data["Elapsed"]

            # Check for coverage information
            if data.get('Action') == 'output' and 'Output' in data:
                output_line = data['Output'].strip()
                if output_line.startswith("coverage:"):
                    # Extract the coverage percentage
                    match = re.search(r'coverage: ([\d\.]+)%', output_line)
                    if match:
                        coverage = float(match.group(1))

            # Alternative: Some versions include 'Coverage' field
            elif data.get('Action') == 'pass' and 'Coverage' in data:
                coverage = data['Coverage']

        total_tests = passed_tests + failed_tests
        pass_percentage = round((passed_tests / total_tests) * 100, 2) if total_tests > 0 else 0

        v = {
            "test_pass_rate": pass_percentage,
            "execution_time_sec": summary_time if summary_time is not None else total_time,
            "line_coverage_percent": coverage,
            "branch_coverage_percent": None,
            "timeout": False,
            "internal_error_occurred": False,
            "runtime_errors_count": runtime_errors
        }
        return v
    except Exception as e:
        print(f"Error running Go tests: {e}")
        return {
            "test_pass_rate": 0,
            "execution_time_sec": None,
            "line_coverage_percent": None,
            "branch_coverage_percent": None,
            "timeout": True,
            "internal_error_occurred": True,
            "runtime_errors_count": runtime_errors
        }


def x_run_tests__mutmut_37(project_dir):
    test_cmd = ["go", "test", "-timeout", "30s", "-json", "-cover", "./..."]
    runtime_errors = 0

    print("Running tests...")
    try:
        result = subprocess.run(
            test_cmd,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            cwd=project_dir,
            text=True
        )
        output = result.stdout
        print(output)
        stderr = result.stderr
        print(stderr)

        # Check if the subprocess exited with a non-zero return code
        if result.returncode != 0:
            # Parse output for timeout messages
            if "test timed out after" in output or "panic: test timed out" in output:
                print("Tests failed due to timeout")
                # Handle timeout scenario
                return {
                    "test_pass_rate": 0,
                    "execution_time_sec": None,
                    "line_coverage_percent": None,
                    "XXbranch_coverage_percentXX": None,
                    "timeout": True,
                    "internal_error_occurred": False,
                    "runtime_errors_count": None
                }
            else:
                runtime_error_pattern = re.compile(
                    r"(panic:|fatal error:|runtime error:)", re.IGNORECASE
                )
                assertion_error_pattern = re.compile(r"assert")

                # Search for runtime errors in stdout and stderr
                for o in (output, stderr):
                    for line in o.splitlines():
                        # Match lines with runtime errors but exclude assertions
                        if runtime_error_pattern.search(line) and not assertion_error_pattern.search(line):
                            runtime_errors = runtime_errors + 1

        # Parse the JSON output
        passed_tests = 0
        failed_tests = 0
        total_time = 0.0
        coverage = None
        summary_time = None

        for line in output.splitlines():
            if not line.strip():
                continue
            try:
                data = json.loads(line)
            except json.JSONDecodeError:
                continue  # Skip lines that aren't valid JSON

            # Check for test results
            if data.get('Action') == 'pass' and 'Test' in data:
                passed_tests += 1
                total_time += data.get('Elapsed', 0)
            elif data.get('Action') == 'fail' and 'Test' in data:
                failed_tests += 1
                total_time += data.get('Elapsed', 0)

            if (data.get('Action') == 'pass' or data.get('Action') == 'fail') and 'Test' not in data and 'Elapsed' in data:
                summary_time = data["Elapsed"]

            # Check for coverage information
            if data.get('Action') == 'output' and 'Output' in data:
                output_line = data['Output'].strip()
                if output_line.startswith("coverage:"):
                    # Extract the coverage percentage
                    match = re.search(r'coverage: ([\d\.]+)%', output_line)
                    if match:
                        coverage = float(match.group(1))

            # Alternative: Some versions include 'Coverage' field
            elif data.get('Action') == 'pass' and 'Coverage' in data:
                coverage = data['Coverage']

        total_tests = passed_tests + failed_tests
        pass_percentage = round((passed_tests / total_tests) * 100, 2) if total_tests > 0 else 0

        v = {
            "test_pass_rate": pass_percentage,
            "execution_time_sec": summary_time if summary_time is not None else total_time,
            "line_coverage_percent": coverage,
            "branch_coverage_percent": None,
            "timeout": False,
            "internal_error_occurred": False,
            "runtime_errors_count": runtime_errors
        }
        return v
    except Exception as e:
        print(f"Error running Go tests: {e}")
        return {
            "test_pass_rate": 0,
            "execution_time_sec": None,
            "line_coverage_percent": None,
            "branch_coverage_percent": None,
            "timeout": True,
            "internal_error_occurred": True,
            "runtime_errors_count": runtime_errors
        }


def x_run_tests__mutmut_38(project_dir):
    test_cmd = ["go", "test", "-timeout", "30s", "-json", "-cover", "./..."]
    runtime_errors = 0

    print("Running tests...")
    try:
        result = subprocess.run(
            test_cmd,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            cwd=project_dir,
            text=True
        )
        output = result.stdout
        print(output)
        stderr = result.stderr
        print(stderr)

        # Check if the subprocess exited with a non-zero return code
        if result.returncode != 0:
            # Parse output for timeout messages
            if "test timed out after" in output or "panic: test timed out" in output:
                print("Tests failed due to timeout")
                # Handle timeout scenario
                return {
                    "test_pass_rate": 0,
                    "execution_time_sec": None,
                    "line_coverage_percent": None,
                    "branch_coverage_percent": None,
                    "XXtimeoutXX": True,
                    "internal_error_occurred": False,
                    "runtime_errors_count": None
                }
            else:
                runtime_error_pattern = re.compile(
                    r"(panic:|fatal error:|runtime error:)", re.IGNORECASE
                )
                assertion_error_pattern = re.compile(r"assert")

                # Search for runtime errors in stdout and stderr
                for o in (output, stderr):
                    for line in o.splitlines():
                        # Match lines with runtime errors but exclude assertions
                        if runtime_error_pattern.search(line) and not assertion_error_pattern.search(line):
                            runtime_errors = runtime_errors + 1

        # Parse the JSON output
        passed_tests = 0
        failed_tests = 0
        total_time = 0.0
        coverage = None
        summary_time = None

        for line in output.splitlines():
            if not line.strip():
                continue
            try:
                data = json.loads(line)
            except json.JSONDecodeError:
                continue  # Skip lines that aren't valid JSON

            # Check for test results
            if data.get('Action') == 'pass' and 'Test' in data:
                passed_tests += 1
                total_time += data.get('Elapsed', 0)
            elif data.get('Action') == 'fail' and 'Test' in data:
                failed_tests += 1
                total_time += data.get('Elapsed', 0)

            if (data.get('Action') == 'pass' or data.get('Action') == 'fail') and 'Test' not in data and 'Elapsed' in data:
                summary_time = data["Elapsed"]

            # Check for coverage information
            if data.get('Action') == 'output' and 'Output' in data:
                output_line = data['Output'].strip()
                if output_line.startswith("coverage:"):
                    # Extract the coverage percentage
                    match = re.search(r'coverage: ([\d\.]+)%', output_line)
                    if match:
                        coverage = float(match.group(1))

            # Alternative: Some versions include 'Coverage' field
            elif data.get('Action') == 'pass' and 'Coverage' in data:
                coverage = data['Coverage']

        total_tests = passed_tests + failed_tests
        pass_percentage = round((passed_tests / total_tests) * 100, 2) if total_tests > 0 else 0

        v = {
            "test_pass_rate": pass_percentage,
            "execution_time_sec": summary_time if summary_time is not None else total_time,
            "line_coverage_percent": coverage,
            "branch_coverage_percent": None,
            "timeout": False,
            "internal_error_occurred": False,
            "runtime_errors_count": runtime_errors
        }
        return v
    except Exception as e:
        print(f"Error running Go tests: {e}")
        return {
            "test_pass_rate": 0,
            "execution_time_sec": None,
            "line_coverage_percent": None,
            "branch_coverage_percent": None,
            "timeout": True,
            "internal_error_occurred": True,
            "runtime_errors_count": runtime_errors
        }


def x_run_tests__mutmut_39(project_dir):
    test_cmd = ["go", "test", "-timeout", "30s", "-json", "-cover", "./..."]
    runtime_errors = 0

    print("Running tests...")
    try:
        result = subprocess.run(
            test_cmd,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            cwd=project_dir,
            text=True
        )
        output = result.stdout
        print(output)
        stderr = result.stderr
        print(stderr)

        # Check if the subprocess exited with a non-zero return code
        if result.returncode != 0:
            # Parse output for timeout messages
            if "test timed out after" in output or "panic: test timed out" in output:
                print("Tests failed due to timeout")
                # Handle timeout scenario
                return {
                    "test_pass_rate": 0,
                    "execution_time_sec": None,
                    "line_coverage_percent": None,
                    "branch_coverage_percent": None,
                    "timeout": False,
                    "internal_error_occurred": False,
                    "runtime_errors_count": None
                }
            else:
                runtime_error_pattern = re.compile(
                    r"(panic:|fatal error:|runtime error:)", re.IGNORECASE
                )
                assertion_error_pattern = re.compile(r"assert")

                # Search for runtime errors in stdout and stderr
                for o in (output, stderr):
                    for line in o.splitlines():
                        # Match lines with runtime errors but exclude assertions
                        if runtime_error_pattern.search(line) and not assertion_error_pattern.search(line):
                            runtime_errors = runtime_errors + 1

        # Parse the JSON output
        passed_tests = 0
        failed_tests = 0
        total_time = 0.0
        coverage = None
        summary_time = None

        for line in output.splitlines():
            if not line.strip():
                continue
            try:
                data = json.loads(line)
            except json.JSONDecodeError:
                continue  # Skip lines that aren't valid JSON

            # Check for test results
            if data.get('Action') == 'pass' and 'Test' in data:
                passed_tests += 1
                total_time += data.get('Elapsed', 0)
            elif data.get('Action') == 'fail' and 'Test' in data:
                failed_tests += 1
                total_time += data.get('Elapsed', 0)

            if (data.get('Action') == 'pass' or data.get('Action') == 'fail') and 'Test' not in data and 'Elapsed' in data:
                summary_time = data["Elapsed"]

            # Check for coverage information
            if data.get('Action') == 'output' and 'Output' in data:
                output_line = data['Output'].strip()
                if output_line.startswith("coverage:"):
                    # Extract the coverage percentage
                    match = re.search(r'coverage: ([\d\.]+)%', output_line)
                    if match:
                        coverage = float(match.group(1))

            # Alternative: Some versions include 'Coverage' field
            elif data.get('Action') == 'pass' and 'Coverage' in data:
                coverage = data['Coverage']

        total_tests = passed_tests + failed_tests
        pass_percentage = round((passed_tests / total_tests) * 100, 2) if total_tests > 0 else 0

        v = {
            "test_pass_rate": pass_percentage,
            "execution_time_sec": summary_time if summary_time is not None else total_time,
            "line_coverage_percent": coverage,
            "branch_coverage_percent": None,
            "timeout": False,
            "internal_error_occurred": False,
            "runtime_errors_count": runtime_errors
        }
        return v
    except Exception as e:
        print(f"Error running Go tests: {e}")
        return {
            "test_pass_rate": 0,
            "execution_time_sec": None,
            "line_coverage_percent": None,
            "branch_coverage_percent": None,
            "timeout": True,
            "internal_error_occurred": True,
            "runtime_errors_count": runtime_errors
        }


def x_run_tests__mutmut_40(project_dir):
    test_cmd = ["go", "test", "-timeout", "30s", "-json", "-cover", "./..."]
    runtime_errors = 0

    print("Running tests...")
    try:
        result = subprocess.run(
            test_cmd,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            cwd=project_dir,
            text=True
        )
        output = result.stdout
        print(output)
        stderr = result.stderr
        print(stderr)

        # Check if the subprocess exited with a non-zero return code
        if result.returncode != 0:
            # Parse output for timeout messages
            if "test timed out after" in output or "panic: test timed out" in output:
                print("Tests failed due to timeout")
                # Handle timeout scenario
                return {
                    "test_pass_rate": 0,
                    "execution_time_sec": None,
                    "line_coverage_percent": None,
                    "branch_coverage_percent": None,
                    "timeout": True,
                    "XXinternal_error_occurredXX": False,
                    "runtime_errors_count": None
                }
            else:
                runtime_error_pattern = re.compile(
                    r"(panic:|fatal error:|runtime error:)", re.IGNORECASE
                )
                assertion_error_pattern = re.compile(r"assert")

                # Search for runtime errors in stdout and stderr
                for o in (output, stderr):
                    for line in o.splitlines():
                        # Match lines with runtime errors but exclude assertions
                        if runtime_error_pattern.search(line) and not assertion_error_pattern.search(line):
                            runtime_errors = runtime_errors + 1

        # Parse the JSON output
        passed_tests = 0
        failed_tests = 0
        total_time = 0.0
        coverage = None
        summary_time = None

        for line in output.splitlines():
            if not line.strip():
                continue
            try:
                data = json.loads(line)
            except json.JSONDecodeError:
                continue  # Skip lines that aren't valid JSON

            # Check for test results
            if data.get('Action') == 'pass' and 'Test' in data:
                passed_tests += 1
                total_time += data.get('Elapsed', 0)
            elif data.get('Action') == 'fail' and 'Test' in data:
                failed_tests += 1
                total_time += data.get('Elapsed', 0)

            if (data.get('Action') == 'pass' or data.get('Action') == 'fail') and 'Test' not in data and 'Elapsed' in data:
                summary_time = data["Elapsed"]

            # Check for coverage information
            if data.get('Action') == 'output' and 'Output' in data:
                output_line = data['Output'].strip()
                if output_line.startswith("coverage:"):
                    # Extract the coverage percentage
                    match = re.search(r'coverage: ([\d\.]+)%', output_line)
                    if match:
                        coverage = float(match.group(1))

            # Alternative: Some versions include 'Coverage' field
            elif data.get('Action') == 'pass' and 'Coverage' in data:
                coverage = data['Coverage']

        total_tests = passed_tests + failed_tests
        pass_percentage = round((passed_tests / total_tests) * 100, 2) if total_tests > 0 else 0

        v = {
            "test_pass_rate": pass_percentage,
            "execution_time_sec": summary_time if summary_time is not None else total_time,
            "line_coverage_percent": coverage,
            "branch_coverage_percent": None,
            "timeout": False,
            "internal_error_occurred": False,
            "runtime_errors_count": runtime_errors
        }
        return v
    except Exception as e:
        print(f"Error running Go tests: {e}")
        return {
            "test_pass_rate": 0,
            "execution_time_sec": None,
            "line_coverage_percent": None,
            "branch_coverage_percent": None,
            "timeout": True,
            "internal_error_occurred": True,
            "runtime_errors_count": runtime_errors
        }


def x_run_tests__mutmut_41(project_dir):
    test_cmd = ["go", "test", "-timeout", "30s", "-json", "-cover", "./..."]
    runtime_errors = 0

    print("Running tests...")
    try:
        result = subprocess.run(
            test_cmd,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            cwd=project_dir,
            text=True
        )
        output = result.stdout
        print(output)
        stderr = result.stderr
        print(stderr)

        # Check if the subprocess exited with a non-zero return code
        if result.returncode != 0:
            # Parse output for timeout messages
            if "test timed out after" in output or "panic: test timed out" in output:
                print("Tests failed due to timeout")
                # Handle timeout scenario
                return {
                    "test_pass_rate": 0,
                    "execution_time_sec": None,
                    "line_coverage_percent": None,
                    "branch_coverage_percent": None,
                    "timeout": True,
                    "internal_error_occurred": True,
                    "runtime_errors_count": None
                }
            else:
                runtime_error_pattern = re.compile(
                    r"(panic:|fatal error:|runtime error:)", re.IGNORECASE
                )
                assertion_error_pattern = re.compile(r"assert")

                # Search for runtime errors in stdout and stderr
                for o in (output, stderr):
                    for line in o.splitlines():
                        # Match lines with runtime errors but exclude assertions
                        if runtime_error_pattern.search(line) and not assertion_error_pattern.search(line):
                            runtime_errors = runtime_errors + 1

        # Parse the JSON output
        passed_tests = 0
        failed_tests = 0
        total_time = 0.0
        coverage = None
        summary_time = None

        for line in output.splitlines():
            if not line.strip():
                continue
            try:
                data = json.loads(line)
            except json.JSONDecodeError:
                continue  # Skip lines that aren't valid JSON

            # Check for test results
            if data.get('Action') == 'pass' and 'Test' in data:
                passed_tests += 1
                total_time += data.get('Elapsed', 0)
            elif data.get('Action') == 'fail' and 'Test' in data:
                failed_tests += 1
                total_time += data.get('Elapsed', 0)

            if (data.get('Action') == 'pass' or data.get('Action') == 'fail') and 'Test' not in data and 'Elapsed' in data:
                summary_time = data["Elapsed"]

            # Check for coverage information
            if data.get('Action') == 'output' and 'Output' in data:
                output_line = data['Output'].strip()
                if output_line.startswith("coverage:"):
                    # Extract the coverage percentage
                    match = re.search(r'coverage: ([\d\.]+)%', output_line)
                    if match:
                        coverage = float(match.group(1))

            # Alternative: Some versions include 'Coverage' field
            elif data.get('Action') == 'pass' and 'Coverage' in data:
                coverage = data['Coverage']

        total_tests = passed_tests + failed_tests
        pass_percentage = round((passed_tests / total_tests) * 100, 2) if total_tests > 0 else 0

        v = {
            "test_pass_rate": pass_percentage,
            "execution_time_sec": summary_time if summary_time is not None else total_time,
            "line_coverage_percent": coverage,
            "branch_coverage_percent": None,
            "timeout": False,
            "internal_error_occurred": False,
            "runtime_errors_count": runtime_errors
        }
        return v
    except Exception as e:
        print(f"Error running Go tests: {e}")
        return {
            "test_pass_rate": 0,
            "execution_time_sec": None,
            "line_coverage_percent": None,
            "branch_coverage_percent": None,
            "timeout": True,
            "internal_error_occurred": True,
            "runtime_errors_count": runtime_errors
        }


def x_run_tests__mutmut_42(project_dir):
    test_cmd = ["go", "test", "-timeout", "30s", "-json", "-cover", "./..."]
    runtime_errors = 0

    print("Running tests...")
    try:
        result = subprocess.run(
            test_cmd,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            cwd=project_dir,
            text=True
        )
        output = result.stdout
        print(output)
        stderr = result.stderr
        print(stderr)

        # Check if the subprocess exited with a non-zero return code
        if result.returncode != 0:
            # Parse output for timeout messages
            if "test timed out after" in output or "panic: test timed out" in output:
                print("Tests failed due to timeout")
                # Handle timeout scenario
                return {
                    "test_pass_rate": 0,
                    "execution_time_sec": None,
                    "line_coverage_percent": None,
                    "branch_coverage_percent": None,
                    "timeout": True,
                    "internal_error_occurred": False,
                    "XXruntime_errors_countXX": None
                }
            else:
                runtime_error_pattern = re.compile(
                    r"(panic:|fatal error:|runtime error:)", re.IGNORECASE
                )
                assertion_error_pattern = re.compile(r"assert")

                # Search for runtime errors in stdout and stderr
                for o in (output, stderr):
                    for line in o.splitlines():
                        # Match lines with runtime errors but exclude assertions
                        if runtime_error_pattern.search(line) and not assertion_error_pattern.search(line):
                            runtime_errors = runtime_errors + 1

        # Parse the JSON output
        passed_tests = 0
        failed_tests = 0
        total_time = 0.0
        coverage = None
        summary_time = None

        for line in output.splitlines():
            if not line.strip():
                continue
            try:
                data = json.loads(line)
            except json.JSONDecodeError:
                continue  # Skip lines that aren't valid JSON

            # Check for test results
            if data.get('Action') == 'pass' and 'Test' in data:
                passed_tests += 1
                total_time += data.get('Elapsed', 0)
            elif data.get('Action') == 'fail' and 'Test' in data:
                failed_tests += 1
                total_time += data.get('Elapsed', 0)

            if (data.get('Action') == 'pass' or data.get('Action') == 'fail') and 'Test' not in data and 'Elapsed' in data:
                summary_time = data["Elapsed"]

            # Check for coverage information
            if data.get('Action') == 'output' and 'Output' in data:
                output_line = data['Output'].strip()
                if output_line.startswith("coverage:"):
                    # Extract the coverage percentage
                    match = re.search(r'coverage: ([\d\.]+)%', output_line)
                    if match:
                        coverage = float(match.group(1))

            # Alternative: Some versions include 'Coverage' field
            elif data.get('Action') == 'pass' and 'Coverage' in data:
                coverage = data['Coverage']

        total_tests = passed_tests + failed_tests
        pass_percentage = round((passed_tests / total_tests) * 100, 2) if total_tests > 0 else 0

        v = {
            "test_pass_rate": pass_percentage,
            "execution_time_sec": summary_time if summary_time is not None else total_time,
            "line_coverage_percent": coverage,
            "branch_coverage_percent": None,
            "timeout": False,
            "internal_error_occurred": False,
            "runtime_errors_count": runtime_errors
        }
        return v
    except Exception as e:
        print(f"Error running Go tests: {e}")
        return {
            "test_pass_rate": 0,
            "execution_time_sec": None,
            "line_coverage_percent": None,
            "branch_coverage_percent": None,
            "timeout": True,
            "internal_error_occurred": True,
            "runtime_errors_count": runtime_errors
        }


def x_run_tests__mutmut_43(project_dir):
    test_cmd = ["go", "test", "-timeout", "30s", "-json", "-cover", "./..."]
    runtime_errors = 0

    print("Running tests...")
    try:
        result = subprocess.run(
            test_cmd,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            cwd=project_dir,
            text=True
        )
        output = result.stdout
        print(output)
        stderr = result.stderr
        print(stderr)

        # Check if the subprocess exited with a non-zero return code
        if result.returncode != 0:
            # Parse output for timeout messages
            if "test timed out after" in output or "panic: test timed out" in output:
                print("Tests failed due to timeout")
                # Handle timeout scenario
                return {
                    "test_pass_rate": 0,
                    "execution_time_sec": None,
                    "line_coverage_percent": None,
                    "branch_coverage_percent": None,
                    "timeout": True,
                    "internal_error_occurred": False,
                    "runtime_errors_count": None
                }
            else:
                runtime_error_pattern = re.compile(
                    r"XX(panic:|fatal error:|runtime error:)XX", re.IGNORECASE
                )
                assertion_error_pattern = re.compile(r"assert")

                # Search for runtime errors in stdout and stderr
                for o in (output, stderr):
                    for line in o.splitlines():
                        # Match lines with runtime errors but exclude assertions
                        if runtime_error_pattern.search(line) and not assertion_error_pattern.search(line):
                            runtime_errors = runtime_errors + 1

        # Parse the JSON output
        passed_tests = 0
        failed_tests = 0
        total_time = 0.0
        coverage = None
        summary_time = None

        for line in output.splitlines():
            if not line.strip():
                continue
            try:
                data = json.loads(line)
            except json.JSONDecodeError:
                continue  # Skip lines that aren't valid JSON

            # Check for test results
            if data.get('Action') == 'pass' and 'Test' in data:
                passed_tests += 1
                total_time += data.get('Elapsed', 0)
            elif data.get('Action') == 'fail' and 'Test' in data:
                failed_tests += 1
                total_time += data.get('Elapsed', 0)

            if (data.get('Action') == 'pass' or data.get('Action') == 'fail') and 'Test' not in data and 'Elapsed' in data:
                summary_time = data["Elapsed"]

            # Check for coverage information
            if data.get('Action') == 'output' and 'Output' in data:
                output_line = data['Output'].strip()
                if output_line.startswith("coverage:"):
                    # Extract the coverage percentage
                    match = re.search(r'coverage: ([\d\.]+)%', output_line)
                    if match:
                        coverage = float(match.group(1))

            # Alternative: Some versions include 'Coverage' field
            elif data.get('Action') == 'pass' and 'Coverage' in data:
                coverage = data['Coverage']

        total_tests = passed_tests + failed_tests
        pass_percentage = round((passed_tests / total_tests) * 100, 2) if total_tests > 0 else 0

        v = {
            "test_pass_rate": pass_percentage,
            "execution_time_sec": summary_time if summary_time is not None else total_time,
            "line_coverage_percent": coverage,
            "branch_coverage_percent": None,
            "timeout": False,
            "internal_error_occurred": False,
            "runtime_errors_count": runtime_errors
        }
        return v
    except Exception as e:
        print(f"Error running Go tests: {e}")
        return {
            "test_pass_rate": 0,
            "execution_time_sec": None,
            "line_coverage_percent": None,
            "branch_coverage_percent": None,
            "timeout": True,
            "internal_error_occurred": True,
            "runtime_errors_count": runtime_errors
        }


def x_run_tests__mutmut_44(project_dir):
    test_cmd = ["go", "test", "-timeout", "30s", "-json", "-cover", "./..."]
    runtime_errors = 0

    print("Running tests...")
    try:
        result = subprocess.run(
            test_cmd,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            cwd=project_dir,
            text=True
        )
        output = result.stdout
        print(output)
        stderr = result.stderr
        print(stderr)

        # Check if the subprocess exited with a non-zero return code
        if result.returncode != 0:
            # Parse output for timeout messages
            if "test timed out after" in output or "panic: test timed out" in output:
                print("Tests failed due to timeout")
                # Handle timeout scenario
                return {
                    "test_pass_rate": 0,
                    "execution_time_sec": None,
                    "line_coverage_percent": None,
                    "branch_coverage_percent": None,
                    "timeout": True,
                    "internal_error_occurred": False,
                    "runtime_errors_count": None
                }
            else:
                runtime_error_pattern = None
                assertion_error_pattern = re.compile(r"assert")

                # Search for runtime errors in stdout and stderr
                for o in (output, stderr):
                    for line in o.splitlines():
                        # Match lines with runtime errors but exclude assertions
                        if runtime_error_pattern.search(line) and not assertion_error_pattern.search(line):
                            runtime_errors = runtime_errors + 1

        # Parse the JSON output
        passed_tests = 0
        failed_tests = 0
        total_time = 0.0
        coverage = None
        summary_time = None

        for line in output.splitlines():
            if not line.strip():
                continue
            try:
                data = json.loads(line)
            except json.JSONDecodeError:
                continue  # Skip lines that aren't valid JSON

            # Check for test results
            if data.get('Action') == 'pass' and 'Test' in data:
                passed_tests += 1
                total_time += data.get('Elapsed', 0)
            elif data.get('Action') == 'fail' and 'Test' in data:
                failed_tests += 1
                total_time += data.get('Elapsed', 0)

            if (data.get('Action') == 'pass' or data.get('Action') == 'fail') and 'Test' not in data and 'Elapsed' in data:
                summary_time = data["Elapsed"]

            # Check for coverage information
            if data.get('Action') == 'output' and 'Output' in data:
                output_line = data['Output'].strip()
                if output_line.startswith("coverage:"):
                    # Extract the coverage percentage
                    match = re.search(r'coverage: ([\d\.]+)%', output_line)
                    if match:
                        coverage = float(match.group(1))

            # Alternative: Some versions include 'Coverage' field
            elif data.get('Action') == 'pass' and 'Coverage' in data:
                coverage = data['Coverage']

        total_tests = passed_tests + failed_tests
        pass_percentage = round((passed_tests / total_tests) * 100, 2) if total_tests > 0 else 0

        v = {
            "test_pass_rate": pass_percentage,
            "execution_time_sec": summary_time if summary_time is not None else total_time,
            "line_coverage_percent": coverage,
            "branch_coverage_percent": None,
            "timeout": False,
            "internal_error_occurred": False,
            "runtime_errors_count": runtime_errors
        }
        return v
    except Exception as e:
        print(f"Error running Go tests: {e}")
        return {
            "test_pass_rate": 0,
            "execution_time_sec": None,
            "line_coverage_percent": None,
            "branch_coverage_percent": None,
            "timeout": True,
            "internal_error_occurred": True,
            "runtime_errors_count": runtime_errors
        }


def x_run_tests__mutmut_45(project_dir):
    test_cmd = ["go", "test", "-timeout", "30s", "-json", "-cover", "./..."]
    runtime_errors = 0

    print("Running tests...")
    try:
        result = subprocess.run(
            test_cmd,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            cwd=project_dir,
            text=True
        )
        output = result.stdout
        print(output)
        stderr = result.stderr
        print(stderr)

        # Check if the subprocess exited with a non-zero return code
        if result.returncode != 0:
            # Parse output for timeout messages
            if "test timed out after" in output or "panic: test timed out" in output:
                print("Tests failed due to timeout")
                # Handle timeout scenario
                return {
                    "test_pass_rate": 0,
                    "execution_time_sec": None,
                    "line_coverage_percent": None,
                    "branch_coverage_percent": None,
                    "timeout": True,
                    "internal_error_occurred": False,
                    "runtime_errors_count": None
                }
            else:
                runtime_error_pattern = re.compile(
                    r"(panic:|fatal error:|runtime error:)", re.IGNORECASE
                )
                assertion_error_pattern = re.compile(r"XXassertXX")

                # Search for runtime errors in stdout and stderr
                for o in (output, stderr):
                    for line in o.splitlines():
                        # Match lines with runtime errors but exclude assertions
                        if runtime_error_pattern.search(line) and not assertion_error_pattern.search(line):
                            runtime_errors = runtime_errors + 1

        # Parse the JSON output
        passed_tests = 0
        failed_tests = 0
        total_time = 0.0
        coverage = None
        summary_time = None

        for line in output.splitlines():
            if not line.strip():
                continue
            try:
                data = json.loads(line)
            except json.JSONDecodeError:
                continue  # Skip lines that aren't valid JSON

            # Check for test results
            if data.get('Action') == 'pass' and 'Test' in data:
                passed_tests += 1
                total_time += data.get('Elapsed', 0)
            elif data.get('Action') == 'fail' and 'Test' in data:
                failed_tests += 1
                total_time += data.get('Elapsed', 0)

            if (data.get('Action') == 'pass' or data.get('Action') == 'fail') and 'Test' not in data and 'Elapsed' in data:
                summary_time = data["Elapsed"]

            # Check for coverage information
            if data.get('Action') == 'output' and 'Output' in data:
                output_line = data['Output'].strip()
                if output_line.startswith("coverage:"):
                    # Extract the coverage percentage
                    match = re.search(r'coverage: ([\d\.]+)%', output_line)
                    if match:
                        coverage = float(match.group(1))

            # Alternative: Some versions include 'Coverage' field
            elif data.get('Action') == 'pass' and 'Coverage' in data:
                coverage = data['Coverage']

        total_tests = passed_tests + failed_tests
        pass_percentage = round((passed_tests / total_tests) * 100, 2) if total_tests > 0 else 0

        v = {
            "test_pass_rate": pass_percentage,
            "execution_time_sec": summary_time if summary_time is not None else total_time,
            "line_coverage_percent": coverage,
            "branch_coverage_percent": None,
            "timeout": False,
            "internal_error_occurred": False,
            "runtime_errors_count": runtime_errors
        }
        return v
    except Exception as e:
        print(f"Error running Go tests: {e}")
        return {
            "test_pass_rate": 0,
            "execution_time_sec": None,
            "line_coverage_percent": None,
            "branch_coverage_percent": None,
            "timeout": True,
            "internal_error_occurred": True,
            "runtime_errors_count": runtime_errors
        }


def x_run_tests__mutmut_46(project_dir):
    test_cmd = ["go", "test", "-timeout", "30s", "-json", "-cover", "./..."]
    runtime_errors = 0

    print("Running tests...")
    try:
        result = subprocess.run(
            test_cmd,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            cwd=project_dir,
            text=True
        )
        output = result.stdout
        print(output)
        stderr = result.stderr
        print(stderr)

        # Check if the subprocess exited with a non-zero return code
        if result.returncode != 0:
            # Parse output for timeout messages
            if "test timed out after" in output or "panic: test timed out" in output:
                print("Tests failed due to timeout")
                # Handle timeout scenario
                return {
                    "test_pass_rate": 0,
                    "execution_time_sec": None,
                    "line_coverage_percent": None,
                    "branch_coverage_percent": None,
                    "timeout": True,
                    "internal_error_occurred": False,
                    "runtime_errors_count": None
                }
            else:
                runtime_error_pattern = re.compile(
                    r"(panic:|fatal error:|runtime error:)", re.IGNORECASE
                )
                assertion_error_pattern = None

                # Search for runtime errors in stdout and stderr
                for o in (output, stderr):
                    for line in o.splitlines():
                        # Match lines with runtime errors but exclude assertions
                        if runtime_error_pattern.search(line) and not assertion_error_pattern.search(line):
                            runtime_errors = runtime_errors + 1

        # Parse the JSON output
        passed_tests = 0
        failed_tests = 0
        total_time = 0.0
        coverage = None
        summary_time = None

        for line in output.splitlines():
            if not line.strip():
                continue
            try:
                data = json.loads(line)
            except json.JSONDecodeError:
                continue  # Skip lines that aren't valid JSON

            # Check for test results
            if data.get('Action') == 'pass' and 'Test' in data:
                passed_tests += 1
                total_time += data.get('Elapsed', 0)
            elif data.get('Action') == 'fail' and 'Test' in data:
                failed_tests += 1
                total_time += data.get('Elapsed', 0)

            if (data.get('Action') == 'pass' or data.get('Action') == 'fail') and 'Test' not in data and 'Elapsed' in data:
                summary_time = data["Elapsed"]

            # Check for coverage information
            if data.get('Action') == 'output' and 'Output' in data:
                output_line = data['Output'].strip()
                if output_line.startswith("coverage:"):
                    # Extract the coverage percentage
                    match = re.search(r'coverage: ([\d\.]+)%', output_line)
                    if match:
                        coverage = float(match.group(1))

            # Alternative: Some versions include 'Coverage' field
            elif data.get('Action') == 'pass' and 'Coverage' in data:
                coverage = data['Coverage']

        total_tests = passed_tests + failed_tests
        pass_percentage = round((passed_tests / total_tests) * 100, 2) if total_tests > 0 else 0

        v = {
            "test_pass_rate": pass_percentage,
            "execution_time_sec": summary_time if summary_time is not None else total_time,
            "line_coverage_percent": coverage,
            "branch_coverage_percent": None,
            "timeout": False,
            "internal_error_occurred": False,
            "runtime_errors_count": runtime_errors
        }
        return v
    except Exception as e:
        print(f"Error running Go tests: {e}")
        return {
            "test_pass_rate": 0,
            "execution_time_sec": None,
            "line_coverage_percent": None,
            "branch_coverage_percent": None,
            "timeout": True,
            "internal_error_occurred": True,
            "runtime_errors_count": runtime_errors
        }


def x_run_tests__mutmut_47(project_dir):
    test_cmd = ["go", "test", "-timeout", "30s", "-json", "-cover", "./..."]
    runtime_errors = 0

    print("Running tests...")
    try:
        result = subprocess.run(
            test_cmd,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            cwd=project_dir,
            text=True
        )
        output = result.stdout
        print(output)
        stderr = result.stderr
        print(stderr)

        # Check if the subprocess exited with a non-zero return code
        if result.returncode != 0:
            # Parse output for timeout messages
            if "test timed out after" in output or "panic: test timed out" in output:
                print("Tests failed due to timeout")
                # Handle timeout scenario
                return {
                    "test_pass_rate": 0,
                    "execution_time_sec": None,
                    "line_coverage_percent": None,
                    "branch_coverage_percent": None,
                    "timeout": True,
                    "internal_error_occurred": False,
                    "runtime_errors_count": None
                }
            else:
                runtime_error_pattern = re.compile(
                    r"(panic:|fatal error:|runtime error:)", re.IGNORECASE
                )
                assertion_error_pattern = re.compile(r"assert")

                # Search for runtime errors in stdout and stderr
                for o in (output, stderr):
                    for line in o.splitlines():
                        # Match lines with runtime errors but exclude assertions
                        if runtime_error_pattern.search(None) and not assertion_error_pattern.search(line):
                            runtime_errors = runtime_errors + 1

        # Parse the JSON output
        passed_tests = 0
        failed_tests = 0
        total_time = 0.0
        coverage = None
        summary_time = None

        for line in output.splitlines():
            if not line.strip():
                continue
            try:
                data = json.loads(line)
            except json.JSONDecodeError:
                continue  # Skip lines that aren't valid JSON

            # Check for test results
            if data.get('Action') == 'pass' and 'Test' in data:
                passed_tests += 1
                total_time += data.get('Elapsed', 0)
            elif data.get('Action') == 'fail' and 'Test' in data:
                failed_tests += 1
                total_time += data.get('Elapsed', 0)

            if (data.get('Action') == 'pass' or data.get('Action') == 'fail') and 'Test' not in data and 'Elapsed' in data:
                summary_time = data["Elapsed"]

            # Check for coverage information
            if data.get('Action') == 'output' and 'Output' in data:
                output_line = data['Output'].strip()
                if output_line.startswith("coverage:"):
                    # Extract the coverage percentage
                    match = re.search(r'coverage: ([\d\.]+)%', output_line)
                    if match:
                        coverage = float(match.group(1))

            # Alternative: Some versions include 'Coverage' field
            elif data.get('Action') == 'pass' and 'Coverage' in data:
                coverage = data['Coverage']

        total_tests = passed_tests + failed_tests
        pass_percentage = round((passed_tests / total_tests) * 100, 2) if total_tests > 0 else 0

        v = {
            "test_pass_rate": pass_percentage,
            "execution_time_sec": summary_time if summary_time is not None else total_time,
            "line_coverage_percent": coverage,
            "branch_coverage_percent": None,
            "timeout": False,
            "internal_error_occurred": False,
            "runtime_errors_count": runtime_errors
        }
        return v
    except Exception as e:
        print(f"Error running Go tests: {e}")
        return {
            "test_pass_rate": 0,
            "execution_time_sec": None,
            "line_coverage_percent": None,
            "branch_coverage_percent": None,
            "timeout": True,
            "internal_error_occurred": True,
            "runtime_errors_count": runtime_errors
        }


def x_run_tests__mutmut_48(project_dir):
    test_cmd = ["go", "test", "-timeout", "30s", "-json", "-cover", "./..."]
    runtime_errors = 0

    print("Running tests...")
    try:
        result = subprocess.run(
            test_cmd,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            cwd=project_dir,
            text=True
        )
        output = result.stdout
        print(output)
        stderr = result.stderr
        print(stderr)

        # Check if the subprocess exited with a non-zero return code
        if result.returncode != 0:
            # Parse output for timeout messages
            if "test timed out after" in output or "panic: test timed out" in output:
                print("Tests failed due to timeout")
                # Handle timeout scenario
                return {
                    "test_pass_rate": 0,
                    "execution_time_sec": None,
                    "line_coverage_percent": None,
                    "branch_coverage_percent": None,
                    "timeout": True,
                    "internal_error_occurred": False,
                    "runtime_errors_count": None
                }
            else:
                runtime_error_pattern = re.compile(
                    r"(panic:|fatal error:|runtime error:)", re.IGNORECASE
                )
                assertion_error_pattern = re.compile(r"assert")

                # Search for runtime errors in stdout and stderr
                for o in (output, stderr):
                    for line in o.splitlines():
                        # Match lines with runtime errors but exclude assertions
                        if runtime_error_pattern.search(line) and  assertion_error_pattern.search(line):
                            runtime_errors = runtime_errors + 1

        # Parse the JSON output
        passed_tests = 0
        failed_tests = 0
        total_time = 0.0
        coverage = None
        summary_time = None

        for line in output.splitlines():
            if not line.strip():
                continue
            try:
                data = json.loads(line)
            except json.JSONDecodeError:
                continue  # Skip lines that aren't valid JSON

            # Check for test results
            if data.get('Action') == 'pass' and 'Test' in data:
                passed_tests += 1
                total_time += data.get('Elapsed', 0)
            elif data.get('Action') == 'fail' and 'Test' in data:
                failed_tests += 1
                total_time += data.get('Elapsed', 0)

            if (data.get('Action') == 'pass' or data.get('Action') == 'fail') and 'Test' not in data and 'Elapsed' in data:
                summary_time = data["Elapsed"]

            # Check for coverage information
            if data.get('Action') == 'output' and 'Output' in data:
                output_line = data['Output'].strip()
                if output_line.startswith("coverage:"):
                    # Extract the coverage percentage
                    match = re.search(r'coverage: ([\d\.]+)%', output_line)
                    if match:
                        coverage = float(match.group(1))

            # Alternative: Some versions include 'Coverage' field
            elif data.get('Action') == 'pass' and 'Coverage' in data:
                coverage = data['Coverage']

        total_tests = passed_tests + failed_tests
        pass_percentage = round((passed_tests / total_tests) * 100, 2) if total_tests > 0 else 0

        v = {
            "test_pass_rate": pass_percentage,
            "execution_time_sec": summary_time if summary_time is not None else total_time,
            "line_coverage_percent": coverage,
            "branch_coverage_percent": None,
            "timeout": False,
            "internal_error_occurred": False,
            "runtime_errors_count": runtime_errors
        }
        return v
    except Exception as e:
        print(f"Error running Go tests: {e}")
        return {
            "test_pass_rate": 0,
            "execution_time_sec": None,
            "line_coverage_percent": None,
            "branch_coverage_percent": None,
            "timeout": True,
            "internal_error_occurred": True,
            "runtime_errors_count": runtime_errors
        }


def x_run_tests__mutmut_49(project_dir):
    test_cmd = ["go", "test", "-timeout", "30s", "-json", "-cover", "./..."]
    runtime_errors = 0

    print("Running tests...")
    try:
        result = subprocess.run(
            test_cmd,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            cwd=project_dir,
            text=True
        )
        output = result.stdout
        print(output)
        stderr = result.stderr
        print(stderr)

        # Check if the subprocess exited with a non-zero return code
        if result.returncode != 0:
            # Parse output for timeout messages
            if "test timed out after" in output or "panic: test timed out" in output:
                print("Tests failed due to timeout")
                # Handle timeout scenario
                return {
                    "test_pass_rate": 0,
                    "execution_time_sec": None,
                    "line_coverage_percent": None,
                    "branch_coverage_percent": None,
                    "timeout": True,
                    "internal_error_occurred": False,
                    "runtime_errors_count": None
                }
            else:
                runtime_error_pattern = re.compile(
                    r"(panic:|fatal error:|runtime error:)", re.IGNORECASE
                )
                assertion_error_pattern = re.compile(r"assert")

                # Search for runtime errors in stdout and stderr
                for o in (output, stderr):
                    for line in o.splitlines():
                        # Match lines with runtime errors but exclude assertions
                        if runtime_error_pattern.search(line) and not assertion_error_pattern.search(None):
                            runtime_errors = runtime_errors + 1

        # Parse the JSON output
        passed_tests = 0
        failed_tests = 0
        total_time = 0.0
        coverage = None
        summary_time = None

        for line in output.splitlines():
            if not line.strip():
                continue
            try:
                data = json.loads(line)
            except json.JSONDecodeError:
                continue  # Skip lines that aren't valid JSON

            # Check for test results
            if data.get('Action') == 'pass' and 'Test' in data:
                passed_tests += 1
                total_time += data.get('Elapsed', 0)
            elif data.get('Action') == 'fail' and 'Test' in data:
                failed_tests += 1
                total_time += data.get('Elapsed', 0)

            if (data.get('Action') == 'pass' or data.get('Action') == 'fail') and 'Test' not in data and 'Elapsed' in data:
                summary_time = data["Elapsed"]

            # Check for coverage information
            if data.get('Action') == 'output' and 'Output' in data:
                output_line = data['Output'].strip()
                if output_line.startswith("coverage:"):
                    # Extract the coverage percentage
                    match = re.search(r'coverage: ([\d\.]+)%', output_line)
                    if match:
                        coverage = float(match.group(1))

            # Alternative: Some versions include 'Coverage' field
            elif data.get('Action') == 'pass' and 'Coverage' in data:
                coverage = data['Coverage']

        total_tests = passed_tests + failed_tests
        pass_percentage = round((passed_tests / total_tests) * 100, 2) if total_tests > 0 else 0

        v = {
            "test_pass_rate": pass_percentage,
            "execution_time_sec": summary_time if summary_time is not None else total_time,
            "line_coverage_percent": coverage,
            "branch_coverage_percent": None,
            "timeout": False,
            "internal_error_occurred": False,
            "runtime_errors_count": runtime_errors
        }
        return v
    except Exception as e:
        print(f"Error running Go tests: {e}")
        return {
            "test_pass_rate": 0,
            "execution_time_sec": None,
            "line_coverage_percent": None,
            "branch_coverage_percent": None,
            "timeout": True,
            "internal_error_occurred": True,
            "runtime_errors_count": runtime_errors
        }


def x_run_tests__mutmut_50(project_dir):
    test_cmd = ["go", "test", "-timeout", "30s", "-json", "-cover", "./..."]
    runtime_errors = 0

    print("Running tests...")
    try:
        result = subprocess.run(
            test_cmd,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            cwd=project_dir,
            text=True
        )
        output = result.stdout
        print(output)
        stderr = result.stderr
        print(stderr)

        # Check if the subprocess exited with a non-zero return code
        if result.returncode != 0:
            # Parse output for timeout messages
            if "test timed out after" in output or "panic: test timed out" in output:
                print("Tests failed due to timeout")
                # Handle timeout scenario
                return {
                    "test_pass_rate": 0,
                    "execution_time_sec": None,
                    "line_coverage_percent": None,
                    "branch_coverage_percent": None,
                    "timeout": True,
                    "internal_error_occurred": False,
                    "runtime_errors_count": None
                }
            else:
                runtime_error_pattern = re.compile(
                    r"(panic:|fatal error:|runtime error:)", re.IGNORECASE
                )
                assertion_error_pattern = re.compile(r"assert")

                # Search for runtime errors in stdout and stderr
                for o in (output, stderr):
                    for line in o.splitlines():
                        # Match lines with runtime errors but exclude assertions
                        if runtime_error_pattern.search(line) or not assertion_error_pattern.search(line):
                            runtime_errors = runtime_errors + 1

        # Parse the JSON output
        passed_tests = 0
        failed_tests = 0
        total_time = 0.0
        coverage = None
        summary_time = None

        for line in output.splitlines():
            if not line.strip():
                continue
            try:
                data = json.loads(line)
            except json.JSONDecodeError:
                continue  # Skip lines that aren't valid JSON

            # Check for test results
            if data.get('Action') == 'pass' and 'Test' in data:
                passed_tests += 1
                total_time += data.get('Elapsed', 0)
            elif data.get('Action') == 'fail' and 'Test' in data:
                failed_tests += 1
                total_time += data.get('Elapsed', 0)

            if (data.get('Action') == 'pass' or data.get('Action') == 'fail') and 'Test' not in data and 'Elapsed' in data:
                summary_time = data["Elapsed"]

            # Check for coverage information
            if data.get('Action') == 'output' and 'Output' in data:
                output_line = data['Output'].strip()
                if output_line.startswith("coverage:"):
                    # Extract the coverage percentage
                    match = re.search(r'coverage: ([\d\.]+)%', output_line)
                    if match:
                        coverage = float(match.group(1))

            # Alternative: Some versions include 'Coverage' field
            elif data.get('Action') == 'pass' and 'Coverage' in data:
                coverage = data['Coverage']

        total_tests = passed_tests + failed_tests
        pass_percentage = round((passed_tests / total_tests) * 100, 2) if total_tests > 0 else 0

        v = {
            "test_pass_rate": pass_percentage,
            "execution_time_sec": summary_time if summary_time is not None else total_time,
            "line_coverage_percent": coverage,
            "branch_coverage_percent": None,
            "timeout": False,
            "internal_error_occurred": False,
            "runtime_errors_count": runtime_errors
        }
        return v
    except Exception as e:
        print(f"Error running Go tests: {e}")
        return {
            "test_pass_rate": 0,
            "execution_time_sec": None,
            "line_coverage_percent": None,
            "branch_coverage_percent": None,
            "timeout": True,
            "internal_error_occurred": True,
            "runtime_errors_count": runtime_errors
        }


def x_run_tests__mutmut_51(project_dir):
    test_cmd = ["go", "test", "-timeout", "30s", "-json", "-cover", "./..."]
    runtime_errors = 0

    print("Running tests...")
    try:
        result = subprocess.run(
            test_cmd,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            cwd=project_dir,
            text=True
        )
        output = result.stdout
        print(output)
        stderr = result.stderr
        print(stderr)

        # Check if the subprocess exited with a non-zero return code
        if result.returncode != 0:
            # Parse output for timeout messages
            if "test timed out after" in output or "panic: test timed out" in output:
                print("Tests failed due to timeout")
                # Handle timeout scenario
                return {
                    "test_pass_rate": 0,
                    "execution_time_sec": None,
                    "line_coverage_percent": None,
                    "branch_coverage_percent": None,
                    "timeout": True,
                    "internal_error_occurred": False,
                    "runtime_errors_count": None
                }
            else:
                runtime_error_pattern = re.compile(
                    r"(panic:|fatal error:|runtime error:)", re.IGNORECASE
                )
                assertion_error_pattern = re.compile(r"assert")

                # Search for runtime errors in stdout and stderr
                for o in (output, stderr):
                    for line in o.splitlines():
                        # Match lines with runtime errors but exclude assertions
                        if runtime_error_pattern.search(line) and not assertion_error_pattern.search(line):
                            runtime_errors = runtime_errors - 1

        # Parse the JSON output
        passed_tests = 0
        failed_tests = 0
        total_time = 0.0
        coverage = None
        summary_time = None

        for line in output.splitlines():
            if not line.strip():
                continue
            try:
                data = json.loads(line)
            except json.JSONDecodeError:
                continue  # Skip lines that aren't valid JSON

            # Check for test results
            if data.get('Action') == 'pass' and 'Test' in data:
                passed_tests += 1
                total_time += data.get('Elapsed', 0)
            elif data.get('Action') == 'fail' and 'Test' in data:
                failed_tests += 1
                total_time += data.get('Elapsed', 0)

            if (data.get('Action') == 'pass' or data.get('Action') == 'fail') and 'Test' not in data and 'Elapsed' in data:
                summary_time = data["Elapsed"]

            # Check for coverage information
            if data.get('Action') == 'output' and 'Output' in data:
                output_line = data['Output'].strip()
                if output_line.startswith("coverage:"):
                    # Extract the coverage percentage
                    match = re.search(r'coverage: ([\d\.]+)%', output_line)
                    if match:
                        coverage = float(match.group(1))

            # Alternative: Some versions include 'Coverage' field
            elif data.get('Action') == 'pass' and 'Coverage' in data:
                coverage = data['Coverage']

        total_tests = passed_tests + failed_tests
        pass_percentage = round((passed_tests / total_tests) * 100, 2) if total_tests > 0 else 0

        v = {
            "test_pass_rate": pass_percentage,
            "execution_time_sec": summary_time if summary_time is not None else total_time,
            "line_coverage_percent": coverage,
            "branch_coverage_percent": None,
            "timeout": False,
            "internal_error_occurred": False,
            "runtime_errors_count": runtime_errors
        }
        return v
    except Exception as e:
        print(f"Error running Go tests: {e}")
        return {
            "test_pass_rate": 0,
            "execution_time_sec": None,
            "line_coverage_percent": None,
            "branch_coverage_percent": None,
            "timeout": True,
            "internal_error_occurred": True,
            "runtime_errors_count": runtime_errors
        }


def x_run_tests__mutmut_52(project_dir):
    test_cmd = ["go", "test", "-timeout", "30s", "-json", "-cover", "./..."]
    runtime_errors = 0

    print("Running tests...")
    try:
        result = subprocess.run(
            test_cmd,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            cwd=project_dir,
            text=True
        )
        output = result.stdout
        print(output)
        stderr = result.stderr
        print(stderr)

        # Check if the subprocess exited with a non-zero return code
        if result.returncode != 0:
            # Parse output for timeout messages
            if "test timed out after" in output or "panic: test timed out" in output:
                print("Tests failed due to timeout")
                # Handle timeout scenario
                return {
                    "test_pass_rate": 0,
                    "execution_time_sec": None,
                    "line_coverage_percent": None,
                    "branch_coverage_percent": None,
                    "timeout": True,
                    "internal_error_occurred": False,
                    "runtime_errors_count": None
                }
            else:
                runtime_error_pattern = re.compile(
                    r"(panic:|fatal error:|runtime error:)", re.IGNORECASE
                )
                assertion_error_pattern = re.compile(r"assert")

                # Search for runtime errors in stdout and stderr
                for o in (output, stderr):
                    for line in o.splitlines():
                        # Match lines with runtime errors but exclude assertions
                        if runtime_error_pattern.search(line) and not assertion_error_pattern.search(line):
                            runtime_errors = runtime_errors + 2

        # Parse the JSON output
        passed_tests = 0
        failed_tests = 0
        total_time = 0.0
        coverage = None
        summary_time = None

        for line in output.splitlines():
            if not line.strip():
                continue
            try:
                data = json.loads(line)
            except json.JSONDecodeError:
                continue  # Skip lines that aren't valid JSON

            # Check for test results
            if data.get('Action') == 'pass' and 'Test' in data:
                passed_tests += 1
                total_time += data.get('Elapsed', 0)
            elif data.get('Action') == 'fail' and 'Test' in data:
                failed_tests += 1
                total_time += data.get('Elapsed', 0)

            if (data.get('Action') == 'pass' or data.get('Action') == 'fail') and 'Test' not in data and 'Elapsed' in data:
                summary_time = data["Elapsed"]

            # Check for coverage information
            if data.get('Action') == 'output' and 'Output' in data:
                output_line = data['Output'].strip()
                if output_line.startswith("coverage:"):
                    # Extract the coverage percentage
                    match = re.search(r'coverage: ([\d\.]+)%', output_line)
                    if match:
                        coverage = float(match.group(1))

            # Alternative: Some versions include 'Coverage' field
            elif data.get('Action') == 'pass' and 'Coverage' in data:
                coverage = data['Coverage']

        total_tests = passed_tests + failed_tests
        pass_percentage = round((passed_tests / total_tests) * 100, 2) if total_tests > 0 else 0

        v = {
            "test_pass_rate": pass_percentage,
            "execution_time_sec": summary_time if summary_time is not None else total_time,
            "line_coverage_percent": coverage,
            "branch_coverage_percent": None,
            "timeout": False,
            "internal_error_occurred": False,
            "runtime_errors_count": runtime_errors
        }
        return v
    except Exception as e:
        print(f"Error running Go tests: {e}")
        return {
            "test_pass_rate": 0,
            "execution_time_sec": None,
            "line_coverage_percent": None,
            "branch_coverage_percent": None,
            "timeout": True,
            "internal_error_occurred": True,
            "runtime_errors_count": runtime_errors
        }


def x_run_tests__mutmut_53(project_dir):
    test_cmd = ["go", "test", "-timeout", "30s", "-json", "-cover", "./..."]
    runtime_errors = 0

    print("Running tests...")
    try:
        result = subprocess.run(
            test_cmd,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            cwd=project_dir,
            text=True
        )
        output = result.stdout
        print(output)
        stderr = result.stderr
        print(stderr)

        # Check if the subprocess exited with a non-zero return code
        if result.returncode != 0:
            # Parse output for timeout messages
            if "test timed out after" in output or "panic: test timed out" in output:
                print("Tests failed due to timeout")
                # Handle timeout scenario
                return {
                    "test_pass_rate": 0,
                    "execution_time_sec": None,
                    "line_coverage_percent": None,
                    "branch_coverage_percent": None,
                    "timeout": True,
                    "internal_error_occurred": False,
                    "runtime_errors_count": None
                }
            else:
                runtime_error_pattern = re.compile(
                    r"(panic:|fatal error:|runtime error:)", re.IGNORECASE
                )
                assertion_error_pattern = re.compile(r"assert")

                # Search for runtime errors in stdout and stderr
                for o in (output, stderr):
                    for line in o.splitlines():
                        # Match lines with runtime errors but exclude assertions
                        if runtime_error_pattern.search(line) and not assertion_error_pattern.search(line):
                            runtime_errors = None

        # Parse the JSON output
        passed_tests = 0
        failed_tests = 0
        total_time = 0.0
        coverage = None
        summary_time = None

        for line in output.splitlines():
            if not line.strip():
                continue
            try:
                data = json.loads(line)
            except json.JSONDecodeError:
                continue  # Skip lines that aren't valid JSON

            # Check for test results
            if data.get('Action') == 'pass' and 'Test' in data:
                passed_tests += 1
                total_time += data.get('Elapsed', 0)
            elif data.get('Action') == 'fail' and 'Test' in data:
                failed_tests += 1
                total_time += data.get('Elapsed', 0)

            if (data.get('Action') == 'pass' or data.get('Action') == 'fail') and 'Test' not in data and 'Elapsed' in data:
                summary_time = data["Elapsed"]

            # Check for coverage information
            if data.get('Action') == 'output' and 'Output' in data:
                output_line = data['Output'].strip()
                if output_line.startswith("coverage:"):
                    # Extract the coverage percentage
                    match = re.search(r'coverage: ([\d\.]+)%', output_line)
                    if match:
                        coverage = float(match.group(1))

            # Alternative: Some versions include 'Coverage' field
            elif data.get('Action') == 'pass' and 'Coverage' in data:
                coverage = data['Coverage']

        total_tests = passed_tests + failed_tests
        pass_percentage = round((passed_tests / total_tests) * 100, 2) if total_tests > 0 else 0

        v = {
            "test_pass_rate": pass_percentage,
            "execution_time_sec": summary_time if summary_time is not None else total_time,
            "line_coverage_percent": coverage,
            "branch_coverage_percent": None,
            "timeout": False,
            "internal_error_occurred": False,
            "runtime_errors_count": runtime_errors
        }
        return v
    except Exception as e:
        print(f"Error running Go tests: {e}")
        return {
            "test_pass_rate": 0,
            "execution_time_sec": None,
            "line_coverage_percent": None,
            "branch_coverage_percent": None,
            "timeout": True,
            "internal_error_occurred": True,
            "runtime_errors_count": runtime_errors
        }


def x_run_tests__mutmut_54(project_dir):
    test_cmd = ["go", "test", "-timeout", "30s", "-json", "-cover", "./..."]
    runtime_errors = 0

    print("Running tests...")
    try:
        result = subprocess.run(
            test_cmd,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            cwd=project_dir,
            text=True
        )
        output = result.stdout
        print(output)
        stderr = result.stderr
        print(stderr)

        # Check if the subprocess exited with a non-zero return code
        if result.returncode != 0:
            # Parse output for timeout messages
            if "test timed out after" in output or "panic: test timed out" in output:
                print("Tests failed due to timeout")
                # Handle timeout scenario
                return {
                    "test_pass_rate": 0,
                    "execution_time_sec": None,
                    "line_coverage_percent": None,
                    "branch_coverage_percent": None,
                    "timeout": True,
                    "internal_error_occurred": False,
                    "runtime_errors_count": None
                }
            else:
                runtime_error_pattern = re.compile(
                    r"(panic:|fatal error:|runtime error:)", re.IGNORECASE
                )
                assertion_error_pattern = re.compile(r"assert")

                # Search for runtime errors in stdout and stderr
                for o in (output, stderr):
                    for line in o.splitlines():
                        # Match lines with runtime errors but exclude assertions
                        if runtime_error_pattern.search(line) and not assertion_error_pattern.search(line):
                            runtime_errors = runtime_errors + 1

        # Parse the JSON output
        passed_tests = 1
        failed_tests = 0
        total_time = 0.0
        coverage = None
        summary_time = None

        for line in output.splitlines():
            if not line.strip():
                continue
            try:
                data = json.loads(line)
            except json.JSONDecodeError:
                continue  # Skip lines that aren't valid JSON

            # Check for test results
            if data.get('Action') == 'pass' and 'Test' in data:
                passed_tests += 1
                total_time += data.get('Elapsed', 0)
            elif data.get('Action') == 'fail' and 'Test' in data:
                failed_tests += 1
                total_time += data.get('Elapsed', 0)

            if (data.get('Action') == 'pass' or data.get('Action') == 'fail') and 'Test' not in data and 'Elapsed' in data:
                summary_time = data["Elapsed"]

            # Check for coverage information
            if data.get('Action') == 'output' and 'Output' in data:
                output_line = data['Output'].strip()
                if output_line.startswith("coverage:"):
                    # Extract the coverage percentage
                    match = re.search(r'coverage: ([\d\.]+)%', output_line)
                    if match:
                        coverage = float(match.group(1))

            # Alternative: Some versions include 'Coverage' field
            elif data.get('Action') == 'pass' and 'Coverage' in data:
                coverage = data['Coverage']

        total_tests = passed_tests + failed_tests
        pass_percentage = round((passed_tests / total_tests) * 100, 2) if total_tests > 0 else 0

        v = {
            "test_pass_rate": pass_percentage,
            "execution_time_sec": summary_time if summary_time is not None else total_time,
            "line_coverage_percent": coverage,
            "branch_coverage_percent": None,
            "timeout": False,
            "internal_error_occurred": False,
            "runtime_errors_count": runtime_errors
        }
        return v
    except Exception as e:
        print(f"Error running Go tests: {e}")
        return {
            "test_pass_rate": 0,
            "execution_time_sec": None,
            "line_coverage_percent": None,
            "branch_coverage_percent": None,
            "timeout": True,
            "internal_error_occurred": True,
            "runtime_errors_count": runtime_errors
        }


def x_run_tests__mutmut_55(project_dir):
    test_cmd = ["go", "test", "-timeout", "30s", "-json", "-cover", "./..."]
    runtime_errors = 0

    print("Running tests...")
    try:
        result = subprocess.run(
            test_cmd,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            cwd=project_dir,
            text=True
        )
        output = result.stdout
        print(output)
        stderr = result.stderr
        print(stderr)

        # Check if the subprocess exited with a non-zero return code
        if result.returncode != 0:
            # Parse output for timeout messages
            if "test timed out after" in output or "panic: test timed out" in output:
                print("Tests failed due to timeout")
                # Handle timeout scenario
                return {
                    "test_pass_rate": 0,
                    "execution_time_sec": None,
                    "line_coverage_percent": None,
                    "branch_coverage_percent": None,
                    "timeout": True,
                    "internal_error_occurred": False,
                    "runtime_errors_count": None
                }
            else:
                runtime_error_pattern = re.compile(
                    r"(panic:|fatal error:|runtime error:)", re.IGNORECASE
                )
                assertion_error_pattern = re.compile(r"assert")

                # Search for runtime errors in stdout and stderr
                for o in (output, stderr):
                    for line in o.splitlines():
                        # Match lines with runtime errors but exclude assertions
                        if runtime_error_pattern.search(line) and not assertion_error_pattern.search(line):
                            runtime_errors = runtime_errors + 1

        # Parse the JSON output
        passed_tests = None
        failed_tests = 0
        total_time = 0.0
        coverage = None
        summary_time = None

        for line in output.splitlines():
            if not line.strip():
                continue
            try:
                data = json.loads(line)
            except json.JSONDecodeError:
                continue  # Skip lines that aren't valid JSON

            # Check for test results
            if data.get('Action') == 'pass' and 'Test' in data:
                passed_tests += 1
                total_time += data.get('Elapsed', 0)
            elif data.get('Action') == 'fail' and 'Test' in data:
                failed_tests += 1
                total_time += data.get('Elapsed', 0)

            if (data.get('Action') == 'pass' or data.get('Action') == 'fail') and 'Test' not in data and 'Elapsed' in data:
                summary_time = data["Elapsed"]

            # Check for coverage information
            if data.get('Action') == 'output' and 'Output' in data:
                output_line = data['Output'].strip()
                if output_line.startswith("coverage:"):
                    # Extract the coverage percentage
                    match = re.search(r'coverage: ([\d\.]+)%', output_line)
                    if match:
                        coverage = float(match.group(1))

            # Alternative: Some versions include 'Coverage' field
            elif data.get('Action') == 'pass' and 'Coverage' in data:
                coverage = data['Coverage']

        total_tests = passed_tests + failed_tests
        pass_percentage = round((passed_tests / total_tests) * 100, 2) if total_tests > 0 else 0

        v = {
            "test_pass_rate": pass_percentage,
            "execution_time_sec": summary_time if summary_time is not None else total_time,
            "line_coverage_percent": coverage,
            "branch_coverage_percent": None,
            "timeout": False,
            "internal_error_occurred": False,
            "runtime_errors_count": runtime_errors
        }
        return v
    except Exception as e:
        print(f"Error running Go tests: {e}")
        return {
            "test_pass_rate": 0,
            "execution_time_sec": None,
            "line_coverage_percent": None,
            "branch_coverage_percent": None,
            "timeout": True,
            "internal_error_occurred": True,
            "runtime_errors_count": runtime_errors
        }


def x_run_tests__mutmut_56(project_dir):
    test_cmd = ["go", "test", "-timeout", "30s", "-json", "-cover", "./..."]
    runtime_errors = 0

    print("Running tests...")
    try:
        result = subprocess.run(
            test_cmd,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            cwd=project_dir,
            text=True
        )
        output = result.stdout
        print(output)
        stderr = result.stderr
        print(stderr)

        # Check if the subprocess exited with a non-zero return code
        if result.returncode != 0:
            # Parse output for timeout messages
            if "test timed out after" in output or "panic: test timed out" in output:
                print("Tests failed due to timeout")
                # Handle timeout scenario
                return {
                    "test_pass_rate": 0,
                    "execution_time_sec": None,
                    "line_coverage_percent": None,
                    "branch_coverage_percent": None,
                    "timeout": True,
                    "internal_error_occurred": False,
                    "runtime_errors_count": None
                }
            else:
                runtime_error_pattern = re.compile(
                    r"(panic:|fatal error:|runtime error:)", re.IGNORECASE
                )
                assertion_error_pattern = re.compile(r"assert")

                # Search for runtime errors in stdout and stderr
                for o in (output, stderr):
                    for line in o.splitlines():
                        # Match lines with runtime errors but exclude assertions
                        if runtime_error_pattern.search(line) and not assertion_error_pattern.search(line):
                            runtime_errors = runtime_errors + 1

        # Parse the JSON output
        passed_tests = 0
        failed_tests = 1
        total_time = 0.0
        coverage = None
        summary_time = None

        for line in output.splitlines():
            if not line.strip():
                continue
            try:
                data = json.loads(line)
            except json.JSONDecodeError:
                continue  # Skip lines that aren't valid JSON

            # Check for test results
            if data.get('Action') == 'pass' and 'Test' in data:
                passed_tests += 1
                total_time += data.get('Elapsed', 0)
            elif data.get('Action') == 'fail' and 'Test' in data:
                failed_tests += 1
                total_time += data.get('Elapsed', 0)

            if (data.get('Action') == 'pass' or data.get('Action') == 'fail') and 'Test' not in data and 'Elapsed' in data:
                summary_time = data["Elapsed"]

            # Check for coverage information
            if data.get('Action') == 'output' and 'Output' in data:
                output_line = data['Output'].strip()
                if output_line.startswith("coverage:"):
                    # Extract the coverage percentage
                    match = re.search(r'coverage: ([\d\.]+)%', output_line)
                    if match:
                        coverage = float(match.group(1))

            # Alternative: Some versions include 'Coverage' field
            elif data.get('Action') == 'pass' and 'Coverage' in data:
                coverage = data['Coverage']

        total_tests = passed_tests + failed_tests
        pass_percentage = round((passed_tests / total_tests) * 100, 2) if total_tests > 0 else 0

        v = {
            "test_pass_rate": pass_percentage,
            "execution_time_sec": summary_time if summary_time is not None else total_time,
            "line_coverage_percent": coverage,
            "branch_coverage_percent": None,
            "timeout": False,
            "internal_error_occurred": False,
            "runtime_errors_count": runtime_errors
        }
        return v
    except Exception as e:
        print(f"Error running Go tests: {e}")
        return {
            "test_pass_rate": 0,
            "execution_time_sec": None,
            "line_coverage_percent": None,
            "branch_coverage_percent": None,
            "timeout": True,
            "internal_error_occurred": True,
            "runtime_errors_count": runtime_errors
        }


def x_run_tests__mutmut_57(project_dir):
    test_cmd = ["go", "test", "-timeout", "30s", "-json", "-cover", "./..."]
    runtime_errors = 0

    print("Running tests...")
    try:
        result = subprocess.run(
            test_cmd,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            cwd=project_dir,
            text=True
        )
        output = result.stdout
        print(output)
        stderr = result.stderr
        print(stderr)

        # Check if the subprocess exited with a non-zero return code
        if result.returncode != 0:
            # Parse output for timeout messages
            if "test timed out after" in output or "panic: test timed out" in output:
                print("Tests failed due to timeout")
                # Handle timeout scenario
                return {
                    "test_pass_rate": 0,
                    "execution_time_sec": None,
                    "line_coverage_percent": None,
                    "branch_coverage_percent": None,
                    "timeout": True,
                    "internal_error_occurred": False,
                    "runtime_errors_count": None
                }
            else:
                runtime_error_pattern = re.compile(
                    r"(panic:|fatal error:|runtime error:)", re.IGNORECASE
                )
                assertion_error_pattern = re.compile(r"assert")

                # Search for runtime errors in stdout and stderr
                for o in (output, stderr):
                    for line in o.splitlines():
                        # Match lines with runtime errors but exclude assertions
                        if runtime_error_pattern.search(line) and not assertion_error_pattern.search(line):
                            runtime_errors = runtime_errors + 1

        # Parse the JSON output
        passed_tests = 0
        failed_tests = None
        total_time = 0.0
        coverage = None
        summary_time = None

        for line in output.splitlines():
            if not line.strip():
                continue
            try:
                data = json.loads(line)
            except json.JSONDecodeError:
                continue  # Skip lines that aren't valid JSON

            # Check for test results
            if data.get('Action') == 'pass' and 'Test' in data:
                passed_tests += 1
                total_time += data.get('Elapsed', 0)
            elif data.get('Action') == 'fail' and 'Test' in data:
                failed_tests += 1
                total_time += data.get('Elapsed', 0)

            if (data.get('Action') == 'pass' or data.get('Action') == 'fail') and 'Test' not in data and 'Elapsed' in data:
                summary_time = data["Elapsed"]

            # Check for coverage information
            if data.get('Action') == 'output' and 'Output' in data:
                output_line = data['Output'].strip()
                if output_line.startswith("coverage:"):
                    # Extract the coverage percentage
                    match = re.search(r'coverage: ([\d\.]+)%', output_line)
                    if match:
                        coverage = float(match.group(1))

            # Alternative: Some versions include 'Coverage' field
            elif data.get('Action') == 'pass' and 'Coverage' in data:
                coverage = data['Coverage']

        total_tests = passed_tests + failed_tests
        pass_percentage = round((passed_tests / total_tests) * 100, 2) if total_tests > 0 else 0

        v = {
            "test_pass_rate": pass_percentage,
            "execution_time_sec": summary_time if summary_time is not None else total_time,
            "line_coverage_percent": coverage,
            "branch_coverage_percent": None,
            "timeout": False,
            "internal_error_occurred": False,
            "runtime_errors_count": runtime_errors
        }
        return v
    except Exception as e:
        print(f"Error running Go tests: {e}")
        return {
            "test_pass_rate": 0,
            "execution_time_sec": None,
            "line_coverage_percent": None,
            "branch_coverage_percent": None,
            "timeout": True,
            "internal_error_occurred": True,
            "runtime_errors_count": runtime_errors
        }


def x_run_tests__mutmut_58(project_dir):
    test_cmd = ["go", "test", "-timeout", "30s", "-json", "-cover", "./..."]
    runtime_errors = 0

    print("Running tests...")
    try:
        result = subprocess.run(
            test_cmd,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            cwd=project_dir,
            text=True
        )
        output = result.stdout
        print(output)
        stderr = result.stderr
        print(stderr)

        # Check if the subprocess exited with a non-zero return code
        if result.returncode != 0:
            # Parse output for timeout messages
            if "test timed out after" in output or "panic: test timed out" in output:
                print("Tests failed due to timeout")
                # Handle timeout scenario
                return {
                    "test_pass_rate": 0,
                    "execution_time_sec": None,
                    "line_coverage_percent": None,
                    "branch_coverage_percent": None,
                    "timeout": True,
                    "internal_error_occurred": False,
                    "runtime_errors_count": None
                }
            else:
                runtime_error_pattern = re.compile(
                    r"(panic:|fatal error:|runtime error:)", re.IGNORECASE
                )
                assertion_error_pattern = re.compile(r"assert")

                # Search for runtime errors in stdout and stderr
                for o in (output, stderr):
                    for line in o.splitlines():
                        # Match lines with runtime errors but exclude assertions
                        if runtime_error_pattern.search(line) and not assertion_error_pattern.search(line):
                            runtime_errors = runtime_errors + 1

        # Parse the JSON output
        passed_tests = 0
        failed_tests = 0
        total_time = 1.0
        coverage = None
        summary_time = None

        for line in output.splitlines():
            if not line.strip():
                continue
            try:
                data = json.loads(line)
            except json.JSONDecodeError:
                continue  # Skip lines that aren't valid JSON

            # Check for test results
            if data.get('Action') == 'pass' and 'Test' in data:
                passed_tests += 1
                total_time += data.get('Elapsed', 0)
            elif data.get('Action') == 'fail' and 'Test' in data:
                failed_tests += 1
                total_time += data.get('Elapsed', 0)

            if (data.get('Action') == 'pass' or data.get('Action') == 'fail') and 'Test' not in data and 'Elapsed' in data:
                summary_time = data["Elapsed"]

            # Check for coverage information
            if data.get('Action') == 'output' and 'Output' in data:
                output_line = data['Output'].strip()
                if output_line.startswith("coverage:"):
                    # Extract the coverage percentage
                    match = re.search(r'coverage: ([\d\.]+)%', output_line)
                    if match:
                        coverage = float(match.group(1))

            # Alternative: Some versions include 'Coverage' field
            elif data.get('Action') == 'pass' and 'Coverage' in data:
                coverage = data['Coverage']

        total_tests = passed_tests + failed_tests
        pass_percentage = round((passed_tests / total_tests) * 100, 2) if total_tests > 0 else 0

        v = {
            "test_pass_rate": pass_percentage,
            "execution_time_sec": summary_time if summary_time is not None else total_time,
            "line_coverage_percent": coverage,
            "branch_coverage_percent": None,
            "timeout": False,
            "internal_error_occurred": False,
            "runtime_errors_count": runtime_errors
        }
        return v
    except Exception as e:
        print(f"Error running Go tests: {e}")
        return {
            "test_pass_rate": 0,
            "execution_time_sec": None,
            "line_coverage_percent": None,
            "branch_coverage_percent": None,
            "timeout": True,
            "internal_error_occurred": True,
            "runtime_errors_count": runtime_errors
        }


def x_run_tests__mutmut_59(project_dir):
    test_cmd = ["go", "test", "-timeout", "30s", "-json", "-cover", "./..."]
    runtime_errors = 0

    print("Running tests...")
    try:
        result = subprocess.run(
            test_cmd,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            cwd=project_dir,
            text=True
        )
        output = result.stdout
        print(output)
        stderr = result.stderr
        print(stderr)

        # Check if the subprocess exited with a non-zero return code
        if result.returncode != 0:
            # Parse output for timeout messages
            if "test timed out after" in output or "panic: test timed out" in output:
                print("Tests failed due to timeout")
                # Handle timeout scenario
                return {
                    "test_pass_rate": 0,
                    "execution_time_sec": None,
                    "line_coverage_percent": None,
                    "branch_coverage_percent": None,
                    "timeout": True,
                    "internal_error_occurred": False,
                    "runtime_errors_count": None
                }
            else:
                runtime_error_pattern = re.compile(
                    r"(panic:|fatal error:|runtime error:)", re.IGNORECASE
                )
                assertion_error_pattern = re.compile(r"assert")

                # Search for runtime errors in stdout and stderr
                for o in (output, stderr):
                    for line in o.splitlines():
                        # Match lines with runtime errors but exclude assertions
                        if runtime_error_pattern.search(line) and not assertion_error_pattern.search(line):
                            runtime_errors = runtime_errors + 1

        # Parse the JSON output
        passed_tests = 0
        failed_tests = 0
        total_time = None
        coverage = None
        summary_time = None

        for line in output.splitlines():
            if not line.strip():
                continue
            try:
                data = json.loads(line)
            except json.JSONDecodeError:
                continue  # Skip lines that aren't valid JSON

            # Check for test results
            if data.get('Action') == 'pass' and 'Test' in data:
                passed_tests += 1
                total_time += data.get('Elapsed', 0)
            elif data.get('Action') == 'fail' and 'Test' in data:
                failed_tests += 1
                total_time += data.get('Elapsed', 0)

            if (data.get('Action') == 'pass' or data.get('Action') == 'fail') and 'Test' not in data and 'Elapsed' in data:
                summary_time = data["Elapsed"]

            # Check for coverage information
            if data.get('Action') == 'output' and 'Output' in data:
                output_line = data['Output'].strip()
                if output_line.startswith("coverage:"):
                    # Extract the coverage percentage
                    match = re.search(r'coverage: ([\d\.]+)%', output_line)
                    if match:
                        coverage = float(match.group(1))

            # Alternative: Some versions include 'Coverage' field
            elif data.get('Action') == 'pass' and 'Coverage' in data:
                coverage = data['Coverage']

        total_tests = passed_tests + failed_tests
        pass_percentage = round((passed_tests / total_tests) * 100, 2) if total_tests > 0 else 0

        v = {
            "test_pass_rate": pass_percentage,
            "execution_time_sec": summary_time if summary_time is not None else total_time,
            "line_coverage_percent": coverage,
            "branch_coverage_percent": None,
            "timeout": False,
            "internal_error_occurred": False,
            "runtime_errors_count": runtime_errors
        }
        return v
    except Exception as e:
        print(f"Error running Go tests: {e}")
        return {
            "test_pass_rate": 0,
            "execution_time_sec": None,
            "line_coverage_percent": None,
            "branch_coverage_percent": None,
            "timeout": True,
            "internal_error_occurred": True,
            "runtime_errors_count": runtime_errors
        }


def x_run_tests__mutmut_60(project_dir):
    test_cmd = ["go", "test", "-timeout", "30s", "-json", "-cover", "./..."]
    runtime_errors = 0

    print("Running tests...")
    try:
        result = subprocess.run(
            test_cmd,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            cwd=project_dir,
            text=True
        )
        output = result.stdout
        print(output)
        stderr = result.stderr
        print(stderr)

        # Check if the subprocess exited with a non-zero return code
        if result.returncode != 0:
            # Parse output for timeout messages
            if "test timed out after" in output or "panic: test timed out" in output:
                print("Tests failed due to timeout")
                # Handle timeout scenario
                return {
                    "test_pass_rate": 0,
                    "execution_time_sec": None,
                    "line_coverage_percent": None,
                    "branch_coverage_percent": None,
                    "timeout": True,
                    "internal_error_occurred": False,
                    "runtime_errors_count": None
                }
            else:
                runtime_error_pattern = re.compile(
                    r"(panic:|fatal error:|runtime error:)", re.IGNORECASE
                )
                assertion_error_pattern = re.compile(r"assert")

                # Search for runtime errors in stdout and stderr
                for o in (output, stderr):
                    for line in o.splitlines():
                        # Match lines with runtime errors but exclude assertions
                        if runtime_error_pattern.search(line) and not assertion_error_pattern.search(line):
                            runtime_errors = runtime_errors + 1

        # Parse the JSON output
        passed_tests = 0
        failed_tests = 0
        total_time = 0.0
        coverage = ""
        summary_time = None

        for line in output.splitlines():
            if not line.strip():
                continue
            try:
                data = json.loads(line)
            except json.JSONDecodeError:
                continue  # Skip lines that aren't valid JSON

            # Check for test results
            if data.get('Action') == 'pass' and 'Test' in data:
                passed_tests += 1
                total_time += data.get('Elapsed', 0)
            elif data.get('Action') == 'fail' and 'Test' in data:
                failed_tests += 1
                total_time += data.get('Elapsed', 0)

            if (data.get('Action') == 'pass' or data.get('Action') == 'fail') and 'Test' not in data and 'Elapsed' in data:
                summary_time = data["Elapsed"]

            # Check for coverage information
            if data.get('Action') == 'output' and 'Output' in data:
                output_line = data['Output'].strip()
                if output_line.startswith("coverage:"):
                    # Extract the coverage percentage
                    match = re.search(r'coverage: ([\d\.]+)%', output_line)
                    if match:
                        coverage = float(match.group(1))

            # Alternative: Some versions include 'Coverage' field
            elif data.get('Action') == 'pass' and 'Coverage' in data:
                coverage = data['Coverage']

        total_tests = passed_tests + failed_tests
        pass_percentage = round((passed_tests / total_tests) * 100, 2) if total_tests > 0 else 0

        v = {
            "test_pass_rate": pass_percentage,
            "execution_time_sec": summary_time if summary_time is not None else total_time,
            "line_coverage_percent": coverage,
            "branch_coverage_percent": None,
            "timeout": False,
            "internal_error_occurred": False,
            "runtime_errors_count": runtime_errors
        }
        return v
    except Exception as e:
        print(f"Error running Go tests: {e}")
        return {
            "test_pass_rate": 0,
            "execution_time_sec": None,
            "line_coverage_percent": None,
            "branch_coverage_percent": None,
            "timeout": True,
            "internal_error_occurred": True,
            "runtime_errors_count": runtime_errors
        }


def x_run_tests__mutmut_61(project_dir):
    test_cmd = ["go", "test", "-timeout", "30s", "-json", "-cover", "./..."]
    runtime_errors = 0

    print("Running tests...")
    try:
        result = subprocess.run(
            test_cmd,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            cwd=project_dir,
            text=True
        )
        output = result.stdout
        print(output)
        stderr = result.stderr
        print(stderr)

        # Check if the subprocess exited with a non-zero return code
        if result.returncode != 0:
            # Parse output for timeout messages
            if "test timed out after" in output or "panic: test timed out" in output:
                print("Tests failed due to timeout")
                # Handle timeout scenario
                return {
                    "test_pass_rate": 0,
                    "execution_time_sec": None,
                    "line_coverage_percent": None,
                    "branch_coverage_percent": None,
                    "timeout": True,
                    "internal_error_occurred": False,
                    "runtime_errors_count": None
                }
            else:
                runtime_error_pattern = re.compile(
                    r"(panic:|fatal error:|runtime error:)", re.IGNORECASE
                )
                assertion_error_pattern = re.compile(r"assert")

                # Search for runtime errors in stdout and stderr
                for o in (output, stderr):
                    for line in o.splitlines():
                        # Match lines with runtime errors but exclude assertions
                        if runtime_error_pattern.search(line) and not assertion_error_pattern.search(line):
                            runtime_errors = runtime_errors + 1

        # Parse the JSON output
        passed_tests = 0
        failed_tests = 0
        total_time = 0.0
        coverage = None
        summary_time = ""

        for line in output.splitlines():
            if not line.strip():
                continue
            try:
                data = json.loads(line)
            except json.JSONDecodeError:
                continue  # Skip lines that aren't valid JSON

            # Check for test results
            if data.get('Action') == 'pass' and 'Test' in data:
                passed_tests += 1
                total_time += data.get('Elapsed', 0)
            elif data.get('Action') == 'fail' and 'Test' in data:
                failed_tests += 1
                total_time += data.get('Elapsed', 0)

            if (data.get('Action') == 'pass' or data.get('Action') == 'fail') and 'Test' not in data and 'Elapsed' in data:
                summary_time = data["Elapsed"]

            # Check for coverage information
            if data.get('Action') == 'output' and 'Output' in data:
                output_line = data['Output'].strip()
                if output_line.startswith("coverage:"):
                    # Extract the coverage percentage
                    match = re.search(r'coverage: ([\d\.]+)%', output_line)
                    if match:
                        coverage = float(match.group(1))

            # Alternative: Some versions include 'Coverage' field
            elif data.get('Action') == 'pass' and 'Coverage' in data:
                coverage = data['Coverage']

        total_tests = passed_tests + failed_tests
        pass_percentage = round((passed_tests / total_tests) * 100, 2) if total_tests > 0 else 0

        v = {
            "test_pass_rate": pass_percentage,
            "execution_time_sec": summary_time if summary_time is not None else total_time,
            "line_coverage_percent": coverage,
            "branch_coverage_percent": None,
            "timeout": False,
            "internal_error_occurred": False,
            "runtime_errors_count": runtime_errors
        }
        return v
    except Exception as e:
        print(f"Error running Go tests: {e}")
        return {
            "test_pass_rate": 0,
            "execution_time_sec": None,
            "line_coverage_percent": None,
            "branch_coverage_percent": None,
            "timeout": True,
            "internal_error_occurred": True,
            "runtime_errors_count": runtime_errors
        }


def x_run_tests__mutmut_62(project_dir):
    test_cmd = ["go", "test", "-timeout", "30s", "-json", "-cover", "./..."]
    runtime_errors = 0

    print("Running tests...")
    try:
        result = subprocess.run(
            test_cmd,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            cwd=project_dir,
            text=True
        )
        output = result.stdout
        print(output)
        stderr = result.stderr
        print(stderr)

        # Check if the subprocess exited with a non-zero return code
        if result.returncode != 0:
            # Parse output for timeout messages
            if "test timed out after" in output or "panic: test timed out" in output:
                print("Tests failed due to timeout")
                # Handle timeout scenario
                return {
                    "test_pass_rate": 0,
                    "execution_time_sec": None,
                    "line_coverage_percent": None,
                    "branch_coverage_percent": None,
                    "timeout": True,
                    "internal_error_occurred": False,
                    "runtime_errors_count": None
                }
            else:
                runtime_error_pattern = re.compile(
                    r"(panic:|fatal error:|runtime error:)", re.IGNORECASE
                )
                assertion_error_pattern = re.compile(r"assert")

                # Search for runtime errors in stdout and stderr
                for o in (output, stderr):
                    for line in o.splitlines():
                        # Match lines with runtime errors but exclude assertions
                        if runtime_error_pattern.search(line) and not assertion_error_pattern.search(line):
                            runtime_errors = runtime_errors + 1

        # Parse the JSON output
        passed_tests = 0
        failed_tests = 0
        total_time = 0.0
        coverage = None
        summary_time = None

        for line in output.splitlines():
            if  line.strip():
                continue
            try:
                data = json.loads(line)
            except json.JSONDecodeError:
                continue  # Skip lines that aren't valid JSON

            # Check for test results
            if data.get('Action') == 'pass' and 'Test' in data:
                passed_tests += 1
                total_time += data.get('Elapsed', 0)
            elif data.get('Action') == 'fail' and 'Test' in data:
                failed_tests += 1
                total_time += data.get('Elapsed', 0)

            if (data.get('Action') == 'pass' or data.get('Action') == 'fail') and 'Test' not in data and 'Elapsed' in data:
                summary_time = data["Elapsed"]

            # Check for coverage information
            if data.get('Action') == 'output' and 'Output' in data:
                output_line = data['Output'].strip()
                if output_line.startswith("coverage:"):
                    # Extract the coverage percentage
                    match = re.search(r'coverage: ([\d\.]+)%', output_line)
                    if match:
                        coverage = float(match.group(1))

            # Alternative: Some versions include 'Coverage' field
            elif data.get('Action') == 'pass' and 'Coverage' in data:
                coverage = data['Coverage']

        total_tests = passed_tests + failed_tests
        pass_percentage = round((passed_tests / total_tests) * 100, 2) if total_tests > 0 else 0

        v = {
            "test_pass_rate": pass_percentage,
            "execution_time_sec": summary_time if summary_time is not None else total_time,
            "line_coverage_percent": coverage,
            "branch_coverage_percent": None,
            "timeout": False,
            "internal_error_occurred": False,
            "runtime_errors_count": runtime_errors
        }
        return v
    except Exception as e:
        print(f"Error running Go tests: {e}")
        return {
            "test_pass_rate": 0,
            "execution_time_sec": None,
            "line_coverage_percent": None,
            "branch_coverage_percent": None,
            "timeout": True,
            "internal_error_occurred": True,
            "runtime_errors_count": runtime_errors
        }


def x_run_tests__mutmut_63(project_dir):
    test_cmd = ["go", "test", "-timeout", "30s", "-json", "-cover", "./..."]
    runtime_errors = 0

    print("Running tests...")
    try:
        result = subprocess.run(
            test_cmd,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            cwd=project_dir,
            text=True
        )
        output = result.stdout
        print(output)
        stderr = result.stderr
        print(stderr)

        # Check if the subprocess exited with a non-zero return code
        if result.returncode != 0:
            # Parse output for timeout messages
            if "test timed out after" in output or "panic: test timed out" in output:
                print("Tests failed due to timeout")
                # Handle timeout scenario
                return {
                    "test_pass_rate": 0,
                    "execution_time_sec": None,
                    "line_coverage_percent": None,
                    "branch_coverage_percent": None,
                    "timeout": True,
                    "internal_error_occurred": False,
                    "runtime_errors_count": None
                }
            else:
                runtime_error_pattern = re.compile(
                    r"(panic:|fatal error:|runtime error:)", re.IGNORECASE
                )
                assertion_error_pattern = re.compile(r"assert")

                # Search for runtime errors in stdout and stderr
                for o in (output, stderr):
                    for line in o.splitlines():
                        # Match lines with runtime errors but exclude assertions
                        if runtime_error_pattern.search(line) and not assertion_error_pattern.search(line):
                            runtime_errors = runtime_errors + 1

        # Parse the JSON output
        passed_tests = 0
        failed_tests = 0
        total_time = 0.0
        coverage = None
        summary_time = None

        for line in output.splitlines():
            if not line.strip():
                break
            try:
                data = json.loads(line)
            except json.JSONDecodeError:
                continue  # Skip lines that aren't valid JSON

            # Check for test results
            if data.get('Action') == 'pass' and 'Test' in data:
                passed_tests += 1
                total_time += data.get('Elapsed', 0)
            elif data.get('Action') == 'fail' and 'Test' in data:
                failed_tests += 1
                total_time += data.get('Elapsed', 0)

            if (data.get('Action') == 'pass' or data.get('Action') == 'fail') and 'Test' not in data and 'Elapsed' in data:
                summary_time = data["Elapsed"]

            # Check for coverage information
            if data.get('Action') == 'output' and 'Output' in data:
                output_line = data['Output'].strip()
                if output_line.startswith("coverage:"):
                    # Extract the coverage percentage
                    match = re.search(r'coverage: ([\d\.]+)%', output_line)
                    if match:
                        coverage = float(match.group(1))

            # Alternative: Some versions include 'Coverage' field
            elif data.get('Action') == 'pass' and 'Coverage' in data:
                coverage = data['Coverage']

        total_tests = passed_tests + failed_tests
        pass_percentage = round((passed_tests / total_tests) * 100, 2) if total_tests > 0 else 0

        v = {
            "test_pass_rate": pass_percentage,
            "execution_time_sec": summary_time if summary_time is not None else total_time,
            "line_coverage_percent": coverage,
            "branch_coverage_percent": None,
            "timeout": False,
            "internal_error_occurred": False,
            "runtime_errors_count": runtime_errors
        }
        return v
    except Exception as e:
        print(f"Error running Go tests: {e}")
        return {
            "test_pass_rate": 0,
            "execution_time_sec": None,
            "line_coverage_percent": None,
            "branch_coverage_percent": None,
            "timeout": True,
            "internal_error_occurred": True,
            "runtime_errors_count": runtime_errors
        }


def x_run_tests__mutmut_64(project_dir):
    test_cmd = ["go", "test", "-timeout", "30s", "-json", "-cover", "./..."]
    runtime_errors = 0

    print("Running tests...")
    try:
        result = subprocess.run(
            test_cmd,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            cwd=project_dir,
            text=True
        )
        output = result.stdout
        print(output)
        stderr = result.stderr
        print(stderr)

        # Check if the subprocess exited with a non-zero return code
        if result.returncode != 0:
            # Parse output for timeout messages
            if "test timed out after" in output or "panic: test timed out" in output:
                print("Tests failed due to timeout")
                # Handle timeout scenario
                return {
                    "test_pass_rate": 0,
                    "execution_time_sec": None,
                    "line_coverage_percent": None,
                    "branch_coverage_percent": None,
                    "timeout": True,
                    "internal_error_occurred": False,
                    "runtime_errors_count": None
                }
            else:
                runtime_error_pattern = re.compile(
                    r"(panic:|fatal error:|runtime error:)", re.IGNORECASE
                )
                assertion_error_pattern = re.compile(r"assert")

                # Search for runtime errors in stdout and stderr
                for o in (output, stderr):
                    for line in o.splitlines():
                        # Match lines with runtime errors but exclude assertions
                        if runtime_error_pattern.search(line) and not assertion_error_pattern.search(line):
                            runtime_errors = runtime_errors + 1

        # Parse the JSON output
        passed_tests = 0
        failed_tests = 0
        total_time = 0.0
        coverage = None
        summary_time = None

        for line in output.splitlines():
            if not line.strip():
                continue
            try:
                data = json.loads(None)
            except json.JSONDecodeError:
                continue  # Skip lines that aren't valid JSON

            # Check for test results
            if data.get('Action') == 'pass' and 'Test' in data:
                passed_tests += 1
                total_time += data.get('Elapsed', 0)
            elif data.get('Action') == 'fail' and 'Test' in data:
                failed_tests += 1
                total_time += data.get('Elapsed', 0)

            if (data.get('Action') == 'pass' or data.get('Action') == 'fail') and 'Test' not in data and 'Elapsed' in data:
                summary_time = data["Elapsed"]

            # Check for coverage information
            if data.get('Action') == 'output' and 'Output' in data:
                output_line = data['Output'].strip()
                if output_line.startswith("coverage:"):
                    # Extract the coverage percentage
                    match = re.search(r'coverage: ([\d\.]+)%', output_line)
                    if match:
                        coverage = float(match.group(1))

            # Alternative: Some versions include 'Coverage' field
            elif data.get('Action') == 'pass' and 'Coverage' in data:
                coverage = data['Coverage']

        total_tests = passed_tests + failed_tests
        pass_percentage = round((passed_tests / total_tests) * 100, 2) if total_tests > 0 else 0

        v = {
            "test_pass_rate": pass_percentage,
            "execution_time_sec": summary_time if summary_time is not None else total_time,
            "line_coverage_percent": coverage,
            "branch_coverage_percent": None,
            "timeout": False,
            "internal_error_occurred": False,
            "runtime_errors_count": runtime_errors
        }
        return v
    except Exception as e:
        print(f"Error running Go tests: {e}")
        return {
            "test_pass_rate": 0,
            "execution_time_sec": None,
            "line_coverage_percent": None,
            "branch_coverage_percent": None,
            "timeout": True,
            "internal_error_occurred": True,
            "runtime_errors_count": runtime_errors
        }


def x_run_tests__mutmut_65(project_dir):
    test_cmd = ["go", "test", "-timeout", "30s", "-json", "-cover", "./..."]
    runtime_errors = 0

    print("Running tests...")
    try:
        result = subprocess.run(
            test_cmd,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            cwd=project_dir,
            text=True
        )
        output = result.stdout
        print(output)
        stderr = result.stderr
        print(stderr)

        # Check if the subprocess exited with a non-zero return code
        if result.returncode != 0:
            # Parse output for timeout messages
            if "test timed out after" in output or "panic: test timed out" in output:
                print("Tests failed due to timeout")
                # Handle timeout scenario
                return {
                    "test_pass_rate": 0,
                    "execution_time_sec": None,
                    "line_coverage_percent": None,
                    "branch_coverage_percent": None,
                    "timeout": True,
                    "internal_error_occurred": False,
                    "runtime_errors_count": None
                }
            else:
                runtime_error_pattern = re.compile(
                    r"(panic:|fatal error:|runtime error:)", re.IGNORECASE
                )
                assertion_error_pattern = re.compile(r"assert")

                # Search for runtime errors in stdout and stderr
                for o in (output, stderr):
                    for line in o.splitlines():
                        # Match lines with runtime errors but exclude assertions
                        if runtime_error_pattern.search(line) and not assertion_error_pattern.search(line):
                            runtime_errors = runtime_errors + 1

        # Parse the JSON output
        passed_tests = 0
        failed_tests = 0
        total_time = 0.0
        coverage = None
        summary_time = None

        for line in output.splitlines():
            if not line.strip():
                continue
            try:
                data = None
            except json.JSONDecodeError:
                continue  # Skip lines that aren't valid JSON

            # Check for test results
            if data.get('Action') == 'pass' and 'Test' in data:
                passed_tests += 1
                total_time += data.get('Elapsed', 0)
            elif data.get('Action') == 'fail' and 'Test' in data:
                failed_tests += 1
                total_time += data.get('Elapsed', 0)

            if (data.get('Action') == 'pass' or data.get('Action') == 'fail') and 'Test' not in data and 'Elapsed' in data:
                summary_time = data["Elapsed"]

            # Check for coverage information
            if data.get('Action') == 'output' and 'Output' in data:
                output_line = data['Output'].strip()
                if output_line.startswith("coverage:"):
                    # Extract the coverage percentage
                    match = re.search(r'coverage: ([\d\.]+)%', output_line)
                    if match:
                        coverage = float(match.group(1))

            # Alternative: Some versions include 'Coverage' field
            elif data.get('Action') == 'pass' and 'Coverage' in data:
                coverage = data['Coverage']

        total_tests = passed_tests + failed_tests
        pass_percentage = round((passed_tests / total_tests) * 100, 2) if total_tests > 0 else 0

        v = {
            "test_pass_rate": pass_percentage,
            "execution_time_sec": summary_time if summary_time is not None else total_time,
            "line_coverage_percent": coverage,
            "branch_coverage_percent": None,
            "timeout": False,
            "internal_error_occurred": False,
            "runtime_errors_count": runtime_errors
        }
        return v
    except Exception as e:
        print(f"Error running Go tests: {e}")
        return {
            "test_pass_rate": 0,
            "execution_time_sec": None,
            "line_coverage_percent": None,
            "branch_coverage_percent": None,
            "timeout": True,
            "internal_error_occurred": True,
            "runtime_errors_count": runtime_errors
        }


def x_run_tests__mutmut_66(project_dir):
    test_cmd = ["go", "test", "-timeout", "30s", "-json", "-cover", "./..."]
    runtime_errors = 0

    print("Running tests...")
    try:
        result = subprocess.run(
            test_cmd,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            cwd=project_dir,
            text=True
        )
        output = result.stdout
        print(output)
        stderr = result.stderr
        print(stderr)

        # Check if the subprocess exited with a non-zero return code
        if result.returncode != 0:
            # Parse output for timeout messages
            if "test timed out after" in output or "panic: test timed out" in output:
                print("Tests failed due to timeout")
                # Handle timeout scenario
                return {
                    "test_pass_rate": 0,
                    "execution_time_sec": None,
                    "line_coverage_percent": None,
                    "branch_coverage_percent": None,
                    "timeout": True,
                    "internal_error_occurred": False,
                    "runtime_errors_count": None
                }
            else:
                runtime_error_pattern = re.compile(
                    r"(panic:|fatal error:|runtime error:)", re.IGNORECASE
                )
                assertion_error_pattern = re.compile(r"assert")

                # Search for runtime errors in stdout and stderr
                for o in (output, stderr):
                    for line in o.splitlines():
                        # Match lines with runtime errors but exclude assertions
                        if runtime_error_pattern.search(line) and not assertion_error_pattern.search(line):
                            runtime_errors = runtime_errors + 1

        # Parse the JSON output
        passed_tests = 0
        failed_tests = 0
        total_time = 0.0
        coverage = None
        summary_time = None

        for line in output.splitlines():
            if not line.strip():
                continue
            try:
                data = json.loads(line)
            except json.JSONDecodeError:
                break  # Skip lines that aren't valid JSON

            # Check for test results
            if data.get('Action') == 'pass' and 'Test' in data:
                passed_tests += 1
                total_time += data.get('Elapsed', 0)
            elif data.get('Action') == 'fail' and 'Test' in data:
                failed_tests += 1
                total_time += data.get('Elapsed', 0)

            if (data.get('Action') == 'pass' or data.get('Action') == 'fail') and 'Test' not in data and 'Elapsed' in data:
                summary_time = data["Elapsed"]

            # Check for coverage information
            if data.get('Action') == 'output' and 'Output' in data:
                output_line = data['Output'].strip()
                if output_line.startswith("coverage:"):
                    # Extract the coverage percentage
                    match = re.search(r'coverage: ([\d\.]+)%', output_line)
                    if match:
                        coverage = float(match.group(1))

            # Alternative: Some versions include 'Coverage' field
            elif data.get('Action') == 'pass' and 'Coverage' in data:
                coverage = data['Coverage']

        total_tests = passed_tests + failed_tests
        pass_percentage = round((passed_tests / total_tests) * 100, 2) if total_tests > 0 else 0

        v = {
            "test_pass_rate": pass_percentage,
            "execution_time_sec": summary_time if summary_time is not None else total_time,
            "line_coverage_percent": coverage,
            "branch_coverage_percent": None,
            "timeout": False,
            "internal_error_occurred": False,
            "runtime_errors_count": runtime_errors
        }
        return v
    except Exception as e:
        print(f"Error running Go tests: {e}")
        return {
            "test_pass_rate": 0,
            "execution_time_sec": None,
            "line_coverage_percent": None,
            "branch_coverage_percent": None,
            "timeout": True,
            "internal_error_occurred": True,
            "runtime_errors_count": runtime_errors
        }


def x_run_tests__mutmut_67(project_dir):
    test_cmd = ["go", "test", "-timeout", "30s", "-json", "-cover", "./..."]
    runtime_errors = 0

    print("Running tests...")
    try:
        result = subprocess.run(
            test_cmd,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            cwd=project_dir,
            text=True
        )
        output = result.stdout
        print(output)
        stderr = result.stderr
        print(stderr)

        # Check if the subprocess exited with a non-zero return code
        if result.returncode != 0:
            # Parse output for timeout messages
            if "test timed out after" in output or "panic: test timed out" in output:
                print("Tests failed due to timeout")
                # Handle timeout scenario
                return {
                    "test_pass_rate": 0,
                    "execution_time_sec": None,
                    "line_coverage_percent": None,
                    "branch_coverage_percent": None,
                    "timeout": True,
                    "internal_error_occurred": False,
                    "runtime_errors_count": None
                }
            else:
                runtime_error_pattern = re.compile(
                    r"(panic:|fatal error:|runtime error:)", re.IGNORECASE
                )
                assertion_error_pattern = re.compile(r"assert")

                # Search for runtime errors in stdout and stderr
                for o in (output, stderr):
                    for line in o.splitlines():
                        # Match lines with runtime errors but exclude assertions
                        if runtime_error_pattern.search(line) and not assertion_error_pattern.search(line):
                            runtime_errors = runtime_errors + 1

        # Parse the JSON output
        passed_tests = 0
        failed_tests = 0
        total_time = 0.0
        coverage = None
        summary_time = None

        for line in output.splitlines():
            if not line.strip():
                continue
            try:
                data = json.loads(line)
            except json.JSONDecodeError:
                continue  # Skip lines that aren't valid JSON

            # Check for test results
            if data.get('XXActionXX') == 'pass' and 'Test' in data:
                passed_tests += 1
                total_time += data.get('Elapsed', 0)
            elif data.get('Action') == 'fail' and 'Test' in data:
                failed_tests += 1
                total_time += data.get('Elapsed', 0)

            if (data.get('Action') == 'pass' or data.get('Action') == 'fail') and 'Test' not in data and 'Elapsed' in data:
                summary_time = data["Elapsed"]

            # Check for coverage information
            if data.get('Action') == 'output' and 'Output' in data:
                output_line = data['Output'].strip()
                if output_line.startswith("coverage:"):
                    # Extract the coverage percentage
                    match = re.search(r'coverage: ([\d\.]+)%', output_line)
                    if match:
                        coverage = float(match.group(1))

            # Alternative: Some versions include 'Coverage' field
            elif data.get('Action') == 'pass' and 'Coverage' in data:
                coverage = data['Coverage']

        total_tests = passed_tests + failed_tests
        pass_percentage = round((passed_tests / total_tests) * 100, 2) if total_tests > 0 else 0

        v = {
            "test_pass_rate": pass_percentage,
            "execution_time_sec": summary_time if summary_time is not None else total_time,
            "line_coverage_percent": coverage,
            "branch_coverage_percent": None,
            "timeout": False,
            "internal_error_occurred": False,
            "runtime_errors_count": runtime_errors
        }
        return v
    except Exception as e:
        print(f"Error running Go tests: {e}")
        return {
            "test_pass_rate": 0,
            "execution_time_sec": None,
            "line_coverage_percent": None,
            "branch_coverage_percent": None,
            "timeout": True,
            "internal_error_occurred": True,
            "runtime_errors_count": runtime_errors
        }


def x_run_tests__mutmut_68(project_dir):
    test_cmd = ["go", "test", "-timeout", "30s", "-json", "-cover", "./..."]
    runtime_errors = 0

    print("Running tests...")
    try:
        result = subprocess.run(
            test_cmd,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            cwd=project_dir,
            text=True
        )
        output = result.stdout
        print(output)
        stderr = result.stderr
        print(stderr)

        # Check if the subprocess exited with a non-zero return code
        if result.returncode != 0:
            # Parse output for timeout messages
            if "test timed out after" in output or "panic: test timed out" in output:
                print("Tests failed due to timeout")
                # Handle timeout scenario
                return {
                    "test_pass_rate": 0,
                    "execution_time_sec": None,
                    "line_coverage_percent": None,
                    "branch_coverage_percent": None,
                    "timeout": True,
                    "internal_error_occurred": False,
                    "runtime_errors_count": None
                }
            else:
                runtime_error_pattern = re.compile(
                    r"(panic:|fatal error:|runtime error:)", re.IGNORECASE
                )
                assertion_error_pattern = re.compile(r"assert")

                # Search for runtime errors in stdout and stderr
                for o in (output, stderr):
                    for line in o.splitlines():
                        # Match lines with runtime errors but exclude assertions
                        if runtime_error_pattern.search(line) and not assertion_error_pattern.search(line):
                            runtime_errors = runtime_errors + 1

        # Parse the JSON output
        passed_tests = 0
        failed_tests = 0
        total_time = 0.0
        coverage = None
        summary_time = None

        for line in output.splitlines():
            if not line.strip():
                continue
            try:
                data = json.loads(line)
            except json.JSONDecodeError:
                continue  # Skip lines that aren't valid JSON

            # Check for test results
            if data.get('Action') != 'pass' and 'Test' in data:
                passed_tests += 1
                total_time += data.get('Elapsed', 0)
            elif data.get('Action') == 'fail' and 'Test' in data:
                failed_tests += 1
                total_time += data.get('Elapsed', 0)

            if (data.get('Action') == 'pass' or data.get('Action') == 'fail') and 'Test' not in data and 'Elapsed' in data:
                summary_time = data["Elapsed"]

            # Check for coverage information
            if data.get('Action') == 'output' and 'Output' in data:
                output_line = data['Output'].strip()
                if output_line.startswith("coverage:"):
                    # Extract the coverage percentage
                    match = re.search(r'coverage: ([\d\.]+)%', output_line)
                    if match:
                        coverage = float(match.group(1))

            # Alternative: Some versions include 'Coverage' field
            elif data.get('Action') == 'pass' and 'Coverage' in data:
                coverage = data['Coverage']

        total_tests = passed_tests + failed_tests
        pass_percentage = round((passed_tests / total_tests) * 100, 2) if total_tests > 0 else 0

        v = {
            "test_pass_rate": pass_percentage,
            "execution_time_sec": summary_time if summary_time is not None else total_time,
            "line_coverage_percent": coverage,
            "branch_coverage_percent": None,
            "timeout": False,
            "internal_error_occurred": False,
            "runtime_errors_count": runtime_errors
        }
        return v
    except Exception as e:
        print(f"Error running Go tests: {e}")
        return {
            "test_pass_rate": 0,
            "execution_time_sec": None,
            "line_coverage_percent": None,
            "branch_coverage_percent": None,
            "timeout": True,
            "internal_error_occurred": True,
            "runtime_errors_count": runtime_errors
        }


def x_run_tests__mutmut_69(project_dir):
    test_cmd = ["go", "test", "-timeout", "30s", "-json", "-cover", "./..."]
    runtime_errors = 0

    print("Running tests...")
    try:
        result = subprocess.run(
            test_cmd,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            cwd=project_dir,
            text=True
        )
        output = result.stdout
        print(output)
        stderr = result.stderr
        print(stderr)

        # Check if the subprocess exited with a non-zero return code
        if result.returncode != 0:
            # Parse output for timeout messages
            if "test timed out after" in output or "panic: test timed out" in output:
                print("Tests failed due to timeout")
                # Handle timeout scenario
                return {
                    "test_pass_rate": 0,
                    "execution_time_sec": None,
                    "line_coverage_percent": None,
                    "branch_coverage_percent": None,
                    "timeout": True,
                    "internal_error_occurred": False,
                    "runtime_errors_count": None
                }
            else:
                runtime_error_pattern = re.compile(
                    r"(panic:|fatal error:|runtime error:)", re.IGNORECASE
                )
                assertion_error_pattern = re.compile(r"assert")

                # Search for runtime errors in stdout and stderr
                for o in (output, stderr):
                    for line in o.splitlines():
                        # Match lines with runtime errors but exclude assertions
                        if runtime_error_pattern.search(line) and not assertion_error_pattern.search(line):
                            runtime_errors = runtime_errors + 1

        # Parse the JSON output
        passed_tests = 0
        failed_tests = 0
        total_time = 0.0
        coverage = None
        summary_time = None

        for line in output.splitlines():
            if not line.strip():
                continue
            try:
                data = json.loads(line)
            except json.JSONDecodeError:
                continue  # Skip lines that aren't valid JSON

            # Check for test results
            if data.get('Action') == 'XXpassXX' and 'Test' in data:
                passed_tests += 1
                total_time += data.get('Elapsed', 0)
            elif data.get('Action') == 'fail' and 'Test' in data:
                failed_tests += 1
                total_time += data.get('Elapsed', 0)

            if (data.get('Action') == 'pass' or data.get('Action') == 'fail') and 'Test' not in data and 'Elapsed' in data:
                summary_time = data["Elapsed"]

            # Check for coverage information
            if data.get('Action') == 'output' and 'Output' in data:
                output_line = data['Output'].strip()
                if output_line.startswith("coverage:"):
                    # Extract the coverage percentage
                    match = re.search(r'coverage: ([\d\.]+)%', output_line)
                    if match:
                        coverage = float(match.group(1))

            # Alternative: Some versions include 'Coverage' field
            elif data.get('Action') == 'pass' and 'Coverage' in data:
                coverage = data['Coverage']

        total_tests = passed_tests + failed_tests
        pass_percentage = round((passed_tests / total_tests) * 100, 2) if total_tests > 0 else 0

        v = {
            "test_pass_rate": pass_percentage,
            "execution_time_sec": summary_time if summary_time is not None else total_time,
            "line_coverage_percent": coverage,
            "branch_coverage_percent": None,
            "timeout": False,
            "internal_error_occurred": False,
            "runtime_errors_count": runtime_errors
        }
        return v
    except Exception as e:
        print(f"Error running Go tests: {e}")
        return {
            "test_pass_rate": 0,
            "execution_time_sec": None,
            "line_coverage_percent": None,
            "branch_coverage_percent": None,
            "timeout": True,
            "internal_error_occurred": True,
            "runtime_errors_count": runtime_errors
        }


def x_run_tests__mutmut_70(project_dir):
    test_cmd = ["go", "test", "-timeout", "30s", "-json", "-cover", "./..."]
    runtime_errors = 0

    print("Running tests...")
    try:
        result = subprocess.run(
            test_cmd,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            cwd=project_dir,
            text=True
        )
        output = result.stdout
        print(output)
        stderr = result.stderr
        print(stderr)

        # Check if the subprocess exited with a non-zero return code
        if result.returncode != 0:
            # Parse output for timeout messages
            if "test timed out after" in output or "panic: test timed out" in output:
                print("Tests failed due to timeout")
                # Handle timeout scenario
                return {
                    "test_pass_rate": 0,
                    "execution_time_sec": None,
                    "line_coverage_percent": None,
                    "branch_coverage_percent": None,
                    "timeout": True,
                    "internal_error_occurred": False,
                    "runtime_errors_count": None
                }
            else:
                runtime_error_pattern = re.compile(
                    r"(panic:|fatal error:|runtime error:)", re.IGNORECASE
                )
                assertion_error_pattern = re.compile(r"assert")

                # Search for runtime errors in stdout and stderr
                for o in (output, stderr):
                    for line in o.splitlines():
                        # Match lines with runtime errors but exclude assertions
                        if runtime_error_pattern.search(line) and not assertion_error_pattern.search(line):
                            runtime_errors = runtime_errors + 1

        # Parse the JSON output
        passed_tests = 0
        failed_tests = 0
        total_time = 0.0
        coverage = None
        summary_time = None

        for line in output.splitlines():
            if not line.strip():
                continue
            try:
                data = json.loads(line)
            except json.JSONDecodeError:
                continue  # Skip lines that aren't valid JSON

            # Check for test results
            if data.get('Action') == 'pass' and 'XXTestXX' in data:
                passed_tests += 1
                total_time += data.get('Elapsed', 0)
            elif data.get('Action') == 'fail' and 'Test' in data:
                failed_tests += 1
                total_time += data.get('Elapsed', 0)

            if (data.get('Action') == 'pass' or data.get('Action') == 'fail') and 'Test' not in data and 'Elapsed' in data:
                summary_time = data["Elapsed"]

            # Check for coverage information
            if data.get('Action') == 'output' and 'Output' in data:
                output_line = data['Output'].strip()
                if output_line.startswith("coverage:"):
                    # Extract the coverage percentage
                    match = re.search(r'coverage: ([\d\.]+)%', output_line)
                    if match:
                        coverage = float(match.group(1))

            # Alternative: Some versions include 'Coverage' field
            elif data.get('Action') == 'pass' and 'Coverage' in data:
                coverage = data['Coverage']

        total_tests = passed_tests + failed_tests
        pass_percentage = round((passed_tests / total_tests) * 100, 2) if total_tests > 0 else 0

        v = {
            "test_pass_rate": pass_percentage,
            "execution_time_sec": summary_time if summary_time is not None else total_time,
            "line_coverage_percent": coverage,
            "branch_coverage_percent": None,
            "timeout": False,
            "internal_error_occurred": False,
            "runtime_errors_count": runtime_errors
        }
        return v
    except Exception as e:
        print(f"Error running Go tests: {e}")
        return {
            "test_pass_rate": 0,
            "execution_time_sec": None,
            "line_coverage_percent": None,
            "branch_coverage_percent": None,
            "timeout": True,
            "internal_error_occurred": True,
            "runtime_errors_count": runtime_errors
        }


def x_run_tests__mutmut_71(project_dir):
    test_cmd = ["go", "test", "-timeout", "30s", "-json", "-cover", "./..."]
    runtime_errors = 0

    print("Running tests...")
    try:
        result = subprocess.run(
            test_cmd,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            cwd=project_dir,
            text=True
        )
        output = result.stdout
        print(output)
        stderr = result.stderr
        print(stderr)

        # Check if the subprocess exited with a non-zero return code
        if result.returncode != 0:
            # Parse output for timeout messages
            if "test timed out after" in output or "panic: test timed out" in output:
                print("Tests failed due to timeout")
                # Handle timeout scenario
                return {
                    "test_pass_rate": 0,
                    "execution_time_sec": None,
                    "line_coverage_percent": None,
                    "branch_coverage_percent": None,
                    "timeout": True,
                    "internal_error_occurred": False,
                    "runtime_errors_count": None
                }
            else:
                runtime_error_pattern = re.compile(
                    r"(panic:|fatal error:|runtime error:)", re.IGNORECASE
                )
                assertion_error_pattern = re.compile(r"assert")

                # Search for runtime errors in stdout and stderr
                for o in (output, stderr):
                    for line in o.splitlines():
                        # Match lines with runtime errors but exclude assertions
                        if runtime_error_pattern.search(line) and not assertion_error_pattern.search(line):
                            runtime_errors = runtime_errors + 1

        # Parse the JSON output
        passed_tests = 0
        failed_tests = 0
        total_time = 0.0
        coverage = None
        summary_time = None

        for line in output.splitlines():
            if not line.strip():
                continue
            try:
                data = json.loads(line)
            except json.JSONDecodeError:
                continue  # Skip lines that aren't valid JSON

            # Check for test results
            if data.get('Action') == 'pass' and 'Test' not in data:
                passed_tests += 1
                total_time += data.get('Elapsed', 0)
            elif data.get('Action') == 'fail' and 'Test' in data:
                failed_tests += 1
                total_time += data.get('Elapsed', 0)

            if (data.get('Action') == 'pass' or data.get('Action') == 'fail') and 'Test' not in data and 'Elapsed' in data:
                summary_time = data["Elapsed"]

            # Check for coverage information
            if data.get('Action') == 'output' and 'Output' in data:
                output_line = data['Output'].strip()
                if output_line.startswith("coverage:"):
                    # Extract the coverage percentage
                    match = re.search(r'coverage: ([\d\.]+)%', output_line)
                    if match:
                        coverage = float(match.group(1))

            # Alternative: Some versions include 'Coverage' field
            elif data.get('Action') == 'pass' and 'Coverage' in data:
                coverage = data['Coverage']

        total_tests = passed_tests + failed_tests
        pass_percentage = round((passed_tests / total_tests) * 100, 2) if total_tests > 0 else 0

        v = {
            "test_pass_rate": pass_percentage,
            "execution_time_sec": summary_time if summary_time is not None else total_time,
            "line_coverage_percent": coverage,
            "branch_coverage_percent": None,
            "timeout": False,
            "internal_error_occurred": False,
            "runtime_errors_count": runtime_errors
        }
        return v
    except Exception as e:
        print(f"Error running Go tests: {e}")
        return {
            "test_pass_rate": 0,
            "execution_time_sec": None,
            "line_coverage_percent": None,
            "branch_coverage_percent": None,
            "timeout": True,
            "internal_error_occurred": True,
            "runtime_errors_count": runtime_errors
        }


def x_run_tests__mutmut_72(project_dir):
    test_cmd = ["go", "test", "-timeout", "30s", "-json", "-cover", "./..."]
    runtime_errors = 0

    print("Running tests...")
    try:
        result = subprocess.run(
            test_cmd,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            cwd=project_dir,
            text=True
        )
        output = result.stdout
        print(output)
        stderr = result.stderr
        print(stderr)

        # Check if the subprocess exited with a non-zero return code
        if result.returncode != 0:
            # Parse output for timeout messages
            if "test timed out after" in output or "panic: test timed out" in output:
                print("Tests failed due to timeout")
                # Handle timeout scenario
                return {
                    "test_pass_rate": 0,
                    "execution_time_sec": None,
                    "line_coverage_percent": None,
                    "branch_coverage_percent": None,
                    "timeout": True,
                    "internal_error_occurred": False,
                    "runtime_errors_count": None
                }
            else:
                runtime_error_pattern = re.compile(
                    r"(panic:|fatal error:|runtime error:)", re.IGNORECASE
                )
                assertion_error_pattern = re.compile(r"assert")

                # Search for runtime errors in stdout and stderr
                for o in (output, stderr):
                    for line in o.splitlines():
                        # Match lines with runtime errors but exclude assertions
                        if runtime_error_pattern.search(line) and not assertion_error_pattern.search(line):
                            runtime_errors = runtime_errors + 1

        # Parse the JSON output
        passed_tests = 0
        failed_tests = 0
        total_time = 0.0
        coverage = None
        summary_time = None

        for line in output.splitlines():
            if not line.strip():
                continue
            try:
                data = json.loads(line)
            except json.JSONDecodeError:
                continue  # Skip lines that aren't valid JSON

            # Check for test results
            if data.get('Action') == 'pass' or 'Test' in data:
                passed_tests += 1
                total_time += data.get('Elapsed', 0)
            elif data.get('Action') == 'fail' and 'Test' in data:
                failed_tests += 1
                total_time += data.get('Elapsed', 0)

            if (data.get('Action') == 'pass' or data.get('Action') == 'fail') and 'Test' not in data and 'Elapsed' in data:
                summary_time = data["Elapsed"]

            # Check for coverage information
            if data.get('Action') == 'output' and 'Output' in data:
                output_line = data['Output'].strip()
                if output_line.startswith("coverage:"):
                    # Extract the coverage percentage
                    match = re.search(r'coverage: ([\d\.]+)%', output_line)
                    if match:
                        coverage = float(match.group(1))

            # Alternative: Some versions include 'Coverage' field
            elif data.get('Action') == 'pass' and 'Coverage' in data:
                coverage = data['Coverage']

        total_tests = passed_tests + failed_tests
        pass_percentage = round((passed_tests / total_tests) * 100, 2) if total_tests > 0 else 0

        v = {
            "test_pass_rate": pass_percentage,
            "execution_time_sec": summary_time if summary_time is not None else total_time,
            "line_coverage_percent": coverage,
            "branch_coverage_percent": None,
            "timeout": False,
            "internal_error_occurred": False,
            "runtime_errors_count": runtime_errors
        }
        return v
    except Exception as e:
        print(f"Error running Go tests: {e}")
        return {
            "test_pass_rate": 0,
            "execution_time_sec": None,
            "line_coverage_percent": None,
            "branch_coverage_percent": None,
            "timeout": True,
            "internal_error_occurred": True,
            "runtime_errors_count": runtime_errors
        }


def x_run_tests__mutmut_73(project_dir):
    test_cmd = ["go", "test", "-timeout", "30s", "-json", "-cover", "./..."]
    runtime_errors = 0

    print("Running tests...")
    try:
        result = subprocess.run(
            test_cmd,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            cwd=project_dir,
            text=True
        )
        output = result.stdout
        print(output)
        stderr = result.stderr
        print(stderr)

        # Check if the subprocess exited with a non-zero return code
        if result.returncode != 0:
            # Parse output for timeout messages
            if "test timed out after" in output or "panic: test timed out" in output:
                print("Tests failed due to timeout")
                # Handle timeout scenario
                return {
                    "test_pass_rate": 0,
                    "execution_time_sec": None,
                    "line_coverage_percent": None,
                    "branch_coverage_percent": None,
                    "timeout": True,
                    "internal_error_occurred": False,
                    "runtime_errors_count": None
                }
            else:
                runtime_error_pattern = re.compile(
                    r"(panic:|fatal error:|runtime error:)", re.IGNORECASE
                )
                assertion_error_pattern = re.compile(r"assert")

                # Search for runtime errors in stdout and stderr
                for o in (output, stderr):
                    for line in o.splitlines():
                        # Match lines with runtime errors but exclude assertions
                        if runtime_error_pattern.search(line) and not assertion_error_pattern.search(line):
                            runtime_errors = runtime_errors + 1

        # Parse the JSON output
        passed_tests = 0
        failed_tests = 0
        total_time = 0.0
        coverage = None
        summary_time = None

        for line in output.splitlines():
            if not line.strip():
                continue
            try:
                data = json.loads(line)
            except json.JSONDecodeError:
                continue  # Skip lines that aren't valid JSON

            # Check for test results
            if data.get('Action') == 'pass' and 'Test' in data:
                passed_tests -= 1
                total_time += data.get('Elapsed', 0)
            elif data.get('Action') == 'fail' and 'Test' in data:
                failed_tests += 1
                total_time += data.get('Elapsed', 0)

            if (data.get('Action') == 'pass' or data.get('Action') == 'fail') and 'Test' not in data and 'Elapsed' in data:
                summary_time = data["Elapsed"]

            # Check for coverage information
            if data.get('Action') == 'output' and 'Output' in data:
                output_line = data['Output'].strip()
                if output_line.startswith("coverage:"):
                    # Extract the coverage percentage
                    match = re.search(r'coverage: ([\d\.]+)%', output_line)
                    if match:
                        coverage = float(match.group(1))

            # Alternative: Some versions include 'Coverage' field
            elif data.get('Action') == 'pass' and 'Coverage' in data:
                coverage = data['Coverage']

        total_tests = passed_tests + failed_tests
        pass_percentage = round((passed_tests / total_tests) * 100, 2) if total_tests > 0 else 0

        v = {
            "test_pass_rate": pass_percentage,
            "execution_time_sec": summary_time if summary_time is not None else total_time,
            "line_coverage_percent": coverage,
            "branch_coverage_percent": None,
            "timeout": False,
            "internal_error_occurred": False,
            "runtime_errors_count": runtime_errors
        }
        return v
    except Exception as e:
        print(f"Error running Go tests: {e}")
        return {
            "test_pass_rate": 0,
            "execution_time_sec": None,
            "line_coverage_percent": None,
            "branch_coverage_percent": None,
            "timeout": True,
            "internal_error_occurred": True,
            "runtime_errors_count": runtime_errors
        }


def x_run_tests__mutmut_74(project_dir):
    test_cmd = ["go", "test", "-timeout", "30s", "-json", "-cover", "./..."]
    runtime_errors = 0

    print("Running tests...")
    try:
        result = subprocess.run(
            test_cmd,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            cwd=project_dir,
            text=True
        )
        output = result.stdout
        print(output)
        stderr = result.stderr
        print(stderr)

        # Check if the subprocess exited with a non-zero return code
        if result.returncode != 0:
            # Parse output for timeout messages
            if "test timed out after" in output or "panic: test timed out" in output:
                print("Tests failed due to timeout")
                # Handle timeout scenario
                return {
                    "test_pass_rate": 0,
                    "execution_time_sec": None,
                    "line_coverage_percent": None,
                    "branch_coverage_percent": None,
                    "timeout": True,
                    "internal_error_occurred": False,
                    "runtime_errors_count": None
                }
            else:
                runtime_error_pattern = re.compile(
                    r"(panic:|fatal error:|runtime error:)", re.IGNORECASE
                )
                assertion_error_pattern = re.compile(r"assert")

                # Search for runtime errors in stdout and stderr
                for o in (output, stderr):
                    for line in o.splitlines():
                        # Match lines with runtime errors but exclude assertions
                        if runtime_error_pattern.search(line) and not assertion_error_pattern.search(line):
                            runtime_errors = runtime_errors + 1

        # Parse the JSON output
        passed_tests = 0
        failed_tests = 0
        total_time = 0.0
        coverage = None
        summary_time = None

        for line in output.splitlines():
            if not line.strip():
                continue
            try:
                data = json.loads(line)
            except json.JSONDecodeError:
                continue  # Skip lines that aren't valid JSON

            # Check for test results
            if data.get('Action') == 'pass' and 'Test' in data:
                passed_tests = 1
                total_time += data.get('Elapsed', 0)
            elif data.get('Action') == 'fail' and 'Test' in data:
                failed_tests += 1
                total_time += data.get('Elapsed', 0)

            if (data.get('Action') == 'pass' or data.get('Action') == 'fail') and 'Test' not in data and 'Elapsed' in data:
                summary_time = data["Elapsed"]

            # Check for coverage information
            if data.get('Action') == 'output' and 'Output' in data:
                output_line = data['Output'].strip()
                if output_line.startswith("coverage:"):
                    # Extract the coverage percentage
                    match = re.search(r'coverage: ([\d\.]+)%', output_line)
                    if match:
                        coverage = float(match.group(1))

            # Alternative: Some versions include 'Coverage' field
            elif data.get('Action') == 'pass' and 'Coverage' in data:
                coverage = data['Coverage']

        total_tests = passed_tests + failed_tests
        pass_percentage = round((passed_tests / total_tests) * 100, 2) if total_tests > 0 else 0

        v = {
            "test_pass_rate": pass_percentage,
            "execution_time_sec": summary_time if summary_time is not None else total_time,
            "line_coverage_percent": coverage,
            "branch_coverage_percent": None,
            "timeout": False,
            "internal_error_occurred": False,
            "runtime_errors_count": runtime_errors
        }
        return v
    except Exception as e:
        print(f"Error running Go tests: {e}")
        return {
            "test_pass_rate": 0,
            "execution_time_sec": None,
            "line_coverage_percent": None,
            "branch_coverage_percent": None,
            "timeout": True,
            "internal_error_occurred": True,
            "runtime_errors_count": runtime_errors
        }


def x_run_tests__mutmut_75(project_dir):
    test_cmd = ["go", "test", "-timeout", "30s", "-json", "-cover", "./..."]
    runtime_errors = 0

    print("Running tests...")
    try:
        result = subprocess.run(
            test_cmd,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            cwd=project_dir,
            text=True
        )
        output = result.stdout
        print(output)
        stderr = result.stderr
        print(stderr)

        # Check if the subprocess exited with a non-zero return code
        if result.returncode != 0:
            # Parse output for timeout messages
            if "test timed out after" in output or "panic: test timed out" in output:
                print("Tests failed due to timeout")
                # Handle timeout scenario
                return {
                    "test_pass_rate": 0,
                    "execution_time_sec": None,
                    "line_coverage_percent": None,
                    "branch_coverage_percent": None,
                    "timeout": True,
                    "internal_error_occurred": False,
                    "runtime_errors_count": None
                }
            else:
                runtime_error_pattern = re.compile(
                    r"(panic:|fatal error:|runtime error:)", re.IGNORECASE
                )
                assertion_error_pattern = re.compile(r"assert")

                # Search for runtime errors in stdout and stderr
                for o in (output, stderr):
                    for line in o.splitlines():
                        # Match lines with runtime errors but exclude assertions
                        if runtime_error_pattern.search(line) and not assertion_error_pattern.search(line):
                            runtime_errors = runtime_errors + 1

        # Parse the JSON output
        passed_tests = 0
        failed_tests = 0
        total_time = 0.0
        coverage = None
        summary_time = None

        for line in output.splitlines():
            if not line.strip():
                continue
            try:
                data = json.loads(line)
            except json.JSONDecodeError:
                continue  # Skip lines that aren't valid JSON

            # Check for test results
            if data.get('Action') == 'pass' and 'Test' in data:
                passed_tests += 2
                total_time += data.get('Elapsed', 0)
            elif data.get('Action') == 'fail' and 'Test' in data:
                failed_tests += 1
                total_time += data.get('Elapsed', 0)

            if (data.get('Action') == 'pass' or data.get('Action') == 'fail') and 'Test' not in data and 'Elapsed' in data:
                summary_time = data["Elapsed"]

            # Check for coverage information
            if data.get('Action') == 'output' and 'Output' in data:
                output_line = data['Output'].strip()
                if output_line.startswith("coverage:"):
                    # Extract the coverage percentage
                    match = re.search(r'coverage: ([\d\.]+)%', output_line)
                    if match:
                        coverage = float(match.group(1))

            # Alternative: Some versions include 'Coverage' field
            elif data.get('Action') == 'pass' and 'Coverage' in data:
                coverage = data['Coverage']

        total_tests = passed_tests + failed_tests
        pass_percentage = round((passed_tests / total_tests) * 100, 2) if total_tests > 0 else 0

        v = {
            "test_pass_rate": pass_percentage,
            "execution_time_sec": summary_time if summary_time is not None else total_time,
            "line_coverage_percent": coverage,
            "branch_coverage_percent": None,
            "timeout": False,
            "internal_error_occurred": False,
            "runtime_errors_count": runtime_errors
        }
        return v
    except Exception as e:
        print(f"Error running Go tests: {e}")
        return {
            "test_pass_rate": 0,
            "execution_time_sec": None,
            "line_coverage_percent": None,
            "branch_coverage_percent": None,
            "timeout": True,
            "internal_error_occurred": True,
            "runtime_errors_count": runtime_errors
        }


def x_run_tests__mutmut_76(project_dir):
    test_cmd = ["go", "test", "-timeout", "30s", "-json", "-cover", "./..."]
    runtime_errors = 0

    print("Running tests...")
    try:
        result = subprocess.run(
            test_cmd,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            cwd=project_dir,
            text=True
        )
        output = result.stdout
        print(output)
        stderr = result.stderr
        print(stderr)

        # Check if the subprocess exited with a non-zero return code
        if result.returncode != 0:
            # Parse output for timeout messages
            if "test timed out after" in output or "panic: test timed out" in output:
                print("Tests failed due to timeout")
                # Handle timeout scenario
                return {
                    "test_pass_rate": 0,
                    "execution_time_sec": None,
                    "line_coverage_percent": None,
                    "branch_coverage_percent": None,
                    "timeout": True,
                    "internal_error_occurred": False,
                    "runtime_errors_count": None
                }
            else:
                runtime_error_pattern = re.compile(
                    r"(panic:|fatal error:|runtime error:)", re.IGNORECASE
                )
                assertion_error_pattern = re.compile(r"assert")

                # Search for runtime errors in stdout and stderr
                for o in (output, stderr):
                    for line in o.splitlines():
                        # Match lines with runtime errors but exclude assertions
                        if runtime_error_pattern.search(line) and not assertion_error_pattern.search(line):
                            runtime_errors = runtime_errors + 1

        # Parse the JSON output
        passed_tests = 0
        failed_tests = 0
        total_time = 0.0
        coverage = None
        summary_time = None

        for line in output.splitlines():
            if not line.strip():
                continue
            try:
                data = json.loads(line)
            except json.JSONDecodeError:
                continue  # Skip lines that aren't valid JSON

            # Check for test results
            if data.get('Action') == 'pass' and 'Test' in data:
                passed_tests += 1
                total_time -= data.get('Elapsed', 0)
            elif data.get('Action') == 'fail' and 'Test' in data:
                failed_tests += 1
                total_time += data.get('Elapsed', 0)

            if (data.get('Action') == 'pass' or data.get('Action') == 'fail') and 'Test' not in data and 'Elapsed' in data:
                summary_time = data["Elapsed"]

            # Check for coverage information
            if data.get('Action') == 'output' and 'Output' in data:
                output_line = data['Output'].strip()
                if output_line.startswith("coverage:"):
                    # Extract the coverage percentage
                    match = re.search(r'coverage: ([\d\.]+)%', output_line)
                    if match:
                        coverage = float(match.group(1))

            # Alternative: Some versions include 'Coverage' field
            elif data.get('Action') == 'pass' and 'Coverage' in data:
                coverage = data['Coverage']

        total_tests = passed_tests + failed_tests
        pass_percentage = round((passed_tests / total_tests) * 100, 2) if total_tests > 0 else 0

        v = {
            "test_pass_rate": pass_percentage,
            "execution_time_sec": summary_time if summary_time is not None else total_time,
            "line_coverage_percent": coverage,
            "branch_coverage_percent": None,
            "timeout": False,
            "internal_error_occurred": False,
            "runtime_errors_count": runtime_errors
        }
        return v
    except Exception as e:
        print(f"Error running Go tests: {e}")
        return {
            "test_pass_rate": 0,
            "execution_time_sec": None,
            "line_coverage_percent": None,
            "branch_coverage_percent": None,
            "timeout": True,
            "internal_error_occurred": True,
            "runtime_errors_count": runtime_errors
        }


def x_run_tests__mutmut_77(project_dir):
    test_cmd = ["go", "test", "-timeout", "30s", "-json", "-cover", "./..."]
    runtime_errors = 0

    print("Running tests...")
    try:
        result = subprocess.run(
            test_cmd,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            cwd=project_dir,
            text=True
        )
        output = result.stdout
        print(output)
        stderr = result.stderr
        print(stderr)

        # Check if the subprocess exited with a non-zero return code
        if result.returncode != 0:
            # Parse output for timeout messages
            if "test timed out after" in output or "panic: test timed out" in output:
                print("Tests failed due to timeout")
                # Handle timeout scenario
                return {
                    "test_pass_rate": 0,
                    "execution_time_sec": None,
                    "line_coverage_percent": None,
                    "branch_coverage_percent": None,
                    "timeout": True,
                    "internal_error_occurred": False,
                    "runtime_errors_count": None
                }
            else:
                runtime_error_pattern = re.compile(
                    r"(panic:|fatal error:|runtime error:)", re.IGNORECASE
                )
                assertion_error_pattern = re.compile(r"assert")

                # Search for runtime errors in stdout and stderr
                for o in (output, stderr):
                    for line in o.splitlines():
                        # Match lines with runtime errors but exclude assertions
                        if runtime_error_pattern.search(line) and not assertion_error_pattern.search(line):
                            runtime_errors = runtime_errors + 1

        # Parse the JSON output
        passed_tests = 0
        failed_tests = 0
        total_time = 0.0
        coverage = None
        summary_time = None

        for line in output.splitlines():
            if not line.strip():
                continue
            try:
                data = json.loads(line)
            except json.JSONDecodeError:
                continue  # Skip lines that aren't valid JSON

            # Check for test results
            if data.get('Action') == 'pass' and 'Test' in data:
                passed_tests += 1
                total_time = data.get('Elapsed', 0)
            elif data.get('Action') == 'fail' and 'Test' in data:
                failed_tests += 1
                total_time += data.get('Elapsed', 0)

            if (data.get('Action') == 'pass' or data.get('Action') == 'fail') and 'Test' not in data and 'Elapsed' in data:
                summary_time = data["Elapsed"]

            # Check for coverage information
            if data.get('Action') == 'output' and 'Output' in data:
                output_line = data['Output'].strip()
                if output_line.startswith("coverage:"):
                    # Extract the coverage percentage
                    match = re.search(r'coverage: ([\d\.]+)%', output_line)
                    if match:
                        coverage = float(match.group(1))

            # Alternative: Some versions include 'Coverage' field
            elif data.get('Action') == 'pass' and 'Coverage' in data:
                coverage = data['Coverage']

        total_tests = passed_tests + failed_tests
        pass_percentage = round((passed_tests / total_tests) * 100, 2) if total_tests > 0 else 0

        v = {
            "test_pass_rate": pass_percentage,
            "execution_time_sec": summary_time if summary_time is not None else total_time,
            "line_coverage_percent": coverage,
            "branch_coverage_percent": None,
            "timeout": False,
            "internal_error_occurred": False,
            "runtime_errors_count": runtime_errors
        }
        return v
    except Exception as e:
        print(f"Error running Go tests: {e}")
        return {
            "test_pass_rate": 0,
            "execution_time_sec": None,
            "line_coverage_percent": None,
            "branch_coverage_percent": None,
            "timeout": True,
            "internal_error_occurred": True,
            "runtime_errors_count": runtime_errors
        }


def x_run_tests__mutmut_78(project_dir):
    test_cmd = ["go", "test", "-timeout", "30s", "-json", "-cover", "./..."]
    runtime_errors = 0

    print("Running tests...")
    try:
        result = subprocess.run(
            test_cmd,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            cwd=project_dir,
            text=True
        )
        output = result.stdout
        print(output)
        stderr = result.stderr
        print(stderr)

        # Check if the subprocess exited with a non-zero return code
        if result.returncode != 0:
            # Parse output for timeout messages
            if "test timed out after" in output or "panic: test timed out" in output:
                print("Tests failed due to timeout")
                # Handle timeout scenario
                return {
                    "test_pass_rate": 0,
                    "execution_time_sec": None,
                    "line_coverage_percent": None,
                    "branch_coverage_percent": None,
                    "timeout": True,
                    "internal_error_occurred": False,
                    "runtime_errors_count": None
                }
            else:
                runtime_error_pattern = re.compile(
                    r"(panic:|fatal error:|runtime error:)", re.IGNORECASE
                )
                assertion_error_pattern = re.compile(r"assert")

                # Search for runtime errors in stdout and stderr
                for o in (output, stderr):
                    for line in o.splitlines():
                        # Match lines with runtime errors but exclude assertions
                        if runtime_error_pattern.search(line) and not assertion_error_pattern.search(line):
                            runtime_errors = runtime_errors + 1

        # Parse the JSON output
        passed_tests = 0
        failed_tests = 0
        total_time = 0.0
        coverage = None
        summary_time = None

        for line in output.splitlines():
            if not line.strip():
                continue
            try:
                data = json.loads(line)
            except json.JSONDecodeError:
                continue  # Skip lines that aren't valid JSON

            # Check for test results
            if data.get('Action') == 'pass' and 'Test' in data:
                passed_tests += 1
                total_time += data.get('XXElapsedXX', 0)
            elif data.get('Action') == 'fail' and 'Test' in data:
                failed_tests += 1
                total_time += data.get('Elapsed', 0)

            if (data.get('Action') == 'pass' or data.get('Action') == 'fail') and 'Test' not in data and 'Elapsed' in data:
                summary_time = data["Elapsed"]

            # Check for coverage information
            if data.get('Action') == 'output' and 'Output' in data:
                output_line = data['Output'].strip()
                if output_line.startswith("coverage:"):
                    # Extract the coverage percentage
                    match = re.search(r'coverage: ([\d\.]+)%', output_line)
                    if match:
                        coverage = float(match.group(1))

            # Alternative: Some versions include 'Coverage' field
            elif data.get('Action') == 'pass' and 'Coverage' in data:
                coverage = data['Coverage']

        total_tests = passed_tests + failed_tests
        pass_percentage = round((passed_tests / total_tests) * 100, 2) if total_tests > 0 else 0

        v = {
            "test_pass_rate": pass_percentage,
            "execution_time_sec": summary_time if summary_time is not None else total_time,
            "line_coverage_percent": coverage,
            "branch_coverage_percent": None,
            "timeout": False,
            "internal_error_occurred": False,
            "runtime_errors_count": runtime_errors
        }
        return v
    except Exception as e:
        print(f"Error running Go tests: {e}")
        return {
            "test_pass_rate": 0,
            "execution_time_sec": None,
            "line_coverage_percent": None,
            "branch_coverage_percent": None,
            "timeout": True,
            "internal_error_occurred": True,
            "runtime_errors_count": runtime_errors
        }


def x_run_tests__mutmut_79(project_dir):
    test_cmd = ["go", "test", "-timeout", "30s", "-json", "-cover", "./..."]
    runtime_errors = 0

    print("Running tests...")
    try:
        result = subprocess.run(
            test_cmd,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            cwd=project_dir,
            text=True
        )
        output = result.stdout
        print(output)
        stderr = result.stderr
        print(stderr)

        # Check if the subprocess exited with a non-zero return code
        if result.returncode != 0:
            # Parse output for timeout messages
            if "test timed out after" in output or "panic: test timed out" in output:
                print("Tests failed due to timeout")
                # Handle timeout scenario
                return {
                    "test_pass_rate": 0,
                    "execution_time_sec": None,
                    "line_coverage_percent": None,
                    "branch_coverage_percent": None,
                    "timeout": True,
                    "internal_error_occurred": False,
                    "runtime_errors_count": None
                }
            else:
                runtime_error_pattern = re.compile(
                    r"(panic:|fatal error:|runtime error:)", re.IGNORECASE
                )
                assertion_error_pattern = re.compile(r"assert")

                # Search for runtime errors in stdout and stderr
                for o in (output, stderr):
                    for line in o.splitlines():
                        # Match lines with runtime errors but exclude assertions
                        if runtime_error_pattern.search(line) and not assertion_error_pattern.search(line):
                            runtime_errors = runtime_errors + 1

        # Parse the JSON output
        passed_tests = 0
        failed_tests = 0
        total_time = 0.0
        coverage = None
        summary_time = None

        for line in output.splitlines():
            if not line.strip():
                continue
            try:
                data = json.loads(line)
            except json.JSONDecodeError:
                continue  # Skip lines that aren't valid JSON

            # Check for test results
            if data.get('Action') == 'pass' and 'Test' in data:
                passed_tests += 1
                total_time += data.get('Elapsed', 1)
            elif data.get('Action') == 'fail' and 'Test' in data:
                failed_tests += 1
                total_time += data.get('Elapsed', 0)

            if (data.get('Action') == 'pass' or data.get('Action') == 'fail') and 'Test' not in data and 'Elapsed' in data:
                summary_time = data["Elapsed"]

            # Check for coverage information
            if data.get('Action') == 'output' and 'Output' in data:
                output_line = data['Output'].strip()
                if output_line.startswith("coverage:"):
                    # Extract the coverage percentage
                    match = re.search(r'coverage: ([\d\.]+)%', output_line)
                    if match:
                        coverage = float(match.group(1))

            # Alternative: Some versions include 'Coverage' field
            elif data.get('Action') == 'pass' and 'Coverage' in data:
                coverage = data['Coverage']

        total_tests = passed_tests + failed_tests
        pass_percentage = round((passed_tests / total_tests) * 100, 2) if total_tests > 0 else 0

        v = {
            "test_pass_rate": pass_percentage,
            "execution_time_sec": summary_time if summary_time is not None else total_time,
            "line_coverage_percent": coverage,
            "branch_coverage_percent": None,
            "timeout": False,
            "internal_error_occurred": False,
            "runtime_errors_count": runtime_errors
        }
        return v
    except Exception as e:
        print(f"Error running Go tests: {e}")
        return {
            "test_pass_rate": 0,
            "execution_time_sec": None,
            "line_coverage_percent": None,
            "branch_coverage_percent": None,
            "timeout": True,
            "internal_error_occurred": True,
            "runtime_errors_count": runtime_errors
        }


def x_run_tests__mutmut_80(project_dir):
    test_cmd = ["go", "test", "-timeout", "30s", "-json", "-cover", "./..."]
    runtime_errors = 0

    print("Running tests...")
    try:
        result = subprocess.run(
            test_cmd,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            cwd=project_dir,
            text=True
        )
        output = result.stdout
        print(output)
        stderr = result.stderr
        print(stderr)

        # Check if the subprocess exited with a non-zero return code
        if result.returncode != 0:
            # Parse output for timeout messages
            if "test timed out after" in output or "panic: test timed out" in output:
                print("Tests failed due to timeout")
                # Handle timeout scenario
                return {
                    "test_pass_rate": 0,
                    "execution_time_sec": None,
                    "line_coverage_percent": None,
                    "branch_coverage_percent": None,
                    "timeout": True,
                    "internal_error_occurred": False,
                    "runtime_errors_count": None
                }
            else:
                runtime_error_pattern = re.compile(
                    r"(panic:|fatal error:|runtime error:)", re.IGNORECASE
                )
                assertion_error_pattern = re.compile(r"assert")

                # Search for runtime errors in stdout and stderr
                for o in (output, stderr):
                    for line in o.splitlines():
                        # Match lines with runtime errors but exclude assertions
                        if runtime_error_pattern.search(line) and not assertion_error_pattern.search(line):
                            runtime_errors = runtime_errors + 1

        # Parse the JSON output
        passed_tests = 0
        failed_tests = 0
        total_time = 0.0
        coverage = None
        summary_time = None

        for line in output.splitlines():
            if not line.strip():
                continue
            try:
                data = json.loads(line)
            except json.JSONDecodeError:
                continue  # Skip lines that aren't valid JSON

            # Check for test results
            if data.get('Action') == 'pass' and 'Test' in data:
                passed_tests += 1
                total_time += data.get('Elapsed', 0)
            elif data.get('XXActionXX') == 'fail' and 'Test' in data:
                failed_tests += 1
                total_time += data.get('Elapsed', 0)

            if (data.get('Action') == 'pass' or data.get('Action') == 'fail') and 'Test' not in data and 'Elapsed' in data:
                summary_time = data["Elapsed"]

            # Check for coverage information
            if data.get('Action') == 'output' and 'Output' in data:
                output_line = data['Output'].strip()
                if output_line.startswith("coverage:"):
                    # Extract the coverage percentage
                    match = re.search(r'coverage: ([\d\.]+)%', output_line)
                    if match:
                        coverage = float(match.group(1))

            # Alternative: Some versions include 'Coverage' field
            elif data.get('Action') == 'pass' and 'Coverage' in data:
                coverage = data['Coverage']

        total_tests = passed_tests + failed_tests
        pass_percentage = round((passed_tests / total_tests) * 100, 2) if total_tests > 0 else 0

        v = {
            "test_pass_rate": pass_percentage,
            "execution_time_sec": summary_time if summary_time is not None else total_time,
            "line_coverage_percent": coverage,
            "branch_coverage_percent": None,
            "timeout": False,
            "internal_error_occurred": False,
            "runtime_errors_count": runtime_errors
        }
        return v
    except Exception as e:
        print(f"Error running Go tests: {e}")
        return {
            "test_pass_rate": 0,
            "execution_time_sec": None,
            "line_coverage_percent": None,
            "branch_coverage_percent": None,
            "timeout": True,
            "internal_error_occurred": True,
            "runtime_errors_count": runtime_errors
        }


def x_run_tests__mutmut_81(project_dir):
    test_cmd = ["go", "test", "-timeout", "30s", "-json", "-cover", "./..."]
    runtime_errors = 0

    print("Running tests...")
    try:
        result = subprocess.run(
            test_cmd,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            cwd=project_dir,
            text=True
        )
        output = result.stdout
        print(output)
        stderr = result.stderr
        print(stderr)

        # Check if the subprocess exited with a non-zero return code
        if result.returncode != 0:
            # Parse output for timeout messages
            if "test timed out after" in output or "panic: test timed out" in output:
                print("Tests failed due to timeout")
                # Handle timeout scenario
                return {
                    "test_pass_rate": 0,
                    "execution_time_sec": None,
                    "line_coverage_percent": None,
                    "branch_coverage_percent": None,
                    "timeout": True,
                    "internal_error_occurred": False,
                    "runtime_errors_count": None
                }
            else:
                runtime_error_pattern = re.compile(
                    r"(panic:|fatal error:|runtime error:)", re.IGNORECASE
                )
                assertion_error_pattern = re.compile(r"assert")

                # Search for runtime errors in stdout and stderr
                for o in (output, stderr):
                    for line in o.splitlines():
                        # Match lines with runtime errors but exclude assertions
                        if runtime_error_pattern.search(line) and not assertion_error_pattern.search(line):
                            runtime_errors = runtime_errors + 1

        # Parse the JSON output
        passed_tests = 0
        failed_tests = 0
        total_time = 0.0
        coverage = None
        summary_time = None

        for line in output.splitlines():
            if not line.strip():
                continue
            try:
                data = json.loads(line)
            except json.JSONDecodeError:
                continue  # Skip lines that aren't valid JSON

            # Check for test results
            if data.get('Action') == 'pass' and 'Test' in data:
                passed_tests += 1
                total_time += data.get('Elapsed', 0)
            elif data.get('Action') != 'fail' and 'Test' in data:
                failed_tests += 1
                total_time += data.get('Elapsed', 0)

            if (data.get('Action') == 'pass' or data.get('Action') == 'fail') and 'Test' not in data and 'Elapsed' in data:
                summary_time = data["Elapsed"]

            # Check for coverage information
            if data.get('Action') == 'output' and 'Output' in data:
                output_line = data['Output'].strip()
                if output_line.startswith("coverage:"):
                    # Extract the coverage percentage
                    match = re.search(r'coverage: ([\d\.]+)%', output_line)
                    if match:
                        coverage = float(match.group(1))

            # Alternative: Some versions include 'Coverage' field
            elif data.get('Action') == 'pass' and 'Coverage' in data:
                coverage = data['Coverage']

        total_tests = passed_tests + failed_tests
        pass_percentage = round((passed_tests / total_tests) * 100, 2) if total_tests > 0 else 0

        v = {
            "test_pass_rate": pass_percentage,
            "execution_time_sec": summary_time if summary_time is not None else total_time,
            "line_coverage_percent": coverage,
            "branch_coverage_percent": None,
            "timeout": False,
            "internal_error_occurred": False,
            "runtime_errors_count": runtime_errors
        }
        return v
    except Exception as e:
        print(f"Error running Go tests: {e}")
        return {
            "test_pass_rate": 0,
            "execution_time_sec": None,
            "line_coverage_percent": None,
            "branch_coverage_percent": None,
            "timeout": True,
            "internal_error_occurred": True,
            "runtime_errors_count": runtime_errors
        }


def x_run_tests__mutmut_82(project_dir):
    test_cmd = ["go", "test", "-timeout", "30s", "-json", "-cover", "./..."]
    runtime_errors = 0

    print("Running tests...")
    try:
        result = subprocess.run(
            test_cmd,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            cwd=project_dir,
            text=True
        )
        output = result.stdout
        print(output)
        stderr = result.stderr
        print(stderr)

        # Check if the subprocess exited with a non-zero return code
        if result.returncode != 0:
            # Parse output for timeout messages
            if "test timed out after" in output or "panic: test timed out" in output:
                print("Tests failed due to timeout")
                # Handle timeout scenario
                return {
                    "test_pass_rate": 0,
                    "execution_time_sec": None,
                    "line_coverage_percent": None,
                    "branch_coverage_percent": None,
                    "timeout": True,
                    "internal_error_occurred": False,
                    "runtime_errors_count": None
                }
            else:
                runtime_error_pattern = re.compile(
                    r"(panic:|fatal error:|runtime error:)", re.IGNORECASE
                )
                assertion_error_pattern = re.compile(r"assert")

                # Search for runtime errors in stdout and stderr
                for o in (output, stderr):
                    for line in o.splitlines():
                        # Match lines with runtime errors but exclude assertions
                        if runtime_error_pattern.search(line) and not assertion_error_pattern.search(line):
                            runtime_errors = runtime_errors + 1

        # Parse the JSON output
        passed_tests = 0
        failed_tests = 0
        total_time = 0.0
        coverage = None
        summary_time = None

        for line in output.splitlines():
            if not line.strip():
                continue
            try:
                data = json.loads(line)
            except json.JSONDecodeError:
                continue  # Skip lines that aren't valid JSON

            # Check for test results
            if data.get('Action') == 'pass' and 'Test' in data:
                passed_tests += 1
                total_time += data.get('Elapsed', 0)
            elif data.get('Action') == 'XXfailXX' and 'Test' in data:
                failed_tests += 1
                total_time += data.get('Elapsed', 0)

            if (data.get('Action') == 'pass' or data.get('Action') == 'fail') and 'Test' not in data and 'Elapsed' in data:
                summary_time = data["Elapsed"]

            # Check for coverage information
            if data.get('Action') == 'output' and 'Output' in data:
                output_line = data['Output'].strip()
                if output_line.startswith("coverage:"):
                    # Extract the coverage percentage
                    match = re.search(r'coverage: ([\d\.]+)%', output_line)
                    if match:
                        coverage = float(match.group(1))

            # Alternative: Some versions include 'Coverage' field
            elif data.get('Action') == 'pass' and 'Coverage' in data:
                coverage = data['Coverage']

        total_tests = passed_tests + failed_tests
        pass_percentage = round((passed_tests / total_tests) * 100, 2) if total_tests > 0 else 0

        v = {
            "test_pass_rate": pass_percentage,
            "execution_time_sec": summary_time if summary_time is not None else total_time,
            "line_coverage_percent": coverage,
            "branch_coverage_percent": None,
            "timeout": False,
            "internal_error_occurred": False,
            "runtime_errors_count": runtime_errors
        }
        return v
    except Exception as e:
        print(f"Error running Go tests: {e}")
        return {
            "test_pass_rate": 0,
            "execution_time_sec": None,
            "line_coverage_percent": None,
            "branch_coverage_percent": None,
            "timeout": True,
            "internal_error_occurred": True,
            "runtime_errors_count": runtime_errors
        }


def x_run_tests__mutmut_83(project_dir):
    test_cmd = ["go", "test", "-timeout", "30s", "-json", "-cover", "./..."]
    runtime_errors = 0

    print("Running tests...")
    try:
        result = subprocess.run(
            test_cmd,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            cwd=project_dir,
            text=True
        )
        output = result.stdout
        print(output)
        stderr = result.stderr
        print(stderr)

        # Check if the subprocess exited with a non-zero return code
        if result.returncode != 0:
            # Parse output for timeout messages
            if "test timed out after" in output or "panic: test timed out" in output:
                print("Tests failed due to timeout")
                # Handle timeout scenario
                return {
                    "test_pass_rate": 0,
                    "execution_time_sec": None,
                    "line_coverage_percent": None,
                    "branch_coverage_percent": None,
                    "timeout": True,
                    "internal_error_occurred": False,
                    "runtime_errors_count": None
                }
            else:
                runtime_error_pattern = re.compile(
                    r"(panic:|fatal error:|runtime error:)", re.IGNORECASE
                )
                assertion_error_pattern = re.compile(r"assert")

                # Search for runtime errors in stdout and stderr
                for o in (output, stderr):
                    for line in o.splitlines():
                        # Match lines with runtime errors but exclude assertions
                        if runtime_error_pattern.search(line) and not assertion_error_pattern.search(line):
                            runtime_errors = runtime_errors + 1

        # Parse the JSON output
        passed_tests = 0
        failed_tests = 0
        total_time = 0.0
        coverage = None
        summary_time = None

        for line in output.splitlines():
            if not line.strip():
                continue
            try:
                data = json.loads(line)
            except json.JSONDecodeError:
                continue  # Skip lines that aren't valid JSON

            # Check for test results
            if data.get('Action') == 'pass' and 'Test' in data:
                passed_tests += 1
                total_time += data.get('Elapsed', 0)
            elif data.get('Action') == 'fail' and 'XXTestXX' in data:
                failed_tests += 1
                total_time += data.get('Elapsed', 0)

            if (data.get('Action') == 'pass' or data.get('Action') == 'fail') and 'Test' not in data and 'Elapsed' in data:
                summary_time = data["Elapsed"]

            # Check for coverage information
            if data.get('Action') == 'output' and 'Output' in data:
                output_line = data['Output'].strip()
                if output_line.startswith("coverage:"):
                    # Extract the coverage percentage
                    match = re.search(r'coverage: ([\d\.]+)%', output_line)
                    if match:
                        coverage = float(match.group(1))

            # Alternative: Some versions include 'Coverage' field
            elif data.get('Action') == 'pass' and 'Coverage' in data:
                coverage = data['Coverage']

        total_tests = passed_tests + failed_tests
        pass_percentage = round((passed_tests / total_tests) * 100, 2) if total_tests > 0 else 0

        v = {
            "test_pass_rate": pass_percentage,
            "execution_time_sec": summary_time if summary_time is not None else total_time,
            "line_coverage_percent": coverage,
            "branch_coverage_percent": None,
            "timeout": False,
            "internal_error_occurred": False,
            "runtime_errors_count": runtime_errors
        }
        return v
    except Exception as e:
        print(f"Error running Go tests: {e}")
        return {
            "test_pass_rate": 0,
            "execution_time_sec": None,
            "line_coverage_percent": None,
            "branch_coverage_percent": None,
            "timeout": True,
            "internal_error_occurred": True,
            "runtime_errors_count": runtime_errors
        }


def x_run_tests__mutmut_84(project_dir):
    test_cmd = ["go", "test", "-timeout", "30s", "-json", "-cover", "./..."]
    runtime_errors = 0

    print("Running tests...")
    try:
        result = subprocess.run(
            test_cmd,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            cwd=project_dir,
            text=True
        )
        output = result.stdout
        print(output)
        stderr = result.stderr
        print(stderr)

        # Check if the subprocess exited with a non-zero return code
        if result.returncode != 0:
            # Parse output for timeout messages
            if "test timed out after" in output or "panic: test timed out" in output:
                print("Tests failed due to timeout")
                # Handle timeout scenario
                return {
                    "test_pass_rate": 0,
                    "execution_time_sec": None,
                    "line_coverage_percent": None,
                    "branch_coverage_percent": None,
                    "timeout": True,
                    "internal_error_occurred": False,
                    "runtime_errors_count": None
                }
            else:
                runtime_error_pattern = re.compile(
                    r"(panic:|fatal error:|runtime error:)", re.IGNORECASE
                )
                assertion_error_pattern = re.compile(r"assert")

                # Search for runtime errors in stdout and stderr
                for o in (output, stderr):
                    for line in o.splitlines():
                        # Match lines with runtime errors but exclude assertions
                        if runtime_error_pattern.search(line) and not assertion_error_pattern.search(line):
                            runtime_errors = runtime_errors + 1

        # Parse the JSON output
        passed_tests = 0
        failed_tests = 0
        total_time = 0.0
        coverage = None
        summary_time = None

        for line in output.splitlines():
            if not line.strip():
                continue
            try:
                data = json.loads(line)
            except json.JSONDecodeError:
                continue  # Skip lines that aren't valid JSON

            # Check for test results
            if data.get('Action') == 'pass' and 'Test' in data:
                passed_tests += 1
                total_time += data.get('Elapsed', 0)
            elif data.get('Action') == 'fail' and 'Test' not in data:
                failed_tests += 1
                total_time += data.get('Elapsed', 0)

            if (data.get('Action') == 'pass' or data.get('Action') == 'fail') and 'Test' not in data and 'Elapsed' in data:
                summary_time = data["Elapsed"]

            # Check for coverage information
            if data.get('Action') == 'output' and 'Output' in data:
                output_line = data['Output'].strip()
                if output_line.startswith("coverage:"):
                    # Extract the coverage percentage
                    match = re.search(r'coverage: ([\d\.]+)%', output_line)
                    if match:
                        coverage = float(match.group(1))

            # Alternative: Some versions include 'Coverage' field
            elif data.get('Action') == 'pass' and 'Coverage' in data:
                coverage = data['Coverage']

        total_tests = passed_tests + failed_tests
        pass_percentage = round((passed_tests / total_tests) * 100, 2) if total_tests > 0 else 0

        v = {
            "test_pass_rate": pass_percentage,
            "execution_time_sec": summary_time if summary_time is not None else total_time,
            "line_coverage_percent": coverage,
            "branch_coverage_percent": None,
            "timeout": False,
            "internal_error_occurred": False,
            "runtime_errors_count": runtime_errors
        }
        return v
    except Exception as e:
        print(f"Error running Go tests: {e}")
        return {
            "test_pass_rate": 0,
            "execution_time_sec": None,
            "line_coverage_percent": None,
            "branch_coverage_percent": None,
            "timeout": True,
            "internal_error_occurred": True,
            "runtime_errors_count": runtime_errors
        }


def x_run_tests__mutmut_85(project_dir):
    test_cmd = ["go", "test", "-timeout", "30s", "-json", "-cover", "./..."]
    runtime_errors = 0

    print("Running tests...")
    try:
        result = subprocess.run(
            test_cmd,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            cwd=project_dir,
            text=True
        )
        output = result.stdout
        print(output)
        stderr = result.stderr
        print(stderr)

        # Check if the subprocess exited with a non-zero return code
        if result.returncode != 0:
            # Parse output for timeout messages
            if "test timed out after" in output or "panic: test timed out" in output:
                print("Tests failed due to timeout")
                # Handle timeout scenario
                return {
                    "test_pass_rate": 0,
                    "execution_time_sec": None,
                    "line_coverage_percent": None,
                    "branch_coverage_percent": None,
                    "timeout": True,
                    "internal_error_occurred": False,
                    "runtime_errors_count": None
                }
            else:
                runtime_error_pattern = re.compile(
                    r"(panic:|fatal error:|runtime error:)", re.IGNORECASE
                )
                assertion_error_pattern = re.compile(r"assert")

                # Search for runtime errors in stdout and stderr
                for o in (output, stderr):
                    for line in o.splitlines():
                        # Match lines with runtime errors but exclude assertions
                        if runtime_error_pattern.search(line) and not assertion_error_pattern.search(line):
                            runtime_errors = runtime_errors + 1

        # Parse the JSON output
        passed_tests = 0
        failed_tests = 0
        total_time = 0.0
        coverage = None
        summary_time = None

        for line in output.splitlines():
            if not line.strip():
                continue
            try:
                data = json.loads(line)
            except json.JSONDecodeError:
                continue  # Skip lines that aren't valid JSON

            # Check for test results
            if data.get('Action') == 'pass' and 'Test' in data:
                passed_tests += 1
                total_time += data.get('Elapsed', 0)
            elif data.get('Action') == 'fail' or 'Test' in data:
                failed_tests += 1
                total_time += data.get('Elapsed', 0)

            if (data.get('Action') == 'pass' or data.get('Action') == 'fail') and 'Test' not in data and 'Elapsed' in data:
                summary_time = data["Elapsed"]

            # Check for coverage information
            if data.get('Action') == 'output' and 'Output' in data:
                output_line = data['Output'].strip()
                if output_line.startswith("coverage:"):
                    # Extract the coverage percentage
                    match = re.search(r'coverage: ([\d\.]+)%', output_line)
                    if match:
                        coverage = float(match.group(1))

            # Alternative: Some versions include 'Coverage' field
            elif data.get('Action') == 'pass' and 'Coverage' in data:
                coverage = data['Coverage']

        total_tests = passed_tests + failed_tests
        pass_percentage = round((passed_tests / total_tests) * 100, 2) if total_tests > 0 else 0

        v = {
            "test_pass_rate": pass_percentage,
            "execution_time_sec": summary_time if summary_time is not None else total_time,
            "line_coverage_percent": coverage,
            "branch_coverage_percent": None,
            "timeout": False,
            "internal_error_occurred": False,
            "runtime_errors_count": runtime_errors
        }
        return v
    except Exception as e:
        print(f"Error running Go tests: {e}")
        return {
            "test_pass_rate": 0,
            "execution_time_sec": None,
            "line_coverage_percent": None,
            "branch_coverage_percent": None,
            "timeout": True,
            "internal_error_occurred": True,
            "runtime_errors_count": runtime_errors
        }


def x_run_tests__mutmut_86(project_dir):
    test_cmd = ["go", "test", "-timeout", "30s", "-json", "-cover", "./..."]
    runtime_errors = 0

    print("Running tests...")
    try:
        result = subprocess.run(
            test_cmd,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            cwd=project_dir,
            text=True
        )
        output = result.stdout
        print(output)
        stderr = result.stderr
        print(stderr)

        # Check if the subprocess exited with a non-zero return code
        if result.returncode != 0:
            # Parse output for timeout messages
            if "test timed out after" in output or "panic: test timed out" in output:
                print("Tests failed due to timeout")
                # Handle timeout scenario
                return {
                    "test_pass_rate": 0,
                    "execution_time_sec": None,
                    "line_coverage_percent": None,
                    "branch_coverage_percent": None,
                    "timeout": True,
                    "internal_error_occurred": False,
                    "runtime_errors_count": None
                }
            else:
                runtime_error_pattern = re.compile(
                    r"(panic:|fatal error:|runtime error:)", re.IGNORECASE
                )
                assertion_error_pattern = re.compile(r"assert")

                # Search for runtime errors in stdout and stderr
                for o in (output, stderr):
                    for line in o.splitlines():
                        # Match lines with runtime errors but exclude assertions
                        if runtime_error_pattern.search(line) and not assertion_error_pattern.search(line):
                            runtime_errors = runtime_errors + 1

        # Parse the JSON output
        passed_tests = 0
        failed_tests = 0
        total_time = 0.0
        coverage = None
        summary_time = None

        for line in output.splitlines():
            if not line.strip():
                continue
            try:
                data = json.loads(line)
            except json.JSONDecodeError:
                continue  # Skip lines that aren't valid JSON

            # Check for test results
            if data.get('Action') == 'pass' and 'Test' in data:
                passed_tests += 1
                total_time += data.get('Elapsed', 0)
            elif data.get('Action') == 'fail' and 'Test' in data:
                failed_tests -= 1
                total_time += data.get('Elapsed', 0)

            if (data.get('Action') == 'pass' or data.get('Action') == 'fail') and 'Test' not in data and 'Elapsed' in data:
                summary_time = data["Elapsed"]

            # Check for coverage information
            if data.get('Action') == 'output' and 'Output' in data:
                output_line = data['Output'].strip()
                if output_line.startswith("coverage:"):
                    # Extract the coverage percentage
                    match = re.search(r'coverage: ([\d\.]+)%', output_line)
                    if match:
                        coverage = float(match.group(1))

            # Alternative: Some versions include 'Coverage' field
            elif data.get('Action') == 'pass' and 'Coverage' in data:
                coverage = data['Coverage']

        total_tests = passed_tests + failed_tests
        pass_percentage = round((passed_tests / total_tests) * 100, 2) if total_tests > 0 else 0

        v = {
            "test_pass_rate": pass_percentage,
            "execution_time_sec": summary_time if summary_time is not None else total_time,
            "line_coverage_percent": coverage,
            "branch_coverage_percent": None,
            "timeout": False,
            "internal_error_occurred": False,
            "runtime_errors_count": runtime_errors
        }
        return v
    except Exception as e:
        print(f"Error running Go tests: {e}")
        return {
            "test_pass_rate": 0,
            "execution_time_sec": None,
            "line_coverage_percent": None,
            "branch_coverage_percent": None,
            "timeout": True,
            "internal_error_occurred": True,
            "runtime_errors_count": runtime_errors
        }


def x_run_tests__mutmut_87(project_dir):
    test_cmd = ["go", "test", "-timeout", "30s", "-json", "-cover", "./..."]
    runtime_errors = 0

    print("Running tests...")
    try:
        result = subprocess.run(
            test_cmd,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            cwd=project_dir,
            text=True
        )
        output = result.stdout
        print(output)
        stderr = result.stderr
        print(stderr)

        # Check if the subprocess exited with a non-zero return code
        if result.returncode != 0:
            # Parse output for timeout messages
            if "test timed out after" in output or "panic: test timed out" in output:
                print("Tests failed due to timeout")
                # Handle timeout scenario
                return {
                    "test_pass_rate": 0,
                    "execution_time_sec": None,
                    "line_coverage_percent": None,
                    "branch_coverage_percent": None,
                    "timeout": True,
                    "internal_error_occurred": False,
                    "runtime_errors_count": None
                }
            else:
                runtime_error_pattern = re.compile(
                    r"(panic:|fatal error:|runtime error:)", re.IGNORECASE
                )
                assertion_error_pattern = re.compile(r"assert")

                # Search for runtime errors in stdout and stderr
                for o in (output, stderr):
                    for line in o.splitlines():
                        # Match lines with runtime errors but exclude assertions
                        if runtime_error_pattern.search(line) and not assertion_error_pattern.search(line):
                            runtime_errors = runtime_errors + 1

        # Parse the JSON output
        passed_tests = 0
        failed_tests = 0
        total_time = 0.0
        coverage = None
        summary_time = None

        for line in output.splitlines():
            if not line.strip():
                continue
            try:
                data = json.loads(line)
            except json.JSONDecodeError:
                continue  # Skip lines that aren't valid JSON

            # Check for test results
            if data.get('Action') == 'pass' and 'Test' in data:
                passed_tests += 1
                total_time += data.get('Elapsed', 0)
            elif data.get('Action') == 'fail' and 'Test' in data:
                failed_tests = 1
                total_time += data.get('Elapsed', 0)

            if (data.get('Action') == 'pass' or data.get('Action') == 'fail') and 'Test' not in data and 'Elapsed' in data:
                summary_time = data["Elapsed"]

            # Check for coverage information
            if data.get('Action') == 'output' and 'Output' in data:
                output_line = data['Output'].strip()
                if output_line.startswith("coverage:"):
                    # Extract the coverage percentage
                    match = re.search(r'coverage: ([\d\.]+)%', output_line)
                    if match:
                        coverage = float(match.group(1))

            # Alternative: Some versions include 'Coverage' field
            elif data.get('Action') == 'pass' and 'Coverage' in data:
                coverage = data['Coverage']

        total_tests = passed_tests + failed_tests
        pass_percentage = round((passed_tests / total_tests) * 100, 2) if total_tests > 0 else 0

        v = {
            "test_pass_rate": pass_percentage,
            "execution_time_sec": summary_time if summary_time is not None else total_time,
            "line_coverage_percent": coverage,
            "branch_coverage_percent": None,
            "timeout": False,
            "internal_error_occurred": False,
            "runtime_errors_count": runtime_errors
        }
        return v
    except Exception as e:
        print(f"Error running Go tests: {e}")
        return {
            "test_pass_rate": 0,
            "execution_time_sec": None,
            "line_coverage_percent": None,
            "branch_coverage_percent": None,
            "timeout": True,
            "internal_error_occurred": True,
            "runtime_errors_count": runtime_errors
        }


def x_run_tests__mutmut_88(project_dir):
    test_cmd = ["go", "test", "-timeout", "30s", "-json", "-cover", "./..."]
    runtime_errors = 0

    print("Running tests...")
    try:
        result = subprocess.run(
            test_cmd,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            cwd=project_dir,
            text=True
        )
        output = result.stdout
        print(output)
        stderr = result.stderr
        print(stderr)

        # Check if the subprocess exited with a non-zero return code
        if result.returncode != 0:
            # Parse output for timeout messages
            if "test timed out after" in output or "panic: test timed out" in output:
                print("Tests failed due to timeout")
                # Handle timeout scenario
                return {
                    "test_pass_rate": 0,
                    "execution_time_sec": None,
                    "line_coverage_percent": None,
                    "branch_coverage_percent": None,
                    "timeout": True,
                    "internal_error_occurred": False,
                    "runtime_errors_count": None
                }
            else:
                runtime_error_pattern = re.compile(
                    r"(panic:|fatal error:|runtime error:)", re.IGNORECASE
                )
                assertion_error_pattern = re.compile(r"assert")

                # Search for runtime errors in stdout and stderr
                for o in (output, stderr):
                    for line in o.splitlines():
                        # Match lines with runtime errors but exclude assertions
                        if runtime_error_pattern.search(line) and not assertion_error_pattern.search(line):
                            runtime_errors = runtime_errors + 1

        # Parse the JSON output
        passed_tests = 0
        failed_tests = 0
        total_time = 0.0
        coverage = None
        summary_time = None

        for line in output.splitlines():
            if not line.strip():
                continue
            try:
                data = json.loads(line)
            except json.JSONDecodeError:
                continue  # Skip lines that aren't valid JSON

            # Check for test results
            if data.get('Action') == 'pass' and 'Test' in data:
                passed_tests += 1
                total_time += data.get('Elapsed', 0)
            elif data.get('Action') == 'fail' and 'Test' in data:
                failed_tests += 2
                total_time += data.get('Elapsed', 0)

            if (data.get('Action') == 'pass' or data.get('Action') == 'fail') and 'Test' not in data and 'Elapsed' in data:
                summary_time = data["Elapsed"]

            # Check for coverage information
            if data.get('Action') == 'output' and 'Output' in data:
                output_line = data['Output'].strip()
                if output_line.startswith("coverage:"):
                    # Extract the coverage percentage
                    match = re.search(r'coverage: ([\d\.]+)%', output_line)
                    if match:
                        coverage = float(match.group(1))

            # Alternative: Some versions include 'Coverage' field
            elif data.get('Action') == 'pass' and 'Coverage' in data:
                coverage = data['Coverage']

        total_tests = passed_tests + failed_tests
        pass_percentage = round((passed_tests / total_tests) * 100, 2) if total_tests > 0 else 0

        v = {
            "test_pass_rate": pass_percentage,
            "execution_time_sec": summary_time if summary_time is not None else total_time,
            "line_coverage_percent": coverage,
            "branch_coverage_percent": None,
            "timeout": False,
            "internal_error_occurred": False,
            "runtime_errors_count": runtime_errors
        }
        return v
    except Exception as e:
        print(f"Error running Go tests: {e}")
        return {
            "test_pass_rate": 0,
            "execution_time_sec": None,
            "line_coverage_percent": None,
            "branch_coverage_percent": None,
            "timeout": True,
            "internal_error_occurred": True,
            "runtime_errors_count": runtime_errors
        }


def x_run_tests__mutmut_89(project_dir):
    test_cmd = ["go", "test", "-timeout", "30s", "-json", "-cover", "./..."]
    runtime_errors = 0

    print("Running tests...")
    try:
        result = subprocess.run(
            test_cmd,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            cwd=project_dir,
            text=True
        )
        output = result.stdout
        print(output)
        stderr = result.stderr
        print(stderr)

        # Check if the subprocess exited with a non-zero return code
        if result.returncode != 0:
            # Parse output for timeout messages
            if "test timed out after" in output or "panic: test timed out" in output:
                print("Tests failed due to timeout")
                # Handle timeout scenario
                return {
                    "test_pass_rate": 0,
                    "execution_time_sec": None,
                    "line_coverage_percent": None,
                    "branch_coverage_percent": None,
                    "timeout": True,
                    "internal_error_occurred": False,
                    "runtime_errors_count": None
                }
            else:
                runtime_error_pattern = re.compile(
                    r"(panic:|fatal error:|runtime error:)", re.IGNORECASE
                )
                assertion_error_pattern = re.compile(r"assert")

                # Search for runtime errors in stdout and stderr
                for o in (output, stderr):
                    for line in o.splitlines():
                        # Match lines with runtime errors but exclude assertions
                        if runtime_error_pattern.search(line) and not assertion_error_pattern.search(line):
                            runtime_errors = runtime_errors + 1

        # Parse the JSON output
        passed_tests = 0
        failed_tests = 0
        total_time = 0.0
        coverage = None
        summary_time = None

        for line in output.splitlines():
            if not line.strip():
                continue
            try:
                data = json.loads(line)
            except json.JSONDecodeError:
                continue  # Skip lines that aren't valid JSON

            # Check for test results
            if data.get('Action') == 'pass' and 'Test' in data:
                passed_tests += 1
                total_time += data.get('Elapsed', 0)
            elif data.get('Action') == 'fail' and 'Test' in data:
                failed_tests += 1
                total_time -= data.get('Elapsed', 0)

            if (data.get('Action') == 'pass' or data.get('Action') == 'fail') and 'Test' not in data and 'Elapsed' in data:
                summary_time = data["Elapsed"]

            # Check for coverage information
            if data.get('Action') == 'output' and 'Output' in data:
                output_line = data['Output'].strip()
                if output_line.startswith("coverage:"):
                    # Extract the coverage percentage
                    match = re.search(r'coverage: ([\d\.]+)%', output_line)
                    if match:
                        coverage = float(match.group(1))

            # Alternative: Some versions include 'Coverage' field
            elif data.get('Action') == 'pass' and 'Coverage' in data:
                coverage = data['Coverage']

        total_tests = passed_tests + failed_tests
        pass_percentage = round((passed_tests / total_tests) * 100, 2) if total_tests > 0 else 0

        v = {
            "test_pass_rate": pass_percentage,
            "execution_time_sec": summary_time if summary_time is not None else total_time,
            "line_coverage_percent": coverage,
            "branch_coverage_percent": None,
            "timeout": False,
            "internal_error_occurred": False,
            "runtime_errors_count": runtime_errors
        }
        return v
    except Exception as e:
        print(f"Error running Go tests: {e}")
        return {
            "test_pass_rate": 0,
            "execution_time_sec": None,
            "line_coverage_percent": None,
            "branch_coverage_percent": None,
            "timeout": True,
            "internal_error_occurred": True,
            "runtime_errors_count": runtime_errors
        }


def x_run_tests__mutmut_90(project_dir):
    test_cmd = ["go", "test", "-timeout", "30s", "-json", "-cover", "./..."]
    runtime_errors = 0

    print("Running tests...")
    try:
        result = subprocess.run(
            test_cmd,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            cwd=project_dir,
            text=True
        )
        output = result.stdout
        print(output)
        stderr = result.stderr
        print(stderr)

        # Check if the subprocess exited with a non-zero return code
        if result.returncode != 0:
            # Parse output for timeout messages
            if "test timed out after" in output or "panic: test timed out" in output:
                print("Tests failed due to timeout")
                # Handle timeout scenario
                return {
                    "test_pass_rate": 0,
                    "execution_time_sec": None,
                    "line_coverage_percent": None,
                    "branch_coverage_percent": None,
                    "timeout": True,
                    "internal_error_occurred": False,
                    "runtime_errors_count": None
                }
            else:
                runtime_error_pattern = re.compile(
                    r"(panic:|fatal error:|runtime error:)", re.IGNORECASE
                )
                assertion_error_pattern = re.compile(r"assert")

                # Search for runtime errors in stdout and stderr
                for o in (output, stderr):
                    for line in o.splitlines():
                        # Match lines with runtime errors but exclude assertions
                        if runtime_error_pattern.search(line) and not assertion_error_pattern.search(line):
                            runtime_errors = runtime_errors + 1

        # Parse the JSON output
        passed_tests = 0
        failed_tests = 0
        total_time = 0.0
        coverage = None
        summary_time = None

        for line in output.splitlines():
            if not line.strip():
                continue
            try:
                data = json.loads(line)
            except json.JSONDecodeError:
                continue  # Skip lines that aren't valid JSON

            # Check for test results
            if data.get('Action') == 'pass' and 'Test' in data:
                passed_tests += 1
                total_time += data.get('Elapsed', 0)
            elif data.get('Action') == 'fail' and 'Test' in data:
                failed_tests += 1
                total_time = data.get('Elapsed', 0)

            if (data.get('Action') == 'pass' or data.get('Action') == 'fail') and 'Test' not in data and 'Elapsed' in data:
                summary_time = data["Elapsed"]

            # Check for coverage information
            if data.get('Action') == 'output' and 'Output' in data:
                output_line = data['Output'].strip()
                if output_line.startswith("coverage:"):
                    # Extract the coverage percentage
                    match = re.search(r'coverage: ([\d\.]+)%', output_line)
                    if match:
                        coverage = float(match.group(1))

            # Alternative: Some versions include 'Coverage' field
            elif data.get('Action') == 'pass' and 'Coverage' in data:
                coverage = data['Coverage']

        total_tests = passed_tests + failed_tests
        pass_percentage = round((passed_tests / total_tests) * 100, 2) if total_tests > 0 else 0

        v = {
            "test_pass_rate": pass_percentage,
            "execution_time_sec": summary_time if summary_time is not None else total_time,
            "line_coverage_percent": coverage,
            "branch_coverage_percent": None,
            "timeout": False,
            "internal_error_occurred": False,
            "runtime_errors_count": runtime_errors
        }
        return v
    except Exception as e:
        print(f"Error running Go tests: {e}")
        return {
            "test_pass_rate": 0,
            "execution_time_sec": None,
            "line_coverage_percent": None,
            "branch_coverage_percent": None,
            "timeout": True,
            "internal_error_occurred": True,
            "runtime_errors_count": runtime_errors
        }


def x_run_tests__mutmut_91(project_dir):
    test_cmd = ["go", "test", "-timeout", "30s", "-json", "-cover", "./..."]
    runtime_errors = 0

    print("Running tests...")
    try:
        result = subprocess.run(
            test_cmd,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            cwd=project_dir,
            text=True
        )
        output = result.stdout
        print(output)
        stderr = result.stderr
        print(stderr)

        # Check if the subprocess exited with a non-zero return code
        if result.returncode != 0:
            # Parse output for timeout messages
            if "test timed out after" in output or "panic: test timed out" in output:
                print("Tests failed due to timeout")
                # Handle timeout scenario
                return {
                    "test_pass_rate": 0,
                    "execution_time_sec": None,
                    "line_coverage_percent": None,
                    "branch_coverage_percent": None,
                    "timeout": True,
                    "internal_error_occurred": False,
                    "runtime_errors_count": None
                }
            else:
                runtime_error_pattern = re.compile(
                    r"(panic:|fatal error:|runtime error:)", re.IGNORECASE
                )
                assertion_error_pattern = re.compile(r"assert")

                # Search for runtime errors in stdout and stderr
                for o in (output, stderr):
                    for line in o.splitlines():
                        # Match lines with runtime errors but exclude assertions
                        if runtime_error_pattern.search(line) and not assertion_error_pattern.search(line):
                            runtime_errors = runtime_errors + 1

        # Parse the JSON output
        passed_tests = 0
        failed_tests = 0
        total_time = 0.0
        coverage = None
        summary_time = None

        for line in output.splitlines():
            if not line.strip():
                continue
            try:
                data = json.loads(line)
            except json.JSONDecodeError:
                continue  # Skip lines that aren't valid JSON

            # Check for test results
            if data.get('Action') == 'pass' and 'Test' in data:
                passed_tests += 1
                total_time += data.get('Elapsed', 0)
            elif data.get('Action') == 'fail' and 'Test' in data:
                failed_tests += 1
                total_time += data.get('XXElapsedXX', 0)

            if (data.get('Action') == 'pass' or data.get('Action') == 'fail') and 'Test' not in data and 'Elapsed' in data:
                summary_time = data["Elapsed"]

            # Check for coverage information
            if data.get('Action') == 'output' and 'Output' in data:
                output_line = data['Output'].strip()
                if output_line.startswith("coverage:"):
                    # Extract the coverage percentage
                    match = re.search(r'coverage: ([\d\.]+)%', output_line)
                    if match:
                        coverage = float(match.group(1))

            # Alternative: Some versions include 'Coverage' field
            elif data.get('Action') == 'pass' and 'Coverage' in data:
                coverage = data['Coverage']

        total_tests = passed_tests + failed_tests
        pass_percentage = round((passed_tests / total_tests) * 100, 2) if total_tests > 0 else 0

        v = {
            "test_pass_rate": pass_percentage,
            "execution_time_sec": summary_time if summary_time is not None else total_time,
            "line_coverage_percent": coverage,
            "branch_coverage_percent": None,
            "timeout": False,
            "internal_error_occurred": False,
            "runtime_errors_count": runtime_errors
        }
        return v
    except Exception as e:
        print(f"Error running Go tests: {e}")
        return {
            "test_pass_rate": 0,
            "execution_time_sec": None,
            "line_coverage_percent": None,
            "branch_coverage_percent": None,
            "timeout": True,
            "internal_error_occurred": True,
            "runtime_errors_count": runtime_errors
        }


def x_run_tests__mutmut_92(project_dir):
    test_cmd = ["go", "test", "-timeout", "30s", "-json", "-cover", "./..."]
    runtime_errors = 0

    print("Running tests...")
    try:
        result = subprocess.run(
            test_cmd,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            cwd=project_dir,
            text=True
        )
        output = result.stdout
        print(output)
        stderr = result.stderr
        print(stderr)

        # Check if the subprocess exited with a non-zero return code
        if result.returncode != 0:
            # Parse output for timeout messages
            if "test timed out after" in output or "panic: test timed out" in output:
                print("Tests failed due to timeout")
                # Handle timeout scenario
                return {
                    "test_pass_rate": 0,
                    "execution_time_sec": None,
                    "line_coverage_percent": None,
                    "branch_coverage_percent": None,
                    "timeout": True,
                    "internal_error_occurred": False,
                    "runtime_errors_count": None
                }
            else:
                runtime_error_pattern = re.compile(
                    r"(panic:|fatal error:|runtime error:)", re.IGNORECASE
                )
                assertion_error_pattern = re.compile(r"assert")

                # Search for runtime errors in stdout and stderr
                for o in (output, stderr):
                    for line in o.splitlines():
                        # Match lines with runtime errors but exclude assertions
                        if runtime_error_pattern.search(line) and not assertion_error_pattern.search(line):
                            runtime_errors = runtime_errors + 1

        # Parse the JSON output
        passed_tests = 0
        failed_tests = 0
        total_time = 0.0
        coverage = None
        summary_time = None

        for line in output.splitlines():
            if not line.strip():
                continue
            try:
                data = json.loads(line)
            except json.JSONDecodeError:
                continue  # Skip lines that aren't valid JSON

            # Check for test results
            if data.get('Action') == 'pass' and 'Test' in data:
                passed_tests += 1
                total_time += data.get('Elapsed', 0)
            elif data.get('Action') == 'fail' and 'Test' in data:
                failed_tests += 1
                total_time += data.get('Elapsed', 1)

            if (data.get('Action') == 'pass' or data.get('Action') == 'fail') and 'Test' not in data and 'Elapsed' in data:
                summary_time = data["Elapsed"]

            # Check for coverage information
            if data.get('Action') == 'output' and 'Output' in data:
                output_line = data['Output'].strip()
                if output_line.startswith("coverage:"):
                    # Extract the coverage percentage
                    match = re.search(r'coverage: ([\d\.]+)%', output_line)
                    if match:
                        coverage = float(match.group(1))

            # Alternative: Some versions include 'Coverage' field
            elif data.get('Action') == 'pass' and 'Coverage' in data:
                coverage = data['Coverage']

        total_tests = passed_tests + failed_tests
        pass_percentage = round((passed_tests / total_tests) * 100, 2) if total_tests > 0 else 0

        v = {
            "test_pass_rate": pass_percentage,
            "execution_time_sec": summary_time if summary_time is not None else total_time,
            "line_coverage_percent": coverage,
            "branch_coverage_percent": None,
            "timeout": False,
            "internal_error_occurred": False,
            "runtime_errors_count": runtime_errors
        }
        return v
    except Exception as e:
        print(f"Error running Go tests: {e}")
        return {
            "test_pass_rate": 0,
            "execution_time_sec": None,
            "line_coverage_percent": None,
            "branch_coverage_percent": None,
            "timeout": True,
            "internal_error_occurred": True,
            "runtime_errors_count": runtime_errors
        }


def x_run_tests__mutmut_93(project_dir):
    test_cmd = ["go", "test", "-timeout", "30s", "-json", "-cover", "./..."]
    runtime_errors = 0

    print("Running tests...")
    try:
        result = subprocess.run(
            test_cmd,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            cwd=project_dir,
            text=True
        )
        output = result.stdout
        print(output)
        stderr = result.stderr
        print(stderr)

        # Check if the subprocess exited with a non-zero return code
        if result.returncode != 0:
            # Parse output for timeout messages
            if "test timed out after" in output or "panic: test timed out" in output:
                print("Tests failed due to timeout")
                # Handle timeout scenario
                return {
                    "test_pass_rate": 0,
                    "execution_time_sec": None,
                    "line_coverage_percent": None,
                    "branch_coverage_percent": None,
                    "timeout": True,
                    "internal_error_occurred": False,
                    "runtime_errors_count": None
                }
            else:
                runtime_error_pattern = re.compile(
                    r"(panic:|fatal error:|runtime error:)", re.IGNORECASE
                )
                assertion_error_pattern = re.compile(r"assert")

                # Search for runtime errors in stdout and stderr
                for o in (output, stderr):
                    for line in o.splitlines():
                        # Match lines with runtime errors but exclude assertions
                        if runtime_error_pattern.search(line) and not assertion_error_pattern.search(line):
                            runtime_errors = runtime_errors + 1

        # Parse the JSON output
        passed_tests = 0
        failed_tests = 0
        total_time = 0.0
        coverage = None
        summary_time = None

        for line in output.splitlines():
            if not line.strip():
                continue
            try:
                data = json.loads(line)
            except json.JSONDecodeError:
                continue  # Skip lines that aren't valid JSON

            # Check for test results
            if data.get('Action') == 'pass' and 'Test' in data:
                passed_tests += 1
                total_time += data.get('Elapsed', 0)
            elif data.get('Action') == 'fail' and 'Test' in data:
                failed_tests += 1
                total_time += data.get('Elapsed', 0)

            if (data.get('XXActionXX') == 'pass' or data.get('Action') == 'fail') and 'Test' not in data and 'Elapsed' in data:
                summary_time = data["Elapsed"]

            # Check for coverage information
            if data.get('Action') == 'output' and 'Output' in data:
                output_line = data['Output'].strip()
                if output_line.startswith("coverage:"):
                    # Extract the coverage percentage
                    match = re.search(r'coverage: ([\d\.]+)%', output_line)
                    if match:
                        coverage = float(match.group(1))

            # Alternative: Some versions include 'Coverage' field
            elif data.get('Action') == 'pass' and 'Coverage' in data:
                coverage = data['Coverage']

        total_tests = passed_tests + failed_tests
        pass_percentage = round((passed_tests / total_tests) * 100, 2) if total_tests > 0 else 0

        v = {
            "test_pass_rate": pass_percentage,
            "execution_time_sec": summary_time if summary_time is not None else total_time,
            "line_coverage_percent": coverage,
            "branch_coverage_percent": None,
            "timeout": False,
            "internal_error_occurred": False,
            "runtime_errors_count": runtime_errors
        }
        return v
    except Exception as e:
        print(f"Error running Go tests: {e}")
        return {
            "test_pass_rate": 0,
            "execution_time_sec": None,
            "line_coverage_percent": None,
            "branch_coverage_percent": None,
            "timeout": True,
            "internal_error_occurred": True,
            "runtime_errors_count": runtime_errors
        }


def x_run_tests__mutmut_94(project_dir):
    test_cmd = ["go", "test", "-timeout", "30s", "-json", "-cover", "./..."]
    runtime_errors = 0

    print("Running tests...")
    try:
        result = subprocess.run(
            test_cmd,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            cwd=project_dir,
            text=True
        )
        output = result.stdout
        print(output)
        stderr = result.stderr
        print(stderr)

        # Check if the subprocess exited with a non-zero return code
        if result.returncode != 0:
            # Parse output for timeout messages
            if "test timed out after" in output or "panic: test timed out" in output:
                print("Tests failed due to timeout")
                # Handle timeout scenario
                return {
                    "test_pass_rate": 0,
                    "execution_time_sec": None,
                    "line_coverage_percent": None,
                    "branch_coverage_percent": None,
                    "timeout": True,
                    "internal_error_occurred": False,
                    "runtime_errors_count": None
                }
            else:
                runtime_error_pattern = re.compile(
                    r"(panic:|fatal error:|runtime error:)", re.IGNORECASE
                )
                assertion_error_pattern = re.compile(r"assert")

                # Search for runtime errors in stdout and stderr
                for o in (output, stderr):
                    for line in o.splitlines():
                        # Match lines with runtime errors but exclude assertions
                        if runtime_error_pattern.search(line) and not assertion_error_pattern.search(line):
                            runtime_errors = runtime_errors + 1

        # Parse the JSON output
        passed_tests = 0
        failed_tests = 0
        total_time = 0.0
        coverage = None
        summary_time = None

        for line in output.splitlines():
            if not line.strip():
                continue
            try:
                data = json.loads(line)
            except json.JSONDecodeError:
                continue  # Skip lines that aren't valid JSON

            # Check for test results
            if data.get('Action') == 'pass' and 'Test' in data:
                passed_tests += 1
                total_time += data.get('Elapsed', 0)
            elif data.get('Action') == 'fail' and 'Test' in data:
                failed_tests += 1
                total_time += data.get('Elapsed', 0)

            if (data.get('Action') != 'pass' or data.get('Action') == 'fail') and 'Test' not in data and 'Elapsed' in data:
                summary_time = data["Elapsed"]

            # Check for coverage information
            if data.get('Action') == 'output' and 'Output' in data:
                output_line = data['Output'].strip()
                if output_line.startswith("coverage:"):
                    # Extract the coverage percentage
                    match = re.search(r'coverage: ([\d\.]+)%', output_line)
                    if match:
                        coverage = float(match.group(1))

            # Alternative: Some versions include 'Coverage' field
            elif data.get('Action') == 'pass' and 'Coverage' in data:
                coverage = data['Coverage']

        total_tests = passed_tests + failed_tests
        pass_percentage = round((passed_tests / total_tests) * 100, 2) if total_tests > 0 else 0

        v = {
            "test_pass_rate": pass_percentage,
            "execution_time_sec": summary_time if summary_time is not None else total_time,
            "line_coverage_percent": coverage,
            "branch_coverage_percent": None,
            "timeout": False,
            "internal_error_occurred": False,
            "runtime_errors_count": runtime_errors
        }
        return v
    except Exception as e:
        print(f"Error running Go tests: {e}")
        return {
            "test_pass_rate": 0,
            "execution_time_sec": None,
            "line_coverage_percent": None,
            "branch_coverage_percent": None,
            "timeout": True,
            "internal_error_occurred": True,
            "runtime_errors_count": runtime_errors
        }


def x_run_tests__mutmut_95(project_dir):
    test_cmd = ["go", "test", "-timeout", "30s", "-json", "-cover", "./..."]
    runtime_errors = 0

    print("Running tests...")
    try:
        result = subprocess.run(
            test_cmd,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            cwd=project_dir,
            text=True
        )
        output = result.stdout
        print(output)
        stderr = result.stderr
        print(stderr)

        # Check if the subprocess exited with a non-zero return code
        if result.returncode != 0:
            # Parse output for timeout messages
            if "test timed out after" in output or "panic: test timed out" in output:
                print("Tests failed due to timeout")
                # Handle timeout scenario
                return {
                    "test_pass_rate": 0,
                    "execution_time_sec": None,
                    "line_coverage_percent": None,
                    "branch_coverage_percent": None,
                    "timeout": True,
                    "internal_error_occurred": False,
                    "runtime_errors_count": None
                }
            else:
                runtime_error_pattern = re.compile(
                    r"(panic:|fatal error:|runtime error:)", re.IGNORECASE
                )
                assertion_error_pattern = re.compile(r"assert")

                # Search for runtime errors in stdout and stderr
                for o in (output, stderr):
                    for line in o.splitlines():
                        # Match lines with runtime errors but exclude assertions
                        if runtime_error_pattern.search(line) and not assertion_error_pattern.search(line):
                            runtime_errors = runtime_errors + 1

        # Parse the JSON output
        passed_tests = 0
        failed_tests = 0
        total_time = 0.0
        coverage = None
        summary_time = None

        for line in output.splitlines():
            if not line.strip():
                continue
            try:
                data = json.loads(line)
            except json.JSONDecodeError:
                continue  # Skip lines that aren't valid JSON

            # Check for test results
            if data.get('Action') == 'pass' and 'Test' in data:
                passed_tests += 1
                total_time += data.get('Elapsed', 0)
            elif data.get('Action') == 'fail' and 'Test' in data:
                failed_tests += 1
                total_time += data.get('Elapsed', 0)

            if (data.get('Action') == 'XXpassXX' or data.get('Action') == 'fail') and 'Test' not in data and 'Elapsed' in data:
                summary_time = data["Elapsed"]

            # Check for coverage information
            if data.get('Action') == 'output' and 'Output' in data:
                output_line = data['Output'].strip()
                if output_line.startswith("coverage:"):
                    # Extract the coverage percentage
                    match = re.search(r'coverage: ([\d\.]+)%', output_line)
                    if match:
                        coverage = float(match.group(1))

            # Alternative: Some versions include 'Coverage' field
            elif data.get('Action') == 'pass' and 'Coverage' in data:
                coverage = data['Coverage']

        total_tests = passed_tests + failed_tests
        pass_percentage = round((passed_tests / total_tests) * 100, 2) if total_tests > 0 else 0

        v = {
            "test_pass_rate": pass_percentage,
            "execution_time_sec": summary_time if summary_time is not None else total_time,
            "line_coverage_percent": coverage,
            "branch_coverage_percent": None,
            "timeout": False,
            "internal_error_occurred": False,
            "runtime_errors_count": runtime_errors
        }
        return v
    except Exception as e:
        print(f"Error running Go tests: {e}")
        return {
            "test_pass_rate": 0,
            "execution_time_sec": None,
            "line_coverage_percent": None,
            "branch_coverage_percent": None,
            "timeout": True,
            "internal_error_occurred": True,
            "runtime_errors_count": runtime_errors
        }


def x_run_tests__mutmut_96(project_dir):
    test_cmd = ["go", "test", "-timeout", "30s", "-json", "-cover", "./..."]
    runtime_errors = 0

    print("Running tests...")
    try:
        result = subprocess.run(
            test_cmd,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            cwd=project_dir,
            text=True
        )
        output = result.stdout
        print(output)
        stderr = result.stderr
        print(stderr)

        # Check if the subprocess exited with a non-zero return code
        if result.returncode != 0:
            # Parse output for timeout messages
            if "test timed out after" in output or "panic: test timed out" in output:
                print("Tests failed due to timeout")
                # Handle timeout scenario
                return {
                    "test_pass_rate": 0,
                    "execution_time_sec": None,
                    "line_coverage_percent": None,
                    "branch_coverage_percent": None,
                    "timeout": True,
                    "internal_error_occurred": False,
                    "runtime_errors_count": None
                }
            else:
                runtime_error_pattern = re.compile(
                    r"(panic:|fatal error:|runtime error:)", re.IGNORECASE
                )
                assertion_error_pattern = re.compile(r"assert")

                # Search for runtime errors in stdout and stderr
                for o in (output, stderr):
                    for line in o.splitlines():
                        # Match lines with runtime errors but exclude assertions
                        if runtime_error_pattern.search(line) and not assertion_error_pattern.search(line):
                            runtime_errors = runtime_errors + 1

        # Parse the JSON output
        passed_tests = 0
        failed_tests = 0
        total_time = 0.0
        coverage = None
        summary_time = None

        for line in output.splitlines():
            if not line.strip():
                continue
            try:
                data = json.loads(line)
            except json.JSONDecodeError:
                continue  # Skip lines that aren't valid JSON

            # Check for test results
            if data.get('Action') == 'pass' and 'Test' in data:
                passed_tests += 1
                total_time += data.get('Elapsed', 0)
            elif data.get('Action') == 'fail' and 'Test' in data:
                failed_tests += 1
                total_time += data.get('Elapsed', 0)

            if (data.get('Action') == 'pass' or data.get('XXActionXX') == 'fail') and 'Test' not in data and 'Elapsed' in data:
                summary_time = data["Elapsed"]

            # Check for coverage information
            if data.get('Action') == 'output' and 'Output' in data:
                output_line = data['Output'].strip()
                if output_line.startswith("coverage:"):
                    # Extract the coverage percentage
                    match = re.search(r'coverage: ([\d\.]+)%', output_line)
                    if match:
                        coverage = float(match.group(1))

            # Alternative: Some versions include 'Coverage' field
            elif data.get('Action') == 'pass' and 'Coverage' in data:
                coverage = data['Coverage']

        total_tests = passed_tests + failed_tests
        pass_percentage = round((passed_tests / total_tests) * 100, 2) if total_tests > 0 else 0

        v = {
            "test_pass_rate": pass_percentage,
            "execution_time_sec": summary_time if summary_time is not None else total_time,
            "line_coverage_percent": coverage,
            "branch_coverage_percent": None,
            "timeout": False,
            "internal_error_occurred": False,
            "runtime_errors_count": runtime_errors
        }
        return v
    except Exception as e:
        print(f"Error running Go tests: {e}")
        return {
            "test_pass_rate": 0,
            "execution_time_sec": None,
            "line_coverage_percent": None,
            "branch_coverage_percent": None,
            "timeout": True,
            "internal_error_occurred": True,
            "runtime_errors_count": runtime_errors
        }


def x_run_tests__mutmut_97(project_dir):
    test_cmd = ["go", "test", "-timeout", "30s", "-json", "-cover", "./..."]
    runtime_errors = 0

    print("Running tests...")
    try:
        result = subprocess.run(
            test_cmd,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            cwd=project_dir,
            text=True
        )
        output = result.stdout
        print(output)
        stderr = result.stderr
        print(stderr)

        # Check if the subprocess exited with a non-zero return code
        if result.returncode != 0:
            # Parse output for timeout messages
            if "test timed out after" in output or "panic: test timed out" in output:
                print("Tests failed due to timeout")
                # Handle timeout scenario
                return {
                    "test_pass_rate": 0,
                    "execution_time_sec": None,
                    "line_coverage_percent": None,
                    "branch_coverage_percent": None,
                    "timeout": True,
                    "internal_error_occurred": False,
                    "runtime_errors_count": None
                }
            else:
                runtime_error_pattern = re.compile(
                    r"(panic:|fatal error:|runtime error:)", re.IGNORECASE
                )
                assertion_error_pattern = re.compile(r"assert")

                # Search for runtime errors in stdout and stderr
                for o in (output, stderr):
                    for line in o.splitlines():
                        # Match lines with runtime errors but exclude assertions
                        if runtime_error_pattern.search(line) and not assertion_error_pattern.search(line):
                            runtime_errors = runtime_errors + 1

        # Parse the JSON output
        passed_tests = 0
        failed_tests = 0
        total_time = 0.0
        coverage = None
        summary_time = None

        for line in output.splitlines():
            if not line.strip():
                continue
            try:
                data = json.loads(line)
            except json.JSONDecodeError:
                continue  # Skip lines that aren't valid JSON

            # Check for test results
            if data.get('Action') == 'pass' and 'Test' in data:
                passed_tests += 1
                total_time += data.get('Elapsed', 0)
            elif data.get('Action') == 'fail' and 'Test' in data:
                failed_tests += 1
                total_time += data.get('Elapsed', 0)

            if (data.get('Action') == 'pass' or data.get('Action') != 'fail') and 'Test' not in data and 'Elapsed' in data:
                summary_time = data["Elapsed"]

            # Check for coverage information
            if data.get('Action') == 'output' and 'Output' in data:
                output_line = data['Output'].strip()
                if output_line.startswith("coverage:"):
                    # Extract the coverage percentage
                    match = re.search(r'coverage: ([\d\.]+)%', output_line)
                    if match:
                        coverage = float(match.group(1))

            # Alternative: Some versions include 'Coverage' field
            elif data.get('Action') == 'pass' and 'Coverage' in data:
                coverage = data['Coverage']

        total_tests = passed_tests + failed_tests
        pass_percentage = round((passed_tests / total_tests) * 100, 2) if total_tests > 0 else 0

        v = {
            "test_pass_rate": pass_percentage,
            "execution_time_sec": summary_time if summary_time is not None else total_time,
            "line_coverage_percent": coverage,
            "branch_coverage_percent": None,
            "timeout": False,
            "internal_error_occurred": False,
            "runtime_errors_count": runtime_errors
        }
        return v
    except Exception as e:
        print(f"Error running Go tests: {e}")
        return {
            "test_pass_rate": 0,
            "execution_time_sec": None,
            "line_coverage_percent": None,
            "branch_coverage_percent": None,
            "timeout": True,
            "internal_error_occurred": True,
            "runtime_errors_count": runtime_errors
        }


def x_run_tests__mutmut_98(project_dir):
    test_cmd = ["go", "test", "-timeout", "30s", "-json", "-cover", "./..."]
    runtime_errors = 0

    print("Running tests...")
    try:
        result = subprocess.run(
            test_cmd,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            cwd=project_dir,
            text=True
        )
        output = result.stdout
        print(output)
        stderr = result.stderr
        print(stderr)

        # Check if the subprocess exited with a non-zero return code
        if result.returncode != 0:
            # Parse output for timeout messages
            if "test timed out after" in output or "panic: test timed out" in output:
                print("Tests failed due to timeout")
                # Handle timeout scenario
                return {
                    "test_pass_rate": 0,
                    "execution_time_sec": None,
                    "line_coverage_percent": None,
                    "branch_coverage_percent": None,
                    "timeout": True,
                    "internal_error_occurred": False,
                    "runtime_errors_count": None
                }
            else:
                runtime_error_pattern = re.compile(
                    r"(panic:|fatal error:|runtime error:)", re.IGNORECASE
                )
                assertion_error_pattern = re.compile(r"assert")

                # Search for runtime errors in stdout and stderr
                for o in (output, stderr):
                    for line in o.splitlines():
                        # Match lines with runtime errors but exclude assertions
                        if runtime_error_pattern.search(line) and not assertion_error_pattern.search(line):
                            runtime_errors = runtime_errors + 1

        # Parse the JSON output
        passed_tests = 0
        failed_tests = 0
        total_time = 0.0
        coverage = None
        summary_time = None

        for line in output.splitlines():
            if not line.strip():
                continue
            try:
                data = json.loads(line)
            except json.JSONDecodeError:
                continue  # Skip lines that aren't valid JSON

            # Check for test results
            if data.get('Action') == 'pass' and 'Test' in data:
                passed_tests += 1
                total_time += data.get('Elapsed', 0)
            elif data.get('Action') == 'fail' and 'Test' in data:
                failed_tests += 1
                total_time += data.get('Elapsed', 0)

            if (data.get('Action') == 'pass' or data.get('Action') == 'XXfailXX') and 'Test' not in data and 'Elapsed' in data:
                summary_time = data["Elapsed"]

            # Check for coverage information
            if data.get('Action') == 'output' and 'Output' in data:
                output_line = data['Output'].strip()
                if output_line.startswith("coverage:"):
                    # Extract the coverage percentage
                    match = re.search(r'coverage: ([\d\.]+)%', output_line)
                    if match:
                        coverage = float(match.group(1))

            # Alternative: Some versions include 'Coverage' field
            elif data.get('Action') == 'pass' and 'Coverage' in data:
                coverage = data['Coverage']

        total_tests = passed_tests + failed_tests
        pass_percentage = round((passed_tests / total_tests) * 100, 2) if total_tests > 0 else 0

        v = {
            "test_pass_rate": pass_percentage,
            "execution_time_sec": summary_time if summary_time is not None else total_time,
            "line_coverage_percent": coverage,
            "branch_coverage_percent": None,
            "timeout": False,
            "internal_error_occurred": False,
            "runtime_errors_count": runtime_errors
        }
        return v
    except Exception as e:
        print(f"Error running Go tests: {e}")
        return {
            "test_pass_rate": 0,
            "execution_time_sec": None,
            "line_coverage_percent": None,
            "branch_coverage_percent": None,
            "timeout": True,
            "internal_error_occurred": True,
            "runtime_errors_count": runtime_errors
        }


def x_run_tests__mutmut_99(project_dir):
    test_cmd = ["go", "test", "-timeout", "30s", "-json", "-cover", "./..."]
    runtime_errors = 0

    print("Running tests...")
    try:
        result = subprocess.run(
            test_cmd,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            cwd=project_dir,
            text=True
        )
        output = result.stdout
        print(output)
        stderr = result.stderr
        print(stderr)

        # Check if the subprocess exited with a non-zero return code
        if result.returncode != 0:
            # Parse output for timeout messages
            if "test timed out after" in output or "panic: test timed out" in output:
                print("Tests failed due to timeout")
                # Handle timeout scenario
                return {
                    "test_pass_rate": 0,
                    "execution_time_sec": None,
                    "line_coverage_percent": None,
                    "branch_coverage_percent": None,
                    "timeout": True,
                    "internal_error_occurred": False,
                    "runtime_errors_count": None
                }
            else:
                runtime_error_pattern = re.compile(
                    r"(panic:|fatal error:|runtime error:)", re.IGNORECASE
                )
                assertion_error_pattern = re.compile(r"assert")

                # Search for runtime errors in stdout and stderr
                for o in (output, stderr):
                    for line in o.splitlines():
                        # Match lines with runtime errors but exclude assertions
                        if runtime_error_pattern.search(line) and not assertion_error_pattern.search(line):
                            runtime_errors = runtime_errors + 1

        # Parse the JSON output
        passed_tests = 0
        failed_tests = 0
        total_time = 0.0
        coverage = None
        summary_time = None

        for line in output.splitlines():
            if not line.strip():
                continue
            try:
                data = json.loads(line)
            except json.JSONDecodeError:
                continue  # Skip lines that aren't valid JSON

            # Check for test results
            if data.get('Action') == 'pass' and 'Test' in data:
                passed_tests += 1
                total_time += data.get('Elapsed', 0)
            elif data.get('Action') == 'fail' and 'Test' in data:
                failed_tests += 1
                total_time += data.get('Elapsed', 0)

            if (data.get('Action') == 'pass' and data.get('Action') == 'fail') and 'Test' not in data and 'Elapsed' in data:
                summary_time = data["Elapsed"]

            # Check for coverage information
            if data.get('Action') == 'output' and 'Output' in data:
                output_line = data['Output'].strip()
                if output_line.startswith("coverage:"):
                    # Extract the coverage percentage
                    match = re.search(r'coverage: ([\d\.]+)%', output_line)
                    if match:
                        coverage = float(match.group(1))

            # Alternative: Some versions include 'Coverage' field
            elif data.get('Action') == 'pass' and 'Coverage' in data:
                coverage = data['Coverage']

        total_tests = passed_tests + failed_tests
        pass_percentage = round((passed_tests / total_tests) * 100, 2) if total_tests > 0 else 0

        v = {
            "test_pass_rate": pass_percentage,
            "execution_time_sec": summary_time if summary_time is not None else total_time,
            "line_coverage_percent": coverage,
            "branch_coverage_percent": None,
            "timeout": False,
            "internal_error_occurred": False,
            "runtime_errors_count": runtime_errors
        }
        return v
    except Exception as e:
        print(f"Error running Go tests: {e}")
        return {
            "test_pass_rate": 0,
            "execution_time_sec": None,
            "line_coverage_percent": None,
            "branch_coverage_percent": None,
            "timeout": True,
            "internal_error_occurred": True,
            "runtime_errors_count": runtime_errors
        }


def x_run_tests__mutmut_100(project_dir):
    test_cmd = ["go", "test", "-timeout", "30s", "-json", "-cover", "./..."]
    runtime_errors = 0

    print("Running tests...")
    try:
        result = subprocess.run(
            test_cmd,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            cwd=project_dir,
            text=True
        )
        output = result.stdout
        print(output)
        stderr = result.stderr
        print(stderr)

        # Check if the subprocess exited with a non-zero return code
        if result.returncode != 0:
            # Parse output for timeout messages
            if "test timed out after" in output or "panic: test timed out" in output:
                print("Tests failed due to timeout")
                # Handle timeout scenario
                return {
                    "test_pass_rate": 0,
                    "execution_time_sec": None,
                    "line_coverage_percent": None,
                    "branch_coverage_percent": None,
                    "timeout": True,
                    "internal_error_occurred": False,
                    "runtime_errors_count": None
                }
            else:
                runtime_error_pattern = re.compile(
                    r"(panic:|fatal error:|runtime error:)", re.IGNORECASE
                )
                assertion_error_pattern = re.compile(r"assert")

                # Search for runtime errors in stdout and stderr
                for o in (output, stderr):
                    for line in o.splitlines():
                        # Match lines with runtime errors but exclude assertions
                        if runtime_error_pattern.search(line) and not assertion_error_pattern.search(line):
                            runtime_errors = runtime_errors + 1

        # Parse the JSON output
        passed_tests = 0
        failed_tests = 0
        total_time = 0.0
        coverage = None
        summary_time = None

        for line in output.splitlines():
            if not line.strip():
                continue
            try:
                data = json.loads(line)
            except json.JSONDecodeError:
                continue  # Skip lines that aren't valid JSON

            # Check for test results
            if data.get('Action') == 'pass' and 'Test' in data:
                passed_tests += 1
                total_time += data.get('Elapsed', 0)
            elif data.get('Action') == 'fail' and 'Test' in data:
                failed_tests += 1
                total_time += data.get('Elapsed', 0)

            if (data.get('Action') == 'pass' or data.get('Action') == 'fail') and 'XXTestXX' not in data and 'Elapsed' in data:
                summary_time = data["Elapsed"]

            # Check for coverage information
            if data.get('Action') == 'output' and 'Output' in data:
                output_line = data['Output'].strip()
                if output_line.startswith("coverage:"):
                    # Extract the coverage percentage
                    match = re.search(r'coverage: ([\d\.]+)%', output_line)
                    if match:
                        coverage = float(match.group(1))

            # Alternative: Some versions include 'Coverage' field
            elif data.get('Action') == 'pass' and 'Coverage' in data:
                coverage = data['Coverage']

        total_tests = passed_tests + failed_tests
        pass_percentage = round((passed_tests / total_tests) * 100, 2) if total_tests > 0 else 0

        v = {
            "test_pass_rate": pass_percentage,
            "execution_time_sec": summary_time if summary_time is not None else total_time,
            "line_coverage_percent": coverage,
            "branch_coverage_percent": None,
            "timeout": False,
            "internal_error_occurred": False,
            "runtime_errors_count": runtime_errors
        }
        return v
    except Exception as e:
        print(f"Error running Go tests: {e}")
        return {
            "test_pass_rate": 0,
            "execution_time_sec": None,
            "line_coverage_percent": None,
            "branch_coverage_percent": None,
            "timeout": True,
            "internal_error_occurred": True,
            "runtime_errors_count": runtime_errors
        }


def x_run_tests__mutmut_101(project_dir):
    test_cmd = ["go", "test", "-timeout", "30s", "-json", "-cover", "./..."]
    runtime_errors = 0

    print("Running tests...")
    try:
        result = subprocess.run(
            test_cmd,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            cwd=project_dir,
            text=True
        )
        output = result.stdout
        print(output)
        stderr = result.stderr
        print(stderr)

        # Check if the subprocess exited with a non-zero return code
        if result.returncode != 0:
            # Parse output for timeout messages
            if "test timed out after" in output or "panic: test timed out" in output:
                print("Tests failed due to timeout")
                # Handle timeout scenario
                return {
                    "test_pass_rate": 0,
                    "execution_time_sec": None,
                    "line_coverage_percent": None,
                    "branch_coverage_percent": None,
                    "timeout": True,
                    "internal_error_occurred": False,
                    "runtime_errors_count": None
                }
            else:
                runtime_error_pattern = re.compile(
                    r"(panic:|fatal error:|runtime error:)", re.IGNORECASE
                )
                assertion_error_pattern = re.compile(r"assert")

                # Search for runtime errors in stdout and stderr
                for o in (output, stderr):
                    for line in o.splitlines():
                        # Match lines with runtime errors but exclude assertions
                        if runtime_error_pattern.search(line) and not assertion_error_pattern.search(line):
                            runtime_errors = runtime_errors + 1

        # Parse the JSON output
        passed_tests = 0
        failed_tests = 0
        total_time = 0.0
        coverage = None
        summary_time = None

        for line in output.splitlines():
            if not line.strip():
                continue
            try:
                data = json.loads(line)
            except json.JSONDecodeError:
                continue  # Skip lines that aren't valid JSON

            # Check for test results
            if data.get('Action') == 'pass' and 'Test' in data:
                passed_tests += 1
                total_time += data.get('Elapsed', 0)
            elif data.get('Action') == 'fail' and 'Test' in data:
                failed_tests += 1
                total_time += data.get('Elapsed', 0)

            if (data.get('Action') == 'pass' or data.get('Action') == 'fail') and 'Test'  in data and 'Elapsed' in data:
                summary_time = data["Elapsed"]

            # Check for coverage information
            if data.get('Action') == 'output' and 'Output' in data:
                output_line = data['Output'].strip()
                if output_line.startswith("coverage:"):
                    # Extract the coverage percentage
                    match = re.search(r'coverage: ([\d\.]+)%', output_line)
                    if match:
                        coverage = float(match.group(1))

            # Alternative: Some versions include 'Coverage' field
            elif data.get('Action') == 'pass' and 'Coverage' in data:
                coverage = data['Coverage']

        total_tests = passed_tests + failed_tests
        pass_percentage = round((passed_tests / total_tests) * 100, 2) if total_tests > 0 else 0

        v = {
            "test_pass_rate": pass_percentage,
            "execution_time_sec": summary_time if summary_time is not None else total_time,
            "line_coverage_percent": coverage,
            "branch_coverage_percent": None,
            "timeout": False,
            "internal_error_occurred": False,
            "runtime_errors_count": runtime_errors
        }
        return v
    except Exception as e:
        print(f"Error running Go tests: {e}")
        return {
            "test_pass_rate": 0,
            "execution_time_sec": None,
            "line_coverage_percent": None,
            "branch_coverage_percent": None,
            "timeout": True,
            "internal_error_occurred": True,
            "runtime_errors_count": runtime_errors
        }


def x_run_tests__mutmut_102(project_dir):
    test_cmd = ["go", "test", "-timeout", "30s", "-json", "-cover", "./..."]
    runtime_errors = 0

    print("Running tests...")
    try:
        result = subprocess.run(
            test_cmd,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            cwd=project_dir,
            text=True
        )
        output = result.stdout
        print(output)
        stderr = result.stderr
        print(stderr)

        # Check if the subprocess exited with a non-zero return code
        if result.returncode != 0:
            # Parse output for timeout messages
            if "test timed out after" in output or "panic: test timed out" in output:
                print("Tests failed due to timeout")
                # Handle timeout scenario
                return {
                    "test_pass_rate": 0,
                    "execution_time_sec": None,
                    "line_coverage_percent": None,
                    "branch_coverage_percent": None,
                    "timeout": True,
                    "internal_error_occurred": False,
                    "runtime_errors_count": None
                }
            else:
                runtime_error_pattern = re.compile(
                    r"(panic:|fatal error:|runtime error:)", re.IGNORECASE
                )
                assertion_error_pattern = re.compile(r"assert")

                # Search for runtime errors in stdout and stderr
                for o in (output, stderr):
                    for line in o.splitlines():
                        # Match lines with runtime errors but exclude assertions
                        if runtime_error_pattern.search(line) and not assertion_error_pattern.search(line):
                            runtime_errors = runtime_errors + 1

        # Parse the JSON output
        passed_tests = 0
        failed_tests = 0
        total_time = 0.0
        coverage = None
        summary_time = None

        for line in output.splitlines():
            if not line.strip():
                continue
            try:
                data = json.loads(line)
            except json.JSONDecodeError:
                continue  # Skip lines that aren't valid JSON

            # Check for test results
            if data.get('Action') == 'pass' and 'Test' in data:
                passed_tests += 1
                total_time += data.get('Elapsed', 0)
            elif data.get('Action') == 'fail' and 'Test' in data:
                failed_tests += 1
                total_time += data.get('Elapsed', 0)

            if (data.get('Action') == 'pass' or data.get('Action') == 'fail') and 'Test' not in data and 'XXElapsedXX' in data:
                summary_time = data["Elapsed"]

            # Check for coverage information
            if data.get('Action') == 'output' and 'Output' in data:
                output_line = data['Output'].strip()
                if output_line.startswith("coverage:"):
                    # Extract the coverage percentage
                    match = re.search(r'coverage: ([\d\.]+)%', output_line)
                    if match:
                        coverage = float(match.group(1))

            # Alternative: Some versions include 'Coverage' field
            elif data.get('Action') == 'pass' and 'Coverage' in data:
                coverage = data['Coverage']

        total_tests = passed_tests + failed_tests
        pass_percentage = round((passed_tests / total_tests) * 100, 2) if total_tests > 0 else 0

        v = {
            "test_pass_rate": pass_percentage,
            "execution_time_sec": summary_time if summary_time is not None else total_time,
            "line_coverage_percent": coverage,
            "branch_coverage_percent": None,
            "timeout": False,
            "internal_error_occurred": False,
            "runtime_errors_count": runtime_errors
        }
        return v
    except Exception as e:
        print(f"Error running Go tests: {e}")
        return {
            "test_pass_rate": 0,
            "execution_time_sec": None,
            "line_coverage_percent": None,
            "branch_coverage_percent": None,
            "timeout": True,
            "internal_error_occurred": True,
            "runtime_errors_count": runtime_errors
        }


def x_run_tests__mutmut_103(project_dir):
    test_cmd = ["go", "test", "-timeout", "30s", "-json", "-cover", "./..."]
    runtime_errors = 0

    print("Running tests...")
    try:
        result = subprocess.run(
            test_cmd,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            cwd=project_dir,
            text=True
        )
        output = result.stdout
        print(output)
        stderr = result.stderr
        print(stderr)

        # Check if the subprocess exited with a non-zero return code
        if result.returncode != 0:
            # Parse output for timeout messages
            if "test timed out after" in output or "panic: test timed out" in output:
                print("Tests failed due to timeout")
                # Handle timeout scenario
                return {
                    "test_pass_rate": 0,
                    "execution_time_sec": None,
                    "line_coverage_percent": None,
                    "branch_coverage_percent": None,
                    "timeout": True,
                    "internal_error_occurred": False,
                    "runtime_errors_count": None
                }
            else:
                runtime_error_pattern = re.compile(
                    r"(panic:|fatal error:|runtime error:)", re.IGNORECASE
                )
                assertion_error_pattern = re.compile(r"assert")

                # Search for runtime errors in stdout and stderr
                for o in (output, stderr):
                    for line in o.splitlines():
                        # Match lines with runtime errors but exclude assertions
                        if runtime_error_pattern.search(line) and not assertion_error_pattern.search(line):
                            runtime_errors = runtime_errors + 1

        # Parse the JSON output
        passed_tests = 0
        failed_tests = 0
        total_time = 0.0
        coverage = None
        summary_time = None

        for line in output.splitlines():
            if not line.strip():
                continue
            try:
                data = json.loads(line)
            except json.JSONDecodeError:
                continue  # Skip lines that aren't valid JSON

            # Check for test results
            if data.get('Action') == 'pass' and 'Test' in data:
                passed_tests += 1
                total_time += data.get('Elapsed', 0)
            elif data.get('Action') == 'fail' and 'Test' in data:
                failed_tests += 1
                total_time += data.get('Elapsed', 0)

            if (data.get('Action') == 'pass' or data.get('Action') == 'fail') and 'Test' not in data and 'Elapsed' not in data:
                summary_time = data["Elapsed"]

            # Check for coverage information
            if data.get('Action') == 'output' and 'Output' in data:
                output_line = data['Output'].strip()
                if output_line.startswith("coverage:"):
                    # Extract the coverage percentage
                    match = re.search(r'coverage: ([\d\.]+)%', output_line)
                    if match:
                        coverage = float(match.group(1))

            # Alternative: Some versions include 'Coverage' field
            elif data.get('Action') == 'pass' and 'Coverage' in data:
                coverage = data['Coverage']

        total_tests = passed_tests + failed_tests
        pass_percentage = round((passed_tests / total_tests) * 100, 2) if total_tests > 0 else 0

        v = {
            "test_pass_rate": pass_percentage,
            "execution_time_sec": summary_time if summary_time is not None else total_time,
            "line_coverage_percent": coverage,
            "branch_coverage_percent": None,
            "timeout": False,
            "internal_error_occurred": False,
            "runtime_errors_count": runtime_errors
        }
        return v
    except Exception as e:
        print(f"Error running Go tests: {e}")
        return {
            "test_pass_rate": 0,
            "execution_time_sec": None,
            "line_coverage_percent": None,
            "branch_coverage_percent": None,
            "timeout": True,
            "internal_error_occurred": True,
            "runtime_errors_count": runtime_errors
        }


def x_run_tests__mutmut_104(project_dir):
    test_cmd = ["go", "test", "-timeout", "30s", "-json", "-cover", "./..."]
    runtime_errors = 0

    print("Running tests...")
    try:
        result = subprocess.run(
            test_cmd,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            cwd=project_dir,
            text=True
        )
        output = result.stdout
        print(output)
        stderr = result.stderr
        print(stderr)

        # Check if the subprocess exited with a non-zero return code
        if result.returncode != 0:
            # Parse output for timeout messages
            if "test timed out after" in output or "panic: test timed out" in output:
                print("Tests failed due to timeout")
                # Handle timeout scenario
                return {
                    "test_pass_rate": 0,
                    "execution_time_sec": None,
                    "line_coverage_percent": None,
                    "branch_coverage_percent": None,
                    "timeout": True,
                    "internal_error_occurred": False,
                    "runtime_errors_count": None
                }
            else:
                runtime_error_pattern = re.compile(
                    r"(panic:|fatal error:|runtime error:)", re.IGNORECASE
                )
                assertion_error_pattern = re.compile(r"assert")

                # Search for runtime errors in stdout and stderr
                for o in (output, stderr):
                    for line in o.splitlines():
                        # Match lines with runtime errors but exclude assertions
                        if runtime_error_pattern.search(line) and not assertion_error_pattern.search(line):
                            runtime_errors = runtime_errors + 1

        # Parse the JSON output
        passed_tests = 0
        failed_tests = 0
        total_time = 0.0
        coverage = None
        summary_time = None

        for line in output.splitlines():
            if not line.strip():
                continue
            try:
                data = json.loads(line)
            except json.JSONDecodeError:
                continue  # Skip lines that aren't valid JSON

            # Check for test results
            if data.get('Action') == 'pass' and 'Test' in data:
                passed_tests += 1
                total_time += data.get('Elapsed', 0)
            elif data.get('Action') == 'fail' and 'Test' in data:
                failed_tests += 1
                total_time += data.get('Elapsed', 0)

            if (data.get('Action') == 'pass' or data.get('Action') == 'fail') or 'Test' not in data and 'Elapsed' in data:
                summary_time = data["Elapsed"]

            # Check for coverage information
            if data.get('Action') == 'output' and 'Output' in data:
                output_line = data['Output'].strip()
                if output_line.startswith("coverage:"):
                    # Extract the coverage percentage
                    match = re.search(r'coverage: ([\d\.]+)%', output_line)
                    if match:
                        coverage = float(match.group(1))

            # Alternative: Some versions include 'Coverage' field
            elif data.get('Action') == 'pass' and 'Coverage' in data:
                coverage = data['Coverage']

        total_tests = passed_tests + failed_tests
        pass_percentage = round((passed_tests / total_tests) * 100, 2) if total_tests > 0 else 0

        v = {
            "test_pass_rate": pass_percentage,
            "execution_time_sec": summary_time if summary_time is not None else total_time,
            "line_coverage_percent": coverage,
            "branch_coverage_percent": None,
            "timeout": False,
            "internal_error_occurred": False,
            "runtime_errors_count": runtime_errors
        }
        return v
    except Exception as e:
        print(f"Error running Go tests: {e}")
        return {
            "test_pass_rate": 0,
            "execution_time_sec": None,
            "line_coverage_percent": None,
            "branch_coverage_percent": None,
            "timeout": True,
            "internal_error_occurred": True,
            "runtime_errors_count": runtime_errors
        }


def x_run_tests__mutmut_105(project_dir):
    test_cmd = ["go", "test", "-timeout", "30s", "-json", "-cover", "./..."]
    runtime_errors = 0

    print("Running tests...")
    try:
        result = subprocess.run(
            test_cmd,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            cwd=project_dir,
            text=True
        )
        output = result.stdout
        print(output)
        stderr = result.stderr
        print(stderr)

        # Check if the subprocess exited with a non-zero return code
        if result.returncode != 0:
            # Parse output for timeout messages
            if "test timed out after" in output or "panic: test timed out" in output:
                print("Tests failed due to timeout")
                # Handle timeout scenario
                return {
                    "test_pass_rate": 0,
                    "execution_time_sec": None,
                    "line_coverage_percent": None,
                    "branch_coverage_percent": None,
                    "timeout": True,
                    "internal_error_occurred": False,
                    "runtime_errors_count": None
                }
            else:
                runtime_error_pattern = re.compile(
                    r"(panic:|fatal error:|runtime error:)", re.IGNORECASE
                )
                assertion_error_pattern = re.compile(r"assert")

                # Search for runtime errors in stdout and stderr
                for o in (output, stderr):
                    for line in o.splitlines():
                        # Match lines with runtime errors but exclude assertions
                        if runtime_error_pattern.search(line) and not assertion_error_pattern.search(line):
                            runtime_errors = runtime_errors + 1

        # Parse the JSON output
        passed_tests = 0
        failed_tests = 0
        total_time = 0.0
        coverage = None
        summary_time = None

        for line in output.splitlines():
            if not line.strip():
                continue
            try:
                data = json.loads(line)
            except json.JSONDecodeError:
                continue  # Skip lines that aren't valid JSON

            # Check for test results
            if data.get('Action') == 'pass' and 'Test' in data:
                passed_tests += 1
                total_time += data.get('Elapsed', 0)
            elif data.get('Action') == 'fail' and 'Test' in data:
                failed_tests += 1
                total_time += data.get('Elapsed', 0)

            if (data.get('Action') == 'pass' or data.get('Action') == 'fail') and 'Test' not in data and 'Elapsed' in data:
                summary_time = data["XXElapsedXX"]

            # Check for coverage information
            if data.get('Action') == 'output' and 'Output' in data:
                output_line = data['Output'].strip()
                if output_line.startswith("coverage:"):
                    # Extract the coverage percentage
                    match = re.search(r'coverage: ([\d\.]+)%', output_line)
                    if match:
                        coverage = float(match.group(1))

            # Alternative: Some versions include 'Coverage' field
            elif data.get('Action') == 'pass' and 'Coverage' in data:
                coverage = data['Coverage']

        total_tests = passed_tests + failed_tests
        pass_percentage = round((passed_tests / total_tests) * 100, 2) if total_tests > 0 else 0

        v = {
            "test_pass_rate": pass_percentage,
            "execution_time_sec": summary_time if summary_time is not None else total_time,
            "line_coverage_percent": coverage,
            "branch_coverage_percent": None,
            "timeout": False,
            "internal_error_occurred": False,
            "runtime_errors_count": runtime_errors
        }
        return v
    except Exception as e:
        print(f"Error running Go tests: {e}")
        return {
            "test_pass_rate": 0,
            "execution_time_sec": None,
            "line_coverage_percent": None,
            "branch_coverage_percent": None,
            "timeout": True,
            "internal_error_occurred": True,
            "runtime_errors_count": runtime_errors
        }


def x_run_tests__mutmut_106(project_dir):
    test_cmd = ["go", "test", "-timeout", "30s", "-json", "-cover", "./..."]
    runtime_errors = 0

    print("Running tests...")
    try:
        result = subprocess.run(
            test_cmd,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            cwd=project_dir,
            text=True
        )
        output = result.stdout
        print(output)
        stderr = result.stderr
        print(stderr)

        # Check if the subprocess exited with a non-zero return code
        if result.returncode != 0:
            # Parse output for timeout messages
            if "test timed out after" in output or "panic: test timed out" in output:
                print("Tests failed due to timeout")
                # Handle timeout scenario
                return {
                    "test_pass_rate": 0,
                    "execution_time_sec": None,
                    "line_coverage_percent": None,
                    "branch_coverage_percent": None,
                    "timeout": True,
                    "internal_error_occurred": False,
                    "runtime_errors_count": None
                }
            else:
                runtime_error_pattern = re.compile(
                    r"(panic:|fatal error:|runtime error:)", re.IGNORECASE
                )
                assertion_error_pattern = re.compile(r"assert")

                # Search for runtime errors in stdout and stderr
                for o in (output, stderr):
                    for line in o.splitlines():
                        # Match lines with runtime errors but exclude assertions
                        if runtime_error_pattern.search(line) and not assertion_error_pattern.search(line):
                            runtime_errors = runtime_errors + 1

        # Parse the JSON output
        passed_tests = 0
        failed_tests = 0
        total_time = 0.0
        coverage = None
        summary_time = None

        for line in output.splitlines():
            if not line.strip():
                continue
            try:
                data = json.loads(line)
            except json.JSONDecodeError:
                continue  # Skip lines that aren't valid JSON

            # Check for test results
            if data.get('Action') == 'pass' and 'Test' in data:
                passed_tests += 1
                total_time += data.get('Elapsed', 0)
            elif data.get('Action') == 'fail' and 'Test' in data:
                failed_tests += 1
                total_time += data.get('Elapsed', 0)

            if (data.get('Action') == 'pass' or data.get('Action') == 'fail') and 'Test' not in data and 'Elapsed' in data:
                summary_time = data[None]

            # Check for coverage information
            if data.get('Action') == 'output' and 'Output' in data:
                output_line = data['Output'].strip()
                if output_line.startswith("coverage:"):
                    # Extract the coverage percentage
                    match = re.search(r'coverage: ([\d\.]+)%', output_line)
                    if match:
                        coverage = float(match.group(1))

            # Alternative: Some versions include 'Coverage' field
            elif data.get('Action') == 'pass' and 'Coverage' in data:
                coverage = data['Coverage']

        total_tests = passed_tests + failed_tests
        pass_percentage = round((passed_tests / total_tests) * 100, 2) if total_tests > 0 else 0

        v = {
            "test_pass_rate": pass_percentage,
            "execution_time_sec": summary_time if summary_time is not None else total_time,
            "line_coverage_percent": coverage,
            "branch_coverage_percent": None,
            "timeout": False,
            "internal_error_occurred": False,
            "runtime_errors_count": runtime_errors
        }
        return v
    except Exception as e:
        print(f"Error running Go tests: {e}")
        return {
            "test_pass_rate": 0,
            "execution_time_sec": None,
            "line_coverage_percent": None,
            "branch_coverage_percent": None,
            "timeout": True,
            "internal_error_occurred": True,
            "runtime_errors_count": runtime_errors
        }


def x_run_tests__mutmut_107(project_dir):
    test_cmd = ["go", "test", "-timeout", "30s", "-json", "-cover", "./..."]
    runtime_errors = 0

    print("Running tests...")
    try:
        result = subprocess.run(
            test_cmd,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            cwd=project_dir,
            text=True
        )
        output = result.stdout
        print(output)
        stderr = result.stderr
        print(stderr)

        # Check if the subprocess exited with a non-zero return code
        if result.returncode != 0:
            # Parse output for timeout messages
            if "test timed out after" in output or "panic: test timed out" in output:
                print("Tests failed due to timeout")
                # Handle timeout scenario
                return {
                    "test_pass_rate": 0,
                    "execution_time_sec": None,
                    "line_coverage_percent": None,
                    "branch_coverage_percent": None,
                    "timeout": True,
                    "internal_error_occurred": False,
                    "runtime_errors_count": None
                }
            else:
                runtime_error_pattern = re.compile(
                    r"(panic:|fatal error:|runtime error:)", re.IGNORECASE
                )
                assertion_error_pattern = re.compile(r"assert")

                # Search for runtime errors in stdout and stderr
                for o in (output, stderr):
                    for line in o.splitlines():
                        # Match lines with runtime errors but exclude assertions
                        if runtime_error_pattern.search(line) and not assertion_error_pattern.search(line):
                            runtime_errors = runtime_errors + 1

        # Parse the JSON output
        passed_tests = 0
        failed_tests = 0
        total_time = 0.0
        coverage = None
        summary_time = None

        for line in output.splitlines():
            if not line.strip():
                continue
            try:
                data = json.loads(line)
            except json.JSONDecodeError:
                continue  # Skip lines that aren't valid JSON

            # Check for test results
            if data.get('Action') == 'pass' and 'Test' in data:
                passed_tests += 1
                total_time += data.get('Elapsed', 0)
            elif data.get('Action') == 'fail' and 'Test' in data:
                failed_tests += 1
                total_time += data.get('Elapsed', 0)

            if (data.get('Action') == 'pass' or data.get('Action') == 'fail') and 'Test' not in data and 'Elapsed' in data:
                summary_time = None

            # Check for coverage information
            if data.get('Action') == 'output' and 'Output' in data:
                output_line = data['Output'].strip()
                if output_line.startswith("coverage:"):
                    # Extract the coverage percentage
                    match = re.search(r'coverage: ([\d\.]+)%', output_line)
                    if match:
                        coverage = float(match.group(1))

            # Alternative: Some versions include 'Coverage' field
            elif data.get('Action') == 'pass' and 'Coverage' in data:
                coverage = data['Coverage']

        total_tests = passed_tests + failed_tests
        pass_percentage = round((passed_tests / total_tests) * 100, 2) if total_tests > 0 else 0

        v = {
            "test_pass_rate": pass_percentage,
            "execution_time_sec": summary_time if summary_time is not None else total_time,
            "line_coverage_percent": coverage,
            "branch_coverage_percent": None,
            "timeout": False,
            "internal_error_occurred": False,
            "runtime_errors_count": runtime_errors
        }
        return v
    except Exception as e:
        print(f"Error running Go tests: {e}")
        return {
            "test_pass_rate": 0,
            "execution_time_sec": None,
            "line_coverage_percent": None,
            "branch_coverage_percent": None,
            "timeout": True,
            "internal_error_occurred": True,
            "runtime_errors_count": runtime_errors
        }


def x_run_tests__mutmut_108(project_dir):
    test_cmd = ["go", "test", "-timeout", "30s", "-json", "-cover", "./..."]
    runtime_errors = 0

    print("Running tests...")
    try:
        result = subprocess.run(
            test_cmd,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            cwd=project_dir,
            text=True
        )
        output = result.stdout
        print(output)
        stderr = result.stderr
        print(stderr)

        # Check if the subprocess exited with a non-zero return code
        if result.returncode != 0:
            # Parse output for timeout messages
            if "test timed out after" in output or "panic: test timed out" in output:
                print("Tests failed due to timeout")
                # Handle timeout scenario
                return {
                    "test_pass_rate": 0,
                    "execution_time_sec": None,
                    "line_coverage_percent": None,
                    "branch_coverage_percent": None,
                    "timeout": True,
                    "internal_error_occurred": False,
                    "runtime_errors_count": None
                }
            else:
                runtime_error_pattern = re.compile(
                    r"(panic:|fatal error:|runtime error:)", re.IGNORECASE
                )
                assertion_error_pattern = re.compile(r"assert")

                # Search for runtime errors in stdout and stderr
                for o in (output, stderr):
                    for line in o.splitlines():
                        # Match lines with runtime errors but exclude assertions
                        if runtime_error_pattern.search(line) and not assertion_error_pattern.search(line):
                            runtime_errors = runtime_errors + 1

        # Parse the JSON output
        passed_tests = 0
        failed_tests = 0
        total_time = 0.0
        coverage = None
        summary_time = None

        for line in output.splitlines():
            if not line.strip():
                continue
            try:
                data = json.loads(line)
            except json.JSONDecodeError:
                continue  # Skip lines that aren't valid JSON

            # Check for test results
            if data.get('Action') == 'pass' and 'Test' in data:
                passed_tests += 1
                total_time += data.get('Elapsed', 0)
            elif data.get('Action') == 'fail' and 'Test' in data:
                failed_tests += 1
                total_time += data.get('Elapsed', 0)

            if (data.get('Action') == 'pass' or data.get('Action') == 'fail') and 'Test' not in data and 'Elapsed' in data:
                summary_time = data["Elapsed"]

            # Check for coverage information
            if data.get('XXActionXX') == 'output' and 'Output' in data:
                output_line = data['Output'].strip()
                if output_line.startswith("coverage:"):
                    # Extract the coverage percentage
                    match = re.search(r'coverage: ([\d\.]+)%', output_line)
                    if match:
                        coverage = float(match.group(1))

            # Alternative: Some versions include 'Coverage' field
            elif data.get('Action') == 'pass' and 'Coverage' in data:
                coverage = data['Coverage']

        total_tests = passed_tests + failed_tests
        pass_percentage = round((passed_tests / total_tests) * 100, 2) if total_tests > 0 else 0

        v = {
            "test_pass_rate": pass_percentage,
            "execution_time_sec": summary_time if summary_time is not None else total_time,
            "line_coverage_percent": coverage,
            "branch_coverage_percent": None,
            "timeout": False,
            "internal_error_occurred": False,
            "runtime_errors_count": runtime_errors
        }
        return v
    except Exception as e:
        print(f"Error running Go tests: {e}")
        return {
            "test_pass_rate": 0,
            "execution_time_sec": None,
            "line_coverage_percent": None,
            "branch_coverage_percent": None,
            "timeout": True,
            "internal_error_occurred": True,
            "runtime_errors_count": runtime_errors
        }


def x_run_tests__mutmut_109(project_dir):
    test_cmd = ["go", "test", "-timeout", "30s", "-json", "-cover", "./..."]
    runtime_errors = 0

    print("Running tests...")
    try:
        result = subprocess.run(
            test_cmd,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            cwd=project_dir,
            text=True
        )
        output = result.stdout
        print(output)
        stderr = result.stderr
        print(stderr)

        # Check if the subprocess exited with a non-zero return code
        if result.returncode != 0:
            # Parse output for timeout messages
            if "test timed out after" in output or "panic: test timed out" in output:
                print("Tests failed due to timeout")
                # Handle timeout scenario
                return {
                    "test_pass_rate": 0,
                    "execution_time_sec": None,
                    "line_coverage_percent": None,
                    "branch_coverage_percent": None,
                    "timeout": True,
                    "internal_error_occurred": False,
                    "runtime_errors_count": None
                }
            else:
                runtime_error_pattern = re.compile(
                    r"(panic:|fatal error:|runtime error:)", re.IGNORECASE
                )
                assertion_error_pattern = re.compile(r"assert")

                # Search for runtime errors in stdout and stderr
                for o in (output, stderr):
                    for line in o.splitlines():
                        # Match lines with runtime errors but exclude assertions
                        if runtime_error_pattern.search(line) and not assertion_error_pattern.search(line):
                            runtime_errors = runtime_errors + 1

        # Parse the JSON output
        passed_tests = 0
        failed_tests = 0
        total_time = 0.0
        coverage = None
        summary_time = None

        for line in output.splitlines():
            if not line.strip():
                continue
            try:
                data = json.loads(line)
            except json.JSONDecodeError:
                continue  # Skip lines that aren't valid JSON

            # Check for test results
            if data.get('Action') == 'pass' and 'Test' in data:
                passed_tests += 1
                total_time += data.get('Elapsed', 0)
            elif data.get('Action') == 'fail' and 'Test' in data:
                failed_tests += 1
                total_time += data.get('Elapsed', 0)

            if (data.get('Action') == 'pass' or data.get('Action') == 'fail') and 'Test' not in data and 'Elapsed' in data:
                summary_time = data["Elapsed"]

            # Check for coverage information
            if data.get('Action') != 'output' and 'Output' in data:
                output_line = data['Output'].strip()
                if output_line.startswith("coverage:"):
                    # Extract the coverage percentage
                    match = re.search(r'coverage: ([\d\.]+)%', output_line)
                    if match:
                        coverage = float(match.group(1))

            # Alternative: Some versions include 'Coverage' field
            elif data.get('Action') == 'pass' and 'Coverage' in data:
                coverage = data['Coverage']

        total_tests = passed_tests + failed_tests
        pass_percentage = round((passed_tests / total_tests) * 100, 2) if total_tests > 0 else 0

        v = {
            "test_pass_rate": pass_percentage,
            "execution_time_sec": summary_time if summary_time is not None else total_time,
            "line_coverage_percent": coverage,
            "branch_coverage_percent": None,
            "timeout": False,
            "internal_error_occurred": False,
            "runtime_errors_count": runtime_errors
        }
        return v
    except Exception as e:
        print(f"Error running Go tests: {e}")
        return {
            "test_pass_rate": 0,
            "execution_time_sec": None,
            "line_coverage_percent": None,
            "branch_coverage_percent": None,
            "timeout": True,
            "internal_error_occurred": True,
            "runtime_errors_count": runtime_errors
        }


def x_run_tests__mutmut_110(project_dir):
    test_cmd = ["go", "test", "-timeout", "30s", "-json", "-cover", "./..."]
    runtime_errors = 0

    print("Running tests...")
    try:
        result = subprocess.run(
            test_cmd,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            cwd=project_dir,
            text=True
        )
        output = result.stdout
        print(output)
        stderr = result.stderr
        print(stderr)

        # Check if the subprocess exited with a non-zero return code
        if result.returncode != 0:
            # Parse output for timeout messages
            if "test timed out after" in output or "panic: test timed out" in output:
                print("Tests failed due to timeout")
                # Handle timeout scenario
                return {
                    "test_pass_rate": 0,
                    "execution_time_sec": None,
                    "line_coverage_percent": None,
                    "branch_coverage_percent": None,
                    "timeout": True,
                    "internal_error_occurred": False,
                    "runtime_errors_count": None
                }
            else:
                runtime_error_pattern = re.compile(
                    r"(panic:|fatal error:|runtime error:)", re.IGNORECASE
                )
                assertion_error_pattern = re.compile(r"assert")

                # Search for runtime errors in stdout and stderr
                for o in (output, stderr):
                    for line in o.splitlines():
                        # Match lines with runtime errors but exclude assertions
                        if runtime_error_pattern.search(line) and not assertion_error_pattern.search(line):
                            runtime_errors = runtime_errors + 1

        # Parse the JSON output
        passed_tests = 0
        failed_tests = 0
        total_time = 0.0
        coverage = None
        summary_time = None

        for line in output.splitlines():
            if not line.strip():
                continue
            try:
                data = json.loads(line)
            except json.JSONDecodeError:
                continue  # Skip lines that aren't valid JSON

            # Check for test results
            if data.get('Action') == 'pass' and 'Test' in data:
                passed_tests += 1
                total_time += data.get('Elapsed', 0)
            elif data.get('Action') == 'fail' and 'Test' in data:
                failed_tests += 1
                total_time += data.get('Elapsed', 0)

            if (data.get('Action') == 'pass' or data.get('Action') == 'fail') and 'Test' not in data and 'Elapsed' in data:
                summary_time = data["Elapsed"]

            # Check for coverage information
            if data.get('Action') == 'XXoutputXX' and 'Output' in data:
                output_line = data['Output'].strip()
                if output_line.startswith("coverage:"):
                    # Extract the coverage percentage
                    match = re.search(r'coverage: ([\d\.]+)%', output_line)
                    if match:
                        coverage = float(match.group(1))

            # Alternative: Some versions include 'Coverage' field
            elif data.get('Action') == 'pass' and 'Coverage' in data:
                coverage = data['Coverage']

        total_tests = passed_tests + failed_tests
        pass_percentage = round((passed_tests / total_tests) * 100, 2) if total_tests > 0 else 0

        v = {
            "test_pass_rate": pass_percentage,
            "execution_time_sec": summary_time if summary_time is not None else total_time,
            "line_coverage_percent": coverage,
            "branch_coverage_percent": None,
            "timeout": False,
            "internal_error_occurred": False,
            "runtime_errors_count": runtime_errors
        }
        return v
    except Exception as e:
        print(f"Error running Go tests: {e}")
        return {
            "test_pass_rate": 0,
            "execution_time_sec": None,
            "line_coverage_percent": None,
            "branch_coverage_percent": None,
            "timeout": True,
            "internal_error_occurred": True,
            "runtime_errors_count": runtime_errors
        }


def x_run_tests__mutmut_111(project_dir):
    test_cmd = ["go", "test", "-timeout", "30s", "-json", "-cover", "./..."]
    runtime_errors = 0

    print("Running tests...")
    try:
        result = subprocess.run(
            test_cmd,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            cwd=project_dir,
            text=True
        )
        output = result.stdout
        print(output)
        stderr = result.stderr
        print(stderr)

        # Check if the subprocess exited with a non-zero return code
        if result.returncode != 0:
            # Parse output for timeout messages
            if "test timed out after" in output or "panic: test timed out" in output:
                print("Tests failed due to timeout")
                # Handle timeout scenario
                return {
                    "test_pass_rate": 0,
                    "execution_time_sec": None,
                    "line_coverage_percent": None,
                    "branch_coverage_percent": None,
                    "timeout": True,
                    "internal_error_occurred": False,
                    "runtime_errors_count": None
                }
            else:
                runtime_error_pattern = re.compile(
                    r"(panic:|fatal error:|runtime error:)", re.IGNORECASE
                )
                assertion_error_pattern = re.compile(r"assert")

                # Search for runtime errors in stdout and stderr
                for o in (output, stderr):
                    for line in o.splitlines():
                        # Match lines with runtime errors but exclude assertions
                        if runtime_error_pattern.search(line) and not assertion_error_pattern.search(line):
                            runtime_errors = runtime_errors + 1

        # Parse the JSON output
        passed_tests = 0
        failed_tests = 0
        total_time = 0.0
        coverage = None
        summary_time = None

        for line in output.splitlines():
            if not line.strip():
                continue
            try:
                data = json.loads(line)
            except json.JSONDecodeError:
                continue  # Skip lines that aren't valid JSON

            # Check for test results
            if data.get('Action') == 'pass' and 'Test' in data:
                passed_tests += 1
                total_time += data.get('Elapsed', 0)
            elif data.get('Action') == 'fail' and 'Test' in data:
                failed_tests += 1
                total_time += data.get('Elapsed', 0)

            if (data.get('Action') == 'pass' or data.get('Action') == 'fail') and 'Test' not in data and 'Elapsed' in data:
                summary_time = data["Elapsed"]

            # Check for coverage information
            if data.get('Action') == 'output' and 'XXOutputXX' in data:
                output_line = data['Output'].strip()
                if output_line.startswith("coverage:"):
                    # Extract the coverage percentage
                    match = re.search(r'coverage: ([\d\.]+)%', output_line)
                    if match:
                        coverage = float(match.group(1))

            # Alternative: Some versions include 'Coverage' field
            elif data.get('Action') == 'pass' and 'Coverage' in data:
                coverage = data['Coverage']

        total_tests = passed_tests + failed_tests
        pass_percentage = round((passed_tests / total_tests) * 100, 2) if total_tests > 0 else 0

        v = {
            "test_pass_rate": pass_percentage,
            "execution_time_sec": summary_time if summary_time is not None else total_time,
            "line_coverage_percent": coverage,
            "branch_coverage_percent": None,
            "timeout": False,
            "internal_error_occurred": False,
            "runtime_errors_count": runtime_errors
        }
        return v
    except Exception as e:
        print(f"Error running Go tests: {e}")
        return {
            "test_pass_rate": 0,
            "execution_time_sec": None,
            "line_coverage_percent": None,
            "branch_coverage_percent": None,
            "timeout": True,
            "internal_error_occurred": True,
            "runtime_errors_count": runtime_errors
        }


def x_run_tests__mutmut_112(project_dir):
    test_cmd = ["go", "test", "-timeout", "30s", "-json", "-cover", "./..."]
    runtime_errors = 0

    print("Running tests...")
    try:
        result = subprocess.run(
            test_cmd,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            cwd=project_dir,
            text=True
        )
        output = result.stdout
        print(output)
        stderr = result.stderr
        print(stderr)

        # Check if the subprocess exited with a non-zero return code
        if result.returncode != 0:
            # Parse output for timeout messages
            if "test timed out after" in output or "panic: test timed out" in output:
                print("Tests failed due to timeout")
                # Handle timeout scenario
                return {
                    "test_pass_rate": 0,
                    "execution_time_sec": None,
                    "line_coverage_percent": None,
                    "branch_coverage_percent": None,
                    "timeout": True,
                    "internal_error_occurred": False,
                    "runtime_errors_count": None
                }
            else:
                runtime_error_pattern = re.compile(
                    r"(panic:|fatal error:|runtime error:)", re.IGNORECASE
                )
                assertion_error_pattern = re.compile(r"assert")

                # Search for runtime errors in stdout and stderr
                for o in (output, stderr):
                    for line in o.splitlines():
                        # Match lines with runtime errors but exclude assertions
                        if runtime_error_pattern.search(line) and not assertion_error_pattern.search(line):
                            runtime_errors = runtime_errors + 1

        # Parse the JSON output
        passed_tests = 0
        failed_tests = 0
        total_time = 0.0
        coverage = None
        summary_time = None

        for line in output.splitlines():
            if not line.strip():
                continue
            try:
                data = json.loads(line)
            except json.JSONDecodeError:
                continue  # Skip lines that aren't valid JSON

            # Check for test results
            if data.get('Action') == 'pass' and 'Test' in data:
                passed_tests += 1
                total_time += data.get('Elapsed', 0)
            elif data.get('Action') == 'fail' and 'Test' in data:
                failed_tests += 1
                total_time += data.get('Elapsed', 0)

            if (data.get('Action') == 'pass' or data.get('Action') == 'fail') and 'Test' not in data and 'Elapsed' in data:
                summary_time = data["Elapsed"]

            # Check for coverage information
            if data.get('Action') == 'output' and 'Output' not in data:
                output_line = data['Output'].strip()
                if output_line.startswith("coverage:"):
                    # Extract the coverage percentage
                    match = re.search(r'coverage: ([\d\.]+)%', output_line)
                    if match:
                        coverage = float(match.group(1))

            # Alternative: Some versions include 'Coverage' field
            elif data.get('Action') == 'pass' and 'Coverage' in data:
                coverage = data['Coverage']

        total_tests = passed_tests + failed_tests
        pass_percentage = round((passed_tests / total_tests) * 100, 2) if total_tests > 0 else 0

        v = {
            "test_pass_rate": pass_percentage,
            "execution_time_sec": summary_time if summary_time is not None else total_time,
            "line_coverage_percent": coverage,
            "branch_coverage_percent": None,
            "timeout": False,
            "internal_error_occurred": False,
            "runtime_errors_count": runtime_errors
        }
        return v
    except Exception as e:
        print(f"Error running Go tests: {e}")
        return {
            "test_pass_rate": 0,
            "execution_time_sec": None,
            "line_coverage_percent": None,
            "branch_coverage_percent": None,
            "timeout": True,
            "internal_error_occurred": True,
            "runtime_errors_count": runtime_errors
        }


def x_run_tests__mutmut_113(project_dir):
    test_cmd = ["go", "test", "-timeout", "30s", "-json", "-cover", "./..."]
    runtime_errors = 0

    print("Running tests...")
    try:
        result = subprocess.run(
            test_cmd,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            cwd=project_dir,
            text=True
        )
        output = result.stdout
        print(output)
        stderr = result.stderr
        print(stderr)

        # Check if the subprocess exited with a non-zero return code
        if result.returncode != 0:
            # Parse output for timeout messages
            if "test timed out after" in output or "panic: test timed out" in output:
                print("Tests failed due to timeout")
                # Handle timeout scenario
                return {
                    "test_pass_rate": 0,
                    "execution_time_sec": None,
                    "line_coverage_percent": None,
                    "branch_coverage_percent": None,
                    "timeout": True,
                    "internal_error_occurred": False,
                    "runtime_errors_count": None
                }
            else:
                runtime_error_pattern = re.compile(
                    r"(panic:|fatal error:|runtime error:)", re.IGNORECASE
                )
                assertion_error_pattern = re.compile(r"assert")

                # Search for runtime errors in stdout and stderr
                for o in (output, stderr):
                    for line in o.splitlines():
                        # Match lines with runtime errors but exclude assertions
                        if runtime_error_pattern.search(line) and not assertion_error_pattern.search(line):
                            runtime_errors = runtime_errors + 1

        # Parse the JSON output
        passed_tests = 0
        failed_tests = 0
        total_time = 0.0
        coverage = None
        summary_time = None

        for line in output.splitlines():
            if not line.strip():
                continue
            try:
                data = json.loads(line)
            except json.JSONDecodeError:
                continue  # Skip lines that aren't valid JSON

            # Check for test results
            if data.get('Action') == 'pass' and 'Test' in data:
                passed_tests += 1
                total_time += data.get('Elapsed', 0)
            elif data.get('Action') == 'fail' and 'Test' in data:
                failed_tests += 1
                total_time += data.get('Elapsed', 0)

            if (data.get('Action') == 'pass' or data.get('Action') == 'fail') and 'Test' not in data and 'Elapsed' in data:
                summary_time = data["Elapsed"]

            # Check for coverage information
            if data.get('Action') == 'output' or 'Output' in data:
                output_line = data['Output'].strip()
                if output_line.startswith("coverage:"):
                    # Extract the coverage percentage
                    match = re.search(r'coverage: ([\d\.]+)%', output_line)
                    if match:
                        coverage = float(match.group(1))

            # Alternative: Some versions include 'Coverage' field
            elif data.get('Action') == 'pass' and 'Coverage' in data:
                coverage = data['Coverage']

        total_tests = passed_tests + failed_tests
        pass_percentage = round((passed_tests / total_tests) * 100, 2) if total_tests > 0 else 0

        v = {
            "test_pass_rate": pass_percentage,
            "execution_time_sec": summary_time if summary_time is not None else total_time,
            "line_coverage_percent": coverage,
            "branch_coverage_percent": None,
            "timeout": False,
            "internal_error_occurred": False,
            "runtime_errors_count": runtime_errors
        }
        return v
    except Exception as e:
        print(f"Error running Go tests: {e}")
        return {
            "test_pass_rate": 0,
            "execution_time_sec": None,
            "line_coverage_percent": None,
            "branch_coverage_percent": None,
            "timeout": True,
            "internal_error_occurred": True,
            "runtime_errors_count": runtime_errors
        }


def x_run_tests__mutmut_114(project_dir):
    test_cmd = ["go", "test", "-timeout", "30s", "-json", "-cover", "./..."]
    runtime_errors = 0

    print("Running tests...")
    try:
        result = subprocess.run(
            test_cmd,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            cwd=project_dir,
            text=True
        )
        output = result.stdout
        print(output)
        stderr = result.stderr
        print(stderr)

        # Check if the subprocess exited with a non-zero return code
        if result.returncode != 0:
            # Parse output for timeout messages
            if "test timed out after" in output or "panic: test timed out" in output:
                print("Tests failed due to timeout")
                # Handle timeout scenario
                return {
                    "test_pass_rate": 0,
                    "execution_time_sec": None,
                    "line_coverage_percent": None,
                    "branch_coverage_percent": None,
                    "timeout": True,
                    "internal_error_occurred": False,
                    "runtime_errors_count": None
                }
            else:
                runtime_error_pattern = re.compile(
                    r"(panic:|fatal error:|runtime error:)", re.IGNORECASE
                )
                assertion_error_pattern = re.compile(r"assert")

                # Search for runtime errors in stdout and stderr
                for o in (output, stderr):
                    for line in o.splitlines():
                        # Match lines with runtime errors but exclude assertions
                        if runtime_error_pattern.search(line) and not assertion_error_pattern.search(line):
                            runtime_errors = runtime_errors + 1

        # Parse the JSON output
        passed_tests = 0
        failed_tests = 0
        total_time = 0.0
        coverage = None
        summary_time = None

        for line in output.splitlines():
            if not line.strip():
                continue
            try:
                data = json.loads(line)
            except json.JSONDecodeError:
                continue  # Skip lines that aren't valid JSON

            # Check for test results
            if data.get('Action') == 'pass' and 'Test' in data:
                passed_tests += 1
                total_time += data.get('Elapsed', 0)
            elif data.get('Action') == 'fail' and 'Test' in data:
                failed_tests += 1
                total_time += data.get('Elapsed', 0)

            if (data.get('Action') == 'pass' or data.get('Action') == 'fail') and 'Test' not in data and 'Elapsed' in data:
                summary_time = data["Elapsed"]

            # Check for coverage information
            if data.get('Action') == 'output' and 'Output' in data:
                output_line = data['XXOutputXX'].strip()
                if output_line.startswith("coverage:"):
                    # Extract the coverage percentage
                    match = re.search(r'coverage: ([\d\.]+)%', output_line)
                    if match:
                        coverage = float(match.group(1))

            # Alternative: Some versions include 'Coverage' field
            elif data.get('Action') == 'pass' and 'Coverage' in data:
                coverage = data['Coverage']

        total_tests = passed_tests + failed_tests
        pass_percentage = round((passed_tests / total_tests) * 100, 2) if total_tests > 0 else 0

        v = {
            "test_pass_rate": pass_percentage,
            "execution_time_sec": summary_time if summary_time is not None else total_time,
            "line_coverage_percent": coverage,
            "branch_coverage_percent": None,
            "timeout": False,
            "internal_error_occurred": False,
            "runtime_errors_count": runtime_errors
        }
        return v
    except Exception as e:
        print(f"Error running Go tests: {e}")
        return {
            "test_pass_rate": 0,
            "execution_time_sec": None,
            "line_coverage_percent": None,
            "branch_coverage_percent": None,
            "timeout": True,
            "internal_error_occurred": True,
            "runtime_errors_count": runtime_errors
        }


def x_run_tests__mutmut_115(project_dir):
    test_cmd = ["go", "test", "-timeout", "30s", "-json", "-cover", "./..."]
    runtime_errors = 0

    print("Running tests...")
    try:
        result = subprocess.run(
            test_cmd,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            cwd=project_dir,
            text=True
        )
        output = result.stdout
        print(output)
        stderr = result.stderr
        print(stderr)

        # Check if the subprocess exited with a non-zero return code
        if result.returncode != 0:
            # Parse output for timeout messages
            if "test timed out after" in output or "panic: test timed out" in output:
                print("Tests failed due to timeout")
                # Handle timeout scenario
                return {
                    "test_pass_rate": 0,
                    "execution_time_sec": None,
                    "line_coverage_percent": None,
                    "branch_coverage_percent": None,
                    "timeout": True,
                    "internal_error_occurred": False,
                    "runtime_errors_count": None
                }
            else:
                runtime_error_pattern = re.compile(
                    r"(panic:|fatal error:|runtime error:)", re.IGNORECASE
                )
                assertion_error_pattern = re.compile(r"assert")

                # Search for runtime errors in stdout and stderr
                for o in (output, stderr):
                    for line in o.splitlines():
                        # Match lines with runtime errors but exclude assertions
                        if runtime_error_pattern.search(line) and not assertion_error_pattern.search(line):
                            runtime_errors = runtime_errors + 1

        # Parse the JSON output
        passed_tests = 0
        failed_tests = 0
        total_time = 0.0
        coverage = None
        summary_time = None

        for line in output.splitlines():
            if not line.strip():
                continue
            try:
                data = json.loads(line)
            except json.JSONDecodeError:
                continue  # Skip lines that aren't valid JSON

            # Check for test results
            if data.get('Action') == 'pass' and 'Test' in data:
                passed_tests += 1
                total_time += data.get('Elapsed', 0)
            elif data.get('Action') == 'fail' and 'Test' in data:
                failed_tests += 1
                total_time += data.get('Elapsed', 0)

            if (data.get('Action') == 'pass' or data.get('Action') == 'fail') and 'Test' not in data and 'Elapsed' in data:
                summary_time = data["Elapsed"]

            # Check for coverage information
            if data.get('Action') == 'output' and 'Output' in data:
                output_line = data[None].strip()
                if output_line.startswith("coverage:"):
                    # Extract the coverage percentage
                    match = re.search(r'coverage: ([\d\.]+)%', output_line)
                    if match:
                        coverage = float(match.group(1))

            # Alternative: Some versions include 'Coverage' field
            elif data.get('Action') == 'pass' and 'Coverage' in data:
                coverage = data['Coverage']

        total_tests = passed_tests + failed_tests
        pass_percentage = round((passed_tests / total_tests) * 100, 2) if total_tests > 0 else 0

        v = {
            "test_pass_rate": pass_percentage,
            "execution_time_sec": summary_time if summary_time is not None else total_time,
            "line_coverage_percent": coverage,
            "branch_coverage_percent": None,
            "timeout": False,
            "internal_error_occurred": False,
            "runtime_errors_count": runtime_errors
        }
        return v
    except Exception as e:
        print(f"Error running Go tests: {e}")
        return {
            "test_pass_rate": 0,
            "execution_time_sec": None,
            "line_coverage_percent": None,
            "branch_coverage_percent": None,
            "timeout": True,
            "internal_error_occurred": True,
            "runtime_errors_count": runtime_errors
        }


def x_run_tests__mutmut_116(project_dir):
    test_cmd = ["go", "test", "-timeout", "30s", "-json", "-cover", "./..."]
    runtime_errors = 0

    print("Running tests...")
    try:
        result = subprocess.run(
            test_cmd,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            cwd=project_dir,
            text=True
        )
        output = result.stdout
        print(output)
        stderr = result.stderr
        print(stderr)

        # Check if the subprocess exited with a non-zero return code
        if result.returncode != 0:
            # Parse output for timeout messages
            if "test timed out after" in output or "panic: test timed out" in output:
                print("Tests failed due to timeout")
                # Handle timeout scenario
                return {
                    "test_pass_rate": 0,
                    "execution_time_sec": None,
                    "line_coverage_percent": None,
                    "branch_coverage_percent": None,
                    "timeout": True,
                    "internal_error_occurred": False,
                    "runtime_errors_count": None
                }
            else:
                runtime_error_pattern = re.compile(
                    r"(panic:|fatal error:|runtime error:)", re.IGNORECASE
                )
                assertion_error_pattern = re.compile(r"assert")

                # Search for runtime errors in stdout and stderr
                for o in (output, stderr):
                    for line in o.splitlines():
                        # Match lines with runtime errors but exclude assertions
                        if runtime_error_pattern.search(line) and not assertion_error_pattern.search(line):
                            runtime_errors = runtime_errors + 1

        # Parse the JSON output
        passed_tests = 0
        failed_tests = 0
        total_time = 0.0
        coverage = None
        summary_time = None

        for line in output.splitlines():
            if not line.strip():
                continue
            try:
                data = json.loads(line)
            except json.JSONDecodeError:
                continue  # Skip lines that aren't valid JSON

            # Check for test results
            if data.get('Action') == 'pass' and 'Test' in data:
                passed_tests += 1
                total_time += data.get('Elapsed', 0)
            elif data.get('Action') == 'fail' and 'Test' in data:
                failed_tests += 1
                total_time += data.get('Elapsed', 0)

            if (data.get('Action') == 'pass' or data.get('Action') == 'fail') and 'Test' not in data and 'Elapsed' in data:
                summary_time = data["Elapsed"]

            # Check for coverage information
            if data.get('Action') == 'output' and 'Output' in data:
                output_line = None
                if output_line.startswith("coverage:"):
                    # Extract the coverage percentage
                    match = re.search(r'coverage: ([\d\.]+)%', output_line)
                    if match:
                        coverage = float(match.group(1))

            # Alternative: Some versions include 'Coverage' field
            elif data.get('Action') == 'pass' and 'Coverage' in data:
                coverage = data['Coverage']

        total_tests = passed_tests + failed_tests
        pass_percentage = round((passed_tests / total_tests) * 100, 2) if total_tests > 0 else 0

        v = {
            "test_pass_rate": pass_percentage,
            "execution_time_sec": summary_time if summary_time is not None else total_time,
            "line_coverage_percent": coverage,
            "branch_coverage_percent": None,
            "timeout": False,
            "internal_error_occurred": False,
            "runtime_errors_count": runtime_errors
        }
        return v
    except Exception as e:
        print(f"Error running Go tests: {e}")
        return {
            "test_pass_rate": 0,
            "execution_time_sec": None,
            "line_coverage_percent": None,
            "branch_coverage_percent": None,
            "timeout": True,
            "internal_error_occurred": True,
            "runtime_errors_count": runtime_errors
        }


def x_run_tests__mutmut_117(project_dir):
    test_cmd = ["go", "test", "-timeout", "30s", "-json", "-cover", "./..."]
    runtime_errors = 0

    print("Running tests...")
    try:
        result = subprocess.run(
            test_cmd,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            cwd=project_dir,
            text=True
        )
        output = result.stdout
        print(output)
        stderr = result.stderr
        print(stderr)

        # Check if the subprocess exited with a non-zero return code
        if result.returncode != 0:
            # Parse output for timeout messages
            if "test timed out after" in output or "panic: test timed out" in output:
                print("Tests failed due to timeout")
                # Handle timeout scenario
                return {
                    "test_pass_rate": 0,
                    "execution_time_sec": None,
                    "line_coverage_percent": None,
                    "branch_coverage_percent": None,
                    "timeout": True,
                    "internal_error_occurred": False,
                    "runtime_errors_count": None
                }
            else:
                runtime_error_pattern = re.compile(
                    r"(panic:|fatal error:|runtime error:)", re.IGNORECASE
                )
                assertion_error_pattern = re.compile(r"assert")

                # Search for runtime errors in stdout and stderr
                for o in (output, stderr):
                    for line in o.splitlines():
                        # Match lines with runtime errors but exclude assertions
                        if runtime_error_pattern.search(line) and not assertion_error_pattern.search(line):
                            runtime_errors = runtime_errors + 1

        # Parse the JSON output
        passed_tests = 0
        failed_tests = 0
        total_time = 0.0
        coverage = None
        summary_time = None

        for line in output.splitlines():
            if not line.strip():
                continue
            try:
                data = json.loads(line)
            except json.JSONDecodeError:
                continue  # Skip lines that aren't valid JSON

            # Check for test results
            if data.get('Action') == 'pass' and 'Test' in data:
                passed_tests += 1
                total_time += data.get('Elapsed', 0)
            elif data.get('Action') == 'fail' and 'Test' in data:
                failed_tests += 1
                total_time += data.get('Elapsed', 0)

            if (data.get('Action') == 'pass' or data.get('Action') == 'fail') and 'Test' not in data and 'Elapsed' in data:
                summary_time = data["Elapsed"]

            # Check for coverage information
            if data.get('Action') == 'output' and 'Output' in data:
                output_line = data['Output'].strip()
                if output_line.startswith("XXcoverage:XX"):
                    # Extract the coverage percentage
                    match = re.search(r'coverage: ([\d\.]+)%', output_line)
                    if match:
                        coverage = float(match.group(1))

            # Alternative: Some versions include 'Coverage' field
            elif data.get('Action') == 'pass' and 'Coverage' in data:
                coverage = data['Coverage']

        total_tests = passed_tests + failed_tests
        pass_percentage = round((passed_tests / total_tests) * 100, 2) if total_tests > 0 else 0

        v = {
            "test_pass_rate": pass_percentage,
            "execution_time_sec": summary_time if summary_time is not None else total_time,
            "line_coverage_percent": coverage,
            "branch_coverage_percent": None,
            "timeout": False,
            "internal_error_occurred": False,
            "runtime_errors_count": runtime_errors
        }
        return v
    except Exception as e:
        print(f"Error running Go tests: {e}")
        return {
            "test_pass_rate": 0,
            "execution_time_sec": None,
            "line_coverage_percent": None,
            "branch_coverage_percent": None,
            "timeout": True,
            "internal_error_occurred": True,
            "runtime_errors_count": runtime_errors
        }


def x_run_tests__mutmut_118(project_dir):
    test_cmd = ["go", "test", "-timeout", "30s", "-json", "-cover", "./..."]
    runtime_errors = 0

    print("Running tests...")
    try:
        result = subprocess.run(
            test_cmd,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            cwd=project_dir,
            text=True
        )
        output = result.stdout
        print(output)
        stderr = result.stderr
        print(stderr)

        # Check if the subprocess exited with a non-zero return code
        if result.returncode != 0:
            # Parse output for timeout messages
            if "test timed out after" in output or "panic: test timed out" in output:
                print("Tests failed due to timeout")
                # Handle timeout scenario
                return {
                    "test_pass_rate": 0,
                    "execution_time_sec": None,
                    "line_coverage_percent": None,
                    "branch_coverage_percent": None,
                    "timeout": True,
                    "internal_error_occurred": False,
                    "runtime_errors_count": None
                }
            else:
                runtime_error_pattern = re.compile(
                    r"(panic:|fatal error:|runtime error:)", re.IGNORECASE
                )
                assertion_error_pattern = re.compile(r"assert")

                # Search for runtime errors in stdout and stderr
                for o in (output, stderr):
                    for line in o.splitlines():
                        # Match lines with runtime errors but exclude assertions
                        if runtime_error_pattern.search(line) and not assertion_error_pattern.search(line):
                            runtime_errors = runtime_errors + 1

        # Parse the JSON output
        passed_tests = 0
        failed_tests = 0
        total_time = 0.0
        coverage = None
        summary_time = None

        for line in output.splitlines():
            if not line.strip():
                continue
            try:
                data = json.loads(line)
            except json.JSONDecodeError:
                continue  # Skip lines that aren't valid JSON

            # Check for test results
            if data.get('Action') == 'pass' and 'Test' in data:
                passed_tests += 1
                total_time += data.get('Elapsed', 0)
            elif data.get('Action') == 'fail' and 'Test' in data:
                failed_tests += 1
                total_time += data.get('Elapsed', 0)

            if (data.get('Action') == 'pass' or data.get('Action') == 'fail') and 'Test' not in data and 'Elapsed' in data:
                summary_time = data["Elapsed"]

            # Check for coverage information
            if data.get('Action') == 'output' and 'Output' in data:
                output_line = data['Output'].strip()
                if output_line.startswith("coverage:"):
                    # Extract the coverage percentage
                    match = re.search(r'XXcoverage: ([\d\.]+)%XX', output_line)
                    if match:
                        coverage = float(match.group(1))

            # Alternative: Some versions include 'Coverage' field
            elif data.get('Action') == 'pass' and 'Coverage' in data:
                coverage = data['Coverage']

        total_tests = passed_tests + failed_tests
        pass_percentage = round((passed_tests / total_tests) * 100, 2) if total_tests > 0 else 0

        v = {
            "test_pass_rate": pass_percentage,
            "execution_time_sec": summary_time if summary_time is not None else total_time,
            "line_coverage_percent": coverage,
            "branch_coverage_percent": None,
            "timeout": False,
            "internal_error_occurred": False,
            "runtime_errors_count": runtime_errors
        }
        return v
    except Exception as e:
        print(f"Error running Go tests: {e}")
        return {
            "test_pass_rate": 0,
            "execution_time_sec": None,
            "line_coverage_percent": None,
            "branch_coverage_percent": None,
            "timeout": True,
            "internal_error_occurred": True,
            "runtime_errors_count": runtime_errors
        }


def x_run_tests__mutmut_119(project_dir):
    test_cmd = ["go", "test", "-timeout", "30s", "-json", "-cover", "./..."]
    runtime_errors = 0

    print("Running tests...")
    try:
        result = subprocess.run(
            test_cmd,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            cwd=project_dir,
            text=True
        )
        output = result.stdout
        print(output)
        stderr = result.stderr
        print(stderr)

        # Check if the subprocess exited with a non-zero return code
        if result.returncode != 0:
            # Parse output for timeout messages
            if "test timed out after" in output or "panic: test timed out" in output:
                print("Tests failed due to timeout")
                # Handle timeout scenario
                return {
                    "test_pass_rate": 0,
                    "execution_time_sec": None,
                    "line_coverage_percent": None,
                    "branch_coverage_percent": None,
                    "timeout": True,
                    "internal_error_occurred": False,
                    "runtime_errors_count": None
                }
            else:
                runtime_error_pattern = re.compile(
                    r"(panic:|fatal error:|runtime error:)", re.IGNORECASE
                )
                assertion_error_pattern = re.compile(r"assert")

                # Search for runtime errors in stdout and stderr
                for o in (output, stderr):
                    for line in o.splitlines():
                        # Match lines with runtime errors but exclude assertions
                        if runtime_error_pattern.search(line) and not assertion_error_pattern.search(line):
                            runtime_errors = runtime_errors + 1

        # Parse the JSON output
        passed_tests = 0
        failed_tests = 0
        total_time = 0.0
        coverage = None
        summary_time = None

        for line in output.splitlines():
            if not line.strip():
                continue
            try:
                data = json.loads(line)
            except json.JSONDecodeError:
                continue  # Skip lines that aren't valid JSON

            # Check for test results
            if data.get('Action') == 'pass' and 'Test' in data:
                passed_tests += 1
                total_time += data.get('Elapsed', 0)
            elif data.get('Action') == 'fail' and 'Test' in data:
                failed_tests += 1
                total_time += data.get('Elapsed', 0)

            if (data.get('Action') == 'pass' or data.get('Action') == 'fail') and 'Test' not in data and 'Elapsed' in data:
                summary_time = data["Elapsed"]

            # Check for coverage information
            if data.get('Action') == 'output' and 'Output' in data:
                output_line = data['Output'].strip()
                if output_line.startswith("coverage:"):
                    # Extract the coverage percentage
                    match = re.search(r'coverage: ([\d\.]+)%', None)
                    if match:
                        coverage = float(match.group(1))

            # Alternative: Some versions include 'Coverage' field
            elif data.get('Action') == 'pass' and 'Coverage' in data:
                coverage = data['Coverage']

        total_tests = passed_tests + failed_tests
        pass_percentage = round((passed_tests / total_tests) * 100, 2) if total_tests > 0 else 0

        v = {
            "test_pass_rate": pass_percentage,
            "execution_time_sec": summary_time if summary_time is not None else total_time,
            "line_coverage_percent": coverage,
            "branch_coverage_percent": None,
            "timeout": False,
            "internal_error_occurred": False,
            "runtime_errors_count": runtime_errors
        }
        return v
    except Exception as e:
        print(f"Error running Go tests: {e}")
        return {
            "test_pass_rate": 0,
            "execution_time_sec": None,
            "line_coverage_percent": None,
            "branch_coverage_percent": None,
            "timeout": True,
            "internal_error_occurred": True,
            "runtime_errors_count": runtime_errors
        }


def x_run_tests__mutmut_120(project_dir):
    test_cmd = ["go", "test", "-timeout", "30s", "-json", "-cover", "./..."]
    runtime_errors = 0

    print("Running tests...")
    try:
        result = subprocess.run(
            test_cmd,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            cwd=project_dir,
            text=True
        )
        output = result.stdout
        print(output)
        stderr = result.stderr
        print(stderr)

        # Check if the subprocess exited with a non-zero return code
        if result.returncode != 0:
            # Parse output for timeout messages
            if "test timed out after" in output or "panic: test timed out" in output:
                print("Tests failed due to timeout")
                # Handle timeout scenario
                return {
                    "test_pass_rate": 0,
                    "execution_time_sec": None,
                    "line_coverage_percent": None,
                    "branch_coverage_percent": None,
                    "timeout": True,
                    "internal_error_occurred": False,
                    "runtime_errors_count": None
                }
            else:
                runtime_error_pattern = re.compile(
                    r"(panic:|fatal error:|runtime error:)", re.IGNORECASE
                )
                assertion_error_pattern = re.compile(r"assert")

                # Search for runtime errors in stdout and stderr
                for o in (output, stderr):
                    for line in o.splitlines():
                        # Match lines with runtime errors but exclude assertions
                        if runtime_error_pattern.search(line) and not assertion_error_pattern.search(line):
                            runtime_errors = runtime_errors + 1

        # Parse the JSON output
        passed_tests = 0
        failed_tests = 0
        total_time = 0.0
        coverage = None
        summary_time = None

        for line in output.splitlines():
            if not line.strip():
                continue
            try:
                data = json.loads(line)
            except json.JSONDecodeError:
                continue  # Skip lines that aren't valid JSON

            # Check for test results
            if data.get('Action') == 'pass' and 'Test' in data:
                passed_tests += 1
                total_time += data.get('Elapsed', 0)
            elif data.get('Action') == 'fail' and 'Test' in data:
                failed_tests += 1
                total_time += data.get('Elapsed', 0)

            if (data.get('Action') == 'pass' or data.get('Action') == 'fail') and 'Test' not in data and 'Elapsed' in data:
                summary_time = data["Elapsed"]

            # Check for coverage information
            if data.get('Action') == 'output' and 'Output' in data:
                output_line = data['Output'].strip()
                if output_line.startswith("coverage:"):
                    # Extract the coverage percentage
                    match = re.search(r'coverage: ([\d\.]+)%',)
                    if match:
                        coverage = float(match.group(1))

            # Alternative: Some versions include 'Coverage' field
            elif data.get('Action') == 'pass' and 'Coverage' in data:
                coverage = data['Coverage']

        total_tests = passed_tests + failed_tests
        pass_percentage = round((passed_tests / total_tests) * 100, 2) if total_tests > 0 else 0

        v = {
            "test_pass_rate": pass_percentage,
            "execution_time_sec": summary_time if summary_time is not None else total_time,
            "line_coverage_percent": coverage,
            "branch_coverage_percent": None,
            "timeout": False,
            "internal_error_occurred": False,
            "runtime_errors_count": runtime_errors
        }
        return v
    except Exception as e:
        print(f"Error running Go tests: {e}")
        return {
            "test_pass_rate": 0,
            "execution_time_sec": None,
            "line_coverage_percent": None,
            "branch_coverage_percent": None,
            "timeout": True,
            "internal_error_occurred": True,
            "runtime_errors_count": runtime_errors
        }


def x_run_tests__mutmut_121(project_dir):
    test_cmd = ["go", "test", "-timeout", "30s", "-json", "-cover", "./..."]
    runtime_errors = 0

    print("Running tests...")
    try:
        result = subprocess.run(
            test_cmd,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            cwd=project_dir,
            text=True
        )
        output = result.stdout
        print(output)
        stderr = result.stderr
        print(stderr)

        # Check if the subprocess exited with a non-zero return code
        if result.returncode != 0:
            # Parse output for timeout messages
            if "test timed out after" in output or "panic: test timed out" in output:
                print("Tests failed due to timeout")
                # Handle timeout scenario
                return {
                    "test_pass_rate": 0,
                    "execution_time_sec": None,
                    "line_coverage_percent": None,
                    "branch_coverage_percent": None,
                    "timeout": True,
                    "internal_error_occurred": False,
                    "runtime_errors_count": None
                }
            else:
                runtime_error_pattern = re.compile(
                    r"(panic:|fatal error:|runtime error:)", re.IGNORECASE
                )
                assertion_error_pattern = re.compile(r"assert")

                # Search for runtime errors in stdout and stderr
                for o in (output, stderr):
                    for line in o.splitlines():
                        # Match lines with runtime errors but exclude assertions
                        if runtime_error_pattern.search(line) and not assertion_error_pattern.search(line):
                            runtime_errors = runtime_errors + 1

        # Parse the JSON output
        passed_tests = 0
        failed_tests = 0
        total_time = 0.0
        coverage = None
        summary_time = None

        for line in output.splitlines():
            if not line.strip():
                continue
            try:
                data = json.loads(line)
            except json.JSONDecodeError:
                continue  # Skip lines that aren't valid JSON

            # Check for test results
            if data.get('Action') == 'pass' and 'Test' in data:
                passed_tests += 1
                total_time += data.get('Elapsed', 0)
            elif data.get('Action') == 'fail' and 'Test' in data:
                failed_tests += 1
                total_time += data.get('Elapsed', 0)

            if (data.get('Action') == 'pass' or data.get('Action') == 'fail') and 'Test' not in data and 'Elapsed' in data:
                summary_time = data["Elapsed"]

            # Check for coverage information
            if data.get('Action') == 'output' and 'Output' in data:
                output_line = data['Output'].strip()
                if output_line.startswith("coverage:"):
                    # Extract the coverage percentage
                    match = None
                    if match:
                        coverage = float(match.group(1))

            # Alternative: Some versions include 'Coverage' field
            elif data.get('Action') == 'pass' and 'Coverage' in data:
                coverage = data['Coverage']

        total_tests = passed_tests + failed_tests
        pass_percentage = round((passed_tests / total_tests) * 100, 2) if total_tests > 0 else 0

        v = {
            "test_pass_rate": pass_percentage,
            "execution_time_sec": summary_time if summary_time is not None else total_time,
            "line_coverage_percent": coverage,
            "branch_coverage_percent": None,
            "timeout": False,
            "internal_error_occurred": False,
            "runtime_errors_count": runtime_errors
        }
        return v
    except Exception as e:
        print(f"Error running Go tests: {e}")
        return {
            "test_pass_rate": 0,
            "execution_time_sec": None,
            "line_coverage_percent": None,
            "branch_coverage_percent": None,
            "timeout": True,
            "internal_error_occurred": True,
            "runtime_errors_count": runtime_errors
        }


def x_run_tests__mutmut_122(project_dir):
    test_cmd = ["go", "test", "-timeout", "30s", "-json", "-cover", "./..."]
    runtime_errors = 0

    print("Running tests...")
    try:
        result = subprocess.run(
            test_cmd,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            cwd=project_dir,
            text=True
        )
        output = result.stdout
        print(output)
        stderr = result.stderr
        print(stderr)

        # Check if the subprocess exited with a non-zero return code
        if result.returncode != 0:
            # Parse output for timeout messages
            if "test timed out after" in output or "panic: test timed out" in output:
                print("Tests failed due to timeout")
                # Handle timeout scenario
                return {
                    "test_pass_rate": 0,
                    "execution_time_sec": None,
                    "line_coverage_percent": None,
                    "branch_coverage_percent": None,
                    "timeout": True,
                    "internal_error_occurred": False,
                    "runtime_errors_count": None
                }
            else:
                runtime_error_pattern = re.compile(
                    r"(panic:|fatal error:|runtime error:)", re.IGNORECASE
                )
                assertion_error_pattern = re.compile(r"assert")

                # Search for runtime errors in stdout and stderr
                for o in (output, stderr):
                    for line in o.splitlines():
                        # Match lines with runtime errors but exclude assertions
                        if runtime_error_pattern.search(line) and not assertion_error_pattern.search(line):
                            runtime_errors = runtime_errors + 1

        # Parse the JSON output
        passed_tests = 0
        failed_tests = 0
        total_time = 0.0
        coverage = None
        summary_time = None

        for line in output.splitlines():
            if not line.strip():
                continue
            try:
                data = json.loads(line)
            except json.JSONDecodeError:
                continue  # Skip lines that aren't valid JSON

            # Check for test results
            if data.get('Action') == 'pass' and 'Test' in data:
                passed_tests += 1
                total_time += data.get('Elapsed', 0)
            elif data.get('Action') == 'fail' and 'Test' in data:
                failed_tests += 1
                total_time += data.get('Elapsed', 0)

            if (data.get('Action') == 'pass' or data.get('Action') == 'fail') and 'Test' not in data and 'Elapsed' in data:
                summary_time = data["Elapsed"]

            # Check for coverage information
            if data.get('Action') == 'output' and 'Output' in data:
                output_line = data['Output'].strip()
                if output_line.startswith("coverage:"):
                    # Extract the coverage percentage
                    match = re.search(r'coverage: ([\d\.]+)%', output_line)
                    if match:
                        coverage = float(match.group(2))

            # Alternative: Some versions include 'Coverage' field
            elif data.get('Action') == 'pass' and 'Coverage' in data:
                coverage = data['Coverage']

        total_tests = passed_tests + failed_tests
        pass_percentage = round((passed_tests / total_tests) * 100, 2) if total_tests > 0 else 0

        v = {
            "test_pass_rate": pass_percentage,
            "execution_time_sec": summary_time if summary_time is not None else total_time,
            "line_coverage_percent": coverage,
            "branch_coverage_percent": None,
            "timeout": False,
            "internal_error_occurred": False,
            "runtime_errors_count": runtime_errors
        }
        return v
    except Exception as e:
        print(f"Error running Go tests: {e}")
        return {
            "test_pass_rate": 0,
            "execution_time_sec": None,
            "line_coverage_percent": None,
            "branch_coverage_percent": None,
            "timeout": True,
            "internal_error_occurred": True,
            "runtime_errors_count": runtime_errors
        }


def x_run_tests__mutmut_123(project_dir):
    test_cmd = ["go", "test", "-timeout", "30s", "-json", "-cover", "./..."]
    runtime_errors = 0

    print("Running tests...")
    try:
        result = subprocess.run(
            test_cmd,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            cwd=project_dir,
            text=True
        )
        output = result.stdout
        print(output)
        stderr = result.stderr
        print(stderr)

        # Check if the subprocess exited with a non-zero return code
        if result.returncode != 0:
            # Parse output for timeout messages
            if "test timed out after" in output or "panic: test timed out" in output:
                print("Tests failed due to timeout")
                # Handle timeout scenario
                return {
                    "test_pass_rate": 0,
                    "execution_time_sec": None,
                    "line_coverage_percent": None,
                    "branch_coverage_percent": None,
                    "timeout": True,
                    "internal_error_occurred": False,
                    "runtime_errors_count": None
                }
            else:
                runtime_error_pattern = re.compile(
                    r"(panic:|fatal error:|runtime error:)", re.IGNORECASE
                )
                assertion_error_pattern = re.compile(r"assert")

                # Search for runtime errors in stdout and stderr
                for o in (output, stderr):
                    for line in o.splitlines():
                        # Match lines with runtime errors but exclude assertions
                        if runtime_error_pattern.search(line) and not assertion_error_pattern.search(line):
                            runtime_errors = runtime_errors + 1

        # Parse the JSON output
        passed_tests = 0
        failed_tests = 0
        total_time = 0.0
        coverage = None
        summary_time = None

        for line in output.splitlines():
            if not line.strip():
                continue
            try:
                data = json.loads(line)
            except json.JSONDecodeError:
                continue  # Skip lines that aren't valid JSON

            # Check for test results
            if data.get('Action') == 'pass' and 'Test' in data:
                passed_tests += 1
                total_time += data.get('Elapsed', 0)
            elif data.get('Action') == 'fail' and 'Test' in data:
                failed_tests += 1
                total_time += data.get('Elapsed', 0)

            if (data.get('Action') == 'pass' or data.get('Action') == 'fail') and 'Test' not in data and 'Elapsed' in data:
                summary_time = data["Elapsed"]

            # Check for coverage information
            if data.get('Action') == 'output' and 'Output' in data:
                output_line = data['Output'].strip()
                if output_line.startswith("coverage:"):
                    # Extract the coverage percentage
                    match = re.search(r'coverage: ([\d\.]+)%', output_line)
                    if match:
                        coverage = None

            # Alternative: Some versions include 'Coverage' field
            elif data.get('Action') == 'pass' and 'Coverage' in data:
                coverage = data['Coverage']

        total_tests = passed_tests + failed_tests
        pass_percentage = round((passed_tests / total_tests) * 100, 2) if total_tests > 0 else 0

        v = {
            "test_pass_rate": pass_percentage,
            "execution_time_sec": summary_time if summary_time is not None else total_time,
            "line_coverage_percent": coverage,
            "branch_coverage_percent": None,
            "timeout": False,
            "internal_error_occurred": False,
            "runtime_errors_count": runtime_errors
        }
        return v
    except Exception as e:
        print(f"Error running Go tests: {e}")
        return {
            "test_pass_rate": 0,
            "execution_time_sec": None,
            "line_coverage_percent": None,
            "branch_coverage_percent": None,
            "timeout": True,
            "internal_error_occurred": True,
            "runtime_errors_count": runtime_errors
        }


def x_run_tests__mutmut_124(project_dir):
    test_cmd = ["go", "test", "-timeout", "30s", "-json", "-cover", "./..."]
    runtime_errors = 0

    print("Running tests...")
    try:
        result = subprocess.run(
            test_cmd,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            cwd=project_dir,
            text=True
        )
        output = result.stdout
        print(output)
        stderr = result.stderr
        print(stderr)

        # Check if the subprocess exited with a non-zero return code
        if result.returncode != 0:
            # Parse output for timeout messages
            if "test timed out after" in output or "panic: test timed out" in output:
                print("Tests failed due to timeout")
                # Handle timeout scenario
                return {
                    "test_pass_rate": 0,
                    "execution_time_sec": None,
                    "line_coverage_percent": None,
                    "branch_coverage_percent": None,
                    "timeout": True,
                    "internal_error_occurred": False,
                    "runtime_errors_count": None
                }
            else:
                runtime_error_pattern = re.compile(
                    r"(panic:|fatal error:|runtime error:)", re.IGNORECASE
                )
                assertion_error_pattern = re.compile(r"assert")

                # Search for runtime errors in stdout and stderr
                for o in (output, stderr):
                    for line in o.splitlines():
                        # Match lines with runtime errors but exclude assertions
                        if runtime_error_pattern.search(line) and not assertion_error_pattern.search(line):
                            runtime_errors = runtime_errors + 1

        # Parse the JSON output
        passed_tests = 0
        failed_tests = 0
        total_time = 0.0
        coverage = None
        summary_time = None

        for line in output.splitlines():
            if not line.strip():
                continue
            try:
                data = json.loads(line)
            except json.JSONDecodeError:
                continue  # Skip lines that aren't valid JSON

            # Check for test results
            if data.get('Action') == 'pass' and 'Test' in data:
                passed_tests += 1
                total_time += data.get('Elapsed', 0)
            elif data.get('Action') == 'fail' and 'Test' in data:
                failed_tests += 1
                total_time += data.get('Elapsed', 0)

            if (data.get('Action') == 'pass' or data.get('Action') == 'fail') and 'Test' not in data and 'Elapsed' in data:
                summary_time = data["Elapsed"]

            # Check for coverage information
            if data.get('Action') == 'output' and 'Output' in data:
                output_line = data['Output'].strip()
                if output_line.startswith("coverage:"):
                    # Extract the coverage percentage
                    match = re.search(r'coverage: ([\d\.]+)%', output_line)
                    if match:
                        coverage = float(match.group(1))

            # Alternative: Some versions include 'Coverage' field
            elif data.get('XXActionXX') == 'pass' and 'Coverage' in data:
                coverage = data['Coverage']

        total_tests = passed_tests + failed_tests
        pass_percentage = round((passed_tests / total_tests) * 100, 2) if total_tests > 0 else 0

        v = {
            "test_pass_rate": pass_percentage,
            "execution_time_sec": summary_time if summary_time is not None else total_time,
            "line_coverage_percent": coverage,
            "branch_coverage_percent": None,
            "timeout": False,
            "internal_error_occurred": False,
            "runtime_errors_count": runtime_errors
        }
        return v
    except Exception as e:
        print(f"Error running Go tests: {e}")
        return {
            "test_pass_rate": 0,
            "execution_time_sec": None,
            "line_coverage_percent": None,
            "branch_coverage_percent": None,
            "timeout": True,
            "internal_error_occurred": True,
            "runtime_errors_count": runtime_errors
        }


def x_run_tests__mutmut_125(project_dir):
    test_cmd = ["go", "test", "-timeout", "30s", "-json", "-cover", "./..."]
    runtime_errors = 0

    print("Running tests...")
    try:
        result = subprocess.run(
            test_cmd,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            cwd=project_dir,
            text=True
        )
        output = result.stdout
        print(output)
        stderr = result.stderr
        print(stderr)

        # Check if the subprocess exited with a non-zero return code
        if result.returncode != 0:
            # Parse output for timeout messages
            if "test timed out after" in output or "panic: test timed out" in output:
                print("Tests failed due to timeout")
                # Handle timeout scenario
                return {
                    "test_pass_rate": 0,
                    "execution_time_sec": None,
                    "line_coverage_percent": None,
                    "branch_coverage_percent": None,
                    "timeout": True,
                    "internal_error_occurred": False,
                    "runtime_errors_count": None
                }
            else:
                runtime_error_pattern = re.compile(
                    r"(panic:|fatal error:|runtime error:)", re.IGNORECASE
                )
                assertion_error_pattern = re.compile(r"assert")

                # Search for runtime errors in stdout and stderr
                for o in (output, stderr):
                    for line in o.splitlines():
                        # Match lines with runtime errors but exclude assertions
                        if runtime_error_pattern.search(line) and not assertion_error_pattern.search(line):
                            runtime_errors = runtime_errors + 1

        # Parse the JSON output
        passed_tests = 0
        failed_tests = 0
        total_time = 0.0
        coverage = None
        summary_time = None

        for line in output.splitlines():
            if not line.strip():
                continue
            try:
                data = json.loads(line)
            except json.JSONDecodeError:
                continue  # Skip lines that aren't valid JSON

            # Check for test results
            if data.get('Action') == 'pass' and 'Test' in data:
                passed_tests += 1
                total_time += data.get('Elapsed', 0)
            elif data.get('Action') == 'fail' and 'Test' in data:
                failed_tests += 1
                total_time += data.get('Elapsed', 0)

            if (data.get('Action') == 'pass' or data.get('Action') == 'fail') and 'Test' not in data and 'Elapsed' in data:
                summary_time = data["Elapsed"]

            # Check for coverage information
            if data.get('Action') == 'output' and 'Output' in data:
                output_line = data['Output'].strip()
                if output_line.startswith("coverage:"):
                    # Extract the coverage percentage
                    match = re.search(r'coverage: ([\d\.]+)%', output_line)
                    if match:
                        coverage = float(match.group(1))

            # Alternative: Some versions include 'Coverage' field
            elif data.get('Action') != 'pass' and 'Coverage' in data:
                coverage = data['Coverage']

        total_tests = passed_tests + failed_tests
        pass_percentage = round((passed_tests / total_tests) * 100, 2) if total_tests > 0 else 0

        v = {
            "test_pass_rate": pass_percentage,
            "execution_time_sec": summary_time if summary_time is not None else total_time,
            "line_coverage_percent": coverage,
            "branch_coverage_percent": None,
            "timeout": False,
            "internal_error_occurred": False,
            "runtime_errors_count": runtime_errors
        }
        return v
    except Exception as e:
        print(f"Error running Go tests: {e}")
        return {
            "test_pass_rate": 0,
            "execution_time_sec": None,
            "line_coverage_percent": None,
            "branch_coverage_percent": None,
            "timeout": True,
            "internal_error_occurred": True,
            "runtime_errors_count": runtime_errors
        }


def x_run_tests__mutmut_126(project_dir):
    test_cmd = ["go", "test", "-timeout", "30s", "-json", "-cover", "./..."]
    runtime_errors = 0

    print("Running tests...")
    try:
        result = subprocess.run(
            test_cmd,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            cwd=project_dir,
            text=True
        )
        output = result.stdout
        print(output)
        stderr = result.stderr
        print(stderr)

        # Check if the subprocess exited with a non-zero return code
        if result.returncode != 0:
            # Parse output for timeout messages
            if "test timed out after" in output or "panic: test timed out" in output:
                print("Tests failed due to timeout")
                # Handle timeout scenario
                return {
                    "test_pass_rate": 0,
                    "execution_time_sec": None,
                    "line_coverage_percent": None,
                    "branch_coverage_percent": None,
                    "timeout": True,
                    "internal_error_occurred": False,
                    "runtime_errors_count": None
                }
            else:
                runtime_error_pattern = re.compile(
                    r"(panic:|fatal error:|runtime error:)", re.IGNORECASE
                )
                assertion_error_pattern = re.compile(r"assert")

                # Search for runtime errors in stdout and stderr
                for o in (output, stderr):
                    for line in o.splitlines():
                        # Match lines with runtime errors but exclude assertions
                        if runtime_error_pattern.search(line) and not assertion_error_pattern.search(line):
                            runtime_errors = runtime_errors + 1

        # Parse the JSON output
        passed_tests = 0
        failed_tests = 0
        total_time = 0.0
        coverage = None
        summary_time = None

        for line in output.splitlines():
            if not line.strip():
                continue
            try:
                data = json.loads(line)
            except json.JSONDecodeError:
                continue  # Skip lines that aren't valid JSON

            # Check for test results
            if data.get('Action') == 'pass' and 'Test' in data:
                passed_tests += 1
                total_time += data.get('Elapsed', 0)
            elif data.get('Action') == 'fail' and 'Test' in data:
                failed_tests += 1
                total_time += data.get('Elapsed', 0)

            if (data.get('Action') == 'pass' or data.get('Action') == 'fail') and 'Test' not in data and 'Elapsed' in data:
                summary_time = data["Elapsed"]

            # Check for coverage information
            if data.get('Action') == 'output' and 'Output' in data:
                output_line = data['Output'].strip()
                if output_line.startswith("coverage:"):
                    # Extract the coverage percentage
                    match = re.search(r'coverage: ([\d\.]+)%', output_line)
                    if match:
                        coverage = float(match.group(1))

            # Alternative: Some versions include 'Coverage' field
            elif data.get('Action') == 'XXpassXX' and 'Coverage' in data:
                coverage = data['Coverage']

        total_tests = passed_tests + failed_tests
        pass_percentage = round((passed_tests / total_tests) * 100, 2) if total_tests > 0 else 0

        v = {
            "test_pass_rate": pass_percentage,
            "execution_time_sec": summary_time if summary_time is not None else total_time,
            "line_coverage_percent": coverage,
            "branch_coverage_percent": None,
            "timeout": False,
            "internal_error_occurred": False,
            "runtime_errors_count": runtime_errors
        }
        return v
    except Exception as e:
        print(f"Error running Go tests: {e}")
        return {
            "test_pass_rate": 0,
            "execution_time_sec": None,
            "line_coverage_percent": None,
            "branch_coverage_percent": None,
            "timeout": True,
            "internal_error_occurred": True,
            "runtime_errors_count": runtime_errors
        }


def x_run_tests__mutmut_127(project_dir):
    test_cmd = ["go", "test", "-timeout", "30s", "-json", "-cover", "./..."]
    runtime_errors = 0

    print("Running tests...")
    try:
        result = subprocess.run(
            test_cmd,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            cwd=project_dir,
            text=True
        )
        output = result.stdout
        print(output)
        stderr = result.stderr
        print(stderr)

        # Check if the subprocess exited with a non-zero return code
        if result.returncode != 0:
            # Parse output for timeout messages
            if "test timed out after" in output or "panic: test timed out" in output:
                print("Tests failed due to timeout")
                # Handle timeout scenario
                return {
                    "test_pass_rate": 0,
                    "execution_time_sec": None,
                    "line_coverage_percent": None,
                    "branch_coverage_percent": None,
                    "timeout": True,
                    "internal_error_occurred": False,
                    "runtime_errors_count": None
                }
            else:
                runtime_error_pattern = re.compile(
                    r"(panic:|fatal error:|runtime error:)", re.IGNORECASE
                )
                assertion_error_pattern = re.compile(r"assert")

                # Search for runtime errors in stdout and stderr
                for o in (output, stderr):
                    for line in o.splitlines():
                        # Match lines with runtime errors but exclude assertions
                        if runtime_error_pattern.search(line) and not assertion_error_pattern.search(line):
                            runtime_errors = runtime_errors + 1

        # Parse the JSON output
        passed_tests = 0
        failed_tests = 0
        total_time = 0.0
        coverage = None
        summary_time = None

        for line in output.splitlines():
            if not line.strip():
                continue
            try:
                data = json.loads(line)
            except json.JSONDecodeError:
                continue  # Skip lines that aren't valid JSON

            # Check for test results
            if data.get('Action') == 'pass' and 'Test' in data:
                passed_tests += 1
                total_time += data.get('Elapsed', 0)
            elif data.get('Action') == 'fail' and 'Test' in data:
                failed_tests += 1
                total_time += data.get('Elapsed', 0)

            if (data.get('Action') == 'pass' or data.get('Action') == 'fail') and 'Test' not in data and 'Elapsed' in data:
                summary_time = data["Elapsed"]

            # Check for coverage information
            if data.get('Action') == 'output' and 'Output' in data:
                output_line = data['Output'].strip()
                if output_line.startswith("coverage:"):
                    # Extract the coverage percentage
                    match = re.search(r'coverage: ([\d\.]+)%', output_line)
                    if match:
                        coverage = float(match.group(1))

            # Alternative: Some versions include 'Coverage' field
            elif data.get('Action') == 'pass' and 'XXCoverageXX' in data:
                coverage = data['Coverage']

        total_tests = passed_tests + failed_tests
        pass_percentage = round((passed_tests / total_tests) * 100, 2) if total_tests > 0 else 0

        v = {
            "test_pass_rate": pass_percentage,
            "execution_time_sec": summary_time if summary_time is not None else total_time,
            "line_coverage_percent": coverage,
            "branch_coverage_percent": None,
            "timeout": False,
            "internal_error_occurred": False,
            "runtime_errors_count": runtime_errors
        }
        return v
    except Exception as e:
        print(f"Error running Go tests: {e}")
        return {
            "test_pass_rate": 0,
            "execution_time_sec": None,
            "line_coverage_percent": None,
            "branch_coverage_percent": None,
            "timeout": True,
            "internal_error_occurred": True,
            "runtime_errors_count": runtime_errors
        }


def x_run_tests__mutmut_128(project_dir):
    test_cmd = ["go", "test", "-timeout", "30s", "-json", "-cover", "./..."]
    runtime_errors = 0

    print("Running tests...")
    try:
        result = subprocess.run(
            test_cmd,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            cwd=project_dir,
            text=True
        )
        output = result.stdout
        print(output)
        stderr = result.stderr
        print(stderr)

        # Check if the subprocess exited with a non-zero return code
        if result.returncode != 0:
            # Parse output for timeout messages
            if "test timed out after" in output or "panic: test timed out" in output:
                print("Tests failed due to timeout")
                # Handle timeout scenario
                return {
                    "test_pass_rate": 0,
                    "execution_time_sec": None,
                    "line_coverage_percent": None,
                    "branch_coverage_percent": None,
                    "timeout": True,
                    "internal_error_occurred": False,
                    "runtime_errors_count": None
                }
            else:
                runtime_error_pattern = re.compile(
                    r"(panic:|fatal error:|runtime error:)", re.IGNORECASE
                )
                assertion_error_pattern = re.compile(r"assert")

                # Search for runtime errors in stdout and stderr
                for o in (output, stderr):
                    for line in o.splitlines():
                        # Match lines with runtime errors but exclude assertions
                        if runtime_error_pattern.search(line) and not assertion_error_pattern.search(line):
                            runtime_errors = runtime_errors + 1

        # Parse the JSON output
        passed_tests = 0
        failed_tests = 0
        total_time = 0.0
        coverage = None
        summary_time = None

        for line in output.splitlines():
            if not line.strip():
                continue
            try:
                data = json.loads(line)
            except json.JSONDecodeError:
                continue  # Skip lines that aren't valid JSON

            # Check for test results
            if data.get('Action') == 'pass' and 'Test' in data:
                passed_tests += 1
                total_time += data.get('Elapsed', 0)
            elif data.get('Action') == 'fail' and 'Test' in data:
                failed_tests += 1
                total_time += data.get('Elapsed', 0)

            if (data.get('Action') == 'pass' or data.get('Action') == 'fail') and 'Test' not in data and 'Elapsed' in data:
                summary_time = data["Elapsed"]

            # Check for coverage information
            if data.get('Action') == 'output' and 'Output' in data:
                output_line = data['Output'].strip()
                if output_line.startswith("coverage:"):
                    # Extract the coverage percentage
                    match = re.search(r'coverage: ([\d\.]+)%', output_line)
                    if match:
                        coverage = float(match.group(1))

            # Alternative: Some versions include 'Coverage' field
            elif data.get('Action') == 'pass' and 'Coverage' not in data:
                coverage = data['Coverage']

        total_tests = passed_tests + failed_tests
        pass_percentage = round((passed_tests / total_tests) * 100, 2) if total_tests > 0 else 0

        v = {
            "test_pass_rate": pass_percentage,
            "execution_time_sec": summary_time if summary_time is not None else total_time,
            "line_coverage_percent": coverage,
            "branch_coverage_percent": None,
            "timeout": False,
            "internal_error_occurred": False,
            "runtime_errors_count": runtime_errors
        }
        return v
    except Exception as e:
        print(f"Error running Go tests: {e}")
        return {
            "test_pass_rate": 0,
            "execution_time_sec": None,
            "line_coverage_percent": None,
            "branch_coverage_percent": None,
            "timeout": True,
            "internal_error_occurred": True,
            "runtime_errors_count": runtime_errors
        }


def x_run_tests__mutmut_129(project_dir):
    test_cmd = ["go", "test", "-timeout", "30s", "-json", "-cover", "./..."]
    runtime_errors = 0

    print("Running tests...")
    try:
        result = subprocess.run(
            test_cmd,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            cwd=project_dir,
            text=True
        )
        output = result.stdout
        print(output)
        stderr = result.stderr
        print(stderr)

        # Check if the subprocess exited with a non-zero return code
        if result.returncode != 0:
            # Parse output for timeout messages
            if "test timed out after" in output or "panic: test timed out" in output:
                print("Tests failed due to timeout")
                # Handle timeout scenario
                return {
                    "test_pass_rate": 0,
                    "execution_time_sec": None,
                    "line_coverage_percent": None,
                    "branch_coverage_percent": None,
                    "timeout": True,
                    "internal_error_occurred": False,
                    "runtime_errors_count": None
                }
            else:
                runtime_error_pattern = re.compile(
                    r"(panic:|fatal error:|runtime error:)", re.IGNORECASE
                )
                assertion_error_pattern = re.compile(r"assert")

                # Search for runtime errors in stdout and stderr
                for o in (output, stderr):
                    for line in o.splitlines():
                        # Match lines with runtime errors but exclude assertions
                        if runtime_error_pattern.search(line) and not assertion_error_pattern.search(line):
                            runtime_errors = runtime_errors + 1

        # Parse the JSON output
        passed_tests = 0
        failed_tests = 0
        total_time = 0.0
        coverage = None
        summary_time = None

        for line in output.splitlines():
            if not line.strip():
                continue
            try:
                data = json.loads(line)
            except json.JSONDecodeError:
                continue  # Skip lines that aren't valid JSON

            # Check for test results
            if data.get('Action') == 'pass' and 'Test' in data:
                passed_tests += 1
                total_time += data.get('Elapsed', 0)
            elif data.get('Action') == 'fail' and 'Test' in data:
                failed_tests += 1
                total_time += data.get('Elapsed', 0)

            if (data.get('Action') == 'pass' or data.get('Action') == 'fail') and 'Test' not in data and 'Elapsed' in data:
                summary_time = data["Elapsed"]

            # Check for coverage information
            if data.get('Action') == 'output' and 'Output' in data:
                output_line = data['Output'].strip()
                if output_line.startswith("coverage:"):
                    # Extract the coverage percentage
                    match = re.search(r'coverage: ([\d\.]+)%', output_line)
                    if match:
                        coverage = float(match.group(1))

            # Alternative: Some versions include 'Coverage' field
            elif data.get('Action') == 'pass' or 'Coverage' in data:
                coverage = data['Coverage']

        total_tests = passed_tests + failed_tests
        pass_percentage = round((passed_tests / total_tests) * 100, 2) if total_tests > 0 else 0

        v = {
            "test_pass_rate": pass_percentage,
            "execution_time_sec": summary_time if summary_time is not None else total_time,
            "line_coverage_percent": coverage,
            "branch_coverage_percent": None,
            "timeout": False,
            "internal_error_occurred": False,
            "runtime_errors_count": runtime_errors
        }
        return v
    except Exception as e:
        print(f"Error running Go tests: {e}")
        return {
            "test_pass_rate": 0,
            "execution_time_sec": None,
            "line_coverage_percent": None,
            "branch_coverage_percent": None,
            "timeout": True,
            "internal_error_occurred": True,
            "runtime_errors_count": runtime_errors
        }


def x_run_tests__mutmut_130(project_dir):
    test_cmd = ["go", "test", "-timeout", "30s", "-json", "-cover", "./..."]
    runtime_errors = 0

    print("Running tests...")
    try:
        result = subprocess.run(
            test_cmd,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            cwd=project_dir,
            text=True
        )
        output = result.stdout
        print(output)
        stderr = result.stderr
        print(stderr)

        # Check if the subprocess exited with a non-zero return code
        if result.returncode != 0:
            # Parse output for timeout messages
            if "test timed out after" in output or "panic: test timed out" in output:
                print("Tests failed due to timeout")
                # Handle timeout scenario
                return {
                    "test_pass_rate": 0,
                    "execution_time_sec": None,
                    "line_coverage_percent": None,
                    "branch_coverage_percent": None,
                    "timeout": True,
                    "internal_error_occurred": False,
                    "runtime_errors_count": None
                }
            else:
                runtime_error_pattern = re.compile(
                    r"(panic:|fatal error:|runtime error:)", re.IGNORECASE
                )
                assertion_error_pattern = re.compile(r"assert")

                # Search for runtime errors in stdout and stderr
                for o in (output, stderr):
                    for line in o.splitlines():
                        # Match lines with runtime errors but exclude assertions
                        if runtime_error_pattern.search(line) and not assertion_error_pattern.search(line):
                            runtime_errors = runtime_errors + 1

        # Parse the JSON output
        passed_tests = 0
        failed_tests = 0
        total_time = 0.0
        coverage = None
        summary_time = None

        for line in output.splitlines():
            if not line.strip():
                continue
            try:
                data = json.loads(line)
            except json.JSONDecodeError:
                continue  # Skip lines that aren't valid JSON

            # Check for test results
            if data.get('Action') == 'pass' and 'Test' in data:
                passed_tests += 1
                total_time += data.get('Elapsed', 0)
            elif data.get('Action') == 'fail' and 'Test' in data:
                failed_tests += 1
                total_time += data.get('Elapsed', 0)

            if (data.get('Action') == 'pass' or data.get('Action') == 'fail') and 'Test' not in data and 'Elapsed' in data:
                summary_time = data["Elapsed"]

            # Check for coverage information
            if data.get('Action') == 'output' and 'Output' in data:
                output_line = data['Output'].strip()
                if output_line.startswith("coverage:"):
                    # Extract the coverage percentage
                    match = re.search(r'coverage: ([\d\.]+)%', output_line)
                    if match:
                        coverage = float(match.group(1))

            # Alternative: Some versions include 'Coverage' field
            elif data.get('Action') == 'pass' and 'Coverage' in data:
                coverage = data['XXCoverageXX']

        total_tests = passed_tests + failed_tests
        pass_percentage = round((passed_tests / total_tests) * 100, 2) if total_tests > 0 else 0

        v = {
            "test_pass_rate": pass_percentage,
            "execution_time_sec": summary_time if summary_time is not None else total_time,
            "line_coverage_percent": coverage,
            "branch_coverage_percent": None,
            "timeout": False,
            "internal_error_occurred": False,
            "runtime_errors_count": runtime_errors
        }
        return v
    except Exception as e:
        print(f"Error running Go tests: {e}")
        return {
            "test_pass_rate": 0,
            "execution_time_sec": None,
            "line_coverage_percent": None,
            "branch_coverage_percent": None,
            "timeout": True,
            "internal_error_occurred": True,
            "runtime_errors_count": runtime_errors
        }


def x_run_tests__mutmut_131(project_dir):
    test_cmd = ["go", "test", "-timeout", "30s", "-json", "-cover", "./..."]
    runtime_errors = 0

    print("Running tests...")
    try:
        result = subprocess.run(
            test_cmd,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            cwd=project_dir,
            text=True
        )
        output = result.stdout
        print(output)
        stderr = result.stderr
        print(stderr)

        # Check if the subprocess exited with a non-zero return code
        if result.returncode != 0:
            # Parse output for timeout messages
            if "test timed out after" in output or "panic: test timed out" in output:
                print("Tests failed due to timeout")
                # Handle timeout scenario
                return {
                    "test_pass_rate": 0,
                    "execution_time_sec": None,
                    "line_coverage_percent": None,
                    "branch_coverage_percent": None,
                    "timeout": True,
                    "internal_error_occurred": False,
                    "runtime_errors_count": None
                }
            else:
                runtime_error_pattern = re.compile(
                    r"(panic:|fatal error:|runtime error:)", re.IGNORECASE
                )
                assertion_error_pattern = re.compile(r"assert")

                # Search for runtime errors in stdout and stderr
                for o in (output, stderr):
                    for line in o.splitlines():
                        # Match lines with runtime errors but exclude assertions
                        if runtime_error_pattern.search(line) and not assertion_error_pattern.search(line):
                            runtime_errors = runtime_errors + 1

        # Parse the JSON output
        passed_tests = 0
        failed_tests = 0
        total_time = 0.0
        coverage = None
        summary_time = None

        for line in output.splitlines():
            if not line.strip():
                continue
            try:
                data = json.loads(line)
            except json.JSONDecodeError:
                continue  # Skip lines that aren't valid JSON

            # Check for test results
            if data.get('Action') == 'pass' and 'Test' in data:
                passed_tests += 1
                total_time += data.get('Elapsed', 0)
            elif data.get('Action') == 'fail' and 'Test' in data:
                failed_tests += 1
                total_time += data.get('Elapsed', 0)

            if (data.get('Action') == 'pass' or data.get('Action') == 'fail') and 'Test' not in data and 'Elapsed' in data:
                summary_time = data["Elapsed"]

            # Check for coverage information
            if data.get('Action') == 'output' and 'Output' in data:
                output_line = data['Output'].strip()
                if output_line.startswith("coverage:"):
                    # Extract the coverage percentage
                    match = re.search(r'coverage: ([\d\.]+)%', output_line)
                    if match:
                        coverage = float(match.group(1))

            # Alternative: Some versions include 'Coverage' field
            elif data.get('Action') == 'pass' and 'Coverage' in data:
                coverage = data[None]

        total_tests = passed_tests + failed_tests
        pass_percentage = round((passed_tests / total_tests) * 100, 2) if total_tests > 0 else 0

        v = {
            "test_pass_rate": pass_percentage,
            "execution_time_sec": summary_time if summary_time is not None else total_time,
            "line_coverage_percent": coverage,
            "branch_coverage_percent": None,
            "timeout": False,
            "internal_error_occurred": False,
            "runtime_errors_count": runtime_errors
        }
        return v
    except Exception as e:
        print(f"Error running Go tests: {e}")
        return {
            "test_pass_rate": 0,
            "execution_time_sec": None,
            "line_coverage_percent": None,
            "branch_coverage_percent": None,
            "timeout": True,
            "internal_error_occurred": True,
            "runtime_errors_count": runtime_errors
        }


def x_run_tests__mutmut_132(project_dir):
    test_cmd = ["go", "test", "-timeout", "30s", "-json", "-cover", "./..."]
    runtime_errors = 0

    print("Running tests...")
    try:
        result = subprocess.run(
            test_cmd,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            cwd=project_dir,
            text=True
        )
        output = result.stdout
        print(output)
        stderr = result.stderr
        print(stderr)

        # Check if the subprocess exited with a non-zero return code
        if result.returncode != 0:
            # Parse output for timeout messages
            if "test timed out after" in output or "panic: test timed out" in output:
                print("Tests failed due to timeout")
                # Handle timeout scenario
                return {
                    "test_pass_rate": 0,
                    "execution_time_sec": None,
                    "line_coverage_percent": None,
                    "branch_coverage_percent": None,
                    "timeout": True,
                    "internal_error_occurred": False,
                    "runtime_errors_count": None
                }
            else:
                runtime_error_pattern = re.compile(
                    r"(panic:|fatal error:|runtime error:)", re.IGNORECASE
                )
                assertion_error_pattern = re.compile(r"assert")

                # Search for runtime errors in stdout and stderr
                for o in (output, stderr):
                    for line in o.splitlines():
                        # Match lines with runtime errors but exclude assertions
                        if runtime_error_pattern.search(line) and not assertion_error_pattern.search(line):
                            runtime_errors = runtime_errors + 1

        # Parse the JSON output
        passed_tests = 0
        failed_tests = 0
        total_time = 0.0
        coverage = None
        summary_time = None

        for line in output.splitlines():
            if not line.strip():
                continue
            try:
                data = json.loads(line)
            except json.JSONDecodeError:
                continue  # Skip lines that aren't valid JSON

            # Check for test results
            if data.get('Action') == 'pass' and 'Test' in data:
                passed_tests += 1
                total_time += data.get('Elapsed', 0)
            elif data.get('Action') == 'fail' and 'Test' in data:
                failed_tests += 1
                total_time += data.get('Elapsed', 0)

            if (data.get('Action') == 'pass' or data.get('Action') == 'fail') and 'Test' not in data and 'Elapsed' in data:
                summary_time = data["Elapsed"]

            # Check for coverage information
            if data.get('Action') == 'output' and 'Output' in data:
                output_line = data['Output'].strip()
                if output_line.startswith("coverage:"):
                    # Extract the coverage percentage
                    match = re.search(r'coverage: ([\d\.]+)%', output_line)
                    if match:
                        coverage = float(match.group(1))

            # Alternative: Some versions include 'Coverage' field
            elif data.get('Action') == 'pass' and 'Coverage' in data:
                coverage = None

        total_tests = passed_tests + failed_tests
        pass_percentage = round((passed_tests / total_tests) * 100, 2) if total_tests > 0 else 0

        v = {
            "test_pass_rate": pass_percentage,
            "execution_time_sec": summary_time if summary_time is not None else total_time,
            "line_coverage_percent": coverage,
            "branch_coverage_percent": None,
            "timeout": False,
            "internal_error_occurred": False,
            "runtime_errors_count": runtime_errors
        }
        return v
    except Exception as e:
        print(f"Error running Go tests: {e}")
        return {
            "test_pass_rate": 0,
            "execution_time_sec": None,
            "line_coverage_percent": None,
            "branch_coverage_percent": None,
            "timeout": True,
            "internal_error_occurred": True,
            "runtime_errors_count": runtime_errors
        }


def x_run_tests__mutmut_133(project_dir):
    test_cmd = ["go", "test", "-timeout", "30s", "-json", "-cover", "./..."]
    runtime_errors = 0

    print("Running tests...")
    try:
        result = subprocess.run(
            test_cmd,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            cwd=project_dir,
            text=True
        )
        output = result.stdout
        print(output)
        stderr = result.stderr
        print(stderr)

        # Check if the subprocess exited with a non-zero return code
        if result.returncode != 0:
            # Parse output for timeout messages
            if "test timed out after" in output or "panic: test timed out" in output:
                print("Tests failed due to timeout")
                # Handle timeout scenario
                return {
                    "test_pass_rate": 0,
                    "execution_time_sec": None,
                    "line_coverage_percent": None,
                    "branch_coverage_percent": None,
                    "timeout": True,
                    "internal_error_occurred": False,
                    "runtime_errors_count": None
                }
            else:
                runtime_error_pattern = re.compile(
                    r"(panic:|fatal error:|runtime error:)", re.IGNORECASE
                )
                assertion_error_pattern = re.compile(r"assert")

                # Search for runtime errors in stdout and stderr
                for o in (output, stderr):
                    for line in o.splitlines():
                        # Match lines with runtime errors but exclude assertions
                        if runtime_error_pattern.search(line) and not assertion_error_pattern.search(line):
                            runtime_errors = runtime_errors + 1

        # Parse the JSON output
        passed_tests = 0
        failed_tests = 0
        total_time = 0.0
        coverage = None
        summary_time = None

        for line in output.splitlines():
            if not line.strip():
                continue
            try:
                data = json.loads(line)
            except json.JSONDecodeError:
                continue  # Skip lines that aren't valid JSON

            # Check for test results
            if data.get('Action') == 'pass' and 'Test' in data:
                passed_tests += 1
                total_time += data.get('Elapsed', 0)
            elif data.get('Action') == 'fail' and 'Test' in data:
                failed_tests += 1
                total_time += data.get('Elapsed', 0)

            if (data.get('Action') == 'pass' or data.get('Action') == 'fail') and 'Test' not in data and 'Elapsed' in data:
                summary_time = data["Elapsed"]

            # Check for coverage information
            if data.get('Action') == 'output' and 'Output' in data:
                output_line = data['Output'].strip()
                if output_line.startswith("coverage:"):
                    # Extract the coverage percentage
                    match = re.search(r'coverage: ([\d\.]+)%', output_line)
                    if match:
                        coverage = float(match.group(1))

            # Alternative: Some versions include 'Coverage' field
            elif data.get('Action') == 'pass' and 'Coverage' in data:
                coverage = data['Coverage']

        total_tests = passed_tests - failed_tests
        pass_percentage = round((passed_tests / total_tests) * 100, 2) if total_tests > 0 else 0

        v = {
            "test_pass_rate": pass_percentage,
            "execution_time_sec": summary_time if summary_time is not None else total_time,
            "line_coverage_percent": coverage,
            "branch_coverage_percent": None,
            "timeout": False,
            "internal_error_occurred": False,
            "runtime_errors_count": runtime_errors
        }
        return v
    except Exception as e:
        print(f"Error running Go tests: {e}")
        return {
            "test_pass_rate": 0,
            "execution_time_sec": None,
            "line_coverage_percent": None,
            "branch_coverage_percent": None,
            "timeout": True,
            "internal_error_occurred": True,
            "runtime_errors_count": runtime_errors
        }


def x_run_tests__mutmut_134(project_dir):
    test_cmd = ["go", "test", "-timeout", "30s", "-json", "-cover", "./..."]
    runtime_errors = 0

    print("Running tests...")
    try:
        result = subprocess.run(
            test_cmd,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            cwd=project_dir,
            text=True
        )
        output = result.stdout
        print(output)
        stderr = result.stderr
        print(stderr)

        # Check if the subprocess exited with a non-zero return code
        if result.returncode != 0:
            # Parse output for timeout messages
            if "test timed out after" in output or "panic: test timed out" in output:
                print("Tests failed due to timeout")
                # Handle timeout scenario
                return {
                    "test_pass_rate": 0,
                    "execution_time_sec": None,
                    "line_coverage_percent": None,
                    "branch_coverage_percent": None,
                    "timeout": True,
                    "internal_error_occurred": False,
                    "runtime_errors_count": None
                }
            else:
                runtime_error_pattern = re.compile(
                    r"(panic:|fatal error:|runtime error:)", re.IGNORECASE
                )
                assertion_error_pattern = re.compile(r"assert")

                # Search for runtime errors in stdout and stderr
                for o in (output, stderr):
                    for line in o.splitlines():
                        # Match lines with runtime errors but exclude assertions
                        if runtime_error_pattern.search(line) and not assertion_error_pattern.search(line):
                            runtime_errors = runtime_errors + 1

        # Parse the JSON output
        passed_tests = 0
        failed_tests = 0
        total_time = 0.0
        coverage = None
        summary_time = None

        for line in output.splitlines():
            if not line.strip():
                continue
            try:
                data = json.loads(line)
            except json.JSONDecodeError:
                continue  # Skip lines that aren't valid JSON

            # Check for test results
            if data.get('Action') == 'pass' and 'Test' in data:
                passed_tests += 1
                total_time += data.get('Elapsed', 0)
            elif data.get('Action') == 'fail' and 'Test' in data:
                failed_tests += 1
                total_time += data.get('Elapsed', 0)

            if (data.get('Action') == 'pass' or data.get('Action') == 'fail') and 'Test' not in data and 'Elapsed' in data:
                summary_time = data["Elapsed"]

            # Check for coverage information
            if data.get('Action') == 'output' and 'Output' in data:
                output_line = data['Output'].strip()
                if output_line.startswith("coverage:"):
                    # Extract the coverage percentage
                    match = re.search(r'coverage: ([\d\.]+)%', output_line)
                    if match:
                        coverage = float(match.group(1))

            # Alternative: Some versions include 'Coverage' field
            elif data.get('Action') == 'pass' and 'Coverage' in data:
                coverage = data['Coverage']

        total_tests = None
        pass_percentage = round((passed_tests / total_tests) * 100, 2) if total_tests > 0 else 0

        v = {
            "test_pass_rate": pass_percentage,
            "execution_time_sec": summary_time if summary_time is not None else total_time,
            "line_coverage_percent": coverage,
            "branch_coverage_percent": None,
            "timeout": False,
            "internal_error_occurred": False,
            "runtime_errors_count": runtime_errors
        }
        return v
    except Exception as e:
        print(f"Error running Go tests: {e}")
        return {
            "test_pass_rate": 0,
            "execution_time_sec": None,
            "line_coverage_percent": None,
            "branch_coverage_percent": None,
            "timeout": True,
            "internal_error_occurred": True,
            "runtime_errors_count": runtime_errors
        }


def x_run_tests__mutmut_135(project_dir):
    test_cmd = ["go", "test", "-timeout", "30s", "-json", "-cover", "./..."]
    runtime_errors = 0

    print("Running tests...")
    try:
        result = subprocess.run(
            test_cmd,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            cwd=project_dir,
            text=True
        )
        output = result.stdout
        print(output)
        stderr = result.stderr
        print(stderr)

        # Check if the subprocess exited with a non-zero return code
        if result.returncode != 0:
            # Parse output for timeout messages
            if "test timed out after" in output or "panic: test timed out" in output:
                print("Tests failed due to timeout")
                # Handle timeout scenario
                return {
                    "test_pass_rate": 0,
                    "execution_time_sec": None,
                    "line_coverage_percent": None,
                    "branch_coverage_percent": None,
                    "timeout": True,
                    "internal_error_occurred": False,
                    "runtime_errors_count": None
                }
            else:
                runtime_error_pattern = re.compile(
                    r"(panic:|fatal error:|runtime error:)", re.IGNORECASE
                )
                assertion_error_pattern = re.compile(r"assert")

                # Search for runtime errors in stdout and stderr
                for o in (output, stderr):
                    for line in o.splitlines():
                        # Match lines with runtime errors but exclude assertions
                        if runtime_error_pattern.search(line) and not assertion_error_pattern.search(line):
                            runtime_errors = runtime_errors + 1

        # Parse the JSON output
        passed_tests = 0
        failed_tests = 0
        total_time = 0.0
        coverage = None
        summary_time = None

        for line in output.splitlines():
            if not line.strip():
                continue
            try:
                data = json.loads(line)
            except json.JSONDecodeError:
                continue  # Skip lines that aren't valid JSON

            # Check for test results
            if data.get('Action') == 'pass' and 'Test' in data:
                passed_tests += 1
                total_time += data.get('Elapsed', 0)
            elif data.get('Action') == 'fail' and 'Test' in data:
                failed_tests += 1
                total_time += data.get('Elapsed', 0)

            if (data.get('Action') == 'pass' or data.get('Action') == 'fail') and 'Test' not in data and 'Elapsed' in data:
                summary_time = data["Elapsed"]

            # Check for coverage information
            if data.get('Action') == 'output' and 'Output' in data:
                output_line = data['Output'].strip()
                if output_line.startswith("coverage:"):
                    # Extract the coverage percentage
                    match = re.search(r'coverage: ([\d\.]+)%', output_line)
                    if match:
                        coverage = float(match.group(1))

            # Alternative: Some versions include 'Coverage' field
            elif data.get('Action') == 'pass' and 'Coverage' in data:
                coverage = data['Coverage']

        total_tests = passed_tests + failed_tests
        pass_percentage = round((passed_tests * total_tests) * 100, 2) if total_tests > 0 else 0

        v = {
            "test_pass_rate": pass_percentage,
            "execution_time_sec": summary_time if summary_time is not None else total_time,
            "line_coverage_percent": coverage,
            "branch_coverage_percent": None,
            "timeout": False,
            "internal_error_occurred": False,
            "runtime_errors_count": runtime_errors
        }
        return v
    except Exception as e:
        print(f"Error running Go tests: {e}")
        return {
            "test_pass_rate": 0,
            "execution_time_sec": None,
            "line_coverage_percent": None,
            "branch_coverage_percent": None,
            "timeout": True,
            "internal_error_occurred": True,
            "runtime_errors_count": runtime_errors
        }


def x_run_tests__mutmut_136(project_dir):
    test_cmd = ["go", "test", "-timeout", "30s", "-json", "-cover", "./..."]
    runtime_errors = 0

    print("Running tests...")
    try:
        result = subprocess.run(
            test_cmd,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            cwd=project_dir,
            text=True
        )
        output = result.stdout
        print(output)
        stderr = result.stderr
        print(stderr)

        # Check if the subprocess exited with a non-zero return code
        if result.returncode != 0:
            # Parse output for timeout messages
            if "test timed out after" in output or "panic: test timed out" in output:
                print("Tests failed due to timeout")
                # Handle timeout scenario
                return {
                    "test_pass_rate": 0,
                    "execution_time_sec": None,
                    "line_coverage_percent": None,
                    "branch_coverage_percent": None,
                    "timeout": True,
                    "internal_error_occurred": False,
                    "runtime_errors_count": None
                }
            else:
                runtime_error_pattern = re.compile(
                    r"(panic:|fatal error:|runtime error:)", re.IGNORECASE
                )
                assertion_error_pattern = re.compile(r"assert")

                # Search for runtime errors in stdout and stderr
                for o in (output, stderr):
                    for line in o.splitlines():
                        # Match lines with runtime errors but exclude assertions
                        if runtime_error_pattern.search(line) and not assertion_error_pattern.search(line):
                            runtime_errors = runtime_errors + 1

        # Parse the JSON output
        passed_tests = 0
        failed_tests = 0
        total_time = 0.0
        coverage = None
        summary_time = None

        for line in output.splitlines():
            if not line.strip():
                continue
            try:
                data = json.loads(line)
            except json.JSONDecodeError:
                continue  # Skip lines that aren't valid JSON

            # Check for test results
            if data.get('Action') == 'pass' and 'Test' in data:
                passed_tests += 1
                total_time += data.get('Elapsed', 0)
            elif data.get('Action') == 'fail' and 'Test' in data:
                failed_tests += 1
                total_time += data.get('Elapsed', 0)

            if (data.get('Action') == 'pass' or data.get('Action') == 'fail') and 'Test' not in data and 'Elapsed' in data:
                summary_time = data["Elapsed"]

            # Check for coverage information
            if data.get('Action') == 'output' and 'Output' in data:
                output_line = data['Output'].strip()
                if output_line.startswith("coverage:"):
                    # Extract the coverage percentage
                    match = re.search(r'coverage: ([\d\.]+)%', output_line)
                    if match:
                        coverage = float(match.group(1))

            # Alternative: Some versions include 'Coverage' field
            elif data.get('Action') == 'pass' and 'Coverage' in data:
                coverage = data['Coverage']

        total_tests = passed_tests + failed_tests
        pass_percentage = round((passed_tests / total_tests) / 100, 2) if total_tests > 0 else 0

        v = {
            "test_pass_rate": pass_percentage,
            "execution_time_sec": summary_time if summary_time is not None else total_time,
            "line_coverage_percent": coverage,
            "branch_coverage_percent": None,
            "timeout": False,
            "internal_error_occurred": False,
            "runtime_errors_count": runtime_errors
        }
        return v
    except Exception as e:
        print(f"Error running Go tests: {e}")
        return {
            "test_pass_rate": 0,
            "execution_time_sec": None,
            "line_coverage_percent": None,
            "branch_coverage_percent": None,
            "timeout": True,
            "internal_error_occurred": True,
            "runtime_errors_count": runtime_errors
        }


def x_run_tests__mutmut_137(project_dir):
    test_cmd = ["go", "test", "-timeout", "30s", "-json", "-cover", "./..."]
    runtime_errors = 0

    print("Running tests...")
    try:
        result = subprocess.run(
            test_cmd,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            cwd=project_dir,
            text=True
        )
        output = result.stdout
        print(output)
        stderr = result.stderr
        print(stderr)

        # Check if the subprocess exited with a non-zero return code
        if result.returncode != 0:
            # Parse output for timeout messages
            if "test timed out after" in output or "panic: test timed out" in output:
                print("Tests failed due to timeout")
                # Handle timeout scenario
                return {
                    "test_pass_rate": 0,
                    "execution_time_sec": None,
                    "line_coverage_percent": None,
                    "branch_coverage_percent": None,
                    "timeout": True,
                    "internal_error_occurred": False,
                    "runtime_errors_count": None
                }
            else:
                runtime_error_pattern = re.compile(
                    r"(panic:|fatal error:|runtime error:)", re.IGNORECASE
                )
                assertion_error_pattern = re.compile(r"assert")

                # Search for runtime errors in stdout and stderr
                for o in (output, stderr):
                    for line in o.splitlines():
                        # Match lines with runtime errors but exclude assertions
                        if runtime_error_pattern.search(line) and not assertion_error_pattern.search(line):
                            runtime_errors = runtime_errors + 1

        # Parse the JSON output
        passed_tests = 0
        failed_tests = 0
        total_time = 0.0
        coverage = None
        summary_time = None

        for line in output.splitlines():
            if not line.strip():
                continue
            try:
                data = json.loads(line)
            except json.JSONDecodeError:
                continue  # Skip lines that aren't valid JSON

            # Check for test results
            if data.get('Action') == 'pass' and 'Test' in data:
                passed_tests += 1
                total_time += data.get('Elapsed', 0)
            elif data.get('Action') == 'fail' and 'Test' in data:
                failed_tests += 1
                total_time += data.get('Elapsed', 0)

            if (data.get('Action') == 'pass' or data.get('Action') == 'fail') and 'Test' not in data and 'Elapsed' in data:
                summary_time = data["Elapsed"]

            # Check for coverage information
            if data.get('Action') == 'output' and 'Output' in data:
                output_line = data['Output'].strip()
                if output_line.startswith("coverage:"):
                    # Extract the coverage percentage
                    match = re.search(r'coverage: ([\d\.]+)%', output_line)
                    if match:
                        coverage = float(match.group(1))

            # Alternative: Some versions include 'Coverage' field
            elif data.get('Action') == 'pass' and 'Coverage' in data:
                coverage = data['Coverage']

        total_tests = passed_tests + failed_tests
        pass_percentage = round((passed_tests / total_tests) * 101, 2) if total_tests > 0 else 0

        v = {
            "test_pass_rate": pass_percentage,
            "execution_time_sec": summary_time if summary_time is not None else total_time,
            "line_coverage_percent": coverage,
            "branch_coverage_percent": None,
            "timeout": False,
            "internal_error_occurred": False,
            "runtime_errors_count": runtime_errors
        }
        return v
    except Exception as e:
        print(f"Error running Go tests: {e}")
        return {
            "test_pass_rate": 0,
            "execution_time_sec": None,
            "line_coverage_percent": None,
            "branch_coverage_percent": None,
            "timeout": True,
            "internal_error_occurred": True,
            "runtime_errors_count": runtime_errors
        }


def x_run_tests__mutmut_138(project_dir):
    test_cmd = ["go", "test", "-timeout", "30s", "-json", "-cover", "./..."]
    runtime_errors = 0

    print("Running tests...")
    try:
        result = subprocess.run(
            test_cmd,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            cwd=project_dir,
            text=True
        )
        output = result.stdout
        print(output)
        stderr = result.stderr
        print(stderr)

        # Check if the subprocess exited with a non-zero return code
        if result.returncode != 0:
            # Parse output for timeout messages
            if "test timed out after" in output or "panic: test timed out" in output:
                print("Tests failed due to timeout")
                # Handle timeout scenario
                return {
                    "test_pass_rate": 0,
                    "execution_time_sec": None,
                    "line_coverage_percent": None,
                    "branch_coverage_percent": None,
                    "timeout": True,
                    "internal_error_occurred": False,
                    "runtime_errors_count": None
                }
            else:
                runtime_error_pattern = re.compile(
                    r"(panic:|fatal error:|runtime error:)", re.IGNORECASE
                )
                assertion_error_pattern = re.compile(r"assert")

                # Search for runtime errors in stdout and stderr
                for o in (output, stderr):
                    for line in o.splitlines():
                        # Match lines with runtime errors but exclude assertions
                        if runtime_error_pattern.search(line) and not assertion_error_pattern.search(line):
                            runtime_errors = runtime_errors + 1

        # Parse the JSON output
        passed_tests = 0
        failed_tests = 0
        total_time = 0.0
        coverage = None
        summary_time = None

        for line in output.splitlines():
            if not line.strip():
                continue
            try:
                data = json.loads(line)
            except json.JSONDecodeError:
                continue  # Skip lines that aren't valid JSON

            # Check for test results
            if data.get('Action') == 'pass' and 'Test' in data:
                passed_tests += 1
                total_time += data.get('Elapsed', 0)
            elif data.get('Action') == 'fail' and 'Test' in data:
                failed_tests += 1
                total_time += data.get('Elapsed', 0)

            if (data.get('Action') == 'pass' or data.get('Action') == 'fail') and 'Test' not in data and 'Elapsed' in data:
                summary_time = data["Elapsed"]

            # Check for coverage information
            if data.get('Action') == 'output' and 'Output' in data:
                output_line = data['Output'].strip()
                if output_line.startswith("coverage:"):
                    # Extract the coverage percentage
                    match = re.search(r'coverage: ([\d\.]+)%', output_line)
                    if match:
                        coverage = float(match.group(1))

            # Alternative: Some versions include 'Coverage' field
            elif data.get('Action') == 'pass' and 'Coverage' in data:
                coverage = data['Coverage']

        total_tests = passed_tests + failed_tests
        pass_percentage = round((passed_tests / total_tests) * 100, 3) if total_tests > 0 else 0

        v = {
            "test_pass_rate": pass_percentage,
            "execution_time_sec": summary_time if summary_time is not None else total_time,
            "line_coverage_percent": coverage,
            "branch_coverage_percent": None,
            "timeout": False,
            "internal_error_occurred": False,
            "runtime_errors_count": runtime_errors
        }
        return v
    except Exception as e:
        print(f"Error running Go tests: {e}")
        return {
            "test_pass_rate": 0,
            "execution_time_sec": None,
            "line_coverage_percent": None,
            "branch_coverage_percent": None,
            "timeout": True,
            "internal_error_occurred": True,
            "runtime_errors_count": runtime_errors
        }


def x_run_tests__mutmut_139(project_dir):
    test_cmd = ["go", "test", "-timeout", "30s", "-json", "-cover", "./..."]
    runtime_errors = 0

    print("Running tests...")
    try:
        result = subprocess.run(
            test_cmd,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            cwd=project_dir,
            text=True
        )
        output = result.stdout
        print(output)
        stderr = result.stderr
        print(stderr)

        # Check if the subprocess exited with a non-zero return code
        if result.returncode != 0:
            # Parse output for timeout messages
            if "test timed out after" in output or "panic: test timed out" in output:
                print("Tests failed due to timeout")
                # Handle timeout scenario
                return {
                    "test_pass_rate": 0,
                    "execution_time_sec": None,
                    "line_coverage_percent": None,
                    "branch_coverage_percent": None,
                    "timeout": True,
                    "internal_error_occurred": False,
                    "runtime_errors_count": None
                }
            else:
                runtime_error_pattern = re.compile(
                    r"(panic:|fatal error:|runtime error:)", re.IGNORECASE
                )
                assertion_error_pattern = re.compile(r"assert")

                # Search for runtime errors in stdout and stderr
                for o in (output, stderr):
                    for line in o.splitlines():
                        # Match lines with runtime errors but exclude assertions
                        if runtime_error_pattern.search(line) and not assertion_error_pattern.search(line):
                            runtime_errors = runtime_errors + 1

        # Parse the JSON output
        passed_tests = 0
        failed_tests = 0
        total_time = 0.0
        coverage = None
        summary_time = None

        for line in output.splitlines():
            if not line.strip():
                continue
            try:
                data = json.loads(line)
            except json.JSONDecodeError:
                continue  # Skip lines that aren't valid JSON

            # Check for test results
            if data.get('Action') == 'pass' and 'Test' in data:
                passed_tests += 1
                total_time += data.get('Elapsed', 0)
            elif data.get('Action') == 'fail' and 'Test' in data:
                failed_tests += 1
                total_time += data.get('Elapsed', 0)

            if (data.get('Action') == 'pass' or data.get('Action') == 'fail') and 'Test' not in data and 'Elapsed' in data:
                summary_time = data["Elapsed"]

            # Check for coverage information
            if data.get('Action') == 'output' and 'Output' in data:
                output_line = data['Output'].strip()
                if output_line.startswith("coverage:"):
                    # Extract the coverage percentage
                    match = re.search(r'coverage: ([\d\.]+)%', output_line)
                    if match:
                        coverage = float(match.group(1))

            # Alternative: Some versions include 'Coverage' field
            elif data.get('Action') == 'pass' and 'Coverage' in data:
                coverage = data['Coverage']

        total_tests = passed_tests + failed_tests
        pass_percentage = round((passed_tests / total_tests) * 100, 2) if total_tests >= 0 else 0

        v = {
            "test_pass_rate": pass_percentage,
            "execution_time_sec": summary_time if summary_time is not None else total_time,
            "line_coverage_percent": coverage,
            "branch_coverage_percent": None,
            "timeout": False,
            "internal_error_occurred": False,
            "runtime_errors_count": runtime_errors
        }
        return v
    except Exception as e:
        print(f"Error running Go tests: {e}")
        return {
            "test_pass_rate": 0,
            "execution_time_sec": None,
            "line_coverage_percent": None,
            "branch_coverage_percent": None,
            "timeout": True,
            "internal_error_occurred": True,
            "runtime_errors_count": runtime_errors
        }


def x_run_tests__mutmut_140(project_dir):
    test_cmd = ["go", "test", "-timeout", "30s", "-json", "-cover", "./..."]
    runtime_errors = 0

    print("Running tests...")
    try:
        result = subprocess.run(
            test_cmd,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            cwd=project_dir,
            text=True
        )
        output = result.stdout
        print(output)
        stderr = result.stderr
        print(stderr)

        # Check if the subprocess exited with a non-zero return code
        if result.returncode != 0:
            # Parse output for timeout messages
            if "test timed out after" in output or "panic: test timed out" in output:
                print("Tests failed due to timeout")
                # Handle timeout scenario
                return {
                    "test_pass_rate": 0,
                    "execution_time_sec": None,
                    "line_coverage_percent": None,
                    "branch_coverage_percent": None,
                    "timeout": True,
                    "internal_error_occurred": False,
                    "runtime_errors_count": None
                }
            else:
                runtime_error_pattern = re.compile(
                    r"(panic:|fatal error:|runtime error:)", re.IGNORECASE
                )
                assertion_error_pattern = re.compile(r"assert")

                # Search for runtime errors in stdout and stderr
                for o in (output, stderr):
                    for line in o.splitlines():
                        # Match lines with runtime errors but exclude assertions
                        if runtime_error_pattern.search(line) and not assertion_error_pattern.search(line):
                            runtime_errors = runtime_errors + 1

        # Parse the JSON output
        passed_tests = 0
        failed_tests = 0
        total_time = 0.0
        coverage = None
        summary_time = None

        for line in output.splitlines():
            if not line.strip():
                continue
            try:
                data = json.loads(line)
            except json.JSONDecodeError:
                continue  # Skip lines that aren't valid JSON

            # Check for test results
            if data.get('Action') == 'pass' and 'Test' in data:
                passed_tests += 1
                total_time += data.get('Elapsed', 0)
            elif data.get('Action') == 'fail' and 'Test' in data:
                failed_tests += 1
                total_time += data.get('Elapsed', 0)

            if (data.get('Action') == 'pass' or data.get('Action') == 'fail') and 'Test' not in data and 'Elapsed' in data:
                summary_time = data["Elapsed"]

            # Check for coverage information
            if data.get('Action') == 'output' and 'Output' in data:
                output_line = data['Output'].strip()
                if output_line.startswith("coverage:"):
                    # Extract the coverage percentage
                    match = re.search(r'coverage: ([\d\.]+)%', output_line)
                    if match:
                        coverage = float(match.group(1))

            # Alternative: Some versions include 'Coverage' field
            elif data.get('Action') == 'pass' and 'Coverage' in data:
                coverage = data['Coverage']

        total_tests = passed_tests + failed_tests
        pass_percentage = round((passed_tests / total_tests) * 100, 2) if total_tests > 1 else 0

        v = {
            "test_pass_rate": pass_percentage,
            "execution_time_sec": summary_time if summary_time is not None else total_time,
            "line_coverage_percent": coverage,
            "branch_coverage_percent": None,
            "timeout": False,
            "internal_error_occurred": False,
            "runtime_errors_count": runtime_errors
        }
        return v
    except Exception as e:
        print(f"Error running Go tests: {e}")
        return {
            "test_pass_rate": 0,
            "execution_time_sec": None,
            "line_coverage_percent": None,
            "branch_coverage_percent": None,
            "timeout": True,
            "internal_error_occurred": True,
            "runtime_errors_count": runtime_errors
        }


def x_run_tests__mutmut_141(project_dir):
    test_cmd = ["go", "test", "-timeout", "30s", "-json", "-cover", "./..."]
    runtime_errors = 0

    print("Running tests...")
    try:
        result = subprocess.run(
            test_cmd,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            cwd=project_dir,
            text=True
        )
        output = result.stdout
        print(output)
        stderr = result.stderr
        print(stderr)

        # Check if the subprocess exited with a non-zero return code
        if result.returncode != 0:
            # Parse output for timeout messages
            if "test timed out after" in output or "panic: test timed out" in output:
                print("Tests failed due to timeout")
                # Handle timeout scenario
                return {
                    "test_pass_rate": 0,
                    "execution_time_sec": None,
                    "line_coverage_percent": None,
                    "branch_coverage_percent": None,
                    "timeout": True,
                    "internal_error_occurred": False,
                    "runtime_errors_count": None
                }
            else:
                runtime_error_pattern = re.compile(
                    r"(panic:|fatal error:|runtime error:)", re.IGNORECASE
                )
                assertion_error_pattern = re.compile(r"assert")

                # Search for runtime errors in stdout and stderr
                for o in (output, stderr):
                    for line in o.splitlines():
                        # Match lines with runtime errors but exclude assertions
                        if runtime_error_pattern.search(line) and not assertion_error_pattern.search(line):
                            runtime_errors = runtime_errors + 1

        # Parse the JSON output
        passed_tests = 0
        failed_tests = 0
        total_time = 0.0
        coverage = None
        summary_time = None

        for line in output.splitlines():
            if not line.strip():
                continue
            try:
                data = json.loads(line)
            except json.JSONDecodeError:
                continue  # Skip lines that aren't valid JSON

            # Check for test results
            if data.get('Action') == 'pass' and 'Test' in data:
                passed_tests += 1
                total_time += data.get('Elapsed', 0)
            elif data.get('Action') == 'fail' and 'Test' in data:
                failed_tests += 1
                total_time += data.get('Elapsed', 0)

            if (data.get('Action') == 'pass' or data.get('Action') == 'fail') and 'Test' not in data and 'Elapsed' in data:
                summary_time = data["Elapsed"]

            # Check for coverage information
            if data.get('Action') == 'output' and 'Output' in data:
                output_line = data['Output'].strip()
                if output_line.startswith("coverage:"):
                    # Extract the coverage percentage
                    match = re.search(r'coverage: ([\d\.]+)%', output_line)
                    if match:
                        coverage = float(match.group(1))

            # Alternative: Some versions include 'Coverage' field
            elif data.get('Action') == 'pass' and 'Coverage' in data:
                coverage = data['Coverage']

        total_tests = passed_tests + failed_tests
        pass_percentage = round((passed_tests / total_tests) * 100, 2) if total_tests > 0 else 1

        v = {
            "test_pass_rate": pass_percentage,
            "execution_time_sec": summary_time if summary_time is not None else total_time,
            "line_coverage_percent": coverage,
            "branch_coverage_percent": None,
            "timeout": False,
            "internal_error_occurred": False,
            "runtime_errors_count": runtime_errors
        }
        return v
    except Exception as e:
        print(f"Error running Go tests: {e}")
        return {
            "test_pass_rate": 0,
            "execution_time_sec": None,
            "line_coverage_percent": None,
            "branch_coverage_percent": None,
            "timeout": True,
            "internal_error_occurred": True,
            "runtime_errors_count": runtime_errors
        }


def x_run_tests__mutmut_142(project_dir):
    test_cmd = ["go", "test", "-timeout", "30s", "-json", "-cover", "./..."]
    runtime_errors = 0

    print("Running tests...")
    try:
        result = subprocess.run(
            test_cmd,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            cwd=project_dir,
            text=True
        )
        output = result.stdout
        print(output)
        stderr = result.stderr
        print(stderr)

        # Check if the subprocess exited with a non-zero return code
        if result.returncode != 0:
            # Parse output for timeout messages
            if "test timed out after" in output or "panic: test timed out" in output:
                print("Tests failed due to timeout")
                # Handle timeout scenario
                return {
                    "test_pass_rate": 0,
                    "execution_time_sec": None,
                    "line_coverage_percent": None,
                    "branch_coverage_percent": None,
                    "timeout": True,
                    "internal_error_occurred": False,
                    "runtime_errors_count": None
                }
            else:
                runtime_error_pattern = re.compile(
                    r"(panic:|fatal error:|runtime error:)", re.IGNORECASE
                )
                assertion_error_pattern = re.compile(r"assert")

                # Search for runtime errors in stdout and stderr
                for o in (output, stderr):
                    for line in o.splitlines():
                        # Match lines with runtime errors but exclude assertions
                        if runtime_error_pattern.search(line) and not assertion_error_pattern.search(line):
                            runtime_errors = runtime_errors + 1

        # Parse the JSON output
        passed_tests = 0
        failed_tests = 0
        total_time = 0.0
        coverage = None
        summary_time = None

        for line in output.splitlines():
            if not line.strip():
                continue
            try:
                data = json.loads(line)
            except json.JSONDecodeError:
                continue  # Skip lines that aren't valid JSON

            # Check for test results
            if data.get('Action') == 'pass' and 'Test' in data:
                passed_tests += 1
                total_time += data.get('Elapsed', 0)
            elif data.get('Action') == 'fail' and 'Test' in data:
                failed_tests += 1
                total_time += data.get('Elapsed', 0)

            if (data.get('Action') == 'pass' or data.get('Action') == 'fail') and 'Test' not in data and 'Elapsed' in data:
                summary_time = data["Elapsed"]

            # Check for coverage information
            if data.get('Action') == 'output' and 'Output' in data:
                output_line = data['Output'].strip()
                if output_line.startswith("coverage:"):
                    # Extract the coverage percentage
                    match = re.search(r'coverage: ([\d\.]+)%', output_line)
                    if match:
                        coverage = float(match.group(1))

            # Alternative: Some versions include 'Coverage' field
            elif data.get('Action') == 'pass' and 'Coverage' in data:
                coverage = data['Coverage']

        total_tests = passed_tests + failed_tests
        pass_percentage = None

        v = {
            "test_pass_rate": pass_percentage,
            "execution_time_sec": summary_time if summary_time is not None else total_time,
            "line_coverage_percent": coverage,
            "branch_coverage_percent": None,
            "timeout": False,
            "internal_error_occurred": False,
            "runtime_errors_count": runtime_errors
        }
        return v
    except Exception as e:
        print(f"Error running Go tests: {e}")
        return {
            "test_pass_rate": 0,
            "execution_time_sec": None,
            "line_coverage_percent": None,
            "branch_coverage_percent": None,
            "timeout": True,
            "internal_error_occurred": True,
            "runtime_errors_count": runtime_errors
        }


def x_run_tests__mutmut_143(project_dir):
    test_cmd = ["go", "test", "-timeout", "30s", "-json", "-cover", "./..."]
    runtime_errors = 0

    print("Running tests...")
    try:
        result = subprocess.run(
            test_cmd,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            cwd=project_dir,
            text=True
        )
        output = result.stdout
        print(output)
        stderr = result.stderr
        print(stderr)

        # Check if the subprocess exited with a non-zero return code
        if result.returncode != 0:
            # Parse output for timeout messages
            if "test timed out after" in output or "panic: test timed out" in output:
                print("Tests failed due to timeout")
                # Handle timeout scenario
                return {
                    "test_pass_rate": 0,
                    "execution_time_sec": None,
                    "line_coverage_percent": None,
                    "branch_coverage_percent": None,
                    "timeout": True,
                    "internal_error_occurred": False,
                    "runtime_errors_count": None
                }
            else:
                runtime_error_pattern = re.compile(
                    r"(panic:|fatal error:|runtime error:)", re.IGNORECASE
                )
                assertion_error_pattern = re.compile(r"assert")

                # Search for runtime errors in stdout and stderr
                for o in (output, stderr):
                    for line in o.splitlines():
                        # Match lines with runtime errors but exclude assertions
                        if runtime_error_pattern.search(line) and not assertion_error_pattern.search(line):
                            runtime_errors = runtime_errors + 1

        # Parse the JSON output
        passed_tests = 0
        failed_tests = 0
        total_time = 0.0
        coverage = None
        summary_time = None

        for line in output.splitlines():
            if not line.strip():
                continue
            try:
                data = json.loads(line)
            except json.JSONDecodeError:
                continue  # Skip lines that aren't valid JSON

            # Check for test results
            if data.get('Action') == 'pass' and 'Test' in data:
                passed_tests += 1
                total_time += data.get('Elapsed', 0)
            elif data.get('Action') == 'fail' and 'Test' in data:
                failed_tests += 1
                total_time += data.get('Elapsed', 0)

            if (data.get('Action') == 'pass' or data.get('Action') == 'fail') and 'Test' not in data and 'Elapsed' in data:
                summary_time = data["Elapsed"]

            # Check for coverage information
            if data.get('Action') == 'output' and 'Output' in data:
                output_line = data['Output'].strip()
                if output_line.startswith("coverage:"):
                    # Extract the coverage percentage
                    match = re.search(r'coverage: ([\d\.]+)%', output_line)
                    if match:
                        coverage = float(match.group(1))

            # Alternative: Some versions include 'Coverage' field
            elif data.get('Action') == 'pass' and 'Coverage' in data:
                coverage = data['Coverage']

        total_tests = passed_tests + failed_tests
        pass_percentage = round((passed_tests / total_tests) * 100, 2) if total_tests > 0 else 0

        v = {
            "XXtest_pass_rateXX": pass_percentage,
            "execution_time_sec": summary_time if summary_time is not None else total_time,
            "line_coverage_percent": coverage,
            "branch_coverage_percent": None,
            "timeout": False,
            "internal_error_occurred": False,
            "runtime_errors_count": runtime_errors
        }
        return v
    except Exception as e:
        print(f"Error running Go tests: {e}")
        return {
            "test_pass_rate": 0,
            "execution_time_sec": None,
            "line_coverage_percent": None,
            "branch_coverage_percent": None,
            "timeout": True,
            "internal_error_occurred": True,
            "runtime_errors_count": runtime_errors
        }


def x_run_tests__mutmut_144(project_dir):
    test_cmd = ["go", "test", "-timeout", "30s", "-json", "-cover", "./..."]
    runtime_errors = 0

    print("Running tests...")
    try:
        result = subprocess.run(
            test_cmd,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            cwd=project_dir,
            text=True
        )
        output = result.stdout
        print(output)
        stderr = result.stderr
        print(stderr)

        # Check if the subprocess exited with a non-zero return code
        if result.returncode != 0:
            # Parse output for timeout messages
            if "test timed out after" in output or "panic: test timed out" in output:
                print("Tests failed due to timeout")
                # Handle timeout scenario
                return {
                    "test_pass_rate": 0,
                    "execution_time_sec": None,
                    "line_coverage_percent": None,
                    "branch_coverage_percent": None,
                    "timeout": True,
                    "internal_error_occurred": False,
                    "runtime_errors_count": None
                }
            else:
                runtime_error_pattern = re.compile(
                    r"(panic:|fatal error:|runtime error:)", re.IGNORECASE
                )
                assertion_error_pattern = re.compile(r"assert")

                # Search for runtime errors in stdout and stderr
                for o in (output, stderr):
                    for line in o.splitlines():
                        # Match lines with runtime errors but exclude assertions
                        if runtime_error_pattern.search(line) and not assertion_error_pattern.search(line):
                            runtime_errors = runtime_errors + 1

        # Parse the JSON output
        passed_tests = 0
        failed_tests = 0
        total_time = 0.0
        coverage = None
        summary_time = None

        for line in output.splitlines():
            if not line.strip():
                continue
            try:
                data = json.loads(line)
            except json.JSONDecodeError:
                continue  # Skip lines that aren't valid JSON

            # Check for test results
            if data.get('Action') == 'pass' and 'Test' in data:
                passed_tests += 1
                total_time += data.get('Elapsed', 0)
            elif data.get('Action') == 'fail' and 'Test' in data:
                failed_tests += 1
                total_time += data.get('Elapsed', 0)

            if (data.get('Action') == 'pass' or data.get('Action') == 'fail') and 'Test' not in data and 'Elapsed' in data:
                summary_time = data["Elapsed"]

            # Check for coverage information
            if data.get('Action') == 'output' and 'Output' in data:
                output_line = data['Output'].strip()
                if output_line.startswith("coverage:"):
                    # Extract the coverage percentage
                    match = re.search(r'coverage: ([\d\.]+)%', output_line)
                    if match:
                        coverage = float(match.group(1))

            # Alternative: Some versions include 'Coverage' field
            elif data.get('Action') == 'pass' and 'Coverage' in data:
                coverage = data['Coverage']

        total_tests = passed_tests + failed_tests
        pass_percentage = round((passed_tests / total_tests) * 100, 2) if total_tests > 0 else 0

        v = {
            "test_pass_rate": pass_percentage,
            "XXexecution_time_secXX": summary_time if summary_time is not None else total_time,
            "line_coverage_percent": coverage,
            "branch_coverage_percent": None,
            "timeout": False,
            "internal_error_occurred": False,
            "runtime_errors_count": runtime_errors
        }
        return v
    except Exception as e:
        print(f"Error running Go tests: {e}")
        return {
            "test_pass_rate": 0,
            "execution_time_sec": None,
            "line_coverage_percent": None,
            "branch_coverage_percent": None,
            "timeout": True,
            "internal_error_occurred": True,
            "runtime_errors_count": runtime_errors
        }


def x_run_tests__mutmut_145(project_dir):
    test_cmd = ["go", "test", "-timeout", "30s", "-json", "-cover", "./..."]
    runtime_errors = 0

    print("Running tests...")
    try:
        result = subprocess.run(
            test_cmd,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            cwd=project_dir,
            text=True
        )
        output = result.stdout
        print(output)
        stderr = result.stderr
        print(stderr)

        # Check if the subprocess exited with a non-zero return code
        if result.returncode != 0:
            # Parse output for timeout messages
            if "test timed out after" in output or "panic: test timed out" in output:
                print("Tests failed due to timeout")
                # Handle timeout scenario
                return {
                    "test_pass_rate": 0,
                    "execution_time_sec": None,
                    "line_coverage_percent": None,
                    "branch_coverage_percent": None,
                    "timeout": True,
                    "internal_error_occurred": False,
                    "runtime_errors_count": None
                }
            else:
                runtime_error_pattern = re.compile(
                    r"(panic:|fatal error:|runtime error:)", re.IGNORECASE
                )
                assertion_error_pattern = re.compile(r"assert")

                # Search for runtime errors in stdout and stderr
                for o in (output, stderr):
                    for line in o.splitlines():
                        # Match lines with runtime errors but exclude assertions
                        if runtime_error_pattern.search(line) and not assertion_error_pattern.search(line):
                            runtime_errors = runtime_errors + 1

        # Parse the JSON output
        passed_tests = 0
        failed_tests = 0
        total_time = 0.0
        coverage = None
        summary_time = None

        for line in output.splitlines():
            if not line.strip():
                continue
            try:
                data = json.loads(line)
            except json.JSONDecodeError:
                continue  # Skip lines that aren't valid JSON

            # Check for test results
            if data.get('Action') == 'pass' and 'Test' in data:
                passed_tests += 1
                total_time += data.get('Elapsed', 0)
            elif data.get('Action') == 'fail' and 'Test' in data:
                failed_tests += 1
                total_time += data.get('Elapsed', 0)

            if (data.get('Action') == 'pass' or data.get('Action') == 'fail') and 'Test' not in data and 'Elapsed' in data:
                summary_time = data["Elapsed"]

            # Check for coverage information
            if data.get('Action') == 'output' and 'Output' in data:
                output_line = data['Output'].strip()
                if output_line.startswith("coverage:"):
                    # Extract the coverage percentage
                    match = re.search(r'coverage: ([\d\.]+)%', output_line)
                    if match:
                        coverage = float(match.group(1))

            # Alternative: Some versions include 'Coverage' field
            elif data.get('Action') == 'pass' and 'Coverage' in data:
                coverage = data['Coverage']

        total_tests = passed_tests + failed_tests
        pass_percentage = round((passed_tests / total_tests) * 100, 2) if total_tests > 0 else 0

        v = {
            "test_pass_rate": pass_percentage,
            "execution_time_sec": summary_time if summary_time is  None else total_time,
            "line_coverage_percent": coverage,
            "branch_coverage_percent": None,
            "timeout": False,
            "internal_error_occurred": False,
            "runtime_errors_count": runtime_errors
        }
        return v
    except Exception as e:
        print(f"Error running Go tests: {e}")
        return {
            "test_pass_rate": 0,
            "execution_time_sec": None,
            "line_coverage_percent": None,
            "branch_coverage_percent": None,
            "timeout": True,
            "internal_error_occurred": True,
            "runtime_errors_count": runtime_errors
        }


def x_run_tests__mutmut_146(project_dir):
    test_cmd = ["go", "test", "-timeout", "30s", "-json", "-cover", "./..."]
    runtime_errors = 0

    print("Running tests...")
    try:
        result = subprocess.run(
            test_cmd,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            cwd=project_dir,
            text=True
        )
        output = result.stdout
        print(output)
        stderr = result.stderr
        print(stderr)

        # Check if the subprocess exited with a non-zero return code
        if result.returncode != 0:
            # Parse output for timeout messages
            if "test timed out after" in output or "panic: test timed out" in output:
                print("Tests failed due to timeout")
                # Handle timeout scenario
                return {
                    "test_pass_rate": 0,
                    "execution_time_sec": None,
                    "line_coverage_percent": None,
                    "branch_coverage_percent": None,
                    "timeout": True,
                    "internal_error_occurred": False,
                    "runtime_errors_count": None
                }
            else:
                runtime_error_pattern = re.compile(
                    r"(panic:|fatal error:|runtime error:)", re.IGNORECASE
                )
                assertion_error_pattern = re.compile(r"assert")

                # Search for runtime errors in stdout and stderr
                for o in (output, stderr):
                    for line in o.splitlines():
                        # Match lines with runtime errors but exclude assertions
                        if runtime_error_pattern.search(line) and not assertion_error_pattern.search(line):
                            runtime_errors = runtime_errors + 1

        # Parse the JSON output
        passed_tests = 0
        failed_tests = 0
        total_time = 0.0
        coverage = None
        summary_time = None

        for line in output.splitlines():
            if not line.strip():
                continue
            try:
                data = json.loads(line)
            except json.JSONDecodeError:
                continue  # Skip lines that aren't valid JSON

            # Check for test results
            if data.get('Action') == 'pass' and 'Test' in data:
                passed_tests += 1
                total_time += data.get('Elapsed', 0)
            elif data.get('Action') == 'fail' and 'Test' in data:
                failed_tests += 1
                total_time += data.get('Elapsed', 0)

            if (data.get('Action') == 'pass' or data.get('Action') == 'fail') and 'Test' not in data and 'Elapsed' in data:
                summary_time = data["Elapsed"]

            # Check for coverage information
            if data.get('Action') == 'output' and 'Output' in data:
                output_line = data['Output'].strip()
                if output_line.startswith("coverage:"):
                    # Extract the coverage percentage
                    match = re.search(r'coverage: ([\d\.]+)%', output_line)
                    if match:
                        coverage = float(match.group(1))

            # Alternative: Some versions include 'Coverage' field
            elif data.get('Action') == 'pass' and 'Coverage' in data:
                coverage = data['Coverage']

        total_tests = passed_tests + failed_tests
        pass_percentage = round((passed_tests / total_tests) * 100, 2) if total_tests > 0 else 0

        v = {
            "test_pass_rate": pass_percentage,
            "execution_time_sec": summary_time if summary_time is not None else total_time,
            "XXline_coverage_percentXX": coverage,
            "branch_coverage_percent": None,
            "timeout": False,
            "internal_error_occurred": False,
            "runtime_errors_count": runtime_errors
        }
        return v
    except Exception as e:
        print(f"Error running Go tests: {e}")
        return {
            "test_pass_rate": 0,
            "execution_time_sec": None,
            "line_coverage_percent": None,
            "branch_coverage_percent": None,
            "timeout": True,
            "internal_error_occurred": True,
            "runtime_errors_count": runtime_errors
        }


def x_run_tests__mutmut_147(project_dir):
    test_cmd = ["go", "test", "-timeout", "30s", "-json", "-cover", "./..."]
    runtime_errors = 0

    print("Running tests...")
    try:
        result = subprocess.run(
            test_cmd,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            cwd=project_dir,
            text=True
        )
        output = result.stdout
        print(output)
        stderr = result.stderr
        print(stderr)

        # Check if the subprocess exited with a non-zero return code
        if result.returncode != 0:
            # Parse output for timeout messages
            if "test timed out after" in output or "panic: test timed out" in output:
                print("Tests failed due to timeout")
                # Handle timeout scenario
                return {
                    "test_pass_rate": 0,
                    "execution_time_sec": None,
                    "line_coverage_percent": None,
                    "branch_coverage_percent": None,
                    "timeout": True,
                    "internal_error_occurred": False,
                    "runtime_errors_count": None
                }
            else:
                runtime_error_pattern = re.compile(
                    r"(panic:|fatal error:|runtime error:)", re.IGNORECASE
                )
                assertion_error_pattern = re.compile(r"assert")

                # Search for runtime errors in stdout and stderr
                for o in (output, stderr):
                    for line in o.splitlines():
                        # Match lines with runtime errors but exclude assertions
                        if runtime_error_pattern.search(line) and not assertion_error_pattern.search(line):
                            runtime_errors = runtime_errors + 1

        # Parse the JSON output
        passed_tests = 0
        failed_tests = 0
        total_time = 0.0
        coverage = None
        summary_time = None

        for line in output.splitlines():
            if not line.strip():
                continue
            try:
                data = json.loads(line)
            except json.JSONDecodeError:
                continue  # Skip lines that aren't valid JSON

            # Check for test results
            if data.get('Action') == 'pass' and 'Test' in data:
                passed_tests += 1
                total_time += data.get('Elapsed', 0)
            elif data.get('Action') == 'fail' and 'Test' in data:
                failed_tests += 1
                total_time += data.get('Elapsed', 0)

            if (data.get('Action') == 'pass' or data.get('Action') == 'fail') and 'Test' not in data and 'Elapsed' in data:
                summary_time = data["Elapsed"]

            # Check for coverage information
            if data.get('Action') == 'output' and 'Output' in data:
                output_line = data['Output'].strip()
                if output_line.startswith("coverage:"):
                    # Extract the coverage percentage
                    match = re.search(r'coverage: ([\d\.]+)%', output_line)
                    if match:
                        coverage = float(match.group(1))

            # Alternative: Some versions include 'Coverage' field
            elif data.get('Action') == 'pass' and 'Coverage' in data:
                coverage = data['Coverage']

        total_tests = passed_tests + failed_tests
        pass_percentage = round((passed_tests / total_tests) * 100, 2) if total_tests > 0 else 0

        v = {
            "test_pass_rate": pass_percentage,
            "execution_time_sec": summary_time if summary_time is not None else total_time,
            "line_coverage_percent": coverage,
            "XXbranch_coverage_percentXX": None,
            "timeout": False,
            "internal_error_occurred": False,
            "runtime_errors_count": runtime_errors
        }
        return v
    except Exception as e:
        print(f"Error running Go tests: {e}")
        return {
            "test_pass_rate": 0,
            "execution_time_sec": None,
            "line_coverage_percent": None,
            "branch_coverage_percent": None,
            "timeout": True,
            "internal_error_occurred": True,
            "runtime_errors_count": runtime_errors
        }


def x_run_tests__mutmut_148(project_dir):
    test_cmd = ["go", "test", "-timeout", "30s", "-json", "-cover", "./..."]
    runtime_errors = 0

    print("Running tests...")
    try:
        result = subprocess.run(
            test_cmd,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            cwd=project_dir,
            text=True
        )
        output = result.stdout
        print(output)
        stderr = result.stderr
        print(stderr)

        # Check if the subprocess exited with a non-zero return code
        if result.returncode != 0:
            # Parse output for timeout messages
            if "test timed out after" in output or "panic: test timed out" in output:
                print("Tests failed due to timeout")
                # Handle timeout scenario
                return {
                    "test_pass_rate": 0,
                    "execution_time_sec": None,
                    "line_coverage_percent": None,
                    "branch_coverage_percent": None,
                    "timeout": True,
                    "internal_error_occurred": False,
                    "runtime_errors_count": None
                }
            else:
                runtime_error_pattern = re.compile(
                    r"(panic:|fatal error:|runtime error:)", re.IGNORECASE
                )
                assertion_error_pattern = re.compile(r"assert")

                # Search for runtime errors in stdout and stderr
                for o in (output, stderr):
                    for line in o.splitlines():
                        # Match lines with runtime errors but exclude assertions
                        if runtime_error_pattern.search(line) and not assertion_error_pattern.search(line):
                            runtime_errors = runtime_errors + 1

        # Parse the JSON output
        passed_tests = 0
        failed_tests = 0
        total_time = 0.0
        coverage = None
        summary_time = None

        for line in output.splitlines():
            if not line.strip():
                continue
            try:
                data = json.loads(line)
            except json.JSONDecodeError:
                continue  # Skip lines that aren't valid JSON

            # Check for test results
            if data.get('Action') == 'pass' and 'Test' in data:
                passed_tests += 1
                total_time += data.get('Elapsed', 0)
            elif data.get('Action') == 'fail' and 'Test' in data:
                failed_tests += 1
                total_time += data.get('Elapsed', 0)

            if (data.get('Action') == 'pass' or data.get('Action') == 'fail') and 'Test' not in data and 'Elapsed' in data:
                summary_time = data["Elapsed"]

            # Check for coverage information
            if data.get('Action') == 'output' and 'Output' in data:
                output_line = data['Output'].strip()
                if output_line.startswith("coverage:"):
                    # Extract the coverage percentage
                    match = re.search(r'coverage: ([\d\.]+)%', output_line)
                    if match:
                        coverage = float(match.group(1))

            # Alternative: Some versions include 'Coverage' field
            elif data.get('Action') == 'pass' and 'Coverage' in data:
                coverage = data['Coverage']

        total_tests = passed_tests + failed_tests
        pass_percentage = round((passed_tests / total_tests) * 100, 2) if total_tests > 0 else 0

        v = {
            "test_pass_rate": pass_percentage,
            "execution_time_sec": summary_time if summary_time is not None else total_time,
            "line_coverage_percent": coverage,
            "branch_coverage_percent": None,
            "XXtimeoutXX": False,
            "internal_error_occurred": False,
            "runtime_errors_count": runtime_errors
        }
        return v
    except Exception as e:
        print(f"Error running Go tests: {e}")
        return {
            "test_pass_rate": 0,
            "execution_time_sec": None,
            "line_coverage_percent": None,
            "branch_coverage_percent": None,
            "timeout": True,
            "internal_error_occurred": True,
            "runtime_errors_count": runtime_errors
        }


def x_run_tests__mutmut_149(project_dir):
    test_cmd = ["go", "test", "-timeout", "30s", "-json", "-cover", "./..."]
    runtime_errors = 0

    print("Running tests...")
    try:
        result = subprocess.run(
            test_cmd,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            cwd=project_dir,
            text=True
        )
        output = result.stdout
        print(output)
        stderr = result.stderr
        print(stderr)

        # Check if the subprocess exited with a non-zero return code
        if result.returncode != 0:
            # Parse output for timeout messages
            if "test timed out after" in output or "panic: test timed out" in output:
                print("Tests failed due to timeout")
                # Handle timeout scenario
                return {
                    "test_pass_rate": 0,
                    "execution_time_sec": None,
                    "line_coverage_percent": None,
                    "branch_coverage_percent": None,
                    "timeout": True,
                    "internal_error_occurred": False,
                    "runtime_errors_count": None
                }
            else:
                runtime_error_pattern = re.compile(
                    r"(panic:|fatal error:|runtime error:)", re.IGNORECASE
                )
                assertion_error_pattern = re.compile(r"assert")

                # Search for runtime errors in stdout and stderr
                for o in (output, stderr):
                    for line in o.splitlines():
                        # Match lines with runtime errors but exclude assertions
                        if runtime_error_pattern.search(line) and not assertion_error_pattern.search(line):
                            runtime_errors = runtime_errors + 1

        # Parse the JSON output
        passed_tests = 0
        failed_tests = 0
        total_time = 0.0
        coverage = None
        summary_time = None

        for line in output.splitlines():
            if not line.strip():
                continue
            try:
                data = json.loads(line)
            except json.JSONDecodeError:
                continue  # Skip lines that aren't valid JSON

            # Check for test results
            if data.get('Action') == 'pass' and 'Test' in data:
                passed_tests += 1
                total_time += data.get('Elapsed', 0)
            elif data.get('Action') == 'fail' and 'Test' in data:
                failed_tests += 1
                total_time += data.get('Elapsed', 0)

            if (data.get('Action') == 'pass' or data.get('Action') == 'fail') and 'Test' not in data and 'Elapsed' in data:
                summary_time = data["Elapsed"]

            # Check for coverage information
            if data.get('Action') == 'output' and 'Output' in data:
                output_line = data['Output'].strip()
                if output_line.startswith("coverage:"):
                    # Extract the coverage percentage
                    match = re.search(r'coverage: ([\d\.]+)%', output_line)
                    if match:
                        coverage = float(match.group(1))

            # Alternative: Some versions include 'Coverage' field
            elif data.get('Action') == 'pass' and 'Coverage' in data:
                coverage = data['Coverage']

        total_tests = passed_tests + failed_tests
        pass_percentage = round((passed_tests / total_tests) * 100, 2) if total_tests > 0 else 0

        v = {
            "test_pass_rate": pass_percentage,
            "execution_time_sec": summary_time if summary_time is not None else total_time,
            "line_coverage_percent": coverage,
            "branch_coverage_percent": None,
            "timeout": True,
            "internal_error_occurred": False,
            "runtime_errors_count": runtime_errors
        }
        return v
    except Exception as e:
        print(f"Error running Go tests: {e}")
        return {
            "test_pass_rate": 0,
            "execution_time_sec": None,
            "line_coverage_percent": None,
            "branch_coverage_percent": None,
            "timeout": True,
            "internal_error_occurred": True,
            "runtime_errors_count": runtime_errors
        }


def x_run_tests__mutmut_150(project_dir):
    test_cmd = ["go", "test", "-timeout", "30s", "-json", "-cover", "./..."]
    runtime_errors = 0

    print("Running tests...")
    try:
        result = subprocess.run(
            test_cmd,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            cwd=project_dir,
            text=True
        )
        output = result.stdout
        print(output)
        stderr = result.stderr
        print(stderr)

        # Check if the subprocess exited with a non-zero return code
        if result.returncode != 0:
            # Parse output for timeout messages
            if "test timed out after" in output or "panic: test timed out" in output:
                print("Tests failed due to timeout")
                # Handle timeout scenario
                return {
                    "test_pass_rate": 0,
                    "execution_time_sec": None,
                    "line_coverage_percent": None,
                    "branch_coverage_percent": None,
                    "timeout": True,
                    "internal_error_occurred": False,
                    "runtime_errors_count": None
                }
            else:
                runtime_error_pattern = re.compile(
                    r"(panic:|fatal error:|runtime error:)", re.IGNORECASE
                )
                assertion_error_pattern = re.compile(r"assert")

                # Search for runtime errors in stdout and stderr
                for o in (output, stderr):
                    for line in o.splitlines():
                        # Match lines with runtime errors but exclude assertions
                        if runtime_error_pattern.search(line) and not assertion_error_pattern.search(line):
                            runtime_errors = runtime_errors + 1

        # Parse the JSON output
        passed_tests = 0
        failed_tests = 0
        total_time = 0.0
        coverage = None
        summary_time = None

        for line in output.splitlines():
            if not line.strip():
                continue
            try:
                data = json.loads(line)
            except json.JSONDecodeError:
                continue  # Skip lines that aren't valid JSON

            # Check for test results
            if data.get('Action') == 'pass' and 'Test' in data:
                passed_tests += 1
                total_time += data.get('Elapsed', 0)
            elif data.get('Action') == 'fail' and 'Test' in data:
                failed_tests += 1
                total_time += data.get('Elapsed', 0)

            if (data.get('Action') == 'pass' or data.get('Action') == 'fail') and 'Test' not in data and 'Elapsed' in data:
                summary_time = data["Elapsed"]

            # Check for coverage information
            if data.get('Action') == 'output' and 'Output' in data:
                output_line = data['Output'].strip()
                if output_line.startswith("coverage:"):
                    # Extract the coverage percentage
                    match = re.search(r'coverage: ([\d\.]+)%', output_line)
                    if match:
                        coverage = float(match.group(1))

            # Alternative: Some versions include 'Coverage' field
            elif data.get('Action') == 'pass' and 'Coverage' in data:
                coverage = data['Coverage']

        total_tests = passed_tests + failed_tests
        pass_percentage = round((passed_tests / total_tests) * 100, 2) if total_tests > 0 else 0

        v = {
            "test_pass_rate": pass_percentage,
            "execution_time_sec": summary_time if summary_time is not None else total_time,
            "line_coverage_percent": coverage,
            "branch_coverage_percent": None,
            "timeout": False,
            "XXinternal_error_occurredXX": False,
            "runtime_errors_count": runtime_errors
        }
        return v
    except Exception as e:
        print(f"Error running Go tests: {e}")
        return {
            "test_pass_rate": 0,
            "execution_time_sec": None,
            "line_coverage_percent": None,
            "branch_coverage_percent": None,
            "timeout": True,
            "internal_error_occurred": True,
            "runtime_errors_count": runtime_errors
        }


def x_run_tests__mutmut_151(project_dir):
    test_cmd = ["go", "test", "-timeout", "30s", "-json", "-cover", "./..."]
    runtime_errors = 0

    print("Running tests...")
    try:
        result = subprocess.run(
            test_cmd,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            cwd=project_dir,
            text=True
        )
        output = result.stdout
        print(output)
        stderr = result.stderr
        print(stderr)

        # Check if the subprocess exited with a non-zero return code
        if result.returncode != 0:
            # Parse output for timeout messages
            if "test timed out after" in output or "panic: test timed out" in output:
                print("Tests failed due to timeout")
                # Handle timeout scenario
                return {
                    "test_pass_rate": 0,
                    "execution_time_sec": None,
                    "line_coverage_percent": None,
                    "branch_coverage_percent": None,
                    "timeout": True,
                    "internal_error_occurred": False,
                    "runtime_errors_count": None
                }
            else:
                runtime_error_pattern = re.compile(
                    r"(panic:|fatal error:|runtime error:)", re.IGNORECASE
                )
                assertion_error_pattern = re.compile(r"assert")

                # Search for runtime errors in stdout and stderr
                for o in (output, stderr):
                    for line in o.splitlines():
                        # Match lines with runtime errors but exclude assertions
                        if runtime_error_pattern.search(line) and not assertion_error_pattern.search(line):
                            runtime_errors = runtime_errors + 1

        # Parse the JSON output
        passed_tests = 0
        failed_tests = 0
        total_time = 0.0
        coverage = None
        summary_time = None

        for line in output.splitlines():
            if not line.strip():
                continue
            try:
                data = json.loads(line)
            except json.JSONDecodeError:
                continue  # Skip lines that aren't valid JSON

            # Check for test results
            if data.get('Action') == 'pass' and 'Test' in data:
                passed_tests += 1
                total_time += data.get('Elapsed', 0)
            elif data.get('Action') == 'fail' and 'Test' in data:
                failed_tests += 1
                total_time += data.get('Elapsed', 0)

            if (data.get('Action') == 'pass' or data.get('Action') == 'fail') and 'Test' not in data and 'Elapsed' in data:
                summary_time = data["Elapsed"]

            # Check for coverage information
            if data.get('Action') == 'output' and 'Output' in data:
                output_line = data['Output'].strip()
                if output_line.startswith("coverage:"):
                    # Extract the coverage percentage
                    match = re.search(r'coverage: ([\d\.]+)%', output_line)
                    if match:
                        coverage = float(match.group(1))

            # Alternative: Some versions include 'Coverage' field
            elif data.get('Action') == 'pass' and 'Coverage' in data:
                coverage = data['Coverage']

        total_tests = passed_tests + failed_tests
        pass_percentage = round((passed_tests / total_tests) * 100, 2) if total_tests > 0 else 0

        v = {
            "test_pass_rate": pass_percentage,
            "execution_time_sec": summary_time if summary_time is not None else total_time,
            "line_coverage_percent": coverage,
            "branch_coverage_percent": None,
            "timeout": False,
            "internal_error_occurred": True,
            "runtime_errors_count": runtime_errors
        }
        return v
    except Exception as e:
        print(f"Error running Go tests: {e}")
        return {
            "test_pass_rate": 0,
            "execution_time_sec": None,
            "line_coverage_percent": None,
            "branch_coverage_percent": None,
            "timeout": True,
            "internal_error_occurred": True,
            "runtime_errors_count": runtime_errors
        }


def x_run_tests__mutmut_152(project_dir):
    test_cmd = ["go", "test", "-timeout", "30s", "-json", "-cover", "./..."]
    runtime_errors = 0

    print("Running tests...")
    try:
        result = subprocess.run(
            test_cmd,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            cwd=project_dir,
            text=True
        )
        output = result.stdout
        print(output)
        stderr = result.stderr
        print(stderr)

        # Check if the subprocess exited with a non-zero return code
        if result.returncode != 0:
            # Parse output for timeout messages
            if "test timed out after" in output or "panic: test timed out" in output:
                print("Tests failed due to timeout")
                # Handle timeout scenario
                return {
                    "test_pass_rate": 0,
                    "execution_time_sec": None,
                    "line_coverage_percent": None,
                    "branch_coverage_percent": None,
                    "timeout": True,
                    "internal_error_occurred": False,
                    "runtime_errors_count": None
                }
            else:
                runtime_error_pattern = re.compile(
                    r"(panic:|fatal error:|runtime error:)", re.IGNORECASE
                )
                assertion_error_pattern = re.compile(r"assert")

                # Search for runtime errors in stdout and stderr
                for o in (output, stderr):
                    for line in o.splitlines():
                        # Match lines with runtime errors but exclude assertions
                        if runtime_error_pattern.search(line) and not assertion_error_pattern.search(line):
                            runtime_errors = runtime_errors + 1

        # Parse the JSON output
        passed_tests = 0
        failed_tests = 0
        total_time = 0.0
        coverage = None
        summary_time = None

        for line in output.splitlines():
            if not line.strip():
                continue
            try:
                data = json.loads(line)
            except json.JSONDecodeError:
                continue  # Skip lines that aren't valid JSON

            # Check for test results
            if data.get('Action') == 'pass' and 'Test' in data:
                passed_tests += 1
                total_time += data.get('Elapsed', 0)
            elif data.get('Action') == 'fail' and 'Test' in data:
                failed_tests += 1
                total_time += data.get('Elapsed', 0)

            if (data.get('Action') == 'pass' or data.get('Action') == 'fail') and 'Test' not in data and 'Elapsed' in data:
                summary_time = data["Elapsed"]

            # Check for coverage information
            if data.get('Action') == 'output' and 'Output' in data:
                output_line = data['Output'].strip()
                if output_line.startswith("coverage:"):
                    # Extract the coverage percentage
                    match = re.search(r'coverage: ([\d\.]+)%', output_line)
                    if match:
                        coverage = float(match.group(1))

            # Alternative: Some versions include 'Coverage' field
            elif data.get('Action') == 'pass' and 'Coverage' in data:
                coverage = data['Coverage']

        total_tests = passed_tests + failed_tests
        pass_percentage = round((passed_tests / total_tests) * 100, 2) if total_tests > 0 else 0

        v = {
            "test_pass_rate": pass_percentage,
            "execution_time_sec": summary_time if summary_time is not None else total_time,
            "line_coverage_percent": coverage,
            "branch_coverage_percent": None,
            "timeout": False,
            "internal_error_occurred": False,
            "XXruntime_errors_countXX": runtime_errors
        }
        return v
    except Exception as e:
        print(f"Error running Go tests: {e}")
        return {
            "test_pass_rate": 0,
            "execution_time_sec": None,
            "line_coverage_percent": None,
            "branch_coverage_percent": None,
            "timeout": True,
            "internal_error_occurred": True,
            "runtime_errors_count": runtime_errors
        }


def x_run_tests__mutmut_153(project_dir):
    test_cmd = ["go", "test", "-timeout", "30s", "-json", "-cover", "./..."]
    runtime_errors = 0

    print("Running tests...")
    try:
        result = subprocess.run(
            test_cmd,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            cwd=project_dir,
            text=True
        )
        output = result.stdout
        print(output)
        stderr = result.stderr
        print(stderr)

        # Check if the subprocess exited with a non-zero return code
        if result.returncode != 0:
            # Parse output for timeout messages
            if "test timed out after" in output or "panic: test timed out" in output:
                print("Tests failed due to timeout")
                # Handle timeout scenario
                return {
                    "test_pass_rate": 0,
                    "execution_time_sec": None,
                    "line_coverage_percent": None,
                    "branch_coverage_percent": None,
                    "timeout": True,
                    "internal_error_occurred": False,
                    "runtime_errors_count": None
                }
            else:
                runtime_error_pattern = re.compile(
                    r"(panic:|fatal error:|runtime error:)", re.IGNORECASE
                )
                assertion_error_pattern = re.compile(r"assert")

                # Search for runtime errors in stdout and stderr
                for o in (output, stderr):
                    for line in o.splitlines():
                        # Match lines with runtime errors but exclude assertions
                        if runtime_error_pattern.search(line) and not assertion_error_pattern.search(line):
                            runtime_errors = runtime_errors + 1

        # Parse the JSON output
        passed_tests = 0
        failed_tests = 0
        total_time = 0.0
        coverage = None
        summary_time = None

        for line in output.splitlines():
            if not line.strip():
                continue
            try:
                data = json.loads(line)
            except json.JSONDecodeError:
                continue  # Skip lines that aren't valid JSON

            # Check for test results
            if data.get('Action') == 'pass' and 'Test' in data:
                passed_tests += 1
                total_time += data.get('Elapsed', 0)
            elif data.get('Action') == 'fail' and 'Test' in data:
                failed_tests += 1
                total_time += data.get('Elapsed', 0)

            if (data.get('Action') == 'pass' or data.get('Action') == 'fail') and 'Test' not in data and 'Elapsed' in data:
                summary_time = data["Elapsed"]

            # Check for coverage information
            if data.get('Action') == 'output' and 'Output' in data:
                output_line = data['Output'].strip()
                if output_line.startswith("coverage:"):
                    # Extract the coverage percentage
                    match = re.search(r'coverage: ([\d\.]+)%', output_line)
                    if match:
                        coverage = float(match.group(1))

            # Alternative: Some versions include 'Coverage' field
            elif data.get('Action') == 'pass' and 'Coverage' in data:
                coverage = data['Coverage']

        total_tests = passed_tests + failed_tests
        pass_percentage = round((passed_tests / total_tests) * 100, 2) if total_tests > 0 else 0

        v = None
        return v
    except Exception as e:
        print(f"Error running Go tests: {e}")
        return {
            "test_pass_rate": 0,
            "execution_time_sec": None,
            "line_coverage_percent": None,
            "branch_coverage_percent": None,
            "timeout": True,
            "internal_error_occurred": True,
            "runtime_errors_count": runtime_errors
        }


def x_run_tests__mutmut_154(project_dir):
    test_cmd = ["go", "test", "-timeout", "30s", "-json", "-cover", "./..."]
    runtime_errors = 0

    print("Running tests...")
    try:
        result = subprocess.run(
            test_cmd,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            cwd=project_dir,
            text=True
        )
        output = result.stdout
        print(output)
        stderr = result.stderr
        print(stderr)

        # Check if the subprocess exited with a non-zero return code
        if result.returncode != 0:
            # Parse output for timeout messages
            if "test timed out after" in output or "panic: test timed out" in output:
                print("Tests failed due to timeout")
                # Handle timeout scenario
                return {
                    "test_pass_rate": 0,
                    "execution_time_sec": None,
                    "line_coverage_percent": None,
                    "branch_coverage_percent": None,
                    "timeout": True,
                    "internal_error_occurred": False,
                    "runtime_errors_count": None
                }
            else:
                runtime_error_pattern = re.compile(
                    r"(panic:|fatal error:|runtime error:)", re.IGNORECASE
                )
                assertion_error_pattern = re.compile(r"assert")

                # Search for runtime errors in stdout and stderr
                for o in (output, stderr):
                    for line in o.splitlines():
                        # Match lines with runtime errors but exclude assertions
                        if runtime_error_pattern.search(line) and not assertion_error_pattern.search(line):
                            runtime_errors = runtime_errors + 1

        # Parse the JSON output
        passed_tests = 0
        failed_tests = 0
        total_time = 0.0
        coverage = None
        summary_time = None

        for line in output.splitlines():
            if not line.strip():
                continue
            try:
                data = json.loads(line)
            except json.JSONDecodeError:
                continue  # Skip lines that aren't valid JSON

            # Check for test results
            if data.get('Action') == 'pass' and 'Test' in data:
                passed_tests += 1
                total_time += data.get('Elapsed', 0)
            elif data.get('Action') == 'fail' and 'Test' in data:
                failed_tests += 1
                total_time += data.get('Elapsed', 0)

            if (data.get('Action') == 'pass' or data.get('Action') == 'fail') and 'Test' not in data and 'Elapsed' in data:
                summary_time = data["Elapsed"]

            # Check for coverage information
            if data.get('Action') == 'output' and 'Output' in data:
                output_line = data['Output'].strip()
                if output_line.startswith("coverage:"):
                    # Extract the coverage percentage
                    match = re.search(r'coverage: ([\d\.]+)%', output_line)
                    if match:
                        coverage = float(match.group(1))

            # Alternative: Some versions include 'Coverage' field
            elif data.get('Action') == 'pass' and 'Coverage' in data:
                coverage = data['Coverage']

        total_tests = passed_tests + failed_tests
        pass_percentage = round((passed_tests / total_tests) * 100, 2) if total_tests > 0 else 0

        v = {
            "test_pass_rate": pass_percentage,
            "execution_time_sec": summary_time if summary_time is not None else total_time,
            "line_coverage_percent": coverage,
            "branch_coverage_percent": None,
            "timeout": False,
            "internal_error_occurred": False,
            "runtime_errors_count": runtime_errors
        }
        return v
    except Exception as e:
        print(f"Error running Go tests: {e}")
        return {
            "XXtest_pass_rateXX": 0,
            "execution_time_sec": None,
            "line_coverage_percent": None,
            "branch_coverage_percent": None,
            "timeout": True,
            "internal_error_occurred": True,
            "runtime_errors_count": runtime_errors
        }


def x_run_tests__mutmut_155(project_dir):
    test_cmd = ["go", "test", "-timeout", "30s", "-json", "-cover", "./..."]
    runtime_errors = 0

    print("Running tests...")
    try:
        result = subprocess.run(
            test_cmd,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            cwd=project_dir,
            text=True
        )
        output = result.stdout
        print(output)
        stderr = result.stderr
        print(stderr)

        # Check if the subprocess exited with a non-zero return code
        if result.returncode != 0:
            # Parse output for timeout messages
            if "test timed out after" in output or "panic: test timed out" in output:
                print("Tests failed due to timeout")
                # Handle timeout scenario
                return {
                    "test_pass_rate": 0,
                    "execution_time_sec": None,
                    "line_coverage_percent": None,
                    "branch_coverage_percent": None,
                    "timeout": True,
                    "internal_error_occurred": False,
                    "runtime_errors_count": None
                }
            else:
                runtime_error_pattern = re.compile(
                    r"(panic:|fatal error:|runtime error:)", re.IGNORECASE
                )
                assertion_error_pattern = re.compile(r"assert")

                # Search for runtime errors in stdout and stderr
                for o in (output, stderr):
                    for line in o.splitlines():
                        # Match lines with runtime errors but exclude assertions
                        if runtime_error_pattern.search(line) and not assertion_error_pattern.search(line):
                            runtime_errors = runtime_errors + 1

        # Parse the JSON output
        passed_tests = 0
        failed_tests = 0
        total_time = 0.0
        coverage = None
        summary_time = None

        for line in output.splitlines():
            if not line.strip():
                continue
            try:
                data = json.loads(line)
            except json.JSONDecodeError:
                continue  # Skip lines that aren't valid JSON

            # Check for test results
            if data.get('Action') == 'pass' and 'Test' in data:
                passed_tests += 1
                total_time += data.get('Elapsed', 0)
            elif data.get('Action') == 'fail' and 'Test' in data:
                failed_tests += 1
                total_time += data.get('Elapsed', 0)

            if (data.get('Action') == 'pass' or data.get('Action') == 'fail') and 'Test' not in data and 'Elapsed' in data:
                summary_time = data["Elapsed"]

            # Check for coverage information
            if data.get('Action') == 'output' and 'Output' in data:
                output_line = data['Output'].strip()
                if output_line.startswith("coverage:"):
                    # Extract the coverage percentage
                    match = re.search(r'coverage: ([\d\.]+)%', output_line)
                    if match:
                        coverage = float(match.group(1))

            # Alternative: Some versions include 'Coverage' field
            elif data.get('Action') == 'pass' and 'Coverage' in data:
                coverage = data['Coverage']

        total_tests = passed_tests + failed_tests
        pass_percentage = round((passed_tests / total_tests) * 100, 2) if total_tests > 0 else 0

        v = {
            "test_pass_rate": pass_percentage,
            "execution_time_sec": summary_time if summary_time is not None else total_time,
            "line_coverage_percent": coverage,
            "branch_coverage_percent": None,
            "timeout": False,
            "internal_error_occurred": False,
            "runtime_errors_count": runtime_errors
        }
        return v
    except Exception as e:
        print(f"Error running Go tests: {e}")
        return {
            "test_pass_rate": 1,
            "execution_time_sec": None,
            "line_coverage_percent": None,
            "branch_coverage_percent": None,
            "timeout": True,
            "internal_error_occurred": True,
            "runtime_errors_count": runtime_errors
        }


def x_run_tests__mutmut_156(project_dir):
    test_cmd = ["go", "test", "-timeout", "30s", "-json", "-cover", "./..."]
    runtime_errors = 0

    print("Running tests...")
    try:
        result = subprocess.run(
            test_cmd,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            cwd=project_dir,
            text=True
        )
        output = result.stdout
        print(output)
        stderr = result.stderr
        print(stderr)

        # Check if the subprocess exited with a non-zero return code
        if result.returncode != 0:
            # Parse output for timeout messages
            if "test timed out after" in output or "panic: test timed out" in output:
                print("Tests failed due to timeout")
                # Handle timeout scenario
                return {
                    "test_pass_rate": 0,
                    "execution_time_sec": None,
                    "line_coverage_percent": None,
                    "branch_coverage_percent": None,
                    "timeout": True,
                    "internal_error_occurred": False,
                    "runtime_errors_count": None
                }
            else:
                runtime_error_pattern = re.compile(
                    r"(panic:|fatal error:|runtime error:)", re.IGNORECASE
                )
                assertion_error_pattern = re.compile(r"assert")

                # Search for runtime errors in stdout and stderr
                for o in (output, stderr):
                    for line in o.splitlines():
                        # Match lines with runtime errors but exclude assertions
                        if runtime_error_pattern.search(line) and not assertion_error_pattern.search(line):
                            runtime_errors = runtime_errors + 1

        # Parse the JSON output
        passed_tests = 0
        failed_tests = 0
        total_time = 0.0
        coverage = None
        summary_time = None

        for line in output.splitlines():
            if not line.strip():
                continue
            try:
                data = json.loads(line)
            except json.JSONDecodeError:
                continue  # Skip lines that aren't valid JSON

            # Check for test results
            if data.get('Action') == 'pass' and 'Test' in data:
                passed_tests += 1
                total_time += data.get('Elapsed', 0)
            elif data.get('Action') == 'fail' and 'Test' in data:
                failed_tests += 1
                total_time += data.get('Elapsed', 0)

            if (data.get('Action') == 'pass' or data.get('Action') == 'fail') and 'Test' not in data and 'Elapsed' in data:
                summary_time = data["Elapsed"]

            # Check for coverage information
            if data.get('Action') == 'output' and 'Output' in data:
                output_line = data['Output'].strip()
                if output_line.startswith("coverage:"):
                    # Extract the coverage percentage
                    match = re.search(r'coverage: ([\d\.]+)%', output_line)
                    if match:
                        coverage = float(match.group(1))

            # Alternative: Some versions include 'Coverage' field
            elif data.get('Action') == 'pass' and 'Coverage' in data:
                coverage = data['Coverage']

        total_tests = passed_tests + failed_tests
        pass_percentage = round((passed_tests / total_tests) * 100, 2) if total_tests > 0 else 0

        v = {
            "test_pass_rate": pass_percentage,
            "execution_time_sec": summary_time if summary_time is not None else total_time,
            "line_coverage_percent": coverage,
            "branch_coverage_percent": None,
            "timeout": False,
            "internal_error_occurred": False,
            "runtime_errors_count": runtime_errors
        }
        return v
    except Exception as e:
        print(f"Error running Go tests: {e}")
        return {
            "test_pass_rate": 0,
            "XXexecution_time_secXX": None,
            "line_coverage_percent": None,
            "branch_coverage_percent": None,
            "timeout": True,
            "internal_error_occurred": True,
            "runtime_errors_count": runtime_errors
        }


def x_run_tests__mutmut_157(project_dir):
    test_cmd = ["go", "test", "-timeout", "30s", "-json", "-cover", "./..."]
    runtime_errors = 0

    print("Running tests...")
    try:
        result = subprocess.run(
            test_cmd,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            cwd=project_dir,
            text=True
        )
        output = result.stdout
        print(output)
        stderr = result.stderr
        print(stderr)

        # Check if the subprocess exited with a non-zero return code
        if result.returncode != 0:
            # Parse output for timeout messages
            if "test timed out after" in output or "panic: test timed out" in output:
                print("Tests failed due to timeout")
                # Handle timeout scenario
                return {
                    "test_pass_rate": 0,
                    "execution_time_sec": None,
                    "line_coverage_percent": None,
                    "branch_coverage_percent": None,
                    "timeout": True,
                    "internal_error_occurred": False,
                    "runtime_errors_count": None
                }
            else:
                runtime_error_pattern = re.compile(
                    r"(panic:|fatal error:|runtime error:)", re.IGNORECASE
                )
                assertion_error_pattern = re.compile(r"assert")

                # Search for runtime errors in stdout and stderr
                for o in (output, stderr):
                    for line in o.splitlines():
                        # Match lines with runtime errors but exclude assertions
                        if runtime_error_pattern.search(line) and not assertion_error_pattern.search(line):
                            runtime_errors = runtime_errors + 1

        # Parse the JSON output
        passed_tests = 0
        failed_tests = 0
        total_time = 0.0
        coverage = None
        summary_time = None

        for line in output.splitlines():
            if not line.strip():
                continue
            try:
                data = json.loads(line)
            except json.JSONDecodeError:
                continue  # Skip lines that aren't valid JSON

            # Check for test results
            if data.get('Action') == 'pass' and 'Test' in data:
                passed_tests += 1
                total_time += data.get('Elapsed', 0)
            elif data.get('Action') == 'fail' and 'Test' in data:
                failed_tests += 1
                total_time += data.get('Elapsed', 0)

            if (data.get('Action') == 'pass' or data.get('Action') == 'fail') and 'Test' not in data and 'Elapsed' in data:
                summary_time = data["Elapsed"]

            # Check for coverage information
            if data.get('Action') == 'output' and 'Output' in data:
                output_line = data['Output'].strip()
                if output_line.startswith("coverage:"):
                    # Extract the coverage percentage
                    match = re.search(r'coverage: ([\d\.]+)%', output_line)
                    if match:
                        coverage = float(match.group(1))

            # Alternative: Some versions include 'Coverage' field
            elif data.get('Action') == 'pass' and 'Coverage' in data:
                coverage = data['Coverage']

        total_tests = passed_tests + failed_tests
        pass_percentage = round((passed_tests / total_tests) * 100, 2) if total_tests > 0 else 0

        v = {
            "test_pass_rate": pass_percentage,
            "execution_time_sec": summary_time if summary_time is not None else total_time,
            "line_coverage_percent": coverage,
            "branch_coverage_percent": None,
            "timeout": False,
            "internal_error_occurred": False,
            "runtime_errors_count": runtime_errors
        }
        return v
    except Exception as e:
        print(f"Error running Go tests: {e}")
        return {
            "test_pass_rate": 0,
            "execution_time_sec": None,
            "XXline_coverage_percentXX": None,
            "branch_coverage_percent": None,
            "timeout": True,
            "internal_error_occurred": True,
            "runtime_errors_count": runtime_errors
        }


def x_run_tests__mutmut_158(project_dir):
    test_cmd = ["go", "test", "-timeout", "30s", "-json", "-cover", "./..."]
    runtime_errors = 0

    print("Running tests...")
    try:
        result = subprocess.run(
            test_cmd,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            cwd=project_dir,
            text=True
        )
        output = result.stdout
        print(output)
        stderr = result.stderr
        print(stderr)

        # Check if the subprocess exited with a non-zero return code
        if result.returncode != 0:
            # Parse output for timeout messages
            if "test timed out after" in output or "panic: test timed out" in output:
                print("Tests failed due to timeout")
                # Handle timeout scenario
                return {
                    "test_pass_rate": 0,
                    "execution_time_sec": None,
                    "line_coverage_percent": None,
                    "branch_coverage_percent": None,
                    "timeout": True,
                    "internal_error_occurred": False,
                    "runtime_errors_count": None
                }
            else:
                runtime_error_pattern = re.compile(
                    r"(panic:|fatal error:|runtime error:)", re.IGNORECASE
                )
                assertion_error_pattern = re.compile(r"assert")

                # Search for runtime errors in stdout and stderr
                for o in (output, stderr):
                    for line in o.splitlines():
                        # Match lines with runtime errors but exclude assertions
                        if runtime_error_pattern.search(line) and not assertion_error_pattern.search(line):
                            runtime_errors = runtime_errors + 1

        # Parse the JSON output
        passed_tests = 0
        failed_tests = 0
        total_time = 0.0
        coverage = None
        summary_time = None

        for line in output.splitlines():
            if not line.strip():
                continue
            try:
                data = json.loads(line)
            except json.JSONDecodeError:
                continue  # Skip lines that aren't valid JSON

            # Check for test results
            if data.get('Action') == 'pass' and 'Test' in data:
                passed_tests += 1
                total_time += data.get('Elapsed', 0)
            elif data.get('Action') == 'fail' and 'Test' in data:
                failed_tests += 1
                total_time += data.get('Elapsed', 0)

            if (data.get('Action') == 'pass' or data.get('Action') == 'fail') and 'Test' not in data and 'Elapsed' in data:
                summary_time = data["Elapsed"]

            # Check for coverage information
            if data.get('Action') == 'output' and 'Output' in data:
                output_line = data['Output'].strip()
                if output_line.startswith("coverage:"):
                    # Extract the coverage percentage
                    match = re.search(r'coverage: ([\d\.]+)%', output_line)
                    if match:
                        coverage = float(match.group(1))

            # Alternative: Some versions include 'Coverage' field
            elif data.get('Action') == 'pass' and 'Coverage' in data:
                coverage = data['Coverage']

        total_tests = passed_tests + failed_tests
        pass_percentage = round((passed_tests / total_tests) * 100, 2) if total_tests > 0 else 0

        v = {
            "test_pass_rate": pass_percentage,
            "execution_time_sec": summary_time if summary_time is not None else total_time,
            "line_coverage_percent": coverage,
            "branch_coverage_percent": None,
            "timeout": False,
            "internal_error_occurred": False,
            "runtime_errors_count": runtime_errors
        }
        return v
    except Exception as e:
        print(f"Error running Go tests: {e}")
        return {
            "test_pass_rate": 0,
            "execution_time_sec": None,
            "line_coverage_percent": None,
            "XXbranch_coverage_percentXX": None,
            "timeout": True,
            "internal_error_occurred": True,
            "runtime_errors_count": runtime_errors
        }


def x_run_tests__mutmut_159(project_dir):
    test_cmd = ["go", "test", "-timeout", "30s", "-json", "-cover", "./..."]
    runtime_errors = 0

    print("Running tests...")
    try:
        result = subprocess.run(
            test_cmd,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            cwd=project_dir,
            text=True
        )
        output = result.stdout
        print(output)
        stderr = result.stderr
        print(stderr)

        # Check if the subprocess exited with a non-zero return code
        if result.returncode != 0:
            # Parse output for timeout messages
            if "test timed out after" in output or "panic: test timed out" in output:
                print("Tests failed due to timeout")
                # Handle timeout scenario
                return {
                    "test_pass_rate": 0,
                    "execution_time_sec": None,
                    "line_coverage_percent": None,
                    "branch_coverage_percent": None,
                    "timeout": True,
                    "internal_error_occurred": False,
                    "runtime_errors_count": None
                }
            else:
                runtime_error_pattern = re.compile(
                    r"(panic:|fatal error:|runtime error:)", re.IGNORECASE
                )
                assertion_error_pattern = re.compile(r"assert")

                # Search for runtime errors in stdout and stderr
                for o in (output, stderr):
                    for line in o.splitlines():
                        # Match lines with runtime errors but exclude assertions
                        if runtime_error_pattern.search(line) and not assertion_error_pattern.search(line):
                            runtime_errors = runtime_errors + 1

        # Parse the JSON output
        passed_tests = 0
        failed_tests = 0
        total_time = 0.0
        coverage = None
        summary_time = None

        for line in output.splitlines():
            if not line.strip():
                continue
            try:
                data = json.loads(line)
            except json.JSONDecodeError:
                continue  # Skip lines that aren't valid JSON

            # Check for test results
            if data.get('Action') == 'pass' and 'Test' in data:
                passed_tests += 1
                total_time += data.get('Elapsed', 0)
            elif data.get('Action') == 'fail' and 'Test' in data:
                failed_tests += 1
                total_time += data.get('Elapsed', 0)

            if (data.get('Action') == 'pass' or data.get('Action') == 'fail') and 'Test' not in data and 'Elapsed' in data:
                summary_time = data["Elapsed"]

            # Check for coverage information
            if data.get('Action') == 'output' and 'Output' in data:
                output_line = data['Output'].strip()
                if output_line.startswith("coverage:"):
                    # Extract the coverage percentage
                    match = re.search(r'coverage: ([\d\.]+)%', output_line)
                    if match:
                        coverage = float(match.group(1))

            # Alternative: Some versions include 'Coverage' field
            elif data.get('Action') == 'pass' and 'Coverage' in data:
                coverage = data['Coverage']

        total_tests = passed_tests + failed_tests
        pass_percentage = round((passed_tests / total_tests) * 100, 2) if total_tests > 0 else 0

        v = {
            "test_pass_rate": pass_percentage,
            "execution_time_sec": summary_time if summary_time is not None else total_time,
            "line_coverage_percent": coverage,
            "branch_coverage_percent": None,
            "timeout": False,
            "internal_error_occurred": False,
            "runtime_errors_count": runtime_errors
        }
        return v
    except Exception as e:
        print(f"Error running Go tests: {e}")
        return {
            "test_pass_rate": 0,
            "execution_time_sec": None,
            "line_coverage_percent": None,
            "branch_coverage_percent": None,
            "XXtimeoutXX": True,
            "internal_error_occurred": True,
            "runtime_errors_count": runtime_errors
        }


def x_run_tests__mutmut_160(project_dir):
    test_cmd = ["go", "test", "-timeout", "30s", "-json", "-cover", "./..."]
    runtime_errors = 0

    print("Running tests...")
    try:
        result = subprocess.run(
            test_cmd,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            cwd=project_dir,
            text=True
        )
        output = result.stdout
        print(output)
        stderr = result.stderr
        print(stderr)

        # Check if the subprocess exited with a non-zero return code
        if result.returncode != 0:
            # Parse output for timeout messages
            if "test timed out after" in output or "panic: test timed out" in output:
                print("Tests failed due to timeout")
                # Handle timeout scenario
                return {
                    "test_pass_rate": 0,
                    "execution_time_sec": None,
                    "line_coverage_percent": None,
                    "branch_coverage_percent": None,
                    "timeout": True,
                    "internal_error_occurred": False,
                    "runtime_errors_count": None
                }
            else:
                runtime_error_pattern = re.compile(
                    r"(panic:|fatal error:|runtime error:)", re.IGNORECASE
                )
                assertion_error_pattern = re.compile(r"assert")

                # Search for runtime errors in stdout and stderr
                for o in (output, stderr):
                    for line in o.splitlines():
                        # Match lines with runtime errors but exclude assertions
                        if runtime_error_pattern.search(line) and not assertion_error_pattern.search(line):
                            runtime_errors = runtime_errors + 1

        # Parse the JSON output
        passed_tests = 0
        failed_tests = 0
        total_time = 0.0
        coverage = None
        summary_time = None

        for line in output.splitlines():
            if not line.strip():
                continue
            try:
                data = json.loads(line)
            except json.JSONDecodeError:
                continue  # Skip lines that aren't valid JSON

            # Check for test results
            if data.get('Action') == 'pass' and 'Test' in data:
                passed_tests += 1
                total_time += data.get('Elapsed', 0)
            elif data.get('Action') == 'fail' and 'Test' in data:
                failed_tests += 1
                total_time += data.get('Elapsed', 0)

            if (data.get('Action') == 'pass' or data.get('Action') == 'fail') and 'Test' not in data and 'Elapsed' in data:
                summary_time = data["Elapsed"]

            # Check for coverage information
            if data.get('Action') == 'output' and 'Output' in data:
                output_line = data['Output'].strip()
                if output_line.startswith("coverage:"):
                    # Extract the coverage percentage
                    match = re.search(r'coverage: ([\d\.]+)%', output_line)
                    if match:
                        coverage = float(match.group(1))

            # Alternative: Some versions include 'Coverage' field
            elif data.get('Action') == 'pass' and 'Coverage' in data:
                coverage = data['Coverage']

        total_tests = passed_tests + failed_tests
        pass_percentage = round((passed_tests / total_tests) * 100, 2) if total_tests > 0 else 0

        v = {
            "test_pass_rate": pass_percentage,
            "execution_time_sec": summary_time if summary_time is not None else total_time,
            "line_coverage_percent": coverage,
            "branch_coverage_percent": None,
            "timeout": False,
            "internal_error_occurred": False,
            "runtime_errors_count": runtime_errors
        }
        return v
    except Exception as e:
        print(f"Error running Go tests: {e}")
        return {
            "test_pass_rate": 0,
            "execution_time_sec": None,
            "line_coverage_percent": None,
            "branch_coverage_percent": None,
            "timeout": False,
            "internal_error_occurred": True,
            "runtime_errors_count": runtime_errors
        }


def x_run_tests__mutmut_161(project_dir):
    test_cmd = ["go", "test", "-timeout", "30s", "-json", "-cover", "./..."]
    runtime_errors = 0

    print("Running tests...")
    try:
        result = subprocess.run(
            test_cmd,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            cwd=project_dir,
            text=True
        )
        output = result.stdout
        print(output)
        stderr = result.stderr
        print(stderr)

        # Check if the subprocess exited with a non-zero return code
        if result.returncode != 0:
            # Parse output for timeout messages
            if "test timed out after" in output or "panic: test timed out" in output:
                print("Tests failed due to timeout")
                # Handle timeout scenario
                return {
                    "test_pass_rate": 0,
                    "execution_time_sec": None,
                    "line_coverage_percent": None,
                    "branch_coverage_percent": None,
                    "timeout": True,
                    "internal_error_occurred": False,
                    "runtime_errors_count": None
                }
            else:
                runtime_error_pattern = re.compile(
                    r"(panic:|fatal error:|runtime error:)", re.IGNORECASE
                )
                assertion_error_pattern = re.compile(r"assert")

                # Search for runtime errors in stdout and stderr
                for o in (output, stderr):
                    for line in o.splitlines():
                        # Match lines with runtime errors but exclude assertions
                        if runtime_error_pattern.search(line) and not assertion_error_pattern.search(line):
                            runtime_errors = runtime_errors + 1

        # Parse the JSON output
        passed_tests = 0
        failed_tests = 0
        total_time = 0.0
        coverage = None
        summary_time = None

        for line in output.splitlines():
            if not line.strip():
                continue
            try:
                data = json.loads(line)
            except json.JSONDecodeError:
                continue  # Skip lines that aren't valid JSON

            # Check for test results
            if data.get('Action') == 'pass' and 'Test' in data:
                passed_tests += 1
                total_time += data.get('Elapsed', 0)
            elif data.get('Action') == 'fail' and 'Test' in data:
                failed_tests += 1
                total_time += data.get('Elapsed', 0)

            if (data.get('Action') == 'pass' or data.get('Action') == 'fail') and 'Test' not in data and 'Elapsed' in data:
                summary_time = data["Elapsed"]

            # Check for coverage information
            if data.get('Action') == 'output' and 'Output' in data:
                output_line = data['Output'].strip()
                if output_line.startswith("coverage:"):
                    # Extract the coverage percentage
                    match = re.search(r'coverage: ([\d\.]+)%', output_line)
                    if match:
                        coverage = float(match.group(1))

            # Alternative: Some versions include 'Coverage' field
            elif data.get('Action') == 'pass' and 'Coverage' in data:
                coverage = data['Coverage']

        total_tests = passed_tests + failed_tests
        pass_percentage = round((passed_tests / total_tests) * 100, 2) if total_tests > 0 else 0

        v = {
            "test_pass_rate": pass_percentage,
            "execution_time_sec": summary_time if summary_time is not None else total_time,
            "line_coverage_percent": coverage,
            "branch_coverage_percent": None,
            "timeout": False,
            "internal_error_occurred": False,
            "runtime_errors_count": runtime_errors
        }
        return v
    except Exception as e:
        print(f"Error running Go tests: {e}")
        return {
            "test_pass_rate": 0,
            "execution_time_sec": None,
            "line_coverage_percent": None,
            "branch_coverage_percent": None,
            "timeout": True,
            "XXinternal_error_occurredXX": True,
            "runtime_errors_count": runtime_errors
        }


def x_run_tests__mutmut_162(project_dir):
    test_cmd = ["go", "test", "-timeout", "30s", "-json", "-cover", "./..."]
    runtime_errors = 0

    print("Running tests...")
    try:
        result = subprocess.run(
            test_cmd,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            cwd=project_dir,
            text=True
        )
        output = result.stdout
        print(output)
        stderr = result.stderr
        print(stderr)

        # Check if the subprocess exited with a non-zero return code
        if result.returncode != 0:
            # Parse output for timeout messages
            if "test timed out after" in output or "panic: test timed out" in output:
                print("Tests failed due to timeout")
                # Handle timeout scenario
                return {
                    "test_pass_rate": 0,
                    "execution_time_sec": None,
                    "line_coverage_percent": None,
                    "branch_coverage_percent": None,
                    "timeout": True,
                    "internal_error_occurred": False,
                    "runtime_errors_count": None
                }
            else:
                runtime_error_pattern = re.compile(
                    r"(panic:|fatal error:|runtime error:)", re.IGNORECASE
                )
                assertion_error_pattern = re.compile(r"assert")

                # Search for runtime errors in stdout and stderr
                for o in (output, stderr):
                    for line in o.splitlines():
                        # Match lines with runtime errors but exclude assertions
                        if runtime_error_pattern.search(line) and not assertion_error_pattern.search(line):
                            runtime_errors = runtime_errors + 1

        # Parse the JSON output
        passed_tests = 0
        failed_tests = 0
        total_time = 0.0
        coverage = None
        summary_time = None

        for line in output.splitlines():
            if not line.strip():
                continue
            try:
                data = json.loads(line)
            except json.JSONDecodeError:
                continue  # Skip lines that aren't valid JSON

            # Check for test results
            if data.get('Action') == 'pass' and 'Test' in data:
                passed_tests += 1
                total_time += data.get('Elapsed', 0)
            elif data.get('Action') == 'fail' and 'Test' in data:
                failed_tests += 1
                total_time += data.get('Elapsed', 0)

            if (data.get('Action') == 'pass' or data.get('Action') == 'fail') and 'Test' not in data and 'Elapsed' in data:
                summary_time = data["Elapsed"]

            # Check for coverage information
            if data.get('Action') == 'output' and 'Output' in data:
                output_line = data['Output'].strip()
                if output_line.startswith("coverage:"):
                    # Extract the coverage percentage
                    match = re.search(r'coverage: ([\d\.]+)%', output_line)
                    if match:
                        coverage = float(match.group(1))

            # Alternative: Some versions include 'Coverage' field
            elif data.get('Action') == 'pass' and 'Coverage' in data:
                coverage = data['Coverage']

        total_tests = passed_tests + failed_tests
        pass_percentage = round((passed_tests / total_tests) * 100, 2) if total_tests > 0 else 0

        v = {
            "test_pass_rate": pass_percentage,
            "execution_time_sec": summary_time if summary_time is not None else total_time,
            "line_coverage_percent": coverage,
            "branch_coverage_percent": None,
            "timeout": False,
            "internal_error_occurred": False,
            "runtime_errors_count": runtime_errors
        }
        return v
    except Exception as e:
        print(f"Error running Go tests: {e}")
        return {
            "test_pass_rate": 0,
            "execution_time_sec": None,
            "line_coverage_percent": None,
            "branch_coverage_percent": None,
            "timeout": True,
            "internal_error_occurred": False,
            "runtime_errors_count": runtime_errors
        }


def x_run_tests__mutmut_163(project_dir):
    test_cmd = ["go", "test", "-timeout", "30s", "-json", "-cover", "./..."]
    runtime_errors = 0

    print("Running tests...")
    try:
        result = subprocess.run(
            test_cmd,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            cwd=project_dir,
            text=True
        )
        output = result.stdout
        print(output)
        stderr = result.stderr
        print(stderr)

        # Check if the subprocess exited with a non-zero return code
        if result.returncode != 0:
            # Parse output for timeout messages
            if "test timed out after" in output or "panic: test timed out" in output:
                print("Tests failed due to timeout")
                # Handle timeout scenario
                return {
                    "test_pass_rate": 0,
                    "execution_time_sec": None,
                    "line_coverage_percent": None,
                    "branch_coverage_percent": None,
                    "timeout": True,
                    "internal_error_occurred": False,
                    "runtime_errors_count": None
                }
            else:
                runtime_error_pattern = re.compile(
                    r"(panic:|fatal error:|runtime error:)", re.IGNORECASE
                )
                assertion_error_pattern = re.compile(r"assert")

                # Search for runtime errors in stdout and stderr
                for o in (output, stderr):
                    for line in o.splitlines():
                        # Match lines with runtime errors but exclude assertions
                        if runtime_error_pattern.search(line) and not assertion_error_pattern.search(line):
                            runtime_errors = runtime_errors + 1

        # Parse the JSON output
        passed_tests = 0
        failed_tests = 0
        total_time = 0.0
        coverage = None
        summary_time = None

        for line in output.splitlines():
            if not line.strip():
                continue
            try:
                data = json.loads(line)
            except json.JSONDecodeError:
                continue  # Skip lines that aren't valid JSON

            # Check for test results
            if data.get('Action') == 'pass' and 'Test' in data:
                passed_tests += 1
                total_time += data.get('Elapsed', 0)
            elif data.get('Action') == 'fail' and 'Test' in data:
                failed_tests += 1
                total_time += data.get('Elapsed', 0)

            if (data.get('Action') == 'pass' or data.get('Action') == 'fail') and 'Test' not in data and 'Elapsed' in data:
                summary_time = data["Elapsed"]

            # Check for coverage information
            if data.get('Action') == 'output' and 'Output' in data:
                output_line = data['Output'].strip()
                if output_line.startswith("coverage:"):
                    # Extract the coverage percentage
                    match = re.search(r'coverage: ([\d\.]+)%', output_line)
                    if match:
                        coverage = float(match.group(1))

            # Alternative: Some versions include 'Coverage' field
            elif data.get('Action') == 'pass' and 'Coverage' in data:
                coverage = data['Coverage']

        total_tests = passed_tests + failed_tests
        pass_percentage = round((passed_tests / total_tests) * 100, 2) if total_tests > 0 else 0

        v = {
            "test_pass_rate": pass_percentage,
            "execution_time_sec": summary_time if summary_time is not None else total_time,
            "line_coverage_percent": coverage,
            "branch_coverage_percent": None,
            "timeout": False,
            "internal_error_occurred": False,
            "runtime_errors_count": runtime_errors
        }
        return v
    except Exception as e:
        print(f"Error running Go tests: {e}")
        return {
            "test_pass_rate": 0,
            "execution_time_sec": None,
            "line_coverage_percent": None,
            "branch_coverage_percent": None,
            "timeout": True,
            "internal_error_occurred": True,
            "XXruntime_errors_countXX": runtime_errors
        }

x_run_tests__mutmut_mutants = {
'x_run_tests__mutmut_1': x_run_tests__mutmut_1, 
    'x_run_tests__mutmut_2': x_run_tests__mutmut_2, 
    'x_run_tests__mutmut_3': x_run_tests__mutmut_3, 
    'x_run_tests__mutmut_4': x_run_tests__mutmut_4, 
    'x_run_tests__mutmut_5': x_run_tests__mutmut_5, 
    'x_run_tests__mutmut_6': x_run_tests__mutmut_6, 
    'x_run_tests__mutmut_7': x_run_tests__mutmut_7, 
    'x_run_tests__mutmut_8': x_run_tests__mutmut_8, 
    'x_run_tests__mutmut_9': x_run_tests__mutmut_9, 
    'x_run_tests__mutmut_10': x_run_tests__mutmut_10, 
    'x_run_tests__mutmut_11': x_run_tests__mutmut_11, 
    'x_run_tests__mutmut_12': x_run_tests__mutmut_12, 
    'x_run_tests__mutmut_13': x_run_tests__mutmut_13, 
    'x_run_tests__mutmut_14': x_run_tests__mutmut_14, 
    'x_run_tests__mutmut_15': x_run_tests__mutmut_15, 
    'x_run_tests__mutmut_16': x_run_tests__mutmut_16, 
    'x_run_tests__mutmut_17': x_run_tests__mutmut_17, 
    'x_run_tests__mutmut_18': x_run_tests__mutmut_18, 
    'x_run_tests__mutmut_19': x_run_tests__mutmut_19, 
    'x_run_tests__mutmut_20': x_run_tests__mutmut_20, 
    'x_run_tests__mutmut_21': x_run_tests__mutmut_21, 
    'x_run_tests__mutmut_22': x_run_tests__mutmut_22, 
    'x_run_tests__mutmut_23': x_run_tests__mutmut_23, 
    'x_run_tests__mutmut_24': x_run_tests__mutmut_24, 
    'x_run_tests__mutmut_25': x_run_tests__mutmut_25, 
    'x_run_tests__mutmut_26': x_run_tests__mutmut_26, 
    'x_run_tests__mutmut_27': x_run_tests__mutmut_27, 
    'x_run_tests__mutmut_28': x_run_tests__mutmut_28, 
    'x_run_tests__mutmut_29': x_run_tests__mutmut_29, 
    'x_run_tests__mutmut_30': x_run_tests__mutmut_30, 
    'x_run_tests__mutmut_31': x_run_tests__mutmut_31, 
    'x_run_tests__mutmut_32': x_run_tests__mutmut_32, 
    'x_run_tests__mutmut_33': x_run_tests__mutmut_33, 
    'x_run_tests__mutmut_34': x_run_tests__mutmut_34, 
    'x_run_tests__mutmut_35': x_run_tests__mutmut_35, 
    'x_run_tests__mutmut_36': x_run_tests__mutmut_36, 
    'x_run_tests__mutmut_37': x_run_tests__mutmut_37, 
    'x_run_tests__mutmut_38': x_run_tests__mutmut_38, 
    'x_run_tests__mutmut_39': x_run_tests__mutmut_39, 
    'x_run_tests__mutmut_40': x_run_tests__mutmut_40, 
    'x_run_tests__mutmut_41': x_run_tests__mutmut_41, 
    'x_run_tests__mutmut_42': x_run_tests__mutmut_42, 
    'x_run_tests__mutmut_43': x_run_tests__mutmut_43, 
    'x_run_tests__mutmut_44': x_run_tests__mutmut_44, 
    'x_run_tests__mutmut_45': x_run_tests__mutmut_45, 
    'x_run_tests__mutmut_46': x_run_tests__mutmut_46, 
    'x_run_tests__mutmut_47': x_run_tests__mutmut_47, 
    'x_run_tests__mutmut_48': x_run_tests__mutmut_48, 
    'x_run_tests__mutmut_49': x_run_tests__mutmut_49, 
    'x_run_tests__mutmut_50': x_run_tests__mutmut_50, 
    'x_run_tests__mutmut_51': x_run_tests__mutmut_51, 
    'x_run_tests__mutmut_52': x_run_tests__mutmut_52, 
    'x_run_tests__mutmut_53': x_run_tests__mutmut_53, 
    'x_run_tests__mutmut_54': x_run_tests__mutmut_54, 
    'x_run_tests__mutmut_55': x_run_tests__mutmut_55, 
    'x_run_tests__mutmut_56': x_run_tests__mutmut_56, 
    'x_run_tests__mutmut_57': x_run_tests__mutmut_57, 
    'x_run_tests__mutmut_58': x_run_tests__mutmut_58, 
    'x_run_tests__mutmut_59': x_run_tests__mutmut_59, 
    'x_run_tests__mutmut_60': x_run_tests__mutmut_60, 
    'x_run_tests__mutmut_61': x_run_tests__mutmut_61, 
    'x_run_tests__mutmut_62': x_run_tests__mutmut_62, 
    'x_run_tests__mutmut_63': x_run_tests__mutmut_63, 
    'x_run_tests__mutmut_64': x_run_tests__mutmut_64, 
    'x_run_tests__mutmut_65': x_run_tests__mutmut_65, 
    'x_run_tests__mutmut_66': x_run_tests__mutmut_66, 
    'x_run_tests__mutmut_67': x_run_tests__mutmut_67, 
    'x_run_tests__mutmut_68': x_run_tests__mutmut_68, 
    'x_run_tests__mutmut_69': x_run_tests__mutmut_69, 
    'x_run_tests__mutmut_70': x_run_tests__mutmut_70, 
    'x_run_tests__mutmut_71': x_run_tests__mutmut_71, 
    'x_run_tests__mutmut_72': x_run_tests__mutmut_72, 
    'x_run_tests__mutmut_73': x_run_tests__mutmut_73, 
    'x_run_tests__mutmut_74': x_run_tests__mutmut_74, 
    'x_run_tests__mutmut_75': x_run_tests__mutmut_75, 
    'x_run_tests__mutmut_76': x_run_tests__mutmut_76, 
    'x_run_tests__mutmut_77': x_run_tests__mutmut_77, 
    'x_run_tests__mutmut_78': x_run_tests__mutmut_78, 
    'x_run_tests__mutmut_79': x_run_tests__mutmut_79, 
    'x_run_tests__mutmut_80': x_run_tests__mutmut_80, 
    'x_run_tests__mutmut_81': x_run_tests__mutmut_81, 
    'x_run_tests__mutmut_82': x_run_tests__mutmut_82, 
    'x_run_tests__mutmut_83': x_run_tests__mutmut_83, 
    'x_run_tests__mutmut_84': x_run_tests__mutmut_84, 
    'x_run_tests__mutmut_85': x_run_tests__mutmut_85, 
    'x_run_tests__mutmut_86': x_run_tests__mutmut_86, 
    'x_run_tests__mutmut_87': x_run_tests__mutmut_87, 
    'x_run_tests__mutmut_88': x_run_tests__mutmut_88, 
    'x_run_tests__mutmut_89': x_run_tests__mutmut_89, 
    'x_run_tests__mutmut_90': x_run_tests__mutmut_90, 
    'x_run_tests__mutmut_91': x_run_tests__mutmut_91, 
    'x_run_tests__mutmut_92': x_run_tests__mutmut_92, 
    'x_run_tests__mutmut_93': x_run_tests__mutmut_93, 
    'x_run_tests__mutmut_94': x_run_tests__mutmut_94, 
    'x_run_tests__mutmut_95': x_run_tests__mutmut_95, 
    'x_run_tests__mutmut_96': x_run_tests__mutmut_96, 
    'x_run_tests__mutmut_97': x_run_tests__mutmut_97, 
    'x_run_tests__mutmut_98': x_run_tests__mutmut_98, 
    'x_run_tests__mutmut_99': x_run_tests__mutmut_99, 
    'x_run_tests__mutmut_100': x_run_tests__mutmut_100, 
    'x_run_tests__mutmut_101': x_run_tests__mutmut_101, 
    'x_run_tests__mutmut_102': x_run_tests__mutmut_102, 
    'x_run_tests__mutmut_103': x_run_tests__mutmut_103, 
    'x_run_tests__mutmut_104': x_run_tests__mutmut_104, 
    'x_run_tests__mutmut_105': x_run_tests__mutmut_105, 
    'x_run_tests__mutmut_106': x_run_tests__mutmut_106, 
    'x_run_tests__mutmut_107': x_run_tests__mutmut_107, 
    'x_run_tests__mutmut_108': x_run_tests__mutmut_108, 
    'x_run_tests__mutmut_109': x_run_tests__mutmut_109, 
    'x_run_tests__mutmut_110': x_run_tests__mutmut_110, 
    'x_run_tests__mutmut_111': x_run_tests__mutmut_111, 
    'x_run_tests__mutmut_112': x_run_tests__mutmut_112, 
    'x_run_tests__mutmut_113': x_run_tests__mutmut_113, 
    'x_run_tests__mutmut_114': x_run_tests__mutmut_114, 
    'x_run_tests__mutmut_115': x_run_tests__mutmut_115, 
    'x_run_tests__mutmut_116': x_run_tests__mutmut_116, 
    'x_run_tests__mutmut_117': x_run_tests__mutmut_117, 
    'x_run_tests__mutmut_118': x_run_tests__mutmut_118, 
    'x_run_tests__mutmut_119': x_run_tests__mutmut_119, 
    'x_run_tests__mutmut_120': x_run_tests__mutmut_120, 
    'x_run_tests__mutmut_121': x_run_tests__mutmut_121, 
    'x_run_tests__mutmut_122': x_run_tests__mutmut_122, 
    'x_run_tests__mutmut_123': x_run_tests__mutmut_123, 
    'x_run_tests__mutmut_124': x_run_tests__mutmut_124, 
    'x_run_tests__mutmut_125': x_run_tests__mutmut_125, 
    'x_run_tests__mutmut_126': x_run_tests__mutmut_126, 
    'x_run_tests__mutmut_127': x_run_tests__mutmut_127, 
    'x_run_tests__mutmut_128': x_run_tests__mutmut_128, 
    'x_run_tests__mutmut_129': x_run_tests__mutmut_129, 
    'x_run_tests__mutmut_130': x_run_tests__mutmut_130, 
    'x_run_tests__mutmut_131': x_run_tests__mutmut_131, 
    'x_run_tests__mutmut_132': x_run_tests__mutmut_132, 
    'x_run_tests__mutmut_133': x_run_tests__mutmut_133, 
    'x_run_tests__mutmut_134': x_run_tests__mutmut_134, 
    'x_run_tests__mutmut_135': x_run_tests__mutmut_135, 
    'x_run_tests__mutmut_136': x_run_tests__mutmut_136, 
    'x_run_tests__mutmut_137': x_run_tests__mutmut_137, 
    'x_run_tests__mutmut_138': x_run_tests__mutmut_138, 
    'x_run_tests__mutmut_139': x_run_tests__mutmut_139, 
    'x_run_tests__mutmut_140': x_run_tests__mutmut_140, 
    'x_run_tests__mutmut_141': x_run_tests__mutmut_141, 
    'x_run_tests__mutmut_142': x_run_tests__mutmut_142, 
    'x_run_tests__mutmut_143': x_run_tests__mutmut_143, 
    'x_run_tests__mutmut_144': x_run_tests__mutmut_144, 
    'x_run_tests__mutmut_145': x_run_tests__mutmut_145, 
    'x_run_tests__mutmut_146': x_run_tests__mutmut_146, 
    'x_run_tests__mutmut_147': x_run_tests__mutmut_147, 
    'x_run_tests__mutmut_148': x_run_tests__mutmut_148, 
    'x_run_tests__mutmut_149': x_run_tests__mutmut_149, 
    'x_run_tests__mutmut_150': x_run_tests__mutmut_150, 
    'x_run_tests__mutmut_151': x_run_tests__mutmut_151, 
    'x_run_tests__mutmut_152': x_run_tests__mutmut_152, 
    'x_run_tests__mutmut_153': x_run_tests__mutmut_153, 
    'x_run_tests__mutmut_154': x_run_tests__mutmut_154, 
    'x_run_tests__mutmut_155': x_run_tests__mutmut_155, 
    'x_run_tests__mutmut_156': x_run_tests__mutmut_156, 
    'x_run_tests__mutmut_157': x_run_tests__mutmut_157, 
    'x_run_tests__mutmut_158': x_run_tests__mutmut_158, 
    'x_run_tests__mutmut_159': x_run_tests__mutmut_159, 
    'x_run_tests__mutmut_160': x_run_tests__mutmut_160, 
    'x_run_tests__mutmut_161': x_run_tests__mutmut_161, 
    'x_run_tests__mutmut_162': x_run_tests__mutmut_162, 
    'x_run_tests__mutmut_163': x_run_tests__mutmut_163
}

def run_tests(*args, **kwargs):
    result = _mutmut_trampoline(x_run_tests__mutmut_orig, x_run_tests__mutmut_mutants, *args, **kwargs)
    return result 

run_tests.__signature__ = _mutmut_signature(x_run_tests__mutmut_orig)
x_run_tests__mutmut_orig.__name__ = 'x_run_tests'




def x_analyze_go_tests__mutmut_orig(go_file_path, test_file_path):
    # Create a temporary directory within "/data/generated/golang/"
    base_temp_dir = "./data/generated/docs_golang/"
    os.makedirs(base_temp_dir, exist_ok=True)
    temp_dir = tempfile.mkdtemp(dir=base_temp_dir)

    # Save the current working directory
    original_cwd = os.getcwd()

    try:
        # Copy the Go file and the specified test file to the temporary directory
        new_go_file_path = shutil.copy(go_file_path, temp_dir)
        new_test_file_path = shutil.copy(test_file_path, temp_dir)

        # Change to temporary directory
        os.chdir(temp_dir)

        # Check syntax
        syntax_build = check_syntax(os.path.basename(test_file_path))
        syntax_vet, warnings = check_for_warnings(temp_dir)
        findings = run_golangci(os.path.basename(test_file_path))
        syntax = determine_syntax_result(syntax_build, syntax_vet)
        assertions_density = assertions_density_go(os.path.basename(test_file_path))
        mccabe = assertions_mccabe_ratio_go(os.path.basename(new_go_file_path), os.path.basename(new_test_file_path))

        if syntax != CompileStatus.OK:
            print("Syntax check failed.")
            return {
                "syntax": syntax,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "syntax_output": warnings,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "timeout": False,
                "internal_error_occurred": False,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
                "runtime_errors_count": None
            }

        # Run tests and get results
        test_results = run_tests(".")

        if test_results:
            test_results["warnings"] = findings
            test_results["warnings_count"] = len(findings) if findings is not None else None
            test_results["syntax"] = syntax
            test_results["syntax_output"] = warnings
            test_results["assertions_density"] = assertions_density
            test_results["assertions_mccabe_ratio"] = mccabe
            print(f"Percentage of passed tests: {test_results['test_pass_rate']}")
            print(f"Total time: {test_results['execution_time_sec']} seconds")
            if test_results is not None and test_results['line_coverage_percent'] is not None:
                print(f"Coverage: {test_results['line_coverage_percent']}%")
            else:
                print("Coverage information not available.")
            return test_results
        else:
            print("Failed to execute Go tests.")
            return {
                "syntax": syntax,
                "syntax_output": warnings,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "timeout": False,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "internal_error_occurred": True,
                "runtime_errors_count": None,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
            }

    finally:
        # Restore the original working directory
        os.chdir(original_cwd)
        # Clean up the temporary directory
        shutil.rmtree(temp_dir)
        print(f"Temporary directory {temp_dir} has been removed.")


def x_analyze_go_tests__mutmut_1(go_file_path, test_file_path):
    # Create a temporary directory within "/data/generated/golang/"
    base_temp_dir = "XX./data/generated/docs_golang/XX"
    os.makedirs(base_temp_dir, exist_ok=True)
    temp_dir = tempfile.mkdtemp(dir=base_temp_dir)

    # Save the current working directory
    original_cwd = os.getcwd()

    try:
        # Copy the Go file and the specified test file to the temporary directory
        new_go_file_path = shutil.copy(go_file_path, temp_dir)
        new_test_file_path = shutil.copy(test_file_path, temp_dir)

        # Change to temporary directory
        os.chdir(temp_dir)

        # Check syntax
        syntax_build = check_syntax(os.path.basename(test_file_path))
        syntax_vet, warnings = check_for_warnings(temp_dir)
        findings = run_golangci(os.path.basename(test_file_path))
        syntax = determine_syntax_result(syntax_build, syntax_vet)
        assertions_density = assertions_density_go(os.path.basename(test_file_path))
        mccabe = assertions_mccabe_ratio_go(os.path.basename(new_go_file_path), os.path.basename(new_test_file_path))

        if syntax != CompileStatus.OK:
            print("Syntax check failed.")
            return {
                "syntax": syntax,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "syntax_output": warnings,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "timeout": False,
                "internal_error_occurred": False,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
                "runtime_errors_count": None
            }

        # Run tests and get results
        test_results = run_tests(".")

        if test_results:
            test_results["warnings"] = findings
            test_results["warnings_count"] = len(findings) if findings is not None else None
            test_results["syntax"] = syntax
            test_results["syntax_output"] = warnings
            test_results["assertions_density"] = assertions_density
            test_results["assertions_mccabe_ratio"] = mccabe
            print(f"Percentage of passed tests: {test_results['test_pass_rate']}")
            print(f"Total time: {test_results['execution_time_sec']} seconds")
            if test_results is not None and test_results['line_coverage_percent'] is not None:
                print(f"Coverage: {test_results['line_coverage_percent']}%")
            else:
                print("Coverage information not available.")
            return test_results
        else:
            print("Failed to execute Go tests.")
            return {
                "syntax": syntax,
                "syntax_output": warnings,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "timeout": False,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "internal_error_occurred": True,
                "runtime_errors_count": None,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
            }

    finally:
        # Restore the original working directory
        os.chdir(original_cwd)
        # Clean up the temporary directory
        shutil.rmtree(temp_dir)
        print(f"Temporary directory {temp_dir} has been removed.")


def x_analyze_go_tests__mutmut_2(go_file_path, test_file_path):
    # Create a temporary directory within "/data/generated/golang/"
    base_temp_dir = None
    os.makedirs(base_temp_dir, exist_ok=True)
    temp_dir = tempfile.mkdtemp(dir=base_temp_dir)

    # Save the current working directory
    original_cwd = os.getcwd()

    try:
        # Copy the Go file and the specified test file to the temporary directory
        new_go_file_path = shutil.copy(go_file_path, temp_dir)
        new_test_file_path = shutil.copy(test_file_path, temp_dir)

        # Change to temporary directory
        os.chdir(temp_dir)

        # Check syntax
        syntax_build = check_syntax(os.path.basename(test_file_path))
        syntax_vet, warnings = check_for_warnings(temp_dir)
        findings = run_golangci(os.path.basename(test_file_path))
        syntax = determine_syntax_result(syntax_build, syntax_vet)
        assertions_density = assertions_density_go(os.path.basename(test_file_path))
        mccabe = assertions_mccabe_ratio_go(os.path.basename(new_go_file_path), os.path.basename(new_test_file_path))

        if syntax != CompileStatus.OK:
            print("Syntax check failed.")
            return {
                "syntax": syntax,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "syntax_output": warnings,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "timeout": False,
                "internal_error_occurred": False,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
                "runtime_errors_count": None
            }

        # Run tests and get results
        test_results = run_tests(".")

        if test_results:
            test_results["warnings"] = findings
            test_results["warnings_count"] = len(findings) if findings is not None else None
            test_results["syntax"] = syntax
            test_results["syntax_output"] = warnings
            test_results["assertions_density"] = assertions_density
            test_results["assertions_mccabe_ratio"] = mccabe
            print(f"Percentage of passed tests: {test_results['test_pass_rate']}")
            print(f"Total time: {test_results['execution_time_sec']} seconds")
            if test_results is not None and test_results['line_coverage_percent'] is not None:
                print(f"Coverage: {test_results['line_coverage_percent']}%")
            else:
                print("Coverage information not available.")
            return test_results
        else:
            print("Failed to execute Go tests.")
            return {
                "syntax": syntax,
                "syntax_output": warnings,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "timeout": False,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "internal_error_occurred": True,
                "runtime_errors_count": None,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
            }

    finally:
        # Restore the original working directory
        os.chdir(original_cwd)
        # Clean up the temporary directory
        shutil.rmtree(temp_dir)
        print(f"Temporary directory {temp_dir} has been removed.")


def x_analyze_go_tests__mutmut_3(go_file_path, test_file_path):
    # Create a temporary directory within "/data/generated/golang/"
    base_temp_dir = "./data/generated/docs_golang/"
    os.makedirs(None, exist_ok=True)
    temp_dir = tempfile.mkdtemp(dir=base_temp_dir)

    # Save the current working directory
    original_cwd = os.getcwd()

    try:
        # Copy the Go file and the specified test file to the temporary directory
        new_go_file_path = shutil.copy(go_file_path, temp_dir)
        new_test_file_path = shutil.copy(test_file_path, temp_dir)

        # Change to temporary directory
        os.chdir(temp_dir)

        # Check syntax
        syntax_build = check_syntax(os.path.basename(test_file_path))
        syntax_vet, warnings = check_for_warnings(temp_dir)
        findings = run_golangci(os.path.basename(test_file_path))
        syntax = determine_syntax_result(syntax_build, syntax_vet)
        assertions_density = assertions_density_go(os.path.basename(test_file_path))
        mccabe = assertions_mccabe_ratio_go(os.path.basename(new_go_file_path), os.path.basename(new_test_file_path))

        if syntax != CompileStatus.OK:
            print("Syntax check failed.")
            return {
                "syntax": syntax,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "syntax_output": warnings,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "timeout": False,
                "internal_error_occurred": False,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
                "runtime_errors_count": None
            }

        # Run tests and get results
        test_results = run_tests(".")

        if test_results:
            test_results["warnings"] = findings
            test_results["warnings_count"] = len(findings) if findings is not None else None
            test_results["syntax"] = syntax
            test_results["syntax_output"] = warnings
            test_results["assertions_density"] = assertions_density
            test_results["assertions_mccabe_ratio"] = mccabe
            print(f"Percentage of passed tests: {test_results['test_pass_rate']}")
            print(f"Total time: {test_results['execution_time_sec']} seconds")
            if test_results is not None and test_results['line_coverage_percent'] is not None:
                print(f"Coverage: {test_results['line_coverage_percent']}%")
            else:
                print("Coverage information not available.")
            return test_results
        else:
            print("Failed to execute Go tests.")
            return {
                "syntax": syntax,
                "syntax_output": warnings,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "timeout": False,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "internal_error_occurred": True,
                "runtime_errors_count": None,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
            }

    finally:
        # Restore the original working directory
        os.chdir(original_cwd)
        # Clean up the temporary directory
        shutil.rmtree(temp_dir)
        print(f"Temporary directory {temp_dir} has been removed.")


def x_analyze_go_tests__mutmut_4(go_file_path, test_file_path):
    # Create a temporary directory within "/data/generated/golang/"
    base_temp_dir = "./data/generated/docs_golang/"
    os.makedirs(base_temp_dir, exist_ok=False)
    temp_dir = tempfile.mkdtemp(dir=base_temp_dir)

    # Save the current working directory
    original_cwd = os.getcwd()

    try:
        # Copy the Go file and the specified test file to the temporary directory
        new_go_file_path = shutil.copy(go_file_path, temp_dir)
        new_test_file_path = shutil.copy(test_file_path, temp_dir)

        # Change to temporary directory
        os.chdir(temp_dir)

        # Check syntax
        syntax_build = check_syntax(os.path.basename(test_file_path))
        syntax_vet, warnings = check_for_warnings(temp_dir)
        findings = run_golangci(os.path.basename(test_file_path))
        syntax = determine_syntax_result(syntax_build, syntax_vet)
        assertions_density = assertions_density_go(os.path.basename(test_file_path))
        mccabe = assertions_mccabe_ratio_go(os.path.basename(new_go_file_path), os.path.basename(new_test_file_path))

        if syntax != CompileStatus.OK:
            print("Syntax check failed.")
            return {
                "syntax": syntax,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "syntax_output": warnings,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "timeout": False,
                "internal_error_occurred": False,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
                "runtime_errors_count": None
            }

        # Run tests and get results
        test_results = run_tests(".")

        if test_results:
            test_results["warnings"] = findings
            test_results["warnings_count"] = len(findings) if findings is not None else None
            test_results["syntax"] = syntax
            test_results["syntax_output"] = warnings
            test_results["assertions_density"] = assertions_density
            test_results["assertions_mccabe_ratio"] = mccabe
            print(f"Percentage of passed tests: {test_results['test_pass_rate']}")
            print(f"Total time: {test_results['execution_time_sec']} seconds")
            if test_results is not None and test_results['line_coverage_percent'] is not None:
                print(f"Coverage: {test_results['line_coverage_percent']}%")
            else:
                print("Coverage information not available.")
            return test_results
        else:
            print("Failed to execute Go tests.")
            return {
                "syntax": syntax,
                "syntax_output": warnings,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "timeout": False,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "internal_error_occurred": True,
                "runtime_errors_count": None,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
            }

    finally:
        # Restore the original working directory
        os.chdir(original_cwd)
        # Clean up the temporary directory
        shutil.rmtree(temp_dir)
        print(f"Temporary directory {temp_dir} has been removed.")


def x_analyze_go_tests__mutmut_5(go_file_path, test_file_path):
    # Create a temporary directory within "/data/generated/golang/"
    base_temp_dir = "./data/generated/docs_golang/"
    os.makedirs( exist_ok=True)
    temp_dir = tempfile.mkdtemp(dir=base_temp_dir)

    # Save the current working directory
    original_cwd = os.getcwd()

    try:
        # Copy the Go file and the specified test file to the temporary directory
        new_go_file_path = shutil.copy(go_file_path, temp_dir)
        new_test_file_path = shutil.copy(test_file_path, temp_dir)

        # Change to temporary directory
        os.chdir(temp_dir)

        # Check syntax
        syntax_build = check_syntax(os.path.basename(test_file_path))
        syntax_vet, warnings = check_for_warnings(temp_dir)
        findings = run_golangci(os.path.basename(test_file_path))
        syntax = determine_syntax_result(syntax_build, syntax_vet)
        assertions_density = assertions_density_go(os.path.basename(test_file_path))
        mccabe = assertions_mccabe_ratio_go(os.path.basename(new_go_file_path), os.path.basename(new_test_file_path))

        if syntax != CompileStatus.OK:
            print("Syntax check failed.")
            return {
                "syntax": syntax,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "syntax_output": warnings,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "timeout": False,
                "internal_error_occurred": False,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
                "runtime_errors_count": None
            }

        # Run tests and get results
        test_results = run_tests(".")

        if test_results:
            test_results["warnings"] = findings
            test_results["warnings_count"] = len(findings) if findings is not None else None
            test_results["syntax"] = syntax
            test_results["syntax_output"] = warnings
            test_results["assertions_density"] = assertions_density
            test_results["assertions_mccabe_ratio"] = mccabe
            print(f"Percentage of passed tests: {test_results['test_pass_rate']}")
            print(f"Total time: {test_results['execution_time_sec']} seconds")
            if test_results is not None and test_results['line_coverage_percent'] is not None:
                print(f"Coverage: {test_results['line_coverage_percent']}%")
            else:
                print("Coverage information not available.")
            return test_results
        else:
            print("Failed to execute Go tests.")
            return {
                "syntax": syntax,
                "syntax_output": warnings,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "timeout": False,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "internal_error_occurred": True,
                "runtime_errors_count": None,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
            }

    finally:
        # Restore the original working directory
        os.chdir(original_cwd)
        # Clean up the temporary directory
        shutil.rmtree(temp_dir)
        print(f"Temporary directory {temp_dir} has been removed.")


def x_analyze_go_tests__mutmut_6(go_file_path, test_file_path):
    # Create a temporary directory within "/data/generated/golang/"
    base_temp_dir = "./data/generated/docs_golang/"
    os.makedirs(base_temp_dir,)
    temp_dir = tempfile.mkdtemp(dir=base_temp_dir)

    # Save the current working directory
    original_cwd = os.getcwd()

    try:
        # Copy the Go file and the specified test file to the temporary directory
        new_go_file_path = shutil.copy(go_file_path, temp_dir)
        new_test_file_path = shutil.copy(test_file_path, temp_dir)

        # Change to temporary directory
        os.chdir(temp_dir)

        # Check syntax
        syntax_build = check_syntax(os.path.basename(test_file_path))
        syntax_vet, warnings = check_for_warnings(temp_dir)
        findings = run_golangci(os.path.basename(test_file_path))
        syntax = determine_syntax_result(syntax_build, syntax_vet)
        assertions_density = assertions_density_go(os.path.basename(test_file_path))
        mccabe = assertions_mccabe_ratio_go(os.path.basename(new_go_file_path), os.path.basename(new_test_file_path))

        if syntax != CompileStatus.OK:
            print("Syntax check failed.")
            return {
                "syntax": syntax,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "syntax_output": warnings,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "timeout": False,
                "internal_error_occurred": False,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
                "runtime_errors_count": None
            }

        # Run tests and get results
        test_results = run_tests(".")

        if test_results:
            test_results["warnings"] = findings
            test_results["warnings_count"] = len(findings) if findings is not None else None
            test_results["syntax"] = syntax
            test_results["syntax_output"] = warnings
            test_results["assertions_density"] = assertions_density
            test_results["assertions_mccabe_ratio"] = mccabe
            print(f"Percentage of passed tests: {test_results['test_pass_rate']}")
            print(f"Total time: {test_results['execution_time_sec']} seconds")
            if test_results is not None and test_results['line_coverage_percent'] is not None:
                print(f"Coverage: {test_results['line_coverage_percent']}%")
            else:
                print("Coverage information not available.")
            return test_results
        else:
            print("Failed to execute Go tests.")
            return {
                "syntax": syntax,
                "syntax_output": warnings,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "timeout": False,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "internal_error_occurred": True,
                "runtime_errors_count": None,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
            }

    finally:
        # Restore the original working directory
        os.chdir(original_cwd)
        # Clean up the temporary directory
        shutil.rmtree(temp_dir)
        print(f"Temporary directory {temp_dir} has been removed.")


def x_analyze_go_tests__mutmut_7(go_file_path, test_file_path):
    # Create a temporary directory within "/data/generated/golang/"
    base_temp_dir = "./data/generated/docs_golang/"
    os.makedirs(base_temp_dir, exist_ok=True)
    temp_dir = tempfile.mkdtemp(dir=None)

    # Save the current working directory
    original_cwd = os.getcwd()

    try:
        # Copy the Go file and the specified test file to the temporary directory
        new_go_file_path = shutil.copy(go_file_path, temp_dir)
        new_test_file_path = shutil.copy(test_file_path, temp_dir)

        # Change to temporary directory
        os.chdir(temp_dir)

        # Check syntax
        syntax_build = check_syntax(os.path.basename(test_file_path))
        syntax_vet, warnings = check_for_warnings(temp_dir)
        findings = run_golangci(os.path.basename(test_file_path))
        syntax = determine_syntax_result(syntax_build, syntax_vet)
        assertions_density = assertions_density_go(os.path.basename(test_file_path))
        mccabe = assertions_mccabe_ratio_go(os.path.basename(new_go_file_path), os.path.basename(new_test_file_path))

        if syntax != CompileStatus.OK:
            print("Syntax check failed.")
            return {
                "syntax": syntax,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "syntax_output": warnings,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "timeout": False,
                "internal_error_occurred": False,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
                "runtime_errors_count": None
            }

        # Run tests and get results
        test_results = run_tests(".")

        if test_results:
            test_results["warnings"] = findings
            test_results["warnings_count"] = len(findings) if findings is not None else None
            test_results["syntax"] = syntax
            test_results["syntax_output"] = warnings
            test_results["assertions_density"] = assertions_density
            test_results["assertions_mccabe_ratio"] = mccabe
            print(f"Percentage of passed tests: {test_results['test_pass_rate']}")
            print(f"Total time: {test_results['execution_time_sec']} seconds")
            if test_results is not None and test_results['line_coverage_percent'] is not None:
                print(f"Coverage: {test_results['line_coverage_percent']}%")
            else:
                print("Coverage information not available.")
            return test_results
        else:
            print("Failed to execute Go tests.")
            return {
                "syntax": syntax,
                "syntax_output": warnings,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "timeout": False,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "internal_error_occurred": True,
                "runtime_errors_count": None,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
            }

    finally:
        # Restore the original working directory
        os.chdir(original_cwd)
        # Clean up the temporary directory
        shutil.rmtree(temp_dir)
        print(f"Temporary directory {temp_dir} has been removed.")


def x_analyze_go_tests__mutmut_8(go_file_path, test_file_path):
    # Create a temporary directory within "/data/generated/golang/"
    base_temp_dir = "./data/generated/docs_golang/"
    os.makedirs(base_temp_dir, exist_ok=True)
    temp_dir = None

    # Save the current working directory
    original_cwd = os.getcwd()

    try:
        # Copy the Go file and the specified test file to the temporary directory
        new_go_file_path = shutil.copy(go_file_path, temp_dir)
        new_test_file_path = shutil.copy(test_file_path, temp_dir)

        # Change to temporary directory
        os.chdir(temp_dir)

        # Check syntax
        syntax_build = check_syntax(os.path.basename(test_file_path))
        syntax_vet, warnings = check_for_warnings(temp_dir)
        findings = run_golangci(os.path.basename(test_file_path))
        syntax = determine_syntax_result(syntax_build, syntax_vet)
        assertions_density = assertions_density_go(os.path.basename(test_file_path))
        mccabe = assertions_mccabe_ratio_go(os.path.basename(new_go_file_path), os.path.basename(new_test_file_path))

        if syntax != CompileStatus.OK:
            print("Syntax check failed.")
            return {
                "syntax": syntax,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "syntax_output": warnings,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "timeout": False,
                "internal_error_occurred": False,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
                "runtime_errors_count": None
            }

        # Run tests and get results
        test_results = run_tests(".")

        if test_results:
            test_results["warnings"] = findings
            test_results["warnings_count"] = len(findings) if findings is not None else None
            test_results["syntax"] = syntax
            test_results["syntax_output"] = warnings
            test_results["assertions_density"] = assertions_density
            test_results["assertions_mccabe_ratio"] = mccabe
            print(f"Percentage of passed tests: {test_results['test_pass_rate']}")
            print(f"Total time: {test_results['execution_time_sec']} seconds")
            if test_results is not None and test_results['line_coverage_percent'] is not None:
                print(f"Coverage: {test_results['line_coverage_percent']}%")
            else:
                print("Coverage information not available.")
            return test_results
        else:
            print("Failed to execute Go tests.")
            return {
                "syntax": syntax,
                "syntax_output": warnings,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "timeout": False,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "internal_error_occurred": True,
                "runtime_errors_count": None,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
            }

    finally:
        # Restore the original working directory
        os.chdir(original_cwd)
        # Clean up the temporary directory
        shutil.rmtree(temp_dir)
        print(f"Temporary directory {temp_dir} has been removed.")


def x_analyze_go_tests__mutmut_9(go_file_path, test_file_path):
    # Create a temporary directory within "/data/generated/golang/"
    base_temp_dir = "./data/generated/docs_golang/"
    os.makedirs(base_temp_dir, exist_ok=True)
    temp_dir = tempfile.mkdtemp(dir=base_temp_dir)

    # Save the current working directory
    original_cwd = None

    try:
        # Copy the Go file and the specified test file to the temporary directory
        new_go_file_path = shutil.copy(go_file_path, temp_dir)
        new_test_file_path = shutil.copy(test_file_path, temp_dir)

        # Change to temporary directory
        os.chdir(temp_dir)

        # Check syntax
        syntax_build = check_syntax(os.path.basename(test_file_path))
        syntax_vet, warnings = check_for_warnings(temp_dir)
        findings = run_golangci(os.path.basename(test_file_path))
        syntax = determine_syntax_result(syntax_build, syntax_vet)
        assertions_density = assertions_density_go(os.path.basename(test_file_path))
        mccabe = assertions_mccabe_ratio_go(os.path.basename(new_go_file_path), os.path.basename(new_test_file_path))

        if syntax != CompileStatus.OK:
            print("Syntax check failed.")
            return {
                "syntax": syntax,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "syntax_output": warnings,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "timeout": False,
                "internal_error_occurred": False,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
                "runtime_errors_count": None
            }

        # Run tests and get results
        test_results = run_tests(".")

        if test_results:
            test_results["warnings"] = findings
            test_results["warnings_count"] = len(findings) if findings is not None else None
            test_results["syntax"] = syntax
            test_results["syntax_output"] = warnings
            test_results["assertions_density"] = assertions_density
            test_results["assertions_mccabe_ratio"] = mccabe
            print(f"Percentage of passed tests: {test_results['test_pass_rate']}")
            print(f"Total time: {test_results['execution_time_sec']} seconds")
            if test_results is not None and test_results['line_coverage_percent'] is not None:
                print(f"Coverage: {test_results['line_coverage_percent']}%")
            else:
                print("Coverage information not available.")
            return test_results
        else:
            print("Failed to execute Go tests.")
            return {
                "syntax": syntax,
                "syntax_output": warnings,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "timeout": False,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "internal_error_occurred": True,
                "runtime_errors_count": None,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
            }

    finally:
        # Restore the original working directory
        os.chdir(original_cwd)
        # Clean up the temporary directory
        shutil.rmtree(temp_dir)
        print(f"Temporary directory {temp_dir} has been removed.")


def x_analyze_go_tests__mutmut_10(go_file_path, test_file_path):
    # Create a temporary directory within "/data/generated/golang/"
    base_temp_dir = "./data/generated/docs_golang/"
    os.makedirs(base_temp_dir, exist_ok=True)
    temp_dir = tempfile.mkdtemp(dir=base_temp_dir)

    # Save the current working directory
    original_cwd = os.getcwd()

    try:
        # Copy the Go file and the specified test file to the temporary directory
        new_go_file_path = shutil.copy(None, temp_dir)
        new_test_file_path = shutil.copy(test_file_path, temp_dir)

        # Change to temporary directory
        os.chdir(temp_dir)

        # Check syntax
        syntax_build = check_syntax(os.path.basename(test_file_path))
        syntax_vet, warnings = check_for_warnings(temp_dir)
        findings = run_golangci(os.path.basename(test_file_path))
        syntax = determine_syntax_result(syntax_build, syntax_vet)
        assertions_density = assertions_density_go(os.path.basename(test_file_path))
        mccabe = assertions_mccabe_ratio_go(os.path.basename(new_go_file_path), os.path.basename(new_test_file_path))

        if syntax != CompileStatus.OK:
            print("Syntax check failed.")
            return {
                "syntax": syntax,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "syntax_output": warnings,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "timeout": False,
                "internal_error_occurred": False,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
                "runtime_errors_count": None
            }

        # Run tests and get results
        test_results = run_tests(".")

        if test_results:
            test_results["warnings"] = findings
            test_results["warnings_count"] = len(findings) if findings is not None else None
            test_results["syntax"] = syntax
            test_results["syntax_output"] = warnings
            test_results["assertions_density"] = assertions_density
            test_results["assertions_mccabe_ratio"] = mccabe
            print(f"Percentage of passed tests: {test_results['test_pass_rate']}")
            print(f"Total time: {test_results['execution_time_sec']} seconds")
            if test_results is not None and test_results['line_coverage_percent'] is not None:
                print(f"Coverage: {test_results['line_coverage_percent']}%")
            else:
                print("Coverage information not available.")
            return test_results
        else:
            print("Failed to execute Go tests.")
            return {
                "syntax": syntax,
                "syntax_output": warnings,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "timeout": False,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "internal_error_occurred": True,
                "runtime_errors_count": None,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
            }

    finally:
        # Restore the original working directory
        os.chdir(original_cwd)
        # Clean up the temporary directory
        shutil.rmtree(temp_dir)
        print(f"Temporary directory {temp_dir} has been removed.")


def x_analyze_go_tests__mutmut_11(go_file_path, test_file_path):
    # Create a temporary directory within "/data/generated/golang/"
    base_temp_dir = "./data/generated/docs_golang/"
    os.makedirs(base_temp_dir, exist_ok=True)
    temp_dir = tempfile.mkdtemp(dir=base_temp_dir)

    # Save the current working directory
    original_cwd = os.getcwd()

    try:
        # Copy the Go file and the specified test file to the temporary directory
        new_go_file_path = shutil.copy(go_file_path, None)
        new_test_file_path = shutil.copy(test_file_path, temp_dir)

        # Change to temporary directory
        os.chdir(temp_dir)

        # Check syntax
        syntax_build = check_syntax(os.path.basename(test_file_path))
        syntax_vet, warnings = check_for_warnings(temp_dir)
        findings = run_golangci(os.path.basename(test_file_path))
        syntax = determine_syntax_result(syntax_build, syntax_vet)
        assertions_density = assertions_density_go(os.path.basename(test_file_path))
        mccabe = assertions_mccabe_ratio_go(os.path.basename(new_go_file_path), os.path.basename(new_test_file_path))

        if syntax != CompileStatus.OK:
            print("Syntax check failed.")
            return {
                "syntax": syntax,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "syntax_output": warnings,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "timeout": False,
                "internal_error_occurred": False,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
                "runtime_errors_count": None
            }

        # Run tests and get results
        test_results = run_tests(".")

        if test_results:
            test_results["warnings"] = findings
            test_results["warnings_count"] = len(findings) if findings is not None else None
            test_results["syntax"] = syntax
            test_results["syntax_output"] = warnings
            test_results["assertions_density"] = assertions_density
            test_results["assertions_mccabe_ratio"] = mccabe
            print(f"Percentage of passed tests: {test_results['test_pass_rate']}")
            print(f"Total time: {test_results['execution_time_sec']} seconds")
            if test_results is not None and test_results['line_coverage_percent'] is not None:
                print(f"Coverage: {test_results['line_coverage_percent']}%")
            else:
                print("Coverage information not available.")
            return test_results
        else:
            print("Failed to execute Go tests.")
            return {
                "syntax": syntax,
                "syntax_output": warnings,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "timeout": False,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "internal_error_occurred": True,
                "runtime_errors_count": None,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
            }

    finally:
        # Restore the original working directory
        os.chdir(original_cwd)
        # Clean up the temporary directory
        shutil.rmtree(temp_dir)
        print(f"Temporary directory {temp_dir} has been removed.")


def x_analyze_go_tests__mutmut_12(go_file_path, test_file_path):
    # Create a temporary directory within "/data/generated/golang/"
    base_temp_dir = "./data/generated/docs_golang/"
    os.makedirs(base_temp_dir, exist_ok=True)
    temp_dir = tempfile.mkdtemp(dir=base_temp_dir)

    # Save the current working directory
    original_cwd = os.getcwd()

    try:
        # Copy the Go file and the specified test file to the temporary directory
        new_go_file_path = shutil.copy( temp_dir)
        new_test_file_path = shutil.copy(test_file_path, temp_dir)

        # Change to temporary directory
        os.chdir(temp_dir)

        # Check syntax
        syntax_build = check_syntax(os.path.basename(test_file_path))
        syntax_vet, warnings = check_for_warnings(temp_dir)
        findings = run_golangci(os.path.basename(test_file_path))
        syntax = determine_syntax_result(syntax_build, syntax_vet)
        assertions_density = assertions_density_go(os.path.basename(test_file_path))
        mccabe = assertions_mccabe_ratio_go(os.path.basename(new_go_file_path), os.path.basename(new_test_file_path))

        if syntax != CompileStatus.OK:
            print("Syntax check failed.")
            return {
                "syntax": syntax,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "syntax_output": warnings,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "timeout": False,
                "internal_error_occurred": False,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
                "runtime_errors_count": None
            }

        # Run tests and get results
        test_results = run_tests(".")

        if test_results:
            test_results["warnings"] = findings
            test_results["warnings_count"] = len(findings) if findings is not None else None
            test_results["syntax"] = syntax
            test_results["syntax_output"] = warnings
            test_results["assertions_density"] = assertions_density
            test_results["assertions_mccabe_ratio"] = mccabe
            print(f"Percentage of passed tests: {test_results['test_pass_rate']}")
            print(f"Total time: {test_results['execution_time_sec']} seconds")
            if test_results is not None and test_results['line_coverage_percent'] is not None:
                print(f"Coverage: {test_results['line_coverage_percent']}%")
            else:
                print("Coverage information not available.")
            return test_results
        else:
            print("Failed to execute Go tests.")
            return {
                "syntax": syntax,
                "syntax_output": warnings,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "timeout": False,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "internal_error_occurred": True,
                "runtime_errors_count": None,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
            }

    finally:
        # Restore the original working directory
        os.chdir(original_cwd)
        # Clean up the temporary directory
        shutil.rmtree(temp_dir)
        print(f"Temporary directory {temp_dir} has been removed.")


def x_analyze_go_tests__mutmut_13(go_file_path, test_file_path):
    # Create a temporary directory within "/data/generated/golang/"
    base_temp_dir = "./data/generated/docs_golang/"
    os.makedirs(base_temp_dir, exist_ok=True)
    temp_dir = tempfile.mkdtemp(dir=base_temp_dir)

    # Save the current working directory
    original_cwd = os.getcwd()

    try:
        # Copy the Go file and the specified test file to the temporary directory
        new_go_file_path = shutil.copy(go_file_path,)
        new_test_file_path = shutil.copy(test_file_path, temp_dir)

        # Change to temporary directory
        os.chdir(temp_dir)

        # Check syntax
        syntax_build = check_syntax(os.path.basename(test_file_path))
        syntax_vet, warnings = check_for_warnings(temp_dir)
        findings = run_golangci(os.path.basename(test_file_path))
        syntax = determine_syntax_result(syntax_build, syntax_vet)
        assertions_density = assertions_density_go(os.path.basename(test_file_path))
        mccabe = assertions_mccabe_ratio_go(os.path.basename(new_go_file_path), os.path.basename(new_test_file_path))

        if syntax != CompileStatus.OK:
            print("Syntax check failed.")
            return {
                "syntax": syntax,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "syntax_output": warnings,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "timeout": False,
                "internal_error_occurred": False,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
                "runtime_errors_count": None
            }

        # Run tests and get results
        test_results = run_tests(".")

        if test_results:
            test_results["warnings"] = findings
            test_results["warnings_count"] = len(findings) if findings is not None else None
            test_results["syntax"] = syntax
            test_results["syntax_output"] = warnings
            test_results["assertions_density"] = assertions_density
            test_results["assertions_mccabe_ratio"] = mccabe
            print(f"Percentage of passed tests: {test_results['test_pass_rate']}")
            print(f"Total time: {test_results['execution_time_sec']} seconds")
            if test_results is not None and test_results['line_coverage_percent'] is not None:
                print(f"Coverage: {test_results['line_coverage_percent']}%")
            else:
                print("Coverage information not available.")
            return test_results
        else:
            print("Failed to execute Go tests.")
            return {
                "syntax": syntax,
                "syntax_output": warnings,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "timeout": False,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "internal_error_occurred": True,
                "runtime_errors_count": None,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
            }

    finally:
        # Restore the original working directory
        os.chdir(original_cwd)
        # Clean up the temporary directory
        shutil.rmtree(temp_dir)
        print(f"Temporary directory {temp_dir} has been removed.")


def x_analyze_go_tests__mutmut_14(go_file_path, test_file_path):
    # Create a temporary directory within "/data/generated/golang/"
    base_temp_dir = "./data/generated/docs_golang/"
    os.makedirs(base_temp_dir, exist_ok=True)
    temp_dir = tempfile.mkdtemp(dir=base_temp_dir)

    # Save the current working directory
    original_cwd = os.getcwd()

    try:
        # Copy the Go file and the specified test file to the temporary directory
        new_go_file_path = None
        new_test_file_path = shutil.copy(test_file_path, temp_dir)

        # Change to temporary directory
        os.chdir(temp_dir)

        # Check syntax
        syntax_build = check_syntax(os.path.basename(test_file_path))
        syntax_vet, warnings = check_for_warnings(temp_dir)
        findings = run_golangci(os.path.basename(test_file_path))
        syntax = determine_syntax_result(syntax_build, syntax_vet)
        assertions_density = assertions_density_go(os.path.basename(test_file_path))
        mccabe = assertions_mccabe_ratio_go(os.path.basename(new_go_file_path), os.path.basename(new_test_file_path))

        if syntax != CompileStatus.OK:
            print("Syntax check failed.")
            return {
                "syntax": syntax,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "syntax_output": warnings,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "timeout": False,
                "internal_error_occurred": False,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
                "runtime_errors_count": None
            }

        # Run tests and get results
        test_results = run_tests(".")

        if test_results:
            test_results["warnings"] = findings
            test_results["warnings_count"] = len(findings) if findings is not None else None
            test_results["syntax"] = syntax
            test_results["syntax_output"] = warnings
            test_results["assertions_density"] = assertions_density
            test_results["assertions_mccabe_ratio"] = mccabe
            print(f"Percentage of passed tests: {test_results['test_pass_rate']}")
            print(f"Total time: {test_results['execution_time_sec']} seconds")
            if test_results is not None and test_results['line_coverage_percent'] is not None:
                print(f"Coverage: {test_results['line_coverage_percent']}%")
            else:
                print("Coverage information not available.")
            return test_results
        else:
            print("Failed to execute Go tests.")
            return {
                "syntax": syntax,
                "syntax_output": warnings,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "timeout": False,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "internal_error_occurred": True,
                "runtime_errors_count": None,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
            }

    finally:
        # Restore the original working directory
        os.chdir(original_cwd)
        # Clean up the temporary directory
        shutil.rmtree(temp_dir)
        print(f"Temporary directory {temp_dir} has been removed.")


def x_analyze_go_tests__mutmut_15(go_file_path, test_file_path):
    # Create a temporary directory within "/data/generated/golang/"
    base_temp_dir = "./data/generated/docs_golang/"
    os.makedirs(base_temp_dir, exist_ok=True)
    temp_dir = tempfile.mkdtemp(dir=base_temp_dir)

    # Save the current working directory
    original_cwd = os.getcwd()

    try:
        # Copy the Go file and the specified test file to the temporary directory
        new_go_file_path = shutil.copy(go_file_path, temp_dir)
        new_test_file_path = shutil.copy(None, temp_dir)

        # Change to temporary directory
        os.chdir(temp_dir)

        # Check syntax
        syntax_build = check_syntax(os.path.basename(test_file_path))
        syntax_vet, warnings = check_for_warnings(temp_dir)
        findings = run_golangci(os.path.basename(test_file_path))
        syntax = determine_syntax_result(syntax_build, syntax_vet)
        assertions_density = assertions_density_go(os.path.basename(test_file_path))
        mccabe = assertions_mccabe_ratio_go(os.path.basename(new_go_file_path), os.path.basename(new_test_file_path))

        if syntax != CompileStatus.OK:
            print("Syntax check failed.")
            return {
                "syntax": syntax,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "syntax_output": warnings,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "timeout": False,
                "internal_error_occurred": False,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
                "runtime_errors_count": None
            }

        # Run tests and get results
        test_results = run_tests(".")

        if test_results:
            test_results["warnings"] = findings
            test_results["warnings_count"] = len(findings) if findings is not None else None
            test_results["syntax"] = syntax
            test_results["syntax_output"] = warnings
            test_results["assertions_density"] = assertions_density
            test_results["assertions_mccabe_ratio"] = mccabe
            print(f"Percentage of passed tests: {test_results['test_pass_rate']}")
            print(f"Total time: {test_results['execution_time_sec']} seconds")
            if test_results is not None and test_results['line_coverage_percent'] is not None:
                print(f"Coverage: {test_results['line_coverage_percent']}%")
            else:
                print("Coverage information not available.")
            return test_results
        else:
            print("Failed to execute Go tests.")
            return {
                "syntax": syntax,
                "syntax_output": warnings,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "timeout": False,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "internal_error_occurred": True,
                "runtime_errors_count": None,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
            }

    finally:
        # Restore the original working directory
        os.chdir(original_cwd)
        # Clean up the temporary directory
        shutil.rmtree(temp_dir)
        print(f"Temporary directory {temp_dir} has been removed.")


def x_analyze_go_tests__mutmut_16(go_file_path, test_file_path):
    # Create a temporary directory within "/data/generated/golang/"
    base_temp_dir = "./data/generated/docs_golang/"
    os.makedirs(base_temp_dir, exist_ok=True)
    temp_dir = tempfile.mkdtemp(dir=base_temp_dir)

    # Save the current working directory
    original_cwd = os.getcwd()

    try:
        # Copy the Go file and the specified test file to the temporary directory
        new_go_file_path = shutil.copy(go_file_path, temp_dir)
        new_test_file_path = shutil.copy(test_file_path, None)

        # Change to temporary directory
        os.chdir(temp_dir)

        # Check syntax
        syntax_build = check_syntax(os.path.basename(test_file_path))
        syntax_vet, warnings = check_for_warnings(temp_dir)
        findings = run_golangci(os.path.basename(test_file_path))
        syntax = determine_syntax_result(syntax_build, syntax_vet)
        assertions_density = assertions_density_go(os.path.basename(test_file_path))
        mccabe = assertions_mccabe_ratio_go(os.path.basename(new_go_file_path), os.path.basename(new_test_file_path))

        if syntax != CompileStatus.OK:
            print("Syntax check failed.")
            return {
                "syntax": syntax,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "syntax_output": warnings,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "timeout": False,
                "internal_error_occurred": False,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
                "runtime_errors_count": None
            }

        # Run tests and get results
        test_results = run_tests(".")

        if test_results:
            test_results["warnings"] = findings
            test_results["warnings_count"] = len(findings) if findings is not None else None
            test_results["syntax"] = syntax
            test_results["syntax_output"] = warnings
            test_results["assertions_density"] = assertions_density
            test_results["assertions_mccabe_ratio"] = mccabe
            print(f"Percentage of passed tests: {test_results['test_pass_rate']}")
            print(f"Total time: {test_results['execution_time_sec']} seconds")
            if test_results is not None and test_results['line_coverage_percent'] is not None:
                print(f"Coverage: {test_results['line_coverage_percent']}%")
            else:
                print("Coverage information not available.")
            return test_results
        else:
            print("Failed to execute Go tests.")
            return {
                "syntax": syntax,
                "syntax_output": warnings,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "timeout": False,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "internal_error_occurred": True,
                "runtime_errors_count": None,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
            }

    finally:
        # Restore the original working directory
        os.chdir(original_cwd)
        # Clean up the temporary directory
        shutil.rmtree(temp_dir)
        print(f"Temporary directory {temp_dir} has been removed.")


def x_analyze_go_tests__mutmut_17(go_file_path, test_file_path):
    # Create a temporary directory within "/data/generated/golang/"
    base_temp_dir = "./data/generated/docs_golang/"
    os.makedirs(base_temp_dir, exist_ok=True)
    temp_dir = tempfile.mkdtemp(dir=base_temp_dir)

    # Save the current working directory
    original_cwd = os.getcwd()

    try:
        # Copy the Go file and the specified test file to the temporary directory
        new_go_file_path = shutil.copy(go_file_path, temp_dir)
        new_test_file_path = shutil.copy( temp_dir)

        # Change to temporary directory
        os.chdir(temp_dir)

        # Check syntax
        syntax_build = check_syntax(os.path.basename(test_file_path))
        syntax_vet, warnings = check_for_warnings(temp_dir)
        findings = run_golangci(os.path.basename(test_file_path))
        syntax = determine_syntax_result(syntax_build, syntax_vet)
        assertions_density = assertions_density_go(os.path.basename(test_file_path))
        mccabe = assertions_mccabe_ratio_go(os.path.basename(new_go_file_path), os.path.basename(new_test_file_path))

        if syntax != CompileStatus.OK:
            print("Syntax check failed.")
            return {
                "syntax": syntax,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "syntax_output": warnings,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "timeout": False,
                "internal_error_occurred": False,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
                "runtime_errors_count": None
            }

        # Run tests and get results
        test_results = run_tests(".")

        if test_results:
            test_results["warnings"] = findings
            test_results["warnings_count"] = len(findings) if findings is not None else None
            test_results["syntax"] = syntax
            test_results["syntax_output"] = warnings
            test_results["assertions_density"] = assertions_density
            test_results["assertions_mccabe_ratio"] = mccabe
            print(f"Percentage of passed tests: {test_results['test_pass_rate']}")
            print(f"Total time: {test_results['execution_time_sec']} seconds")
            if test_results is not None and test_results['line_coverage_percent'] is not None:
                print(f"Coverage: {test_results['line_coverage_percent']}%")
            else:
                print("Coverage information not available.")
            return test_results
        else:
            print("Failed to execute Go tests.")
            return {
                "syntax": syntax,
                "syntax_output": warnings,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "timeout": False,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "internal_error_occurred": True,
                "runtime_errors_count": None,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
            }

    finally:
        # Restore the original working directory
        os.chdir(original_cwd)
        # Clean up the temporary directory
        shutil.rmtree(temp_dir)
        print(f"Temporary directory {temp_dir} has been removed.")


def x_analyze_go_tests__mutmut_18(go_file_path, test_file_path):
    # Create a temporary directory within "/data/generated/golang/"
    base_temp_dir = "./data/generated/docs_golang/"
    os.makedirs(base_temp_dir, exist_ok=True)
    temp_dir = tempfile.mkdtemp(dir=base_temp_dir)

    # Save the current working directory
    original_cwd = os.getcwd()

    try:
        # Copy the Go file and the specified test file to the temporary directory
        new_go_file_path = shutil.copy(go_file_path, temp_dir)
        new_test_file_path = shutil.copy(test_file_path,)

        # Change to temporary directory
        os.chdir(temp_dir)

        # Check syntax
        syntax_build = check_syntax(os.path.basename(test_file_path))
        syntax_vet, warnings = check_for_warnings(temp_dir)
        findings = run_golangci(os.path.basename(test_file_path))
        syntax = determine_syntax_result(syntax_build, syntax_vet)
        assertions_density = assertions_density_go(os.path.basename(test_file_path))
        mccabe = assertions_mccabe_ratio_go(os.path.basename(new_go_file_path), os.path.basename(new_test_file_path))

        if syntax != CompileStatus.OK:
            print("Syntax check failed.")
            return {
                "syntax": syntax,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "syntax_output": warnings,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "timeout": False,
                "internal_error_occurred": False,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
                "runtime_errors_count": None
            }

        # Run tests and get results
        test_results = run_tests(".")

        if test_results:
            test_results["warnings"] = findings
            test_results["warnings_count"] = len(findings) if findings is not None else None
            test_results["syntax"] = syntax
            test_results["syntax_output"] = warnings
            test_results["assertions_density"] = assertions_density
            test_results["assertions_mccabe_ratio"] = mccabe
            print(f"Percentage of passed tests: {test_results['test_pass_rate']}")
            print(f"Total time: {test_results['execution_time_sec']} seconds")
            if test_results is not None and test_results['line_coverage_percent'] is not None:
                print(f"Coverage: {test_results['line_coverage_percent']}%")
            else:
                print("Coverage information not available.")
            return test_results
        else:
            print("Failed to execute Go tests.")
            return {
                "syntax": syntax,
                "syntax_output": warnings,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "timeout": False,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "internal_error_occurred": True,
                "runtime_errors_count": None,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
            }

    finally:
        # Restore the original working directory
        os.chdir(original_cwd)
        # Clean up the temporary directory
        shutil.rmtree(temp_dir)
        print(f"Temporary directory {temp_dir} has been removed.")


def x_analyze_go_tests__mutmut_19(go_file_path, test_file_path):
    # Create a temporary directory within "/data/generated/golang/"
    base_temp_dir = "./data/generated/docs_golang/"
    os.makedirs(base_temp_dir, exist_ok=True)
    temp_dir = tempfile.mkdtemp(dir=base_temp_dir)

    # Save the current working directory
    original_cwd = os.getcwd()

    try:
        # Copy the Go file and the specified test file to the temporary directory
        new_go_file_path = shutil.copy(go_file_path, temp_dir)
        new_test_file_path = None

        # Change to temporary directory
        os.chdir(temp_dir)

        # Check syntax
        syntax_build = check_syntax(os.path.basename(test_file_path))
        syntax_vet, warnings = check_for_warnings(temp_dir)
        findings = run_golangci(os.path.basename(test_file_path))
        syntax = determine_syntax_result(syntax_build, syntax_vet)
        assertions_density = assertions_density_go(os.path.basename(test_file_path))
        mccabe = assertions_mccabe_ratio_go(os.path.basename(new_go_file_path), os.path.basename(new_test_file_path))

        if syntax != CompileStatus.OK:
            print("Syntax check failed.")
            return {
                "syntax": syntax,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "syntax_output": warnings,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "timeout": False,
                "internal_error_occurred": False,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
                "runtime_errors_count": None
            }

        # Run tests and get results
        test_results = run_tests(".")

        if test_results:
            test_results["warnings"] = findings
            test_results["warnings_count"] = len(findings) if findings is not None else None
            test_results["syntax"] = syntax
            test_results["syntax_output"] = warnings
            test_results["assertions_density"] = assertions_density
            test_results["assertions_mccabe_ratio"] = mccabe
            print(f"Percentage of passed tests: {test_results['test_pass_rate']}")
            print(f"Total time: {test_results['execution_time_sec']} seconds")
            if test_results is not None and test_results['line_coverage_percent'] is not None:
                print(f"Coverage: {test_results['line_coverage_percent']}%")
            else:
                print("Coverage information not available.")
            return test_results
        else:
            print("Failed to execute Go tests.")
            return {
                "syntax": syntax,
                "syntax_output": warnings,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "timeout": False,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "internal_error_occurred": True,
                "runtime_errors_count": None,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
            }

    finally:
        # Restore the original working directory
        os.chdir(original_cwd)
        # Clean up the temporary directory
        shutil.rmtree(temp_dir)
        print(f"Temporary directory {temp_dir} has been removed.")


def x_analyze_go_tests__mutmut_20(go_file_path, test_file_path):
    # Create a temporary directory within "/data/generated/golang/"
    base_temp_dir = "./data/generated/docs_golang/"
    os.makedirs(base_temp_dir, exist_ok=True)
    temp_dir = tempfile.mkdtemp(dir=base_temp_dir)

    # Save the current working directory
    original_cwd = os.getcwd()

    try:
        # Copy the Go file and the specified test file to the temporary directory
        new_go_file_path = shutil.copy(go_file_path, temp_dir)
        new_test_file_path = shutil.copy(test_file_path, temp_dir)

        # Change to temporary directory
        os.chdir(None)

        # Check syntax
        syntax_build = check_syntax(os.path.basename(test_file_path))
        syntax_vet, warnings = check_for_warnings(temp_dir)
        findings = run_golangci(os.path.basename(test_file_path))
        syntax = determine_syntax_result(syntax_build, syntax_vet)
        assertions_density = assertions_density_go(os.path.basename(test_file_path))
        mccabe = assertions_mccabe_ratio_go(os.path.basename(new_go_file_path), os.path.basename(new_test_file_path))

        if syntax != CompileStatus.OK:
            print("Syntax check failed.")
            return {
                "syntax": syntax,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "syntax_output": warnings,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "timeout": False,
                "internal_error_occurred": False,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
                "runtime_errors_count": None
            }

        # Run tests and get results
        test_results = run_tests(".")

        if test_results:
            test_results["warnings"] = findings
            test_results["warnings_count"] = len(findings) if findings is not None else None
            test_results["syntax"] = syntax
            test_results["syntax_output"] = warnings
            test_results["assertions_density"] = assertions_density
            test_results["assertions_mccabe_ratio"] = mccabe
            print(f"Percentage of passed tests: {test_results['test_pass_rate']}")
            print(f"Total time: {test_results['execution_time_sec']} seconds")
            if test_results is not None and test_results['line_coverage_percent'] is not None:
                print(f"Coverage: {test_results['line_coverage_percent']}%")
            else:
                print("Coverage information not available.")
            return test_results
        else:
            print("Failed to execute Go tests.")
            return {
                "syntax": syntax,
                "syntax_output": warnings,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "timeout": False,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "internal_error_occurred": True,
                "runtime_errors_count": None,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
            }

    finally:
        # Restore the original working directory
        os.chdir(original_cwd)
        # Clean up the temporary directory
        shutil.rmtree(temp_dir)
        print(f"Temporary directory {temp_dir} has been removed.")


def x_analyze_go_tests__mutmut_21(go_file_path, test_file_path):
    # Create a temporary directory within "/data/generated/golang/"
    base_temp_dir = "./data/generated/docs_golang/"
    os.makedirs(base_temp_dir, exist_ok=True)
    temp_dir = tempfile.mkdtemp(dir=base_temp_dir)

    # Save the current working directory
    original_cwd = os.getcwd()

    try:
        # Copy the Go file and the specified test file to the temporary directory
        new_go_file_path = shutil.copy(go_file_path, temp_dir)
        new_test_file_path = shutil.copy(test_file_path, temp_dir)

        # Change to temporary directory
        os.chdir(temp_dir)

        # Check syntax
        syntax_build = check_syntax(os.path.basename(None))
        syntax_vet, warnings = check_for_warnings(temp_dir)
        findings = run_golangci(os.path.basename(test_file_path))
        syntax = determine_syntax_result(syntax_build, syntax_vet)
        assertions_density = assertions_density_go(os.path.basename(test_file_path))
        mccabe = assertions_mccabe_ratio_go(os.path.basename(new_go_file_path), os.path.basename(new_test_file_path))

        if syntax != CompileStatus.OK:
            print("Syntax check failed.")
            return {
                "syntax": syntax,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "syntax_output": warnings,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "timeout": False,
                "internal_error_occurred": False,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
                "runtime_errors_count": None
            }

        # Run tests and get results
        test_results = run_tests(".")

        if test_results:
            test_results["warnings"] = findings
            test_results["warnings_count"] = len(findings) if findings is not None else None
            test_results["syntax"] = syntax
            test_results["syntax_output"] = warnings
            test_results["assertions_density"] = assertions_density
            test_results["assertions_mccabe_ratio"] = mccabe
            print(f"Percentage of passed tests: {test_results['test_pass_rate']}")
            print(f"Total time: {test_results['execution_time_sec']} seconds")
            if test_results is not None and test_results['line_coverage_percent'] is not None:
                print(f"Coverage: {test_results['line_coverage_percent']}%")
            else:
                print("Coverage information not available.")
            return test_results
        else:
            print("Failed to execute Go tests.")
            return {
                "syntax": syntax,
                "syntax_output": warnings,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "timeout": False,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "internal_error_occurred": True,
                "runtime_errors_count": None,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
            }

    finally:
        # Restore the original working directory
        os.chdir(original_cwd)
        # Clean up the temporary directory
        shutil.rmtree(temp_dir)
        print(f"Temporary directory {temp_dir} has been removed.")


def x_analyze_go_tests__mutmut_22(go_file_path, test_file_path):
    # Create a temporary directory within "/data/generated/golang/"
    base_temp_dir = "./data/generated/docs_golang/"
    os.makedirs(base_temp_dir, exist_ok=True)
    temp_dir = tempfile.mkdtemp(dir=base_temp_dir)

    # Save the current working directory
    original_cwd = os.getcwd()

    try:
        # Copy the Go file and the specified test file to the temporary directory
        new_go_file_path = shutil.copy(go_file_path, temp_dir)
        new_test_file_path = shutil.copy(test_file_path, temp_dir)

        # Change to temporary directory
        os.chdir(temp_dir)

        # Check syntax
        syntax_build = None
        syntax_vet, warnings = check_for_warnings(temp_dir)
        findings = run_golangci(os.path.basename(test_file_path))
        syntax = determine_syntax_result(syntax_build, syntax_vet)
        assertions_density = assertions_density_go(os.path.basename(test_file_path))
        mccabe = assertions_mccabe_ratio_go(os.path.basename(new_go_file_path), os.path.basename(new_test_file_path))

        if syntax != CompileStatus.OK:
            print("Syntax check failed.")
            return {
                "syntax": syntax,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "syntax_output": warnings,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "timeout": False,
                "internal_error_occurred": False,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
                "runtime_errors_count": None
            }

        # Run tests and get results
        test_results = run_tests(".")

        if test_results:
            test_results["warnings"] = findings
            test_results["warnings_count"] = len(findings) if findings is not None else None
            test_results["syntax"] = syntax
            test_results["syntax_output"] = warnings
            test_results["assertions_density"] = assertions_density
            test_results["assertions_mccabe_ratio"] = mccabe
            print(f"Percentage of passed tests: {test_results['test_pass_rate']}")
            print(f"Total time: {test_results['execution_time_sec']} seconds")
            if test_results is not None and test_results['line_coverage_percent'] is not None:
                print(f"Coverage: {test_results['line_coverage_percent']}%")
            else:
                print("Coverage information not available.")
            return test_results
        else:
            print("Failed to execute Go tests.")
            return {
                "syntax": syntax,
                "syntax_output": warnings,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "timeout": False,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "internal_error_occurred": True,
                "runtime_errors_count": None,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
            }

    finally:
        # Restore the original working directory
        os.chdir(original_cwd)
        # Clean up the temporary directory
        shutil.rmtree(temp_dir)
        print(f"Temporary directory {temp_dir} has been removed.")


def x_analyze_go_tests__mutmut_23(go_file_path, test_file_path):
    # Create a temporary directory within "/data/generated/golang/"
    base_temp_dir = "./data/generated/docs_golang/"
    os.makedirs(base_temp_dir, exist_ok=True)
    temp_dir = tempfile.mkdtemp(dir=base_temp_dir)

    # Save the current working directory
    original_cwd = os.getcwd()

    try:
        # Copy the Go file and the specified test file to the temporary directory
        new_go_file_path = shutil.copy(go_file_path, temp_dir)
        new_test_file_path = shutil.copy(test_file_path, temp_dir)

        # Change to temporary directory
        os.chdir(temp_dir)

        # Check syntax
        syntax_build = check_syntax(os.path.basename(test_file_path))
        syntax_vet, warnings = check_for_warnings(None)
        findings = run_golangci(os.path.basename(test_file_path))
        syntax = determine_syntax_result(syntax_build, syntax_vet)
        assertions_density = assertions_density_go(os.path.basename(test_file_path))
        mccabe = assertions_mccabe_ratio_go(os.path.basename(new_go_file_path), os.path.basename(new_test_file_path))

        if syntax != CompileStatus.OK:
            print("Syntax check failed.")
            return {
                "syntax": syntax,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "syntax_output": warnings,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "timeout": False,
                "internal_error_occurred": False,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
                "runtime_errors_count": None
            }

        # Run tests and get results
        test_results = run_tests(".")

        if test_results:
            test_results["warnings"] = findings
            test_results["warnings_count"] = len(findings) if findings is not None else None
            test_results["syntax"] = syntax
            test_results["syntax_output"] = warnings
            test_results["assertions_density"] = assertions_density
            test_results["assertions_mccabe_ratio"] = mccabe
            print(f"Percentage of passed tests: {test_results['test_pass_rate']}")
            print(f"Total time: {test_results['execution_time_sec']} seconds")
            if test_results is not None and test_results['line_coverage_percent'] is not None:
                print(f"Coverage: {test_results['line_coverage_percent']}%")
            else:
                print("Coverage information not available.")
            return test_results
        else:
            print("Failed to execute Go tests.")
            return {
                "syntax": syntax,
                "syntax_output": warnings,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "timeout": False,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "internal_error_occurred": True,
                "runtime_errors_count": None,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
            }

    finally:
        # Restore the original working directory
        os.chdir(original_cwd)
        # Clean up the temporary directory
        shutil.rmtree(temp_dir)
        print(f"Temporary directory {temp_dir} has been removed.")


def x_analyze_go_tests__mutmut_24(go_file_path, test_file_path):
    # Create a temporary directory within "/data/generated/golang/"
    base_temp_dir = "./data/generated/docs_golang/"
    os.makedirs(base_temp_dir, exist_ok=True)
    temp_dir = tempfile.mkdtemp(dir=base_temp_dir)

    # Save the current working directory
    original_cwd = os.getcwd()

    try:
        # Copy the Go file and the specified test file to the temporary directory
        new_go_file_path = shutil.copy(go_file_path, temp_dir)
        new_test_file_path = shutil.copy(test_file_path, temp_dir)

        # Change to temporary directory
        os.chdir(temp_dir)

        # Check syntax
        syntax_build = check_syntax(os.path.basename(test_file_path))
        syntax_vet, warnings = None
        findings = run_golangci(os.path.basename(test_file_path))
        syntax = determine_syntax_result(syntax_build, syntax_vet)
        assertions_density = assertions_density_go(os.path.basename(test_file_path))
        mccabe = assertions_mccabe_ratio_go(os.path.basename(new_go_file_path), os.path.basename(new_test_file_path))

        if syntax != CompileStatus.OK:
            print("Syntax check failed.")
            return {
                "syntax": syntax,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "syntax_output": warnings,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "timeout": False,
                "internal_error_occurred": False,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
                "runtime_errors_count": None
            }

        # Run tests and get results
        test_results = run_tests(".")

        if test_results:
            test_results["warnings"] = findings
            test_results["warnings_count"] = len(findings) if findings is not None else None
            test_results["syntax"] = syntax
            test_results["syntax_output"] = warnings
            test_results["assertions_density"] = assertions_density
            test_results["assertions_mccabe_ratio"] = mccabe
            print(f"Percentage of passed tests: {test_results['test_pass_rate']}")
            print(f"Total time: {test_results['execution_time_sec']} seconds")
            if test_results is not None and test_results['line_coverage_percent'] is not None:
                print(f"Coverage: {test_results['line_coverage_percent']}%")
            else:
                print("Coverage information not available.")
            return test_results
        else:
            print("Failed to execute Go tests.")
            return {
                "syntax": syntax,
                "syntax_output": warnings,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "timeout": False,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "internal_error_occurred": True,
                "runtime_errors_count": None,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
            }

    finally:
        # Restore the original working directory
        os.chdir(original_cwd)
        # Clean up the temporary directory
        shutil.rmtree(temp_dir)
        print(f"Temporary directory {temp_dir} has been removed.")


def x_analyze_go_tests__mutmut_25(go_file_path, test_file_path):
    # Create a temporary directory within "/data/generated/golang/"
    base_temp_dir = "./data/generated/docs_golang/"
    os.makedirs(base_temp_dir, exist_ok=True)
    temp_dir = tempfile.mkdtemp(dir=base_temp_dir)

    # Save the current working directory
    original_cwd = os.getcwd()

    try:
        # Copy the Go file and the specified test file to the temporary directory
        new_go_file_path = shutil.copy(go_file_path, temp_dir)
        new_test_file_path = shutil.copy(test_file_path, temp_dir)

        # Change to temporary directory
        os.chdir(temp_dir)

        # Check syntax
        syntax_build = check_syntax(os.path.basename(test_file_path))
        syntax_vet, warnings = check_for_warnings(temp_dir)
        findings = run_golangci(os.path.basename(None))
        syntax = determine_syntax_result(syntax_build, syntax_vet)
        assertions_density = assertions_density_go(os.path.basename(test_file_path))
        mccabe = assertions_mccabe_ratio_go(os.path.basename(new_go_file_path), os.path.basename(new_test_file_path))

        if syntax != CompileStatus.OK:
            print("Syntax check failed.")
            return {
                "syntax": syntax,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "syntax_output": warnings,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "timeout": False,
                "internal_error_occurred": False,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
                "runtime_errors_count": None
            }

        # Run tests and get results
        test_results = run_tests(".")

        if test_results:
            test_results["warnings"] = findings
            test_results["warnings_count"] = len(findings) if findings is not None else None
            test_results["syntax"] = syntax
            test_results["syntax_output"] = warnings
            test_results["assertions_density"] = assertions_density
            test_results["assertions_mccabe_ratio"] = mccabe
            print(f"Percentage of passed tests: {test_results['test_pass_rate']}")
            print(f"Total time: {test_results['execution_time_sec']} seconds")
            if test_results is not None and test_results['line_coverage_percent'] is not None:
                print(f"Coverage: {test_results['line_coverage_percent']}%")
            else:
                print("Coverage information not available.")
            return test_results
        else:
            print("Failed to execute Go tests.")
            return {
                "syntax": syntax,
                "syntax_output": warnings,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "timeout": False,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "internal_error_occurred": True,
                "runtime_errors_count": None,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
            }

    finally:
        # Restore the original working directory
        os.chdir(original_cwd)
        # Clean up the temporary directory
        shutil.rmtree(temp_dir)
        print(f"Temporary directory {temp_dir} has been removed.")


def x_analyze_go_tests__mutmut_26(go_file_path, test_file_path):
    # Create a temporary directory within "/data/generated/golang/"
    base_temp_dir = "./data/generated/docs_golang/"
    os.makedirs(base_temp_dir, exist_ok=True)
    temp_dir = tempfile.mkdtemp(dir=base_temp_dir)

    # Save the current working directory
    original_cwd = os.getcwd()

    try:
        # Copy the Go file and the specified test file to the temporary directory
        new_go_file_path = shutil.copy(go_file_path, temp_dir)
        new_test_file_path = shutil.copy(test_file_path, temp_dir)

        # Change to temporary directory
        os.chdir(temp_dir)

        # Check syntax
        syntax_build = check_syntax(os.path.basename(test_file_path))
        syntax_vet, warnings = check_for_warnings(temp_dir)
        findings = None
        syntax = determine_syntax_result(syntax_build, syntax_vet)
        assertions_density = assertions_density_go(os.path.basename(test_file_path))
        mccabe = assertions_mccabe_ratio_go(os.path.basename(new_go_file_path), os.path.basename(new_test_file_path))

        if syntax != CompileStatus.OK:
            print("Syntax check failed.")
            return {
                "syntax": syntax,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "syntax_output": warnings,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "timeout": False,
                "internal_error_occurred": False,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
                "runtime_errors_count": None
            }

        # Run tests and get results
        test_results = run_tests(".")

        if test_results:
            test_results["warnings"] = findings
            test_results["warnings_count"] = len(findings) if findings is not None else None
            test_results["syntax"] = syntax
            test_results["syntax_output"] = warnings
            test_results["assertions_density"] = assertions_density
            test_results["assertions_mccabe_ratio"] = mccabe
            print(f"Percentage of passed tests: {test_results['test_pass_rate']}")
            print(f"Total time: {test_results['execution_time_sec']} seconds")
            if test_results is not None and test_results['line_coverage_percent'] is not None:
                print(f"Coverage: {test_results['line_coverage_percent']}%")
            else:
                print("Coverage information not available.")
            return test_results
        else:
            print("Failed to execute Go tests.")
            return {
                "syntax": syntax,
                "syntax_output": warnings,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "timeout": False,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "internal_error_occurred": True,
                "runtime_errors_count": None,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
            }

    finally:
        # Restore the original working directory
        os.chdir(original_cwd)
        # Clean up the temporary directory
        shutil.rmtree(temp_dir)
        print(f"Temporary directory {temp_dir} has been removed.")


def x_analyze_go_tests__mutmut_27(go_file_path, test_file_path):
    # Create a temporary directory within "/data/generated/golang/"
    base_temp_dir = "./data/generated/docs_golang/"
    os.makedirs(base_temp_dir, exist_ok=True)
    temp_dir = tempfile.mkdtemp(dir=base_temp_dir)

    # Save the current working directory
    original_cwd = os.getcwd()

    try:
        # Copy the Go file and the specified test file to the temporary directory
        new_go_file_path = shutil.copy(go_file_path, temp_dir)
        new_test_file_path = shutil.copy(test_file_path, temp_dir)

        # Change to temporary directory
        os.chdir(temp_dir)

        # Check syntax
        syntax_build = check_syntax(os.path.basename(test_file_path))
        syntax_vet, warnings = check_for_warnings(temp_dir)
        findings = run_golangci(os.path.basename(test_file_path))
        syntax = determine_syntax_result(None, syntax_vet)
        assertions_density = assertions_density_go(os.path.basename(test_file_path))
        mccabe = assertions_mccabe_ratio_go(os.path.basename(new_go_file_path), os.path.basename(new_test_file_path))

        if syntax != CompileStatus.OK:
            print("Syntax check failed.")
            return {
                "syntax": syntax,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "syntax_output": warnings,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "timeout": False,
                "internal_error_occurred": False,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
                "runtime_errors_count": None
            }

        # Run tests and get results
        test_results = run_tests(".")

        if test_results:
            test_results["warnings"] = findings
            test_results["warnings_count"] = len(findings) if findings is not None else None
            test_results["syntax"] = syntax
            test_results["syntax_output"] = warnings
            test_results["assertions_density"] = assertions_density
            test_results["assertions_mccabe_ratio"] = mccabe
            print(f"Percentage of passed tests: {test_results['test_pass_rate']}")
            print(f"Total time: {test_results['execution_time_sec']} seconds")
            if test_results is not None and test_results['line_coverage_percent'] is not None:
                print(f"Coverage: {test_results['line_coverage_percent']}%")
            else:
                print("Coverage information not available.")
            return test_results
        else:
            print("Failed to execute Go tests.")
            return {
                "syntax": syntax,
                "syntax_output": warnings,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "timeout": False,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "internal_error_occurred": True,
                "runtime_errors_count": None,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
            }

    finally:
        # Restore the original working directory
        os.chdir(original_cwd)
        # Clean up the temporary directory
        shutil.rmtree(temp_dir)
        print(f"Temporary directory {temp_dir} has been removed.")


def x_analyze_go_tests__mutmut_28(go_file_path, test_file_path):
    # Create a temporary directory within "/data/generated/golang/"
    base_temp_dir = "./data/generated/docs_golang/"
    os.makedirs(base_temp_dir, exist_ok=True)
    temp_dir = tempfile.mkdtemp(dir=base_temp_dir)

    # Save the current working directory
    original_cwd = os.getcwd()

    try:
        # Copy the Go file and the specified test file to the temporary directory
        new_go_file_path = shutil.copy(go_file_path, temp_dir)
        new_test_file_path = shutil.copy(test_file_path, temp_dir)

        # Change to temporary directory
        os.chdir(temp_dir)

        # Check syntax
        syntax_build = check_syntax(os.path.basename(test_file_path))
        syntax_vet, warnings = check_for_warnings(temp_dir)
        findings = run_golangci(os.path.basename(test_file_path))
        syntax = determine_syntax_result(syntax_build, None)
        assertions_density = assertions_density_go(os.path.basename(test_file_path))
        mccabe = assertions_mccabe_ratio_go(os.path.basename(new_go_file_path), os.path.basename(new_test_file_path))

        if syntax != CompileStatus.OK:
            print("Syntax check failed.")
            return {
                "syntax": syntax,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "syntax_output": warnings,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "timeout": False,
                "internal_error_occurred": False,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
                "runtime_errors_count": None
            }

        # Run tests and get results
        test_results = run_tests(".")

        if test_results:
            test_results["warnings"] = findings
            test_results["warnings_count"] = len(findings) if findings is not None else None
            test_results["syntax"] = syntax
            test_results["syntax_output"] = warnings
            test_results["assertions_density"] = assertions_density
            test_results["assertions_mccabe_ratio"] = mccabe
            print(f"Percentage of passed tests: {test_results['test_pass_rate']}")
            print(f"Total time: {test_results['execution_time_sec']} seconds")
            if test_results is not None and test_results['line_coverage_percent'] is not None:
                print(f"Coverage: {test_results['line_coverage_percent']}%")
            else:
                print("Coverage information not available.")
            return test_results
        else:
            print("Failed to execute Go tests.")
            return {
                "syntax": syntax,
                "syntax_output": warnings,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "timeout": False,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "internal_error_occurred": True,
                "runtime_errors_count": None,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
            }

    finally:
        # Restore the original working directory
        os.chdir(original_cwd)
        # Clean up the temporary directory
        shutil.rmtree(temp_dir)
        print(f"Temporary directory {temp_dir} has been removed.")


def x_analyze_go_tests__mutmut_29(go_file_path, test_file_path):
    # Create a temporary directory within "/data/generated/golang/"
    base_temp_dir = "./data/generated/docs_golang/"
    os.makedirs(base_temp_dir, exist_ok=True)
    temp_dir = tempfile.mkdtemp(dir=base_temp_dir)

    # Save the current working directory
    original_cwd = os.getcwd()

    try:
        # Copy the Go file and the specified test file to the temporary directory
        new_go_file_path = shutil.copy(go_file_path, temp_dir)
        new_test_file_path = shutil.copy(test_file_path, temp_dir)

        # Change to temporary directory
        os.chdir(temp_dir)

        # Check syntax
        syntax_build = check_syntax(os.path.basename(test_file_path))
        syntax_vet, warnings = check_for_warnings(temp_dir)
        findings = run_golangci(os.path.basename(test_file_path))
        syntax = determine_syntax_result( syntax_vet)
        assertions_density = assertions_density_go(os.path.basename(test_file_path))
        mccabe = assertions_mccabe_ratio_go(os.path.basename(new_go_file_path), os.path.basename(new_test_file_path))

        if syntax != CompileStatus.OK:
            print("Syntax check failed.")
            return {
                "syntax": syntax,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "syntax_output": warnings,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "timeout": False,
                "internal_error_occurred": False,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
                "runtime_errors_count": None
            }

        # Run tests and get results
        test_results = run_tests(".")

        if test_results:
            test_results["warnings"] = findings
            test_results["warnings_count"] = len(findings) if findings is not None else None
            test_results["syntax"] = syntax
            test_results["syntax_output"] = warnings
            test_results["assertions_density"] = assertions_density
            test_results["assertions_mccabe_ratio"] = mccabe
            print(f"Percentage of passed tests: {test_results['test_pass_rate']}")
            print(f"Total time: {test_results['execution_time_sec']} seconds")
            if test_results is not None and test_results['line_coverage_percent'] is not None:
                print(f"Coverage: {test_results['line_coverage_percent']}%")
            else:
                print("Coverage information not available.")
            return test_results
        else:
            print("Failed to execute Go tests.")
            return {
                "syntax": syntax,
                "syntax_output": warnings,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "timeout": False,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "internal_error_occurred": True,
                "runtime_errors_count": None,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
            }

    finally:
        # Restore the original working directory
        os.chdir(original_cwd)
        # Clean up the temporary directory
        shutil.rmtree(temp_dir)
        print(f"Temporary directory {temp_dir} has been removed.")


def x_analyze_go_tests__mutmut_30(go_file_path, test_file_path):
    # Create a temporary directory within "/data/generated/golang/"
    base_temp_dir = "./data/generated/docs_golang/"
    os.makedirs(base_temp_dir, exist_ok=True)
    temp_dir = tempfile.mkdtemp(dir=base_temp_dir)

    # Save the current working directory
    original_cwd = os.getcwd()

    try:
        # Copy the Go file and the specified test file to the temporary directory
        new_go_file_path = shutil.copy(go_file_path, temp_dir)
        new_test_file_path = shutil.copy(test_file_path, temp_dir)

        # Change to temporary directory
        os.chdir(temp_dir)

        # Check syntax
        syntax_build = check_syntax(os.path.basename(test_file_path))
        syntax_vet, warnings = check_for_warnings(temp_dir)
        findings = run_golangci(os.path.basename(test_file_path))
        syntax = determine_syntax_result(syntax_build,)
        assertions_density = assertions_density_go(os.path.basename(test_file_path))
        mccabe = assertions_mccabe_ratio_go(os.path.basename(new_go_file_path), os.path.basename(new_test_file_path))

        if syntax != CompileStatus.OK:
            print("Syntax check failed.")
            return {
                "syntax": syntax,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "syntax_output": warnings,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "timeout": False,
                "internal_error_occurred": False,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
                "runtime_errors_count": None
            }

        # Run tests and get results
        test_results = run_tests(".")

        if test_results:
            test_results["warnings"] = findings
            test_results["warnings_count"] = len(findings) if findings is not None else None
            test_results["syntax"] = syntax
            test_results["syntax_output"] = warnings
            test_results["assertions_density"] = assertions_density
            test_results["assertions_mccabe_ratio"] = mccabe
            print(f"Percentage of passed tests: {test_results['test_pass_rate']}")
            print(f"Total time: {test_results['execution_time_sec']} seconds")
            if test_results is not None and test_results['line_coverage_percent'] is not None:
                print(f"Coverage: {test_results['line_coverage_percent']}%")
            else:
                print("Coverage information not available.")
            return test_results
        else:
            print("Failed to execute Go tests.")
            return {
                "syntax": syntax,
                "syntax_output": warnings,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "timeout": False,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "internal_error_occurred": True,
                "runtime_errors_count": None,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
            }

    finally:
        # Restore the original working directory
        os.chdir(original_cwd)
        # Clean up the temporary directory
        shutil.rmtree(temp_dir)
        print(f"Temporary directory {temp_dir} has been removed.")


def x_analyze_go_tests__mutmut_31(go_file_path, test_file_path):
    # Create a temporary directory within "/data/generated/golang/"
    base_temp_dir = "./data/generated/docs_golang/"
    os.makedirs(base_temp_dir, exist_ok=True)
    temp_dir = tempfile.mkdtemp(dir=base_temp_dir)

    # Save the current working directory
    original_cwd = os.getcwd()

    try:
        # Copy the Go file and the specified test file to the temporary directory
        new_go_file_path = shutil.copy(go_file_path, temp_dir)
        new_test_file_path = shutil.copy(test_file_path, temp_dir)

        # Change to temporary directory
        os.chdir(temp_dir)

        # Check syntax
        syntax_build = check_syntax(os.path.basename(test_file_path))
        syntax_vet, warnings = check_for_warnings(temp_dir)
        findings = run_golangci(os.path.basename(test_file_path))
        syntax = None
        assertions_density = assertions_density_go(os.path.basename(test_file_path))
        mccabe = assertions_mccabe_ratio_go(os.path.basename(new_go_file_path), os.path.basename(new_test_file_path))

        if syntax != CompileStatus.OK:
            print("Syntax check failed.")
            return {
                "syntax": syntax,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "syntax_output": warnings,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "timeout": False,
                "internal_error_occurred": False,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
                "runtime_errors_count": None
            }

        # Run tests and get results
        test_results = run_tests(".")

        if test_results:
            test_results["warnings"] = findings
            test_results["warnings_count"] = len(findings) if findings is not None else None
            test_results["syntax"] = syntax
            test_results["syntax_output"] = warnings
            test_results["assertions_density"] = assertions_density
            test_results["assertions_mccabe_ratio"] = mccabe
            print(f"Percentage of passed tests: {test_results['test_pass_rate']}")
            print(f"Total time: {test_results['execution_time_sec']} seconds")
            if test_results is not None and test_results['line_coverage_percent'] is not None:
                print(f"Coverage: {test_results['line_coverage_percent']}%")
            else:
                print("Coverage information not available.")
            return test_results
        else:
            print("Failed to execute Go tests.")
            return {
                "syntax": syntax,
                "syntax_output": warnings,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "timeout": False,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "internal_error_occurred": True,
                "runtime_errors_count": None,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
            }

    finally:
        # Restore the original working directory
        os.chdir(original_cwd)
        # Clean up the temporary directory
        shutil.rmtree(temp_dir)
        print(f"Temporary directory {temp_dir} has been removed.")


def x_analyze_go_tests__mutmut_32(go_file_path, test_file_path):
    # Create a temporary directory within "/data/generated/golang/"
    base_temp_dir = "./data/generated/docs_golang/"
    os.makedirs(base_temp_dir, exist_ok=True)
    temp_dir = tempfile.mkdtemp(dir=base_temp_dir)

    # Save the current working directory
    original_cwd = os.getcwd()

    try:
        # Copy the Go file and the specified test file to the temporary directory
        new_go_file_path = shutil.copy(go_file_path, temp_dir)
        new_test_file_path = shutil.copy(test_file_path, temp_dir)

        # Change to temporary directory
        os.chdir(temp_dir)

        # Check syntax
        syntax_build = check_syntax(os.path.basename(test_file_path))
        syntax_vet, warnings = check_for_warnings(temp_dir)
        findings = run_golangci(os.path.basename(test_file_path))
        syntax = determine_syntax_result(syntax_build, syntax_vet)
        assertions_density = assertions_density_go(os.path.basename(None))
        mccabe = assertions_mccabe_ratio_go(os.path.basename(new_go_file_path), os.path.basename(new_test_file_path))

        if syntax != CompileStatus.OK:
            print("Syntax check failed.")
            return {
                "syntax": syntax,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "syntax_output": warnings,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "timeout": False,
                "internal_error_occurred": False,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
                "runtime_errors_count": None
            }

        # Run tests and get results
        test_results = run_tests(".")

        if test_results:
            test_results["warnings"] = findings
            test_results["warnings_count"] = len(findings) if findings is not None else None
            test_results["syntax"] = syntax
            test_results["syntax_output"] = warnings
            test_results["assertions_density"] = assertions_density
            test_results["assertions_mccabe_ratio"] = mccabe
            print(f"Percentage of passed tests: {test_results['test_pass_rate']}")
            print(f"Total time: {test_results['execution_time_sec']} seconds")
            if test_results is not None and test_results['line_coverage_percent'] is not None:
                print(f"Coverage: {test_results['line_coverage_percent']}%")
            else:
                print("Coverage information not available.")
            return test_results
        else:
            print("Failed to execute Go tests.")
            return {
                "syntax": syntax,
                "syntax_output": warnings,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "timeout": False,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "internal_error_occurred": True,
                "runtime_errors_count": None,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
            }

    finally:
        # Restore the original working directory
        os.chdir(original_cwd)
        # Clean up the temporary directory
        shutil.rmtree(temp_dir)
        print(f"Temporary directory {temp_dir} has been removed.")


def x_analyze_go_tests__mutmut_33(go_file_path, test_file_path):
    # Create a temporary directory within "/data/generated/golang/"
    base_temp_dir = "./data/generated/docs_golang/"
    os.makedirs(base_temp_dir, exist_ok=True)
    temp_dir = tempfile.mkdtemp(dir=base_temp_dir)

    # Save the current working directory
    original_cwd = os.getcwd()

    try:
        # Copy the Go file and the specified test file to the temporary directory
        new_go_file_path = shutil.copy(go_file_path, temp_dir)
        new_test_file_path = shutil.copy(test_file_path, temp_dir)

        # Change to temporary directory
        os.chdir(temp_dir)

        # Check syntax
        syntax_build = check_syntax(os.path.basename(test_file_path))
        syntax_vet, warnings = check_for_warnings(temp_dir)
        findings = run_golangci(os.path.basename(test_file_path))
        syntax = determine_syntax_result(syntax_build, syntax_vet)
        assertions_density = None
        mccabe = assertions_mccabe_ratio_go(os.path.basename(new_go_file_path), os.path.basename(new_test_file_path))

        if syntax != CompileStatus.OK:
            print("Syntax check failed.")
            return {
                "syntax": syntax,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "syntax_output": warnings,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "timeout": False,
                "internal_error_occurred": False,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
                "runtime_errors_count": None
            }

        # Run tests and get results
        test_results = run_tests(".")

        if test_results:
            test_results["warnings"] = findings
            test_results["warnings_count"] = len(findings) if findings is not None else None
            test_results["syntax"] = syntax
            test_results["syntax_output"] = warnings
            test_results["assertions_density"] = assertions_density
            test_results["assertions_mccabe_ratio"] = mccabe
            print(f"Percentage of passed tests: {test_results['test_pass_rate']}")
            print(f"Total time: {test_results['execution_time_sec']} seconds")
            if test_results is not None and test_results['line_coverage_percent'] is not None:
                print(f"Coverage: {test_results['line_coverage_percent']}%")
            else:
                print("Coverage information not available.")
            return test_results
        else:
            print("Failed to execute Go tests.")
            return {
                "syntax": syntax,
                "syntax_output": warnings,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "timeout": False,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "internal_error_occurred": True,
                "runtime_errors_count": None,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
            }

    finally:
        # Restore the original working directory
        os.chdir(original_cwd)
        # Clean up the temporary directory
        shutil.rmtree(temp_dir)
        print(f"Temporary directory {temp_dir} has been removed.")


def x_analyze_go_tests__mutmut_34(go_file_path, test_file_path):
    # Create a temporary directory within "/data/generated/golang/"
    base_temp_dir = "./data/generated/docs_golang/"
    os.makedirs(base_temp_dir, exist_ok=True)
    temp_dir = tempfile.mkdtemp(dir=base_temp_dir)

    # Save the current working directory
    original_cwd = os.getcwd()

    try:
        # Copy the Go file and the specified test file to the temporary directory
        new_go_file_path = shutil.copy(go_file_path, temp_dir)
        new_test_file_path = shutil.copy(test_file_path, temp_dir)

        # Change to temporary directory
        os.chdir(temp_dir)

        # Check syntax
        syntax_build = check_syntax(os.path.basename(test_file_path))
        syntax_vet, warnings = check_for_warnings(temp_dir)
        findings = run_golangci(os.path.basename(test_file_path))
        syntax = determine_syntax_result(syntax_build, syntax_vet)
        assertions_density = assertions_density_go(os.path.basename(test_file_path))
        mccabe = assertions_mccabe_ratio_go(os.path.basename(None), os.path.basename(new_test_file_path))

        if syntax != CompileStatus.OK:
            print("Syntax check failed.")
            return {
                "syntax": syntax,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "syntax_output": warnings,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "timeout": False,
                "internal_error_occurred": False,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
                "runtime_errors_count": None
            }

        # Run tests and get results
        test_results = run_tests(".")

        if test_results:
            test_results["warnings"] = findings
            test_results["warnings_count"] = len(findings) if findings is not None else None
            test_results["syntax"] = syntax
            test_results["syntax_output"] = warnings
            test_results["assertions_density"] = assertions_density
            test_results["assertions_mccabe_ratio"] = mccabe
            print(f"Percentage of passed tests: {test_results['test_pass_rate']}")
            print(f"Total time: {test_results['execution_time_sec']} seconds")
            if test_results is not None and test_results['line_coverage_percent'] is not None:
                print(f"Coverage: {test_results['line_coverage_percent']}%")
            else:
                print("Coverage information not available.")
            return test_results
        else:
            print("Failed to execute Go tests.")
            return {
                "syntax": syntax,
                "syntax_output": warnings,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "timeout": False,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "internal_error_occurred": True,
                "runtime_errors_count": None,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
            }

    finally:
        # Restore the original working directory
        os.chdir(original_cwd)
        # Clean up the temporary directory
        shutil.rmtree(temp_dir)
        print(f"Temporary directory {temp_dir} has been removed.")


def x_analyze_go_tests__mutmut_35(go_file_path, test_file_path):
    # Create a temporary directory within "/data/generated/golang/"
    base_temp_dir = "./data/generated/docs_golang/"
    os.makedirs(base_temp_dir, exist_ok=True)
    temp_dir = tempfile.mkdtemp(dir=base_temp_dir)

    # Save the current working directory
    original_cwd = os.getcwd()

    try:
        # Copy the Go file and the specified test file to the temporary directory
        new_go_file_path = shutil.copy(go_file_path, temp_dir)
        new_test_file_path = shutil.copy(test_file_path, temp_dir)

        # Change to temporary directory
        os.chdir(temp_dir)

        # Check syntax
        syntax_build = check_syntax(os.path.basename(test_file_path))
        syntax_vet, warnings = check_for_warnings(temp_dir)
        findings = run_golangci(os.path.basename(test_file_path))
        syntax = determine_syntax_result(syntax_build, syntax_vet)
        assertions_density = assertions_density_go(os.path.basename(test_file_path))
        mccabe = assertions_mccabe_ratio_go(os.path.basename(new_go_file_path), os.path.basename(None))

        if syntax != CompileStatus.OK:
            print("Syntax check failed.")
            return {
                "syntax": syntax,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "syntax_output": warnings,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "timeout": False,
                "internal_error_occurred": False,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
                "runtime_errors_count": None
            }

        # Run tests and get results
        test_results = run_tests(".")

        if test_results:
            test_results["warnings"] = findings
            test_results["warnings_count"] = len(findings) if findings is not None else None
            test_results["syntax"] = syntax
            test_results["syntax_output"] = warnings
            test_results["assertions_density"] = assertions_density
            test_results["assertions_mccabe_ratio"] = mccabe
            print(f"Percentage of passed tests: {test_results['test_pass_rate']}")
            print(f"Total time: {test_results['execution_time_sec']} seconds")
            if test_results is not None and test_results['line_coverage_percent'] is not None:
                print(f"Coverage: {test_results['line_coverage_percent']}%")
            else:
                print("Coverage information not available.")
            return test_results
        else:
            print("Failed to execute Go tests.")
            return {
                "syntax": syntax,
                "syntax_output": warnings,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "timeout": False,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "internal_error_occurred": True,
                "runtime_errors_count": None,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
            }

    finally:
        # Restore the original working directory
        os.chdir(original_cwd)
        # Clean up the temporary directory
        shutil.rmtree(temp_dir)
        print(f"Temporary directory {temp_dir} has been removed.")


def x_analyze_go_tests__mutmut_36(go_file_path, test_file_path):
    # Create a temporary directory within "/data/generated/golang/"
    base_temp_dir = "./data/generated/docs_golang/"
    os.makedirs(base_temp_dir, exist_ok=True)
    temp_dir = tempfile.mkdtemp(dir=base_temp_dir)

    # Save the current working directory
    original_cwd = os.getcwd()

    try:
        # Copy the Go file and the specified test file to the temporary directory
        new_go_file_path = shutil.copy(go_file_path, temp_dir)
        new_test_file_path = shutil.copy(test_file_path, temp_dir)

        # Change to temporary directory
        os.chdir(temp_dir)

        # Check syntax
        syntax_build = check_syntax(os.path.basename(test_file_path))
        syntax_vet, warnings = check_for_warnings(temp_dir)
        findings = run_golangci(os.path.basename(test_file_path))
        syntax = determine_syntax_result(syntax_build, syntax_vet)
        assertions_density = assertions_density_go(os.path.basename(test_file_path))
        mccabe = None

        if syntax != CompileStatus.OK:
            print("Syntax check failed.")
            return {
                "syntax": syntax,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "syntax_output": warnings,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "timeout": False,
                "internal_error_occurred": False,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
                "runtime_errors_count": None
            }

        # Run tests and get results
        test_results = run_tests(".")

        if test_results:
            test_results["warnings"] = findings
            test_results["warnings_count"] = len(findings) if findings is not None else None
            test_results["syntax"] = syntax
            test_results["syntax_output"] = warnings
            test_results["assertions_density"] = assertions_density
            test_results["assertions_mccabe_ratio"] = mccabe
            print(f"Percentage of passed tests: {test_results['test_pass_rate']}")
            print(f"Total time: {test_results['execution_time_sec']} seconds")
            if test_results is not None and test_results['line_coverage_percent'] is not None:
                print(f"Coverage: {test_results['line_coverage_percent']}%")
            else:
                print("Coverage information not available.")
            return test_results
        else:
            print("Failed to execute Go tests.")
            return {
                "syntax": syntax,
                "syntax_output": warnings,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "timeout": False,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "internal_error_occurred": True,
                "runtime_errors_count": None,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
            }

    finally:
        # Restore the original working directory
        os.chdir(original_cwd)
        # Clean up the temporary directory
        shutil.rmtree(temp_dir)
        print(f"Temporary directory {temp_dir} has been removed.")


def x_analyze_go_tests__mutmut_37(go_file_path, test_file_path):
    # Create a temporary directory within "/data/generated/golang/"
    base_temp_dir = "./data/generated/docs_golang/"
    os.makedirs(base_temp_dir, exist_ok=True)
    temp_dir = tempfile.mkdtemp(dir=base_temp_dir)

    # Save the current working directory
    original_cwd = os.getcwd()

    try:
        # Copy the Go file and the specified test file to the temporary directory
        new_go_file_path = shutil.copy(go_file_path, temp_dir)
        new_test_file_path = shutil.copy(test_file_path, temp_dir)

        # Change to temporary directory
        os.chdir(temp_dir)

        # Check syntax
        syntax_build = check_syntax(os.path.basename(test_file_path))
        syntax_vet, warnings = check_for_warnings(temp_dir)
        findings = run_golangci(os.path.basename(test_file_path))
        syntax = determine_syntax_result(syntax_build, syntax_vet)
        assertions_density = assertions_density_go(os.path.basename(test_file_path))
        mccabe = assertions_mccabe_ratio_go(os.path.basename(new_go_file_path), os.path.basename(new_test_file_path))

        if syntax == CompileStatus.OK:
            print("Syntax check failed.")
            return {
                "syntax": syntax,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "syntax_output": warnings,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "timeout": False,
                "internal_error_occurred": False,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
                "runtime_errors_count": None
            }

        # Run tests and get results
        test_results = run_tests(".")

        if test_results:
            test_results["warnings"] = findings
            test_results["warnings_count"] = len(findings) if findings is not None else None
            test_results["syntax"] = syntax
            test_results["syntax_output"] = warnings
            test_results["assertions_density"] = assertions_density
            test_results["assertions_mccabe_ratio"] = mccabe
            print(f"Percentage of passed tests: {test_results['test_pass_rate']}")
            print(f"Total time: {test_results['execution_time_sec']} seconds")
            if test_results is not None and test_results['line_coverage_percent'] is not None:
                print(f"Coverage: {test_results['line_coverage_percent']}%")
            else:
                print("Coverage information not available.")
            return test_results
        else:
            print("Failed to execute Go tests.")
            return {
                "syntax": syntax,
                "syntax_output": warnings,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "timeout": False,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "internal_error_occurred": True,
                "runtime_errors_count": None,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
            }

    finally:
        # Restore the original working directory
        os.chdir(original_cwd)
        # Clean up the temporary directory
        shutil.rmtree(temp_dir)
        print(f"Temporary directory {temp_dir} has been removed.")


def x_analyze_go_tests__mutmut_38(go_file_path, test_file_path):
    # Create a temporary directory within "/data/generated/golang/"
    base_temp_dir = "./data/generated/docs_golang/"
    os.makedirs(base_temp_dir, exist_ok=True)
    temp_dir = tempfile.mkdtemp(dir=base_temp_dir)

    # Save the current working directory
    original_cwd = os.getcwd()

    try:
        # Copy the Go file and the specified test file to the temporary directory
        new_go_file_path = shutil.copy(go_file_path, temp_dir)
        new_test_file_path = shutil.copy(test_file_path, temp_dir)

        # Change to temporary directory
        os.chdir(temp_dir)

        # Check syntax
        syntax_build = check_syntax(os.path.basename(test_file_path))
        syntax_vet, warnings = check_for_warnings(temp_dir)
        findings = run_golangci(os.path.basename(test_file_path))
        syntax = determine_syntax_result(syntax_build, syntax_vet)
        assertions_density = assertions_density_go(os.path.basename(test_file_path))
        mccabe = assertions_mccabe_ratio_go(os.path.basename(new_go_file_path), os.path.basename(new_test_file_path))

        if syntax != CompileStatus.OK:
            print("XXSyntax check failed.XX")
            return {
                "syntax": syntax,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "syntax_output": warnings,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "timeout": False,
                "internal_error_occurred": False,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
                "runtime_errors_count": None
            }

        # Run tests and get results
        test_results = run_tests(".")

        if test_results:
            test_results["warnings"] = findings
            test_results["warnings_count"] = len(findings) if findings is not None else None
            test_results["syntax"] = syntax
            test_results["syntax_output"] = warnings
            test_results["assertions_density"] = assertions_density
            test_results["assertions_mccabe_ratio"] = mccabe
            print(f"Percentage of passed tests: {test_results['test_pass_rate']}")
            print(f"Total time: {test_results['execution_time_sec']} seconds")
            if test_results is not None and test_results['line_coverage_percent'] is not None:
                print(f"Coverage: {test_results['line_coverage_percent']}%")
            else:
                print("Coverage information not available.")
            return test_results
        else:
            print("Failed to execute Go tests.")
            return {
                "syntax": syntax,
                "syntax_output": warnings,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "timeout": False,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "internal_error_occurred": True,
                "runtime_errors_count": None,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
            }

    finally:
        # Restore the original working directory
        os.chdir(original_cwd)
        # Clean up the temporary directory
        shutil.rmtree(temp_dir)
        print(f"Temporary directory {temp_dir} has been removed.")


def x_analyze_go_tests__mutmut_39(go_file_path, test_file_path):
    # Create a temporary directory within "/data/generated/golang/"
    base_temp_dir = "./data/generated/docs_golang/"
    os.makedirs(base_temp_dir, exist_ok=True)
    temp_dir = tempfile.mkdtemp(dir=base_temp_dir)

    # Save the current working directory
    original_cwd = os.getcwd()

    try:
        # Copy the Go file and the specified test file to the temporary directory
        new_go_file_path = shutil.copy(go_file_path, temp_dir)
        new_test_file_path = shutil.copy(test_file_path, temp_dir)

        # Change to temporary directory
        os.chdir(temp_dir)

        # Check syntax
        syntax_build = check_syntax(os.path.basename(test_file_path))
        syntax_vet, warnings = check_for_warnings(temp_dir)
        findings = run_golangci(os.path.basename(test_file_path))
        syntax = determine_syntax_result(syntax_build, syntax_vet)
        assertions_density = assertions_density_go(os.path.basename(test_file_path))
        mccabe = assertions_mccabe_ratio_go(os.path.basename(new_go_file_path), os.path.basename(new_test_file_path))

        if syntax != CompileStatus.OK:
            print("Syntax check failed.")
            return {
                "XXsyntaxXX": syntax,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "syntax_output": warnings,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "timeout": False,
                "internal_error_occurred": False,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
                "runtime_errors_count": None
            }

        # Run tests and get results
        test_results = run_tests(".")

        if test_results:
            test_results["warnings"] = findings
            test_results["warnings_count"] = len(findings) if findings is not None else None
            test_results["syntax"] = syntax
            test_results["syntax_output"] = warnings
            test_results["assertions_density"] = assertions_density
            test_results["assertions_mccabe_ratio"] = mccabe
            print(f"Percentage of passed tests: {test_results['test_pass_rate']}")
            print(f"Total time: {test_results['execution_time_sec']} seconds")
            if test_results is not None and test_results['line_coverage_percent'] is not None:
                print(f"Coverage: {test_results['line_coverage_percent']}%")
            else:
                print("Coverage information not available.")
            return test_results
        else:
            print("Failed to execute Go tests.")
            return {
                "syntax": syntax,
                "syntax_output": warnings,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "timeout": False,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "internal_error_occurred": True,
                "runtime_errors_count": None,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
            }

    finally:
        # Restore the original working directory
        os.chdir(original_cwd)
        # Clean up the temporary directory
        shutil.rmtree(temp_dir)
        print(f"Temporary directory {temp_dir} has been removed.")


def x_analyze_go_tests__mutmut_40(go_file_path, test_file_path):
    # Create a temporary directory within "/data/generated/golang/"
    base_temp_dir = "./data/generated/docs_golang/"
    os.makedirs(base_temp_dir, exist_ok=True)
    temp_dir = tempfile.mkdtemp(dir=base_temp_dir)

    # Save the current working directory
    original_cwd = os.getcwd()

    try:
        # Copy the Go file and the specified test file to the temporary directory
        new_go_file_path = shutil.copy(go_file_path, temp_dir)
        new_test_file_path = shutil.copy(test_file_path, temp_dir)

        # Change to temporary directory
        os.chdir(temp_dir)

        # Check syntax
        syntax_build = check_syntax(os.path.basename(test_file_path))
        syntax_vet, warnings = check_for_warnings(temp_dir)
        findings = run_golangci(os.path.basename(test_file_path))
        syntax = determine_syntax_result(syntax_build, syntax_vet)
        assertions_density = assertions_density_go(os.path.basename(test_file_path))
        mccabe = assertions_mccabe_ratio_go(os.path.basename(new_go_file_path), os.path.basename(new_test_file_path))

        if syntax != CompileStatus.OK:
            print("Syntax check failed.")
            return {
                "syntax": syntax,
                "XXtest_pass_rateXX": None,
                "execution_time_sec": None,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "syntax_output": warnings,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "timeout": False,
                "internal_error_occurred": False,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
                "runtime_errors_count": None
            }

        # Run tests and get results
        test_results = run_tests(".")

        if test_results:
            test_results["warnings"] = findings
            test_results["warnings_count"] = len(findings) if findings is not None else None
            test_results["syntax"] = syntax
            test_results["syntax_output"] = warnings
            test_results["assertions_density"] = assertions_density
            test_results["assertions_mccabe_ratio"] = mccabe
            print(f"Percentage of passed tests: {test_results['test_pass_rate']}")
            print(f"Total time: {test_results['execution_time_sec']} seconds")
            if test_results is not None and test_results['line_coverage_percent'] is not None:
                print(f"Coverage: {test_results['line_coverage_percent']}%")
            else:
                print("Coverage information not available.")
            return test_results
        else:
            print("Failed to execute Go tests.")
            return {
                "syntax": syntax,
                "syntax_output": warnings,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "timeout": False,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "internal_error_occurred": True,
                "runtime_errors_count": None,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
            }

    finally:
        # Restore the original working directory
        os.chdir(original_cwd)
        # Clean up the temporary directory
        shutil.rmtree(temp_dir)
        print(f"Temporary directory {temp_dir} has been removed.")


def x_analyze_go_tests__mutmut_41(go_file_path, test_file_path):
    # Create a temporary directory within "/data/generated/golang/"
    base_temp_dir = "./data/generated/docs_golang/"
    os.makedirs(base_temp_dir, exist_ok=True)
    temp_dir = tempfile.mkdtemp(dir=base_temp_dir)

    # Save the current working directory
    original_cwd = os.getcwd()

    try:
        # Copy the Go file and the specified test file to the temporary directory
        new_go_file_path = shutil.copy(go_file_path, temp_dir)
        new_test_file_path = shutil.copy(test_file_path, temp_dir)

        # Change to temporary directory
        os.chdir(temp_dir)

        # Check syntax
        syntax_build = check_syntax(os.path.basename(test_file_path))
        syntax_vet, warnings = check_for_warnings(temp_dir)
        findings = run_golangci(os.path.basename(test_file_path))
        syntax = determine_syntax_result(syntax_build, syntax_vet)
        assertions_density = assertions_density_go(os.path.basename(test_file_path))
        mccabe = assertions_mccabe_ratio_go(os.path.basename(new_go_file_path), os.path.basename(new_test_file_path))

        if syntax != CompileStatus.OK:
            print("Syntax check failed.")
            return {
                "syntax": syntax,
                "test_pass_rate": None,
                "XXexecution_time_secXX": None,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "syntax_output": warnings,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "timeout": False,
                "internal_error_occurred": False,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
                "runtime_errors_count": None
            }

        # Run tests and get results
        test_results = run_tests(".")

        if test_results:
            test_results["warnings"] = findings
            test_results["warnings_count"] = len(findings) if findings is not None else None
            test_results["syntax"] = syntax
            test_results["syntax_output"] = warnings
            test_results["assertions_density"] = assertions_density
            test_results["assertions_mccabe_ratio"] = mccabe
            print(f"Percentage of passed tests: {test_results['test_pass_rate']}")
            print(f"Total time: {test_results['execution_time_sec']} seconds")
            if test_results is not None and test_results['line_coverage_percent'] is not None:
                print(f"Coverage: {test_results['line_coverage_percent']}%")
            else:
                print("Coverage information not available.")
            return test_results
        else:
            print("Failed to execute Go tests.")
            return {
                "syntax": syntax,
                "syntax_output": warnings,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "timeout": False,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "internal_error_occurred": True,
                "runtime_errors_count": None,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
            }

    finally:
        # Restore the original working directory
        os.chdir(original_cwd)
        # Clean up the temporary directory
        shutil.rmtree(temp_dir)
        print(f"Temporary directory {temp_dir} has been removed.")


def x_analyze_go_tests__mutmut_42(go_file_path, test_file_path):
    # Create a temporary directory within "/data/generated/golang/"
    base_temp_dir = "./data/generated/docs_golang/"
    os.makedirs(base_temp_dir, exist_ok=True)
    temp_dir = tempfile.mkdtemp(dir=base_temp_dir)

    # Save the current working directory
    original_cwd = os.getcwd()

    try:
        # Copy the Go file and the specified test file to the temporary directory
        new_go_file_path = shutil.copy(go_file_path, temp_dir)
        new_test_file_path = shutil.copy(test_file_path, temp_dir)

        # Change to temporary directory
        os.chdir(temp_dir)

        # Check syntax
        syntax_build = check_syntax(os.path.basename(test_file_path))
        syntax_vet, warnings = check_for_warnings(temp_dir)
        findings = run_golangci(os.path.basename(test_file_path))
        syntax = determine_syntax_result(syntax_build, syntax_vet)
        assertions_density = assertions_density_go(os.path.basename(test_file_path))
        mccabe = assertions_mccabe_ratio_go(os.path.basename(new_go_file_path), os.path.basename(new_test_file_path))

        if syntax != CompileStatus.OK:
            print("Syntax check failed.")
            return {
                "syntax": syntax,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "XXline_coverage_percentXX": None,
                "branch_coverage_percent": None,
                "syntax_output": warnings,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "timeout": False,
                "internal_error_occurred": False,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
                "runtime_errors_count": None
            }

        # Run tests and get results
        test_results = run_tests(".")

        if test_results:
            test_results["warnings"] = findings
            test_results["warnings_count"] = len(findings) if findings is not None else None
            test_results["syntax"] = syntax
            test_results["syntax_output"] = warnings
            test_results["assertions_density"] = assertions_density
            test_results["assertions_mccabe_ratio"] = mccabe
            print(f"Percentage of passed tests: {test_results['test_pass_rate']}")
            print(f"Total time: {test_results['execution_time_sec']} seconds")
            if test_results is not None and test_results['line_coverage_percent'] is not None:
                print(f"Coverage: {test_results['line_coverage_percent']}%")
            else:
                print("Coverage information not available.")
            return test_results
        else:
            print("Failed to execute Go tests.")
            return {
                "syntax": syntax,
                "syntax_output": warnings,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "timeout": False,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "internal_error_occurred": True,
                "runtime_errors_count": None,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
            }

    finally:
        # Restore the original working directory
        os.chdir(original_cwd)
        # Clean up the temporary directory
        shutil.rmtree(temp_dir)
        print(f"Temporary directory {temp_dir} has been removed.")


def x_analyze_go_tests__mutmut_43(go_file_path, test_file_path):
    # Create a temporary directory within "/data/generated/golang/"
    base_temp_dir = "./data/generated/docs_golang/"
    os.makedirs(base_temp_dir, exist_ok=True)
    temp_dir = tempfile.mkdtemp(dir=base_temp_dir)

    # Save the current working directory
    original_cwd = os.getcwd()

    try:
        # Copy the Go file and the specified test file to the temporary directory
        new_go_file_path = shutil.copy(go_file_path, temp_dir)
        new_test_file_path = shutil.copy(test_file_path, temp_dir)

        # Change to temporary directory
        os.chdir(temp_dir)

        # Check syntax
        syntax_build = check_syntax(os.path.basename(test_file_path))
        syntax_vet, warnings = check_for_warnings(temp_dir)
        findings = run_golangci(os.path.basename(test_file_path))
        syntax = determine_syntax_result(syntax_build, syntax_vet)
        assertions_density = assertions_density_go(os.path.basename(test_file_path))
        mccabe = assertions_mccabe_ratio_go(os.path.basename(new_go_file_path), os.path.basename(new_test_file_path))

        if syntax != CompileStatus.OK:
            print("Syntax check failed.")
            return {
                "syntax": syntax,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "line_coverage_percent": None,
                "XXbranch_coverage_percentXX": None,
                "syntax_output": warnings,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "timeout": False,
                "internal_error_occurred": False,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
                "runtime_errors_count": None
            }

        # Run tests and get results
        test_results = run_tests(".")

        if test_results:
            test_results["warnings"] = findings
            test_results["warnings_count"] = len(findings) if findings is not None else None
            test_results["syntax"] = syntax
            test_results["syntax_output"] = warnings
            test_results["assertions_density"] = assertions_density
            test_results["assertions_mccabe_ratio"] = mccabe
            print(f"Percentage of passed tests: {test_results['test_pass_rate']}")
            print(f"Total time: {test_results['execution_time_sec']} seconds")
            if test_results is not None and test_results['line_coverage_percent'] is not None:
                print(f"Coverage: {test_results['line_coverage_percent']}%")
            else:
                print("Coverage information not available.")
            return test_results
        else:
            print("Failed to execute Go tests.")
            return {
                "syntax": syntax,
                "syntax_output": warnings,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "timeout": False,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "internal_error_occurred": True,
                "runtime_errors_count": None,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
            }

    finally:
        # Restore the original working directory
        os.chdir(original_cwd)
        # Clean up the temporary directory
        shutil.rmtree(temp_dir)
        print(f"Temporary directory {temp_dir} has been removed.")


def x_analyze_go_tests__mutmut_44(go_file_path, test_file_path):
    # Create a temporary directory within "/data/generated/golang/"
    base_temp_dir = "./data/generated/docs_golang/"
    os.makedirs(base_temp_dir, exist_ok=True)
    temp_dir = tempfile.mkdtemp(dir=base_temp_dir)

    # Save the current working directory
    original_cwd = os.getcwd()

    try:
        # Copy the Go file and the specified test file to the temporary directory
        new_go_file_path = shutil.copy(go_file_path, temp_dir)
        new_test_file_path = shutil.copy(test_file_path, temp_dir)

        # Change to temporary directory
        os.chdir(temp_dir)

        # Check syntax
        syntax_build = check_syntax(os.path.basename(test_file_path))
        syntax_vet, warnings = check_for_warnings(temp_dir)
        findings = run_golangci(os.path.basename(test_file_path))
        syntax = determine_syntax_result(syntax_build, syntax_vet)
        assertions_density = assertions_density_go(os.path.basename(test_file_path))
        mccabe = assertions_mccabe_ratio_go(os.path.basename(new_go_file_path), os.path.basename(new_test_file_path))

        if syntax != CompileStatus.OK:
            print("Syntax check failed.")
            return {
                "syntax": syntax,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "XXsyntax_outputXX": warnings,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "timeout": False,
                "internal_error_occurred": False,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
                "runtime_errors_count": None
            }

        # Run tests and get results
        test_results = run_tests(".")

        if test_results:
            test_results["warnings"] = findings
            test_results["warnings_count"] = len(findings) if findings is not None else None
            test_results["syntax"] = syntax
            test_results["syntax_output"] = warnings
            test_results["assertions_density"] = assertions_density
            test_results["assertions_mccabe_ratio"] = mccabe
            print(f"Percentage of passed tests: {test_results['test_pass_rate']}")
            print(f"Total time: {test_results['execution_time_sec']} seconds")
            if test_results is not None and test_results['line_coverage_percent'] is not None:
                print(f"Coverage: {test_results['line_coverage_percent']}%")
            else:
                print("Coverage information not available.")
            return test_results
        else:
            print("Failed to execute Go tests.")
            return {
                "syntax": syntax,
                "syntax_output": warnings,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "timeout": False,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "internal_error_occurred": True,
                "runtime_errors_count": None,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
            }

    finally:
        # Restore the original working directory
        os.chdir(original_cwd)
        # Clean up the temporary directory
        shutil.rmtree(temp_dir)
        print(f"Temporary directory {temp_dir} has been removed.")


def x_analyze_go_tests__mutmut_45(go_file_path, test_file_path):
    # Create a temporary directory within "/data/generated/golang/"
    base_temp_dir = "./data/generated/docs_golang/"
    os.makedirs(base_temp_dir, exist_ok=True)
    temp_dir = tempfile.mkdtemp(dir=base_temp_dir)

    # Save the current working directory
    original_cwd = os.getcwd()

    try:
        # Copy the Go file and the specified test file to the temporary directory
        new_go_file_path = shutil.copy(go_file_path, temp_dir)
        new_test_file_path = shutil.copy(test_file_path, temp_dir)

        # Change to temporary directory
        os.chdir(temp_dir)

        # Check syntax
        syntax_build = check_syntax(os.path.basename(test_file_path))
        syntax_vet, warnings = check_for_warnings(temp_dir)
        findings = run_golangci(os.path.basename(test_file_path))
        syntax = determine_syntax_result(syntax_build, syntax_vet)
        assertions_density = assertions_density_go(os.path.basename(test_file_path))
        mccabe = assertions_mccabe_ratio_go(os.path.basename(new_go_file_path), os.path.basename(new_test_file_path))

        if syntax != CompileStatus.OK:
            print("Syntax check failed.")
            return {
                "syntax": syntax,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "syntax_output": warnings,
                "XXwarningsXX": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "timeout": False,
                "internal_error_occurred": False,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
                "runtime_errors_count": None
            }

        # Run tests and get results
        test_results = run_tests(".")

        if test_results:
            test_results["warnings"] = findings
            test_results["warnings_count"] = len(findings) if findings is not None else None
            test_results["syntax"] = syntax
            test_results["syntax_output"] = warnings
            test_results["assertions_density"] = assertions_density
            test_results["assertions_mccabe_ratio"] = mccabe
            print(f"Percentage of passed tests: {test_results['test_pass_rate']}")
            print(f"Total time: {test_results['execution_time_sec']} seconds")
            if test_results is not None and test_results['line_coverage_percent'] is not None:
                print(f"Coverage: {test_results['line_coverage_percent']}%")
            else:
                print("Coverage information not available.")
            return test_results
        else:
            print("Failed to execute Go tests.")
            return {
                "syntax": syntax,
                "syntax_output": warnings,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "timeout": False,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "internal_error_occurred": True,
                "runtime_errors_count": None,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
            }

    finally:
        # Restore the original working directory
        os.chdir(original_cwd)
        # Clean up the temporary directory
        shutil.rmtree(temp_dir)
        print(f"Temporary directory {temp_dir} has been removed.")


def x_analyze_go_tests__mutmut_46(go_file_path, test_file_path):
    # Create a temporary directory within "/data/generated/golang/"
    base_temp_dir = "./data/generated/docs_golang/"
    os.makedirs(base_temp_dir, exist_ok=True)
    temp_dir = tempfile.mkdtemp(dir=base_temp_dir)

    # Save the current working directory
    original_cwd = os.getcwd()

    try:
        # Copy the Go file and the specified test file to the temporary directory
        new_go_file_path = shutil.copy(go_file_path, temp_dir)
        new_test_file_path = shutil.copy(test_file_path, temp_dir)

        # Change to temporary directory
        os.chdir(temp_dir)

        # Check syntax
        syntax_build = check_syntax(os.path.basename(test_file_path))
        syntax_vet, warnings = check_for_warnings(temp_dir)
        findings = run_golangci(os.path.basename(test_file_path))
        syntax = determine_syntax_result(syntax_build, syntax_vet)
        assertions_density = assertions_density_go(os.path.basename(test_file_path))
        mccabe = assertions_mccabe_ratio_go(os.path.basename(new_go_file_path), os.path.basename(new_test_file_path))

        if syntax != CompileStatus.OK:
            print("Syntax check failed.")
            return {
                "syntax": syntax,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "syntax_output": warnings,
                "warnings": findings,
                "XXwarnings_countXX": len(findings) if findings is not None else None,
                "timeout": False,
                "internal_error_occurred": False,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
                "runtime_errors_count": None
            }

        # Run tests and get results
        test_results = run_tests(".")

        if test_results:
            test_results["warnings"] = findings
            test_results["warnings_count"] = len(findings) if findings is not None else None
            test_results["syntax"] = syntax
            test_results["syntax_output"] = warnings
            test_results["assertions_density"] = assertions_density
            test_results["assertions_mccabe_ratio"] = mccabe
            print(f"Percentage of passed tests: {test_results['test_pass_rate']}")
            print(f"Total time: {test_results['execution_time_sec']} seconds")
            if test_results is not None and test_results['line_coverage_percent'] is not None:
                print(f"Coverage: {test_results['line_coverage_percent']}%")
            else:
                print("Coverage information not available.")
            return test_results
        else:
            print("Failed to execute Go tests.")
            return {
                "syntax": syntax,
                "syntax_output": warnings,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "timeout": False,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "internal_error_occurred": True,
                "runtime_errors_count": None,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
            }

    finally:
        # Restore the original working directory
        os.chdir(original_cwd)
        # Clean up the temporary directory
        shutil.rmtree(temp_dir)
        print(f"Temporary directory {temp_dir} has been removed.")


def x_analyze_go_tests__mutmut_47(go_file_path, test_file_path):
    # Create a temporary directory within "/data/generated/golang/"
    base_temp_dir = "./data/generated/docs_golang/"
    os.makedirs(base_temp_dir, exist_ok=True)
    temp_dir = tempfile.mkdtemp(dir=base_temp_dir)

    # Save the current working directory
    original_cwd = os.getcwd()

    try:
        # Copy the Go file and the specified test file to the temporary directory
        new_go_file_path = shutil.copy(go_file_path, temp_dir)
        new_test_file_path = shutil.copy(test_file_path, temp_dir)

        # Change to temporary directory
        os.chdir(temp_dir)

        # Check syntax
        syntax_build = check_syntax(os.path.basename(test_file_path))
        syntax_vet, warnings = check_for_warnings(temp_dir)
        findings = run_golangci(os.path.basename(test_file_path))
        syntax = determine_syntax_result(syntax_build, syntax_vet)
        assertions_density = assertions_density_go(os.path.basename(test_file_path))
        mccabe = assertions_mccabe_ratio_go(os.path.basename(new_go_file_path), os.path.basename(new_test_file_path))

        if syntax != CompileStatus.OK:
            print("Syntax check failed.")
            return {
                "syntax": syntax,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "syntax_output": warnings,
                "warnings": findings,
                "warnings_count": len(findings) if findings is  None else None,
                "timeout": False,
                "internal_error_occurred": False,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
                "runtime_errors_count": None
            }

        # Run tests and get results
        test_results = run_tests(".")

        if test_results:
            test_results["warnings"] = findings
            test_results["warnings_count"] = len(findings) if findings is not None else None
            test_results["syntax"] = syntax
            test_results["syntax_output"] = warnings
            test_results["assertions_density"] = assertions_density
            test_results["assertions_mccabe_ratio"] = mccabe
            print(f"Percentage of passed tests: {test_results['test_pass_rate']}")
            print(f"Total time: {test_results['execution_time_sec']} seconds")
            if test_results is not None and test_results['line_coverage_percent'] is not None:
                print(f"Coverage: {test_results['line_coverage_percent']}%")
            else:
                print("Coverage information not available.")
            return test_results
        else:
            print("Failed to execute Go tests.")
            return {
                "syntax": syntax,
                "syntax_output": warnings,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "timeout": False,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "internal_error_occurred": True,
                "runtime_errors_count": None,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
            }

    finally:
        # Restore the original working directory
        os.chdir(original_cwd)
        # Clean up the temporary directory
        shutil.rmtree(temp_dir)
        print(f"Temporary directory {temp_dir} has been removed.")


def x_analyze_go_tests__mutmut_48(go_file_path, test_file_path):
    # Create a temporary directory within "/data/generated/golang/"
    base_temp_dir = "./data/generated/docs_golang/"
    os.makedirs(base_temp_dir, exist_ok=True)
    temp_dir = tempfile.mkdtemp(dir=base_temp_dir)

    # Save the current working directory
    original_cwd = os.getcwd()

    try:
        # Copy the Go file and the specified test file to the temporary directory
        new_go_file_path = shutil.copy(go_file_path, temp_dir)
        new_test_file_path = shutil.copy(test_file_path, temp_dir)

        # Change to temporary directory
        os.chdir(temp_dir)

        # Check syntax
        syntax_build = check_syntax(os.path.basename(test_file_path))
        syntax_vet, warnings = check_for_warnings(temp_dir)
        findings = run_golangci(os.path.basename(test_file_path))
        syntax = determine_syntax_result(syntax_build, syntax_vet)
        assertions_density = assertions_density_go(os.path.basename(test_file_path))
        mccabe = assertions_mccabe_ratio_go(os.path.basename(new_go_file_path), os.path.basename(new_test_file_path))

        if syntax != CompileStatus.OK:
            print("Syntax check failed.")
            return {
                "syntax": syntax,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "syntax_output": warnings,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "XXtimeoutXX": False,
                "internal_error_occurred": False,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
                "runtime_errors_count": None
            }

        # Run tests and get results
        test_results = run_tests(".")

        if test_results:
            test_results["warnings"] = findings
            test_results["warnings_count"] = len(findings) if findings is not None else None
            test_results["syntax"] = syntax
            test_results["syntax_output"] = warnings
            test_results["assertions_density"] = assertions_density
            test_results["assertions_mccabe_ratio"] = mccabe
            print(f"Percentage of passed tests: {test_results['test_pass_rate']}")
            print(f"Total time: {test_results['execution_time_sec']} seconds")
            if test_results is not None and test_results['line_coverage_percent'] is not None:
                print(f"Coverage: {test_results['line_coverage_percent']}%")
            else:
                print("Coverage information not available.")
            return test_results
        else:
            print("Failed to execute Go tests.")
            return {
                "syntax": syntax,
                "syntax_output": warnings,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "timeout": False,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "internal_error_occurred": True,
                "runtime_errors_count": None,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
            }

    finally:
        # Restore the original working directory
        os.chdir(original_cwd)
        # Clean up the temporary directory
        shutil.rmtree(temp_dir)
        print(f"Temporary directory {temp_dir} has been removed.")


def x_analyze_go_tests__mutmut_49(go_file_path, test_file_path):
    # Create a temporary directory within "/data/generated/golang/"
    base_temp_dir = "./data/generated/docs_golang/"
    os.makedirs(base_temp_dir, exist_ok=True)
    temp_dir = tempfile.mkdtemp(dir=base_temp_dir)

    # Save the current working directory
    original_cwd = os.getcwd()

    try:
        # Copy the Go file and the specified test file to the temporary directory
        new_go_file_path = shutil.copy(go_file_path, temp_dir)
        new_test_file_path = shutil.copy(test_file_path, temp_dir)

        # Change to temporary directory
        os.chdir(temp_dir)

        # Check syntax
        syntax_build = check_syntax(os.path.basename(test_file_path))
        syntax_vet, warnings = check_for_warnings(temp_dir)
        findings = run_golangci(os.path.basename(test_file_path))
        syntax = determine_syntax_result(syntax_build, syntax_vet)
        assertions_density = assertions_density_go(os.path.basename(test_file_path))
        mccabe = assertions_mccabe_ratio_go(os.path.basename(new_go_file_path), os.path.basename(new_test_file_path))

        if syntax != CompileStatus.OK:
            print("Syntax check failed.")
            return {
                "syntax": syntax,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "syntax_output": warnings,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "timeout": True,
                "internal_error_occurred": False,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
                "runtime_errors_count": None
            }

        # Run tests and get results
        test_results = run_tests(".")

        if test_results:
            test_results["warnings"] = findings
            test_results["warnings_count"] = len(findings) if findings is not None else None
            test_results["syntax"] = syntax
            test_results["syntax_output"] = warnings
            test_results["assertions_density"] = assertions_density
            test_results["assertions_mccabe_ratio"] = mccabe
            print(f"Percentage of passed tests: {test_results['test_pass_rate']}")
            print(f"Total time: {test_results['execution_time_sec']} seconds")
            if test_results is not None and test_results['line_coverage_percent'] is not None:
                print(f"Coverage: {test_results['line_coverage_percent']}%")
            else:
                print("Coverage information not available.")
            return test_results
        else:
            print("Failed to execute Go tests.")
            return {
                "syntax": syntax,
                "syntax_output": warnings,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "timeout": False,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "internal_error_occurred": True,
                "runtime_errors_count": None,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
            }

    finally:
        # Restore the original working directory
        os.chdir(original_cwd)
        # Clean up the temporary directory
        shutil.rmtree(temp_dir)
        print(f"Temporary directory {temp_dir} has been removed.")


def x_analyze_go_tests__mutmut_50(go_file_path, test_file_path):
    # Create a temporary directory within "/data/generated/golang/"
    base_temp_dir = "./data/generated/docs_golang/"
    os.makedirs(base_temp_dir, exist_ok=True)
    temp_dir = tempfile.mkdtemp(dir=base_temp_dir)

    # Save the current working directory
    original_cwd = os.getcwd()

    try:
        # Copy the Go file and the specified test file to the temporary directory
        new_go_file_path = shutil.copy(go_file_path, temp_dir)
        new_test_file_path = shutil.copy(test_file_path, temp_dir)

        # Change to temporary directory
        os.chdir(temp_dir)

        # Check syntax
        syntax_build = check_syntax(os.path.basename(test_file_path))
        syntax_vet, warnings = check_for_warnings(temp_dir)
        findings = run_golangci(os.path.basename(test_file_path))
        syntax = determine_syntax_result(syntax_build, syntax_vet)
        assertions_density = assertions_density_go(os.path.basename(test_file_path))
        mccabe = assertions_mccabe_ratio_go(os.path.basename(new_go_file_path), os.path.basename(new_test_file_path))

        if syntax != CompileStatus.OK:
            print("Syntax check failed.")
            return {
                "syntax": syntax,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "syntax_output": warnings,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "timeout": False,
                "XXinternal_error_occurredXX": False,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
                "runtime_errors_count": None
            }

        # Run tests and get results
        test_results = run_tests(".")

        if test_results:
            test_results["warnings"] = findings
            test_results["warnings_count"] = len(findings) if findings is not None else None
            test_results["syntax"] = syntax
            test_results["syntax_output"] = warnings
            test_results["assertions_density"] = assertions_density
            test_results["assertions_mccabe_ratio"] = mccabe
            print(f"Percentage of passed tests: {test_results['test_pass_rate']}")
            print(f"Total time: {test_results['execution_time_sec']} seconds")
            if test_results is not None and test_results['line_coverage_percent'] is not None:
                print(f"Coverage: {test_results['line_coverage_percent']}%")
            else:
                print("Coverage information not available.")
            return test_results
        else:
            print("Failed to execute Go tests.")
            return {
                "syntax": syntax,
                "syntax_output": warnings,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "timeout": False,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "internal_error_occurred": True,
                "runtime_errors_count": None,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
            }

    finally:
        # Restore the original working directory
        os.chdir(original_cwd)
        # Clean up the temporary directory
        shutil.rmtree(temp_dir)
        print(f"Temporary directory {temp_dir} has been removed.")


def x_analyze_go_tests__mutmut_51(go_file_path, test_file_path):
    # Create a temporary directory within "/data/generated/golang/"
    base_temp_dir = "./data/generated/docs_golang/"
    os.makedirs(base_temp_dir, exist_ok=True)
    temp_dir = tempfile.mkdtemp(dir=base_temp_dir)

    # Save the current working directory
    original_cwd = os.getcwd()

    try:
        # Copy the Go file and the specified test file to the temporary directory
        new_go_file_path = shutil.copy(go_file_path, temp_dir)
        new_test_file_path = shutil.copy(test_file_path, temp_dir)

        # Change to temporary directory
        os.chdir(temp_dir)

        # Check syntax
        syntax_build = check_syntax(os.path.basename(test_file_path))
        syntax_vet, warnings = check_for_warnings(temp_dir)
        findings = run_golangci(os.path.basename(test_file_path))
        syntax = determine_syntax_result(syntax_build, syntax_vet)
        assertions_density = assertions_density_go(os.path.basename(test_file_path))
        mccabe = assertions_mccabe_ratio_go(os.path.basename(new_go_file_path), os.path.basename(new_test_file_path))

        if syntax != CompileStatus.OK:
            print("Syntax check failed.")
            return {
                "syntax": syntax,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "syntax_output": warnings,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "timeout": False,
                "internal_error_occurred": True,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
                "runtime_errors_count": None
            }

        # Run tests and get results
        test_results = run_tests(".")

        if test_results:
            test_results["warnings"] = findings
            test_results["warnings_count"] = len(findings) if findings is not None else None
            test_results["syntax"] = syntax
            test_results["syntax_output"] = warnings
            test_results["assertions_density"] = assertions_density
            test_results["assertions_mccabe_ratio"] = mccabe
            print(f"Percentage of passed tests: {test_results['test_pass_rate']}")
            print(f"Total time: {test_results['execution_time_sec']} seconds")
            if test_results is not None and test_results['line_coverage_percent'] is not None:
                print(f"Coverage: {test_results['line_coverage_percent']}%")
            else:
                print("Coverage information not available.")
            return test_results
        else:
            print("Failed to execute Go tests.")
            return {
                "syntax": syntax,
                "syntax_output": warnings,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "timeout": False,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "internal_error_occurred": True,
                "runtime_errors_count": None,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
            }

    finally:
        # Restore the original working directory
        os.chdir(original_cwd)
        # Clean up the temporary directory
        shutil.rmtree(temp_dir)
        print(f"Temporary directory {temp_dir} has been removed.")


def x_analyze_go_tests__mutmut_52(go_file_path, test_file_path):
    # Create a temporary directory within "/data/generated/golang/"
    base_temp_dir = "./data/generated/docs_golang/"
    os.makedirs(base_temp_dir, exist_ok=True)
    temp_dir = tempfile.mkdtemp(dir=base_temp_dir)

    # Save the current working directory
    original_cwd = os.getcwd()

    try:
        # Copy the Go file and the specified test file to the temporary directory
        new_go_file_path = shutil.copy(go_file_path, temp_dir)
        new_test_file_path = shutil.copy(test_file_path, temp_dir)

        # Change to temporary directory
        os.chdir(temp_dir)

        # Check syntax
        syntax_build = check_syntax(os.path.basename(test_file_path))
        syntax_vet, warnings = check_for_warnings(temp_dir)
        findings = run_golangci(os.path.basename(test_file_path))
        syntax = determine_syntax_result(syntax_build, syntax_vet)
        assertions_density = assertions_density_go(os.path.basename(test_file_path))
        mccabe = assertions_mccabe_ratio_go(os.path.basename(new_go_file_path), os.path.basename(new_test_file_path))

        if syntax != CompileStatus.OK:
            print("Syntax check failed.")
            return {
                "syntax": syntax,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "syntax_output": warnings,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "timeout": False,
                "internal_error_occurred": False,
                "XXassertions_densityXX": assertions_density,
                "assertions_mccabe_ratio": mccabe,
                "runtime_errors_count": None
            }

        # Run tests and get results
        test_results = run_tests(".")

        if test_results:
            test_results["warnings"] = findings
            test_results["warnings_count"] = len(findings) if findings is not None else None
            test_results["syntax"] = syntax
            test_results["syntax_output"] = warnings
            test_results["assertions_density"] = assertions_density
            test_results["assertions_mccabe_ratio"] = mccabe
            print(f"Percentage of passed tests: {test_results['test_pass_rate']}")
            print(f"Total time: {test_results['execution_time_sec']} seconds")
            if test_results is not None and test_results['line_coverage_percent'] is not None:
                print(f"Coverage: {test_results['line_coverage_percent']}%")
            else:
                print("Coverage information not available.")
            return test_results
        else:
            print("Failed to execute Go tests.")
            return {
                "syntax": syntax,
                "syntax_output": warnings,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "timeout": False,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "internal_error_occurred": True,
                "runtime_errors_count": None,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
            }

    finally:
        # Restore the original working directory
        os.chdir(original_cwd)
        # Clean up the temporary directory
        shutil.rmtree(temp_dir)
        print(f"Temporary directory {temp_dir} has been removed.")


def x_analyze_go_tests__mutmut_53(go_file_path, test_file_path):
    # Create a temporary directory within "/data/generated/golang/"
    base_temp_dir = "./data/generated/docs_golang/"
    os.makedirs(base_temp_dir, exist_ok=True)
    temp_dir = tempfile.mkdtemp(dir=base_temp_dir)

    # Save the current working directory
    original_cwd = os.getcwd()

    try:
        # Copy the Go file and the specified test file to the temporary directory
        new_go_file_path = shutil.copy(go_file_path, temp_dir)
        new_test_file_path = shutil.copy(test_file_path, temp_dir)

        # Change to temporary directory
        os.chdir(temp_dir)

        # Check syntax
        syntax_build = check_syntax(os.path.basename(test_file_path))
        syntax_vet, warnings = check_for_warnings(temp_dir)
        findings = run_golangci(os.path.basename(test_file_path))
        syntax = determine_syntax_result(syntax_build, syntax_vet)
        assertions_density = assertions_density_go(os.path.basename(test_file_path))
        mccabe = assertions_mccabe_ratio_go(os.path.basename(new_go_file_path), os.path.basename(new_test_file_path))

        if syntax != CompileStatus.OK:
            print("Syntax check failed.")
            return {
                "syntax": syntax,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "syntax_output": warnings,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "timeout": False,
                "internal_error_occurred": False,
                "assertions_density": assertions_density,
                "XXassertions_mccabe_ratioXX": mccabe,
                "runtime_errors_count": None
            }

        # Run tests and get results
        test_results = run_tests(".")

        if test_results:
            test_results["warnings"] = findings
            test_results["warnings_count"] = len(findings) if findings is not None else None
            test_results["syntax"] = syntax
            test_results["syntax_output"] = warnings
            test_results["assertions_density"] = assertions_density
            test_results["assertions_mccabe_ratio"] = mccabe
            print(f"Percentage of passed tests: {test_results['test_pass_rate']}")
            print(f"Total time: {test_results['execution_time_sec']} seconds")
            if test_results is not None and test_results['line_coverage_percent'] is not None:
                print(f"Coverage: {test_results['line_coverage_percent']}%")
            else:
                print("Coverage information not available.")
            return test_results
        else:
            print("Failed to execute Go tests.")
            return {
                "syntax": syntax,
                "syntax_output": warnings,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "timeout": False,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "internal_error_occurred": True,
                "runtime_errors_count": None,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
            }

    finally:
        # Restore the original working directory
        os.chdir(original_cwd)
        # Clean up the temporary directory
        shutil.rmtree(temp_dir)
        print(f"Temporary directory {temp_dir} has been removed.")


def x_analyze_go_tests__mutmut_54(go_file_path, test_file_path):
    # Create a temporary directory within "/data/generated/golang/"
    base_temp_dir = "./data/generated/docs_golang/"
    os.makedirs(base_temp_dir, exist_ok=True)
    temp_dir = tempfile.mkdtemp(dir=base_temp_dir)

    # Save the current working directory
    original_cwd = os.getcwd()

    try:
        # Copy the Go file and the specified test file to the temporary directory
        new_go_file_path = shutil.copy(go_file_path, temp_dir)
        new_test_file_path = shutil.copy(test_file_path, temp_dir)

        # Change to temporary directory
        os.chdir(temp_dir)

        # Check syntax
        syntax_build = check_syntax(os.path.basename(test_file_path))
        syntax_vet, warnings = check_for_warnings(temp_dir)
        findings = run_golangci(os.path.basename(test_file_path))
        syntax = determine_syntax_result(syntax_build, syntax_vet)
        assertions_density = assertions_density_go(os.path.basename(test_file_path))
        mccabe = assertions_mccabe_ratio_go(os.path.basename(new_go_file_path), os.path.basename(new_test_file_path))

        if syntax != CompileStatus.OK:
            print("Syntax check failed.")
            return {
                "syntax": syntax,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "syntax_output": warnings,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "timeout": False,
                "internal_error_occurred": False,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
                "XXruntime_errors_countXX": None
            }

        # Run tests and get results
        test_results = run_tests(".")

        if test_results:
            test_results["warnings"] = findings
            test_results["warnings_count"] = len(findings) if findings is not None else None
            test_results["syntax"] = syntax
            test_results["syntax_output"] = warnings
            test_results["assertions_density"] = assertions_density
            test_results["assertions_mccabe_ratio"] = mccabe
            print(f"Percentage of passed tests: {test_results['test_pass_rate']}")
            print(f"Total time: {test_results['execution_time_sec']} seconds")
            if test_results is not None and test_results['line_coverage_percent'] is not None:
                print(f"Coverage: {test_results['line_coverage_percent']}%")
            else:
                print("Coverage information not available.")
            return test_results
        else:
            print("Failed to execute Go tests.")
            return {
                "syntax": syntax,
                "syntax_output": warnings,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "timeout": False,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "internal_error_occurred": True,
                "runtime_errors_count": None,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
            }

    finally:
        # Restore the original working directory
        os.chdir(original_cwd)
        # Clean up the temporary directory
        shutil.rmtree(temp_dir)
        print(f"Temporary directory {temp_dir} has been removed.")


def x_analyze_go_tests__mutmut_55(go_file_path, test_file_path):
    # Create a temporary directory within "/data/generated/golang/"
    base_temp_dir = "./data/generated/docs_golang/"
    os.makedirs(base_temp_dir, exist_ok=True)
    temp_dir = tempfile.mkdtemp(dir=base_temp_dir)

    # Save the current working directory
    original_cwd = os.getcwd()

    try:
        # Copy the Go file and the specified test file to the temporary directory
        new_go_file_path = shutil.copy(go_file_path, temp_dir)
        new_test_file_path = shutil.copy(test_file_path, temp_dir)

        # Change to temporary directory
        os.chdir(temp_dir)

        # Check syntax
        syntax_build = check_syntax(os.path.basename(test_file_path))
        syntax_vet, warnings = check_for_warnings(temp_dir)
        findings = run_golangci(os.path.basename(test_file_path))
        syntax = determine_syntax_result(syntax_build, syntax_vet)
        assertions_density = assertions_density_go(os.path.basename(test_file_path))
        mccabe = assertions_mccabe_ratio_go(os.path.basename(new_go_file_path), os.path.basename(new_test_file_path))

        if syntax != CompileStatus.OK:
            print("Syntax check failed.")
            return {
                "syntax": syntax,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "syntax_output": warnings,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "timeout": False,
                "internal_error_occurred": False,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
                "runtime_errors_count": None
            }

        # Run tests and get results
        test_results = run_tests("XX.XX")

        if test_results:
            test_results["warnings"] = findings
            test_results["warnings_count"] = len(findings) if findings is not None else None
            test_results["syntax"] = syntax
            test_results["syntax_output"] = warnings
            test_results["assertions_density"] = assertions_density
            test_results["assertions_mccabe_ratio"] = mccabe
            print(f"Percentage of passed tests: {test_results['test_pass_rate']}")
            print(f"Total time: {test_results['execution_time_sec']} seconds")
            if test_results is not None and test_results['line_coverage_percent'] is not None:
                print(f"Coverage: {test_results['line_coverage_percent']}%")
            else:
                print("Coverage information not available.")
            return test_results
        else:
            print("Failed to execute Go tests.")
            return {
                "syntax": syntax,
                "syntax_output": warnings,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "timeout": False,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "internal_error_occurred": True,
                "runtime_errors_count": None,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
            }

    finally:
        # Restore the original working directory
        os.chdir(original_cwd)
        # Clean up the temporary directory
        shutil.rmtree(temp_dir)
        print(f"Temporary directory {temp_dir} has been removed.")


def x_analyze_go_tests__mutmut_56(go_file_path, test_file_path):
    # Create a temporary directory within "/data/generated/golang/"
    base_temp_dir = "./data/generated/docs_golang/"
    os.makedirs(base_temp_dir, exist_ok=True)
    temp_dir = tempfile.mkdtemp(dir=base_temp_dir)

    # Save the current working directory
    original_cwd = os.getcwd()

    try:
        # Copy the Go file and the specified test file to the temporary directory
        new_go_file_path = shutil.copy(go_file_path, temp_dir)
        new_test_file_path = shutil.copy(test_file_path, temp_dir)

        # Change to temporary directory
        os.chdir(temp_dir)

        # Check syntax
        syntax_build = check_syntax(os.path.basename(test_file_path))
        syntax_vet, warnings = check_for_warnings(temp_dir)
        findings = run_golangci(os.path.basename(test_file_path))
        syntax = determine_syntax_result(syntax_build, syntax_vet)
        assertions_density = assertions_density_go(os.path.basename(test_file_path))
        mccabe = assertions_mccabe_ratio_go(os.path.basename(new_go_file_path), os.path.basename(new_test_file_path))

        if syntax != CompileStatus.OK:
            print("Syntax check failed.")
            return {
                "syntax": syntax,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "syntax_output": warnings,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "timeout": False,
                "internal_error_occurred": False,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
                "runtime_errors_count": None
            }

        # Run tests and get results
        test_results = None

        if test_results:
            test_results["warnings"] = findings
            test_results["warnings_count"] = len(findings) if findings is not None else None
            test_results["syntax"] = syntax
            test_results["syntax_output"] = warnings
            test_results["assertions_density"] = assertions_density
            test_results["assertions_mccabe_ratio"] = mccabe
            print(f"Percentage of passed tests: {test_results['test_pass_rate']}")
            print(f"Total time: {test_results['execution_time_sec']} seconds")
            if test_results is not None and test_results['line_coverage_percent'] is not None:
                print(f"Coverage: {test_results['line_coverage_percent']}%")
            else:
                print("Coverage information not available.")
            return test_results
        else:
            print("Failed to execute Go tests.")
            return {
                "syntax": syntax,
                "syntax_output": warnings,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "timeout": False,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "internal_error_occurred": True,
                "runtime_errors_count": None,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
            }

    finally:
        # Restore the original working directory
        os.chdir(original_cwd)
        # Clean up the temporary directory
        shutil.rmtree(temp_dir)
        print(f"Temporary directory {temp_dir} has been removed.")


def x_analyze_go_tests__mutmut_57(go_file_path, test_file_path):
    # Create a temporary directory within "/data/generated/golang/"
    base_temp_dir = "./data/generated/docs_golang/"
    os.makedirs(base_temp_dir, exist_ok=True)
    temp_dir = tempfile.mkdtemp(dir=base_temp_dir)

    # Save the current working directory
    original_cwd = os.getcwd()

    try:
        # Copy the Go file and the specified test file to the temporary directory
        new_go_file_path = shutil.copy(go_file_path, temp_dir)
        new_test_file_path = shutil.copy(test_file_path, temp_dir)

        # Change to temporary directory
        os.chdir(temp_dir)

        # Check syntax
        syntax_build = check_syntax(os.path.basename(test_file_path))
        syntax_vet, warnings = check_for_warnings(temp_dir)
        findings = run_golangci(os.path.basename(test_file_path))
        syntax = determine_syntax_result(syntax_build, syntax_vet)
        assertions_density = assertions_density_go(os.path.basename(test_file_path))
        mccabe = assertions_mccabe_ratio_go(os.path.basename(new_go_file_path), os.path.basename(new_test_file_path))

        if syntax != CompileStatus.OK:
            print("Syntax check failed.")
            return {
                "syntax": syntax,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "syntax_output": warnings,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "timeout": False,
                "internal_error_occurred": False,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
                "runtime_errors_count": None
            }

        # Run tests and get results
        test_results = run_tests(".")

        if test_results:
            test_results["XXwarningsXX"] = findings
            test_results["warnings_count"] = len(findings) if findings is not None else None
            test_results["syntax"] = syntax
            test_results["syntax_output"] = warnings
            test_results["assertions_density"] = assertions_density
            test_results["assertions_mccabe_ratio"] = mccabe
            print(f"Percentage of passed tests: {test_results['test_pass_rate']}")
            print(f"Total time: {test_results['execution_time_sec']} seconds")
            if test_results is not None and test_results['line_coverage_percent'] is not None:
                print(f"Coverage: {test_results['line_coverage_percent']}%")
            else:
                print("Coverage information not available.")
            return test_results
        else:
            print("Failed to execute Go tests.")
            return {
                "syntax": syntax,
                "syntax_output": warnings,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "timeout": False,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "internal_error_occurred": True,
                "runtime_errors_count": None,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
            }

    finally:
        # Restore the original working directory
        os.chdir(original_cwd)
        # Clean up the temporary directory
        shutil.rmtree(temp_dir)
        print(f"Temporary directory {temp_dir} has been removed.")


def x_analyze_go_tests__mutmut_58(go_file_path, test_file_path):
    # Create a temporary directory within "/data/generated/golang/"
    base_temp_dir = "./data/generated/docs_golang/"
    os.makedirs(base_temp_dir, exist_ok=True)
    temp_dir = tempfile.mkdtemp(dir=base_temp_dir)

    # Save the current working directory
    original_cwd = os.getcwd()

    try:
        # Copy the Go file and the specified test file to the temporary directory
        new_go_file_path = shutil.copy(go_file_path, temp_dir)
        new_test_file_path = shutil.copy(test_file_path, temp_dir)

        # Change to temporary directory
        os.chdir(temp_dir)

        # Check syntax
        syntax_build = check_syntax(os.path.basename(test_file_path))
        syntax_vet, warnings = check_for_warnings(temp_dir)
        findings = run_golangci(os.path.basename(test_file_path))
        syntax = determine_syntax_result(syntax_build, syntax_vet)
        assertions_density = assertions_density_go(os.path.basename(test_file_path))
        mccabe = assertions_mccabe_ratio_go(os.path.basename(new_go_file_path), os.path.basename(new_test_file_path))

        if syntax != CompileStatus.OK:
            print("Syntax check failed.")
            return {
                "syntax": syntax,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "syntax_output": warnings,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "timeout": False,
                "internal_error_occurred": False,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
                "runtime_errors_count": None
            }

        # Run tests and get results
        test_results = run_tests(".")

        if test_results:
            test_results[None] = findings
            test_results["warnings_count"] = len(findings) if findings is not None else None
            test_results["syntax"] = syntax
            test_results["syntax_output"] = warnings
            test_results["assertions_density"] = assertions_density
            test_results["assertions_mccabe_ratio"] = mccabe
            print(f"Percentage of passed tests: {test_results['test_pass_rate']}")
            print(f"Total time: {test_results['execution_time_sec']} seconds")
            if test_results is not None and test_results['line_coverage_percent'] is not None:
                print(f"Coverage: {test_results['line_coverage_percent']}%")
            else:
                print("Coverage information not available.")
            return test_results
        else:
            print("Failed to execute Go tests.")
            return {
                "syntax": syntax,
                "syntax_output": warnings,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "timeout": False,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "internal_error_occurred": True,
                "runtime_errors_count": None,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
            }

    finally:
        # Restore the original working directory
        os.chdir(original_cwd)
        # Clean up the temporary directory
        shutil.rmtree(temp_dir)
        print(f"Temporary directory {temp_dir} has been removed.")


def x_analyze_go_tests__mutmut_59(go_file_path, test_file_path):
    # Create a temporary directory within "/data/generated/golang/"
    base_temp_dir = "./data/generated/docs_golang/"
    os.makedirs(base_temp_dir, exist_ok=True)
    temp_dir = tempfile.mkdtemp(dir=base_temp_dir)

    # Save the current working directory
    original_cwd = os.getcwd()

    try:
        # Copy the Go file and the specified test file to the temporary directory
        new_go_file_path = shutil.copy(go_file_path, temp_dir)
        new_test_file_path = shutil.copy(test_file_path, temp_dir)

        # Change to temporary directory
        os.chdir(temp_dir)

        # Check syntax
        syntax_build = check_syntax(os.path.basename(test_file_path))
        syntax_vet, warnings = check_for_warnings(temp_dir)
        findings = run_golangci(os.path.basename(test_file_path))
        syntax = determine_syntax_result(syntax_build, syntax_vet)
        assertions_density = assertions_density_go(os.path.basename(test_file_path))
        mccabe = assertions_mccabe_ratio_go(os.path.basename(new_go_file_path), os.path.basename(new_test_file_path))

        if syntax != CompileStatus.OK:
            print("Syntax check failed.")
            return {
                "syntax": syntax,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "syntax_output": warnings,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "timeout": False,
                "internal_error_occurred": False,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
                "runtime_errors_count": None
            }

        # Run tests and get results
        test_results = run_tests(".")

        if test_results:
            test_results["warnings"] = None
            test_results["warnings_count"] = len(findings) if findings is not None else None
            test_results["syntax"] = syntax
            test_results["syntax_output"] = warnings
            test_results["assertions_density"] = assertions_density
            test_results["assertions_mccabe_ratio"] = mccabe
            print(f"Percentage of passed tests: {test_results['test_pass_rate']}")
            print(f"Total time: {test_results['execution_time_sec']} seconds")
            if test_results is not None and test_results['line_coverage_percent'] is not None:
                print(f"Coverage: {test_results['line_coverage_percent']}%")
            else:
                print("Coverage information not available.")
            return test_results
        else:
            print("Failed to execute Go tests.")
            return {
                "syntax": syntax,
                "syntax_output": warnings,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "timeout": False,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "internal_error_occurred": True,
                "runtime_errors_count": None,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
            }

    finally:
        # Restore the original working directory
        os.chdir(original_cwd)
        # Clean up the temporary directory
        shutil.rmtree(temp_dir)
        print(f"Temporary directory {temp_dir} has been removed.")


def x_analyze_go_tests__mutmut_60(go_file_path, test_file_path):
    # Create a temporary directory within "/data/generated/golang/"
    base_temp_dir = "./data/generated/docs_golang/"
    os.makedirs(base_temp_dir, exist_ok=True)
    temp_dir = tempfile.mkdtemp(dir=base_temp_dir)

    # Save the current working directory
    original_cwd = os.getcwd()

    try:
        # Copy the Go file and the specified test file to the temporary directory
        new_go_file_path = shutil.copy(go_file_path, temp_dir)
        new_test_file_path = shutil.copy(test_file_path, temp_dir)

        # Change to temporary directory
        os.chdir(temp_dir)

        # Check syntax
        syntax_build = check_syntax(os.path.basename(test_file_path))
        syntax_vet, warnings = check_for_warnings(temp_dir)
        findings = run_golangci(os.path.basename(test_file_path))
        syntax = determine_syntax_result(syntax_build, syntax_vet)
        assertions_density = assertions_density_go(os.path.basename(test_file_path))
        mccabe = assertions_mccabe_ratio_go(os.path.basename(new_go_file_path), os.path.basename(new_test_file_path))

        if syntax != CompileStatus.OK:
            print("Syntax check failed.")
            return {
                "syntax": syntax,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "syntax_output": warnings,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "timeout": False,
                "internal_error_occurred": False,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
                "runtime_errors_count": None
            }

        # Run tests and get results
        test_results = run_tests(".")

        if test_results:
            test_results["warnings"] = findings
            test_results["XXwarnings_countXX"] = len(findings) if findings is not None else None
            test_results["syntax"] = syntax
            test_results["syntax_output"] = warnings
            test_results["assertions_density"] = assertions_density
            test_results["assertions_mccabe_ratio"] = mccabe
            print(f"Percentage of passed tests: {test_results['test_pass_rate']}")
            print(f"Total time: {test_results['execution_time_sec']} seconds")
            if test_results is not None and test_results['line_coverage_percent'] is not None:
                print(f"Coverage: {test_results['line_coverage_percent']}%")
            else:
                print("Coverage information not available.")
            return test_results
        else:
            print("Failed to execute Go tests.")
            return {
                "syntax": syntax,
                "syntax_output": warnings,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "timeout": False,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "internal_error_occurred": True,
                "runtime_errors_count": None,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
            }

    finally:
        # Restore the original working directory
        os.chdir(original_cwd)
        # Clean up the temporary directory
        shutil.rmtree(temp_dir)
        print(f"Temporary directory {temp_dir} has been removed.")


def x_analyze_go_tests__mutmut_61(go_file_path, test_file_path):
    # Create a temporary directory within "/data/generated/golang/"
    base_temp_dir = "./data/generated/docs_golang/"
    os.makedirs(base_temp_dir, exist_ok=True)
    temp_dir = tempfile.mkdtemp(dir=base_temp_dir)

    # Save the current working directory
    original_cwd = os.getcwd()

    try:
        # Copy the Go file and the specified test file to the temporary directory
        new_go_file_path = shutil.copy(go_file_path, temp_dir)
        new_test_file_path = shutil.copy(test_file_path, temp_dir)

        # Change to temporary directory
        os.chdir(temp_dir)

        # Check syntax
        syntax_build = check_syntax(os.path.basename(test_file_path))
        syntax_vet, warnings = check_for_warnings(temp_dir)
        findings = run_golangci(os.path.basename(test_file_path))
        syntax = determine_syntax_result(syntax_build, syntax_vet)
        assertions_density = assertions_density_go(os.path.basename(test_file_path))
        mccabe = assertions_mccabe_ratio_go(os.path.basename(new_go_file_path), os.path.basename(new_test_file_path))

        if syntax != CompileStatus.OK:
            print("Syntax check failed.")
            return {
                "syntax": syntax,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "syntax_output": warnings,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "timeout": False,
                "internal_error_occurred": False,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
                "runtime_errors_count": None
            }

        # Run tests and get results
        test_results = run_tests(".")

        if test_results:
            test_results["warnings"] = findings
            test_results[None] = len(findings) if findings is not None else None
            test_results["syntax"] = syntax
            test_results["syntax_output"] = warnings
            test_results["assertions_density"] = assertions_density
            test_results["assertions_mccabe_ratio"] = mccabe
            print(f"Percentage of passed tests: {test_results['test_pass_rate']}")
            print(f"Total time: {test_results['execution_time_sec']} seconds")
            if test_results is not None and test_results['line_coverage_percent'] is not None:
                print(f"Coverage: {test_results['line_coverage_percent']}%")
            else:
                print("Coverage information not available.")
            return test_results
        else:
            print("Failed to execute Go tests.")
            return {
                "syntax": syntax,
                "syntax_output": warnings,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "timeout": False,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "internal_error_occurred": True,
                "runtime_errors_count": None,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
            }

    finally:
        # Restore the original working directory
        os.chdir(original_cwd)
        # Clean up the temporary directory
        shutil.rmtree(temp_dir)
        print(f"Temporary directory {temp_dir} has been removed.")


def x_analyze_go_tests__mutmut_62(go_file_path, test_file_path):
    # Create a temporary directory within "/data/generated/golang/"
    base_temp_dir = "./data/generated/docs_golang/"
    os.makedirs(base_temp_dir, exist_ok=True)
    temp_dir = tempfile.mkdtemp(dir=base_temp_dir)

    # Save the current working directory
    original_cwd = os.getcwd()

    try:
        # Copy the Go file and the specified test file to the temporary directory
        new_go_file_path = shutil.copy(go_file_path, temp_dir)
        new_test_file_path = shutil.copy(test_file_path, temp_dir)

        # Change to temporary directory
        os.chdir(temp_dir)

        # Check syntax
        syntax_build = check_syntax(os.path.basename(test_file_path))
        syntax_vet, warnings = check_for_warnings(temp_dir)
        findings = run_golangci(os.path.basename(test_file_path))
        syntax = determine_syntax_result(syntax_build, syntax_vet)
        assertions_density = assertions_density_go(os.path.basename(test_file_path))
        mccabe = assertions_mccabe_ratio_go(os.path.basename(new_go_file_path), os.path.basename(new_test_file_path))

        if syntax != CompileStatus.OK:
            print("Syntax check failed.")
            return {
                "syntax": syntax,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "syntax_output": warnings,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "timeout": False,
                "internal_error_occurred": False,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
                "runtime_errors_count": None
            }

        # Run tests and get results
        test_results = run_tests(".")

        if test_results:
            test_results["warnings"] = findings
            test_results["warnings_count"] = len(findings) if findings is  None else None
            test_results["syntax"] = syntax
            test_results["syntax_output"] = warnings
            test_results["assertions_density"] = assertions_density
            test_results["assertions_mccabe_ratio"] = mccabe
            print(f"Percentage of passed tests: {test_results['test_pass_rate']}")
            print(f"Total time: {test_results['execution_time_sec']} seconds")
            if test_results is not None and test_results['line_coverage_percent'] is not None:
                print(f"Coverage: {test_results['line_coverage_percent']}%")
            else:
                print("Coverage information not available.")
            return test_results
        else:
            print("Failed to execute Go tests.")
            return {
                "syntax": syntax,
                "syntax_output": warnings,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "timeout": False,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "internal_error_occurred": True,
                "runtime_errors_count": None,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
            }

    finally:
        # Restore the original working directory
        os.chdir(original_cwd)
        # Clean up the temporary directory
        shutil.rmtree(temp_dir)
        print(f"Temporary directory {temp_dir} has been removed.")


def x_analyze_go_tests__mutmut_63(go_file_path, test_file_path):
    # Create a temporary directory within "/data/generated/golang/"
    base_temp_dir = "./data/generated/docs_golang/"
    os.makedirs(base_temp_dir, exist_ok=True)
    temp_dir = tempfile.mkdtemp(dir=base_temp_dir)

    # Save the current working directory
    original_cwd = os.getcwd()

    try:
        # Copy the Go file and the specified test file to the temporary directory
        new_go_file_path = shutil.copy(go_file_path, temp_dir)
        new_test_file_path = shutil.copy(test_file_path, temp_dir)

        # Change to temporary directory
        os.chdir(temp_dir)

        # Check syntax
        syntax_build = check_syntax(os.path.basename(test_file_path))
        syntax_vet, warnings = check_for_warnings(temp_dir)
        findings = run_golangci(os.path.basename(test_file_path))
        syntax = determine_syntax_result(syntax_build, syntax_vet)
        assertions_density = assertions_density_go(os.path.basename(test_file_path))
        mccabe = assertions_mccabe_ratio_go(os.path.basename(new_go_file_path), os.path.basename(new_test_file_path))

        if syntax != CompileStatus.OK:
            print("Syntax check failed.")
            return {
                "syntax": syntax,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "syntax_output": warnings,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "timeout": False,
                "internal_error_occurred": False,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
                "runtime_errors_count": None
            }

        # Run tests and get results
        test_results = run_tests(".")

        if test_results:
            test_results["warnings"] = findings
            test_results["warnings_count"] = None
            test_results["syntax"] = syntax
            test_results["syntax_output"] = warnings
            test_results["assertions_density"] = assertions_density
            test_results["assertions_mccabe_ratio"] = mccabe
            print(f"Percentage of passed tests: {test_results['test_pass_rate']}")
            print(f"Total time: {test_results['execution_time_sec']} seconds")
            if test_results is not None and test_results['line_coverage_percent'] is not None:
                print(f"Coverage: {test_results['line_coverage_percent']}%")
            else:
                print("Coverage information not available.")
            return test_results
        else:
            print("Failed to execute Go tests.")
            return {
                "syntax": syntax,
                "syntax_output": warnings,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "timeout": False,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "internal_error_occurred": True,
                "runtime_errors_count": None,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
            }

    finally:
        # Restore the original working directory
        os.chdir(original_cwd)
        # Clean up the temporary directory
        shutil.rmtree(temp_dir)
        print(f"Temporary directory {temp_dir} has been removed.")


def x_analyze_go_tests__mutmut_64(go_file_path, test_file_path):
    # Create a temporary directory within "/data/generated/golang/"
    base_temp_dir = "./data/generated/docs_golang/"
    os.makedirs(base_temp_dir, exist_ok=True)
    temp_dir = tempfile.mkdtemp(dir=base_temp_dir)

    # Save the current working directory
    original_cwd = os.getcwd()

    try:
        # Copy the Go file and the specified test file to the temporary directory
        new_go_file_path = shutil.copy(go_file_path, temp_dir)
        new_test_file_path = shutil.copy(test_file_path, temp_dir)

        # Change to temporary directory
        os.chdir(temp_dir)

        # Check syntax
        syntax_build = check_syntax(os.path.basename(test_file_path))
        syntax_vet, warnings = check_for_warnings(temp_dir)
        findings = run_golangci(os.path.basename(test_file_path))
        syntax = determine_syntax_result(syntax_build, syntax_vet)
        assertions_density = assertions_density_go(os.path.basename(test_file_path))
        mccabe = assertions_mccabe_ratio_go(os.path.basename(new_go_file_path), os.path.basename(new_test_file_path))

        if syntax != CompileStatus.OK:
            print("Syntax check failed.")
            return {
                "syntax": syntax,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "syntax_output": warnings,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "timeout": False,
                "internal_error_occurred": False,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
                "runtime_errors_count": None
            }

        # Run tests and get results
        test_results = run_tests(".")

        if test_results:
            test_results["warnings"] = findings
            test_results["warnings_count"] = len(findings) if findings is not None else None
            test_results["XXsyntaxXX"] = syntax
            test_results["syntax_output"] = warnings
            test_results["assertions_density"] = assertions_density
            test_results["assertions_mccabe_ratio"] = mccabe
            print(f"Percentage of passed tests: {test_results['test_pass_rate']}")
            print(f"Total time: {test_results['execution_time_sec']} seconds")
            if test_results is not None and test_results['line_coverage_percent'] is not None:
                print(f"Coverage: {test_results['line_coverage_percent']}%")
            else:
                print("Coverage information not available.")
            return test_results
        else:
            print("Failed to execute Go tests.")
            return {
                "syntax": syntax,
                "syntax_output": warnings,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "timeout": False,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "internal_error_occurred": True,
                "runtime_errors_count": None,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
            }

    finally:
        # Restore the original working directory
        os.chdir(original_cwd)
        # Clean up the temporary directory
        shutil.rmtree(temp_dir)
        print(f"Temporary directory {temp_dir} has been removed.")


def x_analyze_go_tests__mutmut_65(go_file_path, test_file_path):
    # Create a temporary directory within "/data/generated/golang/"
    base_temp_dir = "./data/generated/docs_golang/"
    os.makedirs(base_temp_dir, exist_ok=True)
    temp_dir = tempfile.mkdtemp(dir=base_temp_dir)

    # Save the current working directory
    original_cwd = os.getcwd()

    try:
        # Copy the Go file and the specified test file to the temporary directory
        new_go_file_path = shutil.copy(go_file_path, temp_dir)
        new_test_file_path = shutil.copy(test_file_path, temp_dir)

        # Change to temporary directory
        os.chdir(temp_dir)

        # Check syntax
        syntax_build = check_syntax(os.path.basename(test_file_path))
        syntax_vet, warnings = check_for_warnings(temp_dir)
        findings = run_golangci(os.path.basename(test_file_path))
        syntax = determine_syntax_result(syntax_build, syntax_vet)
        assertions_density = assertions_density_go(os.path.basename(test_file_path))
        mccabe = assertions_mccabe_ratio_go(os.path.basename(new_go_file_path), os.path.basename(new_test_file_path))

        if syntax != CompileStatus.OK:
            print("Syntax check failed.")
            return {
                "syntax": syntax,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "syntax_output": warnings,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "timeout": False,
                "internal_error_occurred": False,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
                "runtime_errors_count": None
            }

        # Run tests and get results
        test_results = run_tests(".")

        if test_results:
            test_results["warnings"] = findings
            test_results["warnings_count"] = len(findings) if findings is not None else None
            test_results[None] = syntax
            test_results["syntax_output"] = warnings
            test_results["assertions_density"] = assertions_density
            test_results["assertions_mccabe_ratio"] = mccabe
            print(f"Percentage of passed tests: {test_results['test_pass_rate']}")
            print(f"Total time: {test_results['execution_time_sec']} seconds")
            if test_results is not None and test_results['line_coverage_percent'] is not None:
                print(f"Coverage: {test_results['line_coverage_percent']}%")
            else:
                print("Coverage information not available.")
            return test_results
        else:
            print("Failed to execute Go tests.")
            return {
                "syntax": syntax,
                "syntax_output": warnings,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "timeout": False,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "internal_error_occurred": True,
                "runtime_errors_count": None,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
            }

    finally:
        # Restore the original working directory
        os.chdir(original_cwd)
        # Clean up the temporary directory
        shutil.rmtree(temp_dir)
        print(f"Temporary directory {temp_dir} has been removed.")


def x_analyze_go_tests__mutmut_66(go_file_path, test_file_path):
    # Create a temporary directory within "/data/generated/golang/"
    base_temp_dir = "./data/generated/docs_golang/"
    os.makedirs(base_temp_dir, exist_ok=True)
    temp_dir = tempfile.mkdtemp(dir=base_temp_dir)

    # Save the current working directory
    original_cwd = os.getcwd()

    try:
        # Copy the Go file and the specified test file to the temporary directory
        new_go_file_path = shutil.copy(go_file_path, temp_dir)
        new_test_file_path = shutil.copy(test_file_path, temp_dir)

        # Change to temporary directory
        os.chdir(temp_dir)

        # Check syntax
        syntax_build = check_syntax(os.path.basename(test_file_path))
        syntax_vet, warnings = check_for_warnings(temp_dir)
        findings = run_golangci(os.path.basename(test_file_path))
        syntax = determine_syntax_result(syntax_build, syntax_vet)
        assertions_density = assertions_density_go(os.path.basename(test_file_path))
        mccabe = assertions_mccabe_ratio_go(os.path.basename(new_go_file_path), os.path.basename(new_test_file_path))

        if syntax != CompileStatus.OK:
            print("Syntax check failed.")
            return {
                "syntax": syntax,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "syntax_output": warnings,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "timeout": False,
                "internal_error_occurred": False,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
                "runtime_errors_count": None
            }

        # Run tests and get results
        test_results = run_tests(".")

        if test_results:
            test_results["warnings"] = findings
            test_results["warnings_count"] = len(findings) if findings is not None else None
            test_results["syntax"] = None
            test_results["syntax_output"] = warnings
            test_results["assertions_density"] = assertions_density
            test_results["assertions_mccabe_ratio"] = mccabe
            print(f"Percentage of passed tests: {test_results['test_pass_rate']}")
            print(f"Total time: {test_results['execution_time_sec']} seconds")
            if test_results is not None and test_results['line_coverage_percent'] is not None:
                print(f"Coverage: {test_results['line_coverage_percent']}%")
            else:
                print("Coverage information not available.")
            return test_results
        else:
            print("Failed to execute Go tests.")
            return {
                "syntax": syntax,
                "syntax_output": warnings,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "timeout": False,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "internal_error_occurred": True,
                "runtime_errors_count": None,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
            }

    finally:
        # Restore the original working directory
        os.chdir(original_cwd)
        # Clean up the temporary directory
        shutil.rmtree(temp_dir)
        print(f"Temporary directory {temp_dir} has been removed.")


def x_analyze_go_tests__mutmut_67(go_file_path, test_file_path):
    # Create a temporary directory within "/data/generated/golang/"
    base_temp_dir = "./data/generated/docs_golang/"
    os.makedirs(base_temp_dir, exist_ok=True)
    temp_dir = tempfile.mkdtemp(dir=base_temp_dir)

    # Save the current working directory
    original_cwd = os.getcwd()

    try:
        # Copy the Go file and the specified test file to the temporary directory
        new_go_file_path = shutil.copy(go_file_path, temp_dir)
        new_test_file_path = shutil.copy(test_file_path, temp_dir)

        # Change to temporary directory
        os.chdir(temp_dir)

        # Check syntax
        syntax_build = check_syntax(os.path.basename(test_file_path))
        syntax_vet, warnings = check_for_warnings(temp_dir)
        findings = run_golangci(os.path.basename(test_file_path))
        syntax = determine_syntax_result(syntax_build, syntax_vet)
        assertions_density = assertions_density_go(os.path.basename(test_file_path))
        mccabe = assertions_mccabe_ratio_go(os.path.basename(new_go_file_path), os.path.basename(new_test_file_path))

        if syntax != CompileStatus.OK:
            print("Syntax check failed.")
            return {
                "syntax": syntax,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "syntax_output": warnings,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "timeout": False,
                "internal_error_occurred": False,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
                "runtime_errors_count": None
            }

        # Run tests and get results
        test_results = run_tests(".")

        if test_results:
            test_results["warnings"] = findings
            test_results["warnings_count"] = len(findings) if findings is not None else None
            test_results["syntax"] = syntax
            test_results["XXsyntax_outputXX"] = warnings
            test_results["assertions_density"] = assertions_density
            test_results["assertions_mccabe_ratio"] = mccabe
            print(f"Percentage of passed tests: {test_results['test_pass_rate']}")
            print(f"Total time: {test_results['execution_time_sec']} seconds")
            if test_results is not None and test_results['line_coverage_percent'] is not None:
                print(f"Coverage: {test_results['line_coverage_percent']}%")
            else:
                print("Coverage information not available.")
            return test_results
        else:
            print("Failed to execute Go tests.")
            return {
                "syntax": syntax,
                "syntax_output": warnings,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "timeout": False,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "internal_error_occurred": True,
                "runtime_errors_count": None,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
            }

    finally:
        # Restore the original working directory
        os.chdir(original_cwd)
        # Clean up the temporary directory
        shutil.rmtree(temp_dir)
        print(f"Temporary directory {temp_dir} has been removed.")


def x_analyze_go_tests__mutmut_68(go_file_path, test_file_path):
    # Create a temporary directory within "/data/generated/golang/"
    base_temp_dir = "./data/generated/docs_golang/"
    os.makedirs(base_temp_dir, exist_ok=True)
    temp_dir = tempfile.mkdtemp(dir=base_temp_dir)

    # Save the current working directory
    original_cwd = os.getcwd()

    try:
        # Copy the Go file and the specified test file to the temporary directory
        new_go_file_path = shutil.copy(go_file_path, temp_dir)
        new_test_file_path = shutil.copy(test_file_path, temp_dir)

        # Change to temporary directory
        os.chdir(temp_dir)

        # Check syntax
        syntax_build = check_syntax(os.path.basename(test_file_path))
        syntax_vet, warnings = check_for_warnings(temp_dir)
        findings = run_golangci(os.path.basename(test_file_path))
        syntax = determine_syntax_result(syntax_build, syntax_vet)
        assertions_density = assertions_density_go(os.path.basename(test_file_path))
        mccabe = assertions_mccabe_ratio_go(os.path.basename(new_go_file_path), os.path.basename(new_test_file_path))

        if syntax != CompileStatus.OK:
            print("Syntax check failed.")
            return {
                "syntax": syntax,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "syntax_output": warnings,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "timeout": False,
                "internal_error_occurred": False,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
                "runtime_errors_count": None
            }

        # Run tests and get results
        test_results = run_tests(".")

        if test_results:
            test_results["warnings"] = findings
            test_results["warnings_count"] = len(findings) if findings is not None else None
            test_results["syntax"] = syntax
            test_results[None] = warnings
            test_results["assertions_density"] = assertions_density
            test_results["assertions_mccabe_ratio"] = mccabe
            print(f"Percentage of passed tests: {test_results['test_pass_rate']}")
            print(f"Total time: {test_results['execution_time_sec']} seconds")
            if test_results is not None and test_results['line_coverage_percent'] is not None:
                print(f"Coverage: {test_results['line_coverage_percent']}%")
            else:
                print("Coverage information not available.")
            return test_results
        else:
            print("Failed to execute Go tests.")
            return {
                "syntax": syntax,
                "syntax_output": warnings,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "timeout": False,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "internal_error_occurred": True,
                "runtime_errors_count": None,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
            }

    finally:
        # Restore the original working directory
        os.chdir(original_cwd)
        # Clean up the temporary directory
        shutil.rmtree(temp_dir)
        print(f"Temporary directory {temp_dir} has been removed.")


def x_analyze_go_tests__mutmut_69(go_file_path, test_file_path):
    # Create a temporary directory within "/data/generated/golang/"
    base_temp_dir = "./data/generated/docs_golang/"
    os.makedirs(base_temp_dir, exist_ok=True)
    temp_dir = tempfile.mkdtemp(dir=base_temp_dir)

    # Save the current working directory
    original_cwd = os.getcwd()

    try:
        # Copy the Go file and the specified test file to the temporary directory
        new_go_file_path = shutil.copy(go_file_path, temp_dir)
        new_test_file_path = shutil.copy(test_file_path, temp_dir)

        # Change to temporary directory
        os.chdir(temp_dir)

        # Check syntax
        syntax_build = check_syntax(os.path.basename(test_file_path))
        syntax_vet, warnings = check_for_warnings(temp_dir)
        findings = run_golangci(os.path.basename(test_file_path))
        syntax = determine_syntax_result(syntax_build, syntax_vet)
        assertions_density = assertions_density_go(os.path.basename(test_file_path))
        mccabe = assertions_mccabe_ratio_go(os.path.basename(new_go_file_path), os.path.basename(new_test_file_path))

        if syntax != CompileStatus.OK:
            print("Syntax check failed.")
            return {
                "syntax": syntax,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "syntax_output": warnings,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "timeout": False,
                "internal_error_occurred": False,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
                "runtime_errors_count": None
            }

        # Run tests and get results
        test_results = run_tests(".")

        if test_results:
            test_results["warnings"] = findings
            test_results["warnings_count"] = len(findings) if findings is not None else None
            test_results["syntax"] = syntax
            test_results["syntax_output"] = None
            test_results["assertions_density"] = assertions_density
            test_results["assertions_mccabe_ratio"] = mccabe
            print(f"Percentage of passed tests: {test_results['test_pass_rate']}")
            print(f"Total time: {test_results['execution_time_sec']} seconds")
            if test_results is not None and test_results['line_coverage_percent'] is not None:
                print(f"Coverage: {test_results['line_coverage_percent']}%")
            else:
                print("Coverage information not available.")
            return test_results
        else:
            print("Failed to execute Go tests.")
            return {
                "syntax": syntax,
                "syntax_output": warnings,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "timeout": False,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "internal_error_occurred": True,
                "runtime_errors_count": None,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
            }

    finally:
        # Restore the original working directory
        os.chdir(original_cwd)
        # Clean up the temporary directory
        shutil.rmtree(temp_dir)
        print(f"Temporary directory {temp_dir} has been removed.")


def x_analyze_go_tests__mutmut_70(go_file_path, test_file_path):
    # Create a temporary directory within "/data/generated/golang/"
    base_temp_dir = "./data/generated/docs_golang/"
    os.makedirs(base_temp_dir, exist_ok=True)
    temp_dir = tempfile.mkdtemp(dir=base_temp_dir)

    # Save the current working directory
    original_cwd = os.getcwd()

    try:
        # Copy the Go file and the specified test file to the temporary directory
        new_go_file_path = shutil.copy(go_file_path, temp_dir)
        new_test_file_path = shutil.copy(test_file_path, temp_dir)

        # Change to temporary directory
        os.chdir(temp_dir)

        # Check syntax
        syntax_build = check_syntax(os.path.basename(test_file_path))
        syntax_vet, warnings = check_for_warnings(temp_dir)
        findings = run_golangci(os.path.basename(test_file_path))
        syntax = determine_syntax_result(syntax_build, syntax_vet)
        assertions_density = assertions_density_go(os.path.basename(test_file_path))
        mccabe = assertions_mccabe_ratio_go(os.path.basename(new_go_file_path), os.path.basename(new_test_file_path))

        if syntax != CompileStatus.OK:
            print("Syntax check failed.")
            return {
                "syntax": syntax,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "syntax_output": warnings,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "timeout": False,
                "internal_error_occurred": False,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
                "runtime_errors_count": None
            }

        # Run tests and get results
        test_results = run_tests(".")

        if test_results:
            test_results["warnings"] = findings
            test_results["warnings_count"] = len(findings) if findings is not None else None
            test_results["syntax"] = syntax
            test_results["syntax_output"] = warnings
            test_results["XXassertions_densityXX"] = assertions_density
            test_results["assertions_mccabe_ratio"] = mccabe
            print(f"Percentage of passed tests: {test_results['test_pass_rate']}")
            print(f"Total time: {test_results['execution_time_sec']} seconds")
            if test_results is not None and test_results['line_coverage_percent'] is not None:
                print(f"Coverage: {test_results['line_coverage_percent']}%")
            else:
                print("Coverage information not available.")
            return test_results
        else:
            print("Failed to execute Go tests.")
            return {
                "syntax": syntax,
                "syntax_output": warnings,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "timeout": False,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "internal_error_occurred": True,
                "runtime_errors_count": None,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
            }

    finally:
        # Restore the original working directory
        os.chdir(original_cwd)
        # Clean up the temporary directory
        shutil.rmtree(temp_dir)
        print(f"Temporary directory {temp_dir} has been removed.")


def x_analyze_go_tests__mutmut_71(go_file_path, test_file_path):
    # Create a temporary directory within "/data/generated/golang/"
    base_temp_dir = "./data/generated/docs_golang/"
    os.makedirs(base_temp_dir, exist_ok=True)
    temp_dir = tempfile.mkdtemp(dir=base_temp_dir)

    # Save the current working directory
    original_cwd = os.getcwd()

    try:
        # Copy the Go file and the specified test file to the temporary directory
        new_go_file_path = shutil.copy(go_file_path, temp_dir)
        new_test_file_path = shutil.copy(test_file_path, temp_dir)

        # Change to temporary directory
        os.chdir(temp_dir)

        # Check syntax
        syntax_build = check_syntax(os.path.basename(test_file_path))
        syntax_vet, warnings = check_for_warnings(temp_dir)
        findings = run_golangci(os.path.basename(test_file_path))
        syntax = determine_syntax_result(syntax_build, syntax_vet)
        assertions_density = assertions_density_go(os.path.basename(test_file_path))
        mccabe = assertions_mccabe_ratio_go(os.path.basename(new_go_file_path), os.path.basename(new_test_file_path))

        if syntax != CompileStatus.OK:
            print("Syntax check failed.")
            return {
                "syntax": syntax,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "syntax_output": warnings,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "timeout": False,
                "internal_error_occurred": False,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
                "runtime_errors_count": None
            }

        # Run tests and get results
        test_results = run_tests(".")

        if test_results:
            test_results["warnings"] = findings
            test_results["warnings_count"] = len(findings) if findings is not None else None
            test_results["syntax"] = syntax
            test_results["syntax_output"] = warnings
            test_results[None] = assertions_density
            test_results["assertions_mccabe_ratio"] = mccabe
            print(f"Percentage of passed tests: {test_results['test_pass_rate']}")
            print(f"Total time: {test_results['execution_time_sec']} seconds")
            if test_results is not None and test_results['line_coverage_percent'] is not None:
                print(f"Coverage: {test_results['line_coverage_percent']}%")
            else:
                print("Coverage information not available.")
            return test_results
        else:
            print("Failed to execute Go tests.")
            return {
                "syntax": syntax,
                "syntax_output": warnings,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "timeout": False,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "internal_error_occurred": True,
                "runtime_errors_count": None,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
            }

    finally:
        # Restore the original working directory
        os.chdir(original_cwd)
        # Clean up the temporary directory
        shutil.rmtree(temp_dir)
        print(f"Temporary directory {temp_dir} has been removed.")


def x_analyze_go_tests__mutmut_72(go_file_path, test_file_path):
    # Create a temporary directory within "/data/generated/golang/"
    base_temp_dir = "./data/generated/docs_golang/"
    os.makedirs(base_temp_dir, exist_ok=True)
    temp_dir = tempfile.mkdtemp(dir=base_temp_dir)

    # Save the current working directory
    original_cwd = os.getcwd()

    try:
        # Copy the Go file and the specified test file to the temporary directory
        new_go_file_path = shutil.copy(go_file_path, temp_dir)
        new_test_file_path = shutil.copy(test_file_path, temp_dir)

        # Change to temporary directory
        os.chdir(temp_dir)

        # Check syntax
        syntax_build = check_syntax(os.path.basename(test_file_path))
        syntax_vet, warnings = check_for_warnings(temp_dir)
        findings = run_golangci(os.path.basename(test_file_path))
        syntax = determine_syntax_result(syntax_build, syntax_vet)
        assertions_density = assertions_density_go(os.path.basename(test_file_path))
        mccabe = assertions_mccabe_ratio_go(os.path.basename(new_go_file_path), os.path.basename(new_test_file_path))

        if syntax != CompileStatus.OK:
            print("Syntax check failed.")
            return {
                "syntax": syntax,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "syntax_output": warnings,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "timeout": False,
                "internal_error_occurred": False,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
                "runtime_errors_count": None
            }

        # Run tests and get results
        test_results = run_tests(".")

        if test_results:
            test_results["warnings"] = findings
            test_results["warnings_count"] = len(findings) if findings is not None else None
            test_results["syntax"] = syntax
            test_results["syntax_output"] = warnings
            test_results["assertions_density"] = None
            test_results["assertions_mccabe_ratio"] = mccabe
            print(f"Percentage of passed tests: {test_results['test_pass_rate']}")
            print(f"Total time: {test_results['execution_time_sec']} seconds")
            if test_results is not None and test_results['line_coverage_percent'] is not None:
                print(f"Coverage: {test_results['line_coverage_percent']}%")
            else:
                print("Coverage information not available.")
            return test_results
        else:
            print("Failed to execute Go tests.")
            return {
                "syntax": syntax,
                "syntax_output": warnings,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "timeout": False,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "internal_error_occurred": True,
                "runtime_errors_count": None,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
            }

    finally:
        # Restore the original working directory
        os.chdir(original_cwd)
        # Clean up the temporary directory
        shutil.rmtree(temp_dir)
        print(f"Temporary directory {temp_dir} has been removed.")


def x_analyze_go_tests__mutmut_73(go_file_path, test_file_path):
    # Create a temporary directory within "/data/generated/golang/"
    base_temp_dir = "./data/generated/docs_golang/"
    os.makedirs(base_temp_dir, exist_ok=True)
    temp_dir = tempfile.mkdtemp(dir=base_temp_dir)

    # Save the current working directory
    original_cwd = os.getcwd()

    try:
        # Copy the Go file and the specified test file to the temporary directory
        new_go_file_path = shutil.copy(go_file_path, temp_dir)
        new_test_file_path = shutil.copy(test_file_path, temp_dir)

        # Change to temporary directory
        os.chdir(temp_dir)

        # Check syntax
        syntax_build = check_syntax(os.path.basename(test_file_path))
        syntax_vet, warnings = check_for_warnings(temp_dir)
        findings = run_golangci(os.path.basename(test_file_path))
        syntax = determine_syntax_result(syntax_build, syntax_vet)
        assertions_density = assertions_density_go(os.path.basename(test_file_path))
        mccabe = assertions_mccabe_ratio_go(os.path.basename(new_go_file_path), os.path.basename(new_test_file_path))

        if syntax != CompileStatus.OK:
            print("Syntax check failed.")
            return {
                "syntax": syntax,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "syntax_output": warnings,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "timeout": False,
                "internal_error_occurred": False,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
                "runtime_errors_count": None
            }

        # Run tests and get results
        test_results = run_tests(".")

        if test_results:
            test_results["warnings"] = findings
            test_results["warnings_count"] = len(findings) if findings is not None else None
            test_results["syntax"] = syntax
            test_results["syntax_output"] = warnings
            test_results["assertions_density"] = assertions_density
            test_results["XXassertions_mccabe_ratioXX"] = mccabe
            print(f"Percentage of passed tests: {test_results['test_pass_rate']}")
            print(f"Total time: {test_results['execution_time_sec']} seconds")
            if test_results is not None and test_results['line_coverage_percent'] is not None:
                print(f"Coverage: {test_results['line_coverage_percent']}%")
            else:
                print("Coverage information not available.")
            return test_results
        else:
            print("Failed to execute Go tests.")
            return {
                "syntax": syntax,
                "syntax_output": warnings,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "timeout": False,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "internal_error_occurred": True,
                "runtime_errors_count": None,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
            }

    finally:
        # Restore the original working directory
        os.chdir(original_cwd)
        # Clean up the temporary directory
        shutil.rmtree(temp_dir)
        print(f"Temporary directory {temp_dir} has been removed.")


def x_analyze_go_tests__mutmut_74(go_file_path, test_file_path):
    # Create a temporary directory within "/data/generated/golang/"
    base_temp_dir = "./data/generated/docs_golang/"
    os.makedirs(base_temp_dir, exist_ok=True)
    temp_dir = tempfile.mkdtemp(dir=base_temp_dir)

    # Save the current working directory
    original_cwd = os.getcwd()

    try:
        # Copy the Go file and the specified test file to the temporary directory
        new_go_file_path = shutil.copy(go_file_path, temp_dir)
        new_test_file_path = shutil.copy(test_file_path, temp_dir)

        # Change to temporary directory
        os.chdir(temp_dir)

        # Check syntax
        syntax_build = check_syntax(os.path.basename(test_file_path))
        syntax_vet, warnings = check_for_warnings(temp_dir)
        findings = run_golangci(os.path.basename(test_file_path))
        syntax = determine_syntax_result(syntax_build, syntax_vet)
        assertions_density = assertions_density_go(os.path.basename(test_file_path))
        mccabe = assertions_mccabe_ratio_go(os.path.basename(new_go_file_path), os.path.basename(new_test_file_path))

        if syntax != CompileStatus.OK:
            print("Syntax check failed.")
            return {
                "syntax": syntax,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "syntax_output": warnings,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "timeout": False,
                "internal_error_occurred": False,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
                "runtime_errors_count": None
            }

        # Run tests and get results
        test_results = run_tests(".")

        if test_results:
            test_results["warnings"] = findings
            test_results["warnings_count"] = len(findings) if findings is not None else None
            test_results["syntax"] = syntax
            test_results["syntax_output"] = warnings
            test_results["assertions_density"] = assertions_density
            test_results[None] = mccabe
            print(f"Percentage of passed tests: {test_results['test_pass_rate']}")
            print(f"Total time: {test_results['execution_time_sec']} seconds")
            if test_results is not None and test_results['line_coverage_percent'] is not None:
                print(f"Coverage: {test_results['line_coverage_percent']}%")
            else:
                print("Coverage information not available.")
            return test_results
        else:
            print("Failed to execute Go tests.")
            return {
                "syntax": syntax,
                "syntax_output": warnings,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "timeout": False,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "internal_error_occurred": True,
                "runtime_errors_count": None,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
            }

    finally:
        # Restore the original working directory
        os.chdir(original_cwd)
        # Clean up the temporary directory
        shutil.rmtree(temp_dir)
        print(f"Temporary directory {temp_dir} has been removed.")


def x_analyze_go_tests__mutmut_75(go_file_path, test_file_path):
    # Create a temporary directory within "/data/generated/golang/"
    base_temp_dir = "./data/generated/docs_golang/"
    os.makedirs(base_temp_dir, exist_ok=True)
    temp_dir = tempfile.mkdtemp(dir=base_temp_dir)

    # Save the current working directory
    original_cwd = os.getcwd()

    try:
        # Copy the Go file and the specified test file to the temporary directory
        new_go_file_path = shutil.copy(go_file_path, temp_dir)
        new_test_file_path = shutil.copy(test_file_path, temp_dir)

        # Change to temporary directory
        os.chdir(temp_dir)

        # Check syntax
        syntax_build = check_syntax(os.path.basename(test_file_path))
        syntax_vet, warnings = check_for_warnings(temp_dir)
        findings = run_golangci(os.path.basename(test_file_path))
        syntax = determine_syntax_result(syntax_build, syntax_vet)
        assertions_density = assertions_density_go(os.path.basename(test_file_path))
        mccabe = assertions_mccabe_ratio_go(os.path.basename(new_go_file_path), os.path.basename(new_test_file_path))

        if syntax != CompileStatus.OK:
            print("Syntax check failed.")
            return {
                "syntax": syntax,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "syntax_output": warnings,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "timeout": False,
                "internal_error_occurred": False,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
                "runtime_errors_count": None
            }

        # Run tests and get results
        test_results = run_tests(".")

        if test_results:
            test_results["warnings"] = findings
            test_results["warnings_count"] = len(findings) if findings is not None else None
            test_results["syntax"] = syntax
            test_results["syntax_output"] = warnings
            test_results["assertions_density"] = assertions_density
            test_results["assertions_mccabe_ratio"] = None
            print(f"Percentage of passed tests: {test_results['test_pass_rate']}")
            print(f"Total time: {test_results['execution_time_sec']} seconds")
            if test_results is not None and test_results['line_coverage_percent'] is not None:
                print(f"Coverage: {test_results['line_coverage_percent']}%")
            else:
                print("Coverage information not available.")
            return test_results
        else:
            print("Failed to execute Go tests.")
            return {
                "syntax": syntax,
                "syntax_output": warnings,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "timeout": False,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "internal_error_occurred": True,
                "runtime_errors_count": None,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
            }

    finally:
        # Restore the original working directory
        os.chdir(original_cwd)
        # Clean up the temporary directory
        shutil.rmtree(temp_dir)
        print(f"Temporary directory {temp_dir} has been removed.")


def x_analyze_go_tests__mutmut_76(go_file_path, test_file_path):
    # Create a temporary directory within "/data/generated/golang/"
    base_temp_dir = "./data/generated/docs_golang/"
    os.makedirs(base_temp_dir, exist_ok=True)
    temp_dir = tempfile.mkdtemp(dir=base_temp_dir)

    # Save the current working directory
    original_cwd = os.getcwd()

    try:
        # Copy the Go file and the specified test file to the temporary directory
        new_go_file_path = shutil.copy(go_file_path, temp_dir)
        new_test_file_path = shutil.copy(test_file_path, temp_dir)

        # Change to temporary directory
        os.chdir(temp_dir)

        # Check syntax
        syntax_build = check_syntax(os.path.basename(test_file_path))
        syntax_vet, warnings = check_for_warnings(temp_dir)
        findings = run_golangci(os.path.basename(test_file_path))
        syntax = determine_syntax_result(syntax_build, syntax_vet)
        assertions_density = assertions_density_go(os.path.basename(test_file_path))
        mccabe = assertions_mccabe_ratio_go(os.path.basename(new_go_file_path), os.path.basename(new_test_file_path))

        if syntax != CompileStatus.OK:
            print("Syntax check failed.")
            return {
                "syntax": syntax,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "syntax_output": warnings,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "timeout": False,
                "internal_error_occurred": False,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
                "runtime_errors_count": None
            }

        # Run tests and get results
        test_results = run_tests(".")

        if test_results:
            test_results["warnings"] = findings
            test_results["warnings_count"] = len(findings) if findings is not None else None
            test_results["syntax"] = syntax
            test_results["syntax_output"] = warnings
            test_results["assertions_density"] = assertions_density
            test_results["assertions_mccabe_ratio"] = mccabe
            print(f"Percentage of passed tests: {test_results['XXtest_pass_rateXX']}")
            print(f"Total time: {test_results['execution_time_sec']} seconds")
            if test_results is not None and test_results['line_coverage_percent'] is not None:
                print(f"Coverage: {test_results['line_coverage_percent']}%")
            else:
                print("Coverage information not available.")
            return test_results
        else:
            print("Failed to execute Go tests.")
            return {
                "syntax": syntax,
                "syntax_output": warnings,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "timeout": False,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "internal_error_occurred": True,
                "runtime_errors_count": None,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
            }

    finally:
        # Restore the original working directory
        os.chdir(original_cwd)
        # Clean up the temporary directory
        shutil.rmtree(temp_dir)
        print(f"Temporary directory {temp_dir} has been removed.")


def x_analyze_go_tests__mutmut_77(go_file_path, test_file_path):
    # Create a temporary directory within "/data/generated/golang/"
    base_temp_dir = "./data/generated/docs_golang/"
    os.makedirs(base_temp_dir, exist_ok=True)
    temp_dir = tempfile.mkdtemp(dir=base_temp_dir)

    # Save the current working directory
    original_cwd = os.getcwd()

    try:
        # Copy the Go file and the specified test file to the temporary directory
        new_go_file_path = shutil.copy(go_file_path, temp_dir)
        new_test_file_path = shutil.copy(test_file_path, temp_dir)

        # Change to temporary directory
        os.chdir(temp_dir)

        # Check syntax
        syntax_build = check_syntax(os.path.basename(test_file_path))
        syntax_vet, warnings = check_for_warnings(temp_dir)
        findings = run_golangci(os.path.basename(test_file_path))
        syntax = determine_syntax_result(syntax_build, syntax_vet)
        assertions_density = assertions_density_go(os.path.basename(test_file_path))
        mccabe = assertions_mccabe_ratio_go(os.path.basename(new_go_file_path), os.path.basename(new_test_file_path))

        if syntax != CompileStatus.OK:
            print("Syntax check failed.")
            return {
                "syntax": syntax,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "syntax_output": warnings,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "timeout": False,
                "internal_error_occurred": False,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
                "runtime_errors_count": None
            }

        # Run tests and get results
        test_results = run_tests(".")

        if test_results:
            test_results["warnings"] = findings
            test_results["warnings_count"] = len(findings) if findings is not None else None
            test_results["syntax"] = syntax
            test_results["syntax_output"] = warnings
            test_results["assertions_density"] = assertions_density
            test_results["assertions_mccabe_ratio"] = mccabe
            print(f"Percentage of passed tests: {test_results[None]}")
            print(f"Total time: {test_results['execution_time_sec']} seconds")
            if test_results is not None and test_results['line_coverage_percent'] is not None:
                print(f"Coverage: {test_results['line_coverage_percent']}%")
            else:
                print("Coverage information not available.")
            return test_results
        else:
            print("Failed to execute Go tests.")
            return {
                "syntax": syntax,
                "syntax_output": warnings,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "timeout": False,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "internal_error_occurred": True,
                "runtime_errors_count": None,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
            }

    finally:
        # Restore the original working directory
        os.chdir(original_cwd)
        # Clean up the temporary directory
        shutil.rmtree(temp_dir)
        print(f"Temporary directory {temp_dir} has been removed.")


def x_analyze_go_tests__mutmut_78(go_file_path, test_file_path):
    # Create a temporary directory within "/data/generated/golang/"
    base_temp_dir = "./data/generated/docs_golang/"
    os.makedirs(base_temp_dir, exist_ok=True)
    temp_dir = tempfile.mkdtemp(dir=base_temp_dir)

    # Save the current working directory
    original_cwd = os.getcwd()

    try:
        # Copy the Go file and the specified test file to the temporary directory
        new_go_file_path = shutil.copy(go_file_path, temp_dir)
        new_test_file_path = shutil.copy(test_file_path, temp_dir)

        # Change to temporary directory
        os.chdir(temp_dir)

        # Check syntax
        syntax_build = check_syntax(os.path.basename(test_file_path))
        syntax_vet, warnings = check_for_warnings(temp_dir)
        findings = run_golangci(os.path.basename(test_file_path))
        syntax = determine_syntax_result(syntax_build, syntax_vet)
        assertions_density = assertions_density_go(os.path.basename(test_file_path))
        mccabe = assertions_mccabe_ratio_go(os.path.basename(new_go_file_path), os.path.basename(new_test_file_path))

        if syntax != CompileStatus.OK:
            print("Syntax check failed.")
            return {
                "syntax": syntax,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "syntax_output": warnings,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "timeout": False,
                "internal_error_occurred": False,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
                "runtime_errors_count": None
            }

        # Run tests and get results
        test_results = run_tests(".")

        if test_results:
            test_results["warnings"] = findings
            test_results["warnings_count"] = len(findings) if findings is not None else None
            test_results["syntax"] = syntax
            test_results["syntax_output"] = warnings
            test_results["assertions_density"] = assertions_density
            test_results["assertions_mccabe_ratio"] = mccabe
            print(f"Percentage of passed tests: {test_results['test_pass_rate']}")
            print(f"Total time: {test_results['XXexecution_time_secXX']} seconds")
            if test_results is not None and test_results['line_coverage_percent'] is not None:
                print(f"Coverage: {test_results['line_coverage_percent']}%")
            else:
                print("Coverage information not available.")
            return test_results
        else:
            print("Failed to execute Go tests.")
            return {
                "syntax": syntax,
                "syntax_output": warnings,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "timeout": False,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "internal_error_occurred": True,
                "runtime_errors_count": None,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
            }

    finally:
        # Restore the original working directory
        os.chdir(original_cwd)
        # Clean up the temporary directory
        shutil.rmtree(temp_dir)
        print(f"Temporary directory {temp_dir} has been removed.")


def x_analyze_go_tests__mutmut_79(go_file_path, test_file_path):
    # Create a temporary directory within "/data/generated/golang/"
    base_temp_dir = "./data/generated/docs_golang/"
    os.makedirs(base_temp_dir, exist_ok=True)
    temp_dir = tempfile.mkdtemp(dir=base_temp_dir)

    # Save the current working directory
    original_cwd = os.getcwd()

    try:
        # Copy the Go file and the specified test file to the temporary directory
        new_go_file_path = shutil.copy(go_file_path, temp_dir)
        new_test_file_path = shutil.copy(test_file_path, temp_dir)

        # Change to temporary directory
        os.chdir(temp_dir)

        # Check syntax
        syntax_build = check_syntax(os.path.basename(test_file_path))
        syntax_vet, warnings = check_for_warnings(temp_dir)
        findings = run_golangci(os.path.basename(test_file_path))
        syntax = determine_syntax_result(syntax_build, syntax_vet)
        assertions_density = assertions_density_go(os.path.basename(test_file_path))
        mccabe = assertions_mccabe_ratio_go(os.path.basename(new_go_file_path), os.path.basename(new_test_file_path))

        if syntax != CompileStatus.OK:
            print("Syntax check failed.")
            return {
                "syntax": syntax,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "syntax_output": warnings,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "timeout": False,
                "internal_error_occurred": False,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
                "runtime_errors_count": None
            }

        # Run tests and get results
        test_results = run_tests(".")

        if test_results:
            test_results["warnings"] = findings
            test_results["warnings_count"] = len(findings) if findings is not None else None
            test_results["syntax"] = syntax
            test_results["syntax_output"] = warnings
            test_results["assertions_density"] = assertions_density
            test_results["assertions_mccabe_ratio"] = mccabe
            print(f"Percentage of passed tests: {test_results['test_pass_rate']}")
            print(f"Total time: {test_results[None]} seconds")
            if test_results is not None and test_results['line_coverage_percent'] is not None:
                print(f"Coverage: {test_results['line_coverage_percent']}%")
            else:
                print("Coverage information not available.")
            return test_results
        else:
            print("Failed to execute Go tests.")
            return {
                "syntax": syntax,
                "syntax_output": warnings,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "timeout": False,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "internal_error_occurred": True,
                "runtime_errors_count": None,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
            }

    finally:
        # Restore the original working directory
        os.chdir(original_cwd)
        # Clean up the temporary directory
        shutil.rmtree(temp_dir)
        print(f"Temporary directory {temp_dir} has been removed.")


def x_analyze_go_tests__mutmut_80(go_file_path, test_file_path):
    # Create a temporary directory within "/data/generated/golang/"
    base_temp_dir = "./data/generated/docs_golang/"
    os.makedirs(base_temp_dir, exist_ok=True)
    temp_dir = tempfile.mkdtemp(dir=base_temp_dir)

    # Save the current working directory
    original_cwd = os.getcwd()

    try:
        # Copy the Go file and the specified test file to the temporary directory
        new_go_file_path = shutil.copy(go_file_path, temp_dir)
        new_test_file_path = shutil.copy(test_file_path, temp_dir)

        # Change to temporary directory
        os.chdir(temp_dir)

        # Check syntax
        syntax_build = check_syntax(os.path.basename(test_file_path))
        syntax_vet, warnings = check_for_warnings(temp_dir)
        findings = run_golangci(os.path.basename(test_file_path))
        syntax = determine_syntax_result(syntax_build, syntax_vet)
        assertions_density = assertions_density_go(os.path.basename(test_file_path))
        mccabe = assertions_mccabe_ratio_go(os.path.basename(new_go_file_path), os.path.basename(new_test_file_path))

        if syntax != CompileStatus.OK:
            print("Syntax check failed.")
            return {
                "syntax": syntax,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "syntax_output": warnings,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "timeout": False,
                "internal_error_occurred": False,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
                "runtime_errors_count": None
            }

        # Run tests and get results
        test_results = run_tests(".")

        if test_results:
            test_results["warnings"] = findings
            test_results["warnings_count"] = len(findings) if findings is not None else None
            test_results["syntax"] = syntax
            test_results["syntax_output"] = warnings
            test_results["assertions_density"] = assertions_density
            test_results["assertions_mccabe_ratio"] = mccabe
            print(f"Percentage of passed tests: {test_results['test_pass_rate']}")
            print(f"Total time: {test_results['execution_time_sec']} seconds")
            if test_results is  None and test_results['line_coverage_percent'] is not None:
                print(f"Coverage: {test_results['line_coverage_percent']}%")
            else:
                print("Coverage information not available.")
            return test_results
        else:
            print("Failed to execute Go tests.")
            return {
                "syntax": syntax,
                "syntax_output": warnings,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "timeout": False,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "internal_error_occurred": True,
                "runtime_errors_count": None,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
            }

    finally:
        # Restore the original working directory
        os.chdir(original_cwd)
        # Clean up the temporary directory
        shutil.rmtree(temp_dir)
        print(f"Temporary directory {temp_dir} has been removed.")


def x_analyze_go_tests__mutmut_81(go_file_path, test_file_path):
    # Create a temporary directory within "/data/generated/golang/"
    base_temp_dir = "./data/generated/docs_golang/"
    os.makedirs(base_temp_dir, exist_ok=True)
    temp_dir = tempfile.mkdtemp(dir=base_temp_dir)

    # Save the current working directory
    original_cwd = os.getcwd()

    try:
        # Copy the Go file and the specified test file to the temporary directory
        new_go_file_path = shutil.copy(go_file_path, temp_dir)
        new_test_file_path = shutil.copy(test_file_path, temp_dir)

        # Change to temporary directory
        os.chdir(temp_dir)

        # Check syntax
        syntax_build = check_syntax(os.path.basename(test_file_path))
        syntax_vet, warnings = check_for_warnings(temp_dir)
        findings = run_golangci(os.path.basename(test_file_path))
        syntax = determine_syntax_result(syntax_build, syntax_vet)
        assertions_density = assertions_density_go(os.path.basename(test_file_path))
        mccabe = assertions_mccabe_ratio_go(os.path.basename(new_go_file_path), os.path.basename(new_test_file_path))

        if syntax != CompileStatus.OK:
            print("Syntax check failed.")
            return {
                "syntax": syntax,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "syntax_output": warnings,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "timeout": False,
                "internal_error_occurred": False,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
                "runtime_errors_count": None
            }

        # Run tests and get results
        test_results = run_tests(".")

        if test_results:
            test_results["warnings"] = findings
            test_results["warnings_count"] = len(findings) if findings is not None else None
            test_results["syntax"] = syntax
            test_results["syntax_output"] = warnings
            test_results["assertions_density"] = assertions_density
            test_results["assertions_mccabe_ratio"] = mccabe
            print(f"Percentage of passed tests: {test_results['test_pass_rate']}")
            print(f"Total time: {test_results['execution_time_sec']} seconds")
            if test_results is not None and test_results['XXline_coverage_percentXX'] is not None:
                print(f"Coverage: {test_results['line_coverage_percent']}%")
            else:
                print("Coverage information not available.")
            return test_results
        else:
            print("Failed to execute Go tests.")
            return {
                "syntax": syntax,
                "syntax_output": warnings,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "timeout": False,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "internal_error_occurred": True,
                "runtime_errors_count": None,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
            }

    finally:
        # Restore the original working directory
        os.chdir(original_cwd)
        # Clean up the temporary directory
        shutil.rmtree(temp_dir)
        print(f"Temporary directory {temp_dir} has been removed.")


def x_analyze_go_tests__mutmut_82(go_file_path, test_file_path):
    # Create a temporary directory within "/data/generated/golang/"
    base_temp_dir = "./data/generated/docs_golang/"
    os.makedirs(base_temp_dir, exist_ok=True)
    temp_dir = tempfile.mkdtemp(dir=base_temp_dir)

    # Save the current working directory
    original_cwd = os.getcwd()

    try:
        # Copy the Go file and the specified test file to the temporary directory
        new_go_file_path = shutil.copy(go_file_path, temp_dir)
        new_test_file_path = shutil.copy(test_file_path, temp_dir)

        # Change to temporary directory
        os.chdir(temp_dir)

        # Check syntax
        syntax_build = check_syntax(os.path.basename(test_file_path))
        syntax_vet, warnings = check_for_warnings(temp_dir)
        findings = run_golangci(os.path.basename(test_file_path))
        syntax = determine_syntax_result(syntax_build, syntax_vet)
        assertions_density = assertions_density_go(os.path.basename(test_file_path))
        mccabe = assertions_mccabe_ratio_go(os.path.basename(new_go_file_path), os.path.basename(new_test_file_path))

        if syntax != CompileStatus.OK:
            print("Syntax check failed.")
            return {
                "syntax": syntax,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "syntax_output": warnings,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "timeout": False,
                "internal_error_occurred": False,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
                "runtime_errors_count": None
            }

        # Run tests and get results
        test_results = run_tests(".")

        if test_results:
            test_results["warnings"] = findings
            test_results["warnings_count"] = len(findings) if findings is not None else None
            test_results["syntax"] = syntax
            test_results["syntax_output"] = warnings
            test_results["assertions_density"] = assertions_density
            test_results["assertions_mccabe_ratio"] = mccabe
            print(f"Percentage of passed tests: {test_results['test_pass_rate']}")
            print(f"Total time: {test_results['execution_time_sec']} seconds")
            if test_results is not None and test_results[None] is not None:
                print(f"Coverage: {test_results['line_coverage_percent']}%")
            else:
                print("Coverage information not available.")
            return test_results
        else:
            print("Failed to execute Go tests.")
            return {
                "syntax": syntax,
                "syntax_output": warnings,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "timeout": False,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "internal_error_occurred": True,
                "runtime_errors_count": None,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
            }

    finally:
        # Restore the original working directory
        os.chdir(original_cwd)
        # Clean up the temporary directory
        shutil.rmtree(temp_dir)
        print(f"Temporary directory {temp_dir} has been removed.")


def x_analyze_go_tests__mutmut_83(go_file_path, test_file_path):
    # Create a temporary directory within "/data/generated/golang/"
    base_temp_dir = "./data/generated/docs_golang/"
    os.makedirs(base_temp_dir, exist_ok=True)
    temp_dir = tempfile.mkdtemp(dir=base_temp_dir)

    # Save the current working directory
    original_cwd = os.getcwd()

    try:
        # Copy the Go file and the specified test file to the temporary directory
        new_go_file_path = shutil.copy(go_file_path, temp_dir)
        new_test_file_path = shutil.copy(test_file_path, temp_dir)

        # Change to temporary directory
        os.chdir(temp_dir)

        # Check syntax
        syntax_build = check_syntax(os.path.basename(test_file_path))
        syntax_vet, warnings = check_for_warnings(temp_dir)
        findings = run_golangci(os.path.basename(test_file_path))
        syntax = determine_syntax_result(syntax_build, syntax_vet)
        assertions_density = assertions_density_go(os.path.basename(test_file_path))
        mccabe = assertions_mccabe_ratio_go(os.path.basename(new_go_file_path), os.path.basename(new_test_file_path))

        if syntax != CompileStatus.OK:
            print("Syntax check failed.")
            return {
                "syntax": syntax,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "syntax_output": warnings,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "timeout": False,
                "internal_error_occurred": False,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
                "runtime_errors_count": None
            }

        # Run tests and get results
        test_results = run_tests(".")

        if test_results:
            test_results["warnings"] = findings
            test_results["warnings_count"] = len(findings) if findings is not None else None
            test_results["syntax"] = syntax
            test_results["syntax_output"] = warnings
            test_results["assertions_density"] = assertions_density
            test_results["assertions_mccabe_ratio"] = mccabe
            print(f"Percentage of passed tests: {test_results['test_pass_rate']}")
            print(f"Total time: {test_results['execution_time_sec']} seconds")
            if test_results is not None and test_results['line_coverage_percent'] is  None:
                print(f"Coverage: {test_results['line_coverage_percent']}%")
            else:
                print("Coverage information not available.")
            return test_results
        else:
            print("Failed to execute Go tests.")
            return {
                "syntax": syntax,
                "syntax_output": warnings,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "timeout": False,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "internal_error_occurred": True,
                "runtime_errors_count": None,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
            }

    finally:
        # Restore the original working directory
        os.chdir(original_cwd)
        # Clean up the temporary directory
        shutil.rmtree(temp_dir)
        print(f"Temporary directory {temp_dir} has been removed.")


def x_analyze_go_tests__mutmut_84(go_file_path, test_file_path):
    # Create a temporary directory within "/data/generated/golang/"
    base_temp_dir = "./data/generated/docs_golang/"
    os.makedirs(base_temp_dir, exist_ok=True)
    temp_dir = tempfile.mkdtemp(dir=base_temp_dir)

    # Save the current working directory
    original_cwd = os.getcwd()

    try:
        # Copy the Go file and the specified test file to the temporary directory
        new_go_file_path = shutil.copy(go_file_path, temp_dir)
        new_test_file_path = shutil.copy(test_file_path, temp_dir)

        # Change to temporary directory
        os.chdir(temp_dir)

        # Check syntax
        syntax_build = check_syntax(os.path.basename(test_file_path))
        syntax_vet, warnings = check_for_warnings(temp_dir)
        findings = run_golangci(os.path.basename(test_file_path))
        syntax = determine_syntax_result(syntax_build, syntax_vet)
        assertions_density = assertions_density_go(os.path.basename(test_file_path))
        mccabe = assertions_mccabe_ratio_go(os.path.basename(new_go_file_path), os.path.basename(new_test_file_path))

        if syntax != CompileStatus.OK:
            print("Syntax check failed.")
            return {
                "syntax": syntax,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "syntax_output": warnings,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "timeout": False,
                "internal_error_occurred": False,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
                "runtime_errors_count": None
            }

        # Run tests and get results
        test_results = run_tests(".")

        if test_results:
            test_results["warnings"] = findings
            test_results["warnings_count"] = len(findings) if findings is not None else None
            test_results["syntax"] = syntax
            test_results["syntax_output"] = warnings
            test_results["assertions_density"] = assertions_density
            test_results["assertions_mccabe_ratio"] = mccabe
            print(f"Percentage of passed tests: {test_results['test_pass_rate']}")
            print(f"Total time: {test_results['execution_time_sec']} seconds")
            if test_results is not None or test_results['line_coverage_percent'] is not None:
                print(f"Coverage: {test_results['line_coverage_percent']}%")
            else:
                print("Coverage information not available.")
            return test_results
        else:
            print("Failed to execute Go tests.")
            return {
                "syntax": syntax,
                "syntax_output": warnings,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "timeout": False,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "internal_error_occurred": True,
                "runtime_errors_count": None,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
            }

    finally:
        # Restore the original working directory
        os.chdir(original_cwd)
        # Clean up the temporary directory
        shutil.rmtree(temp_dir)
        print(f"Temporary directory {temp_dir} has been removed.")


def x_analyze_go_tests__mutmut_85(go_file_path, test_file_path):
    # Create a temporary directory within "/data/generated/golang/"
    base_temp_dir = "./data/generated/docs_golang/"
    os.makedirs(base_temp_dir, exist_ok=True)
    temp_dir = tempfile.mkdtemp(dir=base_temp_dir)

    # Save the current working directory
    original_cwd = os.getcwd()

    try:
        # Copy the Go file and the specified test file to the temporary directory
        new_go_file_path = shutil.copy(go_file_path, temp_dir)
        new_test_file_path = shutil.copy(test_file_path, temp_dir)

        # Change to temporary directory
        os.chdir(temp_dir)

        # Check syntax
        syntax_build = check_syntax(os.path.basename(test_file_path))
        syntax_vet, warnings = check_for_warnings(temp_dir)
        findings = run_golangci(os.path.basename(test_file_path))
        syntax = determine_syntax_result(syntax_build, syntax_vet)
        assertions_density = assertions_density_go(os.path.basename(test_file_path))
        mccabe = assertions_mccabe_ratio_go(os.path.basename(new_go_file_path), os.path.basename(new_test_file_path))

        if syntax != CompileStatus.OK:
            print("Syntax check failed.")
            return {
                "syntax": syntax,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "syntax_output": warnings,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "timeout": False,
                "internal_error_occurred": False,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
                "runtime_errors_count": None
            }

        # Run tests and get results
        test_results = run_tests(".")

        if test_results:
            test_results["warnings"] = findings
            test_results["warnings_count"] = len(findings) if findings is not None else None
            test_results["syntax"] = syntax
            test_results["syntax_output"] = warnings
            test_results["assertions_density"] = assertions_density
            test_results["assertions_mccabe_ratio"] = mccabe
            print(f"Percentage of passed tests: {test_results['test_pass_rate']}")
            print(f"Total time: {test_results['execution_time_sec']} seconds")
            if test_results is not None and test_results['line_coverage_percent'] is not None:
                print(f"Coverage: {test_results['XXline_coverage_percentXX']}%")
            else:
                print("Coverage information not available.")
            return test_results
        else:
            print("Failed to execute Go tests.")
            return {
                "syntax": syntax,
                "syntax_output": warnings,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "timeout": False,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "internal_error_occurred": True,
                "runtime_errors_count": None,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
            }

    finally:
        # Restore the original working directory
        os.chdir(original_cwd)
        # Clean up the temporary directory
        shutil.rmtree(temp_dir)
        print(f"Temporary directory {temp_dir} has been removed.")


def x_analyze_go_tests__mutmut_86(go_file_path, test_file_path):
    # Create a temporary directory within "/data/generated/golang/"
    base_temp_dir = "./data/generated/docs_golang/"
    os.makedirs(base_temp_dir, exist_ok=True)
    temp_dir = tempfile.mkdtemp(dir=base_temp_dir)

    # Save the current working directory
    original_cwd = os.getcwd()

    try:
        # Copy the Go file and the specified test file to the temporary directory
        new_go_file_path = shutil.copy(go_file_path, temp_dir)
        new_test_file_path = shutil.copy(test_file_path, temp_dir)

        # Change to temporary directory
        os.chdir(temp_dir)

        # Check syntax
        syntax_build = check_syntax(os.path.basename(test_file_path))
        syntax_vet, warnings = check_for_warnings(temp_dir)
        findings = run_golangci(os.path.basename(test_file_path))
        syntax = determine_syntax_result(syntax_build, syntax_vet)
        assertions_density = assertions_density_go(os.path.basename(test_file_path))
        mccabe = assertions_mccabe_ratio_go(os.path.basename(new_go_file_path), os.path.basename(new_test_file_path))

        if syntax != CompileStatus.OK:
            print("Syntax check failed.")
            return {
                "syntax": syntax,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "syntax_output": warnings,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "timeout": False,
                "internal_error_occurred": False,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
                "runtime_errors_count": None
            }

        # Run tests and get results
        test_results = run_tests(".")

        if test_results:
            test_results["warnings"] = findings
            test_results["warnings_count"] = len(findings) if findings is not None else None
            test_results["syntax"] = syntax
            test_results["syntax_output"] = warnings
            test_results["assertions_density"] = assertions_density
            test_results["assertions_mccabe_ratio"] = mccabe
            print(f"Percentage of passed tests: {test_results['test_pass_rate']}")
            print(f"Total time: {test_results['execution_time_sec']} seconds")
            if test_results is not None and test_results['line_coverage_percent'] is not None:
                print(f"Coverage: {test_results[None]}%")
            else:
                print("Coverage information not available.")
            return test_results
        else:
            print("Failed to execute Go tests.")
            return {
                "syntax": syntax,
                "syntax_output": warnings,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "timeout": False,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "internal_error_occurred": True,
                "runtime_errors_count": None,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
            }

    finally:
        # Restore the original working directory
        os.chdir(original_cwd)
        # Clean up the temporary directory
        shutil.rmtree(temp_dir)
        print(f"Temporary directory {temp_dir} has been removed.")


def x_analyze_go_tests__mutmut_87(go_file_path, test_file_path):
    # Create a temporary directory within "/data/generated/golang/"
    base_temp_dir = "./data/generated/docs_golang/"
    os.makedirs(base_temp_dir, exist_ok=True)
    temp_dir = tempfile.mkdtemp(dir=base_temp_dir)

    # Save the current working directory
    original_cwd = os.getcwd()

    try:
        # Copy the Go file and the specified test file to the temporary directory
        new_go_file_path = shutil.copy(go_file_path, temp_dir)
        new_test_file_path = shutil.copy(test_file_path, temp_dir)

        # Change to temporary directory
        os.chdir(temp_dir)

        # Check syntax
        syntax_build = check_syntax(os.path.basename(test_file_path))
        syntax_vet, warnings = check_for_warnings(temp_dir)
        findings = run_golangci(os.path.basename(test_file_path))
        syntax = determine_syntax_result(syntax_build, syntax_vet)
        assertions_density = assertions_density_go(os.path.basename(test_file_path))
        mccabe = assertions_mccabe_ratio_go(os.path.basename(new_go_file_path), os.path.basename(new_test_file_path))

        if syntax != CompileStatus.OK:
            print("Syntax check failed.")
            return {
                "syntax": syntax,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "syntax_output": warnings,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "timeout": False,
                "internal_error_occurred": False,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
                "runtime_errors_count": None
            }

        # Run tests and get results
        test_results = run_tests(".")

        if test_results:
            test_results["warnings"] = findings
            test_results["warnings_count"] = len(findings) if findings is not None else None
            test_results["syntax"] = syntax
            test_results["syntax_output"] = warnings
            test_results["assertions_density"] = assertions_density
            test_results["assertions_mccabe_ratio"] = mccabe
            print(f"Percentage of passed tests: {test_results['test_pass_rate']}")
            print(f"Total time: {test_results['execution_time_sec']} seconds")
            if test_results is not None and test_results['line_coverage_percent'] is not None:
                print(f"Coverage: {test_results['line_coverage_percent']}%")
            else:
                print("XXCoverage information not available.XX")
            return test_results
        else:
            print("Failed to execute Go tests.")
            return {
                "syntax": syntax,
                "syntax_output": warnings,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "timeout": False,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "internal_error_occurred": True,
                "runtime_errors_count": None,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
            }

    finally:
        # Restore the original working directory
        os.chdir(original_cwd)
        # Clean up the temporary directory
        shutil.rmtree(temp_dir)
        print(f"Temporary directory {temp_dir} has been removed.")


def x_analyze_go_tests__mutmut_88(go_file_path, test_file_path):
    # Create a temporary directory within "/data/generated/golang/"
    base_temp_dir = "./data/generated/docs_golang/"
    os.makedirs(base_temp_dir, exist_ok=True)
    temp_dir = tempfile.mkdtemp(dir=base_temp_dir)

    # Save the current working directory
    original_cwd = os.getcwd()

    try:
        # Copy the Go file and the specified test file to the temporary directory
        new_go_file_path = shutil.copy(go_file_path, temp_dir)
        new_test_file_path = shutil.copy(test_file_path, temp_dir)

        # Change to temporary directory
        os.chdir(temp_dir)

        # Check syntax
        syntax_build = check_syntax(os.path.basename(test_file_path))
        syntax_vet, warnings = check_for_warnings(temp_dir)
        findings = run_golangci(os.path.basename(test_file_path))
        syntax = determine_syntax_result(syntax_build, syntax_vet)
        assertions_density = assertions_density_go(os.path.basename(test_file_path))
        mccabe = assertions_mccabe_ratio_go(os.path.basename(new_go_file_path), os.path.basename(new_test_file_path))

        if syntax != CompileStatus.OK:
            print("Syntax check failed.")
            return {
                "syntax": syntax,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "syntax_output": warnings,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "timeout": False,
                "internal_error_occurred": False,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
                "runtime_errors_count": None
            }

        # Run tests and get results
        test_results = run_tests(".")

        if test_results:
            test_results["warnings"] = findings
            test_results["warnings_count"] = len(findings) if findings is not None else None
            test_results["syntax"] = syntax
            test_results["syntax_output"] = warnings
            test_results["assertions_density"] = assertions_density
            test_results["assertions_mccabe_ratio"] = mccabe
            print(f"Percentage of passed tests: {test_results['test_pass_rate']}")
            print(f"Total time: {test_results['execution_time_sec']} seconds")
            if test_results is not None and test_results['line_coverage_percent'] is not None:
                print(f"Coverage: {test_results['line_coverage_percent']}%")
            else:
                print("Coverage information not available.")
            return test_results
        else:
            print("XXFailed to execute Go tests.XX")
            return {
                "syntax": syntax,
                "syntax_output": warnings,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "timeout": False,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "internal_error_occurred": True,
                "runtime_errors_count": None,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
            }

    finally:
        # Restore the original working directory
        os.chdir(original_cwd)
        # Clean up the temporary directory
        shutil.rmtree(temp_dir)
        print(f"Temporary directory {temp_dir} has been removed.")


def x_analyze_go_tests__mutmut_89(go_file_path, test_file_path):
    # Create a temporary directory within "/data/generated/golang/"
    base_temp_dir = "./data/generated/docs_golang/"
    os.makedirs(base_temp_dir, exist_ok=True)
    temp_dir = tempfile.mkdtemp(dir=base_temp_dir)

    # Save the current working directory
    original_cwd = os.getcwd()

    try:
        # Copy the Go file and the specified test file to the temporary directory
        new_go_file_path = shutil.copy(go_file_path, temp_dir)
        new_test_file_path = shutil.copy(test_file_path, temp_dir)

        # Change to temporary directory
        os.chdir(temp_dir)

        # Check syntax
        syntax_build = check_syntax(os.path.basename(test_file_path))
        syntax_vet, warnings = check_for_warnings(temp_dir)
        findings = run_golangci(os.path.basename(test_file_path))
        syntax = determine_syntax_result(syntax_build, syntax_vet)
        assertions_density = assertions_density_go(os.path.basename(test_file_path))
        mccabe = assertions_mccabe_ratio_go(os.path.basename(new_go_file_path), os.path.basename(new_test_file_path))

        if syntax != CompileStatus.OK:
            print("Syntax check failed.")
            return {
                "syntax": syntax,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "syntax_output": warnings,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "timeout": False,
                "internal_error_occurred": False,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
                "runtime_errors_count": None
            }

        # Run tests and get results
        test_results = run_tests(".")

        if test_results:
            test_results["warnings"] = findings
            test_results["warnings_count"] = len(findings) if findings is not None else None
            test_results["syntax"] = syntax
            test_results["syntax_output"] = warnings
            test_results["assertions_density"] = assertions_density
            test_results["assertions_mccabe_ratio"] = mccabe
            print(f"Percentage of passed tests: {test_results['test_pass_rate']}")
            print(f"Total time: {test_results['execution_time_sec']} seconds")
            if test_results is not None and test_results['line_coverage_percent'] is not None:
                print(f"Coverage: {test_results['line_coverage_percent']}%")
            else:
                print("Coverage information not available.")
            return test_results
        else:
            print("Failed to execute Go tests.")
            return {
                "XXsyntaxXX": syntax,
                "syntax_output": warnings,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "timeout": False,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "internal_error_occurred": True,
                "runtime_errors_count": None,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
            }

    finally:
        # Restore the original working directory
        os.chdir(original_cwd)
        # Clean up the temporary directory
        shutil.rmtree(temp_dir)
        print(f"Temporary directory {temp_dir} has been removed.")


def x_analyze_go_tests__mutmut_90(go_file_path, test_file_path):
    # Create a temporary directory within "/data/generated/golang/"
    base_temp_dir = "./data/generated/docs_golang/"
    os.makedirs(base_temp_dir, exist_ok=True)
    temp_dir = tempfile.mkdtemp(dir=base_temp_dir)

    # Save the current working directory
    original_cwd = os.getcwd()

    try:
        # Copy the Go file and the specified test file to the temporary directory
        new_go_file_path = shutil.copy(go_file_path, temp_dir)
        new_test_file_path = shutil.copy(test_file_path, temp_dir)

        # Change to temporary directory
        os.chdir(temp_dir)

        # Check syntax
        syntax_build = check_syntax(os.path.basename(test_file_path))
        syntax_vet, warnings = check_for_warnings(temp_dir)
        findings = run_golangci(os.path.basename(test_file_path))
        syntax = determine_syntax_result(syntax_build, syntax_vet)
        assertions_density = assertions_density_go(os.path.basename(test_file_path))
        mccabe = assertions_mccabe_ratio_go(os.path.basename(new_go_file_path), os.path.basename(new_test_file_path))

        if syntax != CompileStatus.OK:
            print("Syntax check failed.")
            return {
                "syntax": syntax,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "syntax_output": warnings,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "timeout": False,
                "internal_error_occurred": False,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
                "runtime_errors_count": None
            }

        # Run tests and get results
        test_results = run_tests(".")

        if test_results:
            test_results["warnings"] = findings
            test_results["warnings_count"] = len(findings) if findings is not None else None
            test_results["syntax"] = syntax
            test_results["syntax_output"] = warnings
            test_results["assertions_density"] = assertions_density
            test_results["assertions_mccabe_ratio"] = mccabe
            print(f"Percentage of passed tests: {test_results['test_pass_rate']}")
            print(f"Total time: {test_results['execution_time_sec']} seconds")
            if test_results is not None and test_results['line_coverage_percent'] is not None:
                print(f"Coverage: {test_results['line_coverage_percent']}%")
            else:
                print("Coverage information not available.")
            return test_results
        else:
            print("Failed to execute Go tests.")
            return {
                "syntax": syntax,
                "XXsyntax_outputXX": warnings,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "timeout": False,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "internal_error_occurred": True,
                "runtime_errors_count": None,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
            }

    finally:
        # Restore the original working directory
        os.chdir(original_cwd)
        # Clean up the temporary directory
        shutil.rmtree(temp_dir)
        print(f"Temporary directory {temp_dir} has been removed.")


def x_analyze_go_tests__mutmut_91(go_file_path, test_file_path):
    # Create a temporary directory within "/data/generated/golang/"
    base_temp_dir = "./data/generated/docs_golang/"
    os.makedirs(base_temp_dir, exist_ok=True)
    temp_dir = tempfile.mkdtemp(dir=base_temp_dir)

    # Save the current working directory
    original_cwd = os.getcwd()

    try:
        # Copy the Go file and the specified test file to the temporary directory
        new_go_file_path = shutil.copy(go_file_path, temp_dir)
        new_test_file_path = shutil.copy(test_file_path, temp_dir)

        # Change to temporary directory
        os.chdir(temp_dir)

        # Check syntax
        syntax_build = check_syntax(os.path.basename(test_file_path))
        syntax_vet, warnings = check_for_warnings(temp_dir)
        findings = run_golangci(os.path.basename(test_file_path))
        syntax = determine_syntax_result(syntax_build, syntax_vet)
        assertions_density = assertions_density_go(os.path.basename(test_file_path))
        mccabe = assertions_mccabe_ratio_go(os.path.basename(new_go_file_path), os.path.basename(new_test_file_path))

        if syntax != CompileStatus.OK:
            print("Syntax check failed.")
            return {
                "syntax": syntax,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "syntax_output": warnings,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "timeout": False,
                "internal_error_occurred": False,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
                "runtime_errors_count": None
            }

        # Run tests and get results
        test_results = run_tests(".")

        if test_results:
            test_results["warnings"] = findings
            test_results["warnings_count"] = len(findings) if findings is not None else None
            test_results["syntax"] = syntax
            test_results["syntax_output"] = warnings
            test_results["assertions_density"] = assertions_density
            test_results["assertions_mccabe_ratio"] = mccabe
            print(f"Percentage of passed tests: {test_results['test_pass_rate']}")
            print(f"Total time: {test_results['execution_time_sec']} seconds")
            if test_results is not None and test_results['line_coverage_percent'] is not None:
                print(f"Coverage: {test_results['line_coverage_percent']}%")
            else:
                print("Coverage information not available.")
            return test_results
        else:
            print("Failed to execute Go tests.")
            return {
                "syntax": syntax,
                "syntax_output": warnings,
                "XXtest_pass_rateXX": None,
                "execution_time_sec": None,
                "timeout": False,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "internal_error_occurred": True,
                "runtime_errors_count": None,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
            }

    finally:
        # Restore the original working directory
        os.chdir(original_cwd)
        # Clean up the temporary directory
        shutil.rmtree(temp_dir)
        print(f"Temporary directory {temp_dir} has been removed.")


def x_analyze_go_tests__mutmut_92(go_file_path, test_file_path):
    # Create a temporary directory within "/data/generated/golang/"
    base_temp_dir = "./data/generated/docs_golang/"
    os.makedirs(base_temp_dir, exist_ok=True)
    temp_dir = tempfile.mkdtemp(dir=base_temp_dir)

    # Save the current working directory
    original_cwd = os.getcwd()

    try:
        # Copy the Go file and the specified test file to the temporary directory
        new_go_file_path = shutil.copy(go_file_path, temp_dir)
        new_test_file_path = shutil.copy(test_file_path, temp_dir)

        # Change to temporary directory
        os.chdir(temp_dir)

        # Check syntax
        syntax_build = check_syntax(os.path.basename(test_file_path))
        syntax_vet, warnings = check_for_warnings(temp_dir)
        findings = run_golangci(os.path.basename(test_file_path))
        syntax = determine_syntax_result(syntax_build, syntax_vet)
        assertions_density = assertions_density_go(os.path.basename(test_file_path))
        mccabe = assertions_mccabe_ratio_go(os.path.basename(new_go_file_path), os.path.basename(new_test_file_path))

        if syntax != CompileStatus.OK:
            print("Syntax check failed.")
            return {
                "syntax": syntax,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "syntax_output": warnings,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "timeout": False,
                "internal_error_occurred": False,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
                "runtime_errors_count": None
            }

        # Run tests and get results
        test_results = run_tests(".")

        if test_results:
            test_results["warnings"] = findings
            test_results["warnings_count"] = len(findings) if findings is not None else None
            test_results["syntax"] = syntax
            test_results["syntax_output"] = warnings
            test_results["assertions_density"] = assertions_density
            test_results["assertions_mccabe_ratio"] = mccabe
            print(f"Percentage of passed tests: {test_results['test_pass_rate']}")
            print(f"Total time: {test_results['execution_time_sec']} seconds")
            if test_results is not None and test_results['line_coverage_percent'] is not None:
                print(f"Coverage: {test_results['line_coverage_percent']}%")
            else:
                print("Coverage information not available.")
            return test_results
        else:
            print("Failed to execute Go tests.")
            return {
                "syntax": syntax,
                "syntax_output": warnings,
                "test_pass_rate": None,
                "XXexecution_time_secXX": None,
                "timeout": False,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "internal_error_occurred": True,
                "runtime_errors_count": None,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
            }

    finally:
        # Restore the original working directory
        os.chdir(original_cwd)
        # Clean up the temporary directory
        shutil.rmtree(temp_dir)
        print(f"Temporary directory {temp_dir} has been removed.")


def x_analyze_go_tests__mutmut_93(go_file_path, test_file_path):
    # Create a temporary directory within "/data/generated/golang/"
    base_temp_dir = "./data/generated/docs_golang/"
    os.makedirs(base_temp_dir, exist_ok=True)
    temp_dir = tempfile.mkdtemp(dir=base_temp_dir)

    # Save the current working directory
    original_cwd = os.getcwd()

    try:
        # Copy the Go file and the specified test file to the temporary directory
        new_go_file_path = shutil.copy(go_file_path, temp_dir)
        new_test_file_path = shutil.copy(test_file_path, temp_dir)

        # Change to temporary directory
        os.chdir(temp_dir)

        # Check syntax
        syntax_build = check_syntax(os.path.basename(test_file_path))
        syntax_vet, warnings = check_for_warnings(temp_dir)
        findings = run_golangci(os.path.basename(test_file_path))
        syntax = determine_syntax_result(syntax_build, syntax_vet)
        assertions_density = assertions_density_go(os.path.basename(test_file_path))
        mccabe = assertions_mccabe_ratio_go(os.path.basename(new_go_file_path), os.path.basename(new_test_file_path))

        if syntax != CompileStatus.OK:
            print("Syntax check failed.")
            return {
                "syntax": syntax,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "syntax_output": warnings,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "timeout": False,
                "internal_error_occurred": False,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
                "runtime_errors_count": None
            }

        # Run tests and get results
        test_results = run_tests(".")

        if test_results:
            test_results["warnings"] = findings
            test_results["warnings_count"] = len(findings) if findings is not None else None
            test_results["syntax"] = syntax
            test_results["syntax_output"] = warnings
            test_results["assertions_density"] = assertions_density
            test_results["assertions_mccabe_ratio"] = mccabe
            print(f"Percentage of passed tests: {test_results['test_pass_rate']}")
            print(f"Total time: {test_results['execution_time_sec']} seconds")
            if test_results is not None and test_results['line_coverage_percent'] is not None:
                print(f"Coverage: {test_results['line_coverage_percent']}%")
            else:
                print("Coverage information not available.")
            return test_results
        else:
            print("Failed to execute Go tests.")
            return {
                "syntax": syntax,
                "syntax_output": warnings,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "XXtimeoutXX": False,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "internal_error_occurred": True,
                "runtime_errors_count": None,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
            }

    finally:
        # Restore the original working directory
        os.chdir(original_cwd)
        # Clean up the temporary directory
        shutil.rmtree(temp_dir)
        print(f"Temporary directory {temp_dir} has been removed.")


def x_analyze_go_tests__mutmut_94(go_file_path, test_file_path):
    # Create a temporary directory within "/data/generated/golang/"
    base_temp_dir = "./data/generated/docs_golang/"
    os.makedirs(base_temp_dir, exist_ok=True)
    temp_dir = tempfile.mkdtemp(dir=base_temp_dir)

    # Save the current working directory
    original_cwd = os.getcwd()

    try:
        # Copy the Go file and the specified test file to the temporary directory
        new_go_file_path = shutil.copy(go_file_path, temp_dir)
        new_test_file_path = shutil.copy(test_file_path, temp_dir)

        # Change to temporary directory
        os.chdir(temp_dir)

        # Check syntax
        syntax_build = check_syntax(os.path.basename(test_file_path))
        syntax_vet, warnings = check_for_warnings(temp_dir)
        findings = run_golangci(os.path.basename(test_file_path))
        syntax = determine_syntax_result(syntax_build, syntax_vet)
        assertions_density = assertions_density_go(os.path.basename(test_file_path))
        mccabe = assertions_mccabe_ratio_go(os.path.basename(new_go_file_path), os.path.basename(new_test_file_path))

        if syntax != CompileStatus.OK:
            print("Syntax check failed.")
            return {
                "syntax": syntax,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "syntax_output": warnings,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "timeout": False,
                "internal_error_occurred": False,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
                "runtime_errors_count": None
            }

        # Run tests and get results
        test_results = run_tests(".")

        if test_results:
            test_results["warnings"] = findings
            test_results["warnings_count"] = len(findings) if findings is not None else None
            test_results["syntax"] = syntax
            test_results["syntax_output"] = warnings
            test_results["assertions_density"] = assertions_density
            test_results["assertions_mccabe_ratio"] = mccabe
            print(f"Percentage of passed tests: {test_results['test_pass_rate']}")
            print(f"Total time: {test_results['execution_time_sec']} seconds")
            if test_results is not None and test_results['line_coverage_percent'] is not None:
                print(f"Coverage: {test_results['line_coverage_percent']}%")
            else:
                print("Coverage information not available.")
            return test_results
        else:
            print("Failed to execute Go tests.")
            return {
                "syntax": syntax,
                "syntax_output": warnings,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "timeout": True,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "internal_error_occurred": True,
                "runtime_errors_count": None,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
            }

    finally:
        # Restore the original working directory
        os.chdir(original_cwd)
        # Clean up the temporary directory
        shutil.rmtree(temp_dir)
        print(f"Temporary directory {temp_dir} has been removed.")


def x_analyze_go_tests__mutmut_95(go_file_path, test_file_path):
    # Create a temporary directory within "/data/generated/golang/"
    base_temp_dir = "./data/generated/docs_golang/"
    os.makedirs(base_temp_dir, exist_ok=True)
    temp_dir = tempfile.mkdtemp(dir=base_temp_dir)

    # Save the current working directory
    original_cwd = os.getcwd()

    try:
        # Copy the Go file and the specified test file to the temporary directory
        new_go_file_path = shutil.copy(go_file_path, temp_dir)
        new_test_file_path = shutil.copy(test_file_path, temp_dir)

        # Change to temporary directory
        os.chdir(temp_dir)

        # Check syntax
        syntax_build = check_syntax(os.path.basename(test_file_path))
        syntax_vet, warnings = check_for_warnings(temp_dir)
        findings = run_golangci(os.path.basename(test_file_path))
        syntax = determine_syntax_result(syntax_build, syntax_vet)
        assertions_density = assertions_density_go(os.path.basename(test_file_path))
        mccabe = assertions_mccabe_ratio_go(os.path.basename(new_go_file_path), os.path.basename(new_test_file_path))

        if syntax != CompileStatus.OK:
            print("Syntax check failed.")
            return {
                "syntax": syntax,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "syntax_output": warnings,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "timeout": False,
                "internal_error_occurred": False,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
                "runtime_errors_count": None
            }

        # Run tests and get results
        test_results = run_tests(".")

        if test_results:
            test_results["warnings"] = findings
            test_results["warnings_count"] = len(findings) if findings is not None else None
            test_results["syntax"] = syntax
            test_results["syntax_output"] = warnings
            test_results["assertions_density"] = assertions_density
            test_results["assertions_mccabe_ratio"] = mccabe
            print(f"Percentage of passed tests: {test_results['test_pass_rate']}")
            print(f"Total time: {test_results['execution_time_sec']} seconds")
            if test_results is not None and test_results['line_coverage_percent'] is not None:
                print(f"Coverage: {test_results['line_coverage_percent']}%")
            else:
                print("Coverage information not available.")
            return test_results
        else:
            print("Failed to execute Go tests.")
            return {
                "syntax": syntax,
                "syntax_output": warnings,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "timeout": False,
                "XXline_coverage_percentXX": None,
                "branch_coverage_percent": None,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "internal_error_occurred": True,
                "runtime_errors_count": None,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
            }

    finally:
        # Restore the original working directory
        os.chdir(original_cwd)
        # Clean up the temporary directory
        shutil.rmtree(temp_dir)
        print(f"Temporary directory {temp_dir} has been removed.")


def x_analyze_go_tests__mutmut_96(go_file_path, test_file_path):
    # Create a temporary directory within "/data/generated/golang/"
    base_temp_dir = "./data/generated/docs_golang/"
    os.makedirs(base_temp_dir, exist_ok=True)
    temp_dir = tempfile.mkdtemp(dir=base_temp_dir)

    # Save the current working directory
    original_cwd = os.getcwd()

    try:
        # Copy the Go file and the specified test file to the temporary directory
        new_go_file_path = shutil.copy(go_file_path, temp_dir)
        new_test_file_path = shutil.copy(test_file_path, temp_dir)

        # Change to temporary directory
        os.chdir(temp_dir)

        # Check syntax
        syntax_build = check_syntax(os.path.basename(test_file_path))
        syntax_vet, warnings = check_for_warnings(temp_dir)
        findings = run_golangci(os.path.basename(test_file_path))
        syntax = determine_syntax_result(syntax_build, syntax_vet)
        assertions_density = assertions_density_go(os.path.basename(test_file_path))
        mccabe = assertions_mccabe_ratio_go(os.path.basename(new_go_file_path), os.path.basename(new_test_file_path))

        if syntax != CompileStatus.OK:
            print("Syntax check failed.")
            return {
                "syntax": syntax,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "syntax_output": warnings,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "timeout": False,
                "internal_error_occurred": False,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
                "runtime_errors_count": None
            }

        # Run tests and get results
        test_results = run_tests(".")

        if test_results:
            test_results["warnings"] = findings
            test_results["warnings_count"] = len(findings) if findings is not None else None
            test_results["syntax"] = syntax
            test_results["syntax_output"] = warnings
            test_results["assertions_density"] = assertions_density
            test_results["assertions_mccabe_ratio"] = mccabe
            print(f"Percentage of passed tests: {test_results['test_pass_rate']}")
            print(f"Total time: {test_results['execution_time_sec']} seconds")
            if test_results is not None and test_results['line_coverage_percent'] is not None:
                print(f"Coverage: {test_results['line_coverage_percent']}%")
            else:
                print("Coverage information not available.")
            return test_results
        else:
            print("Failed to execute Go tests.")
            return {
                "syntax": syntax,
                "syntax_output": warnings,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "timeout": False,
                "line_coverage_percent": None,
                "XXbranch_coverage_percentXX": None,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "internal_error_occurred": True,
                "runtime_errors_count": None,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
            }

    finally:
        # Restore the original working directory
        os.chdir(original_cwd)
        # Clean up the temporary directory
        shutil.rmtree(temp_dir)
        print(f"Temporary directory {temp_dir} has been removed.")


def x_analyze_go_tests__mutmut_97(go_file_path, test_file_path):
    # Create a temporary directory within "/data/generated/golang/"
    base_temp_dir = "./data/generated/docs_golang/"
    os.makedirs(base_temp_dir, exist_ok=True)
    temp_dir = tempfile.mkdtemp(dir=base_temp_dir)

    # Save the current working directory
    original_cwd = os.getcwd()

    try:
        # Copy the Go file and the specified test file to the temporary directory
        new_go_file_path = shutil.copy(go_file_path, temp_dir)
        new_test_file_path = shutil.copy(test_file_path, temp_dir)

        # Change to temporary directory
        os.chdir(temp_dir)

        # Check syntax
        syntax_build = check_syntax(os.path.basename(test_file_path))
        syntax_vet, warnings = check_for_warnings(temp_dir)
        findings = run_golangci(os.path.basename(test_file_path))
        syntax = determine_syntax_result(syntax_build, syntax_vet)
        assertions_density = assertions_density_go(os.path.basename(test_file_path))
        mccabe = assertions_mccabe_ratio_go(os.path.basename(new_go_file_path), os.path.basename(new_test_file_path))

        if syntax != CompileStatus.OK:
            print("Syntax check failed.")
            return {
                "syntax": syntax,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "syntax_output": warnings,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "timeout": False,
                "internal_error_occurred": False,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
                "runtime_errors_count": None
            }

        # Run tests and get results
        test_results = run_tests(".")

        if test_results:
            test_results["warnings"] = findings
            test_results["warnings_count"] = len(findings) if findings is not None else None
            test_results["syntax"] = syntax
            test_results["syntax_output"] = warnings
            test_results["assertions_density"] = assertions_density
            test_results["assertions_mccabe_ratio"] = mccabe
            print(f"Percentage of passed tests: {test_results['test_pass_rate']}")
            print(f"Total time: {test_results['execution_time_sec']} seconds")
            if test_results is not None and test_results['line_coverage_percent'] is not None:
                print(f"Coverage: {test_results['line_coverage_percent']}%")
            else:
                print("Coverage information not available.")
            return test_results
        else:
            print("Failed to execute Go tests.")
            return {
                "syntax": syntax,
                "syntax_output": warnings,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "timeout": False,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "XXwarningsXX": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "internal_error_occurred": True,
                "runtime_errors_count": None,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
            }

    finally:
        # Restore the original working directory
        os.chdir(original_cwd)
        # Clean up the temporary directory
        shutil.rmtree(temp_dir)
        print(f"Temporary directory {temp_dir} has been removed.")


def x_analyze_go_tests__mutmut_98(go_file_path, test_file_path):
    # Create a temporary directory within "/data/generated/golang/"
    base_temp_dir = "./data/generated/docs_golang/"
    os.makedirs(base_temp_dir, exist_ok=True)
    temp_dir = tempfile.mkdtemp(dir=base_temp_dir)

    # Save the current working directory
    original_cwd = os.getcwd()

    try:
        # Copy the Go file and the specified test file to the temporary directory
        new_go_file_path = shutil.copy(go_file_path, temp_dir)
        new_test_file_path = shutil.copy(test_file_path, temp_dir)

        # Change to temporary directory
        os.chdir(temp_dir)

        # Check syntax
        syntax_build = check_syntax(os.path.basename(test_file_path))
        syntax_vet, warnings = check_for_warnings(temp_dir)
        findings = run_golangci(os.path.basename(test_file_path))
        syntax = determine_syntax_result(syntax_build, syntax_vet)
        assertions_density = assertions_density_go(os.path.basename(test_file_path))
        mccabe = assertions_mccabe_ratio_go(os.path.basename(new_go_file_path), os.path.basename(new_test_file_path))

        if syntax != CompileStatus.OK:
            print("Syntax check failed.")
            return {
                "syntax": syntax,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "syntax_output": warnings,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "timeout": False,
                "internal_error_occurred": False,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
                "runtime_errors_count": None
            }

        # Run tests and get results
        test_results = run_tests(".")

        if test_results:
            test_results["warnings"] = findings
            test_results["warnings_count"] = len(findings) if findings is not None else None
            test_results["syntax"] = syntax
            test_results["syntax_output"] = warnings
            test_results["assertions_density"] = assertions_density
            test_results["assertions_mccabe_ratio"] = mccabe
            print(f"Percentage of passed tests: {test_results['test_pass_rate']}")
            print(f"Total time: {test_results['execution_time_sec']} seconds")
            if test_results is not None and test_results['line_coverage_percent'] is not None:
                print(f"Coverage: {test_results['line_coverage_percent']}%")
            else:
                print("Coverage information not available.")
            return test_results
        else:
            print("Failed to execute Go tests.")
            return {
                "syntax": syntax,
                "syntax_output": warnings,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "timeout": False,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "warnings": findings,
                "XXwarnings_countXX": len(findings) if findings is not None else None,
                "internal_error_occurred": True,
                "runtime_errors_count": None,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
            }

    finally:
        # Restore the original working directory
        os.chdir(original_cwd)
        # Clean up the temporary directory
        shutil.rmtree(temp_dir)
        print(f"Temporary directory {temp_dir} has been removed.")


def x_analyze_go_tests__mutmut_99(go_file_path, test_file_path):
    # Create a temporary directory within "/data/generated/golang/"
    base_temp_dir = "./data/generated/docs_golang/"
    os.makedirs(base_temp_dir, exist_ok=True)
    temp_dir = tempfile.mkdtemp(dir=base_temp_dir)

    # Save the current working directory
    original_cwd = os.getcwd()

    try:
        # Copy the Go file and the specified test file to the temporary directory
        new_go_file_path = shutil.copy(go_file_path, temp_dir)
        new_test_file_path = shutil.copy(test_file_path, temp_dir)

        # Change to temporary directory
        os.chdir(temp_dir)

        # Check syntax
        syntax_build = check_syntax(os.path.basename(test_file_path))
        syntax_vet, warnings = check_for_warnings(temp_dir)
        findings = run_golangci(os.path.basename(test_file_path))
        syntax = determine_syntax_result(syntax_build, syntax_vet)
        assertions_density = assertions_density_go(os.path.basename(test_file_path))
        mccabe = assertions_mccabe_ratio_go(os.path.basename(new_go_file_path), os.path.basename(new_test_file_path))

        if syntax != CompileStatus.OK:
            print("Syntax check failed.")
            return {
                "syntax": syntax,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "syntax_output": warnings,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "timeout": False,
                "internal_error_occurred": False,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
                "runtime_errors_count": None
            }

        # Run tests and get results
        test_results = run_tests(".")

        if test_results:
            test_results["warnings"] = findings
            test_results["warnings_count"] = len(findings) if findings is not None else None
            test_results["syntax"] = syntax
            test_results["syntax_output"] = warnings
            test_results["assertions_density"] = assertions_density
            test_results["assertions_mccabe_ratio"] = mccabe
            print(f"Percentage of passed tests: {test_results['test_pass_rate']}")
            print(f"Total time: {test_results['execution_time_sec']} seconds")
            if test_results is not None and test_results['line_coverage_percent'] is not None:
                print(f"Coverage: {test_results['line_coverage_percent']}%")
            else:
                print("Coverage information not available.")
            return test_results
        else:
            print("Failed to execute Go tests.")
            return {
                "syntax": syntax,
                "syntax_output": warnings,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "timeout": False,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "warnings": findings,
                "warnings_count": len(findings) if findings is  None else None,
                "internal_error_occurred": True,
                "runtime_errors_count": None,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
            }

    finally:
        # Restore the original working directory
        os.chdir(original_cwd)
        # Clean up the temporary directory
        shutil.rmtree(temp_dir)
        print(f"Temporary directory {temp_dir} has been removed.")


def x_analyze_go_tests__mutmut_100(go_file_path, test_file_path):
    # Create a temporary directory within "/data/generated/golang/"
    base_temp_dir = "./data/generated/docs_golang/"
    os.makedirs(base_temp_dir, exist_ok=True)
    temp_dir = tempfile.mkdtemp(dir=base_temp_dir)

    # Save the current working directory
    original_cwd = os.getcwd()

    try:
        # Copy the Go file and the specified test file to the temporary directory
        new_go_file_path = shutil.copy(go_file_path, temp_dir)
        new_test_file_path = shutil.copy(test_file_path, temp_dir)

        # Change to temporary directory
        os.chdir(temp_dir)

        # Check syntax
        syntax_build = check_syntax(os.path.basename(test_file_path))
        syntax_vet, warnings = check_for_warnings(temp_dir)
        findings = run_golangci(os.path.basename(test_file_path))
        syntax = determine_syntax_result(syntax_build, syntax_vet)
        assertions_density = assertions_density_go(os.path.basename(test_file_path))
        mccabe = assertions_mccabe_ratio_go(os.path.basename(new_go_file_path), os.path.basename(new_test_file_path))

        if syntax != CompileStatus.OK:
            print("Syntax check failed.")
            return {
                "syntax": syntax,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "syntax_output": warnings,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "timeout": False,
                "internal_error_occurred": False,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
                "runtime_errors_count": None
            }

        # Run tests and get results
        test_results = run_tests(".")

        if test_results:
            test_results["warnings"] = findings
            test_results["warnings_count"] = len(findings) if findings is not None else None
            test_results["syntax"] = syntax
            test_results["syntax_output"] = warnings
            test_results["assertions_density"] = assertions_density
            test_results["assertions_mccabe_ratio"] = mccabe
            print(f"Percentage of passed tests: {test_results['test_pass_rate']}")
            print(f"Total time: {test_results['execution_time_sec']} seconds")
            if test_results is not None and test_results['line_coverage_percent'] is not None:
                print(f"Coverage: {test_results['line_coverage_percent']}%")
            else:
                print("Coverage information not available.")
            return test_results
        else:
            print("Failed to execute Go tests.")
            return {
                "syntax": syntax,
                "syntax_output": warnings,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "timeout": False,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "XXinternal_error_occurredXX": True,
                "runtime_errors_count": None,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
            }

    finally:
        # Restore the original working directory
        os.chdir(original_cwd)
        # Clean up the temporary directory
        shutil.rmtree(temp_dir)
        print(f"Temporary directory {temp_dir} has been removed.")


def x_analyze_go_tests__mutmut_101(go_file_path, test_file_path):
    # Create a temporary directory within "/data/generated/golang/"
    base_temp_dir = "./data/generated/docs_golang/"
    os.makedirs(base_temp_dir, exist_ok=True)
    temp_dir = tempfile.mkdtemp(dir=base_temp_dir)

    # Save the current working directory
    original_cwd = os.getcwd()

    try:
        # Copy the Go file and the specified test file to the temporary directory
        new_go_file_path = shutil.copy(go_file_path, temp_dir)
        new_test_file_path = shutil.copy(test_file_path, temp_dir)

        # Change to temporary directory
        os.chdir(temp_dir)

        # Check syntax
        syntax_build = check_syntax(os.path.basename(test_file_path))
        syntax_vet, warnings = check_for_warnings(temp_dir)
        findings = run_golangci(os.path.basename(test_file_path))
        syntax = determine_syntax_result(syntax_build, syntax_vet)
        assertions_density = assertions_density_go(os.path.basename(test_file_path))
        mccabe = assertions_mccabe_ratio_go(os.path.basename(new_go_file_path), os.path.basename(new_test_file_path))

        if syntax != CompileStatus.OK:
            print("Syntax check failed.")
            return {
                "syntax": syntax,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "syntax_output": warnings,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "timeout": False,
                "internal_error_occurred": False,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
                "runtime_errors_count": None
            }

        # Run tests and get results
        test_results = run_tests(".")

        if test_results:
            test_results["warnings"] = findings
            test_results["warnings_count"] = len(findings) if findings is not None else None
            test_results["syntax"] = syntax
            test_results["syntax_output"] = warnings
            test_results["assertions_density"] = assertions_density
            test_results["assertions_mccabe_ratio"] = mccabe
            print(f"Percentage of passed tests: {test_results['test_pass_rate']}")
            print(f"Total time: {test_results['execution_time_sec']} seconds")
            if test_results is not None and test_results['line_coverage_percent'] is not None:
                print(f"Coverage: {test_results['line_coverage_percent']}%")
            else:
                print("Coverage information not available.")
            return test_results
        else:
            print("Failed to execute Go tests.")
            return {
                "syntax": syntax,
                "syntax_output": warnings,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "timeout": False,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "internal_error_occurred": False,
                "runtime_errors_count": None,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
            }

    finally:
        # Restore the original working directory
        os.chdir(original_cwd)
        # Clean up the temporary directory
        shutil.rmtree(temp_dir)
        print(f"Temporary directory {temp_dir} has been removed.")


def x_analyze_go_tests__mutmut_102(go_file_path, test_file_path):
    # Create a temporary directory within "/data/generated/golang/"
    base_temp_dir = "./data/generated/docs_golang/"
    os.makedirs(base_temp_dir, exist_ok=True)
    temp_dir = tempfile.mkdtemp(dir=base_temp_dir)

    # Save the current working directory
    original_cwd = os.getcwd()

    try:
        # Copy the Go file and the specified test file to the temporary directory
        new_go_file_path = shutil.copy(go_file_path, temp_dir)
        new_test_file_path = shutil.copy(test_file_path, temp_dir)

        # Change to temporary directory
        os.chdir(temp_dir)

        # Check syntax
        syntax_build = check_syntax(os.path.basename(test_file_path))
        syntax_vet, warnings = check_for_warnings(temp_dir)
        findings = run_golangci(os.path.basename(test_file_path))
        syntax = determine_syntax_result(syntax_build, syntax_vet)
        assertions_density = assertions_density_go(os.path.basename(test_file_path))
        mccabe = assertions_mccabe_ratio_go(os.path.basename(new_go_file_path), os.path.basename(new_test_file_path))

        if syntax != CompileStatus.OK:
            print("Syntax check failed.")
            return {
                "syntax": syntax,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "syntax_output": warnings,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "timeout": False,
                "internal_error_occurred": False,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
                "runtime_errors_count": None
            }

        # Run tests and get results
        test_results = run_tests(".")

        if test_results:
            test_results["warnings"] = findings
            test_results["warnings_count"] = len(findings) if findings is not None else None
            test_results["syntax"] = syntax
            test_results["syntax_output"] = warnings
            test_results["assertions_density"] = assertions_density
            test_results["assertions_mccabe_ratio"] = mccabe
            print(f"Percentage of passed tests: {test_results['test_pass_rate']}")
            print(f"Total time: {test_results['execution_time_sec']} seconds")
            if test_results is not None and test_results['line_coverage_percent'] is not None:
                print(f"Coverage: {test_results['line_coverage_percent']}%")
            else:
                print("Coverage information not available.")
            return test_results
        else:
            print("Failed to execute Go tests.")
            return {
                "syntax": syntax,
                "syntax_output": warnings,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "timeout": False,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "internal_error_occurred": True,
                "XXruntime_errors_countXX": None,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
            }

    finally:
        # Restore the original working directory
        os.chdir(original_cwd)
        # Clean up the temporary directory
        shutil.rmtree(temp_dir)
        print(f"Temporary directory {temp_dir} has been removed.")


def x_analyze_go_tests__mutmut_103(go_file_path, test_file_path):
    # Create a temporary directory within "/data/generated/golang/"
    base_temp_dir = "./data/generated/docs_golang/"
    os.makedirs(base_temp_dir, exist_ok=True)
    temp_dir = tempfile.mkdtemp(dir=base_temp_dir)

    # Save the current working directory
    original_cwd = os.getcwd()

    try:
        # Copy the Go file and the specified test file to the temporary directory
        new_go_file_path = shutil.copy(go_file_path, temp_dir)
        new_test_file_path = shutil.copy(test_file_path, temp_dir)

        # Change to temporary directory
        os.chdir(temp_dir)

        # Check syntax
        syntax_build = check_syntax(os.path.basename(test_file_path))
        syntax_vet, warnings = check_for_warnings(temp_dir)
        findings = run_golangci(os.path.basename(test_file_path))
        syntax = determine_syntax_result(syntax_build, syntax_vet)
        assertions_density = assertions_density_go(os.path.basename(test_file_path))
        mccabe = assertions_mccabe_ratio_go(os.path.basename(new_go_file_path), os.path.basename(new_test_file_path))

        if syntax != CompileStatus.OK:
            print("Syntax check failed.")
            return {
                "syntax": syntax,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "syntax_output": warnings,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "timeout": False,
                "internal_error_occurred": False,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
                "runtime_errors_count": None
            }

        # Run tests and get results
        test_results = run_tests(".")

        if test_results:
            test_results["warnings"] = findings
            test_results["warnings_count"] = len(findings) if findings is not None else None
            test_results["syntax"] = syntax
            test_results["syntax_output"] = warnings
            test_results["assertions_density"] = assertions_density
            test_results["assertions_mccabe_ratio"] = mccabe
            print(f"Percentage of passed tests: {test_results['test_pass_rate']}")
            print(f"Total time: {test_results['execution_time_sec']} seconds")
            if test_results is not None and test_results['line_coverage_percent'] is not None:
                print(f"Coverage: {test_results['line_coverage_percent']}%")
            else:
                print("Coverage information not available.")
            return test_results
        else:
            print("Failed to execute Go tests.")
            return {
                "syntax": syntax,
                "syntax_output": warnings,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "timeout": False,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "internal_error_occurred": True,
                "runtime_errors_count": None,
                "XXassertions_densityXX": assertions_density,
                "assertions_mccabe_ratio": mccabe,
            }

    finally:
        # Restore the original working directory
        os.chdir(original_cwd)
        # Clean up the temporary directory
        shutil.rmtree(temp_dir)
        print(f"Temporary directory {temp_dir} has been removed.")


def x_analyze_go_tests__mutmut_104(go_file_path, test_file_path):
    # Create a temporary directory within "/data/generated/golang/"
    base_temp_dir = "./data/generated/docs_golang/"
    os.makedirs(base_temp_dir, exist_ok=True)
    temp_dir = tempfile.mkdtemp(dir=base_temp_dir)

    # Save the current working directory
    original_cwd = os.getcwd()

    try:
        # Copy the Go file and the specified test file to the temporary directory
        new_go_file_path = shutil.copy(go_file_path, temp_dir)
        new_test_file_path = shutil.copy(test_file_path, temp_dir)

        # Change to temporary directory
        os.chdir(temp_dir)

        # Check syntax
        syntax_build = check_syntax(os.path.basename(test_file_path))
        syntax_vet, warnings = check_for_warnings(temp_dir)
        findings = run_golangci(os.path.basename(test_file_path))
        syntax = determine_syntax_result(syntax_build, syntax_vet)
        assertions_density = assertions_density_go(os.path.basename(test_file_path))
        mccabe = assertions_mccabe_ratio_go(os.path.basename(new_go_file_path), os.path.basename(new_test_file_path))

        if syntax != CompileStatus.OK:
            print("Syntax check failed.")
            return {
                "syntax": syntax,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "syntax_output": warnings,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "timeout": False,
                "internal_error_occurred": False,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
                "runtime_errors_count": None
            }

        # Run tests and get results
        test_results = run_tests(".")

        if test_results:
            test_results["warnings"] = findings
            test_results["warnings_count"] = len(findings) if findings is not None else None
            test_results["syntax"] = syntax
            test_results["syntax_output"] = warnings
            test_results["assertions_density"] = assertions_density
            test_results["assertions_mccabe_ratio"] = mccabe
            print(f"Percentage of passed tests: {test_results['test_pass_rate']}")
            print(f"Total time: {test_results['execution_time_sec']} seconds")
            if test_results is not None and test_results['line_coverage_percent'] is not None:
                print(f"Coverage: {test_results['line_coverage_percent']}%")
            else:
                print("Coverage information not available.")
            return test_results
        else:
            print("Failed to execute Go tests.")
            return {
                "syntax": syntax,
                "syntax_output": warnings,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "timeout": False,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "internal_error_occurred": True,
                "runtime_errors_count": None,
                "assertions_density": assertions_density,
                "XXassertions_mccabe_ratioXX": mccabe,
            }

    finally:
        # Restore the original working directory
        os.chdir(original_cwd)
        # Clean up the temporary directory
        shutil.rmtree(temp_dir)
        print(f"Temporary directory {temp_dir} has been removed.")


def x_analyze_go_tests__mutmut_105(go_file_path, test_file_path):
    # Create a temporary directory within "/data/generated/golang/"
    base_temp_dir = "./data/generated/docs_golang/"
    os.makedirs(base_temp_dir, exist_ok=True)
    temp_dir = tempfile.mkdtemp(dir=base_temp_dir)

    # Save the current working directory
    original_cwd = os.getcwd()

    try:
        # Copy the Go file and the specified test file to the temporary directory
        new_go_file_path = shutil.copy(go_file_path, temp_dir)
        new_test_file_path = shutil.copy(test_file_path, temp_dir)

        # Change to temporary directory
        os.chdir(temp_dir)

        # Check syntax
        syntax_build = check_syntax(os.path.basename(test_file_path))
        syntax_vet, warnings = check_for_warnings(temp_dir)
        findings = run_golangci(os.path.basename(test_file_path))
        syntax = determine_syntax_result(syntax_build, syntax_vet)
        assertions_density = assertions_density_go(os.path.basename(test_file_path))
        mccabe = assertions_mccabe_ratio_go(os.path.basename(new_go_file_path), os.path.basename(new_test_file_path))

        if syntax != CompileStatus.OK:
            print("Syntax check failed.")
            return {
                "syntax": syntax,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "syntax_output": warnings,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "timeout": False,
                "internal_error_occurred": False,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
                "runtime_errors_count": None
            }

        # Run tests and get results
        test_results = run_tests(".")

        if test_results:
            test_results["warnings"] = findings
            test_results["warnings_count"] = len(findings) if findings is not None else None
            test_results["syntax"] = syntax
            test_results["syntax_output"] = warnings
            test_results["assertions_density"] = assertions_density
            test_results["assertions_mccabe_ratio"] = mccabe
            print(f"Percentage of passed tests: {test_results['test_pass_rate']}")
            print(f"Total time: {test_results['execution_time_sec']} seconds")
            if test_results is not None and test_results['line_coverage_percent'] is not None:
                print(f"Coverage: {test_results['line_coverage_percent']}%")
            else:
                print("Coverage information not available.")
            return test_results
        else:
            print("Failed to execute Go tests.")
            return {
                "syntax": syntax,
                "syntax_output": warnings,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "timeout": False,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "internal_error_occurred": True,
                "runtime_errors_count": None,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
            }

    finally:
        # Restore the original working directory
        os.chdir(None)
        # Clean up the temporary directory
        shutil.rmtree(temp_dir)
        print(f"Temporary directory {temp_dir} has been removed.")


def x_analyze_go_tests__mutmut_106(go_file_path, test_file_path):
    # Create a temporary directory within "/data/generated/golang/"
    base_temp_dir = "./data/generated/docs_golang/"
    os.makedirs(base_temp_dir, exist_ok=True)
    temp_dir = tempfile.mkdtemp(dir=base_temp_dir)

    # Save the current working directory
    original_cwd = os.getcwd()

    try:
        # Copy the Go file and the specified test file to the temporary directory
        new_go_file_path = shutil.copy(go_file_path, temp_dir)
        new_test_file_path = shutil.copy(test_file_path, temp_dir)

        # Change to temporary directory
        os.chdir(temp_dir)

        # Check syntax
        syntax_build = check_syntax(os.path.basename(test_file_path))
        syntax_vet, warnings = check_for_warnings(temp_dir)
        findings = run_golangci(os.path.basename(test_file_path))
        syntax = determine_syntax_result(syntax_build, syntax_vet)
        assertions_density = assertions_density_go(os.path.basename(test_file_path))
        mccabe = assertions_mccabe_ratio_go(os.path.basename(new_go_file_path), os.path.basename(new_test_file_path))

        if syntax != CompileStatus.OK:
            print("Syntax check failed.")
            return {
                "syntax": syntax,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "syntax_output": warnings,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "timeout": False,
                "internal_error_occurred": False,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
                "runtime_errors_count": None
            }

        # Run tests and get results
        test_results = run_tests(".")

        if test_results:
            test_results["warnings"] = findings
            test_results["warnings_count"] = len(findings) if findings is not None else None
            test_results["syntax"] = syntax
            test_results["syntax_output"] = warnings
            test_results["assertions_density"] = assertions_density
            test_results["assertions_mccabe_ratio"] = mccabe
            print(f"Percentage of passed tests: {test_results['test_pass_rate']}")
            print(f"Total time: {test_results['execution_time_sec']} seconds")
            if test_results is not None and test_results['line_coverage_percent'] is not None:
                print(f"Coverage: {test_results['line_coverage_percent']}%")
            else:
                print("Coverage information not available.")
            return test_results
        else:
            print("Failed to execute Go tests.")
            return {
                "syntax": syntax,
                "syntax_output": warnings,
                "test_pass_rate": None,
                "execution_time_sec": None,
                "timeout": False,
                "line_coverage_percent": None,
                "branch_coverage_percent": None,
                "warnings": findings,
                "warnings_count": len(findings) if findings is not None else None,
                "internal_error_occurred": True,
                "runtime_errors_count": None,
                "assertions_density": assertions_density,
                "assertions_mccabe_ratio": mccabe,
            }

    finally:
        # Restore the original working directory
        os.chdir(original_cwd)
        # Clean up the temporary directory
        shutil.rmtree(None)
        print(f"Temporary directory {temp_dir} has been removed.")

x_analyze_go_tests__mutmut_mutants = {
'x_analyze_go_tests__mutmut_1': x_analyze_go_tests__mutmut_1, 
    'x_analyze_go_tests__mutmut_2': x_analyze_go_tests__mutmut_2, 
    'x_analyze_go_tests__mutmut_3': x_analyze_go_tests__mutmut_3, 
    'x_analyze_go_tests__mutmut_4': x_analyze_go_tests__mutmut_4, 
    'x_analyze_go_tests__mutmut_5': x_analyze_go_tests__mutmut_5, 
    'x_analyze_go_tests__mutmut_6': x_analyze_go_tests__mutmut_6, 
    'x_analyze_go_tests__mutmut_7': x_analyze_go_tests__mutmut_7, 
    'x_analyze_go_tests__mutmut_8': x_analyze_go_tests__mutmut_8, 
    'x_analyze_go_tests__mutmut_9': x_analyze_go_tests__mutmut_9, 
    'x_analyze_go_tests__mutmut_10': x_analyze_go_tests__mutmut_10, 
    'x_analyze_go_tests__mutmut_11': x_analyze_go_tests__mutmut_11, 
    'x_analyze_go_tests__mutmut_12': x_analyze_go_tests__mutmut_12, 
    'x_analyze_go_tests__mutmut_13': x_analyze_go_tests__mutmut_13, 
    'x_analyze_go_tests__mutmut_14': x_analyze_go_tests__mutmut_14, 
    'x_analyze_go_tests__mutmut_15': x_analyze_go_tests__mutmut_15, 
    'x_analyze_go_tests__mutmut_16': x_analyze_go_tests__mutmut_16, 
    'x_analyze_go_tests__mutmut_17': x_analyze_go_tests__mutmut_17, 
    'x_analyze_go_tests__mutmut_18': x_analyze_go_tests__mutmut_18, 
    'x_analyze_go_tests__mutmut_19': x_analyze_go_tests__mutmut_19, 
    'x_analyze_go_tests__mutmut_20': x_analyze_go_tests__mutmut_20, 
    'x_analyze_go_tests__mutmut_21': x_analyze_go_tests__mutmut_21, 
    'x_analyze_go_tests__mutmut_22': x_analyze_go_tests__mutmut_22, 
    'x_analyze_go_tests__mutmut_23': x_analyze_go_tests__mutmut_23, 
    'x_analyze_go_tests__mutmut_24': x_analyze_go_tests__mutmut_24, 
    'x_analyze_go_tests__mutmut_25': x_analyze_go_tests__mutmut_25, 
    'x_analyze_go_tests__mutmut_26': x_analyze_go_tests__mutmut_26, 
    'x_analyze_go_tests__mutmut_27': x_analyze_go_tests__mutmut_27, 
    'x_analyze_go_tests__mutmut_28': x_analyze_go_tests__mutmut_28, 
    'x_analyze_go_tests__mutmut_29': x_analyze_go_tests__mutmut_29, 
    'x_analyze_go_tests__mutmut_30': x_analyze_go_tests__mutmut_30, 
    'x_analyze_go_tests__mutmut_31': x_analyze_go_tests__mutmut_31, 
    'x_analyze_go_tests__mutmut_32': x_analyze_go_tests__mutmut_32, 
    'x_analyze_go_tests__mutmut_33': x_analyze_go_tests__mutmut_33, 
    'x_analyze_go_tests__mutmut_34': x_analyze_go_tests__mutmut_34, 
    'x_analyze_go_tests__mutmut_35': x_analyze_go_tests__mutmut_35, 
    'x_analyze_go_tests__mutmut_36': x_analyze_go_tests__mutmut_36, 
    'x_analyze_go_tests__mutmut_37': x_analyze_go_tests__mutmut_37, 
    'x_analyze_go_tests__mutmut_38': x_analyze_go_tests__mutmut_38, 
    'x_analyze_go_tests__mutmut_39': x_analyze_go_tests__mutmut_39, 
    'x_analyze_go_tests__mutmut_40': x_analyze_go_tests__mutmut_40, 
    'x_analyze_go_tests__mutmut_41': x_analyze_go_tests__mutmut_41, 
    'x_analyze_go_tests__mutmut_42': x_analyze_go_tests__mutmut_42, 
    'x_analyze_go_tests__mutmut_43': x_analyze_go_tests__mutmut_43, 
    'x_analyze_go_tests__mutmut_44': x_analyze_go_tests__mutmut_44, 
    'x_analyze_go_tests__mutmut_45': x_analyze_go_tests__mutmut_45, 
    'x_analyze_go_tests__mutmut_46': x_analyze_go_tests__mutmut_46, 
    'x_analyze_go_tests__mutmut_47': x_analyze_go_tests__mutmut_47, 
    'x_analyze_go_tests__mutmut_48': x_analyze_go_tests__mutmut_48, 
    'x_analyze_go_tests__mutmut_49': x_analyze_go_tests__mutmut_49, 
    'x_analyze_go_tests__mutmut_50': x_analyze_go_tests__mutmut_50, 
    'x_analyze_go_tests__mutmut_51': x_analyze_go_tests__mutmut_51, 
    'x_analyze_go_tests__mutmut_52': x_analyze_go_tests__mutmut_52, 
    'x_analyze_go_tests__mutmut_53': x_analyze_go_tests__mutmut_53, 
    'x_analyze_go_tests__mutmut_54': x_analyze_go_tests__mutmut_54, 
    'x_analyze_go_tests__mutmut_55': x_analyze_go_tests__mutmut_55, 
    'x_analyze_go_tests__mutmut_56': x_analyze_go_tests__mutmut_56, 
    'x_analyze_go_tests__mutmut_57': x_analyze_go_tests__mutmut_57, 
    'x_analyze_go_tests__mutmut_58': x_analyze_go_tests__mutmut_58, 
    'x_analyze_go_tests__mutmut_59': x_analyze_go_tests__mutmut_59, 
    'x_analyze_go_tests__mutmut_60': x_analyze_go_tests__mutmut_60, 
    'x_analyze_go_tests__mutmut_61': x_analyze_go_tests__mutmut_61, 
    'x_analyze_go_tests__mutmut_62': x_analyze_go_tests__mutmut_62, 
    'x_analyze_go_tests__mutmut_63': x_analyze_go_tests__mutmut_63, 
    'x_analyze_go_tests__mutmut_64': x_analyze_go_tests__mutmut_64, 
    'x_analyze_go_tests__mutmut_65': x_analyze_go_tests__mutmut_65, 
    'x_analyze_go_tests__mutmut_66': x_analyze_go_tests__mutmut_66, 
    'x_analyze_go_tests__mutmut_67': x_analyze_go_tests__mutmut_67, 
    'x_analyze_go_tests__mutmut_68': x_analyze_go_tests__mutmut_68, 
    'x_analyze_go_tests__mutmut_69': x_analyze_go_tests__mutmut_69, 
    'x_analyze_go_tests__mutmut_70': x_analyze_go_tests__mutmut_70, 
    'x_analyze_go_tests__mutmut_71': x_analyze_go_tests__mutmut_71, 
    'x_analyze_go_tests__mutmut_72': x_analyze_go_tests__mutmut_72, 
    'x_analyze_go_tests__mutmut_73': x_analyze_go_tests__mutmut_73, 
    'x_analyze_go_tests__mutmut_74': x_analyze_go_tests__mutmut_74, 
    'x_analyze_go_tests__mutmut_75': x_analyze_go_tests__mutmut_75, 
    'x_analyze_go_tests__mutmut_76': x_analyze_go_tests__mutmut_76, 
    'x_analyze_go_tests__mutmut_77': x_analyze_go_tests__mutmut_77, 
    'x_analyze_go_tests__mutmut_78': x_analyze_go_tests__mutmut_78, 
    'x_analyze_go_tests__mutmut_79': x_analyze_go_tests__mutmut_79, 
    'x_analyze_go_tests__mutmut_80': x_analyze_go_tests__mutmut_80, 
    'x_analyze_go_tests__mutmut_81': x_analyze_go_tests__mutmut_81, 
    'x_analyze_go_tests__mutmut_82': x_analyze_go_tests__mutmut_82, 
    'x_analyze_go_tests__mutmut_83': x_analyze_go_tests__mutmut_83, 
    'x_analyze_go_tests__mutmut_84': x_analyze_go_tests__mutmut_84, 
    'x_analyze_go_tests__mutmut_85': x_analyze_go_tests__mutmut_85, 
    'x_analyze_go_tests__mutmut_86': x_analyze_go_tests__mutmut_86, 
    'x_analyze_go_tests__mutmut_87': x_analyze_go_tests__mutmut_87, 
    'x_analyze_go_tests__mutmut_88': x_analyze_go_tests__mutmut_88, 
    'x_analyze_go_tests__mutmut_89': x_analyze_go_tests__mutmut_89, 
    'x_analyze_go_tests__mutmut_90': x_analyze_go_tests__mutmut_90, 
    'x_analyze_go_tests__mutmut_91': x_analyze_go_tests__mutmut_91, 
    'x_analyze_go_tests__mutmut_92': x_analyze_go_tests__mutmut_92, 
    'x_analyze_go_tests__mutmut_93': x_analyze_go_tests__mutmut_93, 
    'x_analyze_go_tests__mutmut_94': x_analyze_go_tests__mutmut_94, 
    'x_analyze_go_tests__mutmut_95': x_analyze_go_tests__mutmut_95, 
    'x_analyze_go_tests__mutmut_96': x_analyze_go_tests__mutmut_96, 
    'x_analyze_go_tests__mutmut_97': x_analyze_go_tests__mutmut_97, 
    'x_analyze_go_tests__mutmut_98': x_analyze_go_tests__mutmut_98, 
    'x_analyze_go_tests__mutmut_99': x_analyze_go_tests__mutmut_99, 
    'x_analyze_go_tests__mutmut_100': x_analyze_go_tests__mutmut_100, 
    'x_analyze_go_tests__mutmut_101': x_analyze_go_tests__mutmut_101, 
    'x_analyze_go_tests__mutmut_102': x_analyze_go_tests__mutmut_102, 
    'x_analyze_go_tests__mutmut_103': x_analyze_go_tests__mutmut_103, 
    'x_analyze_go_tests__mutmut_104': x_analyze_go_tests__mutmut_104, 
    'x_analyze_go_tests__mutmut_105': x_analyze_go_tests__mutmut_105, 
    'x_analyze_go_tests__mutmut_106': x_analyze_go_tests__mutmut_106
}

def analyze_go_tests(*args, **kwargs):
    result = _mutmut_trampoline(x_analyze_go_tests__mutmut_orig, x_analyze_go_tests__mutmut_mutants, *args, **kwargs)
    return result 

analyze_go_tests.__signature__ = _mutmut_signature(x_analyze_go_tests__mutmut_orig)
x_analyze_go_tests__mutmut_orig.__name__ = 'x_analyze_go_tests'




def x_determine_syntax_result__mutmut_orig(build, vet):
    if build == CompileStatus.SYNTAX_ERROR or build == CompileStatus.EXCEPTION_OCCURRED:
        return build
    if vet == CompileStatus.SYNTAX_ERROR or CompileStatus.EXCEPTION_OCCURRED:
        return vet
    return CompileStatus.OK


def x_determine_syntax_result__mutmut_1(build, vet):
    if build != CompileStatus.SYNTAX_ERROR or build == CompileStatus.EXCEPTION_OCCURRED:
        return build
    if vet == CompileStatus.SYNTAX_ERROR or CompileStatus.EXCEPTION_OCCURRED:
        return vet
    return CompileStatus.OK


def x_determine_syntax_result__mutmut_2(build, vet):
    if build == CompileStatus.SYNTAX_ERROR or build != CompileStatus.EXCEPTION_OCCURRED:
        return build
    if vet == CompileStatus.SYNTAX_ERROR or CompileStatus.EXCEPTION_OCCURRED:
        return vet
    return CompileStatus.OK


def x_determine_syntax_result__mutmut_3(build, vet):
    if build == CompileStatus.SYNTAX_ERROR and build == CompileStatus.EXCEPTION_OCCURRED:
        return build
    if vet == CompileStatus.SYNTAX_ERROR or CompileStatus.EXCEPTION_OCCURRED:
        return vet
    return CompileStatus.OK


def x_determine_syntax_result__mutmut_4(build, vet):
    if build == CompileStatus.SYNTAX_ERROR or build == CompileStatus.EXCEPTION_OCCURRED:
        return build
    if vet != CompileStatus.SYNTAX_ERROR or CompileStatus.EXCEPTION_OCCURRED:
        return vet
    return CompileStatus.OK


def x_determine_syntax_result__mutmut_5(build, vet):
    if build == CompileStatus.SYNTAX_ERROR or build == CompileStatus.EXCEPTION_OCCURRED:
        return build
    if vet == CompileStatus.SYNTAX_ERROR and CompileStatus.EXCEPTION_OCCURRED:
        return vet
    return CompileStatus.OK

x_determine_syntax_result__mutmut_mutants = {
'x_determine_syntax_result__mutmut_1': x_determine_syntax_result__mutmut_1, 
    'x_determine_syntax_result__mutmut_2': x_determine_syntax_result__mutmut_2, 
    'x_determine_syntax_result__mutmut_3': x_determine_syntax_result__mutmut_3, 
    'x_determine_syntax_result__mutmut_4': x_determine_syntax_result__mutmut_4, 
    'x_determine_syntax_result__mutmut_5': x_determine_syntax_result__mutmut_5
}

def determine_syntax_result(*args, **kwargs):
    result = _mutmut_trampoline(x_determine_syntax_result__mutmut_orig, x_determine_syntax_result__mutmut_mutants, *args, **kwargs)
    return result 

determine_syntax_result.__signature__ = _mutmut_signature(x_determine_syntax_result__mutmut_orig)
x_determine_syntax_result__mutmut_orig.__name__ = 'x_determine_syntax_result'


